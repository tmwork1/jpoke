# TODO : 全テストモジュールに共通して、
# イベントを直接emitして低レベルの内部仕様を検証するのではなく、
# battle.xxx_manager.apply() のように実際の処理を呼び出して検証する。
# ターン終了処理の検証ではt.end_turn()を使う。

# TODO : 全テストモジュールに共通して、テスト内容が重複しているケースがある。
# 例えば、ねむけ(Volatile)とあくび(status_move)でカウント進行テストが重複している。
# ここではカウント進行はねむけの責務のため、test_volatile.pyでカバーすべき。
# 他にも同様の重複がある場合は整理する。

# TODO : 全テストモジュールに共通して、
# パラメータが異なるだけの同種のテストがあればパラメタライズでまとめる。
# testerの指示書にも「パラメタライズでまとめること」と明記する。
