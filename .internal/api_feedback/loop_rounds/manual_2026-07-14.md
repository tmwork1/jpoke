# examples/ TODOコメントの反映（2026-07-14、ユーザー手動コミット由来）

[← 目次に戻る](../README.md)

apiループとは別に、ユーザーがorigin/mainへ直接コミット（`8c14a8bf`, `39643351`）した
`examples/`配下9ファイルのTODOコメントを反映した。「1ファイル=1事例」の原則で
`examples/`配下全体を再点検し、`01_basics/`3→5ファイル、`02_ai/`3→4ファイル、
`03_damage_calc/`3→9ファイルに分割、`04_research/`の`janken_nash`2ファイルの番号を
入れ替えた。`04_research/03_janken_nash_cfr.py`の`hp_bucket()`はHP満タンを専用の
最上位バケットとして切り出す仕様に変更（バケット総数が`HP_BUCKETS`→`HP_BUCKETS+1`）。
`src/jpoke/__init__.py`に`Command`を再エクスポート追加。詳細は`CHANGELOG.md`
[Unreleased]を参照。

- （**次ラウンド候補**）`02_ai/01_custom_player.py`のTODOで「poke-envには"有効な行動を
  返す"フォールバック関数があったが、jpokeにも導入すべきかも」という指摘があった
  （`choose_command_poke_env_style()`の`available_moves`が空＝わるあがきのみの場合の
  分岐で、現状は`battle.get_available_commands(self)[0]`で代用している）。設計判断
  （どの引数を取るか、`get_available_commands()`との役割分担）を伴うため今回は
  examples側の対応に留め、新規API追加は見送った。将来のapiループでの検討候補。
