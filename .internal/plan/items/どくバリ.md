# どくバリ 実装計画

## スコープ
- 優先度: -
- 仕様書: `.internal/spec/items/どくバリ.md`
- 実装状態: **実装済み**

## 実装内容
- `ON_CALC_POWER_MODIFIER`（`subject_spec="attacker:self"`）でどくタイプの技威力を1.2倍（4915/4096）にする。
  - `src/jpoke/handlers/item.py` の `どくバリ_modify_power_by_type` で `_modify_power_by_type` ヘルパーを利用。
  - priority は他のタイプ強化アイテム（もくたん・じしゃく等）と同じ扱いのため、明示priority指定なし（デフォルト）。
- `fling_power=70` を設定。なげつける命中時の追加効果（相手をどく状態にする）は
  `src/jpoke/handlers/move_attack.py` の `なげつける_apply_item_effect` に集約実装済み。

## 登録
- `src/jpoke/data/item.py` の `"どくバリ"` エントリに `handlers` を登録済み。

## テスト
- `tests/test_item.py::test_タイプ強化アイテム` にパラメータとして登録済み（威力補正の確認）。
- `tests/moves_attack/test_move_na.py::test_なげつける_どくバリでどくを付与する` でなげつけるの追加効果を確認。

## 今後の対応
- 特になし。
