# のろいのおふだ 実装計画

## スコープ
- 優先度: -
- 仕様書: `.internal/spec/items/_タイプ強化アイテム.md` の「のろいのおふだ」節
- 実装状態: **実装済み**

## 実装内容
- 効果: ゴーストタイプの技の威力を1.2倍（実際の倍率は4915/4096倍）にする。
- イベント: `Event.ON_CALC_POWER_MODIFIER`（`subject_spec="attacker:self"`、`AttackContext`）
  - priority: `.internal/spec/turn.md` に `ON_CALC_POWER_MODIFIER` の記載はなし。
    他のタイプ強化アイテム（かたいいし・きせきのたね等）と同じ
    `h.ItemHandler` 登録のみでpriority指定なし（デフォルト）とし、既存実装に合わせる。
- なげつける威力: 30（`fling_power=30`）

## 実装済み内容
- `src/jpoke/data/item.py` の `のろいのおふだ` に `handlers` を登録済み。
- `src/jpoke/handlers/item.py` の `のろいのおふだ_modify_power_by_type` に本体処理を実装済み。
  - 共通処理 `_modify_power_by_type(move, value, type_="ゴースト", modifier=4915)` を利用。
- テスト: `tests/test_item.py` に実装済み。

## 今後の対応
- 特になし。
