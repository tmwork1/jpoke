# ちからのハチマキ 実装計画

## スコープ
- 優先度: -
- 仕様書: `.internal/spec/items/ちからのハチマキ.md`
- 実装状態: **実装済み**

## 仕様まとめ
- 持たせると物理技の威力が1.1倍（正確な補正率は4505/4096倍）になる。
- なげつける威力10。
- 非消費アイテム。

## 実装内容
- イベント: `Event.ON_CALC_POWER_MODIFIER`
  - `.internal/spec/turn.md` に本イベントの priority 個別記載はないため、`ものしりメガネ` など既存の同種威力補正ハンドラ（`ON_CALC_POWER_MODIFIER` / `subject_spec="attacker:self"`）に倣う。
- `src/jpoke/data/item.py` の `ちからのハチマキ` に `handlers` を登録済み（`fling_power=10`）。
- `src/jpoke/handlers/item.py` の `ちからのハチマキ_boost_physical` で `ctx.move.category == "physical"` のとき `apply_fixed_modifier(value, 4505)` を適用。

## テスト
- `tests/test_item.py`
  - `test_ちからのハチマキ_物理技1_1倍`: 物理技（たいあたり）で `power_modifier == 4505` になることを確認。
  - `test_ちからのハチマキ_特殊技には補正なし`: 特殊技（でんきショック）では補正されない（`power_modifier == 4096`）ことを確認。

## 今後の対応
- 特になし（実装・テストとも完了）。
