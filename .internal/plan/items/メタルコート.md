# メタルコート 実装計画

## スコープ
- 優先度: -
- 仕様書: `.internal/spec/items/メタルコート.md`
- 実装状態: **実装済み**

## 実装方針
- `Event.ON_CALC_POWER_MODIFIER` にハンドラを登録し、`ctx.move.type == "はがね"` の場合に威力を1.2倍(4915/4096)にする。
  - priority: 他のタイプ強化アイテム（じしゃく等）と同様、`.internal/spec/turn.md` の `ON_CALC_POWER_MODIFIER` に記載の優先度に従いデフォルト（未指定）でよい。
- `subject_spec="attacker:self"`（`AttackContext` の attacker ロールに一致）。
- 非消費・なげつける威力30として `fling_power=30` を設定する。

## 実装済み内容
- `src/jpoke/data/item.py` の `メタルコート` に `handlers`（`Event.ON_CALC_POWER_MODIFIER`）と `fling_power=30` を登録済み。
- `src/jpoke/handlers/item.py` の `メタルコート_modify_power_by_type` で `_modify_power_by_type(ctx.move, value, type_="はがね", modifier=4915)` を実行し、共通の `_modify_power_by_type` ヘルパーを利用。
- テスト: `tests/test_item.py` の `test_タイプ強化アイテム`（パラメトライズ）でカバー済み。

## 今後の対応
- 特になし。
