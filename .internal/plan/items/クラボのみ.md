# クラボのみ 実装計画

## 効果概要
まひ状態を回復する（消費）。
- まひ状態が付与された瞬間に自動消費して回復する。
- かがくへんかガス解除・ぶきよう解除などでアイテム効果が有効化された瞬間、既にまひ状態であれば回復する。
- ほおばる・おちゃかい等による強制発動でも回復する。
- 上記のいずれでも回復しなかった場合の保険として、ターン終了時点でまひが残っていれば回復する。

## 実装仕様

| 項目 | 内容 |
|------|------|
| イベント | `Event.ON_APPLY_AILMENT`（まひ付与直後・即時回復） |
| subject_spec | `"target:self"` |
| priority | 50 |
| イベント | `Event.ON_ITEM_ENABLED`（アイテム効果の有効化直後） |
| subject_spec | `"source:self"` |
| priority | 50 |
| イベント | `Event.ON_FORCE_BERRY_TRIGGER`（ほおばる等による強制発動） |
| subject_spec | `"source:self"` |
| priority | 50 |
| イベント | `Event.ON_TURN_END`（保険としてのターン終了時チェック） |
| subject_spec | `"source:self"` |
| priority | 50 |
| once | True（消費アイテム） |
| 消費条件 | 発動時に `battle.consume_item(mon)`（`_announce_and_consume_item` 経由） |

### ハンドラロジック（疑似コード）

`_cure_ailment_berry` / `_cure_ailment_berry_on_apply` という状態異常回復きのみ共通のヘルパーを利用する。

```python
def クラボのみ_cure_paralysis(battle, ctx, value):
    # ON_TURN_END / ON_ITEM_ENABLED / ON_FORCE_BERRY_TRIGGER 用
    mon = ctx.source
    if mon.ailment.name == "まひ" and battle.ailment_manager.remove(mon):
        _announce_and_consume_item(battle, mon)
    return HandlerReturn(value=value)


def クラボのみ_cure_paralysis_on_apply(battle, ctx, value):
    # ON_APPLY_AILMENT 用: まひ付与直後に治療して消費する
    mon = ctx.target
    if value == "まひ" and battle.ailment_manager.remove(mon):
        _announce_and_consume_item(battle, mon)
    return HandlerReturn(value=value)
```

## テスト観点
- まひ状態のとき、ターン終了で回復すること
- まひ以外の状態異常では発動しないこと
- 発動後にアイテムが消費されること
