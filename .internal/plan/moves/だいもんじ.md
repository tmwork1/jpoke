# だいもんじ 実装計画書

参照: .internal/spec/moves/だいもんじ.md

## 現状

* `src/jpoke/data/move.py` の `"だいもんじ"` エントリは以下の通り実装済み。
  - type="ほのお", category="special", pp=5, power=110, accuracy=85
  - flags={"secondary_effect"}
  - `Event.ON_DAMAGE_HIT`: `だいもんじ_apply_burn_to_defender`（10%でやけど付与）
* 上記のうち type/category/pp/power/accuracy/やけど確率は仕様と一致しており修正不要。
* 一方、`flags` に `"thaw"` が欠落している。だいもんじはほのおタイプの攻撃技であるため、
  被弾した相手がこおり状態の場合はこおりを解凍する必要がある
  （`.internal/spec/ailments/こおり.md` 31-32行目）。この解凍処理は `こおり_cure_by_thaw_move`
  （`src/jpoke/handlers/ailment.py`, `Event.ON_DAMAGE_HIT`, `ctx.move.has_flag("thaw")` で判定）
  が担っており、`flags` に `"thaw"` を追加するだけで有効になる（既存の `かえんほうしゃ` と同型）。
  新規ハンドラの実装は不要。

## 追加実装

### data/move.py の変更

```python
    "だいもんじ": MoveData(
        type="ほのお",
        category="special",
        pp=5,
        power=110,
        accuracy=85,
        flags={"secondary_effect", "thaw"},
        handlers={
            Event.ON_DAMAGE_HIT: h.MoveHandler(
                ha.だいもんじ_apply_burn_to_defender,
            )
        }
    ),
```

### handlers/move_attack.py

変更不要。既存の `だいもんじ_apply_burn_to_defender` をそのまま使用する。

## Priority 根拠

`Event.ON_DAMAGE_HIT` は追加効果付与（やけど付与ハンドラ）とこおり解凍判定（`こおり_cure_by_thaw_move`）
の両方が使われる標準イベントであり、他のやけど付与技（かえんほうしゃ等）と同じ priority（デフォルト）
を使用する。本技独自の priority 検討は不要。

## リーサル計算

* `.internal/progress/move.md` の「リーサル実装」「リーサルテスト」列は `n/a`。対象外。

## テスト

* `tests/moves_attack/test_move_ta.py` に、こおり状態の相手に当てると解凍することを確認する
  テストを追加する（`かえんほうしゃ` の `test_かえんほうしゃ_こおり状態の相手に当てると解凍する`
  と同じ構成）。

## 注意点・エッジケース

* 追加効果（やけど付与）の確率変動（ちからずく・てんのめぐみ・りんぷん・おんみつマント等）は
  `apply_ailment_to_defender` 内の `battle.resolve_secondary_chance` が汎用的に処理するため、
  本技側での追加実装は不要。
* だいもんじ自体は「使用者がこおり状態のとき無条件で解凍してから攻撃する」対象技には含まれない
  （かえんぐるま・かえんボール・せいなるほのお・もえつきる等の専用リストとは別）。
  そのため `Event.ON_TRY_ACTION` の thaw_attacker 系ハンドラは追加しない。
