# ほのおのうず 実装計画書

## 仕様要約（.internal/spec/moves/ほのおのうず.md 参照）

* タイプ: ほのお / 分類: 特殊 / 威力: 35 / 命中: 85 / PP: 15
* 対象: 相手1体 / 優先度: 0 / 接触なし
* 効果: 命中した相手を『バインド』状態にする（4〜5ターン、ターン終了時に最大HPの1/8ダメージ）。

## 現状の実装状況

`src/jpoke/data/moves/move_ha.py` に既に登録済みで、仕様と一致している（修正不要）。
うずしお・しめつける・すなじごくと同一パターン（`apply_bind_to_defender` を共有）。

```python
"ほのおのうず": MoveData(
    type="ほのお",
    category="special",
    pp=15,
    power=35,
    accuracy=85,
    handlers={
        Event.ON_DAMAGE_HIT: h.MoveHandler(ha.apply_bind_to_defender)
    },
    lethal_handlers={
        LethalEvent.ON_HIT: LethalHandler(l._apply_bind)
    }
),
```

## ハンドラ構成

| イベント | priority | subject_spec | 役割 |
|---|---|---|---|
| `ON_DAMAGE_HIT` | 100（デフォルト） | attacker:self（`MoveHandler` 既定値） | 相手に『バインド』状態を付与する |

`src/jpoke/handlers/move_attack.py` の実装（登録済み・修正不要、うずしお・しめつける・すなじごく等と共有）:

```python
def apply_bind_to_defender(battle: Battle, ctx: AttackContext, value: Any) -> HandlerReturn:
    """バインド系技: ランダムターン数でバインド状態を付与する。ねばりのかぎづめで7ターン固定。

    しめつけバンドによるダメージ倍率は付与時（攻撃側の所持アイテム）で確定し、
    以降のアイテム変化（入手・喪失）による増減はない。
    """
    count = battle.query.get_volatile_duration(ctx, "バインド", battle.random.randint(4, 5))
    bind_damage_ratio = battle.events.emit(Event.ON_MODIFY_BIND_DAMAGE, ctx, 1/8)
    battle.volatile_manager.apply(
        ctx.defender, "バインド", count=count, source=ctx.attacker, bind_damage_ratio=bind_damage_ratio
    )
    return HandlerReturn(value=value)
```

バインド状態そのものの毎ターンダメージ・交代禁止・解除条件は `VolatileData["バインド"]`
（`src/jpoke/data/volatile.py`）と `.internal/spec/volatiles/バインド.md` を参照する。
本技側で重複実装しない。

## Priority 根拠

ダメージを与えた技のバインド付与は既存の同種ハンドラ（うずしお・しめつける・すなじごく・
マグマストーム）と同じ `ON_DAMAGE_HIT` / priority 100（デフォルト）のパターンに従う。
個別の priority 検討は不要（`.internal/spec/turn.md` に本イベントの掲載はないため、
既存の同種ハンドラに合わせる）。

## リーサル計算

`.internal/progress/move.md` の「リーサル実装」列は `x`。既に `LethalEvent.ON_HIT` に
`l._apply_bind`（`src/jpoke/handlers/lethal.py`）が登録済みで、仕様・実装と一致している。

```python
def _apply_bind(battle: Battle, ctx: LethalContext, hp_dist: StateDist) -> StateDist:
    """バインド状態を付与する（未付与の場合のみ）。バインドダメージはON_TURN_ENDで処理される。"""
    if "バインド" not in ctx.defender.volatiles:
        from jpoke.model.volatile import Volatile
        ctx.defender.volatiles["バインド"] = Volatile("バインド", count=4)
    return hp_dist
```

ターン終了時のバインドダメージは `LethalEvent.ON_TURN_END` 側の共通ハンドラで処理されるため
（`.internal/progress/move.md` の他のバインド技と同様）、本技固有の追加実装は不要。

## 実装箇所

### handlers/move_attack.py / handlers/lethal.py

追加・修正なし（実装済み・パターンも仕様と一致）。

### data/moves/move_ha.py

追加・修正なし（実装済み・仕様と一致）。

## 注意点・エッジケース（review-test 向け）

1. **バインド系技との共通化**
   - `apply_bind_to_defender` / `_apply_bind` はうずしお・しめつける・すなじごく・ほのおのうずと
     共有する汎用ハンドラであるため、本技専用の修正を加える際は他のバインド技への影響を確認すること。
2. **テスト方針**
   - 命中後のバインド付与・毎ターンダメージ・交代禁止・攻撃者交代時の解除を確認する基本テストを
     `tests/moves_attack/test_move_ha.py` に追加する（`test_ほのおのうず_バインド中は相手が交代できない` /
     `test_ほのおのうず_バインド中は相手が毎ターン最大HPの8分の1ダメージを受ける` /
     `test_ほのおのうず_命中後にバインド状態になる` / `test_ほのおのうず_攻撃者が交代するとバインドが解除される`）。
   - リーサルテストは `tests/test_lethal.py` に `test_ほのおのうず_バインド付与` を追加する（うずしおのテストと同型）。
