# ワンダールーム 実装計画書

## 仕様参照

- `.internal/spec/fields/ワンダールーム.md`
- `.internal/wiki/global_fields/ワンダールーム_(場の状態).html`

## 実装方針

ダメージ計算（`damage.py` の `_calc_final_defense`）で:
1. 最初に `deals_physical_damage` で物理/特殊を判定し、参照する基本ステータス (`def` / `spd`) を選択する
2. `ON_CALC_DEF_RANK_MODIFIER` イベントでランク補正を計算する（ワンダールームはここに関与しない）
3. `ON_CALC_DEF_MODIFIER` イベントで実数値の比率補正を掛ける

ワンダールームは 3. のイベントハンドラでのみ防御/特防の**実数値**の参照先を入れ替える。
ランク補正は技の分類に対応する本来の参照先のまま据え置かれる（本家の既知の仕様。
実数値だけが入れ替わり、ランク補正は入れ替わらない）。

## ハンドラ一覧

### `src/jpoke/handlers/field.py`

| 関数名 | イベント | subject_spec | priority | 処理 |
|--------|----------|-------------|----------|------|
| `ワンダールーム_def_modifier` | ON_CALC_DEF_MODIFIER | `defender:self` | 100 | 実数値の比率換算（swap_value / base_value を 4096 スケールで適用） |
| `ワンダールーム_tick_global_field` | ON_TURN_END | `source:self` | 140 | カウントダウン（1P側のみ） |

### `src/jpoke/handlers/move_status.py`

| 関数名 | イベント | 処理 |
|--------|----------|------|
| `ワンダールーム_activate_global_field` | ON_STATUS_HIT | アクティブなら即時解除、非アクティブなら5ターンで発動 |

### `src/jpoke/data/field/global_field.py`

`GLOBAL_FIELD["ワンダールーム"]` に上記ハンドラを登録する。

### `src/jpoke/data/move.py`

`"ワンダールーム"` エントリに `ON_STATUS_HIT` ハンドラを登録する。

## ハンドラ実装詳細

### `ワンダールーム_def_modifier`

```python
def ワンダールーム_def_modifier(battle, ctx, value):
    base_stat = "def" if battle.query.deals_physical_damage(ctx.attacker, ctx.move) else "spd"
    swapped_stat = "spd" if base_stat == "def" else "def"
    base_value = max(1, ctx.defender.stats[base_stat])
    swap_value = max(1, ctx.defender.stats[swapped_stat])
    return HandlerReturn(value=value * swap_value // base_value)
```

- `value` は 4096 ベースの修正値（初期値 4096）
- `swap_value / base_value` の比率を整数演算で適用することで、最終的に swap_stat の実数値が参照される

### `ワンダールーム_tick_global_field`

```python
def ワンダールーム_tick_global_field(battle, ctx, value):
    return tick_global_field(battle, ctx, value, name="ワンダールーム")
```

### `ワンダールーム_activate_global_field`（move_status.py）

```python
def ワンダールーム_activate_global_field(battle, ctx, value):
    manager = battle.global_manager
    if manager.fields["ワンダールーム"].is_active:
        return HandlerReturn(value=manager.deactivate("ワンダールーム"))
    return HandlerReturn(value=manager.activate("ワンダールーム", 5))
```

## テスト項目

| テスト名 | 確認内容 |
|---------|---------|
| `test_ワンダールーム_物理技は特防実数値を使用` | ランク=0で物理技の防御計算が特防実数値になる |
| `test_ワンダールーム_物理技のランク補正はぼうぎょランクのまま` | ぼうぎょランクを変えると結果が変わり、とくぼうランクを変えても結果は変わらない（ランクは入れ替わらない） |
| `test_ワンダールーム_特殊技は防御実数値を使用` | ランク=0で特殊技の防御計算が防御実数値になる |
| `test_ワンダールーム_特殊技のランク補正はとくぼうランクのまま` | とくぼうランクを変えると結果が変わり、ぼうぎょランクを変えても結果は変わらない（ランクは入れ替わらない） |
| `test_サイコショック_ワンダールーム下ではとくぼう実数値とぼうぎょランクを参照する`（tests/moves_attack/test_move_sa.py） | physical_damageフラグを持つ特殊技（サイコショック等）が物理技と同様の実数値差し替えになることを確認 |
| `test_全体フィールドカウント減少（ワンダールーム）` | 2ターン設定でターン終了ごとにカウントが減り0で非活性化 |
| `test_ワンダールーム_再使用で解除` | 発動中に再使用するとフィールドが解除される |
