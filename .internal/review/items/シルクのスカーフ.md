# シルクのスカーフ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/items/シルクのスカーフ.md`）。一次情報（ポケモンWiki）に基づき、ノーマルタイプ技威力1.2倍・なげつける威力10を記載。既存の同種アイテム（じしゃく等）の仕様書フォーマットに合わせた。

## 実装計画書
- 既存の計画書は仕様書が未整備であることを前提にした記述だったため、仕様書作成に合わせて更新。
  - 仕様書参照先を `.internal/spec/items/シルクのスカーフ.md` に修正。
  - 実装方針（`ON_CALC_POWER_MODIFIER` ハンドラ、priority、subject_spec、fling_power）を明記。
  - テストが既にカバー済みであることを反映（従来の計画書は「テスト未実装」と誤記していた）。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/item.py` の `シルクのスカーフ_modify_power_by_type` は共通ヘルパー `_modify_power_by_type` を用いてノーマルタイプ技の威力を4915/4096倍にしており、仕様書・他のタイプ強化アイテムと一致。`src/jpoke/data/item.py` の `fling_power=10` も一次情報と一致。`subject_spec="attacker:self"` も `AttackContext` のロールと整合。

## テスト
- 変更なし。`tests/test_item.py` の `test_タイプ強化アイテム`（パラメトライズ）で `("シルクのスカーフ", "ノーマル")` ケースとして既にカバー済み。`python -m pytest tests/ -v` で3140件パス・1件skipを確認。
