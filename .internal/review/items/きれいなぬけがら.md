# きれいなぬけがら レビュー結果

## 仕様書
- 変更なし。一次情報（`.internal/wiki/items/きれいなぬけがら.html`）と照合し、効果・詳細な仕様・備考・関連項目に誤り・欠落がないことを確認した。

## 実装計画書
- 内容の薄い旧計画書を、CLAUDE.md のハンドラ約束事に沿った形式（テンプレート準拠）に全面的に書き直した。
  - `Event.ON_CHECK_TRAPPED` は `.internal/spec/turn.md` に記載のない内部イベントであるため、既存の同種ハンドラ
    （`かげふみ`/`じりょく`/`ありじごく`/`フェアリーロック`/`にげられない`/`バインド`/`ねをはる`、いずれも
    priority=100 のデフォルト値）を調査し、それらより確実に先に実行され `stop_event=True` で判定を打ち切れる
    `priority=-100` を採用している根拠を明記した。
  - 実装ゴール・テスト観点・リスク（本プロジェクトはトレーナー戦のみが対象のため「にげる」判定は考慮不要）を追加した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/item.py` の `きれいなぬけがら_check_trapped`（`ON_CHECK_TRAPPED` で
  `value=False, stop_event=True` を返す）と、`src/jpoke/data/item.py` の登録（`fling_power=10`,
  `subject_spec="source:self"`, `priority=-100`）を仕様書・計画書と照合し、誤り・欠落がないことを確認した。

## テスト
- 相手の特性（かげふみ）による交代禁止を無視できることを検証する既存テストに加え、
  仕様書に明記されている「自身のにげられない状態（くろいまなざし等）でも交代できる」ケースを検証する
  `test_きれいなぬけがら_自身のにげられない状態でも交代できる` を追加した。
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` で
  `.internal/tests/` を更新し、`python -m pytest tests/ -v` を実行して全2768件（1件skip）が通ることを確認した
  （1回目の実行で `test_オーロラベール_ひかりのかべとは重複しない` が急所判定の乱数依存で偶発的に失敗したが、
  再実行で成功し本アイテムと無関係であることを確認済み）。
