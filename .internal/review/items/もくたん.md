# もくたん レビュー結果

## 仕様書
- 新規作成（`.internal/spec/items/もくたん.md`）。一次情報（`.internal/wiki/items/もくたん.html`）を基に、第四世代以降の効果（ほのおタイプの技の威力1.2倍）となげつける威力30を記載。`じしゃく`の仕様書を参考に同一フォーマットで作成した。

## 実装計画書
- 既存の `.internal/plan/items/もくたん.md` は「仕様書未整備」「テスト未実装」と誤った記載になっていたため、`じしゃく` の計画書と同一フォーマットで実装方針・実装済み内容・テスト状況を正しく記載する形に更新した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/item.py` の `もくたん`（`fling_power=30`, `Event.ON_CALC_POWER_MODIFIER` ハンドラ）と `src/jpoke/handlers/item.py` の `もくたん_modify_power_by_type`（`_modify_power_by_type(ctx.move, value, type_="ほのお", modifier=4915)`）は仕様書・計画書と一致しており修正不要と判断した。

## テスト
- 変更なし。`tests/test_item.py` の `test_タイプ強化アイテム`（パラメトライズテスト、`("もくたん", "ほのお")` を含む）で威力補正（`power_modifier == 4915`）を検証済み。過不足なし。

## 進捗表
- `.internal/progress/item.md` の `もくたん` 行のレビュー列を `-` から `x` に、効果列を `?` から具体的な説明文に更新した。
