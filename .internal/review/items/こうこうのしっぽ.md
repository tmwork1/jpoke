# こうこうのしっぽ レビュー結果

## 仕様書
- 変更なし（`.internal/spec/items/こうこうのしっぽ.md` は一次情報 `.internal/wiki/items/こうこうのしっぽ.html` の内容と一致していることを確認した）。

## 実装計画書
- `.internal/plan/items/こうこうのしっぽ.md` のテスト観点に、特性クイックドロウとの相互作用（道具の効果が無視され先攻になる）を追記した。

## 実装（handlers/ / data/）
- バグを発見し修正した: `handlers/ability.py` の `クイックドロウ_maybe_fast_attack` が、こうこうのしっぽ所持時にクイックドロウが発動しても
  「アイテムの -1」と「クイックドロウの +1」が相殺されて正味 0（通常行動順）になってしまい、
  仕様（`こうこうのしっぽを持ったポケモンの特性クイックドロウが発動した場合、道具の効果は無視されて行動順が早くなる`）どおりの
  「正味 +1（先攻ティア）」にならない不具合があった。
  - 修正: 攻撃者が `こうこうのしっぽ` を所持している場合は `+1` ではなく `+2` を加算するようにし、
    ハンドラの実行順序に依存せず正味 +1（道具の -1 を打ち消して先攻ティア）になるようにした。
  - `mon.item.name` はアイテムが無効化されている場合（かがくへんかガス・アンコール外・クレーンなど）に空文字を返すため、
    アイテムが実際に機能していない状況では誤補正されない。
  - `handlers/item.py` の `こうこうのしっぽ_back_tier`（`ON_CALC_BACK_TIER` で `value - 1` を返す）自体は仕様どおりで変更なし。
  - `data/item.py` の `こうこうのしっぽ` 登録（`fling_power=10`, `subject_spec="attacker:self"`）も仕様どおりで変更なし。
  - 併せて `.internal/plan/abilities/クイックドロウ.md` のテスト観点にもこの相互作用を追記した（実装ファイルが `handlers/ability.py` であるため）。

## テスト
- `tests/test_item.py` に `test_こうこうのしっぽ_クイックドロウ発動時は道具の効果が無視される` を追加し、
  クイックドロウ発動時にこうこうのしっぽ所持でも先攻になることを確認した。
- 既存の `test_こうこうのしっぽ_行動が後になる`（通常時は後攻になること）は変更なし。
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` で一覧更新、
  `python -m pytest tests/ -v` で全 2839 件 pass（1 skip は既存の無関係なスキップ）を確認した。ログは
  `.loop/test_logs/こうこうのしっぽ.log` に保存した。

## 副次的な修正（進捗表の記述誤り）
- `.internal/progress/item.md` の `こうこうのしっぽ` 行: テスト列・レビュー列が `-` のままだったため `x` に更新し、効果列の `?` を実際の効果説明に更新した。
- 作業中に `イバンのみ` 行も実装・テストが完了しているにもかかわらず実装/テスト/レビュー列が `-`・効果列が `?` のままだったことに気づいたため、あわせて修正した。
