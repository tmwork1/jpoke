# しらたま レビュー結果

## 仕様書
- 変更なし（一次情報と一致していることを確認）

## 実装計画書
- 実装状態を「未実装」→「実装済み」に更新
- `ItemData` の実フィールドに合わせて疑似コードを修正（存在しない `consumable` フィールドの記述を削除し、`_dedicated_item_modify_power` 共通ヘルパーの利用を明記）
- なげつける威力60・通常アイテムとして奪う/奪われる旨を明記
- テスト観点に「なげつけるで威力60になる」を追加

## 実装（handlers/ / data/）
- `src/jpoke/data/item.py` の `しらたま` エントリに誤り: `removable=False, fling_power=0` となっていたが、一次情報（なげつける威力60・通常の持ち物）と不一致だったため `fling_power=60` に修正し、`removable=False` を削除（デフォルトの `removable=True` に戻した）。
  - 比較対象の `こんごうだま`（同じ「専用アイテム」系統）は `fling_power=60` かつ `removable` 未指定（デフォルト True）であり、しらたまだけ `だいしらたま`（オリジンフォルム固定用の別アイテム、奪えない・なげつけるが失敗する仕様）と同じ設定が誤って使われていた。
- `handlers/item.py` の `しらたま_modify_power` 自体（`_dedicated_item_modify_power` を使ったパルキア／パルキア(オリジン)・ドラゴン/みずタイプ判定）は仕様通りで変更なし。
- `scripts/sort_data/sort_items.py` を実行し、`ITEMS` 辞書の並びを確認（順序に変化なし）。

## テスト
- `tests/test_item.py` に以下を追加:
  - `test_しらたま_パルキア以外が持っても効果がない`
  - `test_しらたま_対象外タイプの技には効果がない`
  - `test_しらたま_なげつけるで威力60になる`（`fling_power` 修正の回帰テストも兼ねる）
- 既存の共通パラメタライズテスト（`test_専用アイテム補正`）でドラゴン技・みず技の1.2倍補正は確認済みのため変更なし。
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` で一覧更新。
- `python -m pytest tests/ -v` を実行し、2874 passed, 1 skipped で全件パス（`test_いかりのまえば_相手HP半分のダメージ` は初回実行時のみ乱数依存でflaky失敗したが、しらたまとは無関係。再実行で成功を確認）。
- ログ: `.loop/test_logs/しらたま.log`
