# かえんだま レビュー結果

## 仕様書
- 変更なし。`.internal/spec/items/かえんだま.md` は一次情報（`.internal/wiki/items/かえんだま.html`）と照合済みで、効果・発動条件・タイミングの詳細仕様（やけどダメージ/ねむけ/場の状態/フィールド/はねやすめ処理後に発動する点、みずのベール等の免疫、ミストフィールドでの無効化、ものひろいで拾ったターンは発動しない、等）はすべて正しく記載されている。

## 実装計画書
- 修正した内容:
  - `priority` の記載を `90` → `150` に修正（`.internal/spec/turn.md` の `Event.ON_TURN_END` 表645行目に「150 | かえんだま」と明記されているのに、計画書には誤って90と記載されていた）。
  - 90という値のままだと、やけどダメージ(100)・ねむけ(110)・はねやすめのひこうタイプ復帰(120)・場の状態やフィールドの継続終了(130-140)より**先に**かえんだまが発動してしまい、以下の仕様に反することになるため根拠を追記した:
    - 「発動するターンにやけどダメージは受けない」
    - 「ねむけからねむり状態になるターンと重なった場合、ねむり状態が優先される」
    - 「ミストフィールドが解除されたターンからかえんだまが発動する」

## 実装（handlers/ / data/）
- 修正した内容:
  - `src/jpoke/data/item.py` の `"かえんだま"` エントリで `Event.ON_TURN_END` の `priority` を `90` から `150` に修正（`.internal/spec/turn.md` に準拠）。
  - `handlers/item.py` の `かえんだま_apply_burn`（`_apply_ailment_from_item` 経由でやけどを付与）はロジック自体は正しく、変更なし。
- 補足: `src/jpoke/core/ailment_manager.py` の `apply()` がタイプ免疫（ほのお）・`ON_BEFORE_APPLY_AILMENT` 経由の特性/フィールド無効化・重複状態異常防止を共通処理として実装済みであることを確認。かえんだま固有の重複実装は不要と判断。
- 注記: `"どくどくだま"` も同じ `priority=90` の誤りを持っているが、本タスクのスコープ外（並行レビュー作業の対象の可能性があるため）のため今回は変更していない。

## テスト
- 追加したテスト（`tests/test_item.py`）:
  - `test_かえんだま_発動するターンにやけどダメージは受けない` — 発動したターンはやけどダメージが発生せず、翌ターンから発生することを確認。
  - `test_かえんだま_ねむけと重なった場合ねむり優先` — ねむけからねむりになるターンと重なった場合、ねむり状態が優先されやけどが付与されないことを確認。
  - `test_かえんだま_ミストフィールド解除ターンから発動` — ミストフィールドが解除されたターンからかえんだまが発動する（優先度150がフィールド終了処理140より後であることの検証）ことを確認。
  - いずれも priority=90 → 150 の修正がないと失敗する（回帰防止テスト）。
- 既存の `test_状態異常だま_ターン終了で状態異常付与` は変更なし（そのまま通過）。
- `python scripts/sort_tests.py tests/test_item.py` と `python scripts/generate_test_list.py` を実行済み。
- `python -m pytest tests/ -v` は 2731 passed, 1 skipped で全テスト成功（急所依存の無関係な一時的失敗が2件あったが、個別再実行で無関係と確認済み）。
