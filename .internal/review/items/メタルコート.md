# メタルコート レビュー結果

## 仕様書
- 新規作成 (`.internal/spec/items/メタルコート.md`)。一次情報（`.internal/wiki/items/メタルコート.html`）に基づき、はがねタイプ技の威力1.2倍・なげつける威力30を記載。既存の同種タイプ強化アイテム（`じしゃく`）の仕様書フォーマットに準拠。

## 実装計画書
- 修正した。既存の `.internal/plan/items/メタルコート.md` は「仕様書未作成」「テスト未実装」という古い記述のままだったため、`じしゃく`/`もくたん` の計画書フォーマットに合わせて実装方針・実装済み内容を明記する形に更新。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/item.py` の `メタルコート`（`fling_power=30`、`Event.ON_CALC_POWER_MODIFIER` ハンドラ登録）と `src/jpoke/handlers/item.py` の `メタルコート_modify_power_by_type`（`_modify_power_by_type(ctx.move, value, type_="はがね", modifier=4915)`）は仕様書・他のタイプ強化アイテムと一致しており、修正不要と判断。

## テスト
- 変更なし。`tests/test_item.py` の `test_タイプ強化アイテム`（パラメトライズ）に `("メタルコート", "はがね")` が既に含まれており、他のタイプ強化アイテムと同様に威力補正(4915/4096倍)を検証済み。

## テスト結果
- `python -m pytest tests/ -v` で 3142 passed, 1 skipped（既存のスキップで本アイテムと無関係）を確認。ログは `.loop/test_logs/metalcoat.log` に保存。
