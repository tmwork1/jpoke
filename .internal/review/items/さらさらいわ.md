# さらさらいわ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/items/さらさらいわ.md`）。一次情報（`.internal/wiki/items/さらさらいわ.html`）をもとに、効果・詳細な仕様・備考・関連項目を記載した。
  - 効果: すなあらしにするわざ、または特性すなおこし・すなはき（第六世代以降）によるすなあらしの持続ターンを8ターンに延長する。なげつける威力10。ウッウロボ素材タイプいわ・ポイント6。

## 実装計画書
- 既存の `.internal/plan/items/さらさらいわ.md` を、他の天候延長系アイテム（しめったいわ等）の計画書フォーマットに合わせて再整備した。
  - イベント: `Event.ON_MODIFY_DURATION` / `subject_spec: source:self` / priority 100 / 非消費
  - `ON_MODIFY_DURATION` は `field_manager.py` の天候発動処理内で技・特性（すなおこし/すなはき）を問わず共通して発火されるため、両方のトリガーで自動的に延長されることを明記した。

## 実装（handlers/ / data/）
- 変更なし。既存実装（`さらさらいわ_resolve_field_count` in `handlers/item.py` + `data/item.py` の `ON_MODIFY_DURATION` 登録）は仕様と一致していることを確認した。
  - `_resolve_field_count(value, "すなあらし", additonal_count=3)` により、すなあらし時のみ持続ターンを5→8に延長。他の天候には影響しない。
  - `weather_manager.apply()` は技（すなあらし技）・特性（すなおこし/すなはき）のどちらから呼ばれても同一の `ON_MODIFY_DURATION` イベントを通るため、両方のケースを正しくカバーしている。

## テスト
- 変更なし。`tests/test_item.py` の `test_天候延長アイテム`（パラメータ化テスト、さらさらいわ含む）が既に存在し、すなあらしの持続ターンが8になることを検証済み。
- `python -m pytest tests/ -v` で全2852件（+ skip 1件）がパスすることを確認した。
