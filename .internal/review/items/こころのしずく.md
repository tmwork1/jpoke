# こころのしずく レビュー結果

## 仕様書
- 変更なし（一次情報と照合し、内容に誤り・欠落なしを確認）

## 実装計画書
- 変更なし（仕様書と整合しており、`ON_CALC_POWER_MODIFIER` での実装方針・疑似コードに問題なし）

## 実装（handlers/ / data/）
- 変更なし
  - `src/jpoke/handlers/item.py` の `こころのしずく_modify_power` は `_dedicated_item_modify_power` を用いて
    ラティオス・ラティアス限定でエスパー/ドラゴン技の威力を 4915/4096 倍にしており、他の専用アイテム
    （こんごうだま・しらたま・はっきんだま等）と同一のパターンで実装されている。
  - `ON_MODIFY_MOVE_TYPE`（タイプ変換）は `ON_CALC_POWER_MODIFIER` より前に発火するため、`ctx.move.type`
    はタイプ変化後のタイプを参照でき、仕様書の「タイプ変化後のタイプで判定する」要件を満たしている。
  - `src/jpoke/data/item.py` の登録内容（`subject_spec="attacker:self"`、`fling_power=30`）も仕様書と一致。

## テスト
- 変更なし
  - `tests/test_item.py::test_専用アイテム補正` にラティオス×サイコキネシス、ラティアス×ドラゴンクロー
    のケースが既に含まれており、期待倍率 4915 で検証されている。他の専用アイテム群と同一の
    パラメトリックテストで管理されており、テスト内容・命名・ソート順に問題なし。

## 備考
- `python -m pytest tests/ -v` は 2839 passed, 1 skipped で全件成功（ログ: `.loop/test_logs/こころのしずく.log`）。
- `.internal/progress/item.md` のこころのしずく行は レビュー列が `-` のままだったため `x` に更新し、
  効果列の説明（`?`）を実装内容に基づいた具体的な説明文に更新した。
