# とつげきチョッキ レビュー結果

## 仕様書
- 変更なし。`.internal/spec/items/とつげきチョッキ.md` は一次情報（ポケモンWiki）と照合し、効果・詳細仕様（変化技コマンド除外の例外条件、ワンダールームでの補正対象変更、なげつける威力等）に誤り・欠落はなかった。

## 実装計画書
- 変更なし。`.internal/plan/items/とつげきチョッキ.md` は `Event.ON_MODIFY_COMMAND_OPTIONS`（コマンド選択肢からの変化技除外）と `Event.ON_CALC_DEF_MODIFIER`（特殊技に対するとくぼう1.5倍）の2イベント構成で、`subject_spec`・priority の根拠（turn.md 未掲載イベントのため既存の同種ハンドラに合わせる）も適切に記載されていた。`Event.ON_TRY_MOVE_1` で実行時ブロックを設けない理由（アンコール・さいはい・おどりこ・ぶきよう等での変化技実行を妨げないため）も仕様書の詳細仕様と整合していた。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/item.py` の `とつげきチョッキ_boost_spdef`（`ctx.move.category == "special"` のときのみ `apply_fixed_modifier(value, 6144)`）と `とつげきチョッキ_modify_command_options`（`cmd.is_type("move") and mon.moves[cmd.index].category == "status"` を除外）は計画書どおりに実装されていた。前回レビュー（`Event.ON_TRY_MOVE_1` での一律ブロックを削除し、コマンド除外方式に整理）の内容が維持されている。
- `Event.ON_CALC_DEF_MODIFIER` は比率（4096基準）で最終防御実数値に乗算するイベントであり、ワンダールームによる実数値スワップ（`ワンダールーム_def_modifier`）と独立した乗数として合成されるため、`ctx.move.category` のみを条件にする現行実装のままで「ワンダールーム下ではとくぼうの代わりにぼうぎょが1.5倍になる」という仕様も正しく再現できることを確認した（追加の分岐は不要）。
- `data/item.py` の `"とつげきチョッキ"` エントリ（`fling_power=80`、2ハンドラの `subject_spec`）も仕様・計画と一致していた。

## リーサル計算
- 対象外（`.internal/progress/item.md` のリーサル実装列は `n/a`）。とくぼう補正はダメージ計算の `ON_CALC_DEF_MODIFIER` を通常経路で再利用するため、専用のリーサルハンドラは不要（`しんかのきせき` 等の同種の防御補正アイテムと同じ扱い）。

## テスト
- 変更なし。`tests/items/test_item_ta.py` に以下5件が既に実装され、全てパスすることを確認した。
  - `test_とつげきチョッキ_物理技には防御補正なし`
  - `test_とつげきチョッキ_特殊技にとくぼう1_5倍`
  - `test_とつげきチョッキ_変化技はコマンド選択肢から除外される`
  - `test_とつげきチョッキ_変化技しか持たない場合わるあがきのみ選択可能`
  - `test_とつげきチョッキ_アンコールで変化技を強制されたターンは使用できる`
- `python -m pytest tests/ -v` で 5777 passed, 1 skipped（既存のskip、本アイテムと無関係）を確認。ログは `.loop/test_logs/とつげきチョッキ.log` に保存済み。

## 進捗表の反映
- `.internal/progress/item.md` の「とつげきチョッキ」行は、実装・テストが実態としてすでに完了していたにもかかわらず「テスト=-」「レビュー=-」「効果=?」のまま更新漏れになっていた。今回の再レビューで実態を確認したうえで、テスト列を `x`、レビュー列を `x`、効果列を実装内容に沿った説明文に更新した。
