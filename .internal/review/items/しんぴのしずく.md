# しんぴのしずく レビュー結果

## 仕様書
- 新規作成（`.internal/spec/items/しんぴのしずく.md`）。一次情報（ポケモンWiki）をもとに、みずタイプ技の威力1.2倍（第四世代以降）・なげつける威力30・タイプが変わる技への適用条件を記載。

## 実装計画書
- 既存の計画書は仕様書未整備を前提とした暫定的な内容だったため、`くろおび.md` 等の標準フォーマットに合わせて全面的に書き直した。実装方針（`ON_CALC_POWER_MODIFIER` ハンドラ、`subject_spec="attacker:self"`、`fling_power=30`）と実装済み内容・今後の対応を明記。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/item.py` の `しんぴのしずく` エントリと `src/jpoke/handlers/item.py` の `しんぴのしずく_modify_power_by_type` は、共通ヘルパー `_modify_power_by_type` を用いた標準的なタイプ強化アイテム実装として仕様書・計画書と一致していることを確認した。

## テスト
- 変更なし。`tests/test_item.py` の `test_タイプ強化アイテム`（パラメトライズ）に `("しんぴのしずく", "みず")` が既に含まれており、威力補正（4915/4096倍）を検証できている。

## その他
- `.internal/progress/item.md` のしんぴのしずく行の レビュー列を `-` → `x` に、効果列を `?` から実際の効果説明に更新した。
