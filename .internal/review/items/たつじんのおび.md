# たつじんのおび レビュー結果

## 仕様書
- 変更なし。`.internal/spec/items/たつじんのおび.md` は一次情報（ポケモンWiki）と照合し、効果（効果抜群のダメージ1.2倍・なげつける威力10）、詳細な仕様（きのみによる抜群半減後も相性判定は変わらず発動する／らんきりゅうで等倍化した場合は発動しない／フリーズドライ・タールショットによる抜群化でも発動する）に誤り・欠落なし。

## 実装計画書
- 変更なし。`.internal/plan/items/たつじんのおび.md` はイベント `Event.ON_CALC_DAMAGE_MODIFIER`、`subject_spec="attacker:self"`、priority 100（デフォルト値と一致）、非消費という内容で仕様書と整合している。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/item.py` の `たつじんのおび_boost_super_effective` は `battle.query.is_super_effective(ctx)` を用いて効果抜群判定を行い、`apply_fixed_modifier(value, 4915)` で1.2倍補正をかけている。`is_super_effective` は `damage_calculator.calc_def_type_modifier` を利用しており、フリーズドライ・タールショット・らんきりゅう等の相性変化ロジックを他の抜群依存アイテム（ソクノのみ等）と共通利用しているため、詳細な仕様の各ケースも正しく扱われる。
- `src/jpoke/data/item.py` の登録内容（`fling_power=10`、`ON_CALC_DAMAGE_MODIFIER` ハンドラ、`subject_spec="attacker:self"`）も仕様・計画と一致。

## テスト
- 変更なし。`tests/test_item.py` に `test_たつじんのおび_効果抜群で1_2倍`（でんきショック対ゼニガメで1.2倍補正を確認）と `test_たつじんのおび_等倍では補正なし`（たいあたり対カビゴンで補正なしを確認）が存在し、実装と整合。`.internal/tests/test_item.md` にも反映済み。
- `.internal/progress/item.md` のテスト済み・レビューマークが未更新（テスト列 `-`、レビュー列 `-`、効果列 `?`）だったため、実態に合わせて更新した。
