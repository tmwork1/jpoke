# かたいいし レビュー結果

## 仕様書
- `.internal/spec/items/_タイプ強化アイテム.md`（タイプ強化アイテム共通仕様書）内のかたいいし節を修正。
  - 共通表のなげつける威力が「威力30」になっていたが、一次情報（wiki）では「威力100」だったため修正。
  - 効果節・詳細な仕様節が空欄だったため、一次情報をもとに記載（第二〜第四世代の効果内容、なげつける威力100、
    タイプ変化技への対応）を追記。

## 実装計画書
- `.internal/plan/items/かたいいし.md` が汎用テンプレート（未調査状態）のままだったため、
  実装内容に即した計画書に書き直した。
  - イベント: `Event.ON_CALC_POWER_MODIFIER`（`.internal/spec/turn.md`未掲載、`core/damage.py`のダメージ計算内部で発火）
  - subject_spec: `attacker:self`
  - priority: 100（既定値、他のタイプ強化アイテムと同様）
  - fling_power: 100（実装値と一致することを確認）
  - テスト観点を明記

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/item.py`の`かたいいし`エントリ（`fling_power=100`、`ON_CALC_POWER_MODIFIER`ハンドラ登録）と
  `src/jpoke/handlers/item.py`の`かたいいし_modify_power_by_type`（`_modify_power_by_type`で`いわ`タイプ判定・4915/4096倍）は
  仕様と一致しており、他のタイプ強化アイテムと同一のパターンで正しく実装されていることを確認した。

## テスト
- 変更なし。`tests/test_item.py`の`test_タイプ強化アイテム`（パラメータ化テスト）で
  `("かたいいし", "いわ")`が既に対象タイプの威力補正（power_modifier == 4915）を検証しており、十分な網羅性がある。
  なげつける威力100の値自体は既存の`なげつける`共通テスト（`item_data.fling_power`をそのまま`move.power`に
  設定する汎用ロジック）で間接的に担保されており、他のタイプ強化アイテムと同様に個別のなげつけるテストは
  追加しなかった。
- `python -m pytest tests/ -v` で全件確認（3140 passed, 1 skipped）。
  ※実行中に`test_リフレッシュ_まひを治す`が1回だけ失敗したが、単体実行では再現せず、
  まひの追加効果（30%の完全麻痺）に起因する既存の乱数依存フレーキーテストであり、
  かたいいしの変更とは無関係であることを確認した。
