# のどスプレー レビュー結果

## 仕様書
- 変更なし（`.internal/spec/items/のどスプレー.md` は一次情報（Wiki）と照合済みで、内容に誤り・欠落なし）

## 実装計画書
- `.internal/plan/items/のどスプレー.md` を修正した
  - `battle.consume_item(mon)` という存在しないAPI呼び出しの記述を、実際に使われている `_announce_and_consume_item`（`battle.item_manager.consume_item` 経由）に修正
  - 実装のガード条件（`move_missed` / `move_success` の判定、`modify_stats` の戻り値による消費判定）を疑似コードと注意書きに追記し、根拠（うのミサイルの `move_applied` 参照と同様の手法）を明記
  - テスト観点に「技が外れたとき」「ランク最大時」「いやしのすず不発時」の3項目を追加

## 実装（handlers/ / data/）
- `src/jpoke/handlers/item.py` の `のどスプレー_boost_spatk_on_sound` を修正した
  - **バグ修正1**: `battle.modify_stats(...)` の戻り値（実際に変化したランクの辞書）を確認せずに常にアイテムを消費していたため、とくこうランクがすでに最大（+6）のときも誤って消費されていた。他の「ランク上昇して消費」系アイテム（アッキのみ・じゅうでんち等）と同じパターンに合わせ、`if battle.modify_stats(...):` で戻り値を確認してから消費するように修正
  - **バグ修正2**: 音技が外れたとき（`move_missed=True`）や、命中はしたが技自体が不発だったとき（例: いやしのすずで治療対象のポケモンがいない場合、`ON_STATUS_HIT` 内で `move_success=False` になるがその失敗判定後も `ON_MOVE_END` まで処理が続く実装のため）でも常に発動・消費してしまっていたため、`battle.move_executor.move_missed` / `battle.move_executor.move_success` を参照するガードを追加（うのミサイルが `battle.move_executor.move_applied` を参照するのと同様の手法）
- `data/item.py` の登録内容（`Event.ON_MOVE_END`, `subject_spec="attacker:self"`, `fling_power=30`）は仕様通りで変更なし
- `python scripts/sort_handlers.py src/jpoke/handlers/item.py` を実行済み（並び順に変化なし）

## テスト
- `tests/test_item.py` に以下のテストを追加した
  - `test_のどスプレー_いやしのすずが不発のときは発動しない`
  - `test_のどスプレー_とくこうランクが最大のとき発動しない`
  - `test_のどスプレー_技が外れたときは発動しない`
- 既存の `test_のどスプレー_非音技では発動しない` / `test_のどスプレー_音技使用後にC上昇` は変更なし
- `python scripts/sort_tests.py tests/test_item.py` でソート済み
- `python scripts/generate_test_list.py` で `.internal/tests/` を更新済み
- `python -m pytest tests/ -v` で全3007件パス（1件skip、既存分含む）を確認。ログは `.loop/test_logs/のどスプレー.log` に保存
