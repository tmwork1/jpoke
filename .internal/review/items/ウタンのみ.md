# ウタンのみ レビュー結果

## 仕様書
- 変更なし
- `.internal/spec/items/_タイプ半減きのみ.md` のウタンのみ節は一次情報（`.internal/wiki/items/ウタンのみ.html`）と
  照合済みで、効果・「みらいよちは第五世代以降発動」の記述に誤り・欠落なし。

## 実装計画書
- `.internal/plan/items/ウタンのみ.md` を更新した（旧版は「仕様書未作成」「テスト未実装」という
  古い記述のまま放置されていたため、実際の実装内容に合わせて全面的に書き直し）。
  - `Event.ON_CALC_DAMAGE_MODIFIER` での `_modify_super_effective_damage`（共通ヘルパー）呼び出し、
    `LethalEvent.ON_BEFORE_HIT` での `_type_resist_berry` 呼び出しを明記。
  - priority の根拠（`.internal/spec/turn.md` に個別記載なし、既存の同種きのみに合わせる）を明記。
  - テスト・登録状況（`fling_power=10` 等）を実態に合わせて記載。

## 実装（handlers/ / data/）
- 変更なし
- `src/jpoke/data/item.py` の `"ウタンのみ"` は `ON_CALC_DAMAGE_MODIFIER`（`h.ウタンのみ_modify_super_effective_damage`）と
  `LethalEvent.ON_BEFORE_HIT`（`l.ウタンのみ_resist_psychic`）を登録済み。
- `src/jpoke/handlers/item.py` の `ウタンのみ_modify_super_effective_damage` は共通ヘルパー
  `_modify_super_effective_damage(..., type_="エスパー", modifier=2048/4096)` を正しく呼び出しており、
  直前のナモのみ・ヨロギのみレビューで修正済みの共通ヘルパー（アイテム消費処理・
  `calc_def_type_modifier(ctx) > 4096` の抜群判定閾値）を正しく利用できている。
- `src/jpoke/handlers/lethal.py` の `ウタンのみ_resist_psychic` も共通ヘルパー
  `_type_resist_berry(battle, ctx, hp_dist, "エスパー")` を正しく呼び出している。
- コード修正不要のため `sort_handlers.py` / `sort_abilities.py` 等の実行は不要。

## テスト
- 変更なし
- `tests/test_item.py` の `test_タイプ半減実`（パラメータ化テスト）に
  `("ウタンのみ", "エスパー", "コジョフー")` が既に含まれており、
  「効果バツグン技のダメージが1/2になること」「使用後にアイテムが消費されること」を検証済み。
- みらいよち（第五世代以降で発動する旨）は個別のきのみでは特別扱いされておらず、
  通常の`ON_CALC_DAMAGE_MODIFIER`経由のため既存の汎用機構でカバーされる
  （他のタイプ半減きのみ、例: リリバのみ×はめつのねがい も同様に専用テストは追加されていない）。
- `python -m pytest tests/ -v` で全3146件パス、1件skip（`.loop/test_logs/ウタンのみ.log`）。
  途中1回の実行で `test_リフレッシュ_まひを治す` と `test_いかりのまえば_相手HP半分のダメージ` が
  確率発動（まひの動けない判定・いかりのまえばの命中判定）でflaky failした（ウタンのみと無関係）。
  再実行で全件パスすることを確認済み。
