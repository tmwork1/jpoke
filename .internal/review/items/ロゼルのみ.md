# ロゼルのみ レビュー結果

## 仕様書
- `.internal/spec/items/_タイプ半減きのみ.md`（共有仕様書）に既にロゼルのみの節が存在しており、
  一次情報（ポケモンWiki）と照合した結果、内容に誤り・欠落はなかった（変更なし）。

## 実装計画書
- `.internal/plan/items/ロゼルのみ.md` は「仕様書未作成」「テスト未実装」という古い記述のまま
  放置されていたため、実際の状態（共有仕様書に節あり、実装済み、テスト実装済み）に合わせて
  全面的に書き直した。
  - 仕様書の参照先を `.internal/spec/items/_タイプ半減きのみ.md` に修正
  - 実装内容（`data/item.py` のハンドラ登録、`handlers/item.py` / `handlers/lethal.py` の共通実装）を明記
  - 既存テスト（`tests/test_item.py::test_タイプ半減実`、`tests/test_lethal.py::test_タイプ半減きのみ_代表種`）を明記

## 実装（handlers/ / data/）
- `src/jpoke/data/item.py` の `ロゼルのみ` は他のタイプ半減きのみと同一パターンで
  `Event.ON_CALC_DAMAGE_MODIFIER`（`h.ロゼルのみ_modify_super_effective_damage`）と
  `LethalEvent.ON_BEFORE_HIT`（`l.ロゼルのみ_resist_fairy`）を登録済みで、誤りなし（変更なし）。
- `src/jpoke/handlers/item.py` の `ロゼルのみ_modify_super_effective_damage` は
  `_modify_super_effective_damage(..., type_="フェアリー", modifier=2048/4096)` を呼ぶ共通実装で、
  他のタイプ半減きのみ（ヤチェのみ・ヨプのみ等）と同一パターン（変更なし）。
- `src/jpoke/handlers/lethal.py` の `ロゼルのみ_resist_fairy` も
  `_type_resist_berry(battle, ctx, hp_dist, "フェアリー")` を呼ぶ共通実装で、誤りなし（変更なし）。

## テスト
- `tests/test_item.py::test_タイプ半減実` に `("ロゼルのみ", "フェアリー", "ミニリュウ")` が
  パラメータとして既に含まれており、ダメージ半減・アイテム消費を検証できている（変更なし）。
- `tests/test_lethal.py::test_タイプ半減きのみ_代表種` に
  `("ロゼルのみ", "マジカルシャイン", "ミニリュウ", 44, 52, 88, 104)` が既に含まれており、
  リーサル計算上の1発目半減・2発目通常ダメージを検証できている（変更なし）。
- `python -m pytest tests/ -v` は 3161 passed, 1 skipped で全件パス
  （ログ: `.loop/test_logs/ロゼルのみ.log`）。
