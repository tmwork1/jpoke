# タラプのみ レビュー結果

## 仕様書
- 変更なし
  - 独立ファイル `.internal/spec/items/タラプのみ.md` は存在しないが、`.internal/spec/items/_ランク上昇ピンチきのみ.md`
    内の「タラプのみ」節に一次情報（ポケモンWiki）と一致する内容がすでに記載されている
    （効果・特殊技限定・特防ランク上限時不発・ちからずく対象技では不発・あまのじゃく/たんじゅんの影響・
    連続攻撃技での発動タイミング・マジシャン/やきつくすでの喪失・ほおばる/おちゃかい/はんすうでの発動 等）。
    内容に誤り・欠落は見つからなかった。

## 実装計画書
- `.internal/plan/items/タラプのみ.md` を修正
  - `Stat` の実際のキー名（`"D"` ではなく `"spd"`）に修正
  - `once: True` という誤った記載を削除し、実際は `Handler.once` を使わず
    `modify_stats` が実際に変化を返した場合のみ `ItemManager.consume_item` を呼ぶ実装と一致させた
  - 疑似コードの `battle.consume_item(mon)`（存在しないAPI）を実際の
    `battle.item_manager.consume_item(mon)`（`_announce_and_consume_item` 経由）に修正
  - priority=100 の根拠（`.internal/spec/turn.md` に `ON_DAMAGE_HIT` の記載がないため、同種の
    被弾トリガー系アイテム〔アッキのみ・ゴツゴツメット・きゅうこん等〕の既存実装に倣った）を明記
  - 共通ヘルパー `_boost_on_attack_category`（アッキのみと共用）を利用する実装であることを明記
  - あまのじゃく/たんじゅん・かたやぶり・マジシャン/やきつくすとの相互作用は既存の仕組みで
    自動的に処理されるため個別対応不要である旨を追記
  - **レビューで発見した実装漏れ**（下記参照）を反映し、ちからずく対象技での不発条件を追記
  - リーサル実装（`handlers/lethal.py`）のロジック説明とテスト観点を追加

## 実装（handlers/ / data/）
- `src/jpoke/handlers/item.py` の共通ヘルパー `_boost_on_attack_category` を修正
  - **不具合**: 仕様書（および一次情報）に明記されている「ちからずくの効果が発動した技を受けたときは
    発動しない」という条件が実装に存在しなかった。この関数はアッキのみとタラプのみで共用されているため、
    両アイテムに共通する不具合だった（`だっしゅつボタン`・`かいがらのすず`・`レッドカード` では
    同種のちからずく判定が実装済みで、これらとの整合性も取れていなかった）。
  - `ctx.attacker.ability.name == "ちからずく" and ctx.move.has_flag("secondary_effect")` の場合に
    早期リターンする判定を追加して修正した。
- `src/jpoke/handlers/lethal.py` のリーサルハンドラ `アッキのみ_boost_def` / `タラプのみ_boost_spd` にも
  同様のちからずく判定が欠落していたため、同じ条件式を追加して修正した（通常ハンドラとの整合性確保）。
- `src/jpoke/data/item.py` のイベント登録（`Event.ON_DAMAGE_HIT`, `subject_spec="defender:self"`）および
  `LethalEvent.ON_HIT` の登録内容は仕様・計画書と一致しており変更なし。

## テスト
- `tests/test_item.py` に以下を追加
  - `test_タラプのみ_あまのじゃくによる下降はしろいきりで防げない`
  - `test_タラプのみ_とくぼうランクが最大のとき発動しない`
  - `test_タラプのみ_ちからずくの対象技では発動しない`
  - （アッキのみの既存テストと対称になるよう補充。ちからずくテストは今回発見した不具合の回帰防止）
- `tests/test_lethal.py` に以下を追加
  - `test_タラプのみ_ちからずくの対象技では発動しない`
  - `test_アッキのみ_ちからずくの対象技では発動しない`（リーサル側の同種不具合の回帰防止）
- `python scripts/sort_tests.py tests/test_item.py tests/test_lethal.py`、
  `python scripts/generate_test_list.py`、`python -m pytest tests/ -v` を実行し、
  2949 passed, 1 skipped で全テスト成功を確認（ログ: `.loop/test_logs/タラプのみ.log`）。
