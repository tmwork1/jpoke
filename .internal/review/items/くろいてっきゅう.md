# くろいてっきゅう レビュー結果

## 仕様書
- 変更なし。`.internal/spec/items/くろいてっきゅう.md` は既に存在し、一次情報（`.internal/wiki/items/くろいてっきゅう.html`）の内容（すばやさ0.5倍・浮遊無効化・なげつける威力130・でんじふゆうとの関係・ぶきよう/さしおさえとの関係・まひとの計算順序・譲渡時の行動順への影響など）と一致していることを確認した。

## 実装計画書
- 変更なし。`.internal/plan/items/くろいてっきゅう.md` は仕様書と整合しており、イベント（`DomainEvent.ON_CALC_SPEED` / `Event.ON_CHECK_FLOATING`）・subject_spec（`source:self`）・priority（100、`.internal/spec/turn.md` 未掲載のためデフォルト値を採用する他の素早さ系ハンドラ（まひ・かるわざ等）と揃えた妥当な選択）を確認した。

## 実装（handlers/ / data/）
- 変更なし。
  - `src/jpoke/handlers/item.py` の `くろいてっきゅう_halve_speed` は `apply_fixed_modifier(value, 2048)` で正しく0.5倍（切り捨て）を実装している。
  - `くろいてっきゅう_negate_floating` は `HandlerReturn(value=False, stop_event=True)` で浮遊状態を強制的に解除しており、`ON_CHECK_FLOATING` イベントの初期値（`pokemon.has_type("ひこう")`）を含め、ふゆう・でんじふゆう等の他ハンドラの結果によらず地面にいる扱いにできることを確認した。
  - `src/jpoke/data/item.py` の登録内容（`fling_power=130`, ハンドラ2件, `subject_spec="source:self"`）は仕様・計画書と一致。
  - ぶきよう特性によるアイテム無効化は `ItemHandler` の `source="item"` 経由で汎用的に処理されるため、本アイテム固有の重複実装は不要と判断（既存の共通機構に準拠）。

## テスト
- 変更なし。`tests/test_item.py` に以下の既存テストがあり、実装と一致していることを確認した。
  - `test_くろいてっきゅう_浮遊を無効化`: ふゆう特性による浮遊状態を無効化し、じしんが命中することを検証
  - `test_くろいてっきゅう_素早さ半分`: `calc_effective_speed` が実数値の0.5倍（切り捨て）になることを検証
- `python scripts/sort_tests.py tests/test_item.py` / `python scripts/generate_test_list.py` を実行したが差分なし。
- `python -m pytest tests/ -v` を実行し、2835 passed, 1 skipped で全件パス（`.internal/progress/item.md` の該当行のテスト済み・レビュー済みマークを更新）。ログ: `.loop/test_logs/くろいてっきゅう.log`
  - 実行中に `test_いかりのまえば_相手HP半分のダメージ` と `test_てんねん_攻撃側は防御ランク補正を無視する` が一時的に失敗したが、`accuracy`/急所判定を固定していない既存テストの乱数依存によるflakinessであり、単体実行では成功することを確認済み。くろいてっきゅうの実装・変更とは無関係。
