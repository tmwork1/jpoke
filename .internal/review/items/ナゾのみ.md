# ナゾのみ レビュー結果

## 仕様書
- 変更なし。`.internal/spec/items/ナゾのみ.md` は一次情報（ポケモンWiki `.internal/wiki/items/ナゾのみ.html`）と照合し、効果（効果抜群のダメージを受けた直後に最大HPの1/4回復・消費）、詳細な仕様（ほしがる/どろぼう/ついばむ/むしくい/はたきおとす/やきつくす・マジシャンとの世代別関係、連続攻撃技での発動タイミング、一撃必殺技をこらえて耐えた場合は発動する一方それ以外のダメージ固定技は発動しない、みがわり/ばけのかわ/アイスフェイスでの肩代わり時は発動しない、らんきりゅう/タールショットによるタイプ相性変化の影響）に誤り・欠落なし。

## 実装計画書
- 修正した。`.internal/plan/items/ナゾのみ.md` に、じゃくてんほけんの計画書と同様の構成で以下を追記した。
  - `ctx.move.has_flag("fixed_damage")` の場合は発動しない旨（一撃必殺技は`fixed_damage`フラグを持たないため通常通りタイプ相性判定される）
  - `battle.query.is_super_effective(ctx)` がらんきりゅう・タールショットの相性変化を自然に反映する根拠
  - どろぼう等との優先度関係の補足
  - 「既知の制限」（HPが1でこらえる発動時、ダメージが0に補正されると`ON_DAMAGE_HIT`自体が発火せず発動しない）
  - テスト観点にダメージ固定技・一撃必殺技を耐えた場合を追加

## 実装（handlers/ / data/）
- 修正した。`src/jpoke/handlers/item.py` の `ナゾのみ_heal_on_super_effective` に、`ctx.move.has_flag("fixed_damage")` のガードを追加した。修正前は一撃必殺技以外のダメージ固定技（ちきゅうなげ・いかりのまえば・カタストロフィ・ナイトヘッド等）でも、タイプ相性上抜群であればHPが回復してしまう不備があった（`じゃくてんほけん_boost_on_super_effective` には既にこのガードがあり、ナゾのみだけ欠落していた）。
- `src/jpoke/data/item.py` の登録内容（`fling_power=10`、`ON_DAMAGE_HIT`、`subject_spec="defender:self"`）は変更なし。
- みがわり・ばけのかわ/アイスフェイスでの肩代わり時に発動しない点は、`ON_DAMAGE_HIT`が`actual_damage<=0`のとき発火しない共通基盤（`MoveExecutor._execute_hit`）により自然に満たされているため、ハンドラ側の追加対応は不要と確認した。
- らんきりゅう・タールショットによる相性変化は `battle.query.is_super_effective` が内部で `calc_def_type_modifier`（`ON_CALC_DEF_TYPE_MODIFIER` 経由）を参照するため、既存実装のまま正しく反映されることを確認した。

## テスト
- 追加した。`tests/test_item.py` に以下を追加。
  - `test_ナゾのみ_ダメージ固定技では発動しない`（ちきゅうなげでカビゴンを弱点タイプ攻撃しても発動しないことを確認）
  - `test_ナゾのみ_一撃必殺技を耐えたときは発動する`（じわれ+こらえるでHP1に耐えた直後にナゾのみが発動しHP1/4回復・消費されることを確認）
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` で `.internal/tests/test_item.md` を更新し、`python -m pytest tests/ -v` で全件（3187 passed, 1 skipped）を確認した。
- `.internal/progress/item.md` の該当行を `実装=x`・`テスト=x`・`レビュー=x` に更新し、`効果`列に説明文を記載した（`リーサル実装`・`リーサルテスト`は致死率計算モジュールへの対応が未着手のため `-` のまま据え置き。`くっつきバリ`等と同様の扱い）。

## 補足（共有チェックアウトでの並行作業について）
- 本タスク実行中、同一チェックアウトで並行して `でかいきんのたま`・`しんかのきせき` の別レビュータスクが `src/jpoke/handlers/item.py`・`tests/test_item.py`・`.internal/progress/item.md`・`.internal/tests/test_item.md` を同時編集しており、各タスクが `git add -p` 相当の部分ステージングを試みたにもかかわらず、複数回にわたり作業ツリー/インデックスの競合が発生した。
- 結果として `review: ナゾのみ` コミット（`2a57bb98`）に `しんかのきせき` タスクの差分（`_EVIOLITE_NO_EFFECT` 関連のハンドラ変更・テスト3件）が意図せず混入した。混入分を単独コミットで巻き戻そうと試みたが、巻き戻し作業自体が別タスクの新規コミット（`review: しんかのきせき` `a21c8944`）と再度競合したため、これ以上の巻き戻しはリポジトリの安定性を損なうリスクが高いと判断し断念した。
- 現在の `HEAD` は、`ナゾのみ`・`しんかのきせき` 双方の実装・テスト・進捗表が機能的には正しく揃っており（`python -m pytest tests/ -v` で 3187 passed, 1 skipped を確認済み）、実害はない。ただし `2a57bb98` のコミットメッセージ・差分範囲が実態（複数アイテムの変更を含む）と一致していない点はコミット履歴上の既知の不整合として申し送る。
