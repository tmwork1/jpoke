# きせきのタネ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/items/きせきのタネ.md`）。
  - 従来は共通テンプレート `.internal/spec/items/_タイプ強化アイテム.md` の「きせきのタネ」節（空欄）のみで、単独の仕様書が存在しなかった。
  - 一次情報（`.internal/wiki/items/きせきのタネ.html`）および `_タイプ強化アイテム.md` の共通仕様（威力1.2倍・なげつける威力30）を基に、他のタイプ強化アイテム（くろいメガネ・かたいいし等）と同じ体裁で新規作成した。
  - タイプが変わる技の場合は変更後のタイプで判定される旨を「詳細な仕様」に明記した。

## 実装計画書
- 既存の `.internal/plan/items/きせきのタネ.md` を、他のタイプ強化アイテム（かたいいし・くろいメガネ）の計画書と同じ体裁に更新した。
  - イベント（`Event.ON_CALC_POWER_MODIFIER`）、subject_spec（`attacker:self`）、priority（100・既定値）、fling_power（30）を明記。
  - 発動条件（`ctx.move.type == "くさ"` で1.2倍、タイプ変化技は変更後タイプで判定）を明記。

## 実装（handlers/ / data/）
- 変更なし。
  - `src/jpoke/data/item.py` の `きせきのタネ`（`fling_power=30`、`Event.ON_CALC_POWER_MODIFIER` に `きせきのタネ_modify_power_by_type` を `subject_spec="attacker:self"` で登録）は仕様通り。
  - `src/jpoke/handlers/item.py` の `きせきのタネ_modify_power_by_type` は共通ヘルパー `_modify_power_by_type(ctx.move, value, type_="くさ", modifier=4915)` を利用しており、タイプ変化技にも `ctx.move.type` 判定により自然に対応済み。他のタイプ強化アイテムと同一実装パターンで問題なし。

## テスト
- 変更なし。
  - `tests/test_item.py` の `test_タイプ強化アイテム`（パラメータ化テスト）に `("きせきのタネ", "くさ")` が既に含まれており、威力補正（`power_modifier == 4915`）を検証済み。
  - 他のタイプ強化アイテム（かたいいし・くろいメガネ等）にも個別のタイプ変化技・なげつける威力の追加テストは無く、既存のカバレッジ水準と一致しているため追加テストは不要と判断した。

## テスト実行結果
- `python -m pytest tests/ -v` を実行し、3140 passed, 1 skipped（既存のskipで無関係）で全件成功を確認。ログは `.loop/test_logs/きせきのタネ.log` に保存。
