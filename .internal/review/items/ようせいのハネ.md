# ようせいのハネ レビュー結果

## 仕様書
- `.internal/spec/items/_タイプ強化アイテム.md` に既に収録されていた（個別ファイルは未作成、他のタイプ強化アイテムと同様グループファイルで管理）。
- 一次情報（ポケモンWiki）と照合し、一覧表内の記載が `威力10` になっていた誤りを `威力30` に修正した（同ファイル内の詳細節「### 効果」は元々 `威力30` で正しく、一覧表とのみ食い違っていた）。

## 実装計画書
- `.internal/plan/items/ようせいのハネ.md` が「仕様書: 未作成」「テスト: 未実装」のまま古い記載だったため、実際の状態（仕様書はグループファイルに存在、テストも既存のパラメータ化テストで対応済み）に合わせて全面的に更新した。
- せいれいプレートとの違い（アルセウス・マルチタイプ・さばきのつぶてとの相互作用が無いこと）を明記した。

## 実装（handlers/ / data/）
- `src/jpoke/data/item.py` の `ようせいのハネ` の `fling_power` が `20` になっていたバグを発見・修正（正しくは `30`）。一次情報および `.internal/spec/items/_タイプ強化アイテム.md` の詳細節に基づく。
- `python scripts/sort_data/sort_items.py` を実行し、他の変更は無いことを確認。
- `handlers/item.py` の `ようせいのハネ_modify_power_by_type`（`_modify_power_by_type(ctx.move, value, type_="フェアリー", modifier=4915)`）は他のタイプ強化アイテムと同一パターンで実装済みであり、修正不要だった。

## テスト
- `tests/test_item.py::test_タイプ強化アイテム` のパラメータ化テストで威力補正（1.2倍）は既に検証済みだった。
- なげつける時の威力を検証するテストが無かったため、`test_ようせいのハネ_なげつけるで威力30になる` を新規追加し、修正後の `fling_power=30` を検証した（せいれいプレートの同種テストを参考に作成）。
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新。
- `python -m pytest tests/ -v` で全3144件パス（1件skip）。途中1件 `test_リフレッシュ_まひを治す` が失敗したが、これは麻痺の完全麻痺判定が乱数固定されていない既存のflaky testであり、本タスクの変更とは無関係（単体実行では成功することを確認済み）。

## 進捗表
- `.internal/progress/item.md` の `ようせいのハネ` 行の レビュー列を `-` → `x`、効果列を `?` → 実際の効果説明に更新した。
