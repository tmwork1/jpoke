# くろおび レビュー結果

## 仕様書
- 新規作成（`.internal/spec/items/くろおび.md`）。一次情報（`.internal/wiki/items/くろおび.html`）を確認し、第四世代以降はかくとうタイプの技の威力が1.2倍になること、なげつける威力30であることを記載。

## 実装計画書
- 既存の `.internal/plan/items/くろおび.md` を更新。存在しないとされていた仕様書へのリンクを修正し、実装方針（`ON_CALC_POWER_MODIFIER` ハンドラでの威力補正、`subject_spec="attacker:self"`、fling_power=30）とテストの対応状況を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/item.py` の `くろおび`（`Event.ON_CALC_POWER_MODIFIER` ハンドラ登録、`fling_power=30`）と `src/jpoke/handlers/item.py` の `くろおび_modify_power_by_type`（`_modify_power_by_type` により威力を4915/4096倍＝1.2倍に補正）は仕様書・計画書と整合しており、修正不要と判断。

## テスト
- 変更なし。`tests/test_item.py` の `test_タイプ強化アイテム`（パラメトライズ、`("くろおび", "かくとう")` を含む）で `power_modifier == 4915` を検証済み。全件（3140 passed, 1 skipped）確認済み。
