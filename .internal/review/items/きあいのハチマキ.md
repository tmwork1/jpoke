# きあいのハチマキ レビュー結果

## 仕様書
- 変更なし（既存の `.internal/spec/items/きあいのハチマキ.md` は一次情報（Wiki）と照合済みで、発動確率は現行世代基準の「10%（第二世代のみ30/256）」が正しく記載されていた）

## 実装計画書
- 修正した内容:
  - 発動確率の記載を誤り「11.7%」から一次情報どおりの「10%」に修正（`.internal/plan/items/きあいのハチマキ.md`）
  - 疑似コード中の確率定数を `0.117` → `0.1` に修正
  - テスト観点の記載も「11.7%」→「10%」に修正
  - 対象がポケモンチャンピオンズ（現行仕様）であるため10%を採用する旨の補足を追記

## 実装（handlers/ / data/）
- 修正した内容:
  - `src/jpoke/handlers/item.py` の `きあいのハチマキ_survive_by_chance` が確率 `0.117`（11.7%）で発動していたバグを `0.1`（10%）に修正。docstringも修正
  - 一次情報（Wiki）によれば現行世代の発動確率は10%であり、30/256(≈11.7%)は第二世代のみの仕様のため、旧い実装値は誤りだった
  - `data/item.py` の登録内容（イベント: `ON_MODIFY_MOVE_DAMAGE`, subject_spec: `defender:self`, fling_power=10）は仕様と一致しており変更なし
  - `python scripts/sort_handlers.py src/jpoke/handlers/item.py` を実行済み（関数順は変化なし）

### 既知の未実装事項（今回のスコープ外として記録）
- 一次情報には「こんらんによる自傷ダメージに対しても第三世代以降は発動しうる」とあるが、現状の実装では `こんらん_try_action`（`src/jpoke/handlers/volatile.py`）がダメージ計算を `battle.roll_damage` + `battle.modify_hp` で直接適用しており、`ON_MODIFY_MOVE_DAMAGE` イベント自体が発火しない（`move_executor.py` の通常攻撃フローでのみ発火）ため、きあいのハチマキ（および同種のきあいのタスキ・がんじょう）はこんらん自傷では発動しない
- この制約はきあいのタスキ・がんじょうにも共通する既存のアーキテクチャ上の制約であり、対象を「きあいのハチマキ」のみに限定した本レビューでは範囲外として修正を見送った（こんらんダメージ処理自体の変更は影響範囲が広く、並行レビュー中の他アイテム・特性と競合するリスクがあるため）

## テスト
- 追加・修正した内容:
  - `test_きあいのハチマキ_確率でひんしにならない` / `test_きあいのハチマキ_確率外は耐えない` のコメント・docstringを「11.7%」→「10%」に修正（判定閾値0.0/0.5自体は変更なし、正しく動作することを確認）
  - 新規テスト `test_きあいのハチマキ_発動しても消費されない` を追加し、発動後もアイテムが消費されない（きあいのタスキとの違い）ことを検証
  - `python scripts/sort_tests.py tests/test_item.py` を実行済み
  - `python scripts/generate_test_list.py` を実行し `.internal/tests/test_item.md` を更新済み
  - `python -m pytest tests/ -v` 実行結果: 2759 passed, 1 skipped（`.loop/test_logs/きあいのハチマキ.log` に保存）
    - 初回実行で `test_いかりのまえば_相手HP半分のダメージ` が2件失敗したが、これは命中判定（乱数依存）の無関係な既知の不安定テストであり、単体再実行で成功することを確認済み（きあいのハチマキとは無関係）
