# クラボのみ レビュー結果

## 仕様書
- 変更なし（`.internal/spec/items/_状態回復きのみ.md` に既に「まひ状態を回復させる」と正しく記載されており、一次情報（wiki）とも一致していた）

## 実装計画書
- 修正した: `.internal/plan/items/クラボのみ.md` が「やけど状態であれば回復する」という誤った内容（チーゴのみの効果と取り違え）になっていたため、一次情報・仕様書・実際の実装（`まひ`回復）に合わせて全面的に書き直した
- 実際の実装は `ON_TURN_END` だけでなく `ON_APPLY_AILMENT`（付与直後の即時回復）・`ON_ITEM_ENABLED`（アイテム有効化時）・`ON_FORCE_BERRY_TRIGGER`（強制発動時）にも登録されていたため、計画書にその全イベントを明記した

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/item.py` の `クラボのみ` エントリは `ON_TURN_END` / `ON_APPLY_AILMENT` / `ON_ITEM_ENABLED` / `ON_FORCE_BERRY_TRIGGER` の4イベントに登録され、`src/jpoke/handlers/item.py` の `クラボのみ_cure_paralysis` / `クラボのみ_cure_paralysis_on_apply` が共通ヘルパー `_cure_ailment_berry` / `_cure_ailment_berry_on_apply` 経由で「まひ」を正しく回復・消費しており、仕様と一致していた

## テスト
- 既存: `tests/test_item.py` の `test_状態異常回復きのみ_ターン終了で回復` / `test_状態異常回復きのみ_対象外状態では発動しない` にて、クラボのみ＋まひ（回復対象）／クラボのみ＋ねむり（対象外）のケースが検証済みだった
- 追加: `test_クラボのみ_まひ付与直後に即時回復する` を追加し、`でんじは` でまひを付与された瞬間（ターン終了を待たず）に回復・消費されることを検証した（`test_カゴのみ_ねむり付与直後に即時回復する` と同様のパターン）
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新し、`python -m pytest tests/ -v` で全2827件パス（結果は `.loop/test_logs/クラボのみ.log` に保存）。なお実行中に `tests/moves_status/test_move_ra.py::test_リフレッシュ_まひを治す` が1回だけ失敗したが、これはまひの「動けない」判定が固定されていないことによる既存の無関係なテストのflakinessであり（単体で複数回実行すると常にpass）、クラボのみの変更とは無関係のため本タスクでは修正対象外とした
