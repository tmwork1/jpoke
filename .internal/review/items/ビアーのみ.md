# ビアーのみ レビュー結果

## 仕様書
- 変更なし（`.internal/spec/items/_タイプ半減きのみ.md` の「ビアーのみ」節は一次情報
  （`.internal/wiki/items/ビアーのみ.html`）と照合済み。効果バツグンのどくわざを受けたときに
  ダメージを1/2にして消費するのみで、他のタイプ半減きのみのような特殊な相互作用の記載はなく、
  「詳細な仕様」欄が空欄のままで問題ないことを確認した）。

## 実装計画書
- 修正した。`.internal/plan/items/ビアーのみ.md` は「仕様書未整備」「テスト未実装」という古い記述の
  ままだったため、実際の仕様書（共有ファイル）・実装（`handlers/item.py` の
  `ビアーのみ_modify_super_effective_damage` / `handlers/lethal.py` の `ビアーのみ_resist_poison`）・
  テスト（`tests/test_item.py` の `test_タイプ半減実` パラメータ化テスト）の現状に合わせて、
  他のタイプ半減きのみ（例: シュカのみ）の計画書と同じ形式で書き直した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/item.py` の `"ビアーのみ"` は `ON_CALC_DAMAGE_MODIFIER`
  （`subject_spec="defender:self"`）と `LethalEvent.ON_BEFORE_HIT`（`subject="defender"`）を
  登録済みで、`fling_power=10`。`handlers/item.py` の共通処理 `_modify_super_effective_damage`
  （`type_="どく"`, `modifier=2048/4096`）と `handlers/lethal.py` の共通処理
  `_type_resist_berry(..., "どく")` を呼び出しており、他のタイプ半減きのみと同じ実装パターンで
  問題ないことを確認した。

## テスト
- 変更なし。`tests/test_item.py` の `test_タイプ半減実` に
  `("ビアーのみ", "どく", "プリン")`（どく技はフェアリー複合のプリンに抜群）が既に含まれており、
  `damage_modifier == 2048` とアイテム消費を検証済み。リーサル側の
  `test_タイプ半減きのみ_代表種`（`tests/test_lethal.py`）は共有ロジックの動作確認が目的の代表種
  3件（オッカのみ・ヤチェのみ・ロゼルのみ）のみ検証する設計であり、ビアーのみを個別に追加する
  必要はないと判断した。
- `python -m pytest tests/ -v` で全件パス（3151 passed, 1 skipped）。結果は
  `.loop/test_logs/ビアーのみ.log` に保存した。
