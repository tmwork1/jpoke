# こだわりハチマキ レビュー結果

## 仕様書
- 変更なし（`.internal/spec/items/_こだわり系.md` に既存。一次情報（`.internal/wiki/items/こだわりハチマキ.html`）と照合し、内容（効果・なげつける威力・こんらん自傷ダメージへの影響なし・イカサマ使用/被弾時の挙動）に誤り・欠落がないことを確認した）。

## 実装計画書
- 既存の `.internal/plan/items/こだわりハチマキ.md` に以下を追記した:
  - こんらんの自傷ダメージ（`_こんらん`）には補正がかからないことをハンドラロジックの条件として明記
  - イカサマ使用時／被弾時の挙動が既存の `ON_CALC_ATK_MODIFIER` + `subject_spec="attacker:self"` の仕組みだけで仕様通りに動作することの根拠を追記
  - テスト観点にイカサマ・こんらんのケースを追加

## 実装（handlers/ / data/）
- `handlers/item.py` の `こだわりハチマキ_boost_physical` に不備を発見し修正した:
  - 修正前: 物理技であれば `_こんらん`（こんらんの自傷ダメージ）にも1.5倍補正がかかっていた
  - 修正後: `ctx.move.name != "_こんらん"` を条件に追加し、こんらんの自傷ダメージには補正がかからないようにした（Champions対応範囲＝第五世代以降の仕様に準拠。一次情報に明記されている挙動）
  - `python scripts/sort_handlers.py src/jpoke/handlers/item.py` を実行済み
- イカサマ関連の挙動（使用時は1.5倍、被弾時は補正なし）は既存の `Event.ON_CALC_ATK_MODIFIER` + `subject_spec="attacker:self"` の実装のみで仕様通りに動作することを確認した（`core/damage.py` の `_calc_final_attack` がイカサマ時に防御側のこうげき実数値を使う一方、`ON_CALC_ATK_MODIFIER` は真の攻撃者＝所持者視点で発火するため）。追加実装は不要。
- `data/item.py` のエントリ自体に変更なし。

## テスト
- `tests/test_item.py` に以下を追加した:
  - `test_こだわりハチマキ_イカサマで攻撃するときも1_5倍` — イカサマ使用時に相手の攻撃実数値へ1.5倍補正がかかることを確認
  - `test_こだわりハチマキ_イカサマを受けるときは補正なし` — 所持者がイカサマを受ける場合は補正がかからないことを確認
  - `test_こだわりハチマキ_こんらん自傷ダメージには補正なし` — こんらんの自傷ダメージに補正がかからないことを確認（今回の実装修正の回帰テスト）
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新した。
- `python -m pytest tests/ -v` を実行し、2842 passed, 1 skipped で全件パスを確認した（結果は `.loop/test_logs/こだわりハチマキ.log` に保存）。
