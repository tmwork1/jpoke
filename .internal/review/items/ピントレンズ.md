# ピントレンズ レビュー結果

## 仕様書
- 変更なし。一次情報（`.internal/wiki/items/ピントレンズ.html`）と照合し、効果（急所ランク+1）・なげつける威力30の記述に誤りなし。命中率補正アイテムとの混同（一撃必殺技除外の要否）も確認したが、ピントレンズは急所ランクのみを操作する効果であり、命中率補正やしょうりのほし・ふくがんのような一撃必殺技除外パターンは一次情報上も存在しないため、対応不要と判断。

## 実装計画書
- 変更なし。`Event.ON_CALC_CRITICAL_RANK` / `subject_spec="attacker:self"` / priority 100（デフォルト、するどいツメと同一）の設定は実装と一致している。

## 実装（handlers/ / data/）
- 変更なし。`handlers/item.py` の `ピントレンズ_boost_critical_rank`（急所ランク+1を返すのみ）、`data/item.py` の `ItemData(fling_power=30, handlers={Event.ON_CALC_CRITICAL_RANK: h.ItemHandler(h.ピントレンズ_boost_critical_rank, subject_spec="attacker:self")})` は仕様書・計画書と一致し、するどいツメの実装パターンとも整合している。五十音順の並びも問題なし。

## テスト
- 変更なし。`tests/test_item.py` の `test_ピントレンズ_急所ランク加算` は急所ランクが+1されることを確認しており、するどいツメの対応テストと同一パターンで妥当。追加・修正は不要と判断。

## 進捗表
- `.internal/progress/item.md` のピントレンズ行で、テスト列が `-`、レビュー列が `-`、効果列が `?` のままだったため、実態（テスト実装済み・レビュー完了）に合わせて更新した。効果列はするどいツメの記述形式に倣い「持たせたポケモンが使用する技の急所ランクを+1する（非消費）。するどいツメと重複可能。なげつける威力30。」に補完した。

## テスト実行結果
- `python -m pytest tests/ -v` を実行し、3047 passed, 1 skipped で全件パス（`.loop/test_logs/ピントレンズ.log` に保存）。1回目の実行で `いかりのまえば` 関連テストが2件失敗したが、単体実行・再実行で常に成功しており、本タスク（ピントレンズ）とは無関係な既存の並行実行起因のフレーク性と判断した。
