# のろいのおふだ レビュー結果

## 仕様書
- `.internal/spec/items/_タイプ強化アイテム.md` の「のろいのおふだ」節は本文が未記載（見出しのみ）だったため、一次情報（`.internal/wiki/items/のろいのおふだ.html`）をもとに以下を追記した。
  - 効果: 持たせると第四世代以降ゴーストタイプの技の威力が1.2倍になる（第二・第三世代の記述も参考として記載）。
  - なげつける: 威力30
  - 詳細な仕様: タイプが変わる技で変更後のタイプがゴーストタイプであれば効果があること（他のタイプ強化アイテムと共通の仕様）を追記。

## 実装計画書
- `.internal/plan/items/のろいのおふだ.md` は仕様書未整備を前提とした古い内容だったため、仕様書の節を参照する形に更新した。
- イベント（`ON_CALC_POWER_MODIFIER`）・priorityの根拠（`.internal/spec/turn.md` に記載なし、他のタイプ強化アイテムと同様デフォルト優先度）を明記した。
- テストが未実装と記載されていたが、実際には `tests/test_item.py` の `test_タイプ強化アイテム` に含まれていたため修正した。

## 実装（handlers/ / data/）
- 変更なし。`handlers/item.py` の `のろいのおふだ_modify_power_by_type` は共通処理 `_modify_power_by_type(move, value, type_="ゴースト", modifier=4915)` を使い、`data/item.py` で `Event.ON_CALC_POWER_MODIFIER` に `subject_spec="attacker:self"` で正しく登録されている。他のタイプ強化アイテム（かたいいし・きせきのタネ等）と同一実装パターンで問題なし。

## テスト
- 変更なし。`tests/test_item.py::test_タイプ強化アイテム` のパラメータ一覧に `("のろいのおふだ", "ゴースト")` が含まれており、威力補正（4915/4096倍）を検証済み。

## その他
- `.internal/progress/item.md` の「のろいのおふだ」行のレビュー列を `-` から `x` に更新し、効果列の説明文（`?`）を実際の効果内容で補完した。
