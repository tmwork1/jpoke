# こだわりスカーフ レビュー結果

## 仕様書
- 変更なし（`.internal/spec/items/_こだわり系.md` に既存の記述があり、一次情報（Wiki）と照合して誤り・欠落なしを確認した）

## 実装計画書
- 変更なし（`.internal/plan/items/こだわりスカーフ.md` は仕様書・実装と整合していることを確認した）

## 実装（handlers/ / data/）
- 変更なし
  - `data/item.py`: `DomainEvent.ON_CALC_SPEED`（`こだわりスカーフ_boost_speed`, subject_spec=`source:self`）と `Event.ON_MOVE_END`（`こだわり_lock_move`, subject_spec=`attacker:self`）の登録を確認。同カテゴリの他アイテム（くろいてっきゅう等）と priority 指定の方針も一致している。
  - `handlers/item.py`: `こだわりスカーフ_boost_speed` は `apply_fixed_modifier(value, 6144)`（×1.5）で正しい。
  - `handlers/volatile.py` の共通 `こだわり` volatile（`restrict_commands` 経由）が固定技以外の選択を禁止し、交代コマンドは許可する実装になっており仕様と一致。

## テスト
- 変更なし
  - `tests/test_item.py::test_こだわりスカーフ_素早さ強化` — 素早さ実数値が1.5倍になることを確認。
  - `tests/test_item.py::test_こだわり系_交代でロック解除` / `test_こだわり系_技ロック`（こだわりスカーフを含むパラメータ化テスト）— 技使用後のロックと交代によるロック解除を確認。
  - 全2839件（1件skip、既存の無関係スキップ）が成功することを確認済み（`.loop/test_logs/こだわりスカーフ.log`）。

## 進捗表
- `.internal/progress/item.md` の こだわりスカーフ 行の レビュー列を `x`、効果列を実装内容に沿った説明文に更新した。
