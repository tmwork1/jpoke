# だいはっきんだま レビュー結果

## 仕様書
- 変更なし。一次情報（`.internal/wiki/items/だいはっきんだま.html`）と照合したが、既存の `.internal/spec/items/だいはっきんだま.md` の内容（フォルムチェンジ・威力1.2倍・なげつける失敗・奪取/譲渡制限）に誤り・欠落なし。

## 実装計画書
- 修正した内容:
  - 実装状態を「未実装」→「実装済み」に更新。
  - `Event.ON_CHECK_ITEM_CHANGE` によるギラティナからの奪取/交換/除去防止、およびアナザーフォルムのギラティナへの譲渡防止を実装方針・実装ゴール・実装対象イベント・変更対象・実装ステップ・テスト観点に追記（類似アイテム「だいこんごうだま」の計画書に合わせた形式）。
  - 疑似コードと `data/item.py` への登録例を追記。
  - `はたきおとす_power` が `ItemManager.can_change_item` を確認する共通実装であることをリスクと対策に明記。

## 実装（handlers/ / data/）
- 修正した内容:
  - `src/jpoke/handlers/item.py` に `だいはっきんだま_prevent_item_change`・`だいはっきんだま_prevent_transfer_to_base_form` を追加（既存の共通処理 `_dedicated_item_prevent_item_change` / `_dedicated_item_prevent_transfer_to_base_form` を利用、「だいこんごうだま」と同じパターン）。
  - `src/jpoke/data/item.py` の `だいはっきんだま` エントリに `Event.ON_CHECK_ITEM_CHANGE`（`target:self` と `target:foe` の2ハンドラ）を追加登録。
  - 修正前は `ON_SWITCH_IN` / `ON_ITEM_GAINED` / `ON_CALC_POWER_MODIFIER` のみが登録されており、仕様書に明記されている「ギラティナが持っている間はトリック・すりかえ・ほしがる・どろぼう・特性マジシャン/わるいてぐせ・ふしょくガス・はたきおとすで奪取/交換/除去されない」「アナザーフォルムのギラティナへは渡せない」という挙動が未実装だった（類似アイテム「だいこんごうだま」で見つかった不備と同種の欠落）。
  - `はたきおとす_power`（`src/jpoke/handlers/move_attack.py`）は既に `ItemManager.can_change_item` を参照する共通実装になっており、今回の `ON_CHECK_ITEM_CHANGE` 登録追加によりだいはっきんだまに対しても威力補正なしの挙動が自動的に機能することを確認した（追加修正不要）。
  - `removable=False` / `fling_power=0` の設定値は既に正しく設定されていた（変更なし）。

## テスト
- 追加した内容（`tests/test_item.py`）:
  - `test_だいはっきんだま_ギラティナから奪えない`（`はたきおとす`・`どろぼう` をパラメトライズ）: ギラティナ(オリジン)がだいはっきんだまを持っている間ははたきおとす・どろぼうで奪われないことを確認。
  - `test_だいはっきんだま_トリックで交換されない`: トリック使用時にだいはっきんだまが交換されないことを確認。
  - `test_だいはっきんだま_フォルムチェンジ`: 既存踏襲（ギラティナ(アナザー)がだいはっきんだまを持って登場するとギラティナ(オリジン)になることを確認、既存テストの並び位置整理のみ）。
  - `test_だいはっきんだま_通常のギラティナへ渡せない`: アナザーフォルムのギラティナへトリック等で渡せないことを確認。
  - 既存の `test_専用アイテム補正`（`だいはっきんだま` パラメータ）は変更なし。
  - `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新、`python -m pytest tests/ -v` で全件パス（2908 passed, 1 skipped）を確認。ログは `.loop/test_logs/だいはっきんだま.log` に保存。
