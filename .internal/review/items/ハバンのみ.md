# ハバンのみ レビュー結果

## 仕様書
- 変更なし。`.internal/spec/items/_タイプ半減きのみ.md` の「ハバンのみ」節は一次情報（ポケモンWiki）と照合し、
  効果バツグンのドラゴンわざを受けたときにダメージを1/2にして消費する、という記述で過不足がないことを確認した。
  他の一部タイプ半減きのみ（ヤチェのみのフリーズドライ、ソクノのみのタイプ変化技等）のような
  特殊な相互作用はハバンのみには存在せず、一次情報にもそれを示唆する記述はなかった。

## 実装計画書
- `.internal/plan/items/ハバンのみ.md` を新形式（優先度・共通処理の呼び出し方・priorityの根拠・
  リーサル計算側の扱い・テスト対応表を明記する形式）で書き直した。
  従来の記述は「仕様書未整備」「テスト未実装」など既に事実と異なる古い内容だったため更新した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/item.py` の `"ハバンのみ"` エントリは
  `Event.ON_CALC_DAMAGE_MODIFIER`（`subject_spec="defender:self"`）と
  `LethalEvent.ON_BEFORE_HIT`（`subject="defender"`）を正しく登録している。
- `src/jpoke/handlers/item.py::ハバンのみ_modify_super_effective_damage` は共通処理
  `_modify_super_effective_damage(type_="ドラゴン", modifier=2048/4096)` を呼び出しており、
  他の17種のタイプ半減きのみと同一パターンで実装済み。
- `src/jpoke/handlers/lethal.py::ハバンのみ_resist_dragon` も共通処理 `_type_resist_berry` を
  正しく利用している。

## テスト
- 変更なし。`tests/test_item.py::test_タイプ半減実`（パラメータ化テスト）に
  `("ハバンのみ", "ドラゴン", "ミニリュウ")` が既に含まれており、
  ダメージ半減（`damage_modifier == 2048`）とアイテム消費の両方を検証している。
- リーサル側も `tests/test_lethal.py::test_タイプ半減きのみ_代表種` が共通処理 `_type_resist_berry` を
  代表3種（オッカのみ・ヤチェのみ・ロゼルのみ）で検証しており、共通ロジックを使うハバンのみは
  個別のリーサルテストを追加する必要はないと判断した。

## 進捗表
- `.internal/progress/item.md` の「ハバンのみ」行のレビュー列を `-` → `x` に更新し、
  効果列に「効果バツグンのドラゴンわざを受けたときに消費して、1度だけダメージを1/2にする。」を記載した。

## テスト実行結果
- `python -m pytest tests/ -v` で全3151件パス（1件スキップ）。ログは
  `.loop/test_logs/ハバンのみ.log` に保存。
  （実行時に他の並行タスクの未コミット編集の影響で `test_ability_ta.py` が一時的に失敗する
  ケースが1回あったが、無関係であることを確認した上で再実行し、最終的に全件パスした。）
