# せいれいプレート レビュー結果

## 仕様書
- 既存の `.internal/spec/items/せいれいプレート.md` を一次情報（プレート#一覧・#効果・#値段）と照合し、以下を補充した。
  - なげつける威力90（こわもてプレートの場合のみプレート自身の補正で108になる）を効果欄に追記。
  - 詳細な仕様に「タイプが変わる技（ノーマルスキン等）は変更後のタイプがフェアリーであれば効果がある」を追記。
  - 関連項目に「ようせいのハネ」（同様のフェアリー強化アイテム）と「マルチタイプ」（アルセウス専用特性、プレートによるフォルム・タイプシフトと道具変更妨害を担う）を追加。

## 実装計画書
- `.internal/plan/items/せいれいプレート.md` は内容が薄く（イベント・priority・subject_specの明記なし）実態と乖離していたため、`するどいくちばし.md` 等の既存フォーマットに合わせて全面的に書き直した。
  - イベント: `Event.ON_CALC_POWER_MODIFIER` / subject_spec: `attacker:self` / priority: 100（デフォルト） を明記。
  - アルセウスのフォルム・タイプシフトや道具変更妨害はマルチタイプ特性側の実装範囲であり、本アイテム自体には実装不要であることを明記。
  - さばきのつぶての技タイプ変化は技側（`.internal/progress/move.md`）の管理範囲であり、本アイテムの計画には含めないことを明記。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/item.py` の `せいれいプレート_modify_power_by_type` はフェアリータイプ技の威力を1.2倍（4915/4096）にする実装で、仕様と一致。修正なし。
- `src/jpoke/data/item.py` の `fling_power` が `30` になっていたが、一次情報（プレートのなげつける威力は90）と不一致だったため `90` に修正した。
  - 修正後 `scripts/sort_data/sort_items.py` を実行し、並び順に変化がないことを確認済み。
- `scripts/sort_handlers.py src/jpoke/handlers/item.py` を実行し、並び順に変化がないことを確認済み。

## テスト
- `tests/test_item.py` にフェアリータイプ技の威力補正を確認する `test_タイプ強化アイテム` パラメータ化テストは既存で網羅されていた。
- `fling_power` 修正に伴い、なげつける使用時の威力が90になることを確認する `test_せいれいプレート_なげつけるで威力90になる` を新規追加した（フェアリー以外の技のためタイプ補正は乗らないことも兼ねて確認）。
- `scripts/sort_tests.py tests/test_item.py` でソート、`scripts/generate_test_list.py` で一覧を更新し、`python -m pytest tests/ -v` で全件パス（3143 passed, 1 skipped）を確認した。
