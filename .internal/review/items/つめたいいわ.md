# つめたいいわ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/items/つめたいいわ.md` が存在しなかったため、`.internal/wiki/items/つめたいいわ.html` の一次情報をもとに作成）
- あついいわ・さらさらいわの仕様書と同じ構成で、効果・詳細な仕様・備考・関連項目を記載

## 実装計画書
- 既存の計画書に以下を補記
  - 疑似コードの引数名を実装と一致させた（`field="ゆき"` → `"ゆき"`。実装は位置引数）
  - `ON_MODIFY_DURATION` が技・特性（ゆきふらし）双方の天候発動処理で共通して発火される旨を追記
  - 「実装状態」セクションを追加（さらさらいわ等の計画書と揃える）
  - テスト観点に「特性ゆきふらしで発動したゆきも8ターン継続すること」を追記

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/item.py` の `つめたいいわ`（fling_power=40, `ON_MODIFY_DURATION` ハンドラ登録）、
  `src/jpoke/handlers/item.py` の `つめたいいわ_resolve_field_count`（`_resolve_field_count(value, "ゆき", additonal_count=3)`）は仕様書・計画書と一致しており、
  あついいわ・さらさらいわ・しめったいわと同一パターンで実装済みだった

## テスト
- 変更なし。`tests/test_item.py` の `test_天候延長アイテム`（パラメータ化テスト）で
  つめたいいわ所持時にゆきが8ターンに延長されることを既に検証済み。
  `weather_manager.apply()` は技・特性ゆきふらしいずれの発動経路でも共通して `ON_MODIFY_DURATION` を発火するため、
  既存の1テストで両経路をカバーできている（`tests/abilities/test_ability__group.py` の `test_天候始動特性_登場時に発動` で
  ゆきふらしが同じ `apply()` 経路を通ることを確認済み）
- `.internal/progress/item.md` のつめたいいわ行を更新（レビュー列 `-` → `x`、効果列 `?` → 具体的な効果説明）
