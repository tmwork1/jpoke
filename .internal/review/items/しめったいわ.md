# しめったいわ レビュー結果

## 仕様書
- 変更なし
  - `.internal/spec/items/_天候石.md`（あついいわ・しめったいわ・さらさらいわ・つめたいいわを収録した共有仕様書）に
    しめったいわの節が既に存在しており、一次情報（`.internal/wiki/items/しめったいわ.html`）と照合した結果、
    効果（あめ状態にする技・特性あめふらし発動時に持続ターンが8ターンに延長）、なげつける威力60、
    ダイマックス技での適用、じょうきょう画面表示の詳細仕様など、内容に誤り・欠落はなかった

## 実装計画書
- 修正した内容（`.internal/plan/items/しめったいわ.md`）
  - `ON_MODIFY_DURATION` が `.internal/spec/turn.md` に未掲載であることを明記し、priority をデフォルト値100とする根拠として
    既存の同種ハンドラ（さらさらいわ・あついいわ・つめたいいわ・ひかりのねんど・グランドコート）が
    いずれも priority を明示指定していない事実を追記
  - あまごい（技）・あめふらし（特性）のどちらの発生源でも `WeatherManager.apply()` が
    唯一の発動経路であるため区別なく延長されることを補足として追記

## 実装（handlers/ / data/）
- 変更なし
  - `src/jpoke/handlers/item.py` の `しめったいわ_resolve_field_count`（`_resolve_field_count(value, "あめ", additonal_count=3)`）
  - `src/jpoke/data/item.py` の `ON_MODIFY_DURATION` ハンドラ登録（`subject_spec="source:self"`）
  - いずれも仕様・計画書と一致し、さらさらいわ等の兄弟アイテムと同一パターンで実装済み

## テスト
- 変更なし
  - `tests/test_item.py::test_天候延長アイテム`（パラメータ化: あついいわ／しめったいわ／つめたいいわ）で
    `weather_manager.apply` 経由の持続ターン延長（5→8）を検証済み。技・特性いずれの発生源でも
    `WeatherManager.apply()` を通るため、このテストで両方のケースをカバーしている
  - `python scripts/sort_tests.py tests/test_item.py`、`python scripts/generate_test_list.py`、
    `python -m pytest tests/ -v` を実行し、2856 passed, 1 skipped で全テスト成功を確認
    （`test_いかりのまえば_相手HP半分のダメージ` の失敗が一度観測されたが、単独実行では再現せず
    命中判定に依存する既存のflaky testでありしめったいわの変更とは無関係）

## 進捗表
- `.internal/progress/item.md` の しめったいわ 行のレビュー列を `-` → `x` に更新し、効果列に
  「持たせたポケモンがあめ状態にする技・特性（あめふらし）を発動させたとき、あめの持続ターンを8ターンに延長する
  （非消費）。なげつける威力60。」を記載
- 作業中に気づいた誤りとして、あついいわ行（レビュー済みだが効果列が `?` のまま）の効果列も
  同様の説明文で補完した
