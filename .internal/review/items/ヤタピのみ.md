# ヤタピのみ レビュー結果

## 仕様書
- 変更なし。`.internal/spec/items/_ランク上昇ピンチきのみ.md` の「ヤタピのみ」節が既に存在し、一次情報（`.internal/wiki/items/ヤタピのみ.html`）と照合した結果、内容（HP1/4以下でとくこう+1・消費、とくこうランク最大時は不発、詳細な仕様はチイラのみを参照、第五世代以降は特性ぎゃくじょうの後に発動）に誤りは無かった。

## 実装計画書
- `.internal/plan/items/ヤタピのみ.md` を修正した。
  - 旧内容は「hp_before/hp_after の閾値跨ぎ判定」という実際の実装と異なる疑似コードのままだった（実際の実装は共有ヘルパー `_boost_on_quarter_hp` を使用し、こんらん自傷ダメージの除外・強制発動・ランク上限時不消費を扱う）。
  - 実際の共有ヘルパーのロジックに合わせて記述を更新し、priority・once の根拠、たんじゅん/じゅくせい/あまのじゃくの扱い（既存の `ON_BEFORE_MODIFY_STAT` 経由の共通処理に委譲）を明記した。
  - テスト観点に「とくこうランクが+6のとき発動してもランク上限で止まる（不発・不消費）こと」「こんらんの自傷ダメージでは発動しないこと」を追加した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/item.py` の `ヤタピのみ_boost_spatk` は共有ヘルパー `_boost_on_quarter_hp(battle, ctx, value, stat="spa", amount=+1)` を呼び出しており、直前の「スターのみ」レビューで修正済みの `ctx.hp_change_reason == "self_attack"` 除外ロジックを反映している。
- `src/jpoke/data/item.py` の登録内容（`fling_power=10`, `Event.ON_HP_CHANGED`, `subject_spec="target:self"`, priority既定値100）も他のピンチきのみ系アイテム（チイラのみ・カムラのみ・ズアのみ・リュガのみ）と一致しており、問題なし。

## テスト
- `tests/test_item.py` に既存の `test_HP25以下でランク上昇するきのみ` と `test_HP25以下でランク上昇するきのみ_こんらんの自傷では発動しない`（いずれもパラメトライズ、ヤタピのみを含む）で基本発動・こんらん自傷ダメージ除外は確認済みだったが、「とくこうランクが最大のとき発動しない」個別ケース（チイラのみ・カムラのみ・ズアのみには既存）がヤタピのみには無かったため、`test_ヤタピのみ_とくこうランクが最大のとき発動しない` を追加した。
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` で一覧更新、`python -m pytest tests/ -v` を実行し全件（3105 passed, 1 skipped）成功を確認（ログ: `.loop/test_logs/ヤタピのみ.log`）。

## 進捗表
- `.internal/progress/item.md` のヤタピのみ行を更新: テスト列 `-` → `x`、レビュー列 `-` → `x`、効果列 `?` → 「HPが最大HPの1/4以下になった瞬間にとくこう+1して消費する。すでにとくこうランクが最大のときは発動せず消費されない。こんらんの自傷ダメージでは発動しない。なげつける威力10。」
