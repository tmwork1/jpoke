# じしゃく レビュー結果

## 仕様書
- 新規作成（`.internal/spec/items/じしゃく.md` が未整備だったため、一次情報（`.internal/wiki/items/じしゃく.html`）と類似アイテム（`くろおび`）の仕様書を参考に作成）
  - 効果: 持たせると、でんきタイプの技の威力が1.2倍になる（第四世代以降）。なげつける威力30。
  - 詳細な仕様: タイプが変わる技（わざレンジャー等）は変更後のタイプがでんきタイプであれば効果が適用される旨を明記。

## 実装計画書
- 修正した内容:
  - スタブ状態だった `.internal/plan/items/じしゃく.md` を、`くろおび` 実装計画書と同じ形式に整備。
  - 仕様書へのリンクを追記し、実装方針（`ON_CALC_POWER_MODIFIER` ハンドラ登録、`subject_spec="attacker:self"`、`fling_power=30`）を明記。

## 実装（handlers/ / data/）
- ロジック自体は仕様書・計画書と一致しており正しい（`_modify_power_by_type(ctx.move, value, type_="でんき", modifier=4915)`、`fling_power=30`）。
- `src/jpoke/data/item.py` の `じしゃく` エントリで末尾カンマが欠落していたためフォーマットを修正（`scripts/sort_data/sort_items.py` 実行済み、他エントリへの差分なし）。
- `handlers/item.py` は既に五十音順で正しい位置にあり、`scripts/sort_handlers.py` 実行後も差分なし。

## テスト
- 変更なし。`tests/test_item.py` の `test_タイプ強化アイテム`（パラメトライズ）で `じしゃく` が既にカバーされていることを確認。
- `scripts/sort_tests.py tests/test_item.py` / `scripts/generate_test_list.py` を実行（差分なし）。
- `python -m pytest tests/ -v` を実行し、3140 passed, 1 skipped で全件パスを確認（`.loop/test_logs/じしゃく.log` に保存）。
