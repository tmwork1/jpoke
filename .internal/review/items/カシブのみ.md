# カシブのみ レビュー結果

## 仕様書
- 変更なし。`.internal/spec/items/_タイプ半減きのみ.md` の「カシブのみ」節を一次情報（`.internal/wiki/items/カシブのみ.html`）と照合し、内容が一致していることを確認した（効果バツグンのゴーストわざで発動・消費・ダメージ1/2、ポルターガイストは効果抜群でも成功する旨）。

## 実装計画書
- `.internal/plan/items/カシブのみ.md` を更新した。旧内容は「仕様書未作成・テスト未実装」のまま古い状態だったため、以下を反映して書き直した。
  - 仕様書の参照先を `.internal/spec/items/_タイプ半減きのみ.md` に修正。
  - 効果概要（ポルターガイストとの相互作用を含む）を追記。
  - 実装方針（`ON_CALC_DAMAGE_MODIFIER` + 共通処理 `_modify_super_effective_damage`、`lethal_handlers` の `ON_BEFORE_HIT` + `_type_resist_berry`）を明記。
  - 実装済み内容・テスト状況を現状に合わせて更新。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/item.py` の `カシブのみ` エントリ（`handlers` に `ON_CALC_DAMAGE_MODIFIER`、`lethal_handlers` に `ON_BEFORE_HIT`）、
  `src/jpoke/handlers/item.py::カシブのみ_modify_super_effective_damage`、`src/jpoke/handlers/lethal.py::カシブのみ_resist_ghost` を確認したが、
  共通処理 `_modify_super_effective_damage` / `_type_resist_berry` は既に修正済み（アイテム消費漏れ・効果抜群判定の閾値誤りは解消済み）であり、
  カシブのみ固有の実装に誤りは見つからなかった。
- ポルターガイストとの相互作用（`ON_BEFORE_APPLY_MOVE` でのアイテム所持チェックが `ON_CALC_DAMAGE_MODIFIER` より先に行われるため、
  効果抜群でカシブのみを消費してもポルターガイスト自体は成功する）についても、既存の実装のイベント順序で正しく再現されることを確認した。

## テスト
- `tests/test_item.py` の `test_タイプ半減実`（パラメトライズ、カシブのみ含む）を確認し、既存のまま問題なしと判断した。
- 仕様書に明記されているポルターガイストとの相互作用（技自体は成功しつつダメージが半減する）を検証するテストが存在しなかったため、
  `test_カシブのみ_ポルターガイストは成功しダメージ半減` を追加した。
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新、
  `python -m pytest tests/ -v` を実行し、3151 passed, 1 skipped（既存の無関係なskip）で全件成功を確認した。
