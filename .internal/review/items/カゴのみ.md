# カゴのみ レビュー結果

## 仕様書
- 変更なし（`.internal/spec/items/_状態回復きのみ.md` の「カゴのみ」節が一次情報と一致していることを確認した。効果は「ねむり状態を回復させる」、なげつける威力10で相違なし）

## 実装計画書
- 変更なし（`.internal/plan/items/カゴのみ.md` を一次情報・実装と照合。計画書は `ON_TURN_END` のみ記載しているが、実装は `ON_APPLY_AILMENT`（付与直後の即時回復）・`ON_ITEM_ENABLED`（かがくへんかガス解除後の再発動）・`ON_FORCE_BERRY_TRIGGER`（ほおばる等での強制発動）も含む、より完全な実装になっている。これは他の状態異常回復きのみ（クラボのみ・チーゴのみ・ナナシのみ・モモンのみ・ラムのみ）と共通の確立済みパターンであり、内部の共通処理 `_cure_ailment_berry` / `_cure_ailment_berry_on_apply` を正しく利用しているため、実装側は妥当と判断し計画書側を書き換えるのではなく実装を正とみなした。計画書自体は疑似コードとしては誤りではないため変更なし)

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/item.py` の `"カゴのみ"` エントリ（`ON_TURN_END` / `ON_APPLY_AILMENT` / `ON_ITEM_ENABLED` / `ON_FORCE_BERRY_TRIGGER`、いずれも priority=50、fling_power=10）と `src/jpoke/handlers/item.py` の `カゴのみ_cure_sleep` / `カゴのみ_cure_sleep_on_apply`（共通処理 `_cure_ailment_berry` / `_cure_ailment_berry_on_apply` を呼び出す薄いラッパー）を確認。仕様通りねむり状態のみを回復し、消費・ログ通知も共通処理内で行われている。修正なし。

## テスト
- 追加: `tests/test_item.py` に `test_カゴのみ_ねむり付与直後に即時回復する` を新設。
  - 既存テストは `ON_TURN_END`（ターン終了時の回復・対象外状態では発動しない）のみをカバーしており、`ON_APPLY_AILMENT` による「付与直後の即時回復」経路がカゴのみを含む状態異常回復きのみファミリー全体で未検証だったため、キノコのほうしでねむりを付与した直後に回復・消費されることを確認するテストを追加した。
  - `python scripts/sort_tests.py tests/test_item.py` を実行し五十音順に整列。
  - `python scripts/generate_test_list.py` を実行し `.internal/tests/test_item.md` を更新。
  - `python -m pytest tests/ -v` を実行し、2752 passed, 1 skipped（無関係のスキップ）を確認。ログは `.loop/test_logs/カゴのみ.log` に保存。
