# ものしりメガネ レビュー結果

## 仕様書
- 変更なし。既存の `.internal/spec/items/ものしりメガネ.md` を一次情報（`.internal/wiki/items/ものしりメガネ.html`）と照合したが、記載内容（特殊技威力1.1倍・第五世代以降は4505/4096倍・なげつける威力10・関連項目にちからのハチマキ）は正確で欠落もなかった。

## 実装計画書
- 修正した。旧計画書は「仕様書: 未作成」「テスト: 未実装」のまま古い状態で残っていたため、現状（仕様書が既に存在し、実装・テストとも完了）に合わせて `.internal/plan/items/ものしりメガネ.md` を `ちからのハチマキ` の計画書フォーマットに揃えて書き直した（priority根拠・実装内容・テスト一覧・今後の対応を明記）。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/item.py` の `ものしりメガネ`（`fling_power=10`, `Event.ON_CALC_POWER_MODIFIER` / `subject_spec="attacker:self"`）と `src/jpoke/handlers/item.py` の `ものしりメガネ_boost_special`（`ctx.move.category == "special"` のとき `apply_fixed_modifier(value, 4505)`）は仕様書・計画書と一致しており、姉妹アイテム `ちからのハチマキ_boost_physical`（物理版）とも実装パターンが一致していることを確認した。

## テスト
- 変更なし。`tests/test_item.py` に `test_ものしりメガネ_物理技には補正なし`（物理技で `power_modifier == 4096`）と `test_ものしりメガネ_特殊技1_1倍`（特殊技で `power_modifier == 4505`）が既に実装されており、`ちからのハチマキ` のテストと対称的な構成になっていることを確認した。追加・修正は不要と判断した。

## その他
- `.internal/progress/item.md` のものしりメガネ行が「テスト」「レビュー」列が未更新（`-`）かつ「効果」列が `?` のままだったため、実態（テスト済み・レビュー済み）に合わせて更新し、効果説明文を追記した。
