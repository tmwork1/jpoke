# くろいヘドロ レビュー結果

## 仕様書
- 変更なし（一次情報と完全に一致していることを確認した）

## 実装計画書
- 変更なし（仕様書・turn.md（ON_TURN_END priority=60）と整合していることを確認した）

## 実装（handlers/ / data/）
- `src/jpoke/data/item.py`: `Event.ON_TURN_END` の `ItemHandler` priority が `50` になっており、
  `.internal/spec/turn.md`（60: くろいヘドロ）および計画書の priority=60 と食い違っていたため `60` に修正した。
- `src/jpoke/handlers/lethal.py` の `くろいヘドロ_recover_or_damage`: 非どくタイプのダメージ計算が
  `max_hp / 16` になっており、仕様（最大HPの1/8ダメージ）と異なるバグを発見。`max_hp / 8` に修正し、
  併せて既存の `_damage()` ヘルパーを使うよう簡略化した（`_heal` と対称な実装に統一）。
  docstring の誤記（「1/16ダメージ」）も修正した。

## テスト
- `tests/test_item.py` の既存テスト（`test_くろいヘドロ_どくタイプは回復`, `test_くろいヘドロ_非どくタイプはダメージ`）は
  実装と一致しており問題なし。変更なし。
- `tests/test_lethal.py` の `test_くろいヘドロ_非どくタイプは毎ターンダメージ` が、上記のダメージ計算バグに
  合わせて `max_hp // 16` を期待値としていた（バグと整合する誤ったテスト）ため、
  修正後の実装（1/8ダメージ）に合わせて `max_hp // 8` に修正し、docstring も修正した。
- `python -m pytest tests/` は全件パス（2836 passed, 1 skipped）。
  なお、フルスイート実行時に `test_move_a.py::test_いかりのまえば_相手HP半分のダメージ` や
  `test_field.py` の一部テストなど、`accuracy`/乱数を固定していないテストに起因する
  くろいヘドロと無関係な既存の間欠的な失敗が観測されることがあるが、単体実行では常にパスすることを確認済み。
  本タスクのスコープ外のため修正していない。
