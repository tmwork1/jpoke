# ぎんのこな レビュー結果

## 仕様書
- 新規作成（`.internal/spec/items/ぎんのこな.md`）。
  - 一次情報（ポケモンWiki）に基づき、効果（むしタイプ技威力1.2倍）となげつける威力10を記載。

## 実装計画書
- `.internal/plan/items/ぎんのこな.md` を更新した。
  - 仕様書へのリンクを反映し、実装内容（イベント・handler・modifier値）とテスト状況を明記した。

## 実装（handlers/ / data/）
- 変更なし。
  - `src/jpoke/data/item.py` の `"ぎんのこな"` エントリは `fling_power=10` を持ち、一次情報の
    なげつける威力10と一致していることを確認した（プレート系90・ようせいのハネ30とは異なる正しい値）。
  - `src/jpoke/handlers/item.py` の `ぎんのこな_modify_power_by_type` は
    `_modify_power_by_type(ctx.move, value, type_="むし", modifier=4915)` を呼び出しており、
    4915/4096 ≒ 1.2倍で仕様と一致していることを確認した。
  - `data/item.py` は既に五十音順にソート済みであることを `scripts/sort_data/sort_items.py`（実体は
    `scripts/sort_items.py`）実行により確認した（差分なし）。

## テスト
- 変更なし。
  - `tests/test_item.py` の `test_タイプ強化アイテム`（パラメータ化テスト）に
    `("ぎんのこな", "むし")` が既に含まれており、威力補正値（`power_modifier == 4915`）を検証済み。
  - `python scripts/sort_tests.py tests/test_item.py` / `python scripts/generate_test_list.py` を
    実行したが差分なし（既に整った状態）。
  - `python -m pytest tests/ -v` を実行し、ぎんのこな関連テストは全てPASSであることを確認した。
    なお `test_いかりのまえば_相手HP半分のダメージ` の2件が失敗したが、これは並行作業中の別タスクによる
    `src/jpoke/core/move_executor.py` / `src/jpoke/core/player_state.py` の未コミット変更に起因するもので、
    ぎんのこなの実装・テストとは無関係であることを確認済み（該当変更を一時的に stash して再実行し、
    stash除去後は成功することを確認した）。

## 進捗表
- `.internal/progress/item.md` の `ぎんのこな` 行のレビュー列を `-` から `x` に、効果列を
  「持たせると、むしタイプの技の威力が1.2倍になる。なげつける威力10。」に更新した。
