# たべのこし レビュー結果

## 仕様書
- 変更なし（一次情報と照合し誤り・欠落なしを確認）。

## 実装計画書
- 内容は仕様通りだったが、記述が簡素なテンプレートのままだったため、他のアイテム計画書（`くろいヘドロ.md`等）の形式に合わせて構造化した：
  - `実装仕様` テーブル（イベント・subject_spec・priority・once・消費条件）を追加。
  - ハンドラロジック（疑似コード）を追加し、`battle.modify_hp(mon, r=1/16)` が `ON_MODIFY_HEAL` 経由でかいふくふうじによる回復無効化を自動的に受けることを明記（専用ガード不要である根拠を記載）。
  - テスト観点を追加。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/item.py` の `たべのこし_heal`、`src/jpoke/data/item.py` の登録（`Event.ON_TURN_END`, `subject_spec="source:self"`, `priority=60`）は `.internal/spec/turn.md` の ON_TURN_END 表（たべのこし: priority 60）と一致。
- `src/jpoke/handlers/lethal.py` の `たべのこし_heal`（リーサル側）も `_heal` ヘルパーで最大HPの1/16回復（切り捨て、最低1）を実装済みで仕様と一致。
- かいふくふうじによる回復無効化は `かいふくふうじ_block_heal`（`handlers/volatile.py`）が `ON_MODIFY_HEAL` イベントを介して全ての回復処理に共通適用されるため、`たべのこし_heal` 個別の対応は不要（既存実装のまま）。

## テスト
- `tests/test_item.py::test_たべのこし` は既存（HP満タン時は非発動・アイテム非露見、HP減少時は1/16回復することを確認）。
- 仕様書に明記されているかいふくふうじとの相互作用（第五世代以降は回復不可）を検証するテストが欠けていたため、`test_たべのこし_かいふくふうじ中は回復しない` を追加。
- `tests/test_lethal.py::test_たべのこし_ターン終了時に回復` は既存でリーサル計算への回復反映を検証済み（変更なし）。
- `python scripts/sort_tests.py tests/test_item.py` と `python scripts/generate_test_list.py` を実行し、`python -m pytest tests/ -v` で全2923件（+1 skip）がパスすることを確認。
