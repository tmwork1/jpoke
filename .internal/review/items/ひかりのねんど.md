# ひかりのねんど レビュー結果

## 仕様書
- 変更なし（`.internal/spec/items/ひかりのねんど.md` は一次情報（ポケモンWiki）と内容が一致していることを確認した）

## 実装計画書
- 変更なし（`.internal/plan/items/ひかりのねんど.md` は仕様書の内容と整合しており、`かるいし`（グランドコート）系アイテムと同一の実装パターンで問題ない）

## 実装（handlers/ / data/）
- 変更なし
  - `src/jpoke/data/item.py` の `"ひかりのねんど"` エントリ（`Event.ON_MODIFY_DURATION` / `subject_spec="source:self"` / `fling_power=30`）は仕様・計画と一致
  - `src/jpoke/handlers/item.py` の `ひかりのねんど_resolve_field_count` は `_resolve_field_count(value, "リフレクター", "ひかりのかべ", "オーロラベール", additonal_count=3)` を呼び出しており、対象3種の場状態のみ+3ターン延長する実装で仕様と一致（あついいわ等の天候延長アイテム・グランドコートと同一パターン）
  - `おいかぜ`・`しんぴのまもり` 等は `side.activate()` を使用しており `ON_MODIFY_DURATION` が発火しないため、対象外の場状態が誤って延長されないことも確認した

## テスト
- `tests/test_item.py` に以下を追加
  - `test_ひかりのねんど_対象の壁を8ターンに延長`（リフレクター・ひかりのかべ・オーロラベールをパラメタライズして8ターンに延長されることを確認）
  - `test_ひかりのねんど_非所持では5ターンのまま`（アイテム非所持時は5ターンのまま延長されないことを確認）
  - `test_ひかりのねんど_対象外の場は延長されない`（しんぴのまもり等、対象外の場状態が延長されないことを確認）
  - 既存の `test_ひかりのねんど_スクリーン8ターンに延長`（リフレクターを技経由で8ターンに延長）は変更なし
- `python scripts/sort_tests.py tests/test_item.py` でソート済み
- `python scripts/generate_test_list.py` で `.internal/tests/test_item.md` を更新済み
- `python -m pytest tests/ -v` で全3042件（+1 skipped）がパスすることを確認（ログ: `.loop/test_logs/ひかりのねんど.log`）
