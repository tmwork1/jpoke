# ひかりのこな レビュー結果

## 仕様書
- 変更なし（一次情報と照合し、内容に誤り・欠落なし）

## 実装計画書
- 一撃必殺技（OHKO技）を命中率補正の対象外とする旨をハンドラロジック・テスト観点に追記した
  （一次情報「第三世代以降は一撃必殺技の命中率に影響しない」に対応する記述が欠落していたため）

## 実装（handlers/ / data/）
- `handlers/item.py` の `ひかりのこな_reduce_accuracy` に `ctx.move.has_flag("ohko")` のガードを追加し、
  一撃必殺技には命中率補正が適用されないよう修正した（しょうりのほし・ふくがんの実装パターンに準拠）
- `data/item.py` の登録内容（`Event.ON_MODIFY_ACCURACY`, `subject_spec="defender:self"`）は仕様通りで変更なし
- 必中技（`move.accuracy is None`）はハンドラ発火前に `move_executor.py` 側で必中扱いとなるため、
  ハンドラ側での追加対応は不要（既存の共通処理で仕様を満たす）

## テスト
- `tests/test_item.py` に以下を追加した
  - `test_ひかりのこな_命中率が0_9倍になる`
  - `test_ひかりのこな_非所持では命中率が変化しない`
  - `test_ひかりのこな_一撃必殺技には適用されない`
- 全テスト実行結果: 3037 passed, 1 skipped（`.loop/test_logs/ひかりのこな.log`）
