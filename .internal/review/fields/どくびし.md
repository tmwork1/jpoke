# どくびし レビュー結果

## 仕様書

- 新規作成: `.internal/spec/side_fields/どくびし.md`
  - 効果・無効条件・消滅条件・実装上の注意を記載

## 実装計画書

- 新規作成: `.internal/plan/side_fields/どくびし.md`
  - 既存の `.internal/plan/moves/どくびし.md`（技の設置側）とは別に、サイドフィールド効果側の計画書を作成
  - バグ修正の経緯と正しいチェック順序を明記

## 実装（handlers/ / data/）

- バグ修正: `src/jpoke/handlers/field.py` の `どくびし_poison` 関数
  - **修正前**: `is_hazard_immune` チェックが `has_type("どく")` チェックより先にあったため、あつぞこブーツを持つどくタイプのポケモンがどくびしを消滅させられなかった
  - **修正後**: `is_floating` → `has_type("どく")` → `is_hazard_immune` の順にチェック
  - Wiki 根拠: 「地面にいるどくタイプのポケモンがあつぞこブーツを持っている場合でも場に出たときどくびしを消すことができる。」

## テスト

- 追加: `tests/test_field.py`
  - `test_どくびし_どくタイプはあつぞこブーツでも解除する`: バグ修正を検証
  - `test_どくびし_はがねタイプには効かない`: はがねタイプは毒にならず、フィールドも消滅しない
- ソート: `python scripts/sort_tests.py tests/test_field.py` 実行済み
- 全テスト: 2528 passed, 0 failed, 1 skipped
