# ねがいごと レビュー結果

## 仕様書

- 新規作成: `.internal/spec/fields/ねがいごと.md`
  - ポケモンWiki の記述をもとに以下を文書化
    - 技基本情報（ノーマル/変化/PP10）
    - フィールドの発動タイミング（ON_TURN_END priority=50）
    - 回復量 = 使用者の最大HPの1/2（小数切り捨て）
    - 重複設置失敗条件
    - かいふくふうじによるブロック
    - HP 満タン時は回復しない（Gen5+）
    - まもる・みがわり貫通（場の状態扱いのため）

## 実装計画書

- 新規作成: `.internal/plan/fields/ねがいごと.md`
  - 4 ハンドラの仕様と subject_spec・priority を記載
    1. ねがいごと_can_apply（ON_BEFORE_APPLY_MOVE, priority=100）
    2. ねがいごと_set_side_field（ON_STATUS_HIT, priority=100）
    3. ねがいごと_tick_side_field（ON_TURN_END, priority=50）
    4. ねがいごと_heal（ON_FIELD_DEACTIVATE, priority=100）

## 実装（handlers/ / data/）

- 変更なし
- 実装は仕様書と一致している
  - count=2 で設置し、2 回目の ON_TURN_END で heal が発動する流れが正しい
  - ON_FIELD_DEACTIVATE 経由で heal することで、かいふくふうじ(ON_MODIFY_HEAL)が自動的に機能する
  - ctx.source は Player を registered_subject として解決されるため、常に現在の場のポケモンが対象となる

## テスト

- `test_ねがいごと_回復と解除`: `mon.hp = 1`（直接代入）を `battle.modify_hp(mon, v=-(mon.hp - 1))` に修正
- 新規追加: `test_ねがいごと_かいふくふうじ中は回復しない`
  - volatile0={"かいふくふうじ": 3} でセットアップし、2 ターン後も HP が変化しないことを確認
- `python scripts/sort_tests.py tests/test_field.py` でソート済み
- `python scripts/generate_test_list.py` で .internal/tests/ を更新済み
- 全 2534 テスト (0 failed) パス確認済み
