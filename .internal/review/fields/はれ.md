# はれ レビュー結果

## 仕様書

- 新規作成: `.internal/spec/fields/はれ.md`
  - 効果一覧（ON_CALC_POWER_MODIFIER でほのお1.5倍・みず0.5倍、ON_BEFORE_APPLY_AILMENT でこおり防止）を記載
  - ばんのうがさとの相互作用（防御側持ちは補正無効・攻撃側持ちは通常通り）を記載
  - ON_TURN_END priority=10（.internal/spec/turn.md 根拠）を記載

## 実装計画書

- 新規作成: `.internal/plan/fields/はれ.md`
  - はれ_power_modifier, はれ_prevent_freeze, tick_weather の subject_spec・priority を明記
  - おおひでりと共有していることを明記

## 実装（handlers/ / data/）

- 変更なし
- `handlers/field.py` の `はれ_power_modifier` / `はれ_prevent_freeze` は仕様書と一致
  - `battle.weather_for(ctx.defender).name == ""` で防御側ばんのうがさをチェック ✓
  - subject_spec: power_modifier は `attacker:self`、prevent_freeze は `target:self` ✓
- `data/field.py` の `"はれ"` FieldData 登録は仕様書と一致

## テスト

- `test_あめはれ_ばんのうがさ防御側は補正なし` を新規追加（あめエージェントが追加）
  - はれ時ほのお技補正なし（ばんのうがさ防御側）✓
  - はれ時みず技補正なし（ばんのうがさ防御側）✓
- 全 1894 テスト通過確認
