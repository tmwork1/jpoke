# しんぴのまもり レビュー結果

## 仕様書
- 新規作成: `.internal/spec/fields/しんぴのまもり.md`
  - 効果（状態異常・こんらん・ねむけを防ぐ）を記載
  - 防がないもの（メロメロ・ふういん・ほろびのうた・ねむる自己付与）を明記
  - 発生・消滅条件・特性との相互作用・イベント優先度を記載

## 実装計画書
- 新規作成: `.internal/plan/fields/しんぴのまもり.md`
  - 3つのハンドラ（prevent_ailment・prevent_confusion・tick）の仕様を明記
  - is_foe_target() チェックの必要性を明記
  - ON_TURN_END の priority=130 を明記

## 実装（handlers/ / data/）
- **バグ修正**: `data/field/side_field.py` の `しんぴのまもり` ON_TURN_END priority を 10 → 130 に修正
  - `.internal/spec/turn.md` の priority=130 と一致していなかった
- **バグ修正**: `handlers/field.py` の `しんぴのまもり_prevent_ailment` に `ctx.is_foe_target()` チェックを追加
  - 自分が使う「ねむる」（source == target）による自己付与のねむりを誤ってブロックしていた
- **バグ修正**: `handlers/field.py` の `しんぴのまもり_prevent_confusion` に `ctx.is_foe_target()` チェックを追加
  - 同様に自己付与によるこんらん・ねむけが誤ってブロックされる問題を修正
- `scripts/sort_handlers.py` を実行して五十音順を確認

## テスト
- 追加: `test_しんぴのまもり_ねむけ防止` — ねむけ付与が防がれることを確認
- 追加: `test_しんぴのまもり_ねむるは防がない` — source==target の自己付与（ねむる）は防がれないことを確認
- `scripts/sort_tests.py` でソート済み
- `python -m pytest tests/ -v` 全 2525 件パス（0 失敗）
