# サイコフィールド レビュー結果

## 仕様書

- 新規作成（`.internal/spec/fields/サイコフィールド.md`）
  - wiki の内容を元に、エスパー技威力1.3倍（接地攻撃者）と優先度+1以上技無効（接地防御者）の2効果を文書化
  - 先制技無効化の詳細仕様（いたずらごころ・はやてのつばさ適用技も対象、せんせいのツメ等は対象外）を記載
  - ハンドラ優先度の根拠（turn.md 参照）を記載

## 実装計画書

- 新規作成（`.internal/plan/fields/サイコフィールド.md`）
  - 各ハンドラの subject_spec・priority・ロジックを文書化
  - テスト一覧を記載

## 実装（handlers/ / data/）

- 変更なし
- `handlers/field.py` の `サイコフィールド_power_modifier` および `サイコフィールド_block_priority_move` は仕様通り実装済み
- `data/field/terrain.py` の TERRAIN["サイコフィールド"] エントリは仕様通り登録済み
- 各 subject_spec の整合性確認:
  - `サイコフィールド_power_modifier`: `"attacker:self"` ✓（AttackContext の attacker ロール）
  - `サイコフィールド_block_priority_move`: `"defender:self"` ✓（AttackContext の defender ロール）
  - `tick_terrain`: `"source:self"` ✓（EventContext の source ロール）

## テスト

- `test_サイコフィールド_優先度0技は有効` を追加
  - 優先度0の技（たいあたり）が接地防御者に対して無効化されないことを確認
  - `block_priority_move` の `priority > 0` 条件が正しく機能することを保証
- 追加後 `python scripts/sort_tests.py tests/test_field.py` でソート済み
- `python scripts/generate_test_list.py` でテスト一覧更新済み
- `python -m pytest tests/test_field.py -v` で全136件パス確認済み
