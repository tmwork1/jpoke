# おおひでり レビュー記録

## レビュー日: 2026-06-21

## 確認結果: 合格

## 実装レビュー

### handlers/field.py

- `おおひでり_block_move`: みずタイプ技のとき `LogCode.MOVE_FAILED` を記録し `HandlerReturn(value=False, stop_event=True)` を返す — 正しい
- `はれ_power_modifier`: おおひでりと共有。ばんのうがさチェック（`battle.weather_for(ctx.defender).name == ""`）を含む — 正しい
- `はれ_prevent_freeze`: おおひでりと共有 — 正しい

### data/field.py

- `"おおひでり"` の handlers に以下が登録されている:
  - `ON_CALC_POWER_MODIFIER`: `はれ_power_modifier`（subject_spec: `attacker:self`）
  - `ON_BEFORE_APPLY_AILMENT`: `はれ_prevent_freeze`（subject_spec: `target:self`）
  - `ON_TRY_MOVE_1`: `おおひでり_block_move`（priority=10, subject_spec: `attacker:self`）
- ON_TURN_END ハンドラなし（カウントダウンなし）— 正しい

### priority 確認

- ON_TRY_MOVE_1 の priority=10 は `.internal/spec/turn.md` の「おおあめ・おおひでりによる技失敗 Priority 10」と一致 — 正しい

### 天候優先度

- `WEATHER_PRIORITY["おおひでり"] = 1`、`WEATHER_PRIORITY["らんきりゅう"] = 2`
- `WeatherManager.apply` は `new_priority >= current_priority` のとき上書きを許可
  - 通常天候（priority 0）はおおひでり（priority 1）を上書きできない — 正しい
  - おおあめ（priority 1）はおおひでり（priority 1）を上書きできる（>=比較）— 正しい
  - らんきりゅう（priority 2）はおおひでりを上書きできる — 正しい

## テストレビュー

### 確認済みテスト（tests/test_field.py）

| テスト | 確認内容 |
|--------|----------|
| `test_強天候_同タイプ技は威力強化` | おおひでり+ほのお技が6144（1.5倍）になることを確認 |
| `test_強天候_反対タイプ技は失敗` | おおひでり+みずでっぽう/みずびたし（攻撃技・変化技）が失敗することを確認 |
| `test_強天候_同タイプ技はブロックされない` | おおひでり+ほのお技がブロックされないことを確認 |
| `test_強天候中は通常天候で上書きできない` | おおひでり中にはれを発動しても上書きされないことを確認 |
| `test_強天候同士は上書きできる` | おおひでり中におおあめを発動すると上書きされることを確認 |
| `test_らんきりゅう_全天候を上書き` | らんきりゅうがおおひでりを上書きできることを確認 |
| `test_はれ_こおり防止` | はれ経由でおおひでりのこおり防止ロジックを間接確認 |

### テスト実行結果

`python -m pytest tests/ -v` → **1893 passed**（失敗なし）

## 新規作成ファイル

- `.internal/spec/fields/おおひでり.md` — 仕様書（新規作成）
- `.internal/plan/fields/おおひでり.md` — 計画書（新規作成）

## 修正内容

実装・テストに誤りや欠落はなかった。仕様書・計画書が存在しなかったため新規作成した。
