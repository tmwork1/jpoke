# しろいきり レビュー結果

## 仕様書

- 新規作成（`.internal/spec/fields/しろいきり.md`）
  - wiki から概要・効果・詳細仕様・発動/解除条件・優先度を整理
  - 防げる例／防げない例を明示
  - ON_BEFORE_MODIFY_STAT の priority が 130 であることを明記

## 実装計画書

- 新規作成（`.internal/plan/fields/しろいきり.md`）
  - 2つのハンドラ（しろいきり_prevent_stat_drop / しろいきり_tick_side_field）を記録
  - 各ハンドラの subject_spec・priority・ロジックを仕様書から導出して記載

## 実装（handlers/ / data/）

- `data/field/side_field.py` の `しろいきり` エントリ修正
  - `ON_BEFORE_MODIFY_STAT` ハンドラの priority が 100（デフォルト）→ 130 に変更
  - 根拠: `.internal/spec/turn.md`「しろいきりによるランク補正下げ無効」Priority 130

## テスト

- `tests/test_field.py` にテストを1件追加
  - `test_しろいきり_すりぬけは能力低下防止を貫通する`: すりぬけ特性を持つポケモンによる能力低下はしろいきりで防げないことを確認
- `python scripts/sort_tests.py tests/test_field.py` でソート済み
- `python scripts/generate_test_list.py` で .internal/tests/ 更新済み
- `python -m pytest tests/ -v`: 2523 passed, 0 failed, 1 skipped
