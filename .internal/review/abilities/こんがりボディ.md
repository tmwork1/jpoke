# こんがりボディ レビュー結果

## 仕様書
- 変更なし。一次情報（.internal/wiki/abilities/こんがりボディ.html）と照合したが、既存の `.internal/spec/abilities/こんがりボディ.md` の内容（効果・特性の仕様・備考）は一致しており誤り・欠落はなかった。

## 実装計画書
- 新規作成。`.internal/progress/ability.md` では計画書列が `x` だったが、実際には `.internal/plan/abilities/こんがりボディ.md` が存在しなかった（進捗表と実体の不整合）。仕様書ともらいび系の既存実装パターンを基に新規作成した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/ability.py` の `こんがりボディ_absorb_fire` は共通ヘルパー `_apply_type_absorb(move_type="ほのお", stats={"def": 2})` を使用しており、`src/jpoke/data/ability.py` では `Event.ON_BEFORE_APPLY_MOVE`（`subject_spec="defender:self"`）に登録され、`mold_breaker_ignorable` フラグも設定済みだった。以下の仕様を満たすことをコードとイベント発火順序（`move_executor.py`）から確認した。
  - 単体対象技（`move.target == "foe"`）にのみ発動し、自分対象技・場対象技には発動しない。
  - `ON_BEFORE_APPLY_MOVE` は `ON_TRY_MOVE_1`（まもるの判定）より後、ダメージ適用（みがわり処理含む）より前に発火するため、「まもるで防がれたときは発動しない」「みがわり状態でも発動する」を自然に満たす。
  - `mold_breaker_ignorable` によりかたやぶり相手には無効化されない。
- 修正は不要だった。

## リーサル計算
- 対象外（`.internal/progress/ability.md` のリーサル実装列は `n/a`）。ダメージ無効化とぼうぎょランク上昇のみの効果であり、こんがりボディ自体が相手ポケモンを倒す・耐えるかの判定には関与しないため。

## テスト
- `tests/abilities/test_ability_ka.py` に以下のテストを追加した。
  - `test_こんがりボディ_かたやぶり相手には無効化されずダメージを受ける`
  - `test_こんがりボディ_ほのおタイプの変化技も無効化してぼうぎょが2段階上がる`（おにびで確認）
  - `test_こんがりボディ_まもるで防がれたときは発動しない`
  - `test_こんがりボディ_みがわり状態でも発動してぼうぎょが上がる`
  - `test_こんがりボディ_自分対象技では発動しない`（かえんのまもりで確認）
- 既存の `tests/abilities/test_ability__group.py` のパラメータ化テスト（無効化＋ぼうぎょ+2段階、かたやぶりで無効化される系）は変更なし。
- `python -m pytest tests/ -v` を実行し、4846 passed, 1 skipped で全件成功を確認した（ログ: `.loop/test_logs/こんがりボディ.log`）。
