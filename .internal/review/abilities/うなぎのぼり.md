# うなぎのぼり レビュー結果

## 仕様書
- 新規作成（`.internal/spec/abilities/うなぎのぼり.md`）。
- 一次情報（ポケモンWiki）で、メガシビルドン専用の特性として実在し、対象範囲（Championsシングル）にも
  含まれることを確認した。
- 効果は「ふゆう（地面にいない扱い）」と「ビーストブースト（攻撃技で相手を倒すと最も実数値が高い能力が
  1段階上がる）」を組み合わせた特性であることを明記した。

## 実装計画書
- 新規作成（`.internal/plan/abilities/うなぎのぼり.md`）。
- 効果1・効果2とも既存ハンドラ（`ふゆう_float` / `ビーストブースト_boost_best_stat_on_ko`）と完全に同一の
  挙動のため、新規ハンドラ関数は追加せず、`data/ability.py` から既存関数をそのまま再利用する方針とした
  （`ファントムガード` が `マルチスケイル_reduce_damage` を再利用している既存パターンに倣った）。

## 実装（handlers/ / data/）
- `src/jpoke/data/ability.py` の `"うなぎのぼり": AbilityData()`（空定義）を、以下を持つ定義に修正した。
  - `flags={"mold_breaker_ignorable"}`（ふゆうと同様、かたやぶりの技を受けるときは地面にいる扱いになるため）
  - `Event.ON_CHECK_FLOATING`: `h.ふゆう_float`, `subject_spec="source:self"`
  - `Event.ON_MOVE_KO`: `h.ビーストブースト_boost_best_stat_on_ko`, `subject_spec="attacker:self"`
- `handlers/ability.py` は変更なし（既存関数をそのまま再利用したため新規実装は不要）。

## リーサル計算
- 対象外（`.internal/progress/ability.md` のリーサル実装列は `n/a`）。
  同種の特性であるふゆう・ビーストブースト・じしんかじょうもいずれも `n/a` であり、本特性も対象外として
  据え置いた。

## テスト
- `tests/abilities/test_ability_a.py` に以下を追加した（ふゆう・ビーストブーストの既存テストに倣った内容）。
  - `test_うなぎのぼり_かたやぶりでじめん技が通る`
  - `test_うなぎのぼり_じめん技が通らない`
  - `test_うなぎのぼり_浮いている`
  - `test_うなぎのぼり_倒すと最高実数値の能力が上がる`（パラメタライズ、こうげき/すばやさが最高値のケース）
- `python -m pytest tests/ -v` を実行し、4708 passed, 1 skipped（既存分）で全件成功を確認した。
  ログは `.loop/test_logs/うなぎのぼり.log` に保存。
