# サイコメイカー レビュー結果

## 仕様書
- 変更なし。一次情報（.internal/wiki/abilities/サイコメイカー.html）と照合し、
  「ポケモンの特性がサイコメイカーに書き換わった場合、即座に特性が発動する」を
  含め既存記述に誤り・欠落はなかった。

## 実装計画書
- 新規作成（`.internal/plan/abilities/サイコメイカー.md`）。同種のグラスメイカー/
  エレキメイカーの計画書を踏襲し、`Event.ON_SWITCH_IN` と
  `Event.ON_ABILITY_ENABLED` の2イベントで `_activate_terrain` を呼び出す方針を明記した。

## 実装（handlers/ / data/）
- `src/jpoke/data/ability.py` の `サイコメイカー` に `Event.ON_ABILITY_ENABLED`
  ハンドラの登録が漏れていたため追加した（`ON_SWITCH_IN` のみ登録されており、
  スキルスワップ・トレース・かがくへんかガス解除等で特性がサイコメイカーに
  書き換わった際に即座にサイコフィールドが展開されないバグがあった）。
  同種特性グラスメイカー/エレキメイカー/ハドロンエンジンでは既に対応済みで、
  今回のレビューで指摘通りサイコメイカーにも同様の登録漏れが見つかった。
- `src/jpoke/handlers/ability.py` の `サイコメイカー_activate_terrain` は
  既存の共通ヘルパー `_activate_terrain` を使っており、`ON_ABILITY_ENABLED`
  でもそのまま利用可能なため変更不要。

## リーサル計算
- 対象外（`.internal/progress/ability.md` のリーサル実装列が `n/a`）。

## テスト
- `tests/abilities/test_ability_sa.py` に
  `test_サイコメイカー_特性再有効化時にも発動する` を追加。
  かがくへんかガスで特性が無効化された状態から解除されたとき、
  サイコフィールドが即座に展開されることを確認する
  （グラスメイカー/エレキメイカーの同種テストに準拠）。
- `python -m pytest tests/ -v` 実行結果: 4851 passed, 1 skipped
  （ログ: `.loop/test_logs/サイコメイカー.log`）。自身の変更に起因する失敗はなし。
