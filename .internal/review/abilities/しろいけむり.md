# しろいけむり レビュー結果

## 仕様書
- 変更なし。一次情報（.internal/wiki/abilities/しろいけむり.html）と照合し、バトルでの効果（相手からランク補正を下げられない）・フィールドでの効果・備考・関連項目のいずれも一致していることを確認した。

## 実装計画書
- `.internal/plan/abilities/しろいけむり.md` を全面的に書き直した。旧版は「クリアボディと同一のハンドラ関数を登録するか、共通ヘルパー関数を呼び出す形で実装する」という抽象的な記述に留まっていたが、実際の実装（クリアボディ/メタルプロテクトと共通のハンドラ関数 `クリアボディ_block_stat_drop` を共有し、`AbilityData.flags` に `mold_breaker_ignorable` を付与してかたやぶり判定を委ねる方式）を正確に反映した、レビュー済みの `クリアボディ.md` と同じ書式（更新日・仕様参照・実装ゴール・実装方針・変更候補・テスト観点）に揃えた。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/ability.py` の `しろいけむり` エントリは `flags={"mold_breaker_ignorable"}` と `ON_BEFORE_MODIFY_STAT`（`subject_spec="target:self"`）ハンドラに `h.クリアボディ_block_stat_drop` を登録しており、クリアボディ・メタルプロテクトと同一の確立済みパターンに従っていることを確認した。`クリアボディ_block_stat_drop` は共通ヘルパー `_block_stat_drop_by_foe` 経由で相手由来・低下方向の項目のみを除去しており、仕様（相手からのランク低下のみ防ぐ、自分由来は防げない）と一致する。

## リーサル計算
- 対象外（`.internal/progress/ability.md` の「リーサル実装」列が `n/a`）。

## テスト
- `tests/abilities/test_ability_sa.py` にしろいけむり本体のテストが存在しなかったため、クリアボディの既存テスト（`test_クリアボディ_かたやぶりで無効` / `test_クリアボディ_能力低下を防ぐ` / `test_クリアボディ_自己低下は防げない`）と同型の3件を新規追加した。
  - `test_しろいけむり_かたやぶりで無効`: かたやぶり持ちの相手のなきごえでこうげきランクが下がることを確認。
  - `test_しろいけむり_能力低下を防ぐ`: 相手由来の能力変化で低下方向のみが除去されることを確認。
  - `test_しろいけむり_自己低下は防げない`: 自分由来の能力変化はすべて適用されることを確認。
  - `python -m pytest tests/ -v` で全4917件（+1 skipped）がパスすることを確認した。
