# どくのトゲ レビュー結果

## 仕様書
- 変更なし。`.internal/spec/abilities/どくのトゲ.md` は既存。一次情報（`.internal/wiki/abilities/どくのトゲ.html`）
  と照合し、効果・特性の仕様（発動除外条件・しんぴのまもりの世代差・連続攻撃技の扱い等）・
  備考の記述に誤り・欠落がないことを確認した。

## 実装計画書
- 新規作成した（`.internal/plan/abilities/どくのトゲ.md`）。`.internal/progress/ability.md` では
  計画書列が `x` になっていたが、実ファイルは存在しなかった。仕様書をもとに、
  `Event.ON_HIT` + `ctx.substitute_damage` 判定によるみがわり除外という実装方針
  （後述のバグ修正内容）を明記して作成した。

## 実装（handlers/ / data/）
- 修正した。`どくのトゲ`はどくげしょう・どくのくさりの直近レビューと同じ「`Event.ON_DAMAGE_HIT`は
  `actual_damage<=0`のとき発火しない」バグパターンに該当していた。
  - `src/jpoke/data/ability.py`: `どくのトゲ` の登録イベントを `Event.ON_DAMAGE_HIT` から
    `Event.ON_HIT` に変更した（`src/jpoke/core/move_executor.py` の `_execute_hit` で
    `actual_damage<=0` のとき `ON_DAMAGE_HIT` が発火しないことを確認済み）。どくのトゲは
    どくげしょう・どくのくさりと同じ「直接攻撃を受けたときに発動するとくせい」の構造
    （`subject_spec="defender:self"`）であり、こらえるでHP1のまま耐えたときやみねうちを
    受けたとき（実HPダメージ0）にも発動すべきところ、修正前は発動していなかった（バグ）。
  - `src/jpoke/handlers/ability.py` の `どくのトゲ_maybe_poison_attacker` に
    `ctx.substitute_damage` のチェックを追加した（`Event.ON_HIT` はみがわり被弾時にも発火する
    ため、みがわりに阻まれたとき発動しないようハンドラ内で明示的に除外する必要がある）。
    それ以外（接触判定・ぼうごパット除外・状態異常付与）は既存の共通ヘルパー
    `_apply_contact_counter_ailment`（`battle.query.is_contact_reaction` /
    `battle.ailment_manager.apply`）に委ねる実装のまま変更していない。

## リーサル計算
- 対象外（`.internal/progress/ability.md` の「リーサル実装」「リーサルテスト」列はともに `n/a`）。

## テスト
- 追加した（`tests/abilities/test_ability_ta.py`）。
  - `test_どくのトゲ_こらえるでHP1のまま耐えたときも発動する`
    — 修正前のバグ（`ON_DAMAGE_HIT` では発火しない）を検出するための回帰テスト。
  - `test_どくのトゲ_みがわりに阻まれたときは発動しない`
    — 仕様「攻撃技をみがわり状態で防いだときは発動しない」の確認。
  - 既存の `tests/abilities/test_ability__group.py` の `CONTACT_AILMENT_CASES`
    （せいでんき・どくのトゲ・ほのおのからだを共通パラメタライズ）の2件
    （接触技で状態異常付与／非接触技では発動しない）は変更なしで引き続きパスすることを確認した。
- `python -m pytest tests/ -v` を実行し、5050件成功・1件skippedで全件パスすることを確認した。
  `.loop/test_logs/どくのトゲ.log` に保存した。
