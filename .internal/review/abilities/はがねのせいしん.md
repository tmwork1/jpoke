# はがねのせいしん レビュー結果

## 仕様書
- 変更なし。既存の `.internal/spec/abilities/はがねのせいしん.md` を一次情報（ポケモンWiki
  `.internal/wiki/abilities/はがねのせいしん.html`）と照合したが、効果・特性の仕様・備考ともに
  記載内容は正確で欠落もなかった。

## 実装計画書
- 新規作成（`.internal/plan/abilities/はがねのせいしん.md` が存在しなかったため）。
- `Event.ON_CALC_POWER_MODIFIER` / `subject_spec="attacker:self"` / `_modify_by_move_condition`
  を用いた既存実装（はがねつかい・トランジスタと同系列パターン）を計画として明文化した。
- 一次情報にある「味方複数所持による累積」は、本プロジェクトの対象がシングルバトルのみ
  （CLAUDE.md）であり自分の場には常に1匹しかいないため実質「自分自身」と等価になり
  テスト不能である旨を明記した。
- はめつのねがい（はがねタイプ技）への効果が、`ON_MOVE_CHARGE` 時点で `roll_damage` により
  攻撃者=特性所持者自身としてダメージが確定するため、追加実装なしに反映されることを明記した。

## 実装（handlers/ / data/）
- 変更なし。
- `src/jpoke/data/ability.py` の `はがねのせいしん` エントリ（`Event.ON_CALC_POWER_MODIFIER`,
  `subject_spec="attacker:self"`）と `src/jpoke/handlers/ability.py` の
  `はがねのせいしん_modify_power`（`_modify_by_move_condition(ctx.move, value, modifier=6144,
  move_type="はがね")`）を仕様書・計画書と照合したが、誤り・欠落は見つからなかった。
- タイプ変更技によりタイプが変わった場合も `ctx.move.type` を参照するため対応済み。
- モールドブレイカー／かがくへんかガス関連のチェックはハンドラ側に存在しないが、これは
  フレームワーク側で一括処理される設計のため正しい（本特性はかたやぶりで無効化される
  対象の効果ではない）。

## リーサル計算
- 対象外（`.internal/progress/ability.md` の「リーサル実装」「リーサルテスト」列がともに n/a）。

## テスト
- 追加。`tests/abilities/test_ability_ha.py` に以下の3件を新規追加した（既存実装には
  はがねのせいしん専用のテストが一件も存在しなかったため）。
  - `test_はがねのせいしん_はがね技の威力が1_5倍になる`: `power_modifier == 6144` を確認。
  - `test_はがねのせいしん_はがね以外の技は威力補正なし`: `power_modifier == 4096` のまま
    変化しないことを確認。
  - `test_はがねのせいしん_はめつのねがいの威力も上がる`: `ON_MOVE_CHARGE` 時点の
    `power_modifier == 6144` を確認（はめつのねがいへの効果反映を検証）。
- `python -m pytest tests/ -v` を2回連続で実行し、いずれも 5128 passed, 1 skipped で
  再現性のある結果となった。flaky なテストは確認されなかった。
- テスト結果ログ: `.loop/test_logs/はがねのせいしん.log`
