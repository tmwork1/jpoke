# えんかく レビュー結果

## 仕様書
- 変更なし（一次情報と照合し、記載内容に誤り・欠落なし）

## 実装計画書
- 変更なし（`Event.ON_CHECK_CONTACT` を用いる方針は既存の `パンチグローブ`（アイテム）実装と一致しており妥当）

## 実装（handlers/ / data/）
- 変更なし
  - `handlers/ability.py` の `えんかく_nullify_contact` は `ON_CHECK_CONTACT` で `False` を返す実装で、
    `query.is_contact()` / `is_contact_reaction()` の両方に反映されるため、わるいてぐせ（自分側判定）・
    もふもふ／さめはだ等の接触反応効果（相手側判定）の双方を正しく無効化できている。
  - `subject_spec="attacker:self"` は `AttackContext` のロールと一致しており問題なし。
  - かがくへんかガス中の無効化は特性ハンドラの汎用無効化機構に委譲されており、ハンドラ内に
    個別のガス／かたやぶりチェックを書いていない点も CLAUDE.md の方針と整合している。

## リーサル計算
- 対象外（`.internal/progress/ability.md` のリーサル実装列は `n/a`。えんかくはダメージ量やHP増減に
  影響しないため、リーサル計算への影響なし）

## テスト
- `tests/abilities/test_ability_a.py` に `test_えんかく_直接攻撃でもふもふのダメージ半減が発動しない` を追加した。
  既存テスト（さめはだ・わるいてぐせ無効化）に加えて、もふもふによる接触ダメージ半減も無効化される
  ことを確認するテストを補った。
- 既存の `test_えんかく_直接攻撃でさめはだが発動しない` / `test_えんかく_直接攻撃でわるいてぐせが発動しない` は
  実装と一致しており修正不要。
- `python -m pytest tests/ -v` は全件パス（4729 passed, 1 skipped）。
  （途中 `test_リフレクター_ワンダールーム中でも物理技を軽減する` が急所発生の乱数により1回だけ
  失敗したが、再実行で成功しておりえんかくの変更とは無関係のフレーキーテストであることを確認した）
