# エレキメイカー レビュー結果

## 仕様書
- 変更なし。`.internal/spec/abilities/エレキメイカー.md` は一次情報（`.internal/wiki/abilities/エレキメイカー.html`）と照合し、内容・記載漏れともに問題なし。

## 実装計画書
- 新規作成。`.internal/plan/abilities/エレキメイカー.md` が存在しなかったため、仕様書と実装（`ON_SWITCH_IN` / `ON_ABILITY_ENABLED`）をもとに新規作成した。

## 実装（handlers/ / data/）
- 修正した。`data/ability.py` の `エレキメイカー` に `Event.ON_ABILITY_ENABLED` のハンドラ登録が欠落していた（同スペック内「ポケモンの特性がエレキメイカーに書き換わった場合、即座に特性が発動する」に対応する実装が漏れていた）。
  - 同種の天候始動特性（`あめふらし`/`ひでり`/`すなおこし`/`ゆきふらし`）や `ハドロンエンジン` は `ON_SWITCH_IN` と `ON_ABILITY_ENABLED` の両方を登録済みだったが、フィールド始動特性4種（エレキメイカー/グラスメイカー/サイコメイカー/ミストメイカー）はいずれも `ON_SWITCH_IN` のみで、スキルスワップ・トレース・かがくへんかガス解除等での即時発動に対応していなかった。
  - 本レビューでは対象の `エレキメイカー` のみ修正（他の3特性は対象外のため今回は変更していない）。
  - 既存の `エレキメイカー_activate_terrain` 関数（`handlers/ability.py`）はそのまま両イベントで共用可能なため、関数自体の変更は不要だった。

## リーサル計算
- 対象外（`.internal/progress/ability.md` の「リーサル実装」列が `n/a`）。

## テスト
- 追加した。`tests/abilities/test_ability_a.py` に `test_エレキメイカー_特性再有効化時にも発動する` を追加し、かがくへんかガスによる無効化解除時に `ON_ABILITY_ENABLED` 経由でエレキフィールドが即時展開されることを確認した（`おもかげやどし_特性再有効化時にも発動する` と同じパターンを使用）。
- `python -m pytest tests/ -v` は 4729 passed, 1 skipped（本レビューと無関係な `test_むしくい_ねんちゃく持ちをひんしにさせた場合は奪える` はまひの完全麻痺乱数を固定していないため稀に失敗するフレーキーテストで、単独再実行では通過することを確認済み。今回の変更とは無関係のため修正対象外とした）。
