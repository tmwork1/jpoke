# はっこう レビュー結果

## 仕様書
- 変更なし。一次情報（`.internal/wiki/abilities/はっこう.html`）と照合したが、既存の
  `.internal/spec/abilities/はっこう.md` は効果（命中率ランク低下無効・相手の回避率ランク無視、
  第九世代からするどいめと同一効果になった経緯、フィールド効果、備考）を過不足なく反映していた。

## 実装計画書
- 修正した。既存の計画書は「命中率（ACC）を下げられない」効果のみを対象としており、仕様書に
  明記されている「相手の回避率ランクを無視して攻撃できる」効果（第九世代からするどいめと同一効果）が
  実装ゴール・実装方針から丸ごと欠落していた。するどいめの実装（`するどいめ_block_ACC_drop` /
  `するどいめ_ignore_evasion`）と対になるよう、以下を追記・修正した。
  - 実装ゴールに回避率ランク無視の項目を追加
  - ACC↓無効化は「相手由来のみ」無効化する旨を明記（`_block_stat_drop_by_foe` を使う方針に修正）
  - `Event.ON_GET_STAT_RANK` / `subject_spec="attacker:self"` による回避率無視ハンドラの追加を明記
  - テスト観点・疑似コードを更新

## 実装（handlers/ / data/）
- 修正した。
  - `src/jpoke/handlers/ability.py`
    - `はっこう_block_acc_drop`: 独自の dict フィルタ実装（相手由来かどうかを判定していなかった）を、
      するどいめと同じ共通ヘルパー `_block_stat_drop_by_foe(value, ctx, "accuracy")` を使う実装に修正。
    - `はっこう_ignore_evasion`（新規追加）: `するどいめ_ignore_evasion` と同一の実装で
      `value["evasion"] = 0` として相手の回避率ランクを無視する。
  - `src/jpoke/data/ability.py`
    - `はっこう` のハンドラ登録に `Event.ON_GET_STAT_RANK`
      （`はっこう_ignore_evasion`, `subject_spec="attacker:self"`) を追加登録。
      従来は `Event.ON_BEFORE_MODIFY_STAT` の命中率低下無効化のみが登録されており、
      仕様書に明記された「相手の回避率ランクを無視して攻撃できる」効果が未実装だった。
  - `flags={"mold_breaker_ignorable"}` は既に設定済みで変更なし。

## リーサル計算
- 対象外（`.internal/progress/ability.md` の「リーサル実装」列が `n/a`）。

## テスト
- 追加した。`tests/abilities/test_ability_ha.py`
  - `test_はっこう_相手の回避率ランクを無視する`（するどいめの同名テストに準拠、evasion+6でも命中率が変化しないことを確認）
  - `test_はっこう_相手の回避率ランク低下も無視する`（evasion-1でも命中率を上げないことを確認）
  - 既存の `test_はっこう_かたやぶりで無効` / `test_はっこう_命中率低下を防ぐ` は変更なし（実装修正後も従来通り通過することを確認済み）。
- `python -m pytest tests/ -v` を実行し、5136 passed, 1 skipped で全テスト成功。結果は
  `.loop/test_logs/はっこう.log` に保存。再実行によるflaky挙動は確認されなかった。
