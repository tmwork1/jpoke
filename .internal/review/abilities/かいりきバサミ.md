# かいりきバサミ レビュー結果

## 仕様書
- 変更なし（一次情報（.internal/wiki/abilities/かいりきバサミ.html）と内容が完全一致していることを確認）

## 実装計画書
- テスト観点の番号付けの誤り（3の次が5になっていた）を4に修正
- 変更候補ファイルパス `tests/test_ability.py`（現存しない旧パス）を `tests/abilities/test_ability_ka.py` に修正

## 実装（handlers/ / data/）
- 変更なし
  - `ON_BEFORE_MODIFY_STAT` + `_block_stat_drop_by_foe(value, ctx, "atk")` により、
    相手（source != target）由来のこうげきランク低下のみを無効化する実装を確認。
    自己効果（ばかぢから等、source == target）は無効化されない。
  - `mold_breaker_ignorable` フラグによりかたやぶり無視が正しく機能する。
  - はとむね・するどいめ・クリアボディと同一の共通関数 `_block_stat_drop_by_foe` を用いた
    実装パターンで統一されており、仕様（ばかぢから等の自己効果は防げない、味方からの低下も防ぐ、
    かたやぶりには無視される 等）と整合している。
  - ビビリだま側の優先度（priority=50）が `ON_BEFORE_MODIFY_STAT` の既定優先度より先に
    判定されるため、「いかくをかいりきバサミで防いだときもビビリだまは発動する」仕様も
    正しく再現されることを確認した。

## リーサル計算
- 対象外（`.internal/progress/ability.md` の「リーサル実装」列が `n/a`）

## テスト
- 変更なし
  - `tests/abilities/test_ability__group.py::test_能力低下防止特性_param` で、
    相手由来のこうげきランク低下を防ぐケースと、自己効果（source_is_self）では
    防がれないケースの両方を確認済み。
  - かたやぶり無視・ビビリだま連携（いかくを防いでもビビリだま発動）は
    クリアボディ側の既存テスト（共通実装のため）でカバー済みであることを確認した。
  - `python -m pytest tests/ -v` 実行結果: 4739 passed, 1 skipped
