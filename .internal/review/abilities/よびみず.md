# よびみず レビュー結果

## 仕様書
- 変更なし（一次情報 https://wiki.pokemonwiki.com/wiki/よびみず をダウンロードして全文照合し、既存の `.internal/spec/abilities/よびみず.md` の記述と完全に一致していることを確認した）。

## 実装計画書
- 新規作成（`.internal/plan/abilities/よびみず.md` が存在しなかったため、仕様書と既存実装・既存の同系特性（ひらいしん・ちょすい）のレビュー結果を踏まえて新規作成した）。
  - 共通ヘルパー `_apply_type_absorb()` を利用した実装方針、`Event.ON_BEFORE_APPLY_MOVE` の登録根拠、`mold_breaker_ignorable` フラグの根拠を記載。
  - みがわり状態の変化技に対する既知の制約、いたずらごころ+あくタイプ無効化との優先度タイブレークに関する既知の制約（ちょすい・ひらいしんと共通）を記載。
  - 技を引き寄せる効果（ターゲット変更）は本プロジェクトがシングルバトル専用のため対象外（発生しえない）である旨を明記。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/ability.py` の `よびみず_absorb_water`（共通ヘルパー `_apply_type_absorb(move_type="みず", stats={"spa": 1})` を利用）、`src/jpoke/data/ability.py` の `よびみず` エントリ（`flags={"mold_breaker_ignorable"}`, `Event.ON_BEFORE_APPLY_MOVE` に `subject_spec="defender:self"` で登録）は仕様書・計画書と整合しており、修正不要と判断した。

## リーサル計算
- 対象外（`.internal/progress/ability.md` の よびみず 行は「リーサル実装」「リーサルテスト」ともに `n/a`）。

## テスト
- `tests/abilities/test_ability__group.py::test_タイプ無効バフ系_かたやぶりで無効` のよびみず行が技に `でんきショック`（電気技）を指定しており、よびみずが本来反応しないタイプのためかたやぶりの貫通効果を実質的に検証できていなかったバグを修正（`みずでっぽう` に変更）。
- `tests/abilities/test_ability_yarawa.py` によびみず専用テストを新規追加（9件）:
  - 場を対象とする技（あまごい）には発動しない
  - 自分自身を対象とする技（アクアリング）には発動しない
  - みずタイプの変化技（みずびたし）も無効化する
  - マジックコートで跳ね返された変化技を自分で受けるととくこうが上がる
  - マジックコート状態では変化技を跳ね返しとくこうが上がらない
  - まもるで防がれたときは発動しない
  - みがわり状態の攻撃技でも発動する
  - 無効化時にきゅうこんは発動しない
  - 連続技（すいりゅうれんだ）を受けてもとくこう上昇は1回のみ
- `python -m pytest tests/ -v` で全件（5674 passed, 1 skipped）を確認。skipはよびみずと無関係な既存テスト（`test_なないろほし_マグニチュードは地震の相手に威力が2倍になる`）。
