# おみとおし レビュー結果

## 仕様書
- 変更なし（一次情報 `.internal/wiki/abilities/おみとおし.html` と照合し、効果・特性の仕様の内容に相違なし）

## 実装計画書
- 変更なし（仕様書と照合し、イベント・priority・subject_spec・実装方針に問題なし）

## 実装（handlers/ / data/）
- 変更なし（`handlers/ability.py` の `おみとおし_reveal_foe_item` と `data/ability.py` の登録を仕様書・計画書と照合。
  `foe.has_item()`（デフォルトで `base_name` を参照）によりぶきよう等でアイテム効果が無効化されていても
  公開判定が行われる点、`ON_SWITCH_IN` の priority がデフォルト100（`.internal/spec/turn.md` の記載と一致）である点を確認）

## リーサル計算
- 対象外（`.internal/progress/ability.md` のリーサル実装列が `n/a`。アイテム公開のみの効果で戦闘結果に影響しないため）

## テスト
- `tests/abilities/test_ability_a.py` に `test_おみとおし_ぶきようでアイテム無効化中でも公開される` を追加
  （計画書のテストケース「ぶきようでアイテム無効化中でも `base_name` があれば公開される」が未カバーだったため）
- 既存の2テスト（アイテムありで公開される／アイテムなしで発動しない）は実装と一致しており変更なし
