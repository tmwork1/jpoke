# フィルター レビュー結果

## 仕様書
- 変更なし。一次情報（.internal/wiki/abilities/フィルター.html）と照合し、効果・特性の仕様（ハードロック#特性の仕様を参照する仕様）・備考の記述に誤り・欠落がないことを確認した。

## 実装計画書
- 新規作成。`.internal/plan/abilities/フィルター.md` が存在しなかったため、`.internal/plan/abilities/ハードロック.md` を参照しつつ新規作成した。
  - 効果はハードロックと完全に同一であり、実装が共有ハンドラ `ハードロック_reduce_effective` を利用する方針を明記した。
  - `mold_breaker_ignorable` フラグでかたやぶり・かがくへんかガス等に無効化される点（プリズムアーマーとの違い）を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/ability.py` の `"フィルター"` エントリを確認した。
  - `flags={"mold_breaker_ignorable"}` が付与されている。
  - `Event.ON_CALC_DAMAGE_MODIFIER`（`subject_spec="defender:self"`）で `h.ハードロック_reduce_effective` を登録しており、ハードロック・プリズムアーマーと同一のハンドラ関数を共用している。
  - `handlers/ability.py` の `ハードロック_reduce_effective` は `battle.query.is_super_effective(ctx)` を判定し、真なら固定小数点 3072/4096（0.75倍）を乗算する実装で、仕様・計画書と一致していることを確認した。

## リーサル計算
- 対象外。`.internal/progress/ability.md` の「リーサル実装」列が `n/a` のため対象外。`handlers/lethal.py` にも `フィルター` 関連の専用ハンドラは存在せず、他の同種特性（ハードロック・あついしぼう等）と同様に、通常の `ON_CALC_DAMAGE_MODIFIER` ハンドラがリーサル計算の共通ダメージ計算経路でもそのまま参照されるため専用実装は不要と判断した。

## テスト
- 追加。`tests/abilities/test_ability_ha.py` に `フィルター` のテストが1件も存在しなかったため、`ハードロック` の既存テストと対をなす2件を追加した。
  - `test_フィルター_かたやぶりで無効`: かたやぶり持ちの攻撃に対して `damage_modifier` が4096のまま（無効化される）ことを確認。
  - `test_フィルター_効果抜群ダメージを0_75倍`: 効果抜群の技を受けたとき `damage_modifier` が3072（0.75倍）になることを確認。
- `python -m pytest tests/ -v` を実行し、5341 passed, 1 skipped, 0 failed を確認した（ログ: `.loop/test_logs/フィルター.log`）。
