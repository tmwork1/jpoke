# リミットシールド レビュー結果

## 仕様書
- 変更なし（一次情報 https://wiki.pokemonwiki.com/wiki/リミットシールド と照合し、既存の記述に誤り・欠落は見つからなかった）

## 実装計画書
- 実装状態を「部分実装」→「実装済み」に更新
- `Event.ON_BEFORE_APPLY_VOLATILE`（ねむけ状態の無効化）を実装対象イベントに追記
- 変更対象ファイルの関数名を実際の実装（`リミットシールド_enter_meteor_form` 等）に合わせて修正
- 実装ステップ・テスト観点に `リミットシールド_prevent_drowsy` の追加を反映

## 実装（handlers/ / data/）
- **欠落を修正**: 仕様書に明記されている「りゅうせいのすがたでは状態異常だけでなくねむけ状態にもならない」という
  効果が未実装だった（`ON_BEFORE_APPLY_AILMENT` による状態異常無効化のみで、あくびによる「ねむけ」volatile を
  防ぐ `ON_BEFORE_APPLY_VOLATILE` ハンドラが存在しなかった）。
  - `src/jpoke/handlers/ability.py` に `リミットシールド_prevent_drowsy` を新規実装
    （きよめのしお等と同じ `_prevent_volatile(..., blocked_volatiles=["ねむけ"])` を利用）
  - `src/jpoke/data/ability.py` の `リミットシールド` に `Event.ON_BEFORE_APPLY_VOLATILE` ハンドラ
    （`subject_spec="target:self"`）を追加登録
- その他の項目（登場時・ターン終了時のフォルムチェンジ、状態異常無効化、交代時の姿戻し、
  かたやぶり無効化、かがくへんかガス非適用、スキルスワップ等の失敗、トレース/ミイラ無効化）は
  実装・優先度（`ON_SWITCH_IN` priority=120, `ON_TURN_END` priority=160）とも仕様書と一致しており変更なし

## リーサル計算
- 対象外（`.internal/progress/ability.md` のリーサル実装列が n/a）

## テスト
- `tests/abilities/test_ability_yarawa.py` に以下を追加
  - `test_リミットシールド_りゅうせいのすがたであくびのねむけを防ぐ`
    （新規実装した `リミットシールド_prevent_drowsy` の回帰テスト）
  - `test_リミットシールド_設置技のダメージでHP1_2以下になった場合はコアの姿のまま`
    （仕様書「設置技のダメージでHPが1/2以下になった場合、コアの姿のままリミットシールドは発動しない」の
    優先度順（ステルスロック priority=100 < リミットシールド priority=120）を検証する回帰テスト）
- `python -m pytest tests/ -v` で全5696件パス（1件は既存の無関係なskip）を確認。
  ログは `.loop/test_logs/リミットシールド.log` に保存
