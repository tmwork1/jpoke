# くろのいななき レビュー結果

## 仕様書
- 変更なし（`.internal/spec/abilities/くろのいななき.md` は一次情報（ポケモンWiki）と一致していることを確認した）

## 実装計画書
- 新規作成（`.internal/plan/abilities/くろのいななき.md` が存在しなかったため、既存実装・仕様書をもとに作成した）
  - `Event.ON_MOVE_KO` / `subject_spec="attacker:self"` で実装されていること、
    `じしんかじょう`・`しろのいななき` と共通ヘルパー `_boost_on_move_ko` を使っていることを明記
  - priority: `turn.md` に `ON_MOVE_KO` の記載がないため、同一イベントを使う既存ハンドラ群と同様に
    priority指定なし（デフォルト）とする根拠を明記

## 実装（handlers/ / data/）
- 変更なし
  - `handlers/ability.py` の `くろのいななき_boost` は `_boost_on_move_ko(battle, ctx, value, stats={"spa": +1})`
    を呼び出しており、仕様（攻撃技で相手をひんしにしたときとくこう+1、最大ランク時は不発動）と一致
  - `data/ability.py` の登録（`ON_MOVE_KO` / `subject_spec="attacker:self"`）も妥当
  - `ON_MOVE_KO` は `move_executor._execute_hit` が「攻撃技でひんしにした」場合のみ発火するため、
    ハンドラ側に重複ガードは不要（実装済みの状態が正しい）

## リーサル計算
- 対象外（`.internal/progress/ability.md` の「リーサル実装」列は `n/a`）

## テスト
- 追加
  - `tests/abilities/test_ability__group.py` に
    `test_倒すと能力上昇系_ランクがすでに最大のときは発動しない`
    （`じしんかじょう`/`しろのいななき`/`くろのいななき` をパラメタライズ）を追加。
    仕様書に明記されている「すでに最大ランクのときは発動しない」ケースが未テストだったため補完した。
  - 既存の以下のテストは実装と整合しており変更なし:
    - `test_倒すと能力上昇系_ばけのかわのフォルムチェンジ消費ダメージで倒しても発動する`
    - `test_倒すと能力上昇系_相手を倒すと指定ステータスが1段階上昇する`
    - `test_倒すと能力上昇系_相手を倒せないと発動しない`
