# せいでんき レビュー結果

## 仕様書
- 変更なし（`.internal/spec/abilities/せいでんき.md` は既存。一次情報（`.internal/wiki/abilities/せいでんき.html`）と照合し、バトルでの効果・特性の仕様（発動除外条件・連続攻撃技の扱い等）・備考の記述に誤り・欠落がないことを確認した）

## 実装計画書
- 変更なし（`.internal/plan/abilities/せいでんき.md` は既存。仕様書と整合しており、実装方針（`ON_DAMAGE_HIT` での接触時まひ付与、どくのトゲ/ほのおのからだと共通パターン）も実装と一致していることを確認した）

## 実装（handlers/ / data/）
- 変更なし。
  - `data/ability.py`: `Event.ON_DAMAGE_HIT` に `subject_spec="defender:self"` で `h.せいでんき_maybe_paralyze_attacker` を登録済み。どくのトゲ・ほのおのからだと同一パターン。
  - `handlers/ability.py`: `せいでんき_maybe_paralyze_attacker` は共通ヘルパー `_apply_contact_counter_ailment(battle, ctx, value, ailment="まひ", chance=0.3)` を呼ぶだけの実装で、接触判定（`battle.query.is_contact_reaction`）・状態異常付与（`battle.ailment_manager.apply`）を通じて型免疫（でんきタイプ）・既存状態異常・特性免疫（じゅうなん等）・ぼうごパット・みがわり無効化などの仕様がすべて共通処理で正しく担保されている。

## リーサル計算
- 対象外（`.internal/progress/ability.md` の「リーサル実装」列は `n/a`）

## テスト
- 変更なし。`tests/abilities/test_ability__group.py` の `CONTACT_AILMENT_CASES`（せいでんき・どくのトゲ・ほのおのからだの3特性を共通パラメタライズ）で以下を確認済み：
  - `test_接触時に状態異常付与_接触技で状態異常を付与する`: 接触技を受けたとき30%（乱数固定で必ず）まひ状態になる
  - `test_接触時に状態異常付与_非接触技では発動しない`: 非接触技では発動しない
  - 型免疫・特性免疫・ぼうごパット等は `AilmentManager.apply` 側の共通ロジックであり、`tests/test_ailment.py` 等で汎用的にカバーされている
- `python -m pytest tests/ -v` を実行し、`tests/test_field.py::test_オーロラベール_ひかりのかべとは重複しない` の単発失敗（急所乱数未固定の既知flaky）以外は全件パス。単独再実行で該当テストもパスすることを確認した（せいでんきの変更に起因しない）。
