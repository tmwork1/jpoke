# あめうけざら レビュー結果

## 仕様書
- 変更なし（一次情報 `.internal/wiki/abilities/あめうけざら.html` と照合し、効果・特性の仕様・備考すべて一致していることを確認した）

## 実装計画書
- 変更なし（仕様書と整合しており、`.internal/spec/turn.md` の ON_TURN_END priority=30 とも一致していることを確認した）

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/ability.py` の `あめうけざら_heal` と `src/jpoke/data/ability.py` の登録内容を確認した。
  - `Event.ON_TURN_END` / `priority=30`（`.internal/spec/turn.md` の記載と一致）
  - `subject_spec="source:self"`（非攻撃フェーズの `EventContext` ロールと一致）
  - `battle.weather_for(mon).rainy` で天候判定（あめ/おおあめの両方をカバーし、`weather_for` 経由のためばんのうがさ・ノーてんき/エアロックによる無効化も自動的に反映される）
  - `battle.modify_hp(mon, r=1/16)` で回復（正規API使用、内部で `ON_MODIFY_HEAL` が発火するためかいふくふうじも自動的にブロックされる）
  - アイスボディ (`アイスボディ_heal`) と同一パターンで実装されており、整合している

## リーサル計算
- 変更なし。`src/jpoke/handlers/lethal.py` の `あめうけざら_heal`（`LethalEvent.ON_TURN_END`, `subject="defender"`）と `src/jpoke/data/ability.py` の `lethal_handlers` 登録を確認した。
  - `battle.weather_for(ctx.defender).rainy` で天候判定し、`_heal(hp_dist, ctx.defender, r=1/16)` で回復しており、アイスボディの `アイスボディ_heal`（リーサル版）と同一パターン。
  - 実装・テストともに既に完了しており、`.internal/progress/ability.md` の該当列が `-` のままだったのは記載漏れ（今回 x に修正）。

## テスト
- `tests/abilities/test_ability_a.py` に以下を追加した:
  - `test_あめうけざら_かいふくふうじ中は回復しない`
  - `test_あめうけざら_ノーてんき下では発動しない`
  - `test_あめうけざら_ばんのうがさ所持時は発動しない`
  （いずれもアイスボディの既存テストと同型のパターンで作成）
- `tests/abilities/test_ability__group.py`（あめ/おおあめでの回復）、`tests/abilities/test_ability_a.py`（あめ以外では発動しない）、`tests/test_lethal.py`（リーサル計算での回復）の既存テストは実装内容と一致しており変更なし。
  追加位置は既存の `test_あめうけざら_あめ以外では発動しない` の直後（五十音順で正しい位置）とした。
  ※ 本タスクの指示により `sort_tests.py` / `generate_test_list.py` は実行していない（マージ後に一括実行）。
- `python -m pytest tests/ -v` で 4670 passed, 1 skipped（全件パス）を確認した。ログは `.loop/test_logs/あめうけざら.log` に保存済み。
