# てつのトゲ レビュー結果

## 仕様書
- 変更なし。一次情報（ポケモンWiki `てつのトゲ`）と照合し、`.internal/spec/abilities/てつのトゲ.md` の
  効果（直接攻撃でダメージを受けたとき、攻撃してきた相手に最大HPの1/8のダメージを与える。
  小数点以下切り捨て・最低1）、および「特性の仕様」節が一次情報どおり `さめはだ` の詳細仕様
  （さめはだ#特性の仕様）を参照する形で記載されていることを確認した。誤り・欠落はなかった。

## 実装計画書
- 新規作成した。`.internal/plan/abilities/てつのトゲ.md` が存在しなかった
  （`.internal/progress/ability.md` では計画書列が `x` になっていたが実ファイルは未作成という
  不整合があった）ため、`.internal/plan/abilities/さめはだ.md` を土台に新規作成した。
  - 実装方針として、効果・発動条件が `さめはだ` と完全に同一であるため、専用ハンドラを
    新設せず既存の共通関数 `さめはだ_chip_contact_attacker()`（内部で
    `_apply_contact_counter_chip()` を呼び出す）を再利用する旨を明記した。
  - 実装ゴール・実装対象イベント（`Event.ON_DAMAGE_HIT`）・変更対象ファイル・実装ステップ・
    テスト観点・検証コマンド・リスク（さめはだと実装を共有しているため片方だけ修正すると
    差異が生じる点）を記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/ability.py` の `てつのトゲ` エントリは `Event.ON_DAMAGE_HIT` に
  `h.さめはだ_chip_contact_attacker`（`subject_spec="defender:self"`）を登録しており、
  `さめはだ` エントリと完全に同一の登録内容（priority指定なし＝デフォルト100）だった。
  `src/jpoke/handlers/ability.py` の `_apply_contact_counter_chip()` は
  `battle.query.is_contact_reaction(ctx)`（接触判定＋ぼうごパット等の反応ブロックを内包）で
  発動条件を判定し、`battle.modify_hp(ctx.attacker, r=-1/8, reason="")` で固定割合ダメージを
  適用したうえで `ctx.attacker.hits_taken` をインクリメント（ダメおし判定用）しており、
  仕様書・計画書と整合していた。誤り・欠落はなかった。

## リーサル計算
- 対象外（`.internal/progress/ability.md` の該当行が `n/a` / `n/a`）。

## テスト
- 変更なし。`tests/abilities/test_ability_sa.py` の `test_さめはだ_接触ダメージ`
  （パラメータ化テスト）に `てつのトゲ`（たいあたりで1/8ダメージ、みずでっぽうで発動しない）
  の2ケースが既に含まれていた。マジックガード・みがわり・ひんし時発動などの個別分岐は
  `さめはだ` 側の既存テストで検証済みであり、実装を共有する `てつのトゲ` 側での重複実装は
  不要と判断し、追加しなかった。
  `python -m pytest tests/ -v` を実行し、5010 passed, 1 skipped（既存のskipは本レビューと
  無関係）で全テストが通ることを確認した。ログ:
  `C:\Users\tmtmp\Documents\pokemon\jpoke\.loop\test_logs\てつのトゲ.log`
