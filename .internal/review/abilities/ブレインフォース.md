# ブレインフォース レビュー結果

## 仕様書
- 変更なし（一次情報（ポケモンWiki）と全項目一致を確認した）

## 実装計画書
- 新規作成（`.internal/plan/abilities/ブレインフォース.md` が存在しなかったため、仕様書と実装内容を
  もとに作成した）
- たつじんのおびとの併用時の累積計算（固定小数点演算の逐次適用により厳密には1.5倍ちょうど
  ではなく modifier=6143 になる点）を明記した

## 実装（handlers/ / data/）
- 変更なし（`ブレインフォース_boost_effective()` は `is_super_effective(ctx)` 判定後に
  `apply_fixed_modifier(value, 5120)` を適用しており、仕様書・計画書と一致していることを確認した）

## リーサル計算
- 対象外（`.internal/progress/ability.md` の「リーサル実装」「リーサルテスト」列は `n/a`）

## テスト
- 既存の `test_ブレインフォース_効果抜群のとき強化` に加えて以下を追加した（`tests/abilities/test_ability_ha.py`）
  - `test_ブレインフォース_等倍のときは発動しない`: 等倍技では damage_modifier が変化しないことを確認
  - `test_ブレインフォース_たつじんのおびと併用すると累積して1_5倍になる`: たつじんのおびとの
    併用で damage_modifier が 6143（固定小数点演算の丸めにより1.5倍ちょうどの6144より1小さい値）
    になることを確認
