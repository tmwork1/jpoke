# きゅうばん レビュー結果

## 仕様書
- 変更なし（`.internal/spec/abilities/きゅうばん.md` は一次情報（`.internal/wiki/abilities/きゅうばん.html`）の
  効果・特性の仕様・備考・関連項目の内容と一致していることを確認した）

## 実装計画書
- `.internal/plan/abilities/きゅうばん.md` を実態に合わせて全面的に更新した。
  旧計画書は `Event.ON_HIT` / `priority=200` / `ctx.check_def_ability_enabled()` という、
  実際には存在しない古い実装案のまま放置されていたため、実際の実装
  （`Event.ON_TRY_BLOW`・`subject_spec="defender:self"`・priority指定なし）に
  合わせて書き直した。
- `ON_TRY_BLOW` が `.internal/spec/turn.md` に掲載されていない制御系イベントであることと、
  同種の無効化ハンドラ（`ねをはる`）を参照して priority 不要と判断した根拠を明記した。
- レッドカードとの連携（`handlers/item.py` の `_レッドカード_try_force_switch`）・
  `ばんけん` とのハンドラ共用についても記載を追加した。

## 実装（handlers/ / data/）
- 変更なし。`handlers/ability.py` の `きゅうばん_block_blow`（`ON_TRY_BLOW` で
  `value=False, stop_event=True` を返す）と `data/ability.py` の登録
  （`flags={"mold_breaker_ignorable"}`、`subject_spec="defender:self"`）を確認し、
  一次情報・仕様書と整合していることを確認した。
- `handlers/item.py` のレッドカード実装（`foe.ability.name in ("きゅうばん", "ばんけん")` で
  発動を防ぎつつアイテムは消費する）も仕様どおりであることを確認した。
- かたやぶり系の無効化は `mold_breaker_ignorable` フラグ経由で汎用的に処理されており、
  ハンドラ内での重複チェックは不要（既存のプロジェクト方針どおり）。

## リーサル計算
- 対象外（`.internal/progress/ability.md` のリーサル実装列は `n/a`。きゅうばんはHP・ダメージ計算に
  関与せず強制交代を防ぐのみのため、リーサル計算ハンドラの対象外）。

## テスト
- 変更なし。以下の既存テストが仕様のエッジケースを網羅していることを確認した。
  - `tests/moves_status/test_move_ha.py`: ふきとばし・ほえるを防ぐ
  - `tests/moves_attack/test_move_ta.py`: ともえなげ・ドラゴンテールを受けてもダメージ後に交代しない
  - `tests/items/test_item_yarawa.py`: レッドカードを防ぐが、アイテムは消費される
  - `tests/abilities/test_ability_ka.py`: かたやぶりで無効化を貫通される／吹き飛ばしを防ぐ
- `python -m pytest tests/ -v` は 4774 passed, 1 skipped で全件成功
  （ログ: `C:\Users\tmtmp\Documents\pokemon\jpoke\.loop\test_logs\きゅうばん.log`）。
