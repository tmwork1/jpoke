# パステルベール レビュー結果

## 仕様書
- 変更なし。`.internal/wiki/abilities/パステルベール.html`（一次情報）と照合し、既存の
  `.internal/spec/abilities/パステルベール.md` は効果・特性の仕様・備考のすべての項目が
  一致しており、欠落・誤りは見つからなかった。

## 実装計画書
- 新規作成（`.internal/progress/ability.md` では計画書列が `x` になっていたが実体のファイルは
  存在しなかった）。仕様書をもとに `.internal/plan/abilities/パステルベール.md` を作成した。
  内容は既存実装（めんえきと共通のパターン）を追認する形でまとめ、ダブルバトル以上限定の
  「味方全体保護」「登場時の相互発動」「治療順序」は本プロジェクトの対象外
  （シングルバトルのみ）である旨を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/ability.py` の `パステルベール` エントリ、
  `src/jpoke/handlers/ability.py` の `パステルベール_cure_poison_on_enable` は
  いずれも `めんえき` と同一パターン（`prevent_poison_ailment` + `_cure_ailment_on_enable`
  共通ヘルパー、`mold_breaker_ignorable` フラグ）で実装されており、仕様書の内容
  （どく/もうどく無効化、特性有効化時の即時回復、かたやぶりで無効化された場合の
  技終了直後の回復）と整合していた。
  どくびし・どくどくだま経由の付与も `ON_BEFORE_APPLY_AILMENT` を経由するため、
  追加実装なしで正しくブロックされることをコードパスで確認した。

## リーサル計算
- 対象外。`.internal/progress/ability.md` の該当行は「リーサル実装」「リーサルテスト」列とも
  `n/a`。`src/jpoke/handlers/lethal.py` にも `パステルベール` / `めんえき` 関連の
  ハンドラは存在せず、n/a の記載は妥当と判断した。

## テスト
- 変更なし。`tests/abilities/test_ability__group.py` の `test_状態異常無効` /
  `test_状態異常無効_かたやぶりで無効` にパステルベールのパラメータが既に含まれており、
  基本の無効化とかたやぶり時の即時回復の両方をカバーしていた。
- `python -m pytest tests/ -v` を実行し、5134 passed, 1 skipped（無関係な既存スキップ）
  で全テストが通過することを確認した。再実行で結果が変わる flaky テストは
  観測されなかった。
