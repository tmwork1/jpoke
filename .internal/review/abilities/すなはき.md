# すなはき レビュー結果

## 仕様書
- 変更なし（一次情報（.internal/wiki/abilities/すなはき.html）と照合し、誤り・欠落なしを確認した）

## 実装計画書
- 実装方針を修正した:
  - `Event.ON_DAMAGE_HIT` ではなく `Event.ON_HIT` を使う方針に修正（理由を明記）。
    `ON_DAMAGE_HIT` は `actual_damage<=0` のとき発火しないため、こらえるでHP1のまま
    耐えたときやみねうちを受けたとき（実HPダメージ0）も発動する仕様
    （.internal/spec/abilities/すなはき.md）を満たせない。
  - みがわりに阻まれた場合の除外（`ctx.substitute_damage`）を明記。
  - テスト観点に「0ダメージでも発動」「みがわりで発動しない」「変化技では発動しない」を追加。
  - 疑似コードを実装に合わせて更新。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/ability.py` の `すなはき_set_sandstorm` を修正:
  - `ctx.substitute_damage` が真のとき（みがわりに阻まれた場合）は何もせず return するガードを追加。
- `src/jpoke/data/ability.py` の `すなはき` ハンドラ登録を修正:
  - `Event.ON_DAMAGE_HIT` → `Event.ON_HIT` に変更。
  - 変更理由をコメントで明記（こぼれダネ・じきゅうりょくと同じパターン）。
  - 修正理由: 従来の `ON_DAMAGE_HIT` 実装では、こらえる・みねうちなど実HPダメージが0の
    攻撃を受けた場合に特性が発動しないバグがあった（一次情報の「特性の仕様」節に明記された
    挙動と不一致）。

## リーサル計算
- 対象外（`.internal/progress/ability.md` の「リーサル実装」列が `n/a`。すなおこし・ひでり・
  あめふらしと同様、天候変化のみでHPに直接影響しないためリーサル計算の対象外）。

## テスト
- `tests/abilities/test_ability_sa.py` に以下を追加:
  - `test_すなはき_こらえるでHP1のまま耐えたときも発動する`
  - `test_すなはき_さらさらいわ所持で継続ターンが8になる`
  - `test_すなはき_タイプ相性でダメージを無効化したときは発動しない`
  - `test_すなはき_みがわりに阻まれたときは発動しない`
  - `test_すなはき_変化技を受けても発動しない`
- 既存の `test_すなはき_すでにすなあらし中は変化なし` / `test_すなはき_被弾ですなあらし発動`
  は変更なし。
- `python -m pytest tests/ -v` で全4936件パス（1件skip、すなはき無関係）。
