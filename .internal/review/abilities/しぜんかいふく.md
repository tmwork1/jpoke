# しぜんかいふく レビュー結果

## 仕様書
- 変更なし（一次情報（.internal/wiki/abilities/しぜんかいふく.html）と照合し、内容が完全に一致していることを確認した）

## 実装計画書
- 「変更候補」の `tests/test_ability.py` を実体に合わせて `tests/abilities/test_ability_sa.py` に修正
- 「テスト観点」の「3. かがくへんかガス発動中は回復しない。」を削除し、代わりに
  「とんぼがえり等の使うと交代する技で引っ込んだときも回復する」「ドラゴンテール等で強制交代
  させられたときも回復する」を追加（かがくへんかガスによる無効化は `ON_CHECK_ABILITY_ENABLED`
  経由で汎用的に処理されるため個別特性の計画書に明記しない方針（メモリ:
  feedback-no-gas-mold-check）に合わせ、既にレビュー済みの `さいせいりょく` と同じ方針で統一した）

## 実装（handlers/ / data/）
- 変更なし（`handlers/ability.py` の `しぜんかいふく_cure_ailment` は `battle.ailment_manager.remove(mon)`
  で状態異常を除去しており、`data/ability.py` の `ON_SWITCH_OUT` 登録も `subject_spec="source:self"`
  で `さいせいりょく` 等と同パターン。かがくへんかガスによる無効化は `EventManager` の
  `ability.enabled_ignoring` チェックで汎用的に処理されており、ハンドラ側に重複ガードは不要で
  実際に存在しないことを確認した。かたやぶりは `ON_SWITCH_OUT` に影響しないため専用の対応は不要）

## リーサル計算
- 対象外（`.internal/progress/ability.md` のリーサル実装列は `n/a`。しぜんかいふくは状態異常のみを
  操作しHPに影響しないため、そもそも `handlers/lethal.py` のHP変動計算チェーンとは無関係）

## テスト
- 既存1件（交代で控えに戻ると状態異常が回復する）は変更なし
- 計画書の「とんぼがえり等の使うと交代する技で引っ込んだときも回復する」
  「ドラゴンテール等で強制交代させられたときも回復する」に対応するテストが存在しなかったため
  `さいせいりょく` のテストパターンに倣って以下を追加した:
  - `test_しぜんかいふく_とんぼがえりで交代しても状態異常回復`
  - `test_しぜんかいふく_強制交代させられても状態異常回復`（`ailment1` で相手にどく状態を付与し、
    ドラゴンテールで強制交代させたあと状態異常が解除されることを確認）
