# ちょすい レビュー結果

## 仕様書
- 変更なし。`.internal/spec/abilities/ちょすい.md` は既に存在し、一次情報（`.internal/wiki/abilities/ちょすい.html`）の
  「効果」「特性の仕様」節の内容と一致していることを確認した（マジックコートとの関係、アクアリング・みずあそび等の
  対象外技、いのちのしずくとの関係、うるおいボイスで水タイプ化した全体対象技、連続攻撃技、みがわり、まもる等での
  非発動、いたずらごころ×あくタイプ、おおひでりでの不発優先、かたやぶり、命中判定順序、メッセージ、かいふくふうじ
  など、いずれも一次情報と整合）。誤り・欠落はなし。

## 実装計画書
- 新規作成。`.internal/plan/abilities/ちょすい.md` が存在しなかったため（進捗表では計画書列が `x` だったが実体が
  無い不整合があった）、仕様書と既存実装（`_apply_type_absorb()` を使った `Event.ON_BEFORE_APPLY_MOVE` ハンドラ）
  をもとに新規作成した。
  - 既知の制約（命中判定順序、みがわり状態での変化技、いたずらごころ×あくタイプの速度依存タイブレーク、
    いのちのしずくのシングル限定での非該当性、うるおいボイスで水タイプ化する全体対象技の未実装）を
    計画書に明記し、本レビューのスコープ外として扱う根拠を残した（`かんそうはだ`・`そうしょく` の
    既存レビューと同一パターン）。
  - かいふくふうじについては当初「未実装」と誤認したが、実際には `src/jpoke/data/volatile.py` の
    `かいふくふうじ` エントリ（`Event.ON_MODIFY_HEAL`）が既に実装されており、`_apply_type_absorb()` の
    `battle.modify_hp()` 呼び出しを介して自動的に回復無効化が効くことを確認し、計画書の記載を訂正した。

## 実装（handlers/ / data/）
- 変更なし。既存実装を確認：
  - `ちょすい_absorb_water`（`handlers/ability.py`）: `_apply_type_absorb` 共通ヘルパーを使用してみず技を
    無効化し、最大HPの1/4回復する。`かんそうはだ`・`ちくでん`・`そうしょく` と同一ヘルパーを利用しており、
    対象外の技（自分自身・場対象）は無効化しない仕様と一致。
  - `data/ability.py` の `ちょすい` エントリは `flags={"mold_breaker_ignorable"}` を持ち、かたやぶり無効化
    仕様と整合。
  - `そうしょく` のような、コア側の型無効化ルールとの競合（`_check_grass_type_powder_immunity` 相当）は
    みずタイプには存在しないため、`move_executor.py` の修正は不要と確認した。

## リーサル計算
- 対象外（`.internal/progress/ability.md` のリーサル実装列が `n/a`）。

## テスト
- `tests/abilities/test_ability_ta.py` にちょすい専用のテストが1件も存在しなかったため（既存は
  `test_ability__group.py` の `test_タイプ無効回復` / `test_タイプ無効回復_かたやぶりで無効`
  パラメトライズのみ）、以下を新規追加した：
  - `test_ちょすい_みず技を無効化してHPが減っていれば1_4回復`
  - `test_ちょすい_HP満タンならみず技無効化のみで回復しない`
  - `test_ちょすい_みずタイプの変化技も無効化する`
  - `test_ちょすい_自分自身を対象とするみず技には発動しない`
  - `test_ちょすい_場を対象とするみず技には発動しない`
  - `test_ちょすい_連続技を受けても回復は1回のみ`
  - `test_ちょすい_まもるで防がれたときは発動しない`
  - `test_ちょすい_みがわり状態の攻撃技でも発動する`
  - `test_ちょすい_マジックコート状態では変化技を跳ね返し回復しない`
  - `test_ちょすい_マジックコートで跳ね返された変化技を受けると回復する`
  - `test_ちょすい_かいふくふうじ状態でも無効化はできるが回復はできない`
- `python -m pytest tests/ -v` を実行し、全5001件（+1 skipped）がパスすることを確認した
  （ログ: `.loop/test_logs/ちょすい.log`）。
  - 初回実行時に無関係な `test_ごりむちゅう_特殊技には効果がない`（命中率をtest_option等で固定していない
    既存のプリ既存テスト）が偶発的に失敗したが、本レビューでは触れていないファイルであり再実行で
    パスすることを確認した（既知のflaky挙動、本レビューのスコープ外）。
