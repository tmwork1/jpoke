# ファーコート レビュー結果

## 仕様書
- 変更なし（一次情報〈ポケモンWiki〉と照合したが、内容・記載に誤り・欠落なし）

## 実装計画書
- 新規作成（`.internal/plan/abilities/ファーコート.md`）
  - `くさのけがわ.md`（同系統の `ON_CALC_DEF_MODIFIER` ハンドラ）を参考に、実装ゴール・対象イベント・実装ステップ・テスト観点・リスクを記載
  - こんらん自傷ダメージ除外漏れのバグをリスク欄に明記

## 実装（handlers/ / data/）
- 修正した内容:
  - `src/jpoke/handlers/ability.py` の `ファーコート_boost_B` に `ctx.move.name != "_こんらん"` の除外条件が欠落していたバグを修正。
    内部技 `_こんらん`（こんらんの自傷ダメージ）も `category="physical"` であるため、修正前は自傷ダメージにも誤って防御2倍が適用されてしまう状態だった。
    仕様書「こんらんで自傷ダメージを受けるときは効果が無い。」に反する実装だったため、同系統の `くさのけがわ_boost_B` と同じパターンで修正した。
- 変更なし（他の仕様項目は既存の一般機構で自動的に満たされることを確認）:
  - サイコショック/サイコブレイク/しんぴのつるぎ: `battle.query.deals_physical_damage` が `physical_damage` フラグ付き特殊技も物理扱いするため、追加実装不要。
  - シェルアームズ: 分類判定（`シェルアームズ_modify_move_category`）は `defender.ranked_stats` を直接参照し、`ON_CALC_DEF_MODIFIER` の乗算後の値は見ないため、ファーコートの効果は分類判定に影響しない。
  - ボディプレス: 本特性は `Event.ON_CALC_DEF_MODIFIER`・`subject_spec="defender:self"` にのみ登録されており、`ボディプレス` が参照する自分の防御（攻撃力側、`ON_CALC_ATK_MODIFIER`）には無関係。
  - ワンダールーム: `ワンダールーム_def_modifier` が防御/特防の実数値参照を入れ替える一般処理のため、追加実装なしで「特防が2倍になる」効果が成立する。

## リーサル計算
- 対象外（`.internal/progress/ability.md` のリーサル実装列が `n/a`）

## テスト
- 追加した内容（`tests/abilities/test_ability_ha.py`）:
  - `test_ファーコート_物理技の防御が2倍になる` のパラメータに `サイコショック`（特殊技だが防御を参照するため2倍になる）を追加
  - `test_ファーコート_こんらんの自傷ダメージには発動しない`（バグ修正の回帰テスト）
  - `test_ファーコート_ボディプレス使用時は自分の防御実数値を2倍にしない`
  - `test_ファーコート_ワンダールームでとくぼうが2倍になる`
- `python -m pytest tests/ -v` で全5200件（1件skip、無関係）が成功したことを確認（ログ: `.loop/test_logs/ファーコート.log`）。
- flakyテストは発見されなかった。
