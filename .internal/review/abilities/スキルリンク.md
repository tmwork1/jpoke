# スキルリンク レビュー結果

## 仕様書
- 変更なし。既存の `.internal/spec/abilities/スキルリンク.md` を一次情報（ポケモンWiki）と照合し、
  効果・特性の仕様（2～5回連続技の最大化、トリプルキック/トリプルアクセルの初回ヒット判定、
  ネズミざんの10回固定、いかさまダイスとの優先関係、固定回数技への非影響、ミイラ等との相互作用、
  ひんし・さめはだ・ほうし絡みの中断条件）がすべて一致していることを確認した。

## 実装計画書
- 修正した（実質新規作成）。既存の計画書が「仕様書未作成」という古い前提のまま放置されており、
  実際の実装（`Event.ON_MODIFY_HIT_COUNT` / `Event.ON_MODIFY_HIT_CHECK_EACH_TIME` を用いた実装）と
  乖離していたため、実装内容・優先度の根拠（いかさまダイスの `priority=90` に対しスキルリンクは
  デフォルト優先度100で後勝ちさせる設計）・テスト観点を実態に合わせて書き直した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/ability.py` の `スキルリンク_modify_hit_count` /
  `スキルリンク_modify_hit_check_each_time` と `src/jpoke/data/ability.py` の登録内容を確認したところ、
  仕様書・計画書の内容と一致しており、`subject_spec="attacker:self"` も `AttackContext` のロールと
  整合していた。`いかさまダイス`（`src/jpoke/data/item.py`）との優先度関係（item側priority=90、
  ability側デフォルト100で後勝ち上書き）も意図通り実装されている。修正なし。

## リーサル計算
- 対象外（`.internal/progress/ability.md` の該当行は「リーサル実装」「リーサルテスト」ともに `n/a`）。
  `src/jpoke/handlers/lethal.py` を確認したところ、連続技のリーサル計算は既定で `move.max_hits`
  （最大ヒット数）を用いる設計であり、スキルリンク固有のリーサルハンドラは不要であることを確認した。

## テスト
- 変更なし。`tests/abilities/test_ability_sa.py` の
  `test_スキルリンク_2から5連続技が最大ヒット数になる`（2～5連続技が必ず5回ヒット）、
  `test_スキルリンク_毎ヒット命中判定技は初回のみ判定`（トリプルキック/トリプルアクセル/ネズミざんの
  3パターンをパラメータ化し、初回ヒット後は命中判定なしで全ヒットすることを確認）、
  `tests/items/test_item_a.py` の `test_いかさまダイス_スキルリンク所持時はスキルリンクが優先される`
  を確認し、仕様書・実装と整合していることを確認した。追加・修正の必要なし。
  `python -m pytest tests/ -v` は 4927 passed, 1 skipped で全件パス。
