# かわりもの レビュー結果

## 仕様書
- 既存の `.internal/spec/abilities/かわりもの.md` を一次情報（`.internal/wiki/abilities/かわりもの.html`）と
  照合した結果、内容は一次情報と一致しており誤り・欠落は無かった。変更なし。
- ただし `.internal/progress/ability.md` の該当行は仕様書列が `-`（未作成扱い）になっており、
  実際の状態と食い違っていたため進捗表側を修正した（下記「進捗表」参照）。

## 実装計画書
- `.internal/plan/abilities/かわりもの.md` は存在しなかったため新規作成した。
- かわりものの変身処理は技「へんしん」（`.internal/plan/moves/へんしん.md`）とまったく同一の
  変身機構（対象のスナップショット取得・実数値の固定値コピー・退場時の完全復元・技PPの
  一律5化）を必要とする。既存の `Pokemon.set_form`（種族値ベースの恒久的フォルム変化専用）や
  `PokemonStats`（努力値からの逆算方式で任意の実数値を固定できない）では代替できないことを
  確認し、この本質的な制約を計画書に明記したうえで実装保留を維持した。
- `uncopyable` フラグによるコピー不可挙動（なりきり/うつしえの失敗、トレース/かがくのちから/
  レシーバーの不発、なかまづくりの失敗）は既存実装で正しくカバーされていることを確認し、
  計画書にも明記した。

## 実装（handlers/ / data/）
- `data/ability.py` の `"かわりもの": AbilityData(flags={"uncopyable"})` を確認した。
  変身本体は未実装（保留）で、`uncopyable` フラグのみ設定されている状態は仕様・計画書と
  整合しているため変更なし。
- `handlers/ability.py` / `handlers/move_status.py` 内の `uncopyable` フラグ判定
  （なりきり/うつしえ/トレース/かがくのちから/レシーバー/なかまづくり）を確認し、
  仕様書の記述と一致していることを確認した。変更なし。

## リーサル計算
- 対象外（`.internal/progress/ability.md` のリーサル実装列は `n/a`）。変身本体が未実装のため
  リーサル計算ハンドラも対象外。

## テスト
- 変身本体が未実装（保留）のため、`tests/abilities/` にかわりもの単体のテストは追加しなかった
  （対象外）。既存テストに `かわりもの` への参照は無いことを確認した。
- `python -m pytest tests/ -v` で全件パス（4753 passed, 1 skipped）を確認した。
  ※ 初回実行時に `test_むしくい_...` `test_サイコブレイク_...` の2件が失敗したが、いずれも
  本レビューでのソースコード変更（0件）とは無関係な既存のテスト順序依存の不安定挙動であり、
  単体実行および再実行では安定してパスすることを確認した。
