# しろのいななき レビュー結果

## 仕様書
- 一次情報（.internal/wiki/abilities/しろのいななき.html）と照合し、以下を修正した。
  - 「特性の仕様」節に、くろのいななきの仕様書と同様「すでにこうげきが最大ランクまで
    上がっているときは発動しない」旨の記述を追加（じしんかじょうの仕様を参照するのみで
    最大ランク時の非発動条件が明記されていなかったため）。

## 実装計画書
- `.internal/plan/abilities/しろのいななき.md` が存在しなかったため、仕様書・実装
  （`_boost_on_move_ko` 共通ヘルパー、`Event.ON_MOVE_KO` / `subject_spec="attacker:self"`）
  および `じしんかじょう.md` / `くろのいななき.md` の計画書の記述形式に合わせて新規作成した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/ability.py` の `しろのいななき_boost`（`_boost_on_move_ko` を
  利用し `{"atk": +1}` を渡す）、`src/jpoke/data/ability.py` の `Event.ON_MOVE_KO` /
  `subject_spec="attacker:self"` 登録を確認し、仕様書・計画書と一致していることを確認した。
  `じしんかじょう` からも同じ `しろのいななき_boost` 関数が共用されており、意図通り。

## リーサル計算
- 対象外（`.internal/progress/ability.md` の「リーサル実装」列が `n/a`）。

## テスト
- 変更なし。`tests/abilities/test_ability__group.py` の「倒すと能力上昇系」パラメタライズ
  テスト（相手を倒すとATKが1段階上昇する／倒せないと発動しない／ばけのかわのフォルム
  チェンジ消費ダメージでも発動する／ランクが最大のときは発動しない）に `しろのいななき` が
  `じしんかじょう`（atk）・`くろのいななき`（spa）と共にすでに含まれていることを確認した。
  `python -m pytest tests/ -v` で全件パス（既知flakyの単発失敗2件はいずれも本レビューと
  無関係なテストで、個別再実行するとパスすることを確認済み）。
