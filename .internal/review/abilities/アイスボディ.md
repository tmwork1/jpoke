# アイスボディ レビュー結果

## 仕様書
- 変更なし。一次情報（.internal/wiki/abilities/アイスボディ.html）と照合し、効果・特性の仕様・備考の記述に誤り・欠落がないことを確認した。

## 実装計画書
- 変更なし。「第九世代ではあられは存在しないためゆきのみ対象とする」という前提を確認し、`src/jpoke/types` にあられ天候が定義されていないことも確認済み。

## 実装（handlers/ / data/）
- 変更なし。
- `handlers/ability.py` の `アイスボディ_heal`（`Event.ON_TURN_END`, `subject_spec="source:self"`, `priority=30`）は `.internal/spec/turn.md` の ON_TURN_END priority 30（アイスボディ）と一致していることを確認した。
- `battle.weather.name` は `WeatherManager.active` を経由し、`ON_CHECK_WEATHER_ENABLED`（ノーてんき/エアロック）による無効化を自動的に反映するため、専用の無効化チェックは不要と判断した。
- `battle.modify_hp` は内部で `ON_MODIFY_HEAL` を発火し、かいふくふうじ状態の回復ブロックを共通処理で行っているため、ハンドラ側に重複ガードは不要（既存実装のまま）。
- ぼうじんゴーグル・ばんのうがさはいずれも「あられ/ゆき」関連の効果を防げないことを一次情報で確認し、実装（無条件で `battle.weather.name` を参照）が仕様と一致していることを確認した。

## リーサル計算
- `.internal/progress/ability.md` の「リーサル実装」列は `-` だったが、実際には `handlers/lethal.py` の `アイスボディ_heal`（`LethalEvent.ON_TURN_END`, `subject="defender"`）が既に実装済みで `data/ability.py` にも登録されていた（進捗表の記載漏れ）。
- 内容を確認し、通常ハンドラと同じ条件（ゆき天気）・回復量（最大HPの1/16）で、共通ヘルパー `_heal` を通じてかいふくふうじブロックも正しく処理されていることを確認した。修正は不要。
- 進捗表の該当列を実態に合わせて `x` に修正した。

## テスト
- `tests/abilities/test_ability__group.py` の `test_天候回復特性_対応天候中に回復`（ゆき天気での回復）、`tests/abilities/test_gas.py` のかがくへんかガス無効化ケース、`tests/test_lethal.py` の `test_アイスボディ_ゆき天気でターン終了時回復` は既存のまま変更なし。
- 以下のテストを `tests/abilities/test_ability_a.py` に追加した（他の重要な仕様分岐を明示的に検証するため）。
  - `test_アイスボディ_ゆき以外では発動しない`
  - `test_アイスボディ_かいふくふうじ中は回復しない`
  - `test_アイスボディ_ノーてんき下では発動しない`
- `python -m pytest tests/ -v` を実行し、4665件全件成功（1件skip）を確認した。ログは `.loop/test_logs/アイスボディ.log` に保存した。
