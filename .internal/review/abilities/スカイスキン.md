# スカイスキン レビュー結果

## 仕様書
- 変更なし。`.internal/spec/abilities/スカイスキン.md` の内容を一次情報（`.internal/wiki/abilities/スカイスキン.html`）と
  照合したが、誤り・欠落はなかった。「効果」「特性の仕様」「備考」の各項目は一次情報の記述と一致している。

## 実装計画書
- `.internal/plan/abilities/スカイスキン.md` を新しい規約（`.internal/spec/abilities/` パス、
  `tests/abilities/test_ability__group.py` への参照、priority テーブル、`turn.md` 未掲載イベントの
  priority 根拠明記）に沿って更新した。旧版は 2026-05-03 時点の古いパス表記（`spec/ability/...`、
  `tests/test_ability.py`）を含んでおり、現行のファイル構成と一致していなかったため修正した。
- 実装が共通ヘルパー `_skin_modify_move_type` / `_skin_boost_power` を利用している事実と、
  ウェザーボール・テラバースト等のタイプ変化技との優先順位（技側ハンドラが後で登録されるため
  型変換が技側で上書きされる）を計画書に明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/ability.py` の `スカイスキン_modify_move_type` /
  `スカイスキン_modify_power` は共通ヘルパー `_skin_modify_move_type` / `_skin_boost_power` に
  委譲しており、フェアリースキン・ドラゴンスキン・フリーズスキンと完全に一貫したパターン。
  `src/jpoke/data/ability.py` の登録（`ON_MODIFY_MOVE_TYPE` / `ON_CALC_POWER_MODIFIER`、
  `subject_spec="attacker:self"`）も同様に一致しており、修正は不要だった。

## リーサル計算
- 対象外（`.internal/progress/ability.md` の「リーサル実装」列が `n/a`）。

## テスト
- `tests/abilities/test_ability__group.py` の既存の汎用テスト
  （`test_スキン系_ノーマル技を対応タイプに変換する` / `test_スキン系_変換した技の威力が4915倍になる`）で
  スカイスキンはすでにカバーされていた。
- 仕様書に明記されている「もともとひこうタイプである技の威力は上昇しない」というエッジケースを
  検証するテストが存在しなかったため、`test_スキン系_もともと対応タイプの技は威力補正を受けない`
  を新規追加した（スカイスキン・ドラゴンスキン・フェアリースキン・フリーズスキンの4特性を
  パラメータ化してカバー）。
- `python -m pytest tests/ -v` を実行し、既知の flaky テスト
  （`test_むしくい_ねんちゃく持ちをひんしにさせた場合は奪える`、乱数固定漏れによる単発失敗）を除き
  全件パスすることを確認した（単体実行では合格）。ログは
  `.loop/test_logs/スカイスキン.log` に保存済み。
