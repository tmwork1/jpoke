# ダウンロード レビュー結果

## 仕様書
- 変更なし（一次情報と照合し、内容・記述に誤りや欠落は確認されなかった）

## 実装計画書
- 実装ゴール・実装方針を実際のAPI（`Pokemon.ranked_stats` / `battle.modify_stats` /
  `battle.get_global_field`）に合わせて更新
- ユーザー記憶の方針（かがくへんかガス等の無効化チェックを特性ハンドラ・計画書に書かない）に反し
  「かがくへんかガス中は発動しない」が実装ゴール・テスト観点に記載されていたため削除
- 仕様書に明記されているワンダールーム下の特殊な比較仕様（ランク補正のみ入れ替え）が
  実装方針に欠落していたため追記
- 変更候補のテストファイルパスを実際の配置（`tests/abilities/test_ability_ta.py`）に修正

## 実装（handlers/ / data/）
- `handlers/ability.py` の `ダウンロード_raise_stat` にワンダールーム下の比較ロジックが
  欠落していたため追加。ワンダールーム発動中は実数値を据え置いたまま防御・特防のランク補正のみ
  入れ替えて比較するよう修正（`foe.stats["def"] * foe.rank_modifier("spd")` と
  `foe.stats["spd"] * foe.rank_modifier("def")` を比較）
- それ以外（ランク補正込みの実数値比較、持ち物・特性・天候の除外、上限到達時に発動しない挙動、
  かがくへんかガスでの無効化）は既存実装で仕様通りに動作しており変更なし
- `data/ability.py` の登録内容（`ON_SWITCH_IN` / `subject_spec="source:self"`）は
  仕様・CLAUDE.mdの約束事と一致しており変更なし

## リーサル計算
- 対象外（`.internal/progress/ability.md` の「リーサル実装」列が n/a）

## テスト
- `tests/abilities/test_ability_ta.py` に以下を追加
  - `test_ダウンロード_ランク補正を考慮して比較する`:
    ランク補正後の実数値で比較することを検証
  - `test_ダウンロード_ワンダールームでランク補正のみ入れ替えて比較する`:
    今回追加したワンダールーム対応ロジックを検証
  - `test_ダウンロード_上げる能力が既に最大なら発動しない`:
    上限到達時にランクが変化せず、特性発動ログも記録されないことを検証
- 既存の `test_ダウンロード_能力アップ`（防御/特防比較の基本動作）は変更なし
- `python -m pytest tests/ -v` で全4988件成功（1件skip）を確認
  （実行中に `test_ミラーコート_まねっこでコピーできる` が1度だけ失敗したが、
  まひ（麻痺）の行動不能判定に依存する既知のflaky挙動で本レビューの変更とは無関係。
  再実行で成功することを確認済み）
