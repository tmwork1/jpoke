# すながくれ レビュー結果

## 仕様書
- 変更なし（既存の `.internal/spec/abilities/すながくれ.md` を一次情報（`.internal/wiki/abilities/すながくれ.html`）と照合したが、内容は一致しており誤り・欠落はなかった）

## 実装計画書
- 新規作成（`.internal/plan/abilities/すながくれ.md` が存在しなかったため、仕様書をもとに新規作成した）
  - 命中率補正（`ON_MODIFY_ACCURACY`）、天候ダメージ無効化（`ON_MODIFY_NON_MOVE_DAMAGE`）の実装方針を明記
  - 必中技（`ON_MODIFY_ACCURACY` 自体が発火しない）・一撃必殺技（`ohko` フラグで除外）が命中率補正の対象外であることを明記

## 実装（handlers/ / data/）
- 修正した内容:
  - `handlers/ability.py` の `すながくれ_reduce_accuracy` に一撃必殺技（`ohko` フラグ）の除外条件が抜けていたため追加した。修正前はすなあらし中に一撃必殺技（じわれ等）の命中率まで3277/4096倍に補正してしまうバグがあった（仕様書「必中技・一撃必殺技には適用されない」に反する）。
    - 併せて `ctx` の型注釈を `EventContext` から `AttackContext` に修正（`ctx.move` を参照するため。他の同種ハンドラ `ちどりあし_reduce_accuracy` 等と同じ型）。
  - `すながくれ_ignore_sandstorm_damage`（すなあらしダメージ無効化、`ON_MODIFY_NON_MOVE_DAMAGE` + `hp_change_reason == "sandstorm"`）は既存実装のまま問題なし。タイプ無効化（いわ/じめん/はがね）とは別ロジックで、共通関数 `_ignore_sandstorm_damage` を再利用しており仕様と一致することを確認した。
  - `flags={"mold_breaker_ignorable"}` はすながくれの回避率上昇効果がかたやぶり等で無効化される仕様と一致しており問題なし。

## リーサル計算
- 対象外（`.internal/progress/ability.md` の「リーサル実装」列が `n/a`）

## テスト
- 追加した内容:
  - `tests/abilities/test_ability_sa.py` に `test_すながくれ_一撃必殺技には命中率変化なし` を追加し、上記バグ修正を検証（じわれの命中率がすなあらし中でも補正されないことを確認）
- 既存の `tests/abilities/test_ability__group.py`（天候がくれ系の命中率補正・かたやぶり無効化・対応天候以外での据え置き）、`tests/test_field.py`（すなあらしダメージ免疫）は仕様・実装と一致しており変更なし
