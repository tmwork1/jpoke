# ちからずく レビュー結果

## 仕様書
- 変更なし（`.internal/spec/abilities/ちからずく.md` は一次情報（ポケモンWiki）と照合し、内容に誤り・欠落は見つからなかった）。

## 実装計画書
- 更新した。旧計画書は 2026-05-01 時点のもので、現存しない `src/jpoke/core/move_executor.py` の
  記載自体は正しかったが、`tests/test_ability.py` 等の現存しないテストファイルパスを参照していた。
  現在のアーキテクチャ（`tests/abilities/`, `tests/moves_attack/`, `tests/items/` 等への分割）と
  実装の実態（`_is_sheer_force_blocked` ヘルパーによる常時発動効果のガード方式、対象技の
  フラグ運用ルール）に合わせて全面的に書き直した。「保留・論点」も本レビューで解消したため削除した。

## 実装（handlers/ / data/）
以下の誤り・欠落を修正した（一次情報との照合により、ちからずく対象と明記されている技・特性が
`secondary_effect` フラグや個別ガードを持たず、ちからずくの威力上昇・効果無効化の対象になって
いなかったもの）。

- `src/jpoke/data/moves/move_ka.py`「がんせきアックス」: `secondary_effect` フラグが欠落していた。
  一次情報のちからずく対象技リストに明記されているため追加した。
- `src/jpoke/handlers/move_attack.py`「がんせきアックス_set_stealth_rock」: ステルスロック設置に
  `_is_sheer_force_blocked` ガードが無かったため追加した（ひけん・ちえなみのまきびし設置と同じ
  パターン）。
- `src/jpoke/data/moves/move_ha.py`「ぶきみなじゅもん」: `secondary_effect` フラグが欠落していた。
  一次情報の「かげぬい/ぶきみなじゅもんなどがちからずくの効果対象になるフラグを持つ」旨の記載を
  根拠に追加した。
- `src/jpoke/handlers/move_attack.py`「ぶきみなじゅもん_reduce_defender_pp」: 相手技PP減少に
  `_is_sheer_force_blocked` ガードが無かったため追加した。
- `src/jpoke/handlers/ability.py`「ききかいひ_switch_on_half_hp」: ちからずくの効果が発動した技の
  ダメージ(`hp_change_reason == "move_damage"` かつ攻撃者がちからずく所持かつ使用技が
  `secondary_effect` フラグを持つ場合)ではききかいひが発動しないガードが無かったため追加した。
  `.internal/spec/abilities/ききかいひ.md`（既存レビュー済み仕様書）に明記されている挙動で、
  Championsでの例外記載も無いため無条件に適用されると判断した。
- `src/jpoke/handlers/ability.py`「へんしょく_copy_move_type」: ちからずくの効果が発動した技を
  受けた場合はタイプ変化しないガードが無かったため追加した。`.internal/spec/abilities/へんしょく.md`
  （既存レビュー済み仕様書）に明記されている挙動で、こちらもChampionsでの例外記載が無い。

### 確認したが変更しなかった箇所（意図的に現状維持）
- いっちょうあがり: `secondary_effect` フラグ付きのまま維持。`.internal/spec/moves/いっちょうあがり.md`
  に「エレクトロビームと異なりChampionsでも威力上昇効果を除外する記述はない」と明記されており、
  現状の実装（フラグあり）が正しい。
- エレクトロビーム: `secondary_effect` フラグ無しのまま維持。Championsではちからずくの効果が
  無いことが一次情報に明記されている。
- いかりのこうら: ちからずくガード無しのまま維持。`.internal/spec/abilities/いかりのこうら.md` に
  「Championsではこのときもいかりのこうらでランクが変動する（ぎゃくじょうと同様の修正）」と
  明記されているため、現状（ガード無し＝常時発動）が正しい。
- ぎゃくじょう / わるいてぐせ: ちからずくガード無しのまま維持。`.internal/spec/abilities/ちからずく.md`
  に「Championsではちからずくの技に対してもぎゃくじょう/わるいてぐせは発動する」と明記されている
  ため、現状（ガード無し＝常時発動）が正しい。
- だっしゅつボタン / レッドカード / アッキのみ / タラプのみ / かいがらのすず / いのちのたま:
  既存のちからずくガードは一次情報と一致しており変更なし。
- アンカーショット / オリジンズスーパーノヴァ / ひみつのちから: `src/jpoke/data/ps-champ-ja/moves.json`
  に収録が無く、Champions未実装の技のため対象外（既存の未実装状態のまま）。
- にげごし: 実装が存在しない（グソクムシの特性で、専用ポケモン未実装の可能性がある）。ちからずくとの
  相互作用は対象外。

## リーサル計算
- 対象外（`.internal/progress/ability.md` のちからずく行は「リーサル実装」「リーサルテスト」列とも n/a）。
  なお `handlers/lethal.py` の「アッキのみ」「タラプのみ」のリーサル実装に含まれるちからずく判定は
  一次情報と一致しており変更なし。

## テスト
以下のちからずく相互作用テストを追加した（全4995件のテストが成功することを確認、
`.loop/test_logs/ちからずく.log` 参照）。

- `tests/moves_attack/test_move_ka.py`:
  `test_がんせきアックス_ちからずくで威力上昇しステルスロックは設置されない`
- `tests/moves_attack/test_move_ha.py`:
  `test_ぶきみなじゅもん_ちからずくで威力上昇しPP減少は発動しない`
- `tests/abilities/test_ability_ka.py`:
  `test_ききかいひ_ちからずくの技のダメージでは発動しない`
  `test_ぎゃくじょう_ちからずくの技を受けても発動する`（Championsでの例外挙動の回帰防止）
- `tests/abilities/test_ability_ha.py`:
  `test_へんしょく_ちからずくの技を受けてもタイプ変化しない`
