# ライトメタル レビュー結果

## 仕様書
- 変更なし（一次情報 https://wiki.pokemonwiki.com/wiki/ライトメタル と全文照合し、誤り・欠落なしを確認した）

## 実装計画書
- テスト観点を補強した（くさむすび等おもさ参照技の威力変化、かるいしとの累積を追記）
- 検証コマンドの対象ファイルを実際のテストファイル（`tests/abilities/test_ability_yarawa.py` 等）に修正した
- 変更候補の記載も実際のファイルパスに修正した

## 実装（handlers/ / data/）
- 変更なし
  - `Pokemon.weight` プロパティ（`src/jpoke/model/pokemon.py`）でおもさ半減を実装済みであることを確認した
  - `data/ability.py` の `ライトメタル` エントリに `mold_breaker_ignorable` フラグが設定済みであることを確認した
  - かたやぶり無効化は `かたやぶり_disable_foe_ability` / `かたやぶり_restore_foe_ability`
    （`ON_BEGIN_MOVE` / `ON_END_MOVE`）が `Ability.name`（無効化時は空文字を返す `GameEffect.name`）を
    経由して汎用的に実現しており、`Pokemon.weight` の `match self.ability.name` 判定と整合していることを確認した
  - `かるいし` との重複計算順序（先に特性、後にアイテム）も仕様通りであることを確認した
  - 仕様書に記載のある「ボディパージ状態時はその効果の後にライトメタルを計算する」という項目は、
    ボディパージを発生させる技（オートトミー）自体が本リポジトリで未実装のため、現時点では対応不要と判断した
    （将来オートトミーを実装する際は `Pokemon.weight` の `w = self.data.weight` の直後に
    ボディパージ減少分を差し引く実装にすれば、既存のライトメタル/ヘヴィメタル/かるいし計算順序と整合する）

## リーサル計算
- 対象外（`.internal/progress/ability.md` のリーサル実装列が `n/a`）

## テスト
- `tests/abilities/test_ability_yarawa.py` に以下を追加した
  - `test_ライトメタル_おもさが半分になりくさむすびの威力が下がる`
    （フシギバナ 100.0kg → 50.0kg でくさむすび威力が 100 → 80 に変化することを確認）
  - `test_ライトメタル_かたやぶりで無効化されおもさが基本値になる`
    （かたやぶり持ちの技を受けるとライトメタルが無視され、くさむすび威力が基本おもさ基準の 100 に戻ることを確認。
    `test_ヘヴィメタル_かたやぶりで無効化されおもさが基本値になる` と対になる構成）
  - `test_ライトメタル_かるいしと重複しておもさが4分の1になる`
    （ライトメタル＋かるいしでおもさが基本値の 1/4 になることを確認）
- 既存の `tests/abilities/test_ability__group.py::test_体重操作系` はそのまま維持（おもさが0.5倍になる基本ケース）
- `python -m pytest tests/ -v` を実行し、5679 passed, 1 skipped（既存の無関係な `マグニチュード` テストのスキップ）で全件成功を確認した。ログは `.loop/test_logs/ライトメタル.log` に保存した
