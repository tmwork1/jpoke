# はとむね レビュー結果

## 仕様書
- 変更なし（`.internal/spec/abilities/はとむね.md` は一次情報 HTML から自動生成した内容と完全一致していることを確認した。差分なし）

## 実装計画書
- 修正した内容:
  - 仕様参照パスが旧形式 `spec/ability/はとむね.md` のままだったため、実在パス `.internal/spec/abilities/はとむね.md` に修正
  - 実装方針に、共通ヘルパー `_block_stat_drop_by_foe(value, ctx, stat="def")` を使うこと、
    `mold_breaker_ignorable` フラグでかたやぶり無効化を共通処理に委ねることを明記（クリアボディ計画書と同水準の記述に統一）
  - 実装ゴールに「かたやぶり系の効果を持つ技・特性には無視されてぼうぎょランクを下げられる」を追記
  - 変更候補のテストファイルを実態に合わせて `tests/test_ability.py` → `tests/abilities/test_ability__group.py` に修正
  - 更新日を更新

## 実装（handlers/ / data/）
- 変更なし。`handlers/ability.py` の `はとむね_block_B_drop` は `_block_stat_drop_by_foe(value, ctx, "def")` を呼び出しており、
  `data/ability.py` の登録（`ON_BEFORE_MODIFY_STAT` / `subject_spec="target:self"` / `flags={"mold_breaker_ignorable"}`）とも整合している。
  クリアボディ／かいりきバサミ／するどいめ／はっこうと同一パターンで実装されており、仕様書・計画書の内容と相違なし。

## リーサル計算
- 対象外（`.internal/progress/ability.md` のリーサル実装列が `n/a`。ランク低下防止のみで火力・耐久計算に影響しないため）

## テスト
- 修正した内容:
  - `tests/abilities/test_ability__group.py::test_能力低下防止特性_param` に、はとむねの自己効果によるぼうぎょランク低下は防げないケース
    （`("はとむね", {"def": -1}, True, {"def": -1})`）を追加した。従来は相手由来の低下を防ぐケースのみで、
    かいりきバサミと異なり自己効果ケースが欠けていた。
- `python -m pytest tests/ -v` は 5136 passed, 1 skipped で全テスト成功（flaky なテストは検出されなかった）。
