# どく レビュー結果

## 仕様書

- 変更なし
  - `.internal/spec/ailments/どく.md` の内容（効果・タイプ免疫・関連インタラクション・ターン処理優先度・
    ダメージ計算の詳細）を一次情報・実装と照合したが、誤り・欠落は見つからなかった

## 実装計画書

- 変更なし
  - `.internal/plan/ailments/どく.md` の内容（`どく_damage` の subject_spec・priority・計算式・
    最小値1保証の注意点）を仕様書・実装と照合したが、誤り・欠落は見つからなかった

## 実装（handlers/ / data/）

- 変更なし
  - `handlers/ailment.py` の `どく_damage`: `max(1, mon.max_hp // 8)` で最小ダメージ1を保証しており、
    `reason="poison"` で `modify_hp` を呼び出している（`ON_MODIFY_POISON_DAMAGE` 経由でポイズンヒール等が
    正しく動作する）。`mon.fainted` チェックも同ターンの先行ダメージでの蘇生防止として妥当
  - `data/ailment.py` の `"どく"` エントリ: `Event.ON_TURN_END` priority=90（`.internal/spec/turn.md` と一致）、
    `subject_spec="source:self"` で `EventContext` のロールと一致
  - タイプ免疫（どく・はがねタイプ、ふしょく例外）は `AilmentManager._can_apply_by_type` で実装済みで
    仕様と一致

## リーサル計算

- 変更なし
  - `.internal/progress/ailment.md` の「どく」行「リーサル実装」列は `x`（実装済み）
  - `handlers/lethal.py` の `どく_damage`（LethalEvent.ON_TURN_END, subject="defender"）を確認:
    `max(1, ctx.defender.max_hp // 8)` でダメージ計算し、`ポイズンヒール` 所持時は何もしない
    （ポイズンヒール側の `ポイズンヒール_heal` が同イベントで回復処理する分離設計）。
    通常ハンドラと計算式・最小値保証が一致しており、誤り・欠落は見つからなかった
  - マジックガードによる無効化は `.internal/progress/ability.md` でマジックガードのリーサル列が
    `n/a` と明記されており、リーサル計算のスコープ外として意図的に未対応（どく固有の欠落ではない）

## テスト

- 変更なし
  - `tests/test_ailment.py::test_どく_ダメージ`: ターン終了時ダメージ（`max_hp - max_hp // 8`）を確認
  - `tests/test_ailment.py::test_タイプ一致の状態異常は入らない`: どくタイプへの付与不可を確認
  - `tests/abilities/test_ability_ha.py` にふしょくとどく系状態異常のインタラクションテストが存在
  - `tests/test_lethal.py::test_どく_ターン終了時ダメージ`: リーサル計算でのどくダメージ加算を確認
  - `tests/test_lethal.py::test_ポイズンヒール_どく状態でターン終了時回復`: ポイズンヒールとの
    インタラクションを確認
  - 上記いずれも実装と整合しており、追加・修正の必要はなかった
  - `python -m pytest tests/ -v` で 5755 passed, 1 skipped（既存のskip、本レビューと無関係）
