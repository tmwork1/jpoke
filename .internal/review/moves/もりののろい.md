# もりののろい レビュー結果

## 仕様書
- 新規作成した（`.internal/spec/moves/もりののろい.md`）
- 一次情報（ポケモンWiki）をもとに、基本情報（タイプ: くさ / 分類: 変化 / PP: 20 / 命中: 100）、
  効果（相手のタイプにくさタイプを追加する）、失敗条件（既にくさタイプを持つ／テラスタル中）、
  技自体がくさタイプであることによる無効化（そうしょく）、防がれる条件、
  タイプ追加の実装方式（`added_types` を使う揮発性状態方式）を記載した

## 実装計画書
- 既存の `.internal/plan/moves/もりののろい.md` は実装前の古い草案（`ON_CALC_DEF_TYPE_MODIFIER` で
  倍率を乗算する未確定の方式を検討する内容）のままで、実際の実装（`added_types` 方式）と
  乖離していたため、現状の実装内容に合わせて全面的に書き直した
- テラスタル中の失敗条件をハンドラ構成・実装箇所に明記した

## 実装（handlers/ / data/）
- `handlers/move_status.py` の `もりののろい_can_apply` に **テラスタル中の相手には失敗する** という
  仕様（一次情報に明記）の判定が漏れていたため、`defender.terastallized` チェックを追加した
  （みずびたし・まほうのこなと同じ扱い）
- その他（`もりののろい_apply`、`data/moves/move_ma.py` のエントリ、`data/volatile.py` の
  「もりののろい」volatile、`handlers/volatile.py` の `もりののろい_add_type` /
  `もりののろい_remove_type`）は仕様書・計画書と整合しており変更なし

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」列が `n/a`）

## テスト
- テラスタル中の相手に対する失敗を検証するテスト
  `test_もりののろい_テラスタル中の相手には失敗する` を `tests/moves_status/test_move_ma.py` に追加した
  （`test_みずびたし_テラスタル中の相手には失敗する` と同じパターン）
- 既存の4テスト（くさタイプ付与・すでにくさタイプなら失敗・volatile付与・交代後リセット）は
  実装と一致しており変更なし
- `python -m pytest tests/ -v` を実行し、全4438件パス（1件skip）を確認した
  （ログ: `.loop/test_logs/もりののろい.log`）
