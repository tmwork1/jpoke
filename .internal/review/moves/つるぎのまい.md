# つるぎのまい レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/つるぎのまい.md`）。ポケモンWiki一次情報（`.internal/wiki/moves/つるぎのまい.html`）
  に基づき、タイプ・分類・PP（20、champions基準）・対象（自分）・効果（こうげき2段階上昇）・
  まもる／マジックコート／よこどり等の判定フラグを記載した。

## 実装計画書
- 修正した。既存の計画書は `EventContext` / `labels=[...]` / `stats={"A": 2}` など、現行コードベースの
  実装規約（`AttackContext` / `flags={...}`（set） / `stats={"atk": 2}`）と食い違う記述だったため、
  実際の実装・他の類似技（`ちょうのまい` 等）の計画書フォーマットに合わせて全面的に更新した。
  内容自体（priority=100、subject_spec=attacker:self デフォルト、target="self"、dance フラグ）に
  誤りはなく、表記のみの修正。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/move_status.py` の `つるぎのまい_modify_attacker_stats` は
  `modify_attacker_stats(battle, ctx, value, stats={"atk": 2})` を呼び出しており仕様どおり。
  `src/jpoke/data/moves/move_ta.py` の `MoveData` は `type="ノーマル"`, `category="status"`, `pp=20`,
  `target="self"`, `flags={"dance"}` で、champions PP値・対象・まもる/マジックコート回避すべて正しい。

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行は `n/a`）。ダメージを与えない自己バフ技のためリーサル計算は不要。

## テスト
- 変更なし。`tests/moves_status/test_move_ta.py::test_つるぎのまい_こうげき2段階上がる` が
  使用後に `atk` ランクが 0→2 になることを確認しており、仕様と一致。追加・修正は不要と判断した。
- `python -m pytest tests/ -v` を実行し全件パスを確認した（結果: `.loop/test_logs/つるぎのまい.log`）。
  ※ 実行順によって `test_field.py` のクリティカル関連テストや `しんぴのつるぎ` のテストが
  つるぎのまいの変更と無関係にまれに失敗することを確認したが（変更前のコードでも再現）、
  今回の対象範囲外の既存フレークであり本エントリの修正では対処していない。
