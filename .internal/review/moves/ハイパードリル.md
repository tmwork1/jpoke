# ハイパードリル レビュー結果

## 仕様書

- 新規作成（`.internal/spec/moves/ハイパードリル.md`）。
- `.internal/wiki/moves/ハイパードリル.html`（タイプ: ノーマル、分類: 物理、威力: 100、命中率: 100、
  PP: 5、範囲: 1体選択、優先度: 0、直接攻撃: あり、効果: 「まもる」状態を無視して攻撃する）を
  根拠に作成した。
- `src/jpoke/data/ps-champ-ja/moves.json` に本技のエントリ自体が存在しない（ハイドロポンプと
  ハイパーボイスの間を確認したが該当行なし）ため、PPはChampionsのPP再定義（8/12/16/20統一）の
  対象外と判断し、Gen9本家の値（5）をそのまま採用する旨を明記した。実装の `pp=5` と一致している。
- Wikiの「技の仕様」節（まもる状態は無視するが解除しない／かえんのまもり等の防御系変化技の
  反撃効果も受けない／ゆびをふるで選ばれない）を仕様書に反映した。

## 実装計画書

- 新規作成（`.internal/plan/moves/ハイパードリル.md`）。
- `data/moves/move_ha.py` の既存実装（`flags={"contact", "unprotectable"}`、`handlers={}`）を
  仕様書と照合し、誤り・欠落がないことを確認した。
- `unprotectable` フラグが `model/move.py` の `Move.is_blocked_by_protect` を通じて
  `handlers/volatile.py` の `_check_protect_success`/`_run_protect`（まもる・みきり・
  かえんのまもり・トーチカ・ニードルガード・ファストガード等、防御系変化技の共通骨格）と
  `handlers/ability.py` 側の判定の双方に反映される仕組みであることを確認し、専用実装が
  不要である根拠として明記した。
- `_run_protect` は防御不成立時にまもる状態を解除する処理を持たないため、
  「まもる系状態は解除しない」という仕様と実装が自然に一致していることを確認した。

## 実装（handlers/ / data/）

- 変更なし。`src/jpoke/data/moves/move_ha.py` の `ハイパードリル` エントリ
  （type=ノーマル, category=physical, pp=5, power=100, accuracy=100,
  flags={"contact", "unprotectable"}, handlers={}）は仕様書・一次情報と一致しており、
  防御貫通フラグ（`unprotectable`）も正しく設定されていることを確認した。
  `_run_protect` の共通構造により、まもる系状態は無視しつつ状態自体を解除しない挙動、
  および接触時反撃効果（かえんのまもり等）を受けない挙動も自動的に満たされている。

## リーサル計算

- 対象外（`.internal/progress/move.md` の当該行は「リーサル実装: n/a」「リーサルテスト: n/a」）。
  追加効果を持たない通常の物理攻撃技のため、専用のリーサル計算ハンドラは不要と判断した。

## テスト

- 変更なし。`tests/moves_attack/test_move_ha.py` の
  `test_ハイパードリル_まもる中の相手にダメージが入る`（まもる状態の相手に
  unprotectableフラグでダメージが通ることを確認）で必要なケースが既にカバーされていることを
  確認した。過不足なし。
- `python -m pytest tests/ -v` を実行し、3933件成功・0件失敗・1件スキップで全件パスすることを
  確認した（ログ: `.loop/test_logs/ハイパードリル.log`）。

## 進捗表

- `.internal/progress/move.md` の `ハイパードリル` 行を更新: 仕様書 `x`、計画書 `x`、レビュー `✓`
  に変更（実装 `x`・テスト `x`・リーサル実装/テスト `n/a`/`n/a`・分類/タイプ/威力/命中率/PP/対象・
  効果列は変更なし、既存の記述が実装・一次情報と一致していることを確認した）。
