# ぜったいれいど レビュー結果

## 仕様書

- 新規作成（`.internal/spec/moves/ぜったいれいど.md`）。
  - 一次情報（`.internal/wiki/moves/ぜったいれいど.html`）と `.internal/spec/moves/_ohko.md` を基に、
    こおりタイプ・特殊・威力なし・命中30・PP5・非接触の基本情報を整理した。
  - 本技固有の仕様として、対象がこおりタイプの場合の無効化（通常のタイプ相性表では
    こおり×こおりは0.5倍でしかないため専用ハンドラが必要）と、使用者がこおりタイプでない場合の
    命中率20への低下を明記した。
  - `.internal/spec/turn.md` の `Event.ON_TRY_MOVE_2`（優先度120）「こおりタイプ: ぜったいれいど無効」
    を根拠として引用した。

## 実装計画書

- 新規作成（`.internal/plan/moves/ぜったいれいど.md`）。
  - 既存実装（`ha.ohko_damage` による確定ダメージ設定のみ）では、こおりタイプへの無効化と
    命中率低下が未実装だった旨を記録した。
  - `Event.ON_TRY_MOVE_2`（priority=120）でこおりタイプ無効化を、`Event.ON_MODIFY_ACCURACY`
    （デフォルトpriority）で命中率低下を実装する方針とし、既存の類似ハンドラ
    （`メロメロ_check_gender`、`かみなり_accuracy`／`ふぶき_accuracy`）を参照した根拠を明記した。

## 実装（handlers/ / data/）

- `src/jpoke/handlers/move_attack.py` に以下を追加した。
  - `ぜったいれいど_check_ice_immunity`: 対象がこおりタイプの場合、`MOVE_IMMUNED` ログを出して
    技を無効化する（`ON_TRY_MOVE_2`、priority=120）。
  - `ぜったいれいど_modify_accuracy`: 使用者がこおりタイプでない場合、命中率を20に上書きする
    （`ON_MODIFY_ACCURACY`）。
- `src/jpoke/data/move.py` の `ぜったいれいど` エントリに上記2つのハンドラ登録を追加した
  （既存の `ON_MODIFY_MOVE_DAMAGE` は変更なし）。
- `python scripts/sort_handlers.py src/jpoke/handlers/move_attack.py` と
  `python scripts/sort_data/sort_moves.py` を実行済み。

## リーサル計算

- 対象外（`.internal/progress/move.md` の該当行は「リーサル実装: n/a」「リーサルテスト: n/a」）。

## テスト

- `tests/moves_attack/test_move_sa.py` に以下を追加した。
  - `test_ぜったいれいど_こおりタイプでない使用者は命中率が20になる`
  - `test_ぜったいれいど_こおりタイプの使用者は命中率が30のまま`
  - `test_ぜったいれいど_こおりタイプの相手には無効`
  - `test_ぜったいれいど_命中時は相手を一撃で倒す`
- `python scripts/sort_tests.py tests/moves_attack/test_move_sa.py` でソート済み。
- `python scripts/generate_test_list.py` で `.internal/tests/` を更新済み。
- `python -m pytest tests/ -v` は 3420 passed, 1 skipped で全件パス
  （結果は `.loop/test_logs/ぜったいれいど.log` に保存）。

## 進捗表の修正

- `.internal/progress/move.md` の `ぜったいれいど` 行を更新（仕様書・計画書・レビューを更新、
  PPの誤記載 `8` を実際の基本PP `5` に修正）。
- ついでに同一の誤記載が見つかった `じわれ`・`つのドリル`・`ハサミギロチン` 行のPP列
  （誤って `8` と記載されていた）も `5` に修正した（一次情報で全て基本PP5を確認済み）。
