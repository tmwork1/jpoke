# ふんえん レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ふんえん.md`）。
  - `.internal/wiki/moves/ふんえん.html` の一次情報（タイプ: ほのお / 分類: 特殊 / 威力80 / 命中100 / PP15 /
    対象: 自分以外の全員 / 優先度0 / 直接攻撃なし / 追加効果30%やけど / Zワザ威力160）をもとに記載。
  - ほのおタイプへのやけど耐性は `AilmentManager._can_apply_by_type` の汎用処理で担保されている旨を明記。
  - ちからずく・おうじゃのしるし・マジックコート・よこどり・オウムがえしとの相互作用を明記。

## 実装計画書
- 新規作成（`.internal/plan/moves/ふんえん.md`）。
  - 既存実装（`data/moves/move_ha.py` / `handlers/move_attack.py`）が仕様と一致していることを記録。
  - `ON_DAMAGE_HIT` / priority 100（デフォルト）で `ほうでん` 等の同種ハンドラと同じパターンであることを
    根拠として記載。

## 実装（handlers/ / data/）
- 変更なし。既存実装（`data/moves/move_ha.py` の `ふんえん` エントリ、
  `handlers/move_attack.py` の `ふんえん_apply_burn_to_defender`）は仕様書・計画書と完全に一致しており、
  修正不要と判断した。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列は `n/a`）。追加効果由来の確定ダメージ等はなく、
  リーサル計算ハンドラは不要。

## テスト
- 変更なし。`tests/moves_attack/test_move_ha.py` の `test_ふんえん_やけどが発動する` が
  `secondary_chance=1.0` でやけど付与を確認しており、既存実装のカバレッジとして十分と判断した。
- `python -m pytest tests/ -v` で全3579件（+skip 1件）がパスすることを確認（
  `.loop/test_logs/ふんえん.log` に記録）。

## 進捗表
- `.internal/progress/move.md` の `ふんえん` 行を更新。
  - 仕様書・計画書列を `x` に更新。
  - PP列を誤記の `16` から実装・一次情報と一致する `15` に修正。
  - レビュー列を `✓` に更新。
  - 効果列を「？」から「追加効果として、30%の確率で相手をやけど状態にする。」に更新。
