# とびかかる レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/とびかかる.md`）。一次情報（`.internal/wiki/moves/とびかかる.html`）をもとに
  タイプ・分類・威力・命中・PP・対象・優先度・接触の有無・追加効果（100%の確率で相手のこうげきを1段階下げる）・
  各種判定フラグ（急所／命中判定／追加効果／まもる／おうじゃのしるし／マジックコート／よこどり／オウムがえし）を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/とびかかる.md`）。仕様書の内容とハンドラ構成（`ON_DAMAGE_HIT` / priority 100
  デフォルト / `modify_defender_stats` による atk -1）、Priority根拠、実装箇所、注意点を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/move.py` の `とびかかる` エントリで `flags` に `secondary_effect` が欠落していたため追加した。
  - この欠落により、ちからずく特性による威力1.3倍化・追加効果無効化（`ちからずく_boost` /
    `ちからずく_disable_secondary_effect`）が機能していなかった不具合を修正。
  - `python scripts/sort_data/sort_moves.py` を実行し並び順を確認した。
  - `handlers/move_attack.py` の `とびかかる_lower_defender_atk`（`modify_defender_stats` で atk -1）は
    仕様書と一致しており修正不要。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」「リーサルテスト」列は n/a）。

## テスト
- `tests/moves_attack/test_move_ta.py` に以下を追加:
  - `test_とびかかる_こうげき1段階低下が発動する`: 100%の確率で相手のこうげきランクが1段階下がることを確認。
  - `test_とびかかる_secondary_effectフラグを持つ`: `secondary_effect` フラグを持つことを確認（ちからずく相互作用の回帰防止）。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ta.py` でソートし、
  `python scripts/generate_test_list.py` でテスト一覧を更新した。
- `python -m pytest tests/ -v` で全件（3455 passed, 1 skipped）パスを確認した。

## 進捗表
- `.internal/progress/move.md` の PP 列が実際の値（15）と異なり `16` と誤記載されていたため修正した。
- 仕様書・計画書・テスト・レビュー列を実装状況に合わせて更新した。
