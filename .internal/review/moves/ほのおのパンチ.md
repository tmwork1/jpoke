# ほのおのパンチ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ほのおのパンチ.md`）。一次情報（ポケモンWiki）と照合し、タイプ・分類・威力75・命中100・PP15・直接攻撃・パンチ技フラグ・追加効果10%やけどを記載。

## 実装計画書
- 新規作成（`.internal/plan/moves/ほのおのパンチ.md`）。既存実装が仕様書どおりであることを確認し、事後記録として作成。priorityの根拠は既実装の`かみなりパンチ`・`ほのおのキバ`（`Event.ON_DAMAGE_HIT`, priority=100）と同一である旨を明記した。

## 実装（handlers/ / data/）
- 変更なし。`handlers/move_attack.py` の `ほのおのパンチ_apply_burn_to_defender` は共通関数 `apply_ailment_to_defender` を使いやけど10%を正しく実装している。`data/moves/move_ha.py` の `MoveData`（type/category/pp/power/accuracy/flags）は一次情報と一致していることを確認した。
- なお `.internal/champions/changes_from_sv.md` の「技のPPが4段階（8/12/16/20）に統一」という記述、および `src/jpoke/data/ps-champ-ja/moves.json` ではほのおのパンチのPPが16と記載されているが、既に✓レビュー済みの`かみなりパンチ`を含む223件の既存実装・進捗表はいずれもSV基準PP（この技では15）を採用しており、一次情報（Wiki）のPPも15であるため、プロジェクトの既存方針との整合性を優先しPP=15を維持した（変更していない）。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- 変更なし。`tests/moves_attack/test_move_ha.py::test_ほのおのパンチ_やけどが発動する` が既存実装と仕様書に整合していることを確認した。全テスト（3608件）が通過することを確認済み。
