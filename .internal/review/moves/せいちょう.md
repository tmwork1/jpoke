# せいちょう レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/せいちょう.md`）
- 一次情報（ポケモンWiki）と `src/jpoke/data/ps-champ-ja/moves.json` を突き合わせ、以下を明記
  - タイプは第一世代〜スカーレット・バイオレットまでノーマルだったが、LEGENDS Z-A・
    Championsでは **くさタイプに変更**されている点
  - 対象は自分（`target="self"`）であり、まもる・マジックコートの対象にならない点
  - ばんのうがさ／ノーてんき／エアロックとの相互作用（にほんばれ補正の無効化）
  - たんじゅんによるランク変化量2倍の相互作用

## 実装計画書
- 既存の `.internal/plan/moves/せいちょう.md` は情報が古かったため修正した
  - タイプを `ノーマル` → `くさ` に修正
  - `target="self"` の明記漏れを追記（未指定だとデフォルト値 `"foe"` になり、まもる・
    マジックコートの判定を誤る旨を明記）
  - `category="変化"`（誤記）→ `category="status"` に修正
  - 天候判定を `battle.weather.sunny`（グローバル天候）から `battle.weather_for(ctx.attacker).sunny`
    （使用者視点の有効天候。ばんのうがさ／ノーてんき／エアロックを自動的に考慮）に修正
  - 参照先ファイルパスを `src/jpoke/data/move.py` → `src/jpoke/data/moves/move_sa.py` に修正
  - たんじゅんとの相互作用（`battle.modify_stats` による倍化）を追記

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_sa.py` の `"せいちょう"` エントリを修正
  - `type="ノーマル"` → `type="くさ"`（Champions/LEGENDS Z-Aでのタイプ変更に追従）
  - `target="self"` を追加（欠落していたため、デフォルト値 `"foe"` によりまもる・
    マジックコートの判定を誤って通過してしまうバグがあった）
- `src/jpoke/handlers/move_status.py` の `せいちょう_modify_attacker_stats` は
  `battle.weather_for(ctx.attacker).sunny` を使用しており妥当だったため変更なし
  （ばんのうがさ／ノーてんき／エアロックは `weather_for` 側で汎用的に処理される）
- PP（20）は `src/jpoke/data/ps-champ-ja/moves.json` と一致しており変更なし

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行「リーサル実装」列は `n/a`）

## テスト
- 既存の天候別ランク上昇テスト（はれ/おおひでり2段階、通常時1段階）は妥当なため変更なし
- 以下を新規追加（`tests/moves_status/test_move_sa.py`）
  - `test_せいちょう_まもるで防がれない`（target欠落バグの回帰テスト）
  - `test_せいちょう_マジックコートで跳ね返されない`（target欠落バグの回帰テスト）
  - `test_せいちょう_ばんのうがさで晴れでも1段階のみ`
  - `test_せいちょう_ノーてんきで晴れでも1段階のみ`
  - `test_せいちょう_たんじゅんで晴れ中4段階上がる`
- `python -m pytest tests/ -v` で全4213件パスを確認（`.loop/test_logs/せいちょう.log`）。
  実行中に見られた `test_field.py` / `test_ability_ta.py` の一部失敗は急所発生有無に依存する
  既存の flaky テスト（本タスクと無関係）であり、再実行で解消することを確認済み
