# みだれづき レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/みだれづき.md`）。一次情報（.internal/wiki/moves/みだれづき.html）および
  `.internal/spec/moves/_multi_hit.md`「9.3 2-5回攻撃技」と照合し、タイプ=ノーマル、分類=物理、
  威力15（1ヒットあたり）、命中85、PP20、接触あり、2～5回攻撃・命中判定は1発目のみ・急所は
  各ヒット独立判定・追加効果なし、を記載。同系統の `みだれひっかき.md` を参考テンプレートとした。

## 実装計画書
- 新規作成（`.internal/plan/moves/みだれづき.md`）。仕様書をもとに、既存実装が
  `multi_hit={"min": 2, "max": 5, "check_hit_each_time": False, "power_sequence": ()}` の
  汎用機構のみで仕様を満たしており、固有ハンドラ不要であることを明記した
  （`みだれひっかき` の計画書と同一パターン）。

## 実装（handlers/ / data/）
- 変更なし
- `src/jpoke/data/moves/move_ma.py` の `MoveData("みだれづき")` を確認。
  type="ノーマル", category="physical", pp=20, power=15, accuracy=85, flags={"contact"},
  multi_hit={"min": 2, "max": 5, "check_hit_each_time": False, "power_sequence": ()}, handlers={} であり、
  一次情報（威力15・命中85・PP20・直接攻撃○・連続攻撃技2-5回・命中判定は初回のみ・追加効果なし）と完全に一致。
  兄弟技のみだれひっかき・スイープビンタなどと同じ構造で実装されており、誤り・欠落なし。

## リーサル計算
- 対象外
- `.internal/progress/move.md` のみだれづき行のリーサル実装列は n/a のため対象外。

## テスト
- 変更なし
- `tests/moves_attack/test_move_ma.py` に以下が既に整備されていることを確認した（前回レビューで追加済み）:
  - `test_みだれづき_相手にダメージを与える`: 追加効果のない通常攻撃技として相手にダメージを与えることを確認。
  - `test_みだれづき_複数ヒットする`: `battle.move_executor._resolve_hit_count()` の結果が2〜5回の範囲であることを確認
    （兄弟技`test_みだれひっかき_複数ヒットする`と同じ検証パターン）。
- `python -m pytest tests/ -v` で全5801件（+1 skipped）が成功することを確認。
