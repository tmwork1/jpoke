# じゅうでん レビュー結果

## 仕様書
- `.internal/spec/moves/じゅうでん.md` が存在しなかったため新規作成した（技本体の基本情報・効果・判定フラグ）。
- `.internal/spec/volatiles/じゅうでん.md`（揮発性状態側の仕様書）は既存かつ内容も一次情報と一致しており変更なし。

## 実装計画書
- `.internal/plan/moves/じゅうでん.md`・`.internal/plan/volatiles/じゅうでん.md` はいずれも既存で、内容は実装と一致しており変更なし。

## 実装（handlers/ / data/）
- `handlers/move_status.py` の `じゅうでん_apply`、`data/moves/move_sa.py` の `"じゅうでん"` MoveData、
  `handlers/volatile.py` の `じゅうでん_boost_electric`、`data/volatile.py` の `"じゅうでん"` VolatileData は
  いずれも仕様と一致しており変更なし。
- **バグ修正**: `handlers/move_status.py` の `_BATON_PASS_VOLATILES`（バトンタッチで引き継ぐ揮発性状態の名前セット）に
  `"じゅうでん"` が誤って含まれていた。一次情報（`.internal/wiki/moves/バトンタッチ.html` の状態変化引き継ぎ一覧表、
  および `.internal/wiki/volatiles/じゅうでん_(状態変化).html` の「バトンタッチで引き継がれない。」という明記）と
  既存の `.internal/spec/volatiles/じゅうでん.md`（「バトンタッチで引き継がれない」と明記済み）に反していたため、
  `_BATON_PASS_VOLATILES` から `"じゅうでん"` を削除した。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`。じゅうでんはダメージを与えない変化技のため）。

## テスト
- `tests/moves_status/test_move_ha.py` の
  `test_バトンタッチ_引き継ぎ対象volatileが交代先に引き継がれる` パラメータから `"じゅうでん"` を削除し、
  `test_バトンタッチ_引き継ぎ対象外volatileは交代先に引き継がれない` パラメータに `"じゅうでん"` を追加した
  （バグ修正に伴う期待値の是正）。
- `tests/moves_status/test_move_sa.py` に技「じゅうでん」自体の効果を検証するテストを新規追加:
  - `test_じゅうでん_じゅうでん状態付与ととくぼう上昇`
  - `test_じゅうでん_とくぼうランク最大でも状態は付与される`
- `tests/volatiles/test_volatile_satana.py` の既存テスト（威力補正・交代解除・非でんき技で継続）は
  仕様と一致しており変更なし。
- `python -m pytest tests/ -v` を実行し、4179 passed, 1 skipped で全件パスを確認
  （`.loop/test_logs/じゅうでん.log` に保存）。
