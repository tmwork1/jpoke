# トライアタック レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/トライアタック.md`）。ポケモンWiki一次情報（`.internal/wiki/moves/トライアタック.html`）を基に、
  ノーマル/特殊/威力80/命中100、追加効果20%でまひ・やけど・こおりのいずれか（各1/3、単一乱数判定）を記載。
  PPは Champions 基準（`src/jpoke/data/ps-champ-ja/moves.json`）で12であることを明記（Gen9本家は10）。

## 実装計画書
- 新規作成（`.internal/plan/moves/トライアタック.md`）。既存の共有計画書 `.internal/plan/moves/攻撃技_特殊効果系.md` に
  トライアタックの内容が含まれていたが、他の同グループ技（いにしえのうた・しおづけ等）と同様に
  個別ファイルへ分離していなかったため、単独計画書として新規作成した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py`: `pp=10` → `pp=12` に修正（Champions基準の `move_list.txt` に合わせるバグ修正。
  `.internal/progress/move.md` のPP列は既に12となっており、実装側が移行漏れだった）。
- `src/jpoke/handlers/move_attack.py` の `トライアタック_apply_ailment_to_defender` はロジック・
  `subject_spec`（AttackContext の `attacker:self` 相当のデフォルト）とも仕様書・計画書と整合しており、修正不要。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が n/a）。

## テスト
- `tests/moves_attack/test_move_ta.py` に PP=12 の回帰テスト
  `test_トライアタック_PPは12` を追加。
- 既存のまひ/やけど/こおり/追加効果なしの4テストは境界値計算を確認したところ正しく実装されており、修正不要。
- `python -m pytest tests/ -v` で3900件パス、1件skip（既存の無関係skip）を確認（
  `.loop/test_logs/トライアタック.log`）。
