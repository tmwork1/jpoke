# うたう レビュー結果

## 仕様書
- 存在しなかったため新規作成した（`.internal/spec/moves/うたう.md`）。
- 一次情報（ポケモンWiki）と `src/jpoke/data/ps-champ-ja/moves.json` を照合し、タイプ/分類/命中率/PP/対象/効果/フラグ/判定を記載。
- PPは Gen9本家の15ではなく、champions側で標準化された16を採用（`changes_from_sv.md` のPP4段階統一ルールに基づく）。

## 実装計画書
- 既存の計画書（`.internal/plan/moves/うたう.md`）に以下の誤りがあり、実装済みの内容に合わせて修正した。
  - PP値が15のままだったのを16に修正（champions/move_list.txtが正）。
  - `MoveData` のフラグ指定が旧仕様の `labels=["sound"]` になっていたのを、実際のモデル定義（`flags: set[MoveFlag]`）に合わせて `flags={"sound"}` に修正。
  - ハンドラ関数名が計画書では `うたう_apply_ailment_to_defender` / `ctx: EventContext` となっていたが、実装済みの実際の関数名 `うたう_apply_sleep` / `ctx: AttackContext` に合わせて修正。
  - 「現状の実装状況」節を「未実装」から「実装済み」の記述に更新。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `うたう` エントリで `pp=15` になっていたバグを `pp=16` に修正（champions/move_list.txtの値に合わせた）。
- `handlers/move_status.py` の `うたう_apply_sleep`（`apply_ailment_to_defender` 経由でねむり付与）は仕様と一致しており、修正なし。
- `subject_spec` はデフォルトの `attacker:self`（`MoveHandler` 既定値）で、`AttackContext` のロールと一致している。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- 既存テスト `test_うたう_ねむり付与`（`tests/moves_status/test_move_a.py`）は accuracy=100 固定でねむり付与を確認しており、実装と一致。追加修正なし。
- `python -m pytest tests/ -v` で全4093件（+1 skipped）が通過することを確認した。
