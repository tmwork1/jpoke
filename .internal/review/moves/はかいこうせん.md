# はかいこうせん レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/はかいこうせん.md`）。
  - 既存の `.internal/spec/moves/がんせきほう.md` と同様の「リチャージ」系技の共通仕様を踏襲。
  - タイプ: ノーマル / 分類: 特殊 / 威力150 / 命中90 / PP5 / 対象1体 / 優先度0 / 接触なし / 分類フラグなし。
  - 命中して成功した次のターンに反動で行動不能になる（リチャージ状態）効果、まもる等で防がれた場合や
    命中しなかった場合は付与されないこと、相手をひんしにした場合でも付与されることを明記。

## 実装計画書
- 新規作成（`.internal/plan/moves/はかいこうせん.md`）。
  - `.internal/plan/moves/がんせきほう.md` と同じ構成で、既存実装（`data/move.py` の `はかいこうせん` エントリ、
    `handlers/move_attack.py` の `リチャージ_apply`、`handlers/volatile.py` の `リチャージ_block_action`）が
    共用ハンドラとして正しく機能していることを確認・記載。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `はかいこうせん` エントリ（type=ノーマル, category=special, pp=5,
  power=150, accuracy=90, `ON_DAMAGE_HIT` で `リチャージ_apply` を登録）は仕様書と一致しており修正不要。
  共用の `リチャージ_apply` / `リチャージ_block_action` も他のリチャージ系技（がんせきほう等）で
  レビュー済みのため修正不要。

## リーサル計算
- 対象外（`.internal/progress/move.md` で「リーサル実装: n/a」「リーサルテスト: n/a」）。
  反動で動けなくなる効果はHP依存の状態ではないため、専用のリーサルハンドラは不要。

## テスト
- 既存の5テスト（まもるで防がれた場合／交代で解除／命中後付与／外れた場合／次ターン行動不能）は
  仕様と一致しており問題なし。
- 追加: `test_はかいこうせん_相手をひんしにしてもリチャージ状態が付与される`
  （`tests/moves_attack/test_move_ha.py`）— 一次情報にある「相手をひんしにしたときもリチャージ状態になる」
  というエッジケースをカバー。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ha.py` でソート済み。
- `python scripts/generate_test_list.py` でテスト一覧を更新済み。
- `python -m pytest tests/ -v` で全3547件（+1 skipped）がパス。結果は `.loop/test_logs/はかいこうせん.log` に保存。
