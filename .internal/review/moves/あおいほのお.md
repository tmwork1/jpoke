# あおいほのお レビュー結果

## 仕様書
- `.internal/spec/moves/あおいほのお.md` のような専用ファイルは作成していない（かえんぐるま・だいもんじ等、単純な追加効果技は個別仕様書を持たず `.internal/spec/moves/_move_secondary_1.md` のカタログ表に集約する既存方針に合わせた）。
- `.internal/spec/moves/_move_secondary_1.md` に `あおいほのお defender 0.2 ... やけど1 ...` の行を新規追加（一次情報どおり「20%の確率で相手をやけど状態にする」を反映）。

## 実装計画書
- `.internal/plan/moves/あおいほのお.md` は作成していない（上記と同じ理由。だいもんじ・かえんぐるま等の同種技も個別計画書を持たない）。

## 実装（handlers/ / data/）
- `src/jpoke/data/move.py` の `あおいほのお` エントリが `handlers={}` （コメント「追加効果なし」）となっており、一次情報の「20%の確率でやけど」という追加効果が実装漏れだったため修正。
  - `flags={"secondary_effect"}` を追加。
  - `Event.ON_DAMAGE_HIT` に `ha.あおいほのお_apply_burn_to_defender` を登録（だいもんじ等と同じ priority/イベントの慣例に合わせた）。
- `src/jpoke/handlers/move_attack.py` に `あおいほのお_apply_burn_to_defender`（`apply_ailment_to_defender(..., ailment="やけど", chance=0.2)`）を新規実装。
- `python scripts/sort_handlers.py src/jpoke/handlers/move_attack.py` と `python scripts/sort_data/sort_moves.py` を実行して並び順を整えた。

## テスト
- `tests/moves_attack/test_move_a.py` に `test_あおいほのお_やけどが発動する`（レシラムを使用し、`secondary_chance=1.0` でやけど付与を確認）を追加。
- `python scripts/sort_tests.py tests/moves_attack/test_move_a.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新。
- `python -m pytest tests/ -v` を実行し、あおいほのお関連テストは全てパス。
  なお `test_ability_ta.py::test_てんねん_...` と `test_megaevol.py::test_メガシンカ_フォルムが変わる[...カエンジシ.../シビルドン...]` の一部が `KeyError: 'ほのおのたてがみ'`（特性データ未登録）で失敗しているが、これは本タスク（あおいほのお）と無関係な並行作業（他特性の実装途中状態）によるものであり、本レビューでは対応していない。

## 進捗表
- `.internal/progress/move.md` の `あおいほのお` 行を更新: テスト=x、レビュー=✓、効果列を「追加効果として、20%の確率で相手をやけど状態にする。」に修正。
