# かぜおこし レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/かぜおこし.md`）。
- ポケモンWiki一次情報（`.internal/wiki/moves/かぜおこし.html`）をもとに、タイプ・分類・威力・命中・PP・
  対象・優先度・接触判定・追加効果なし・風技フラグ・`そらをとぶ`滞空中の相手への命中と威力2倍
  （第二世代以降）・各種判定フラグ（急所／命中判定／追加効果／まもる／おうじゃのしるし／
  マジックコート／よこどり／オウムがえし）を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/かぜおこし.md`）。
- 既存の `data/move.py` の `"かぜおこし"` エントリ（`type="ひこう"`, `category="special"`,
  `pp=35`, `power=40`, `accuracy=100`, `flags={"wind"}`, `handlers={}`）が仕様と一致していることを
  確認し、変更不要である旨を記載。
- `そらをとぶ` 滞空中の命中・威力2倍処理は `handlers/volatile.py` の
  `そらをとぶ_can_hit_hidden_target` / `そらをとぶ_boost_power` に既存実装済みであり、
  本技固有のハンドラは不要であることを明記。
- リーサル計算は対象外（n/a）である根拠を記載。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `"かぜおこし"` エントリ（type/category/power/accuracy/pp/flags）
  は一次情報と一致しており、追加効果を持たない通常攻撃技として `handlers={}` になっている点も
  仕様と一致することを確認した。
- `そらをとぶ` 側の威力2倍・命中判定（`handlers/volatile.py`）、風技依存の `ふうりょくでんき`
  （`handlers/ability.py`）、おうじゃのしるし等の汎用ひるみ判定（`handlers/item.py` の
  `flinch_on_hit_10pct`、`かぜおこし` は除外リストに含まれない）も仕様と整合していることを確認した。

## リーサル計算
- 対象外。追加効果を持たない通常攻撃技であり、HP依存の分岐やダメージ以外の状態変化がないため
  リーサル計算ハンドラは不要（`.internal/progress/move.md` は既に `n/a` / `n/a`）。

## テスト
- `tests/moves_attack/test_move_ka.py` に `test_かぜおこし_相手にダメージを与える` を追加。
  かぜおこし単体の基本ダメージ挙動をカバーするテストがこれまで存在しなかったため新規追加。
- `tests/volatiles/test_volatile_satana.py` の `test_そらをとぶ_かぜおこしたつまきの威力が2倍になる`、
  `tests/abilities/test_ability_ha.py` の `test_ふうりょくでんき_風技を受けるとじゅうでん` は
  既存のまま変更なし（既にかぜおこしの風技・そらをとぶ相互作用をカバーしている）。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ka.py` でソート、
  `python scripts/generate_test_list.py` でテスト一覧を更新、
  `python -m pytest tests/ -v` で全3263件パス（1件skip、無関係）を確認
  （ログ: `.loop/test_logs/かぜおこし.log`）。
