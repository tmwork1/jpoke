# つじぎり レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/つじぎり.md`）。一次情報（`.internal/wiki/moves/つじぎり.html`）と照合し、
  あくタイプ・物理・威力70・命中100・PP15・急所ランク+1・追加効果なしであることを確認した。

## 実装計画書
- 新規作成（`.internal/plan/moves/つじぎり.md`）。既存実装（`data/move.py` の `"つじぎり"` エントリ）は
  仕様と完全に一致しており、変更不要と結論づけた。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `"つじぎり"` エントリ
  （`type="あく", category="physical", pp=15, power=70, accuracy=100, critical_rank=1,
  flags={"contact", "slash"}, handlers={}`）は一次情報・仕様書と一致しており、修正不要と判断した。

## リーサル計算
- 対象外（追加効果を持たない通常の物理攻撃技のため、`.internal/progress/move.md` の
  「リーサル実装」列は `n/a`）。

## テスト
- 変更なし。`tests/moves_attack/test_move_ta.py` の
  `test_つじぎり_急所ランクが1` / `test_つじぎり_急所ランクが1_乱数大で急所なし` は
  急所ランク+1の仕様（乱数0で急所発生、乱数0.5で急所不発）を正しく検証しており、修正不要と判断した。

## その他
- `.internal/progress/move.md` の つじぎり行を確認したところ、PP列が誤って `20`（正しくは `15`）と
  なっていたため修正した。あわせて仕様書・計画書の新規作成に伴い該当列を `x` に、
  レビュー列を `✓` に更新した。
- `.internal/tests/moves_attack.md` が一部の既存テスト（`つけあがる`, `アシストパワー`）の変更を
  反映しておらず古くなっていたため、`scripts/generate_test_list.py` を再実行して更新した
  （つじぎり自体の変更に起因するものではない）。
