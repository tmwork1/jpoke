# アクアリング レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/アクアリング.md`）。`.internal/spec/moves/ねをはる.md` を参考に、
  `.internal/wiki/moves/アクアリング.html` および `.internal/wiki/volatiles/アクアリング_(状態変化).html`
  の一次情報（タイプ・分類・PP・対象・効果・かいふくふうじ耐性・heal フラグ非該当など）を反映した。

## 実装計画書
- `.internal/plan/moves/アクアリング.md` は既存かつ内容は仕様・実装と整合していたため変更なし。
- `.internal/plan/volatiles/アクアリング.md` の「未実装事項（将来対応）」節が実態と食い違っていたため修正。
  おおきなねっこによる回復量1.3倍（`ON_CALC_DRAIN` 経由）とバトンタッチによる引き継ぎ
  （`_BATON_PASS_VOLATILES` に登録済み）はすでに実装済みだったため、「実装済み事項」として
  記載し直した。

## 実装（handlers/ / data/）
- `data/moves/move_a.py` の `MoveData`（type=みず, category=status, pp=20, target=self,
  `Event.ON_STATUS_HIT` で `アクアリング_apply`）、`data/volatile.py` の `VolatileData`
  （`Event.ON_TURN_END` priority=70 で `アクアリング_self_heal`、`LethalEvent.ON_TURN_END` で
  `アクアリング_heal`）はいずれも仕様と一致しており、動作面の修正は不要だった。
- `handlers/move_status.py` の `アクアリング_apply` に docstring が欠落していたため、
  `ねをはる_apply` 等の既存関数に倣って一行docstringを追加した（実装ロジック自体は変更なし）。

## リーサル計算
- 既存実装（`handlers/lethal.py` の `アクアリング_heal`、`data/volatile.py` の
  `LethalEvent.ON_TURN_END` 登録）を仕様書・`ねをはる_heal` 等の既存パターンと照合し、
  `_heal(hp_dist, ctx.defender, r=1/16)`（かいふくふうじブロック込み、切り捨て・最小1）が
  正しく実装されていることを確認した。修正なし。
  `.internal/progress/move.md` の アクアリング 行は「リーサル実装」「リーサルテスト」列が `n/a` だが、
  これは move 側ではなく volatile 側（`.internal/progress/volatile.md`）でリーサル実装が管理される
  設計（`ねをはる` 等の他の自己回復系変化技も同様に move 行は `n/a`）のため、想定通り。

## テスト
- 既存テスト（`tests/moves_status/test_move_a.py::test_アクアリング_揮発性状態が付与される`、
  `tests/volatiles/test_volatile_a.py` のかいふくふうじ・交代解除・ターン終了回復の3件、
  `tests/items/test_item_a.py::test_おおきなねっこ_アクアリングの回復量増加`、
  `tests/moves_status/test_move_ha.py` のバトンタッチ引き継ぎパラメータ化テスト、
  `tests/test_lethal.py::test_アクアリング_ターン終了時回復`）を実装と照合し、いずれも
  仕様と一致していたため変更なし。
  `python -m pytest tests/ -v` で4453件通過（1件skip）を確認した
  （`.loop/test_logs/アクアリング.log`）。
