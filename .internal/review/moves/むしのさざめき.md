# むしのさざめき レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/むしのさざめき.md`）
- 一次情報（ポケモンWiki）に基づき、タイプ・分類・威力90・命中100・PP10・優先度0・非接触・
  音（sound）技である点、10%の確率で相手のとくぼうを1段階下げる追加効果、
  各種判定フラグ（急所・命中判定・まもる・おうじゃのしるし・マジックコート・よこどり・
  オウムがえし・ちからずく・てんのめぐみ）、音技としてのみがわり貫通・ぼうおん無効化・
  パンクロック対象である点を記載した

## 実装計画書
- 新規作成（`.internal/plan/moves/むしのさざめき.md`）
- 既存実装の現状整理、`secondary_effect` フラグ欠落バグの発見と修正方針、
  priority根拠（既存の同種ハンドラ〈きあいだま・サイコキネシス等〉との整合性）、
  テスト方針を記載した

## 実装（handlers/ / data/）
- `handlers/move_attack.py` の `むしのさざめき_lower_defender_spd`（`stats={"spd": -1}, chance=0.1`）は
  仕様通りで変更なし
- `data/moves/move_ma.py` の `MoveData.flags` に `secondary_effect` を追加する不具合修正を行った
  （修正前: `flags={"sound"}` → 修正後: `flags={"sound", "secondary_effect"}`）。
  この欠落により、`ちからずく` 特性使用時に威力1.3倍化・追加効果無効化のどちらも
  発動しない不具合があったため修正した
- `python scripts/sort_data/sort_moves.py` を実行済み（並び順に変化なし）

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）

## テスト
- `tests/moves_attack/test_move_ma.py` に以下を追加した
  - `test_むしのさざめき_とくぼう1段階低下が発動しない`
  - `test_むしのさざめき_とくぼう1段階低下が発動する`
  - `test_むしのさざめき_相手にダメージを与える`
  - `test_むしのさざめき_みがわりを貫通して本体にダメージを与える`
- `python scripts/sort_tests.py tests/moves_attack/test_move_ma.py` でソート済み
- `python scripts/generate_test_list.py` で `.internal/tests/` を更新済み
- `python -m pytest tests/ -v` を実行し、3612 passed, 1 skipped（既存の無関係なskip）で全テスト成功
