# 10まんボルト レビュー結果

## 仕様書
- 変更なし。個別の仕様書ファイルは存在しないが、標準的な追加効果（確率発動のまひ）は
  `.internal/spec/moves/_move_secondary_1.md` の一覧表に既に記載されており、
  一次情報（`.internal/wiki/moves/10まんボルト.html`）と照合した結果、内容は正しい
  （でんき / 特殊 / 威力90（第六世代以降）/ 命中100 / PP15 / 対象1体 / 追加効果10%でまひ）。

## 実装計画書
- 変更なし。個別の計画書は存在しないが、同種の「確率でまひ付与」技
  （かみなり・かみなりパンチ・でんきショック等）にも個別計画書は作成されておらず、
  共通ヘルパー `apply_ailment_to_defender` を使う定型実装のため計画書省略は既存方針と整合している。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `10まんボルト` エントリ
  （type=でんき, category=special, pp=15, power=90, accuracy=100, flags={"secondary_effect"},
  `Event.ON_DAMAGE_HIT` に `_10まんボルト_apply_paralysis_to_defender` を登録）は仕様と一致。
  `src/jpoke/handlers/move_attack.py` の `_10まんボルト_apply_paralysis_to_defender` は
  `apply_ailment_to_defender(battle, ctx, value, ailment="まひ", chance=0.1)` を呼び出しており、
  10%の確率でまひを付与する仕様と一致する。`apply_ailment_to_defender` は
  `battle.resolve_secondary_chance` 経由でせいでんき等の確率補正にも対応済み。
  `python scripts/sort_handlers.py src/jpoke/handlers/move_attack.py` を実行したが差分なし（既に五十音順）。

## テスト
- 変更なし。`tests/moves_attack/test_move_sa.py` に
  `test_10まんボルト_まひが発動しない`（secondary_chance=0 でまひ不発）と
  `test_10まんボルト_まひが発動する`（secondary_chance=1 でまひ発動）が既に存在し、
  実装と仕様を十分にカバーしている。追加・修正なし。
- `python -m pytest tests/ -v` で全3200件パス（1件skip）を確認。

## その他
- `.internal/progress/move.md` の該当行で PP 列が誤って `16` になっていたため、
  一次情報・実装と一致する `15` に修正した。あわせてレビュー列を `✓` に更新した。
