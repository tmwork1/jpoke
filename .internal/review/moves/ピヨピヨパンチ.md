# ピヨピヨパンチ レビュー結果

## 一次情報の確認

`.internal/wiki/moves/ピヨピヨパンチ.html` を確認したところ、以下が判明した。

- タイプ: ノーマル / 分類: 物理 / 威力: 70（固定値。ランダムなダメージロールを持つ技ではない） / 命中率: 100 / PP: 10（Gen9基準） / 対象: 1体選択 / 直接攻撃: ○
- 追加効果: 20%の確率で相手を『こんらん』状態にする（第二世代以降）
- 特性てつのこぶしで威力1.2倍
- **記事のカテゴリタグに「ソード・シールドで使用不可能なわざ」「ブリリアントダイヤモンド・シャイニングパールで使用不可能なわざ」「スカーレット・バイオレットで使用不可能なわざ」が明記されている。**
  → 第9世代（スカーレット・バイオレット）で使用不可能な技であることが一次情報で確認された。
- `src/jpoke/data/ps-champ-ja/moves.json` にも `ピヨピヨパンチ` のエントリは存在しない（Champions側でも未収録）。

以上より、README.md の対象範囲規定
「第9世代で実装されていない技・アイテム・特性・ポケモンなどは実装しない」
に該当し、**本技は実装対象外**と判断した（同様の対象外技として `ダークファイア` の前例あり）。

## 仕様書
- 新規作成しない（対象外技のため個別仕様書は作成しない。`ダークファイア` の前例に合わせた）。

## 実装計画書
- 新規作成しない（対象外技のため）。
- ただし既存の `.internal/plan/moves/攻撃技_こんらん系.md`（こんらん付与技のグループ計画書）に本技が「未登録・要実装」として誤って含まれていたため、以下を修正した。
  - 対象技一覧・現状の実装状況表・ハンドラ構成表・実装コード例からピヨピヨパンチの行/コードブロックを削除
  - 対象外である旨と根拠（一次情報のカテゴリタグ、README.md対象範囲規定）を明記する注記を追加

## 実装（handlers/ / data/）
- **バグとして削除**: `src/jpoke/data/moves/move_ha.py` に `"ピヨピヨパンチ": MoveData(...)` が誤って登録されていたため削除した。
- **バグとして削除**: `src/jpoke/handlers/move_attack.py` の `ピヨピヨパンチ_apply_confusion_to_defender` 関数を削除した（共有ヘルパー `apply_confusion_to_defender` 自体は他の技（サイケこうせん・ねんりき・ばくれつパンチ等）で引き続き使用されるため維持）。
- `src/jpoke/types/move.py` の `MoveName` Literal 型は `python scripts/generate_literals/generate_move_literal.py` で再生成し、`ピヨピヨパンチ` を除去した。

## リーサル計算
- 対象外（元々 `.internal/progress/move.md` で `n/a` 指定であり、今回の対象外判定によりリーサル計算自体が不要）。

## テスト
- `tests/moves_attack/test_move_ha.py` の `test_ピヨピヨパンチ_こんらんが発動する` を削除した（対象外技のため）。
- `python -m pytest tests/ -v` を実行し、全件（3962 passed, 1 skipped）通過を確認した。ログは `.loop/test_logs/ピヨピヨパンチ.log` に保存。

## 進捗表
- `.internal/progress/move.md` のピヨピヨパンチ行を、`ダークファイア` と同じ「対象外」形式に更新した。
