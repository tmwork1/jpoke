# かいりき レビュー結果

## 仕様書
- 新規作成（.internal/spec/moves/かいりき.md）。ポケモンWiki一次情報（.internal/wiki/moves/かいりき.html）を基に、タイプ:ノーマル、分類:物理、威力80、命中100、PP15、接触あり、追加効果なしの通常攻撃であることを確認して記載。

## 実装計画書
- 新規作成（.internal/plan/moves/かいりき.md）。追加効果を持たないため専用ハンドラは不要と結論づけ、既存の `data/move.py` エントリがそのまま仕様に一致することを明記。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の既存 `"かいりき"` エントリ（type="ノーマル", category="physical", pp=15, power=80, accuracy=100, flags={"contact"}, handlers={}）は仕様書と完全に一致しており、修正不要と判断。

## リーサル計算
- 対象外（.internal/progress/move.md の「リーサル実装」列が n/a）。

## テスト
- 追加: `tests/moves_attack/test_move_ka.py` に `test_かいりき_相手にダメージを与える` を新規追加（既存テストが存在しなかったため）。
- `scripts/sort_tests.py` でソート、`scripts/generate_test_list.py` でテスト一覧を更新。
- `python -m pytest tests/ -v` を実行し、3258 passed, 1 skipped（既知の無関係スキップ）で全テスト成功を確認。失敗なし。
