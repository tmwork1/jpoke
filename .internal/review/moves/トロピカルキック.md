# トロピカルキック レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/トロピカルキック.md`）。
- ポケモンWiki一次情報より、威力はSVまで70だがChampionsでは85に変更されている旨、
  PP・命中・追加効果（100%の確率で相手のこうげきを1段階下げる）・判定フラグ・専用技（アマージョ）を記載。
- PPは `src/jpoke/data/ps-champ-ja/moves.json` の16を正として採用。

## 実装計画書
- 新規作成（`.internal/plan/moves/トロピカルキック.md`）。
- 既存の同種技（とびかかる・ひやみず）のパターンを踏襲し、`ON_DAMAGE_HIT` / priority100（デフォルト）で
  `modify_defender_stats` を用いる構成であることを明記。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `トロピカルキック` エントリを修正:
  - `power` を 70（SV基準の移行漏れ）から 85（Champions基準）に修正。
  - `pp` を 15（SV基準の移行漏れ）から 16（Champions基準、`src/jpoke/data/ps-champ-ja/moves.json` に一致）に修正。
  - `flags` に `secondary_effect` を追加（欠落していた。ちからずく・てんのめぐみとの相互作用に必須）。
- `src/jpoke/handlers/move_attack.py` の `トロピカルキック_lower_defender_atk` は
  `modify_defender_stats(battle, ctx, value, stats={"atk": -1})` で実装済みかつ仕様通り
  （100%発動、`chance` 省略でデフォルト100%扱い）。修正なし。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」「リーサルテスト」は n/a）。

## テスト
- `tests/moves_attack/test_move_ta.py` に以下を追加:
  - `test_トロピカルキック_secondary_effectフラグを持つ`
  - `test_トロピカルキック_こうげき1段階低下が発動する`
- `python -m pytest tests/ -v` で全件（3901件）パスを確認。
  なお実行中、本タスクと無関係な既存テスト
  （`test_てんねん_攻撃側は防御ランク補正を無視する` 等）が乱数依存で稀に失敗する
  既存のフレーキーな挙動を確認したが、変更前のコードでも同様に発生することを確認済みであり、
  本タスクの変更とは無関係。
