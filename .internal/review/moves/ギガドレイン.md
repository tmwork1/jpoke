# ギガドレイン レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ギガドレイン.md`）。一次情報（.internal/wiki/moves/ギガドレイン.html）を参照し、
  タイプ/分類/威力/命中率/対象/優先度/接触フラグ、ドレイン技共通仕様（回復量計算・おおきなねっこ・
  ヘドロえき・みがわり吸収）、判定フラグ（まもる・おうじゃのしるし・マジックコート・よこどり・
  オウムがえし）を記載した。
  PPは本家の10ではなく `src/jpoke/data/ps-champ-ja/moves.json` の値（12）を採用する旨を明記した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ギガドレイン.md`）。既存のドレイン技（きゅうけつ）の計画書を参考に、
  MoveData定義・ハンドラ実装・priority根拠（`.internal/spec/turn.md` の ON_HIT priority=20）・
  テスト方針・リーサル計算対象外の理由を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ka.py` の `ギガドレイン` の `pp` を `10` → `12` に修正した
  （`src/jpoke/data/ps-champ-ja/moves.json` ではPP12。本家のPP10がそのまま残っていた移行漏れ）。
- `handlers/move_attack.py` の `ギガドレイン_drain` は既存の `_drain_hp` 共通処理を正しく利用しており、
  他ドレイン技（きゅうけつ等）と実装パターンが一致していることを確認した（修正なし）。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列は `n/a`）。他のドレイン技と同様に
  専用のリーサル計算ハンドラは未実装の方針を踏襲。

## テスト
- 既存テスト（`test_ギガドレイン_使用後に攻撃者のHPが回復する`、
  `test_ギガドレイン_回復量が与ダメの半分になる`、`test_drain技グループ_回復量が与ダメのheal_ratio倍になる`
  内の `("ギガドレイン", 0.5)` ケース）を確認し、実装・仕様と整合していたため変更なし。
  PP変更（10→12）によって既存テストが壊れないことを `python -m pytest tests/ -v` で確認済み
  （3707 passed, 1 skipped）。
