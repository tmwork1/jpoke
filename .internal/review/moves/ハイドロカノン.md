# ハイドロカノン レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ハイドロカノン.md`）。.internal/wiki/moves/ハイドロカノン.html を参照し、
  タイプ・分類・威力・命中・PP（本家基準）・効果（リチャージ状態付与）・判定フラグ・備考を記載した。
  はかいこうせん・ギガインパクト等の既存の同系統仕様書と記述形式を統一した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ハイドロカノン.md`）。仕様書をもとに、既存実装（`リチャージ_apply`
  共用ハンドラ）とハンドラ構成・priority根拠・エッジケースを記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `ハイドロカノン` エントリの PP を `5`（Gen9本家基準の移行漏れ）
  から `8`（`src/jpoke/data/ps-champ-ja/moves.json` 基準）に修正した。
- `リチャージ_apply` ハンドラ自体（`src/jpoke/handlers/move_attack.py`）とリチャージ揮発性状態
  （`src/jpoke/handlers/volatile.py` の `リチャージ_block_action`）は、はかいこうせん等と共用の
  既存実装であり、仕様と一致していたため変更なし。

## リーサル計算
- 対象外（`.internal/progress/move.md` で「リーサル実装: n/a」「リーサルテスト: n/a」）。
  反動で動けなくなる効果はHP依存の状態ではなく、通常の固定威力技として扱われるため専用ハンドラは不要。

## テスト
- `tests/moves_attack/test_move_ha.py` に、はかいこうせんの既存テスト群と同等のエッジケースを追加した:
  - `test_ハイドロカノン_まもるで防がれた場合はリチャージ状態にならない`
  - `test_ハイドロカノン_交代するとリチャージ状態が解除される`
  - `test_ハイドロカノン_外れた場合はリチャージ状態にならない`
  - `test_ハイドロカノン_相手をひんしにしてもリチャージ状態が付与される`
  （既存の `命中後にリチャージ状態が付与される` `次ターン行動不能になる` は変更なし）
- `python -m pytest tests/ -v` で全3937件パス（1件skip）を確認。ログは
  `.loop/test_logs/ハイドロカノン.log` に保存。
