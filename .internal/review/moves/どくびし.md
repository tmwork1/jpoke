# どくびし レビュー結果

## 仕様書
- 新規作成: `.internal/spec/moves/どくびし.md`
  - 技本体（設置行為）の仕様を記載。どくびし状態そのものの詳細（毒付与・無効条件・消滅条件）は
    既存の `.internal/spec/side_fields/どくびし.md` に委譲する構成とした
  - 判定フラグ節で `target="foe_side"` により `まもる`（対象外）・`マジックコート`（反射可能）・
    `よこどり`/`オウムがえし`（未実装のため対象外）の挙動を実装コード（`Move.is_blocked_by_protect`
    / `Move.is_reflectable`）の判定条件と対応付けて明記した

## 実装計画書
- 修正: `.internal/plan/moves/どくびし.md`
  - 「未実装」となっていた `handlers/move_status.py` の記述を実装済み状態に更新
    （実際には `どくびし_set_field` が `ステルスロック_set_field` / `まきびし_set_field` と
    同一パターンで実装済みだった）
  - 実装ファイルパスを `data/move.py` → 実際の配置先 `data/moves/move_ta.py` に修正
  - Priority根拠の記述を、実際に使用されているイベント（`ON_STATUS_HIT`、`ON_APPLY_MOVE`ではない）
    に即した説明に補足

## 実装（handlers/ / data/）
- 変更なし
  - `src/jpoke/handlers/move_status.py` の `どくびし_set_field`、
    `src/jpoke/data/moves/move_ta.py` の `"どくびし"` エントリともに仕様書・計画書と整合しており、
    `ステルスロック_set_field` / `まきびし_set_field` と同一パターンで実装済みであることを確認した
  - `Move.is_reflectable` が `target in ("foe", "foe_side")` を含むため、`target="foe_side"` の
    本技もマジックコートで正しく反射される実装になっていることをコードレベルで確認した

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）

## テスト
- 追加: `tests/moves_status/test_move_ta.py`
  - `test_どくびし_マジックコートで跳ね返り使用者側に設置される`: マジックコート反射時に
    使用者側の場にどくびしが設置され、相手側には設置されないことを確認する既存未カバーの
    エッジケースを追加
- 既存確認: `test_どくびし_カウント累積`（0→1→2層、最大層で変化なし）、
  `test_どくびし_相手陣営に設置される` は仕様と一致しており変更なし
- 既存確認: `tests/test_field.py`（どくびし状態側の毒付与・消滅条件）、
  `tests/abilities/test_ability_ta.py`（どくげしょう連携）、
  `tests/items/test_item_a.py`（あつぞこブーツ無効化）、
  `tests/moves_status/test_move_a.py`（おかたづけでの解除）、
  `tests/moves_status/test_move_ka.py`（きりばらいでの解除）は
  すべて仕様と整合しており変更なし
- 全テスト: `python -m pytest tests/ -v` で 4289 passed, 1 skipped（`.loop/test_logs/どくびし.log`）
