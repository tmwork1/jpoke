# きあいだま レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/きあいだま.md`）。一次情報（`.internal/wiki/moves/きあいだま.html`）に基づき、
  タイプ・分類・威力・命中率・PP・対象・優先度・接触判定・追加効果（10%とくぼう-1）・判定フラグ
  （まもる／おうじゃのしるし／マジックコート／よこどり／オウムがえし／ちからずく）・Zワザ/ダイマックス威力を記載。

## 実装計画書
- 新規作成（`.internal/plan/moves/きあいだま.md`）。既存の `handlers/move_attack.py` の
  `きあいだま_lower_defender_spd`（`ON_DAMAGE_HIT`、`modify_defender_stats(stats={"spd": -1}, chance=0.1)`）と
  `data/move.py` の登録内容を仕様と照合し、priority 根拠（既存の同種ハンドラ ON_DAMAGE_HIT パターンを踏襲）を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/move_attack.py` の `きあいだま_lower_defender_spd` および
  `src/jpoke/data/move.py` の `"きあいだま"` エントリ（type=かくとう, category=special, pp=5, power=120,
  accuracy=70, flags={"bullet", "secondary_effect"}）は仕様書・計画書と一致しており、修正不要だった。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列は n/a）。確率発動の相手能力ランク低下効果であり、
  ダメージ量自体を変化させないため、リーサル計算（`core/lethal.py`）の対象としないことを確認した。

## テスト
- 既存の `test_きあいだま_とくぼう1段階低下が発動する`（`tests/moves_attack/test_move_ka.py`）に加えて、
  以下を追加した:
  - `test_きあいだま_とくぼう1段階低下が発動しない`（`secondary_chance=0.0` で追加効果が発動しないことを確認）
  - `test_きあいだま_相手にダメージを与える`（追加効果なしでも通常ダメージが発生することを確認）
- `python scripts/sort_tests.py tests/moves_attack/test_move_ka.py` でソート、
  `python scripts/generate_test_list.py` で一覧更新、`pytest tests/` で全件パス（3288 passed, 1 skipped）を確認した。

## 進捗表の修正
- `.internal/progress/move.md` のきあいだま行: 仕様書/計画書 `-` → `x`、レビュー `-` → `✓`、
  PP列の誤り `8` → `5`（実装値と一致するよう修正）。
