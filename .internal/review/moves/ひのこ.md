# ひのこ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ひのこ.md`）。
  - 一次情報（`.internal/wiki/moves/ひのこ.html`）に基づき、タイプ=ほのお、分類=特殊、威力=40、命中=100、PP=25、
    追加効果として10%の確率でやけど付与、を記載。
  - ほのおタイプの攻撃技であるため、被弾した相手のこおりを解凍する効果（`.internal/spec/ailments/こおり.md`）も明記。

## 実装計画書
- 新規作成（`.internal/plan/moves/ひのこ.md`）。
  - 既存実装（やけど付与）は仕様通りで修正不要である旨と、`flags` に `"thaw"` が欠落していた点を記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `"ひのこ"` エントリに `flags={"secondary_effect", "thaw"}` を追加
  （修正前は `flags={"secondary_effect"}` のみで、こおり解凍フラグが欠落していた）。
  これにより、`src/jpoke/data/ailment.py` の `"こおり"` エントリに登録済みの汎用ハンドラ
  `こおり_cure_by_thaw_move`（`ctx.move.has_flag("thaw")` を判定）が発動するようになる。
- `handlers/move_attack.py` の `ひのこ_apply_burn_to_defender`（やけど付与10%）は既存実装のまま変更なし。
- `.internal/spec/moves/_move_labels.md` の `thaw` セクションに `ひのこ` を五十音順で追加（ハイドロスチームとフレアドライブの間）。
- `python scripts/sort_data/sort_moves.py` を実行し、他ファイルへの影響がないことを確認済み。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」「リーサルテスト」列は `n/a`）。

## テスト
- `tests/moves_attack/test_move_ha.py` に `test_ひのこ_こおり状態の相手に当てると解凍する` を追加。
  こおり状態の相手にひのこを当てると解凍されることを確認。
- 既存の `test_ひのこ_やけどが発動する`（10%でやけど付与）は変更なし。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ha.py` と
  `python scripts/generate_test_list.py` を実行し、`python -m pytest tests/ -v` で全3577件パス（1件スキップ）を確認。
  結果は `.loop/test_logs/ひのこ.log` に保存。
