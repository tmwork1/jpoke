# かみくだく レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/かみくだく.md`）。一次情報（.internal/wiki/moves/かみくだく.html）をもとに、
  タイプ・分類・威力・命中・PP・追加効果（20%でぼうぎょ-1）・判定フラグ・かみつき技（がんじょうあご対象）
  である旨を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/かみくだく.md`）。既存実装（`data/move.py` / `handlers/move_attack.py`）が
  仕様と一致していることを確認し、修正不要である旨と根拠（priority・ちからずく/がんじょうあごとの
  相互作用）を記載した。

## 実装（handlers/ / data/）
- 変更なし。`data/move.py` の `かみくだく` エントリ（type=あく, category=physical, pp=15, power=80,
  accuracy=100, flags={"bite", "contact", "secondary_effect"}）、
  `handlers/move_attack.py` の `かみくだく_lower_defender_def`（`modify_defender_stats` で
  20%の確率でぼうぎょ-1）は仕様と一致しており、修正は不要だった。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装・リーサルテスト列は n/a）。

## テスト
- `tests/moves_attack/test_move_ka.py` に以下を追加:
  - `test_かみくだく_ぼうぎょ1段階低下が発動しない`（追加効果不発時にランクが変化しないことを確認）
  - `test_かみくだく_相手にダメージを与える`（ダメージが発生することを確認）
- 既存の `test_かみくだく_ぼうぎょ1段階低下が発動する` は変更なし。
- `scripts/sort_tests.py tests/moves_attack/test_move_ka.py` でソート、
  `scripts/generate_test_list.py` で一覧更新、`python -m pytest tests/ -v` で全件パス（3272 passed, 1 skipped）
  を確認した。
