# ドリルライナー レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ドリルライナー.md`）。
  - 一次情報（`.internal/wiki/moves/ドリルライナー.html`）と `src/jpoke/data/ps-champ-ja/moves.json` を照合し、
    タイプ・分類・威力・命中・PP・急所ランク+1・追加効果なしを記載。
  - PPはWiki記載の10（Gen9本家基準）ではなくChampions基準の12を採用する旨を明記。

## 実装計画書
- 新規作成（`.internal/plan/moves/ドリルライナー.md`）。
  - `data/moves/move_ta.py` の `pp=10` がChampions基準（PP=12）と不一致であることを指摘し、修正内容を明記。
  - 併せて `.internal/plan/moves/攻撃技_急所系.md` の一覧表内 `ドリルライナー` 行のPP値も `10` → `12` に修正（同ファイル内の既存誤記）。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `ドリルライナー` エントリの `pp` を `10` → `12` に修正
  （`src/jpoke/data/ps-champ-ja/moves.json` のPP=12が正。Gen9本家基準の値が移行漏れで残っていたバグ）。
- 急所ランク補正（`critical_rank=1`）・`flags={"contact"}`・追加効果なしは仕様通りで修正不要。
- 専用ハンドラは不要（`handlers/move_attack.py` に変更なし。既存の急所判定ロジックのみで実現）。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」列が `n/a`）。

## テスト
- 変更なし。既存の `test_ドリルライナー_急所ランクが1` / `test_ドリルライナー_急所ランクが1_乱数大で急所なし`
  （`tests/moves_attack/test_move_ta.py`）は仕様と一致しており、他の急所ランク+1技（つじぎり等）と
  同様のテスト構成のため追加は不要と判断。
- `python -m pytest tests/ -v` で3917 passed, 1 skipped（既存の無関係なskip）を確認。
  結果は `.loop/test_logs/ドリルライナー.log` に保存。
