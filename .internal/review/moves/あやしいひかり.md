# あやしいひかり レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/あやしいひかり.md`）。ポケモンWiki一次情報（タイプ=ゴースト、分類=変化、
  威力=-、命中率=100、範囲=1体選択、優先度=0、直接攻撃×、まもる○、マジックコート○、よこどり×、
  オウムがえし○）に基づき作成。こんらん自体の詳細（付与ターン数・自傷確率等）は既存の
  `.internal/spec/volatiles/こんらん.md` を参照する形にした。
- PP は `src/jpoke/data/ps-champ-ja/moves.json`（あやしいひかり 12）を正として記載。

## 実装計画書
- PP を 10 → 12 に修正（`src/jpoke/data/ps-champ-ja/moves.json` 準拠。Gen9本家値10の移行漏れだった）。
- 実装済みハンドラ（`volatile_manager.apply_confusion` を直接呼ぶ形）に合わせて記述を更新
  （旧計画書は `apply` を直接呼ぶ専用実装を提案していたが、既存の `いばる`・`おだてる` と同じ
  `apply_confusion` ヘルパーを使う実装のほうが適切なため、計画書側をそちらに合わせた）。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `"あやしいひかり"` エントリの PP を 10 → 12 に修正
  （Pokémon Champions のPP4段階統一ルールに反していた）。
- `handlers/move_status.py` の `あやしいひかり_apply` はロジック自体は問題なし
  （`battle.volatile_manager.apply_confusion` を使用しており、こんらんの
  2〜5ターンランダム抽選・マイペース/ミストフィールドでの無効化・重複付与拒否は
  いずれも共通処理で保証されている）。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が n/a。威力を持たない変化技のため）。

## テスト
- 既存の `test_あやしいひかり_こんらん付与`（`tests/moves_status/test_move_a.py`）で
  こんらん付与を確認済み。マイペース・ミストフィールドによる無効化は
  `tests/moves_attack/test_move_ka.py` の汎用こんらんテスト（`かかとおとし` 経由）で
  既に確認されているため重複追加はしなかった。
- `python -m pytest tests/ -v` で 4072 passed, 1 skipped を確認（ログ:
  `.loop/test_logs/あやしいひかり.log`）。
