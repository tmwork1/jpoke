# メタルバースト レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/メタルバースト.md`）。
- ポケモンWiki（`.internal/wiki/moves/メタルバースト.html`）と `.internal/spec/moves/固定ダメージ.md`
  （4.3節）・`.internal/spec/moves/ほうふく.md`（同一挙動の姉妹技）を参照して作成。
- 基本情報（はがね/物理/威力-/命中100/PP/対象不定/優先度0/非接触）、固定ダメージ計算、
  失敗条件（無効化・命中せず・みがわりで防いだ場合は反撃対象にならない）、判定フラグ、
  特性・道具との相互作用（プレッシャー・よちむ・きけんよち・おやこあい・ばけのかわ・
  きあいのハチマキ）、リーサル計算（n/a）を記載した。
- よちむとの相互作用について、仕様上はメタルバーストの技威力区分が120として扱われるべきだが、
  現状の `よちむ_reveal_strongest_move`（`src/jpoke/handlers/ability.py`）は
  `MoveData.power`（0）をそのまま参照するため未反映という既知のギャップを明記した。
  これは固定ダメージ技全般（カウンター・ミラーコート・ほうふく等）に共通する
  よちむ側の実装課題であり、本レビューでは修正対象外とした（よちむの個別レビューで対応すべき事項）。

## 実装計画書
- `.internal/plan/moves/メタルバースト.md` という単独ファイルは存在しないが、
  `.internal/plan/moves/攻撃技_反撃系.md`（カウンター・ミラーコート・ほうふく・メタルバーストの
  共通計画書）が既にメタルバーストの節を含んでいたため、この方式を踏襲した
  （カウンター・ミラーコート・ほうふくの既存レビュー時も同様の扱い）。
- 修正: メタルバースト節のPP表記を `PP 10` → `PP 12（champions基準。move_list.txt参照）` に修正。
  コード例中の `pp=10` も `pp=12` に修正（下記実装のPP修正と整合させるため）。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_attack.py` の `メタルバースト_check_can_use` /
  `メタルバースト_modify_damage` は仕様通りに実装済み（`ON_TRY_MOVE_1` priority=30 で
  `last_damage_received <= 0` なら失敗、`ON_MODIFY_MOVE_DAMAGE` で1.5倍・切り捨て）。
- `src/jpoke/core/move_executor.py` の `_execute_hit` で `actual_damage > 0` の場合のみ
  `last_damage_received` / `last_physical_damage_received` / `last_special_damage_received`
  を更新する実装になっており、無効化・みがわりで防いだダメージが反撃対象にならない仕様と一致することを確認した。
- **バグ修正**: `src/jpoke/data/moves/move_ma.py` の `"メタルバースト"` 定義が `pp=10`
  （Gen9本家基準）になっていたが、`src/jpoke/data/ps-champ-ja/moves.json`（PP=12）および
  `.internal/progress/move.md` の既存表記（PP=12）と食い違っていたため、`pp=12` に修正した
  （championsの全技PP8/12/16/20統一への移行漏れバグ）。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装・リーサルテスト列は `n/a`）。
  メタルバーストのダメージ量は攻撃側が受けた被弾ダメージに依存するため、防御側HP分布を
  左右する事前計算要素がなく、既存の判断は妥当と確認した。

## テスト
- 既存テスト2件（ダメージ未受領時の失敗、受けたダメージの1.5倍返し）を実装と照合し、問題なし。
- 追加: `test_メタルバースト_Champions基準のPPを持つ`（PP=12であることを確認する回帰テスト）。
- 追加: `test_メタルバースト_みがわりに当たったダメージは参照されない`
  （カウンターの同様テストに倣い、みがわりで防いだダメージが反撃対象として記録されないことを確認）。
- `python -m pytest tests/ -v` で全4024件（1 skipped）がパスすることを確認
  （ログ: `.loop/test_logs/メタルバースト.log`）。
