# こんげんのはどう レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/こんげんのはどう.md`）。ポケモンWiki一次情報（みずタイプ・特殊・威力110・命中85・PP10・対象相手全員・優先度0・非接触・波動技）をもとに記載。

## 実装計画書
- 新規作成（`.internal/plan/moves/こんげんのはどう.md`）。追加効果を持たないため専用ハンドラは不要で、`flags={"pulse"}` によるメガランチャー汎用ハンドラ（`handlers/ability.py:メガランチャー_modify_power`）で威力補正が処理される旨を明記。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `"こんげんのはどう"` エントリ（type="みず", category="special", pp=10, power=110, accuracy=85, flags={"pulse"}, handlers={}）は仕様・計画と一致しており修正不要と確認した。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装・リーサルテストは n/a）。追加効果・確率分岐のない通常攻撃のため専用リーサルハンドラは不要。

## テスト
- `tests/moves_attack/test_move_ka.py` に `test_こんげんのはどう_相手にダメージを与える` を追加（既存の類似技テスト `このは` 等と同様のパターンで、追加効果なしの通常攻撃として相手にダメージを与えることを確認）。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ka.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新。
- `python -m pytest tests/ -v` を実行し、3351 passed, 1 skipped で全件パスを確認（`.loop/test_logs/こんげんのはどう.log`）。
