# アンコール レビュー結果

## 仕様書

- 新規作成: `.internal/spec/moves/アンコール.md`
  - 基本情報（タイプ・分類・PP・命中率・範囲・優先度）、失敗条件、判定（急所・命中・追加効果・まもる・マジックコート・みがわり）、
    イベントフローを記載。揮発性状態自体の詳細は既存の `.internal/spec/volatiles/アンコール.md` を参照する形にした
- 副次的に `.internal/spec/volatiles/アンコール.md` の priority 根拠の記述を修正
  - 「`Event.ON_STATUS_HIT`」となっていた参照を「`Event.ON_APPLY_MOVE`（コード上は `Event.ON_BEFORE_APPLY_MOVE` に対応）」に修正
    （`.internal/spec/turn.md` を確認したところ、「アンコール: 失敗条件」の記載は `## Event.ON_APPLY_MOVE` 見出し配下にあり、
    `ON_STATUS_HIT` という見出しは存在しなかったため）

## 実装計画書

- 新規作成: `.internal/plan/moves/アンコール.md`
  - ハンドラ構成（`ON_BEFORE_APPLY_MOVE` / `ON_STATUS_HIT`）、priority根拠、実装箇所、エッジケースを記載
- 副次的に `.internal/plan/volatiles/アンコール.md` の priority 根拠の記述を同様に修正（`ON_STATUS_HIT` → `ON_APPLY_MOVE`）

## 実装（handlers/ / data/）

- `src/jpoke/data/moves/move_a.py`: `"アンコール"` の `pp` を `5`（Gen9本家基準の移行漏れ）から `8`
  （`src/jpoke/data/ps-champ-ja/moves.json` 基準）に修正
- `handlers/move_status.py` の `アンコール_can_apply` / `アンコール_apply` は仕様・計画と一致しており修正なし

## リーサル計算

- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`。ダメージを与えない変化技のため）

## テスト

- `tests/moves_status/test_move_a.py` に 2 件追加:
  - `test_アンコール_成功してアンコール揮発状態が付与される`: 相手が技を使った後にアンコールを使うと
    アンコール揮発状態（count=3）が付与されることを確認
  - `test_アンコール_すでにアンコール状態の相手には失敗する`: 相手がすでにアンコール状態の場合、
    2回目のアンコールが失敗し count が変化しないことを確認
- `tests/volatiles/test_volatile_a.py` の既存8件（コマンド制限・技固定・ターン経過・かなしばりとの重複・
  PP0時のわるあがき強制・未行動/non_encoreラベルでの失敗）は実装と一致しており修正なし
- `python -m pytest tests/ -v` で 4458 passed, 1 skipped を確認（結果は `.loop/test_logs/アンコール.log` に保存）
