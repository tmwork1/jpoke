# こがらしあらし レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/こがらしあらし.md`）
  - 基本情報（ひこう/特殊/威力100/命中80/PP10/対象:相手全員/優先度0/接触なし/風の技/トルネロス専用技）
  - 効果: 30%で相手のすばやさランクを1段階下げる／天気が『あめ』のとき必中（にほんばれ時の命中低下はなし）
  - そらをとぶ状態の相手には必中補正でも命中しない旨（`ぼうふう`・`かみなり`との違い）を明記

## 実装計画書
- 新規作成（`.internal/plan/moves/こがらしあらし.md`）
  - `かみなりあらし`の実装計画書をテンプレートに、`ON_MODIFY_ACCURACY`ハンドラの新規追加を計画
  - そらをとぶ回避は`handlers/volatile.py`の`HIDDEN_MOVE_ALLOWED_MOVES`に未登録のため変更不要と明記

## 実装（handlers/ / data/）
- `handlers/move_attack.py` に `こがらしあらし_accuracy` を新規追加
  （あめ時に必中、防御側の `weather_for` を経由してばんのうがさ等を考慮）
- `data/move.py` の `こがらしあらし` に `Event.ON_MODIFY_ACCURACY` ハンドラを追加登録
  （既存の `Event.ON_DAMAGE_HIT`（すばやさ低下30%）はそのまま）
  - `python scripts/sort_handlers.py src/jpoke/handlers/move_attack.py` 実行済み
  - `python scripts/sort_data/sort_moves.py` 実行済み

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）

## テスト
- `tests/moves_attack/test_move_ka.py` に以下を追加
  - `test_こがらしあらし_あめ中は必中になる`: あめ天候中、命中率80では本来外れる乱数（0.9）でも命中することを確認
  - `test_こがらしあらし_すばやさが下がる`: 追加効果確率1.0固定で相手のすばやさランクが-1になることを確認
- `python scripts/sort_tests.py tests/moves_attack/test_move_ka.py` 実行済み
- `python scripts/generate_test_list.py` 実行済み
- `python -m pytest tests/ -v` 全件パス（3349 passed, 1 skipped）
