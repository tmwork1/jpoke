# ハイパーボイス レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ハイパーボイス.md`）。
- 一次情報は `.internal/wiki/moves/ハイパーボイス.html` を想定していたが、本セッションでは
  wiki.pokemonwiki.com がCloudflareのボット対策で403を返しダウンロードできなかったため、
  `src/jpoke/data/ps-champ-ja/moves.json`（ハイパーボイス行: ノーマル/特殊/威力90/命中100/PP12/相手全体/
  音系の技・みがわり貫通）を一次情報として採用し、既存実装済みの同種技
  （`ばくおんぱ`・`こんげんのはどう`・`じしん`）の仕様書を参考に作成した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ハイパーボイス.md`）。
- 追加効果なし・`sound`フラグと既存の汎用ハンドラ（`reduce_damage_in_double_battle`、
  みがわり貫通の`sound`判定）のみで挙動が完結することを明記。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ha.py` の `"ハイパーボイス"` エントリ
  （type=ノーマル, category=special, pp=12, power=90, accuracy=100, flags={"sound"},
  handlers={ON_CALC_DAMAGE_MODIFIER: reduce_damage_in_double_battle}）は
  `src/jpoke/data/ps-champ-ja/moves.json` および仕様書と一致しており、修正不要だった。
  PP=12もChampions基準の値と一致（本家Gen9の10ではない）。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装/リーサルテスト列は n/a。追加効果を持たない
  通常ダメージ技のため専用ハンドラは不要）。

## テスト
- `tests/moves_attack/test_move_ha.py` に `test_ハイパーボイス_相手にダメージを与える` を追加。
  既存の `test_ハイパーボイス_みがわりを貫通して本体にダメージを与える` は内容が正しく変更なし。
  （同カテゴリの `ばくおんぱ` に倣い、通常ダメージ確認テストとみがわり貫通テストの両方を揃えた）
- ダブルバトル時の0.75倍補正は `tests/test_battle_option.py` の既存テストで既にカバー済みのため、
  重複追加はしなかった。
- `python -m pytest tests/ -v` を実行し、5812 passed, 1 skipped で全件成功を確認
  （skip 1件は本タスクと無関係の既存スキップ）。
