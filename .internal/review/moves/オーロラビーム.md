# オーロラビーム レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/オーロラビーム.md`）。一次情報（ポケモンWiki）と既存の集約仕様書
  `.internal/spec/moves/_move_secondary_1.md`（防御側A-1・10%）の内容を突き合わせ、個別仕様書として書き出した。
  内容自体に不整合はなかった。

## 実装計画書
- 新規作成（`.internal/plan/moves/オーロラビーム.md`）。既存実装が仕様と完全に一致していたため、
  計画書には現状の実装内容（変更不要）を記録した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_a.py` の `MoveData`（type=こおり, category=special, pp=20,
  power=65, accuracy=100, flags={"secondary_effect"}）と `src/jpoke/handlers/move_attack.py` の
  `オーロラビーム_lower_defender_atk`（`Event.ON_DAMAGE_HIT` で `modify_defender_stats(stats={"atk": -1}, chance=0.1)`）
  はいずれも仕様通りで、同系統の技（じゃれつく・うらみつらみ・はるのあらし・ひやみず・とびかかる 等）と
  実装パターンも一致していたため修正不要と判断した。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が n/a）。

## テスト
- `tests/moves_attack/test_move_a.py` にテストが存在しなかったため、以下3件を追加した。
  - `test_オーロラビーム_こうげき1段階低下が発動しない`
  - `test_オーロラビーム_こうげき1段階低下が発動する`
  - `test_オーロラビーム_相手にダメージを与える`
- `python -m pytest tests/ -v` で全件パス（3695 passed, 1 skipped）を確認した。
  ログ: `.loop/test_logs/オーロラビーム.log`
