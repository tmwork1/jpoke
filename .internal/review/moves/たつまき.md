# たつまき レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/たつまき.md`）。ポケモンWiki一次情報を参照し、
  ドラゴン/特殊/威力40/命中100/PP20/追加効果20%ひるみ/風技/そらをとぶ滞空中命中・威力2倍
  の仕様をまとめた。

## 実装計画書
- 新規作成（`.internal/plan/moves/たつまき.md`）。既存実装が仕様と一致していることを確認し、
  差分なしである根拠を記載した。

## 実装（handlers/ / data/）
- 変更なし。`data/move.py` の `"たつまき"` エントリ（type=ドラゴン, category=special,
  pp=20, power=40, accuracy=100, flags={"wind","secondary_effect"}）、
  `handlers/move_attack.py` の `たつまき_apply_flinch`（20%ひるみ）、
  `handlers/volatile.py` の `そらをとぶ_boost_power` / `HIDDEN_MOVE_ALLOWED_MOVES` への
  登録、`handlers/item.py` の `_INNATE_FLINCH_MOVES` への登録は、いずれも仕様書・計画書と
  一致しており修正不要と判断した。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」列が `n/a`）。
  ひるみ付与のみでHP分布に影響を与える効果ではないため、リーサル計算ハンドラは不要。

## テスト
- `tests/moves_status/test_move_ta.py` に誤って重複登録されていた
  `test_たつまき_ひるみが発動する`（たつまきは変化技ではなく攻撃技のため誤配置）を削除。
  `tests/moves_attack/test_move_ta.py` の同名テストのみを正とした。
- `tests/volatiles/test_volatile_satana.py` の
  `test_そらをとぶ_かぜおこしたつまきの威力が2倍になる`（parametrize でたつまきも網羅）で
  そらをとぶ滞空中への命中・威力2倍を確認済みであり、追加は不要と判断した。
- `python scripts/sort_tests.py tests/moves_status/test_move_ta.py tests/moves_attack/test_move_ta.py`
  でソート、`python scripts/generate_test_list.py` でテスト一覧を更新、
  `python -m pytest tests/ -v` で全3422件通過（結果は `.loop/test_logs/たつまき.log` に保存）。

## 進捗表
- `.internal/progress/move.md` の たつまき 行を更新（仕様書 `-`→`x`、計画書 `-`→`x`、
  レビュー `-`→`✓`、効果列にそらをとぶ・風技の記述を追加）。
