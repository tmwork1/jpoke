# もろはのずつき レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/もろはのずつき.md`）。
- 一次情報（`.internal/wiki/moves/もろはのずつき.html`）に基づき、基本情報・反動ダメージの
  計算式・判定フラグ・特性との関係（いしあたま、マジックガード、すてみ、がんじょう）を記載。
- がんじょう特性は本技の反動ダメージによる瀕死を防がない（メインの攻撃ダメージによる
  瀕死のみ防ぐ）ことを明記した（`.internal/spec/abilities/がんじょう.md` に既に同様の記載あり）。

## 実装計画書
- 新規作成（`.internal/plan/moves/もろはのずつき.md`）。
- 既存のグループ計画書 `.internal/plan/moves/反動技_05.md`（反動量1/2の技をまとめた計画書。
  はめつのひかりと共通）で既に登録・実装済みだったため、本ファイルでは一次情報・仕様書との
  照合結果を個別に記録する形とした。
- priorityは `.internal/spec/turn.md` の `Event.ON_HIT` 表「100 | 反動技による反動ダメージ」に基づき、
  既存実装（`MoveHandler` デフォルトのpriority=100）のまま変更不要と確認した。

## 実装（handlers/ / data/）
- 変更なし。
- `handlers/move_attack.py` の `もろはのずつき_recoil` は共通ヘルパー `_recoil(ratio=1/2)`
  を使用しており、`recoil = max(1, round_half_down(damage * 1/2))` で反動量を計算する
  実装が仕様と一致していることを確認した。
- `data/moves/move_ma.py` の `MoveData`
  （type="いわ", category="physical", pp=5, power=150, accuracy=80,
  flags={"contact", "recoil"}）が一次情報と一致していることを確認した。
- 特性との連携（いしあたま特性の `いしあたま_ignore_recoil`、マジックガード特性の
  `マジックガード_ignore_damage`、すてみ特性の `すてみ_boost_power`）が
  `reason="recoil"` / `flags={"recoil"}` を通じて正しく機能することを確認した。
- がんじょう特性の `がんじょう_survive_lethal` は `ON_BEFORE_DAMAGE_APPLY`
  （メイン攻撃ダメージの適用時）のみに作用し、反動ダメージの `modify_hp` 呼び出しには
  影響しないことを確認し、仕様と実装の一致を確認した。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列がn/a）。

## テスト
- 変更なし。
- `tests/moves_attack/test_move_ha.py`
  - `test_もろはのずつき_使用後に攻撃者が反動ダメージを受ける`（基本動作の確認）
  - `test_もろはのずつき_反動ダメージが与ダメの2分の1になる`（反動量が
    max(1, 与ダメ*1/2) になることを確認）
  - いずれも実装と一致しており妥当と判断し、変更しなかった。
- `tests/moves_attack/test_move__group.py` の
  `test_recoil技グループ_反動量が与ダメのrecoil_ratio倍になる` に
  `("もろはのずつき", 1/2)` が含まれており、既存のグループテストでもカバーされている
  ことを確認した。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ha.py` → 変更なし（既に整列済み）。
- `python scripts/generate_test_list.py` → `python -m pytest tests/ -v` を実行し、
  全3620件（+1 skipped）が成功したことを確認
  （ログ: `.loop/test_logs/もろはのずつき.log`）。
