# ラスターパージ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ラスターパージ.md`）。
- 基本情報（タイプ: エスパー / 分類: 特殊 / 威力95 / 命中100 / 対象1体 / 優先度0 / 接触なし）、
  追加効果（50%の確率で相手の『とくぼう』を1段階下げる）、判定フラグを一次情報
  （`.internal/wiki/moves/ラスターパージ.html`）から記載。
- PPについては `src/jpoke/data/ps-champ-ja/moves.json` に本技の記載が存在しなかったため、
  `.internal/champions/changes_from_sv.md` の「本家PP5の技は一律champions PP8に変換」という
  規則を、move_list.txtに実在する同条件の技（りゅうせいぐん・じばく・だいばくはつ等、
  いずれも本家PP5→championsPP8）から確認し、PP8として明記した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ラスターパージ.md`）。
- 既存実装の2点の乖離（PP・secondary_effectフラグ欠落）を明記し、修正方針・ハンドラ構成・
  priority根拠・エッジケース（ちからずくとの相互作用）を記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ra.py` の `ラスターパージ` エントリを修正:
  - `pp=5` → `pp=8`（championsのPP統一規則に合わせて修正。本家Gen9基準のPP5は移行漏れ）
  - `flags={"secondary_effect"}` を追加（50%発動の追加効果を持つため、ちからずく
    （威力1.3倍化・追加効果確率0化）との連動に必須。同種技である10まんボルト・
    だいちのちから等はいずれもこのフラグを持っており、本技だけ欠落していた）
- `src/jpoke/handlers/move_attack.py` の `ラスターパージ_lower_defender_spd` は
  `modify_defender_stats(..., stats={"spd": -1}, chance=0.5)` で仕様と一致しており、修正なし。

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行はリーサル実装・リーサルテストともに `n/a`）。

## テスト
- `tests/moves_attack/test_move_yarawa.py` に3件追加（既存の `だいちのちから` パターンに準拠）:
  - `test_ラスターパージ_とくぼう1段階低下が発動しない`（secondary_chance=0.0）
  - `test_ラスターパージ_とくぼう1段階低下が発動する`（secondary_chance=1.0）
  - `test_ラスターパージ_相手にダメージを与える`
- `python -m pytest tests/ -v` で全4028件パス（1件skip、既存の無関係なskip）を確認。
  ログは `.loop/test_logs/ラスターパージ.log` に保存。

## 備考（対象外だが確認した内容）
- `ラスターカノン`（隣接エントリ）は本worktree上の実装で `pp=10` になっているが、
  `src/jpoke/data/ps-champ-ja/moves.json` の値は `pp=12`、`.internal/progress/move.md` の当該行も `12`
  であり不整合が見られた。今回のレビュー対象外のため修正はせず、次回 `ラスターカノン` の
  レビュー時の参考として記録のみ残す。
