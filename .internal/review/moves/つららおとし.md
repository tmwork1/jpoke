# つららおとし レビュー結果

## 仕様書
- 変更なし。個別の仕様書ファイルは存在しないが、標準的な追加効果（確率発動のひるみ付与）は
  `.internal/spec/moves/_move_secondary_1.md` の一覧表（175行目）に既に記載されており、
  一次情報（`.internal/wiki/moves/つららおとし.html`）と照合した結果、内容は正しい
  （こおり / 物理 / 威力85 / 命中90 / PP10 / 対象1体選択（defender固定）/
  追加効果30%でひるみ / 直接攻撃×）。

## 実装計画書
- 変更なし。個別の計画書は存在しないが、`.internal/plan/moves/攻撃技_ひるみ系.md` に
  つららおとしを含む同種の「確率でひるみを付与する」技がまとめて記載されている
  （共通ヘルパー `apply_volatile_to_defender` を使う定型実装）。個別計画書省略は
  いわなだれ等の既存レビューで確認済みの方針と整合している。
  なお同ファイル内のコード例（`labels=[...]`, `category="物理"`, `Event.ON_HIT`,
  関数名 `つららおとし_apply_volatile_to_defender` 等）は初期草案段階の表記であり、
  実際の実装（`flags={"secondary_effect"}`, `category="physical"`,
  `Event.ON_DAMAGE_HIT`, 関数名 `つららおとし_apply_flinch`）とは異なるが、
  これは計画時点の草案と実装後の最終形の差分であり、他の技（いわなだれ等）でも
  同様の差分が残っていることを確認したため、本レビューでは修正対象としない。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `つららおとし` エントリ
  （type=こおり, category=physical, pp=10, power=85, accuracy=90,
  flags={"secondary_effect"}, `Event.ON_DAMAGE_HIT` に `つららおとし_apply_flinch` を登録）は
  仕様と一致。`src/jpoke/handlers/move_attack.py` の `つららおとし_apply_flinch` は
  `apply_volatile_to_defender(battle, ctx, value, volatile="ひるみ", chance=0.3)` を呼び出しており、
  30%の確率で相手を『ひるみ』状態にする仕様と一致する。五十音順の位置も既に正しいため
  `sort_handlers.py` / `sort_moves.py` の実行差分はなし（実行不要と判断）。
- `src/jpoke/handlers/item.py` の `_INNATE_FLINCH_MOVES` に `つららおとし` が含まれており、
  おうじゃのしるし・するどいキバの追加ひるみ効果が重複しない仕様も正しく実装済み
  （`tests/test_item.py` の該当パラメータ化テストで一般的に検証済み）。

## リーサル計算
- 対象外。`.internal/progress/move.md` のリーサル実装・リーサルテスト列がともに `n/a`。

## テスト
- 変更なし。`tests/moves_attack/test_move_ta.py` の `test_つららおとし_ひるみが発動する`
  （secondary_chance=1.0 でひるみ付与を確認）は既存実装と一致しており修正不要。
- `python -m pytest tests/ -v` で全3436件パス（1件skip）を確認
  （ログ: `.loop/test_logs/つららおとし.log`）。

## その他
- `.internal/progress/move.md` の該当行で PP列が `12`（誤り）になっていたため一次情報に基づき `10` に修正し、
  効果列の `？` を「追加効果として、30%の確率で相手をひるませる。」に補完、レビュー列を `✓` に更新した。
