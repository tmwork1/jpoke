# アクセルロック レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/アクセルロック.md`）。一次情報（.internal/wiki/moves/アクセルロック.html）に基づき、
  タイプ・分類・威力・命中・PP・優先度・接触・判定フラグを記載。既存の同種先制技「でんこうせっか」の
  仕様書を参考に構成を統一した。

## 実装計画書
- 新規作成（`.internal/plan/moves/アクセルロック.md`）。実装済みの `MoveData` 定義（`src/jpoke/data/moves/move_a.py`）が
  仕様書と一致していることを確認し、追加ハンドラ不要である根拠・priority検討・エッジケースを記載。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_a.py` の `MoveData`（type="いわ", category="physical", pp=20, power=40,
  accuracy=100, priority=1, flags={"contact"}, handlers={}）は一次情報・仕様書と完全に一致しており、
  修正不要と判断。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- 変更なし。`tests/moves_attack/test_move_a.py::test_アクセルロック_相手にダメージを与える` が
  優先度+1の先制物理技としてダメージを与えることを確認しており、仕様と整合。
  `tests/test_field.py::test_らんきりゅう_ひこう弱点半減` でも いわ技代表として使用されており問題なし。
  `python -m pytest tests/ -v` で全3658件（+1 skipped）がパスすることを確認。
