# きょじゅうざん レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/きょじゅうざん.md`）。
  一次情報（`.internal/wiki/moves/きょじゅうざん.html`）を参照し、タイプ・分類・威力・命中・PP・フラグ・判定を記載。
  本家の「相手ダイマックス時ダメージ2倍」効果はダイマックス非対応環境のため対象外と明記。

## 実装計画書
- 新規作成（`.internal/plan/moves/きょじゅうざん.md`）。
  既存実装（`data/move.py`）と仕様書の整合を確認し、専用ハンドラ不要である旨を記載。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の登録内容（`type="はがね"`, `category="physical"`, `power=100`,
  `accuracy=100`, `pp=5`, `flags={"contact", "slash"}`, `handlers={}`）は一次情報と一致しており修正不要。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」「リーサルテスト」列は `n/a`）。
  追加効果を持たない通常攻撃技のため専用のリーサルハンドラは不要。

## テスト
- 新規追加。`tests/moves_attack/test_move_ka.py` に
  `test_きょじゅうざん_相手にダメージを与える`（追加効果なしの物理はがね技で相手にダメージを与える）を追加。
  `scripts/sort_tests.py` でソート、`scripts/generate_test_list.py` でテスト一覧を更新し、
  `python -m pytest tests/ -v` で全件パス（3309 passed, 1 skipped）を確認。
