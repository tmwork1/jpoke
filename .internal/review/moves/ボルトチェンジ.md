# ボルトチェンジ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ボルトチェンジ.md`）。
- 一次情報（`.internal/wiki/moves/ボルトチェンジ.html`）を確認し、既存の `とんぼがえり` 仕様書
  （でんきタイプ版という位置づけ）をベースに、独自差分（でんきタイプゆえのじめん無効・
  ひらいしん/ちくでん/でんきエンジンによる吸収、非接触技である点）を追記した。
  判定フラグ・交代の発動条件は `とんぼがえり` と共通のため参照する形にした。

## 実装計画書
- 新規作成（`.internal/plan/moves/ボルトチェンジ.md`）。
- `.internal/spec/turn.md` の `Event.ON_HIT` を確認し、既存の `とんぼがえり` と同じデフォルト
  priority で `pivot` ハンドラを共有していることを明記。でんきタイプ固有の無効化パターンは
  `Event.ON_BEFORE_APPLY_MOVE` 側の既存の汎用実装（型相性判定・ひらいしん等の特性ハンドラ）
  でカバーされ、本技固有の追加実装が不要であることを根拠付きで記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ha.py` の `ボルトチェンジ` エントリ
  （type=でんき, category=special, pp=20, power=70, accuracy=100,
  `Event.ON_HIT: h.MoveHandler(ha.pivot)`）は仕様書・`src/jpoke/data/ps-champ-ja/moves.json`
  （PP=20）と一致しており、修正不要と判断した。
- `pivot`（`handlers/move_attack.py`）は `とんぼがえり` と共通実装であり、
  でんきタイプの無効化は `Event.ON_BEFORE_APPLY_MOVE` 側の型判定・特性ハンドラ
  （ひらいしん/ちくでん/でんきエンジン）で技自体が不発になるため、
  `Event.ON_HIT` が発火せず交代も自動的に発動しないことを確認した。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列は `n/a`）。

## テスト
- `tests/moves_attack/test_move_ha.py` にボルトチェンジ用のテストを新規追加（8件）。
  - ダメージを与える
  - じめんタイプには無効（ダメージ・交代とも発動しない）
  - ひらいしんに吸収されると交代しない
  - まもるで防がれた場合は交代しない
  - 命中しなかった場合は交代しない
  - 控えがいない場合は交代しない
  - 攻撃後に交代可能状態になる（PIVOT interrupt→実際の交代）
  - 相手を倒した場合でも交代する
- `Interrupt` のインポートを追加。
- `python -m pytest tests/ -v` を実行し、4007 passed, 1 skipped（既存のskipで本レビューと無関係）
  で全件パスを確認した。ログ: `.loop/test_logs/ボルトチェンジ.log`
  （初回実行時に本レビューと無関係の `tests/items/test_item_ta.py` の2件が偶発的に失敗したが、
  これは対象側ポケモンの技を固定していない既存テストの非決定性によるフレーキーであり、
  再実行で解消することを確認済み。ボルトチェンジの実装・テストには影響なし）。
