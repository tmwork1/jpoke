# かみつく レビュー結果

## 仕様書
- 変更なし
- 個別の仕様書ファイルは存在しないが、`.internal/spec/moves/_move_secondary_1.md` にかみつく（defender / 確率0.3 / ひるみ）の行が既に登録されており、一次情報（あく物理・威力60・命中100・PP25・追加効果30%ひるみ）と一致していることを確認した。

## 実装計画書
- 変更なし
- 個別の計画書ファイルは存在しないが、他の「30%ひるみ」系の技（ふみつけ・ずつき等）と同様に個別計画書を持たない運用のため、新規作成は不要と判断した。
- `src/jpoke/data/move.py` の `かみつく` エントリ（type=あく, category=physical, pp=25, power=60, accuracy=100, flags={"bite", "contact", "secondary_effect"}）と `handlers/move_attack.py` の `かみつく_apply_flinch`（`apply_volatile_to_defender` で30%の確率で「ひるみ」を付与）は一次情報・仕様と整合しており、修正不要と判断した。
- `bite` フラグにより `がんじょうあご`（威力1.5倍）との連携も既存の汎用ハンドラで正しく処理されることを確認した。

## 実装（handlers/ / data/）
- 変更なし（`かみつく_apply_flinch` の実装は仕様通り）

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列は n/a。ひるみ付与のみでダメージ分布に影響しないため妥当）

## テスト
- 変更なし
- `tests/moves_attack/test_move_ka.py::test_かみつく_ひるみが発動する` が既にひるみ付与を確認済み。
- `tests/abilities/test_ability_ka.py::test_がんじょうあご_かみつき技で威力補正1_5倍` および `test_ability__group.py` でがんじょうあごとの相互作用も確認済み。
- `python -m pytest tests/ -v` で3272件全件パス（1件skip）。

## その他
- `.internal/progress/move.md` の `かみつく` 行で PP が誤って `20` と記載されていたため、実装・一次情報と一致する `25` に修正し、レビュー列を `✓` に更新した。
- 同ファイルの `かみくだく` 行で PP が誤って `16` と記載されていたため、実装・一次情報と一致する `15` に修正した（かみくだく自体は今回のレビュー対象外だが、作業中に気づいたため修正）。
