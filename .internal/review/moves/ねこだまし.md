# ねこだまし レビュー結果

## 仕様書
- 存在しなかったため新規作成（`.internal/spec/moves/ねこだまし.md`）。
- 一次情報（`.internal/wiki/moves/ねこだまし.html`）をもとに、基本情報・追加効果・
  「場に出てから最初の行動でなければ失敗する」という成功条件・判定フラグ・
  Champions固有の挙動（コマンド選択キャンセル、本プロジェクトでは対象外とする旨）を記載した。

## 実装計画書
- 存在しなかったため新規作成（`.internal/plan/moves/ねこだまし.md`）。
- 既存の `であいがしら`（成功条件が完全に同一の技）の実装パターンを踏襲する方針を明記した。

## 実装（handlers/ / data/）
- **重大な欠落を修正**: `ねこだまし` は「場に出てから最初の行動でなければ失敗する」が
  本技の根幹となる成功条件であるにもかかわらず、`ON_DAMAGE_HIT` のひるみ付与ハンドラのみが
  登録されており、この成功条件が一切実装されていなかった。
- `handlers/move_attack.py` に `ねこだまし_check_first_turn` を新規追加し、既存の
  `であいがしら_check_first_turn` と同型のロジック（`ctx.attacker.acted_since_switch_in` を
  参照して不発にする）で実装した。
- `data/move.py` の `ねこだまし` エントリに `Event.ON_TRY_MOVE_1`
  （`subject_spec="attacker:self"`, `priority=30`）ハンドラを追加した
  （`.internal/spec/turn.md` の「30 | ねこだまし等: すでに行動をした」に準拠）。
- `python scripts/sort_handlers.py src/jpoke/handlers/move_attack.py` と
  `python scripts/sort_data/sort_moves.py` を実行済み。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- `tests/moves_attack/test_move_na.py` に以下を追加:
  - `test_ねこだまし_交代後の最初の行動では成功する`
  - `test_ねこだまし_場に出てから最初の行動でなければ失敗する`
- `tests/moves_status/test_move_na.py` に混入していた重複テスト
  `test_ねこだまし_ひるみが発動する`（本技は攻撃技であり `moves_status` に置くべきではない）を削除。
- `python scripts/sort_tests.py tests/moves_attack/test_move_na.py tests/moves_status/test_move_na.py` と
  `python scripts/generate_test_list.py` を実行し、`python -m pytest tests/ -v` で
  3533 passed, 1 skipped を確認（`.loop/test_logs/ねこだまし.log`）。
