# りゅうのいぶき レビュー結果

## 仕様書
- 変更なし。りゅうのいぶきは個別仕様書を持たず、共有仕様書 `.internal/spec/moves/_move_secondary_1.md` の該当行（対象: defender, 確率: 0.3, まひ列: 1）でカバーされている。
- 一次情報（`.internal/wiki/moves/りゅうのいぶき.html`）と照合し、タイプ=ドラゴン、分類=特殊、威力=60、命中率=100、PP=20、追加効果「30%の確率で相手をまひ状態にする」が一致することを確認した。

## 実装計画書
- `.internal/plan/moves/りゅうのいぶき.md` という個別ファイルは存在せず、複数技をまとめた計画書 `.internal/plan/moves/攻撃技_まひ系.md` に含まれている（他の多数のまひ付与技と共有）。
- 実装との差分を発見したため、りゅうのいぶきに関する箇所のみレビュー追記で修正した（他技の行は今回のスコープ外のため変更していない）:
  - 「現状の実装状況」表のりゅうのいぶき行を実装済み状態に更新（`flags={"secondary_effect"}` / `Event.ON_DAMAGE_HIT` / 実装済み）
  - 「ハンドラ構成」表のりゅうのいぶき行のイベントを `ON_HIT` → `ON_DAMAGE_HIT` に修正
  - 「Priority 根拠」節に、実装が `ON_DAMAGE_HIT`（`.internal/spec/turn.md` 未掲載イベント、既存の同種ハンドラ実績に基づく）を採用している旨を追記
  - 関数定義・MoveData 登録のコード例に「レビュー追記」として実際のコード（関数名 `りゅうのいぶき_apply_paralysis_to_defender`、`flags`、`Event.ON_DAMAGE_HIT`）を追記

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ra.py` の `MoveData`（type="ドラゴン", category="special", pp=20, power=60, accuracy=100, flags={"secondary_effect"}, handlers={Event.ON_DAMAGE_HIT: ...}）と `src/jpoke/handlers/move_attack.py` の `りゅうのいぶき_apply_paralysis_to_defender`（`apply_ailment_to_defender` を ailment="まひ", chance=0.3 で呼び出し）を確認し、仕様・同系統技（フリーズボルト・ほうでん・のしかかり等）と整合していることを確認した。
- `apply_ailment_to_defender`（`handlers/move.py`）内部で `battle.ailment_manager.apply` を通して電気タイプのまひ無効判定が行われるため、ハンドラ側の追加ガードは不要であることを確認（重複ガードなし、問題なし）。
- subject_spec はデフォルトの `attacker:self`（`MoveHandler` のデフォルト）で、`AttackContext` のロールと一致している。

## リーサル計算
- 対象外。`.internal/progress/move.md` の「リーサル実装」「リーサルテスト」列は n/a。

## テスト
- `tests/moves_attack/test_move_yarawa.py` に既存の `test_りゅうのいぶき_まひが発動する`（secondary_chance=1.0 でまひ発動を確認）に加えて、`test_りゅうのいぶき_まひが発動しない`（secondary_chance=0.0 でまひが付与されないことを確認）を追加した。他の同系統技（10まんボルト等）の「発動する/発動しない」両方をテストする方針に合わせた。
- `python -m pytest tests/ -v` を実行し、全件（3643 passed, 1 skipped）がパスすることを確認した。実行時に無関係な一部テスト（`test_field.py` のダメージ軽減系、`test_item_ta.py` のだっしゅつパック系）が乱数依存で一時的に失敗する既知のフレーキーな挙動を確認したが、りゅうのいぶきとは無関係であり、再実行で全件パスすることを確認済み。
