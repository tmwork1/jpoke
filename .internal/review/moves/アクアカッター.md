# アクアカッター レビュー結果

## 仕様書
- 新規作成（.internal/spec/moves/アクアカッター.md）。.internal/wiki/moves/アクアカッター.html を一次情報とし、
  みずタイプ/物理/威力70/命中100/PP20/対象1体/優先度0/接触なし/急所ランク+1/`slash`フラグを記載。
  判定は既存のサイコカッター仕様書（同一パターンの技）に倣って整備した。

## 実装計画書
- 新規作成（.internal/plan/moves/アクアカッター.md）。既存実装が仕様と完全一致していることを確認し、
  差分なしとして記録した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_a.py` の `"アクアカッター"` エントリは
  `type="みず", category="physical", pp=20, power=70, accuracy=100, critical_rank=1,
  flags={"slash"}, handlers={}` で仕様と一致しており、修正不要だった。
  PPも src/jpoke/data/ps-champ-ja/moves.json の値（20）と一致。

## リーサル計算
- 対象外（.internal/progress/move.md の「リーサル実装」列は `n/a`。追加効果を持たない
  通常の物理攻撃技のため）。

## テスト
- 変更なし。`tests/moves_attack/test_move_a.py` の
  `test_アクアカッター_急所ランクが1` / `test_アクアカッター_急所ランクが1_乱数大で急所なし`
  は critical_rank=1（急所発生率1/8）の境界値を正しく検証しており修正不要だった。
  `python -m pytest tests/ -v` で全5801件合格・1件スキップ（既存の無関係なスキップ）を確認した。
