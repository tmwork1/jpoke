# ハサミギロチン レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ハサミギロチン.md`）。既存の `つのドリル.md`・`じわれ.md`・
  `ぜったいれいど.md` と同じ構成で、`.internal/spec/moves/_ohko.md`（一撃必殺技共通仕様）を参照する形で作成。
- 一次情報（`.internal/wiki/moves/ハサミギロチン.html`）と `src/jpoke/data/ps-champ-ja/moves.json` を突き合わせ、
  PPを8（champions基準）と明記した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ハサミギロチン.md`）。既存実装（`ha.ohko_damage` を
  `Event.ON_MODIFY_MOVE_DAMAGE` に `priority=90` で登録）が `つのドリル`・`じわれ`・
  `ぜったいれいど` と同一パターンであることを確認し、その根拠を記載した。
- レビューで発見したPPバグの修正内容を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `ハサミギロチン` エントリで `pp=5`
  （Gen9本家基準）になっていたバグを修正し、`src/jpoke/data/ps-champ-ja/moves.json` の値である
  `pp=8` に変更した（プロジェクト規約によりチャンピオンズのPP値が正）。
- ダメージ計算・タイプ相性無効化・がんじょう/きあいのタスキ等との相互作用は既存の
  共通 `flags={"ohko"}` ハンドラ群で正しく処理されており、修正なし。
- 参考: `つのドリル`・`じわれ`・`ぜったいれいど` も同様に `pp=5` のままであり、
  同一のPP移行漏れバグを抱えている可能性が高い。ただし本レビューの対象エントリ外のため、
  今回は修正せず計画書に記録のみ残した（別エントリのレビュー時に対応想定）。

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行は「リーサル実装: n/a」「リーサルテスト: n/a」）。

## テスト
- `tests/moves_attack/test_move_ha.py` に以下を新規追加:
  - `test_ハサミギロチン_タイプ分類命中率PPが仕様通り`（type/category/power/accuracy/pp/flags の確認）
  - `test_ハサミギロチン_ゴーストタイプに無効`
  - `test_ハサミギロチン_命中時は相手を一撃で倒す`
- 既存の `tests/moves_attack/test_move__group.py` の `test_一撃必殺技_*` 系テストは
  `つのドリル`・`じわれ` を代表技として `flags={"ohko"}` 共通ハンドラの挙動
  （命中時KO・外れ時ノーダメージ・タイプ相性無効化）をカバーしており、変更なし。
- `python -m pytest tests/ -v` で全3950件パス（1件skip）を確認（
  `.loop/test_logs/ハサミギロチン.log`）。
