# エレキネット レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/エレキネット.md`）。一次情報（`.internal/wiki/moves/エレキネット.html`）を基に、
  でんきタイプ・特殊・威力55・命中95・PP16（Champions基準）・非接触・100%すばやさ1段階ダウンの
  仕様を記載。姉妹技 `こごえるかぜ` の仕様書構成に合わせた。

## 実装計画書
- 新規作成（`.internal/plan/moves/エレキネット.md`）。既存実装との差分（PP誤り・`secondary_effect`
  フラグ欠落）を明記し、ハンドラ構成・リーサル対象外の理由を記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `"エレキネット"` エントリを修正：
  - `pp=15` → `pp=16`（`src/jpoke/data/ps-champ-ja/moves.json` の値に合わせて修正。既存実装が
    Gen9本家基準のPPのまま移行されていなかったバグ）。
  - `flags={"secondary_effect"}` を追加（100%発動の追加効果を持つため、ちからずくの
    威力1.3倍・効果無効化の対象技として扱われるようにする。姉妹技 `こごえるかぜ` の
    既存実装と同じ扱いに合わせた）。
  - `handlers/move_attack.py` の `エレキネット_lower_defender_spd` 本体は
    `modify_defender_stats(battle, ctx, value, stats={"spe": -1})` で仕様通りのため変更なし。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」列が `n/a`）。すばやさダウンのみの
  追加効果でHP分布に影響しないため。

## テスト
- `tests/moves_attack/test_move_a.py` に以下を追加：
  - `test_エレキネット_すばやさが下がる`（100%発動確認）
  - `test_エレキネット_ちからずくで威力上昇しすばやさダウンは発動しない`
    （`secondary_effect` フラグ修正の回帰テスト。ちからずくで威力1.3倍・追加効果無効化を確認）
  - `test_エレキネット_相手にダメージを与える`
  - `python -m pytest tests/ -v` で全件（3695 passed, 1 skipped）を確認。
