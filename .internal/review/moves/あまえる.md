# あまえる レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/あまえる.md`）。一次情報（ポケモンWiki）と照合し、タイプ（フェアリー）・
  分類（変化）・PP20・命中率100・効果（相手のこうげきを2段階下げる）・まもる/マジックコート/
  オウムがえし/よこどりの適否を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/あまえる.md`）。`ON_STATUS_HIT` の subject_spec・priority の根拠
  （同種ハンドラ `あまいかおり_modify_defender_stats` / `なきごえ_modify_defender_stats` との
  パターン一致）を明記した。

## 実装（handlers/ / data/）
- 変更なし。既存実装（`src/jpoke/handlers/move_status.py` の `あまえる_modify_defender_stats`、
  `src/jpoke/data/moves/move_a.py` の `"あまえる"` エントリ）は仕様書・計画書と一致しており、
  タイプ・PP・命中率・効果（`modify_defender_stats` で `atk: -2` を defender に付与）に誤りはなかった。
  PP20 は `src/jpoke/data/ps-champ-ja/moves.json` の値とも一致している。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列は n/a）。

## テスト
- 変更なし。既存の `tests/moves_status/test_move_a.py::test_あまえる_defenderのこうげきが2段階下がる`
  は仕様どおりの内容で十分にカバーしており、追加修正は不要と判断した。
- `python -m pytest tests/ -v` を実行し、4067 passed, 1 skipped で全件パスを確認した
  （実行途中で無関係な既存テスト3件が乱数由来のフレークで一時的に失敗したが、再実行で全件成功。
  あまえる関連の変更とは無関係）。
