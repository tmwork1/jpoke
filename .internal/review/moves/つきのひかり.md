# つきのひかり レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/つきのひかり.md`）。既存の `あさのひざし.md` / `こうごうせい.md` と
  完全に同一の効果（天候依存回復）であることを一次情報（wiki）で確認し、同一構成で作成した。
  - 基本情報（タイプ: フェアリー、分類: 変化、PP: 8、対象: 自分、優先度: 0、直接攻撃: ×）
  - 天候別回復量テーブル（はれ/おおひでり2/3、あめ/おおあめ/すなあらし/ゆき1/4、それ以外1/2）
  - ばんのうがさ・ノーてんき・エアロックとの相互作用
  - かいふくふうじとの相互作用
  - まもる・マジックコートに無効である旨と、その実装条件（`target="self"` の明示が必須）

## 実装計画書
- 既存の `.internal/plan/moves/つきのひかり.md` を修正した。
  - `data/move.py` → 実際のファイルパス `data/moves/move_ta.py` に修正
  - サンプルコードに `target="self"` を追加（欠落していた）
  - `pp=5` を `pp=8`（`src/jpoke/data/ps-champ-ja/moves.json` 準拠）に修正
  - 専用ハンドラ関数 `つきのひかり_heal_self` を新規実装する案から、既存の `あさのひざし_heal_self`
    を共用する方針（実際の実装と一致）に変更
  - 注意点に `target="self"` 未設定時のまもる/マジックコート誤反応リスクを明記

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `"つきのひかり"` エントリを修正した。
  - `target="self"` が欠落していたため追加（デフォルト値 `"foe"` のままだと
    `Move.is_blocked_by_protect` / `Move.is_reflectable` が誤って `True` を返し、
    まもる・マジックコートに誤反応するバグがあった）
  - `pp=5` を `pp=8` に修正（`src/jpoke/data/ps-champ-ja/moves.json` の値と一致させた。Gen9本家基準の
    5 が移行漏れとして残っていた）
  - ハンドラ自体（`hs.あさのひざし_heal_self` を ON_STATUS_HIT で登録）は仕様と一致しており
    変更なし。`あさのひざし_heal_self` は `battle.weather_for(mon)` を参照しており
    ばんのうがさにも対応済み

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行「リーサル実装」列が `n/a`）

## テスト
- `tests/moves_status/test_move_ta.py` の つきのひかり テストを修正・追加した。
  - `test_つきのひかり_マジックコートで跳ね返されない` を新規追加（`target="self"` バグの
    回帰防止。相手のマジックコート状態下でも自分のHPが回復し、相手には影響しないことを確認）
  - `test_つきのひかり_まもるで防がれない` を新規追加（相手のまもる状態下でも回復することを確認）
  - `test_つきのひかり_天候別回復量` のパラメータを `おおあめ`・`おおひでり`・`すなあらし` を
    追加して `あさのひざし`・`こうごうせい` と同じ網羅性に統一（従来は通常/あめ/はれの3パターンのみ）
  - `python -m pytest tests/ -v` で全4236件（+skip 1件）がパスすることを確認
    （ログ: `.loop/test_logs/つきのひかり.log`）
