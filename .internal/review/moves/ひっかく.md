# ひっかく レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ひっかく.md`）。一次情報（`.internal/wiki/moves/ひっかく.html`）に基づき、
  ノーマルタイプ・物理・威力40・命中100・PP35・優先度0・直接攻撃・追加効果なしという基本情報と
  判定フラグ（急所・命中判定・まもる・おうじゃのしるし・マジックコート・よこどり・オウムがえし）を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ひっかく.md`）。既存実装（`src/jpoke/data/moves/move_ha.py`）が
  仕様と一致していることを確認した内容と、追加効果を持たないため専用ハンドラが不要である旨を記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ha.py` の `ひっかく` エントリ
  （`type="ノーマル"`, `category="physical"`, `pp=35`, `power=40`, `accuracy=100`,
  `flags={"contact"}`, `handlers={}`）は仕様書と一致しており、修正不要と判断した。
  `target` 未指定により `MoveData` のデフォルト `"foe"`（相手1体）が適用され、
  仕様の「対象: 相手1体」とも一致する。

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行が「リーサル実装: n/a」）。

## テスト
- `tests/moves_attack/test_move_ha.py` に以下を追加した。
  - `test_ひっかく_相手にダメージを与える`: 通常のダメージ計算でダメージが発生することを確認。
  - `test_ひっかく_追加効果が発生しない`: 状態異常・能力ランク変化が一切発生しないことを確認。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ha.py` でソート、
  `python scripts/generate_test_list.py` でテスト一覧を更新した。
- `python -m pytest tests/ -v` を実行し、3578 passed, 1 skipped で全件パスを確認した
  （結果は `.loop/test_logs/ひっかく.log` に保存）。
