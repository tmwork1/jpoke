# たたりめ レビュー結果

## 仕様書
- 存在しなかったため新規作成した（`.internal/spec/moves/たたりめ.md`）。
- 一次情報（ポケモンWiki）から、ゴースト/特殊/威力65（第六世代以降）/命中100/PP10/対象1体/優先度0/非接触、
  相手が状態異常のとき威力2倍、という基本仕様を整理した。
- 「技の仕様」節にあった特性『ぜったいねむり』の相手には常に威力2倍になる（ただしネッコアラは通常
  ノーマルタイプのためゴースト技が無効化される）という補足も明記した。

## 実装計画書
- 存在しなかったため新規作成した（`.internal/plan/moves/たたりめ.md`）。
- `Event.ON_CALC_POWER_MODIFIER` のデフォルト priority／subject_spec を採用する根拠として、
  同種の可変威力技（`からげんき_double_power_when_ailment` 等）の既存実装を参照した旨を明記した。

## 実装（handlers/ / data/）
- `src/jpoke/data/move.py` の `"たたりめ"` エントリ（type=ゴースト, category=special, pp=10,
  power=65, accuracy=100）と `src/jpoke/handlers/move_attack.py` の
  `たたりめ_double_power_when_ailment` を仕様書と照合した結果、既に仕様通りで変更なし。
- `ctx.defender.ailment.is_active` は状態異常名が空でないかどうかで判定するため、
  特性『ぜったいねむり』が付与する状態異常『ゆめうつつ』も自動的に対象へ含まれており、
  追加実装なしで「ぜったいねむりの相手には常に威力2倍」という仕様を満たしていることを確認した。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）。
  威力補正のみでダメージ計算自体はダメージ計算モジュールに委譲されるため専用ハンドラは不要。

## テスト
- 既存テスト（`tests/moves_attack/test_move_ta.py`）は
  - まひ状態の相手に対して威力2倍になること
  - 状態異常なしのとき威力補正なし
  - どく状態の相手に対して威力2倍になること
  を確認しており、内容は実装と一致していた。
- 特性『ぜったいねむり』の相手（状態異常『ゆめうつつ』）に対しても威力2倍になることを確認する
  テスト `test_たたりめ_ぜったいねむりの相手には常に威力2倍` を新規追加した。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ta.py` でソート、
  `python scripts/generate_test_list.py` でテスト一覧を更新し、全テストを実行して確認した
  （`.loop/test_logs/たたりめ.log`）。たたりめ関連のテストは全件パス。
  なお `tests/moves_status/test_move_ra.py::test_リフレッシュ_まひを治す` が本タスクとは無関係に
  既存の作業ツリー（`ailment_manager.py` 等が未コミットで変更済み）の影響で失敗していたが、
  たたりめの実装・テストとは無関係のため本タスクでは対応していない。

## 進捗表の修正
- `.internal/progress/move.md` の PP 列が誤って `12` と記載されていたため `10` に修正した。
- 仕様書・計画書・レビュー列を `x`／`x`／`✓` に更新し、効果列に説明文を記載した。
