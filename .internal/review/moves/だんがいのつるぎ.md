# だんがいのつるぎ レビュー結果

## 仕様書
- 新規作成（.internal/spec/moves/だんがいのつるぎ.md）。
  - 基本情報（じめん/物理/威力120/命中85/PP10/対象:相手全員/優先度0/非接触）を一次情報（ポケモンWiki）から記載。
  - 追加効果なし、および『あなをほる』威力2倍・『グラスフィールド』威力半減の対象外であることを明記
    （`handlers/volatile.py` の `HIDDEN_MOVE_ALLOWED_MOVES["あなをほる"]` と
    `handlers/field.py` の `グラスフィールド_power_modifier` の対象技リストがいずれも
    `じしん`・`マグニチュード`（・`じならし`）限定で、`だんがいのつるぎ` を含まないことを確認済み）。

## 実装計画書
- 新規作成（.internal/plan/moves/だんがいのつるぎ.md）。
  - 追加効果がなく専用ハンドラを実装しないこと、`data/move.py` の既存エントリが仕様と一致していることを記載。

## 実装（handlers/ / data/）
- 変更なし。既存の `data/move.py` の `"だんがいのつるぎ"` エントリ（じめん/物理/pp10/power120/accuracy85/`handlers={}`）は
  仕様と一致しており修正不要と判断。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- `tests/moves_attack/test_move_ta.py` に `test_だんがいのつるぎ_相手にダメージを与える` を追加
  （既存の `test_じしん_相手にダメージを与える` と同型の基本ダメージ確認テスト）。
  `scripts/sort_tests.py` でソート後、`scripts/generate_test_list.py` でテスト一覧を更新し、
  `python -m pytest tests/ -v` で全3424件パス（1件skip）を確認。
