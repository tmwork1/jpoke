# みずしゅりけん レビュー結果

## 仕様書
- 対象外（変更なし）: みずしゅりけんは追加効果を持たない汎用の2～5回連続攻撃技であり、
  `.internal/spec/moves/連続技.md`（9.3 2-5回攻撃技の表）に「みず | 特殊 | 15 | 100 | 20 | × | 優先度+1 | 6-」
  として既に記載されている。一次情報（`.internal/wiki/moves/みずしゅりけん.html`）と照合し、
  タイプ・分類・威力・命中率・PP・優先度・直接攻撃×・命中判定は初回のみ（追加効果×）の
  いずれも一致することを確認した。同種の他の連続攻撃技（つっぱり・つららばり等）と同様、
  個別の仕様書ファイルは不要と判断し、新規作成しなかった。

## 実装計画書
- 対象外（変更なし）: 固有ハンドラを持たないため、個別の実装計画書は不要と判断した
  （つっぱり・つららばり等の同種の技と同様の扱い）。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ma.py` の `みずしゅりけん` エントリ
  （type=みず, category=special, pp=20, power=15, accuracy=100, priority=1,
  multi_hit={min:2, max:5, check_hit_each_time:False, power_sequence:()}, handlers={},
  flags未指定=直接攻撃なし）は、一次情報（優先度+1・特殊・威力15・命中100・PP20・
  2～5回連続攻撃・直接攻撃×・追加効果×）と完全に一致しており、修正不要と判断した。

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行は n/a / n/a のまま）。連続攻撃技はリーサル計算の
  対象外として扱われており、`handlers/lethal.py` に多段ヒット技全般を扱う専用ロジックは無い。

## テスト
- 変更なし。`tests/moves_attack/test_move_ma.py` の
  `test_みずしゅりけん_相手にダメージを与える`（優先度+1の先制特殊技でダメージを与えることを確認）と
  `test_みずしゅりけん_複数ヒットする`（`_resolve_hit_count` が2～5の範囲に収まることを確認）が
  既存の同種技のテストパターンと一致しており、過不足なし。
  `python -m pytest tests/ -v` で全件パスすることを確認した。

## 進捗ファイルの修正
- `.internal/progress/move.md` の みずしゅりけん 行のレビュー列を `-` → `✓` に更新した。
