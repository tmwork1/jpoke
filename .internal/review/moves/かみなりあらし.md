# かみなりあらし レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/かみなりあらし.md`）。
- ウィキ（`.internal/wiki/moves/かみなりあらし.html`）に基づき、タイプ・分類・威力・命中・PP・対象・優先度・
  接触・追加効果（20%まひ）・天候必中仕様（あめ時必中、`かみなり`と異なりにほんばれ時の命中率低下はない）を記載。

## 実装計画書
- 新規作成（`.internal/plan/moves/かみなりあらし.md`）。
- `.internal/plan/moves/かみなり.md` のパターンを参考に、ハンドラ構成・priority根拠・実装箇所・
  テスト方針を記載。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_attack.py`: `かみなりあらし_accuracy` を新規実装。
  あめ天候時に必中（`HandlerReturn(value=None)`）、それ以外は補正なしで元の命中率80を維持する。
  （まひ付与ハンドラ `かみなりあらし_apply_paralysis_to_defender` は既存実装で誤りなし。）
- `src/jpoke/data/move.py`: `かみなりあらし` の `handlers` に
  `Event.ON_MODIFY_ACCURACY: h.MoveHandler(ha.かみなりあらし_accuracy, subject_spec="attacker:self")` を追加。
  従来はあめ時必中の仕様が未実装だった（まひ付与ハンドラのみ登録されていた）ため修正。
- `scripts/sort_handlers.py src/jpoke/handlers/move_attack.py` / `scripts/sort_data/sort_moves.py` を実行済み。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装・リーサルテスト列は `n/a`）。

## テスト
- `tests/moves_attack/test_move_ka.py` に `test_かみなりあらし_あめ中は必中になる` を追加
  （あめ天候時、命中率80では外れるはずの乱数でも命中することを確認）。
- 既存の `test_かみなりあらし_まひが発動する`（20%まひ付与）は変更なし。
- `scripts/sort_tests.py tests/moves_attack/test_move_ka.py` でソート後、
  `scripts/generate_test_list.py` でテスト一覧を更新。
- `python -m pytest tests/ -v` 実行結果: 3281 passed, 1 skipped（`.loop/test_logs/かみなりあらし.log`）。
