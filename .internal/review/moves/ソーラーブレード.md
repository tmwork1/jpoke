# ソーラーブレード レビュー結果

## 仕様書
- 変更なし。`.internal/spec/moves/ソーラーブレード.md` は一次情報（Wiki）と照合し、威力・命中率・PP（wiki記載値）・タイプ・分類・
  溜め技/切る技の仕様・天候による威力半減・にほんばれでの即時発動・パワフルハーブ/ばんのうがさとの相互作用など
  すべて一致しており、誤り・欠落はなかった。

## 実装計画書
- 新規作成。`.internal/plan/moves/ソーラーブレード.md` が存在しなかったため新規作成した
  （`.internal/progress/move.md` では計画書列が既に x になっていたが、実際にはファイルが存在しない不整合があった）。
  既存実装（ソーラービームと共通のON_MOVE_CHARGEパターン、揮発状態、威力半減ロジック）を計画書として文書化し、
  priorityの根拠（turn.mdに未掲載のため既存のソーラービーム実装パターンを踏襲する旨）を明記した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_sa.py` の "ソーラーブレード" エントリの PP を `10` → `12` に修正した。
  `src/jpoke/data/ps-champ-ja/moves.json`（552行目）および `.internal/progress/move.md` のPP列はいずれも12だが、
  実装はGen9本家基準のwiki記載値（10）のままになっていた移行漏れのバグだった。
- その他（ON_MOVE_CHARGEによる2ターン構造、にほんばれ/おおひでりでの即時発動、天候による威力半減、
  slashフラグによるきれあじ対応、専用揮発状態"ソーラーブレード"がソーラービームと独立していること）は
  すべて仕様通り正しく実装されていたため変更なし。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装・リーサルテスト列がいずれも n/a）。

## テスト
- 新規追加。`tests/moves_attack/test_move_sa.py` にソーラーブレード専用のテストが1件も存在しなかったため、
  以下7件を追加した:
  - 2ターンで攻撃する（揮発状態の付与・解除を確認）
  - にほんばれ下では1ターンで攻撃する
  - あめ/すなあらし/ゆき下で威力が半減する（各天候ごとに3件）
  - きれあじ特性で威力1.5倍になる（slashフラグの確認）
  - パワフルハーブで1ターンで攻撃し、道具を消費する
- 追加後、`python -m pytest tests/ -v` を実行し、全3849件（1件skip）が成功することを確認した。
