# きんぞくおん レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/きんぞくおん.md`）。一次情報（Wiki）から基本情報（はがね/変化/命中85/優先度0/直接攻撃×/音技）と
  効果（相手のとくぼうを2段階下げる）、判定（まもる○・マジックコート○・オウムがえし○・よこどり×）を記載。
  PPはポケモンチャンピオンズ仕様（`src/jpoke/data/ps-champ-ja/moves.json`）に従い20とし、Gen9本家の40と異なる旨を注記した。

## 実装計画書
- 新規作成（`.internal/plan/moves/きんぞくおん.md`）。同効果の既存実装（`うそなき`）を参考に、
  `ON_STATUS_HIT`（priority=100デフォルト）で`modify_defender_stats(stats={"spd": -2})`を呼ぶ構成とした。
  PP修正の経緯（40→20）を明記。

## 実装（handlers/ / data/）
- `data/moves/move_ka.py`: `pp=40`（Gen9本家値のまま）となっていたバグを`pp=20`
  （`src/jpoke/data/ps-champ-ja/moves.json`準拠）に修正。
- `handlers/move_status.py`の`きんぞくおん_modify_defender_stats`は`modify_defender_stats(stats={"spd": -2})`
  を呼ぶのみで仕様通りのため変更なし。

## リーサル計算
- 対象外（`.internal/progress/move.md`のリーサル実装列が`n/a`）。とくぼうダウンは相手の次ターン以降の
  被ダメージに影響するのみで、本ターンの生存・与ダメージ判定には関与しないため。

## テスト
- `tests/moves_status/test_move_ka.py`にテストが存在しなかったため以下を追加:
  - `test_きんぞくおん_defenderのとくぼうが2段階下がる`: 通常命中時にdefenderのとくぼうランクが-2になることを確認
  - `test_きんぞくおん_まもるで防がれる`: defenderが`まもる`状態のとき、とくぼうランクが変化しないことを確認
- `python -m pytest tests/ -v` で全4137件パス（1件skip）を確認。ログは
  `.loop/test_logs/きんぞくおん.log` に保存。
