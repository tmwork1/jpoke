# じゃれつく レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/じゃれつく.md`）。一次情報（`.internal/wiki/moves/じゃれつく.html`）に基づき、
  タイプ: フェアリー / 分類: 物理 / 威力: 90 / 命中: 90 / PP: 10 / 接触あり / 追加効果
  「10%の確率で相手のこうげきを1段階下げる」を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/じゃれつく.md`）。既存の同種ハンドラ（きあいだま・うらみつらみ 等）の
  実装パターンに合わせ、`ON_DAMAGE_HIT` / priority 100（デフォルト）でのハンドラ構成・
  ちからずくとの相互作用・テスト方針を記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/move_attack.py` の `じゃれつく_lower_defender_atk`
  （`modify_defender_stats` を10%で呼び出す実装）、`src/jpoke/data/move.py` の
  `MoveData`（フェアリー・物理・pp=10・power=90・accuracy=90・`{"contact", "secondary_effect"}`）は
  仕様と一致しており、修正不要だった。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列は `n/a`）。確率発動の相手ランク低下効果であり、
  ダメージ量そのものを変化させないため。

## テスト
- `tests/moves_attack/test_move_sa.py` に以下を追加した：
  - `test_じゃれつく_こうげき1段階低下が発動しない`（`secondary_chance=0.0` でランク変化なしを確認）
  - `test_じゃれつく_相手にダメージを与える`（追加効果なしでもダメージが発生することを確認）
  - 既存の `test_じゃれつく_こうげき1段階低下が発動する` は変更なし。
- `.internal/progress/move.md` のじゃれつく行に誤り（PP列が `12` になっていた。正しくは `10`）を発見し修正した。
  併せて 仕様書・計画書・レビュー 列を `x` / `x` / `✓` に更新した（実装・テストは既に `x` 済みだった）。
