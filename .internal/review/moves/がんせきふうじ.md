# がんせきふうじ レビュー結果

## 仕様書

- 新規作成（`.internal/spec/moves/がんせきふうじ.md`）。
  - `.internal/wiki/moves/がんせきふうじ.html` を参照し、本プロジェクト対象世代（第六世代以降）の値
    （いわ・物理・威力60・命中95・PP15・追加効果100%で相手のすばやさランク-1）を記載した。

## 実装計画書

- 新規作成（`.internal/plan/moves/がんせきふうじ.md`）。
  - `data/move.py` に `flags={"secondary_effect"}` が欠落していた点を修正計画として明記した。
  - リーサル計算は対象外（n/a）である根拠を記載した。

## 実装（handlers/ / data/）

- `src/jpoke/data/move.py` の `"がんせきふうじ"` エントリに `flags={"secondary_effect"}` を追加した。
  - `.internal/spec/moves/_move_secondary_1.md` にも「がんせきふうじ defender 確率1 S-1」と記載されている通り、
    本技はちからずくの対象（威力1.3倍・追加効果無効化）となるべきだが、フラグ欠落によりちからずくが
    全く発動しない状態になっていた。
  - `handlers/move_attack.py` の `がんせきふうじ_lower_defender_spd`（`modify_defender_stats` 経由で
    `spe: -1` を適用）は仕様通りで修正不要。
  - `python scripts/sort_data/sort_moves.py` を実行済み。

## リーサル計算

- 対象外（n/a）。本技の追加効果は相手の『すばやさ』ランクを下げるのみでHPに影響しないため、専用のリーサル
  計算ハンドラは不要（`あわ`・`からみつく`・`じならし`・`とびつく`・`エレキネット`・`マッドショット` など
  同種の速度低下技と同じ扱い）。`.internal/progress/move.md` の「リーサル実装」「リーサルテスト」列を
  `-` から `n/a` に修正した。

## テスト

- `tests/moves_attack/test_move_ka.py` に以下を追加した。
  - `test_がんせきふうじ_すばやさ低下が発動する`: 100%の確率で相手のすばやさランクが-1になることを確認。
  - `test_がんせきふうじ_ちからずくで威力上昇しすばやさ低下は発動しない`: ちからずく所持時に威力補正が
    1.3倍（`power_modifier == 5325`）になり、すばやさ低下が発動しないことを確認。
  - `python scripts/sort_tests.py tests/moves_attack/test_move_ka.py` でソート、
    `python scripts/generate_test_list.py` でテスト一覧を更新、`python -m pytest tests/ -v` で全件成功を確認した。

## 進捗表の修正

- `.internal/progress/move.md` の がんせきふうじ 行を修正:
  - PP: 16 → 15（誤記を修正）
  - 仕様書・計画書: `-` → `x`
  - テスト: `-` → `x`
  - リーサル実装・リーサルテスト: `-` → `n/a`
  - レビュー: `-` → `✓`
