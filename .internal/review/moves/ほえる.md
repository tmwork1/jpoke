# ほえる レビュー結果

## 仕様書
- `.internal/spec/moves/ほえる.md` が存在しなかったため、`.internal/wiki/moves/ほえる.html` と
  `src/jpoke/data/ps-champ-ja/moves.json`、および既存の `.internal/spec/moves/ふきとばし.md` の構成に倣って新規作成した。
- PP20・優先度-6・音技（sound）フラグ・まもる系防御技を貫通（`unprotectable`）・
  マジックコートで反射・みがわり貫通・きゅうばん/ばんけん/ねをはるで無効化・
  ゴーストタイプにも有効、などを明記した。

## 実装計画書
- `.internal/plan/moves/ほえる.md` は既に存在していたが、以下の誤り・欠落を修正した。
  - 「`ふきとばし` は `ON_HIT` を使う」という誤った記述を修正
    （実際は `ふきとばし` も `category="status"` のため `ON_STATUS_HIT` を使用）。
  - `unprotectable` フラグの欠落、および `on_blow_check_switch_target`
    （交代不可判定ハンドラ）の欠落について明記し、実装箇所のコード例を最新化した。
  - テストケース一覧を実際に追加したテスト名に更新した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `ほえる` エントリに以下の修正を行った。
  - `flags` に `"unprotectable"` を追加（欠落していたため `src/jpoke/data/ps-champ-ja/moves.json` の
    「守×」およびまもる貫通の仕様に反し、まもるで防がれてしまうバグがあった）。
  - `Event.ON_BEFORE_APPLY_MOVE` に `on_blow_check_switch_target`（priority=100）を追加
    （`ふきとばし` には既に登録されていたが `ほえる` には欠落しており、相手に交代可能な
    控えがいない場合の失敗ログが出ないまま何も起きない実装になっていた）。
  - `handlers/move_status.py` 側の `on_blow_apply` / `on_blow_check_switch_target` / `blow` は
    既存実装のままで問題なく、`ふきとばし` と共用できることを確認した（変更なし）。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」「リーサルテスト」列は `n/a`。
  ほえるはダメージを与えない変化技のためリーサル計算の対象にならない）。

## テスト
- `tests/moves_status/test_move_ha.py` に `ふきとばし` の既存テストと対になるテストを追加した。
  - `test_ほえる_きゅうばんで無効化される`
  - `test_ほえる_まもるを貫通する`（`unprotectable` フラグ追加により新たに正しく通過することを確認）
  - `test_ほえる_マジックコートで跳ね返り使用者が交代する`
  - `test_ほえる_みがわりを貫通する`
  - 既存の `test_ほえる_ねをはる状態の相手には失敗する` /
    `test_ほえる_控えポケモンがいない場合は失敗する` /
    `test_ほえる_控えポケモンがいる場合に交代が発生する` は内容を確認し変更なし。
- `python -m pytest tests/ -v` を実行し、全4399件（1件skip）がパスすることを確認した
  （結果は `.loop/test_logs/ほえる.log` に保存）。
  ※ `tests/moves_attack/test_move_ma.py::test_むしくい_ねんちゃく持ちをひんしにさせた場合は奪える`
  はまひによる行動不発の確率（25%）に依存した既存の flaky テストであり、本レビュー対象
  （ほえる）とは無関係。再実行で再現しないことを確認済み。
