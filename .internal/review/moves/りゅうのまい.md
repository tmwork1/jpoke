# りゅうのまい レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/りゅうのまい.md`）。.internal/wiki/moves/りゅうのまい.html の一次情報
  （タイプ: ドラゴン、分類: 変化、PP20、対象: 自分、こうげき・すばやさ+1ずつ、まもる: ×、
  マジックコート: ×、よこどり: ○、おどりのわざ）を反映。

## 実装計画書
- 既存の `.internal/plan/moves/りゅうのまい.md` を全面的に修正。
  - `category="変化"` → `category="status"`（実装の Literal 型に合わせた誤記修正）
  - `labels=["dance"]` → `flags={"dance"}`（実装フィールド名の誤記修正）
  - `target="self"` の明記漏れを追加し、「注意点・エッジケース」に必須である理由
    （未指定だとデフォルトの `"foe"` になり、まもる・マジックコート判定に誤反応する）を追記
  - Priority 根拠に `つるぎのまい_modify_attacker_stats` も参照として追加
  - 「リーサル計算」節を追加（対象外である旨を明記）

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ra.py` の `"りゅうのまい"` エントリに `target="self"` が
  指定されていないバグを修正（デフォルトの `"foe"` のままだと、`is_blocked_by_protect` /
  `is_reflectable` の判定でまもる・マジックコートが誤って有効になってしまう）。
  `handlers/move_status.py` の `りゅうのまい_modify_attacker_stats` 自体は仕様どおり
  （`modify_attacker_stats` で `atk+1`, `spe+1`）で修正不要。

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行は `n/a`。ダメージを与えない自己バフ技のため
  リーサル計算ハンドラは不要）。

## テスト
- 既存の2件（こうげき・すばやさが1段階ずつ上がる／こうげき最大でもすばやさは上昇する）は
  変更なし。
- `target="self"` 修正の回帰テストとして、`ちょうのまい` と同様の観点で以下を追加：
  - `test_りゅうのまい_マジックコートで跳ね返されない`
  - `test_りゅうのまい_自分対象のためまもるで防がれない`
- `python -m pytest tests/ -v` は全件パス（4452 passed, 1 skipped）。結果は
  `.loop/test_logs/りゅうのまい.log` に保存。
