# パワーシフト レビュー結果

## 調査結果

- 一次情報として `.internal/wiki/moves/パワーシフト.html`（ポケモンWiki、記事ID 55786）を新規取得し確認した。
  `Pokémon LEGENDS アルセウス` と `スカーレット・バイオレット` の双方に掲載がある正規の技であることを確認した
- ウィキ「技の仕様」節: 「パワートリックと同じ効果を持つ」と明記。効果は使用者自身の
  こうげき（A）・ぼうぎょ（B）の実数値を入れ替えるもので、パワートリックと完全に同一
- ウィキ「備考」節: 「スカーレット・バイオレットでは習得できるポケモンがおらず、ゆびをふるでも
  出現しないため使用手段が無いが、カテゴリ:スカーレット・バイオレットで使用不可能な技として
  設定されているわけではない」。すなわちSVで現在この技を習得できるポケモンは存在しないが、
  技自体はSV内部のゲームエンジンとして正規に実装されている（グリッチ技や使用不可能技の扱いではない）
- `src/jpoke/data/ps-champ-ja/moves.json` に `"パワーシフト"` エントリが実際には存在することを確認した
  （`num: 829, type: "ノーマル", category: "Status", pp: 12, target: "self", flags: ["snatch"]`）。
  `.internal/progress/move.md` の従来の注記「moves.jsonに未掲載」は調査時点（2026-07-16頃）の
  記述が古くなっていたことが判明したため、`.internal/plan/ps_champ_ja/data_source.md` の該当箇所も
  あわせて更新した
- 以上より、実在性・効果内容ともに十分な確証が得られたと判断し、通常の実装手順（仕様書→計画書→
  実装→テスト）を実施した

## 仕様書

- 新規作成: `.internal/spec/moves/パワーシフト.md`
  - タイプ: ノーマル / 分類: 変化 / PP: 12（ps-champ-ja準拠） / 命中: 必中 / 対象: 自分
  - 効果: 自分の実数値の『こうげき』と『ぼうぎょ』を入れ替える（パワートリックと同一効果）。
    ランク補正は変化しない
  - まもる・マジックコートは対象が自分自身のため自動的に無効化される。よこどり・ゆびをふる
    （メトロノーム）はいずれも本シミュレータ未実装のため対象外（ゲーム仕様上もパワーシフトは
    どちらの対象にもならない点をウィキで確認済み）

## 実装計画書

- 新規作成: `.internal/plan/moves/パワーシフト.md`
  - 実在性・着手可否の調査結果を明記
  - ハンドラは新規実装せず、既存の `パワートリック_swap_stats`（`handlers/move_status.py`）を
    共有する設計とした（`あさのひざし_heal_self` を複数技で共有する既存パターンに準拠）
  - priorityは `.internal/spec/turn.md` に `ON_STATUS_HIT` の記載がないため、既存の実数値操作
    ハンドラ（パワートリック等）と同一の priority=100（`MoveHandler` デフォルト）を採用する根拠を明記
- 既存の `.internal/plan/moves/パワートリック.md` にあった「パワーシフトは未登録」という記述が
  古くなったため、実装完了を反映するよう更新した

## 実装（handlers/ / data/）

- `src/jpoke/handlers/move_status.py`: 変更なし（新規ハンドラ関数は追加せず、既存の
  `パワートリック_swap_stats` を再利用）
- `src/jpoke/data/moves/move_ha.py`: `"パワーシフト"` エントリを新規追加
  （`パワーシェア`と`パワージェム`の間、`ON_STATUS_HIT`ハンドラに`パワートリック_swap_stats`を登録。
  `type`/`category`/`pp`/`target`等は`ps-champ-ja/moves.json`から`common_setup()`により自動的に
  埋まるため明示指定は不要、パワートリックと同一パターン）
- `src/jpoke/types/move.py`: `MoveName` Literal に `"パワーシフト"` を追加
  （`python scripts/generate_literals/generate_move_literal.py` を実行し、既存の自動生成テスト
  `test_generateliteralスクリプト_再実行しても差分が出ない` と整合する正規の並び順に更新した）
- 動作確認: `MOVES["パワーシフト"]` の `type/category/pp/power/accuracy/priority/crit_ratio/target/exist`
  が期待通り（`ノーマル/status/12/None/None/0/0/self/True`）に設定されることを確認した

## リーサル計算

- 対象外（変化技であり、いずれの `LethalEvent` にも該当しない。パワートリック側もリーサル実装なし
  であり、`.internal/progress/move.md` の `リーサル実装`/`リーサルテスト` 列は `n/a` のまま）

## テスト

- `tests/moves_status/test_move_ha.py` に以下を新規追加（パワートリックの既存テストに準拠し、
  加えて技名を跨いだ入れ替え操作の共有を確認するテストを追加）:
  - `test_パワーシフト_2回使用で元の値に戻る`
  - `test_パワーシフト_PPは12`
  - `test_パワーシフト_タイプはノーマル`
  - `test_パワーシフト_こうげきとぼうぎょが入れ替わる`
  - `test_パワーシフト_パワートリックと入れ替え操作を共有する`
    （パワーシフト→パワートリックの順で使用すると実数値が元に戻ることを確認。ハンドラ関数共有の
    妥当性を検証する固有テスト）
  - `test_パワーシフト_ランクは変化しない`
  - `test_パワーシフト_対象はself`
  - `test_パワーシフト_相手の実数値は変化しない`
- `python -m pytest tests/ -q` を実行し、6155 passed / 1 failed / 1 skipped を確認した。
  唯一の失敗 `test_py_examples_エラーなく実行できる[01_getting_started/05_cli_vs_ai.py]` は
  `CLIPlayer` が対話的に標準入力を要求するサンプルスクリプトであり、本レビュー作業環境（非対話的な
  サブプロセス実行、stdin 未提供）で `EOFError` により毎回（3回連続で）決定的に失敗することを
  確認した。今回の変更（`パワーシフト`関連ファイルのみ）とは無関係で、間欠的失敗ではなく常に
  同一原因で失敗するため CLAUDE.md/`.claude/loop/_common.md` §共通13 が定義する「flaky」の対象外
  （同節: 「毎回失敗する場合は flaky ではなく通常のデグレ／バグなので対象外」）と判断し、
  本タスクの範囲外として修正は行わなかった
