# まほうのこな レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/まほうのこな.md`）
- 一次情報（ポケモンWiki）をもとに、基本情報（タイプ/分類/PP/命中率/範囲/優先度/直接攻撃/粉技フラグ）、
  効果、失敗条件（既にエスパー単タイプ／テラスタル中／アルセウス・シルヴァディ）、粉技としての無効化、
  みがわり・マジックコート・まもるとの関係、タイプ変化の実装方式（みずびたしと同一パターン）をまとめた

## 実装計画書
- 新規作成（`.internal/plan/moves/まほうのこな.md`）
- ハンドラ構成・priority根拠（`.internal/spec/turn.md` の `Event.ON_APPLY_MOVE`＝実装上の
  `ON_BEFORE_APPLY_MOVE` テーブルの「みずびたし等: すでに該当タイプ」priority=100 を根拠に採用）、
  実装コード例、アルセウス・シルヴァディの無効化を特性ではなく種族名で判定する理由、
  粉技無効化はコア共通ルールに委譲する旨を明記した

## 実装（handlers/ / data/）
- `data/moves/move_ma.py`: 「まほうのこな」エントリが `handlers={}`（コメント「追加効果なし」）のまま
  未実装だったバグを修正。`flags={"powder"}` を追加し、`ON_BEFORE_APPLY_MOVE` / `ON_STATUS_HIT`
  ハンドラを登録
- `handlers/move_status.py`: `まほうのこな_can_apply`（既にエスパー単タイプ／テラスタル中／
  アルセウス・シルヴァディなら失敗）、`まほうのこな_apply`（`apply_volatile_to_defender` で
  「まほうのこな」揮発性状態を付与）を新規実装
- `data/volatile.py`: 「まほうのこな」揮発性状態エントリを新規追加
  （`ON_VOLATILE_START` / `ON_VOLATILE_END`）
- `handlers/volatile.py`: `まほうのこな_set_type`（`volatile_override_type = "エスパー"` に設定し
  `added_types` をクリア）、`まほうのこな_clear_type`（`volatile_override_type = None` に戻す）を新規実装。
  みずびたしと全く同じ実装パターン
- `types/volatile.py`: `scripts/generate_literals/generate_volatile_literal.py` を実行し、
  `VolatileName` Literal に `"まほうのこな"` を反映

## リーサル計算
- 対象外（状態異常を与えない変化技のためダメージ計算に関与しない。進捗表の「リーサル実装」
  「リーサルテスト」列は `n/a` とした）

## テスト
- `tests/moves_status/test_move_ma.py` に以下を追加
  - `test_まほうのこな_エスパー単タイプに変える`（複合タイプの相手がエスパー単タイプになる）
  - `test_まほうのこな_すでにエスパー単タイプなら失敗`
  - `test_まほうのこな_テラスタル中の相手には失敗する`
  - `test_まほうのこな_アルセウスには無効`
  - `test_まほうのこな_シルヴァディには無効`
  - `test_まほうのこな_くさタイプの相手には粉技として無効`
  - `test_まほうのこな_特性ぼうじんを持つ相手には無効`
  - `test_まほうのこな_道具ぼうじんゴーグルを持つ相手には無効`
  - `test_まほうのこな_もりののろいでくさタイプになった相手には効かない`
  - `test_まほうのこな_ハロウィン後の相手に使うとaddedTypesがクリアされエスパーのみになる`
  - `test_まほうのこな_交代すると元のタイプに戻る`
- `python -m pytest tests/ -v` で全4423件（+1 skipped）がパスすることを確認
  （ログ: 作業ディレクトリ内 `.loop/test_logs/まほうのこな.log`。実行中に
  `test_てんねん_攻撃側は防御ランク補正を無視する` `test_オーロラベール_ひかりのかべとは重複しない`
  が確率依存で一時的に落ちる既存のflakyテストに遭遇したが、再実行で通過することを確認済みで
  本タスクの変更とは無関係）

## 進捗表の補足
- `.internal/progress/move.md` の対象行が仕様書・計画書・実装・テスト・レビューの全列で「ダブル専用」と
  誤って記載されていた。まほうのこなの範囲は「1体選択」（`自分以外の1体が対象のわざ` カテゴリ）で、
  同カテゴリを持つ実装済みの `みずびたし` もシングルバトルで問題なく機能するため、ダブル専用扱いは
  誤りと判断し `x` / `✓` に修正した
