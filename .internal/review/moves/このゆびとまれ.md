# このゆびとまれ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/このゆびとまれ.md`）。.internal/wiki/moves/このゆびとまれ.html および
  src/jpoke/data/ps-champ-ja/moves.json L282 をもとに、タイプ・分類・PP・優先度（+3→+2の世代変遷）・
  対象・効果（ちゅうもくのまと状態の付与）・判定項目を記載。
  類似技いかりのこなとの相違点（このゆびとまれは粉・胞子技に分類されず、くさタイプ・
  特性ぼうじん・道具ぼうじんゴーグルによる無効化を受けない）を明記した。
  本プロジェクトはシングルバトル専用で `battle.actives` が常に2体以下であるため、
  ちゅうもくのまとによる対象リダイレクトはダブル/トリプルバトル専有の効果として対象外と判断。

## 実装計画書
- 新規作成（`.internal/plan/moves/このゆびとまれ.md`）。仕様書をもとに、いかりのこなの計画書
  （`.internal/plan/moves/いかりのこな.md`）と同じ方針で「ダブル専用のため対象外」と判断した根拠を記載。
  `.internal/spec/turn.md` の `Event.ON_TRY_MOVE_1`（priority=30、「このゆびとまれ: シングルバトルである」
  失敗条件）についても、本プロジェクトの構造的制約（`len(battle.actives) <= 2`）により
  自明で分岐のないハンドラになってしまうため未実装のまま据え置くことを明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ka.py` の `MoveData`（type="ノーマル", category="status",
  pp=20, priority=2, handlers={}）はPP（champions move_list.txt L282と一致）・優先度・タイプとも
  仕様と一致しており、いかりのこなと同様「追加効果なし」として妥当な実装であることを確認した。

## リーサル計算
- 対象外（進捗表の「リーサル実装」列は「ダブル専用」＝対象外の慣例表記。いかりのこなと同じ扱い）。

## テスト
- 変更なし。ダブル専用の効果でありシングルバトルでは影響を与えないため、いかりのこなと同様
  新規テストは追加しない（`tests/` にこのゆびとまれ関連のテストが存在しないことを確認済み）。
  `python -m pytest tests/ -v` を実行し、4148 passed, 1 skipped で全件パスを確認した
  （`.loop/test_logs/このゆびとまれ.log`）。
