# こなゆき レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/こなゆき.md`）。一次情報（`.internal/wiki/moves/こなゆき.html`）をもとに、
  タイプ（こおり）・分類（特殊）・威力（40）・命中率（100）・PP（25）・対象（相手全員／
  シングルバトルでは相手1体と同義）・優先度（0）・非接触・追加効果（10%こおり）・判定フラグ・
  Zワザ威力（100）を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/こなゆき.md`）。既存実装（`data/move.py` の `こなゆき`、
  `handlers/move_attack.py` の `こなゆき_apply_freeze_to_defender`）が仕様と一致していることを
  確認し、ハンドラ構成・priority根拠・エッジケース（ちからずくとの相互作用）を記録した。

## 実装（handlers/ / data/）
- 変更なし。既存実装は仕様（威力40・命中100・PP25・こおりタイプ・特殊・10%こおり付与）と
  完全に一致しており、`apply_ailment_to_defender` を用いた汎用パターンに沿っていたため修正不要。

## リーサル計算
- 対象外（`.internal/progress/move.md` の こなゆき 行はリーサル実装列が n/a）。

## テスト
- 変更なし。`tests/moves_attack/test_move_ka.py` の `test_こなゆき_こおりが発動する` が
  `secondary_chance=1.0` でこおり付与を確認しており、仕様と一致していたため追加修正は不要。
  `tests/test_item.py` 内の こなゆき を使ったこおりタイプ技のテスト（ゆきだま等）も影響なし。
