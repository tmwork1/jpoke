# ドわすれ レビュー結果

## 仕様書
- 新規作成（.internal/spec/moves/ドわすれ.md）。.internal/wiki/moves/ドわすれ.html を一次情報として、
  タイプ・分類・威力・命中率・PP・対象・優先度・直接攻撃有無・効果（自分のとくぼうを2段階上げる）・
  各種判定フラグ（まもる/マジックコート/よこどり/オウムがえし）を記載した。

## 実装計画書
- 新規作成（.internal/plan/moves/ドわすれ.md）。同系統の自己バフ技（つるぎのまい・りゅうのまい）の
  計画書を踏襲し、ハンドラ構成・priority根拠・実装箇所・注意点・リーサル計算対象外の旨を記載した。

## 実装（handlers/ / data/）
- `handlers/move_status.py` の `ドわすれ_modify_attacker_stats`（`modify_attacker_stats(..., stats={"spd": 2})`）
  は仕様通りで変更なし。
- `data/moves/move_ta.py` の `MoveData` に `target="self"` が指定されていないバグを発見し修正した。
  `target` を省略するとデフォルトの `"foe"` になり、`is_blocked_by_protect` / `is_reflectable` の
  判定が誤って「相手を対象とする技」として扱われてしまう（まもる・マジックコートで誤反応する）ため、
  同系統の `つるぎのまい` / `りゅうのまい` と同様に `target="self"` を明記した。

## リーサル計算
- 対象外（.internal/progress/move.md の該当行は `n/a`）。ダメージを与えない自己バフ技のため
  リーサル計算ハンドラは不要。

## テスト
- `tests/moves_status/test_move_ta.py` に以下を新規追加した:
  - `test_ドわすれ_とくぼう2段階上がる`
  - `test_ドわすれ_自分対象のためまもるで防がれない`
  - `test_ドわすれ_マジックコートで跳ね返されない`
  （後者2件は `target="self"` バグ修正により新たに正しく通るようになったことを検証するテスト）
- `python -m pytest tests/ -v` を実行し、4560 passed, 1 skipped で全件パスを確認した
  （ログ: `.loop/test_logs/ドわすれ.log`）。
