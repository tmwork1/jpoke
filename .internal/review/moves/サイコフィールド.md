# サイコフィールド レビュー結果

## 仕様書
- `.internal/spec/moves/サイコフィールド.md` を新規作成した（技本体の基本情報・判定・簡略化事項）。
  サイコフィールドの継続効果（威力補正・先制技無効化等）は既存の `.internal/spec/fields/サイコフィールド.md`
  を参照する形にした（内容は一次情報 `.internal/wiki/moves/サイコフィールド.html` と照合済みで誤りなし。
  PP=12・第八世代以降1.3倍等は `src/jpoke/data/ps-champ-ja/moves.json` の記載と一致）

## 実装計画書
- `.internal/plan/moves/サイコフィールド.md` が `global_manager` / `data/move.py`（分割前）を前提にした
  旧アーキテクチャの内容のまま放置されていたため、現行実装（`terrain_manager`, `data/moves/move_sa.py`,
  `category="status"`, PP=12）に合わせて全面的に書き直した
- `.internal/plan/fields/サイコフィールド.md` は本タスクの対象外（フィールド効果本体は別タスクで管理）

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_sa.py` の `"サイコフィールド"` エントリ（PP=12・`category="status"`・
  `target="field"`・`ON_STATUS_HIT` で `terrain_manager.apply` を呼ぶハンドラ登録）は仕様・計画と
  完全に一致しており変更なし
- `handlers/move_status.py` の `サイコフィールド_activate_terrain` は
  `エレキフィールド_activate_terrain` と同一パターンで実装済み、変更なし

## リーサル計算
- 対象外（`.internal/progress/move.md` の当該行は `n/a`。サイコフィールド自体は直接ダメージ・回復を
  発生させないステータス変化技のため）

## テスト
- 技本体（`ON_STATUS_HIT` での地形発動）を直接検証するテストが `tests/moves_status/test_move_sa.py`
  に存在しなかったため、以下を追加した:
  - `test_サイコフィールド_地形がサイコフィールドになる`: 使用後に `battle.terrain.name` が
    `"サイコフィールド"` になり、`count == 5` になることを確認
  - `test_サイコフィールド_すでに同じ地形なら失敗`: すでにサイコフィールドが有効な場合、
    再使用してもカウントが変化しない（再設置されない）ことを確認
- 地形自体の効果（先制技無効化・威力補正など）を検証する `tests/test_field.py` 等の既存テストは
  実装と一致しており変更なし
- `python -m pytest tests/ -v` を実行し、5811 passed, 1 skipped で全件成功を確認
