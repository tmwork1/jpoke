# ねばねばネット レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ねばねばネット.md`）
- `.internal/wiki/moves/ねばねばネット.html`・`src/jpoke/data/ps-champ-ja/moves.json`（648〜649行目）を一次情報として、
  タイプ・分類・PP・対象・判定フラグ（まもる/みがわり/マジックコート等）を記載
- 状態そのものの詳細仕様（免疫・特性相互作用・消滅条件）は既存の `.internal/spec/fields/ねばねばネット.md`
  を参照する形にし、技側の責務（相手の場に設置すること）のみを本仕様書に記載

## 実装計画書
- 既存の `.internal/plan/moves/ねばねばネット.md` を仕様書・実装と照合して修正
  - 「現状の実装状況」表が `handlers/move_status.py` 関数を「未実装」としていたが、実際には実装済みだったため修正
  - コード例の `ctx` の型が `EventContext` になっていたが、技ハンドラは常に `AttackContext` を受け取るため修正
  - Priority根拠が `.internal/spec/turn.md` の `Event.ON_APPLY_MOVE`「設置技: すでに最大」を誤って引用していたため、
    `Event.ON_STATUS_HIT` は turn.md に掲載されていない技内部イベントであり、同種の
    `ステルスロック_set_field`・`どくびし_set_field` と同じデフォルト priority（100）を使う、という正しい根拠に修正
  - `data/move.py` への登録として記載されていたが、実際は `data/moves/move_na.py` に既に登録済みだったため修正
  - テスト計画を追記（既存テストを列挙し、フィールド側のテストは `.internal/plan/fields/ねばねばネット.md` を参照するよう明記）

## 実装（handlers/ / data/）
- 変更なし
- `src/jpoke/handlers/move_status.py` の `ねばねばネット_set_side_field` は仕様書・計画書と一致している
  - `ctx.defender` の陣営（相手側）に `side.activate("ねばねばネット", 1)` で設置
  - すでに設置済みの場合は `HandlerReturn(value=False, stop_event=True)` で失敗
  - 五十音順の配置（`ねごと_suppress_pp` の後、`ねむりごな_apply_ailment_to_defender` の前）も正しい
- `src/jpoke/data/moves/move_na.py` の `"ねばねばネット"` エントリは type="むし"、category="status"、pp=20、
  target="foe_side" で `src/jpoke/data/ps-champ-ja/moves.json` と一致している
- 五十音順（`data/moves/move_na.py` 内の定義順）も正しい

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が n/a。変化技のためダメージ計算対象外）

## テスト
- 変更なし
- `tests/moves_status/test_move_na.py` の `test_ねばねばネット_相手陣営に設置される`・
  `test_ねばねばネット_すでに設置済みなら失敗` が技側の責務（設置・重複失敗）を十分にカバーしている
- すばやさ低下自体の免疫・特性相互作用テストは `tests/test_field.py` 側（フィールドカテゴリのレビューで実施済み）
- `python -m pytest tests/ -v` で全4306件（1件skip）パスを確認（`.loop/test_logs/ねばねばネット.log`）
