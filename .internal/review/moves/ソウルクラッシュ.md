# ソウルクラッシュ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ソウルクラッシュ.md`）
- ポケモンWiki一次情報（.internal/wiki/moves/ソウルクラッシュ.html）に基づき、フェアリー・物理・威力75・
  命中100・PP15・接触あり・優先度0・追加効果「100%の確率で相手のとくこうを1段階下げる」を記載
- PPについては `src/jpoke/data/ps-champ-ja/moves.json` に該当行が存在せず、`changes_from_sv.md` にも
  本技の変更記載がないため、第9世代基準値（15）をそのまま採用する旨を明記した

## 実装計画書
- 新規作成（`.internal/plan/moves/ソウルクラッシュ.md`）
- 既存実装（`data/moves/move_sa.py` / `handlers/move_attack.py`）を確認し、`flags` に
  `secondary_effect` が欠落しているバグを特定・記載した

## 実装（handlers/ / data/）
- `handlers/move_attack.py` の `ソウルクラッシュ_lower_spa_C` はロジック自体正しく、修正なし
- `data/moves/move_sa.py` の `"ソウルクラッシュ"` エントリの `flags` を `{"contact"}` から
  `{"contact", "secondary_effect"}` に修正
  - 同種の100%確率とくこう1段階下げ技（はいよるいちげき・むしのていこう）は共に
    `secondary_effect` フラグを持ち、`.internal/spec/moves/_move_secondary_1.md` にもソウルクラッシュ自身が
    追加効果一覧に掲載済みだったため、フラグ欠落によりちからずく特性（威力1.3倍化・追加効果無効化）が
    正しく機能しないバグと判断した

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」「リーサルテスト」は n/a）

## テスト
- `tests/moves_attack/test_move_sa.py` に以下を新規追加
  - `test_ソウルクラッシュ_secondary_effectフラグを持つ`
  - `test_ソウルクラッシュ_とくこう1段階低下が発動する`
  - `test_ソウルクラッシュ_ちからずくで追加効果が発動せず威力が1_3倍`
- 追加前はソウルクラッシュのテストが存在しなかった
- `python -m pytest tests/ -v` で全3845件成功（1件skip）を確認。フルスイート実行中に
  乱数依存の既存テスト（サイコブレイク・むしくい・くらいつく等）が偶発的に失敗する既知の問題を
  観測したが、いずれも単体実行で再現せず、本レビューの変更とは無関係と判断した
