# フェザーダンス レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/フェザーダンス.md`）。一次情報（ポケモンWiki）をもとに、タイプ・分類・PP・命中率・範囲・優先度・直接攻撃の有無・効果・判定（まもる/マジックコート/オウムがえし/よこどり/おうじゃのしるし）を記載。PPは `src/jpoke/data/ps-champ-ja/moves.json` 準拠の16とし、Gen9本家値（15）との差異を注記した。

## 実装計画書
- 新規作成（`.internal/plan/moves/フェザーダンス.md`）。既存の `あまえる` 計画書と同一パターン（`ON_STATUS_HIT` / `subject_spec="attacker:self"` / priority=100 / 共通ユーティリティ `modify_defender_stats` 利用）に倣って記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `フェザーダンス` の `pp` を `15` → `16` に修正（`src/jpoke/data/ps-champ-ja/moves.json` の値に合わせるバグ修正。Gen9本家値からの移行漏れ）。
- `handlers/move_status.py` の `フェザーダンス_modify_defender_stats`（`modify_defender_stats(..., stats={"atk": -2})`）はロジック・登録位置とも仕様書・計画書と一致しており、修正不要。
- `flags={"dance"}` はWikiの「おどりのわざ」カテゴリと一致しており妥当。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列は `n/a`。ダメージを与えない変化技のため）。

## テスト
- `tests/moves_status/test_move_ha.py` に以下を追加:
  - `test_フェザーダンス_PPは16`（PP=16の回帰テスト、上記のPP修正を検証）
  - `test_フェザーダンス_defenderのこうげきが2段階下がる`
  - `test_フェザーダンス_まもるで防がれる`
  - `test_フェザーダンス_マジックコートで跳ね返される`
- `python -m pytest tests/ -v` は 4614 passed, 1 skipped（既存のskipで無関係）で全件パス。ログは `.loop/test_logs/フェザーダンス.log` に保存。
