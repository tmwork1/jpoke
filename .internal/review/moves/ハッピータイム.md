# ハッピータイム レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ハッピータイム.md`）。一次情報（`.internal/wiki/moves/ハッピータイム.html`）
  を参照し、タイプ・分類・PP（30、`champions/move_list.txt` に記載なくPP標準化対象外の扱い。
  `おいわい` と同様）・範囲（味方の場）・効果（トレーナー戦後の賞金2倍、本プロジェクトは
  賞金を扱わないため戦闘中の効果なし）・判定（まもる/マジックコート/よこどり/オウムがえし
  すべて無効）を記載した。
- `.internal/spec/moves/よこどり.md`・`.internal/spec/moves/ねごと.md`・`.internal/spec/moves/さいはい.md`
  の既存記述を確認し、`ハッピータイム` が `おいわい`/`てをつなぐ` とは異なり
  ねごとで選択されうる・さいはいで指示できる点を仕様書に明記した。まねっこについても
  同様の理由でコピー可能とみなす方針を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ハッピータイム.md`）。`data/moves/move_ha.py` の
  `target` 未設定バグ（デフォルト `"foe"` になっており仕様の「範囲: 味方の場」と
  不一致）を指摘し、修正方針（`target="own_side"`、flagsは付与しない）を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `"ハッピータイム"` エントリに
  `target="own_side"` を追加した（従来はデフォルトの `"foe"` のままで、
  ダブルバトルで相手を選択できてしまう仕様不一致があった）。
- `handlers/move_status.py` の `_INSTRUCT_BLOCKED_MOVES`（さいはい指示不可技）に
  `ハッピータイム` が含まれていないこと、`ねごと_select_and_execute` の候補除外が
  `non_negoto` フラグ判定のみであることを確認し、いずれも仕様（選択・指示可能）と
  一致していたため変更なし。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- `tests/moves_status/test_move_ha.py` に以下を追加:
  - `test_ハッピータイム_使用してもHPやランクなど戦闘状態が変化しない`
  - `test_ハッピータイム_マジックコートで跳ね返されない`
  - `test_ハッピータイム_まもるで防がれない`
- `tests/moves_status/test_move_ma.py` に `test_まねっこ_ハッピータイムはコピーできる`
  を追加（`おいわい` はコピー不可という既存テストとの対比で、`non_copycat` フラグを
  持たない `ハッピータイム` はコピーに成功することを確認）。
- `tests/moves_status/test_move_yarawa.py` に `test_ねごと_ハッピータイムも候補になる`
  を追加（`non_negoto` フラグを持たないため候補になり選択・成功しうることを確認）。
- `python -m pytest tests/ -v` で全件パスを確認（`.loop/test_logs/ハッピータイム.log`）。
  実行時に無関係な既存テスト（`くらいつく`・`むしくい` 関連、確率依存で非決定的）が
  たまたま失敗するケースがあったが、再実行で全件成功することを確認済み
  （本タスクの変更とは無関係な既存のflaky挙動）。
