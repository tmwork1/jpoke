# ちからをすいとる レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ちからをすいとる.md`）
- 一次情報（`.internal/wiki/moves/ちからをすいとる.html`）の「技の仕様」節を基に、回復量が
  「相手のこうげきの**ランク補正込みの実数値**」であること、ヘドロえき・ミラーアーマー・
  あまのじゃく・たんじゅん・てんねん・おおきなねっこ・かいふくふうじとの相互作用を明記した

## 実装計画書
- 既存の計画書（`.internal/plan/moves/ちからをすいとる.md`）を実際の実装内容に合わせて修正
  - 回復量の算出を `ctx.defender.stats["atk"]`（ランク未補正）から
    `ctx.defender.ranked_stats["atk"]`（ランク補正込み）に修正（一次情報と整合）
  - `Event.ON_CALC_DRAIN` 経由でおおきなねっこの1.3倍補正が乗る点を明記
  - `battle.modify_hp(..., reason="drain")` がヘドロえきの反転処理に必須である点を明記
  - PP を 10 → 12 に修正（`src/jpoke/data/ps-champ-ja/moves.json` 準拠）
  - あまのじゃく・ミラーアーマー・そうしょく・おうごんのからだ・かいふくふうじは
    汎用処理でカバーされるため個別対応不要である旨を明記

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_status.py` の `ちからをすいとる_apply`: `battle.modify_hp` 呼び出しに
  `reason="drain"` を追加。従来は理由指定がなく、ヘドロえき特性の反転処理
  （`ヘドロえき_reverse_drain` が `ctx.hp_change_reason == "drain"` を条件に発動）が
  効かないバグがあったため修正した
  （回復量の算出自体はすでに `ranked_stats["atk"]` を使用しており、この点は正しかった）
- `src/jpoke/data/moves/move_ta.py` の `"ちからをすいとる"` エントリ: `pp=10` を `pp=12` に修正
  （`src/jpoke/data/ps-champ-ja/moves.json` の PP 値は 12 であり、Gen9本家基準の10のままになっていた
  移行漏れのバグ）
- `subject_spec`（デフォルトの `attacker:self`）・ハンドラの並び順・登録内容はいずれも
  仕様と一致しており問題なし

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）

## テスト
- `tests/moves_status/test_move_ta.py` に
  `test_ちからをすいとる_ヘドロえきで回復がダメージに変換される` を追加し、
  `reason="drain"` 修正によりヘドロえきの反転処理が正しく発動することを確認した
- 既存の3件（クリアボディでの回復発動・こうげき-6での失敗・ランク補正込み回復量）は
  実装と整合しており変更なし
- `tests/items/test_item_a.py::test_おおきなねっこ_ちからをすいとるの回復量増加` は
  既存のまま変更なし（`ON_CALC_DRAIN` 経由の1.3倍補正を確認済み）
- `python -m pytest tests/ -v` を実行し、4219 passed / 1 skipped（既存のskip、本件と無関係）で
  全件成功を確認した（ログ: `.loop/test_logs/ちからをすいとる.log`）
