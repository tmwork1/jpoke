# いにしえのうた レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/いにしえのうた.md`）。
- `.internal/wiki/moves/いにしえのうた.html`（ポケモンWiki一次情報）と `src/jpoke/data/ps-champ-ja/moves.json` を照合。
  - 基本情報（ノーマル/特殊/威力75/命中100/PP10/音のわざ）と、10%の確率でねむり状態にする追加効果を記載。
  - 一次情報にあるメロエッタのフォルムチェンジ効果は、`src/jpoke/data/ps-champ-ja/moves.json` に本技自体が
    収録されていないため本プロジェクトの対象外と明記した。

## 実装計画書
- 新規作成（`.internal/plan/moves/いにしえのうた.md`）。
- 既存実装（`data/move.py` の `MoveData` エントリ、`handlers/move_attack.py` の
  `いにしえのうた_apply_sleep_to_defender`）をそのまま仕様書に合致するものとして記載。
  ハンドラ構成表・priority根拠・エッジケース（音のわざ、ちからずくとの相互作用、
  フォルムチェンジ対象外の理由）を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py`（`いにしえのうた` エントリ）・
  `src/jpoke/handlers/move_attack.py`（`いにしえのうた_apply_sleep_to_defender`）とも
  仕様書・計画書と一致しており、誤り・欠落は見つからなかった。
  - `flags={"sound", "secondary_effect"}` によりみがわり貫通・ちからずく対応は
    フレームワーク側で汎用的に処理されている。

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行で リーサル実装=n/a、リーサルテスト=n/a）。

## テスト
- 変更なし。`tests/moves_attack/test_move_a.py` の
  `test_いにしえのうた_ねむりが発動しない` / `test_いにしえのうた_ねむりが発動する` が
  実装と仕様書に一致しており、追加・修正は不要と判断した。
  - 音のわざによるみがわり貫通は `move_executor.py` のフラグ判定でフレームワーク共通に処理されており、
    他の音技（いびき等）にも技個別の貫通テストが存在しないため、本技でも追加しなかった。
- `python -m pytest tests/ -v` を実行し、3234 passed, 1 skipped で全テスト成功を確認
  （`.loop/test_logs/いにしえのうた.log`）。
  - 実行中に `test_field.py` のダメージ軽減系テストや `test_リフレッシュ_まひを治す` が
    一時的に失敗することがあったが、いずれもいにしえのうたと無関係な既存テストで、
    乱数シード固定なしに起因する再実行時の揺れ（再実行で全て成功）であり、本レビューの対象外。
