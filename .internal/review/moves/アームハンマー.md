# アームハンマー レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/アームハンマー.md`）。
- 一次情報（`.internal/wiki/moves/アームハンマー.html`）の基本情報（かくとう/物理/威力100/命中90/直接攻撃/パンチ技）・
  判定表（追加効果:×、まもる:○、おうじゃのしるし:○、マジックコート:×、よこどり:×、オウムがえし:○）を反映。
- PPは一次情報（第四世代）では10だが、`src/jpoke/data/ps-champ-ja/moves.json` に基づき12（Champions基準）と明記。
- `.internal/wiki/abilities/ちからずく.html` を参照し、自分自身のランクを下げる確定効果はちからずくの対象外である旨を明記（`いじげんラッシュ.md` と同じ判断）。

## 実装計画書
- 新規作成（`.internal/plan/moves/アームハンマー.md`）。
- 既存実装の誤り（PP・`secondary_effect`フラグ）を指摘し、修正方針を記載。
- ハンドラ構成（`Event.ON_HIT` / priority100デフォルト / `attacker:self`）を既存の同種実装（りゅうせいぐん・いじげんラッシュ等）と比較して妥当性を確認。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `アームハンマー` エントリを修正:
  - `pp=10` → `pp=12`（`src/jpoke/data/ps-champ-ja/moves.json` / `changes_from_sv.md` のPP統一ルールに準拠。SVまでの値10が残っていた移行漏れ）。
  - `flags={"contact", "punch", "secondary_effect"}` → `flags={"contact", "punch"}`。
    `.internal/wiki/abilities/ちからずく.html` によれば、ちからずくの対象技は「相手のランク補正を下げる技」
    「自分のランク補正を上げる技」等に限られ、「自分のランク補正を下げる技」は対象外であるため、
    誤って付与されていた `secondary_effect` フラグを削除した（`いじげんラッシュ` の既存実装と同じ扱いに統一）。
  - インデントの乱れ（5スペース/17スペース）も併せて修正。
- `handlers/move_attack.py` の `アームハンマー_lower_attacker_spe` は仕様と一致しており修正なし。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列は `n/a`）。

## テスト
- `tests/moves_attack/test_move_a.py` に以下を新規追加:
  - `test_アームハンマー_すばやさ1段階低下が発動する`: 命中後に確定で自分の『すばやさ』ランクが1段階下がることを確認。
  - `test_アームハンマー_ちからずくは無関係で発動する`: ちからずく所持時も威力上昇（`power_modifier`）が発生せず、
    すばやさランク低下は通常通り発動することを確認（`secondary_effect` フラグ削除の回帰確認）。
- 既存の `tests/items/test_item_ta.py`（だっしゅつパック）・`tests/abilities/test_ability__group.py`（かちき/まけんき）の
  アームハンマーを使ったテストは、本レビューの修正内容（PP・`secondary_effect`フラグ）と無関係のため変更なし。
- `python -m pytest tests/ -v` で全3660件パス（1件skip）。ログ: `.loop/test_logs/アームハンマー.log`。
