# オーバーヒート レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/オーバーヒート.md`）。一次情報（`.internal/wiki/moves/オーバーヒート.html`）
  をもとに、タイプ・分類・威力（第六世代以降130）・命中率90・PP・対象・判定フラグ
  （急所・命中判定・追加効果・まもる・おうじゃのしるし・マジックコート・よこどり・
  オウムがえし・ちからずく）・ダイマックス/Zワザ威力（参考情報として対象外明記）を記載した。
  PPは`src/jpoke/data/ps-champ-ja/moves.json:134`の値（8）を採用。

## 実装計画書
- 新規作成（`.internal/plan/moves/オーバーヒート.md`）。既存実装（`オーバーヒート_lower_attacker_spa`）
  が同系統の反動技`りゅうせいぐん_sharply_lower_spa_C`と同一パターンであることを確認し、
  `Event.ON_HIT`・デフォルトpriority（100）の妥当性を`.internal/plan/moves/攻撃技_攻撃側ランク変化系.md`
  の既存方針に基づき記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `オーバーヒート` エントリで `pp=5`（Gen9本家基準の
  移行漏れ）となっていたのを `pp=8`（`src/jpoke/data/ps-champ-ja/moves.json` の値）に修正した。
- `handlers/move_attack.py` の `オーバーヒート_lower_attacker_spa`、
  `handlers/lethal.py` の `オーバーヒート_lower_attacker_spa`（リーサル用）はいずれも
  仕様・既存パターン（りゅうせいぐん）と整合しており修正不要。

## リーサル計算
- 変更なし。`handlers/lethal.py` の `オーバーヒート_lower_attacker_spa` は
  `ctx.attacker.rank["spa"]` を確定で2段階下げる実装であり、仕様と一致している。

## テスト
- `tests/moves_attack/test_move_a.py` に `test_オーバーヒート_とくこう2段階低下が発動する` を
  新規追加（通常の技実行フローでのランク低下確認。従来は `tests/test_lethal.py` の
  `test_オーバーヒート_とくこうダウン` のみ存在し、moves_attack側のテストが欠落していた）。
- `python -m pytest tests/ -v` で全3693件（+1 skip）がパスすることを確認した。
