# でんこうそうげき レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/でんこうそうげき.md`）。
  - 一次情報（`.internal/wiki/moves/でんこうそうげき.html`）をもとに、基本情報・効果
    （でんきタイプでないと失敗／命中時に自分のでんきタイプを消失／テラスタルとの相互作用等）を記載。

## 実装計画書
- 新規作成（`.internal/plan/moves/でんこうそうげき.md`）。
  - `もえつきる`（ほのお・特殊版の同種専用技）の実装パターンを踏襲する方針とし、
    `ON_TRY_MOVE_1`（priority=10）と `ON_DAMAGE_HIT`（priority=180）のハンドラ構成、
    priority根拠（`.internal/spec/turn.md` の該当行）、エッジケースを明記。

## 実装（handlers/ / data/）
- 修正した。実装前は `data/move.py` に `handlers={}` として登録されており、
  「でんきタイプでない場合の失敗」と「命中後のでんきタイプ消失」のどちらも未実装だった
  （進捗表では実装済み `x` とされていたが実際は誤りだった）。
- `handlers/move_attack.py` に `でんこうそうげき_fail_if_no_electric_type`
  （`ON_TRY_MOVE_1`, priority=10, `attacker:self`）と
  `でんこうそうげき_remove_electric_type`（`ON_DAMAGE_HIT`, priority=180, `attacker:self`）を追加。
  `もえつきる_fail_if_no_fire_type` / `もえつきる_remove_fire_type` と同一パターン
  （こおり解凍要素は無いため実装しない）。
- `data/move.py` の `でんこうそうげき` エントリにハンドラ登録を追加。
- `.internal/spec/turn.md` の `Event.ON_TRY_MOVE_1` に
  「でんきタイプでないでんこうそうげきの失敗」を追記し、
  `Event.ON_DAMAGE` の「もえつきるによるタイプ消失」を
  「もえつきる・でんこうそうげきによるタイプ消失」に修正。
- `python scripts/sort_handlers.py src/jpoke/handlers/move_attack.py` と
  `python scripts/sort_data/sort_moves.py` を実行済み。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装・リーサルテスト列は `n/a`）。

## テスト
- `tests/moves_attack/test_move_ta.py` に3件追加:
  - でんきタイプでないポケモンが使うと失敗する
  - でんきタイプのポケモンが使うと成功し攻撃後にでんきタイプでなくなる
  - テラスタル（でんき）中でも成功するが、タイプ消失処理はテラスタル中は無視される
- `python scripts/sort_tests.py tests/moves_attack/test_move_ta.py` でソート、
  `python scripts/generate_test_list.py` でテスト一覧を更新。
- `python -m pytest tests/ -v` で全3446件パス（1件skip、既存の無関係なskip）。
  結果は `.loop/test_logs/でんこうそうげき.log` に保存。
