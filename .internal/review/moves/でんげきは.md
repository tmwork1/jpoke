# でんげきは レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/でんげきは.md`）。一次情報（`.internal/wiki/moves/でんげきは.html`）に基づき、
  タイプ（でんき）・分類（特殊）・威力（60）・命中（必中）・PP（20）・優先度（0）・接触なし・
  判定フラグ（急所あり、まもるで防がれる、おうじゃのしるし発動、オウムがえしコピー可）を記載。

## 実装計画書
- 新規作成（`.internal/plan/moves/でんげきは.md`）。`あてみなげ` と同様、`accuracy=None` を
  必中として扱う既存の core 実装（`move_executor.py`）を利用するため専用ハンドラは不要である旨を明記。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `MOVES["でんげきは"]` は
  `type="でんき"`, `category="special"`, `pp=20`, `power=60`, `accuracy=None`, `handlers={}`
  となっており、仕様書・計画書と一致していることを確認した。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）。追加効果のない固定威力・必中技のため
  専用のリーサルハンドラは不要。

## テスト
- `tests/moves_attack/test_move_ta.py` に以下を追加:
  - `test_でんげきは_相手の回避率が高くても必ず命中する`
  - `test_でんげきは_タイプと分類と威力とppが正しい`
- `python scripts/sort_tests.py tests/moves_attack/test_move_ta.py` でソート、
  `python scripts/generate_test_list.py` でテスト一覧を更新、
  `python -m pytest tests/ -v` で全3443件パス（1 skipped）を確認。
