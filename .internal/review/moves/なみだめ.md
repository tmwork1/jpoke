# なみだめ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/なみだめ.md`）。一次情報（`.internal/wiki/moves/なみだめ.html`）と
  `src/jpoke/data/ps-champ-ja/moves.json` を突き合わせ、こうげき・とくこうを1段階ずつ下げる効果、必中、
  まもる（ダイウォールを除く各種防御技）を無視して発動すること、みがわりは貫通しないこと
  （関連項目の「おたけび」の記述との対比で確認）を明記した。

## 実装計画書
- 新規作成（`.internal/plan/moves/なみだめ.md`）。`なかよくする` の計画書を参考に、
  `unprotectable` フラグのみを付与し `bypass_substitute` は付与しない方針を明記した。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_status.py` の `なみだめ_modify_defender_stats` が `spa` のみを
  下げており、`atk` の低下が欠落していたバグを修正（`stats={"atk": -1, "spa": -1}`）。
- `src/jpoke/data/moves/move_na.py` の `なみだめ` に `unprotectable` フラグが設定されておらず、
  まもる状態の相手にも通常通り防がれてしまう不具合を修正（`flags={"unprotectable"}` を追加）。
  併せて `accuracy=None`（必中）を明示的に指定した（既定値と同じだが、他の必中変化技との
  記述統一のため）。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`。ダメージを与えない変化技のため）。

## テスト
- `tests/moves_status/test_move_na.py` に以下を追加:
  - `test_なみだめ_こうげきととくこうが1段階ずつ下がる`
  - `test_なみだめ_まもる状態を無視してランクを下げる`
  - `test_なみだめ_みがわり状態の相手には防がれる`
- `python -m pytest tests/ -v` で全4297件パス（1件skip）を確認し、
  `.loop/test_logs/なみだめ.log` に保存した。
