# ジャングルヒール レビュー結果

## 発見事項（レビュー前の状態）

- `.internal/spec/moves/ジャングルヒール.md` / `.internal/plan/moves/ジャングルヒール.md` が未作成だった。
- `src/jpoke/data/moves/move_sa.py` の `"ジャングルヒール"` エントリに以下のバグがあった:
  - `target` が未指定でデフォルト値 `"foe"` のままだった。本技は自分を対象とする技のため、
    `まもる`・`おうごんのからだ`・マジックコート等の `move.target == "foe"` を前提とする判定に
    誤って合致し、防がれる/跳ね返される不具合が発生し得る状態だった。
  - `handlers={}`（コメント「追加効果なし」）となっており、回復・状態異常治療の効果が
    まったく実装されていなかった（技を撃ってもHPが変化しない状態だった）。
- `tests/` にジャングルヒール関連のテストが一件も存在しなかった。
- `.internal/progress/move.md` の該当行が仕様書・計画書・テスト・レビュー列すべて未着手（`-`）、
  効果列が `？` のままだった。

## 仕様書
- 新規作成（`.internal/spec/moves/ジャングルヒール.md`）。`.internal/wiki/moves/` に一次情報HTMLが
  存在しない（`.gitignore` により `.internal/wiki/` はリポジトリ管理外）ため、Gen9本家の技仕様
  （最大HPの1/4回復・状態異常治療・端数切り捨て・専用技ザルード）と、既にレビュー済みの
  `みかづきのいのり`・`いのちのしずく` の一次情報に記載された相互参照
  （「タイプとPPを除けば基本的にジャングルヒールと同様の効果」）を根拠に仕様を確定した。
  基本情報・効果・判定表・ヒーリングシフト/かいふくふうじとの相互作用を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ジャングルヒール.md`）。`みかづきのいのり`の計画書をテンプレートに、
  ハンドラ構成・priority根拠（`.internal/spec/turn.md`の「ジャングルヒール: HP満タン・状態異常無」を
  根拠に`ON_STATUS_HIT`priority=100を採用）・実装箇所・エッジケースを記載した。

## 実装（handlers/ / data/）
- `handlers/move_status.py` に `ジャングルヒール_apply` を新規実装した（`みかづきのいのり_apply`
  と同型。HPが満タンかつ状態異常もない場合に失敗し、それ以外は最大HPの1/4を切り捨てで回復して
  状態異常を治す）。五十音順に従い `じばそうさ_modify_attacker_stats` の後・`じゅうでん_apply` の
  前に配置し、`scripts/sort_handlers.py` で並びを確認した。
- `data/moves/move_sa.py` の `"ジャングルヒール"` エントリを修正:
  - `target="self"` を追加（バグ修正: 未設定によりデフォルト `"foe"` のままだった）
  - `handlers={Event.ON_STATUS_HIT: h.MoveHandler(hs.ジャングルヒール_apply)}` を追加
    （バグ修正: 効果が一切実装されていなかった）
  - `pp=10` は変更なし。`src/jpoke/data/ps-champ-ja/moves.json` に本技の行が存在しないため、README.md
    「対象範囲」の規定に従いGen9本家値（PP=10）を採用する妥当性を確認した。
  - `scripts/sort_data/sort_moves.py` を実行し、辞書順を確認した（順序に変更なし）。

## リーサル計算
- 対象外（`.internal/progress/move.md`のリーサル実装列をn/aに設定。回復技のためダメージ計算・
  リーサル計算とは無関係）。

## テスト
- `tests/moves_status/test_move_sa.py` に以下を新規追加（`みかづきのいのり`・`いのちのしずく`の
  既存テストをテンプレートに、`じこさいせい`のまもる/マジックコートテストも参考にした）:
  - `test_ジャングルヒール_HPが4分の1回復する`
  - `test_ジャングルヒール_HP満タンかつ無状態異常だと失敗する`
  - `test_ジャングルヒール_HP満タンでも状態異常があれば成功する`
  - `test_ジャングルヒール_状態異常を治す`（どく/まひ/やけどをパラメトライズ）
  - `test_ジャングルヒール_かいふくふうじ中は回復が無効化される`
  - `test_ジャングルヒール_まもるで防がれない`
  - `test_ジャングルヒール_マジックコートで跳ね返されない`
  （`まもる`・`マジックコート`のテストは `target="self"` バグ修正の回帰テストを兼ねる）
- `scripts/sort_tests.py tests/moves_status/test_move_sa.py` で並び替え、
  `scripts/generate_test_list.py` でテスト一覧を更新した。
- `python -m pytest tests/ -q` で全件（4486件）成功を確認した。

## 進捗表更新
- `.internal/progress/move.md` の該当行を以下のように更新:
  仕様書=✓, 計画書=✓, 実装=✓, テスト=✓, リーサル実装=n/a, リーサルテスト=n/a, レビュー=✓,
  分類=変化, タイプ=くさ, 威力=-, 命中率=-, PP=10, 対象=attacker,
  効果=「自分のHPを最大HPの1/4回復し、状態異常を治す（端数切り捨て）。HPが満タンかつ状態異常も
  ない場合は失敗する。」
