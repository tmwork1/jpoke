# きしかいせい レビュー結果

## 仕様書
- 新規作成した（`.internal/spec/moves/きしかいせい.md`）。
  一次情報（`.internal/wiki/moves/きしかいせい.html`）の技の仕様節に基づき、
  X = floor(残りHP × 48 / 最大HP) と6段階の威力表（20/40/80/100/150/200）を明記した。
- 併せて `.internal/spec/moves/_variable_power.md` 第4節「使用者HP低下依存威力技」の威力段階表が
  誤り（60/80/100/150 の4段階のみで最上位の200が欠落し、値もずれていた）だったため、
  正しい6段階表（20/40/80/100/150/200）に修正した。

## 実装計画書
- 新規作成した（`.internal/plan/moves/きしかいせい.md`）。
  既存実装（`きしかいせい_calc_power` / `_hp_low_to_power` 共通関数）を仕様書と照合した結果を記載し、
  `Event.ON_CALC_POWER_MODIFIER` のpriorityがデフォルト（100）である根拠
  （`.internal/spec/turn.md` に未掲載のため、既存の同種ハンドラの慣例に合わせた）を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/move_attack.py` の `きしかいせい_calc_power`（および
  じたばたと共用の `_hp_low_to_power`）と `src/jpoke/data/move.py` の `"きしかいせい"` エントリ
  （type="かくとう", category="physical", pp=15, power=1(可変のためプレースホルダ), accuracy=100,
  flags={"contact"}）は、いずれも一次情報・仕様書と一致しており修正不要と判断した。

## リーサル計算
- 対象外。`.internal/progress/move.md` のリーサル実装・リーサルテスト列は「n/a」。
  威力が使用者自身のHPに依存するのみで、相手のHP分布（`StateDist`）を直接左右する要素ではないため、
  専用のリーサルハンドラは不要と判断した。

## テスト
- 既存の `tests/moves_attack/test_move_ka.py` 内の6件の威力段階テストを実装と照合し、
  計算式・期待値ともに正しいことを確認した。
- `test_きしかいせい_HP約10パーセントのとき威力100` の docstring に、実際のテストコード
  （`hp=26 → x=5 → 威力100`）と矛盾する記述（`hp=24 → x=4 → 威力150`）が残っていたため修正した。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ka.py` でソート済み。
- `python scripts/generate_test_list.py` で `.internal/tests/moves_attack.md` を更新済み。
- `python -m pytest tests/ -v` を実行し、3293 passed, 1 skipped（既存のskipで無関係）で
  全件成功を確認した（ログ: `.loop/test_logs/きしかいせい.log`）。

## 進捗表 (`.internal/progress/move.md`)
- 仕様書・計画書列を新規作成に伴い `x` に更新した。
- PP列が誤って `16` と記載されていたため、一次情報・実装（PP=15）に合わせて `15` に修正した。
- レビュー列を `-` から `✓` に更新した。
- ついでに `じたばた` 行のPP列も同様の誤り（`16`）があったため `15` に修正した
  （じたばたは今回のレビュー対象外だが、作業中に気づいた誤りとして修正）。
