# ワイドブレイカー レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ワイドブレイカー.md`）。
  一次情報（`.internal/wiki/moves/ワイドブレイカー.html`）と`.internal/spec/moves/_move_secondary_1.md`
  （追加効果一覧・掲載済み）を突き合わせ、基本情報・効果・判定フラグ・ちからずくとの相互作用
  ・PP根拠（`src/jpoke/data/ps-champ-ja/moves.json`＝16）をまとめた。

## 実装計画書
- 新規作成（`.internal/plan/moves/ワイドブレイカー.md`）。
  同系統の `らいめいげり` 計画書を踏襲し、既存実装の誤り2点（PP・secondary_effectフラグ欠落）
  を明記した修正計画とした。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_wa.py` の `ワイドブレイカー` エントリを修正：
  - `pp=15` → `pp=16`（`src/jpoke/data/ps-champ-ja/moves.json` 準拠。Gen9本家値のまま移行されていない
    バグだった）
  - `flags={"contact"}` → `flags={"contact", "secondary_effect"}`
    （ちからずく特性による威力1.3倍化・追加効果無効化の対象から漏れていたバグ。
    同じ「100%相手ランク低下」パターンの `うらみつらみ` `ほのおのムチ` `らいめいげり`
    `りんごさん` は全て `secondary_effect` を保持しており、本技も対象とすべきだった）
- `handlers/move_attack.py` の `ワイドブレイカー_lower_defender_atk` は既存実装のまま変更なし
  （`modify_defender_stats` を使用しており正規のAPI経由で仕様と一致）。
- sort_handlers.py / sort_data/*.py はマージ後に一括実行するため今回は未実行。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列は `n/a`。確率発動の相手ランク低下効果で
  ダメージ量自体を変えないため）。

## テスト
- `tests/moves_attack/test_move_yarawa.py` に以下を追加：
  - `test_ワイドブレイカー_PPは16`
  - `test_ワイドブレイカー_こうげき1段階低下が発動する`
  - `test_ワイドブレイカー_ちからずくで威力上昇しこうげき低下は発動しない`
- sort_tests.py / generate_test_list.py はマージ後に一括実行するため今回は未実行。
- `python -m pytest tests/ -v` で全件パス（4036 passed, 1 skipped）。
  ログ: `.loop/test_logs/ワイドブレイカー.log`
  （初回実行時、`tests/test_field.py` の乱数シード未固定テスト5件が偶発的な急所発生により
  失敗したが、本エントリの変更とは無関係の既存の flaky テストであることを再実行で確認済み。
  再実行では全件パス）。

## 進捗表
- `.internal/progress/move.md` の該当行を更新：仕様書=x、計画書=x、テスト=x、レビュー=✓、
  PP=16（`?`から修正）、対象=defender（`attacker`の誤りを修正。相手のこうげきを下げる
  効果のため他の同系統技と同様 `defender` が正しい）。
