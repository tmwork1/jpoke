# いわなだれ レビュー結果

## 仕様書
- 変更なし。個別の仕様書ファイルは存在しないが、標準的な追加効果（確率発動のひるみ付与）は
  `.internal/spec/moves/_move_secondary_1.md` の一覧表（162行目）に既に記載されており、
  一次情報（`.internal/wiki/moves/いわなだれ.html`）と照合した結果、内容は正しい
  （いわ / 物理 / 威力75 / 命中90 / PP10 / 対象1体（相手全員技だがシングル戦のためdefender固定）/
  追加効果30%でひるみ / 直接攻撃×）。
- 一次情報では「おうじゃのしるし」「するどいキバ」の追加ひるみ効果が本技には重複しない
  （元々ひるみの追加効果を持つ技のため）ことも確認した。

## 実装計画書
- 変更なし。個別の計画書は存在しないが、同種の「確率でひるみを付与する」技
  （アイアンヘッド・かみつく・ゴッドバード等）にも個別計画書は作成されておらず、
  共通ヘルパー `apply_volatile_to_defender` を使う定型実装のため計画書省略は既存方針と整合している。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `いわなだれ` エントリ
  （type=いわ, category=physical, pp=10, power=75, accuracy=90, flags={"secondary_effect"},
  `Event.ON_DAMAGE_HIT` に `いわなだれ_apply_flinch` を登録）は仕様と一致。
  `src/jpoke/handlers/move_attack.py` の `いわなだれ_apply_flinch` は
  `apply_volatile_to_defender(battle, ctx, value, volatile="ひるみ", chance=0.3)` を呼び出しており、
  30%の確率で相手を『ひるみ』状態にする仕様と一致する。五十音順の位置も既に正しいため
  `sort_handlers.py` / `sort_moves.py` の実行差分はなし（実行不要と判断）。
- `src/jpoke/handlers/item.py` の `_INNATE_FLINCH_MOVES` に `いわなだれ` が含まれており、
  おうじゃのしるし・するどいキバの追加ひるみ効果が重複しない仕様も正しく実装済み
  （`tests/test_item.py` の該当パラメータ化テストで一般的に検証済み）。

## リーサル計算
- 対象外。`.internal/progress/move.md` のリーサル実装・リーサルテスト列がともに `n/a`。

## テスト
- 変更なし。`tests/moves_attack/test_move_a.py` の `test_いわなだれ_ひるみが発動する`
  （secondary_chance=1.0 でひるみ付与を確認）は既存実装と一致しており修正不要。
- `python -m pytest tests/ -v` で全3242件パス（1件skip）を確認
  （ログ: `.loop/test_logs/いわなだれ.log`）。

## その他
- `.internal/progress/move.md` の該当行で PP列が `12`（誤り）になっていたため一次情報に基づき `10` に修正し、
  効果列の `？` を「30%の確率で相手を『ひるみ』状態にする。」に補完、レビュー列を `✓` に更新した。
