# だいちのちから レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/だいちのちから.md`）。
  一次情報（`.internal/wiki/moves/だいちのちから.html`）より以下を確認して記載:
  タイプ=じめん / 分類=特殊 / 威力=90 / 命中=100 / PP=10 / 対象=相手1体 / 優先度=0 / 接触なし。
  追加効果: 10%の確率で相手の『とくぼう』ランクを1段階下げる。

## 実装計画書
- 新規作成（`.internal/plan/moves/だいちのちから.md`）。
  既存実装（`data/move.py` / `handlers/move_attack.py`）が仕様書と一致していることを確認し、
  ハンドラ構成表・Priority根拠・注意点を記載した（実装コードの変更は不要）。

## 実装（handlers/ / data/）
- 変更なし。`ha.だいちのちから_lower_defender_spd`（`handlers/move_attack.py`）が
  `modify_defender_stats(battle, ctx, value, stats={"spd": -1}, chance=0.1)` を呼ぶ実装で、
  `data/move.py` の `MoveData`（type=じめん, category=special, pp=10, power=90, accuracy=100,
  flags={"secondary_effect"}）ともに仕様と一致しており修正不要だった。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- 既存テスト `test_だいちのちから_とくぼう1段階低下が発動する`（`tests/moves_attack/test_move_ta.py`）
  は仕様通りで問題なし。
- 不足していた以下2件を追加した:
  - `test_だいちのちから_とくぼう1段階低下が発動しない`（`secondary_chance=0.0` で発動しないことを確認）
  - `test_だいちのちから_相手にダメージを与える`（`secondary_chance=0.0` で通常ダメージが入ることを確認）
- `python scripts/sort_tests.py tests/moves_attack/test_move_ta.py` でソート、
  `python scripts/generate_test_list.py` で `.internal/tests/` を更新、
  `python -m pytest tests/ -v` で全件パスを確認した（3423 passed, 0 failed, 1 skipped）。

## その他
- `.internal/progress/move.md` の だいちのちから 行の PP 列が `12` と誤っていたため、
  一次情報・実装（PP=10）に合わせて `10` に修正した。
- 同行の 仕様書・計画書 列を `x` に、レビュー列を `✓` に更新した。
