# パワフルエッジ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/パワフルエッジ.md`）。基本情報（いわ・物理・威力95・命中100・PP5・
  対象1体・優先度0・直接攻撃）、「まもる」無視の仕様（`ハイパードリル` と同種）、判定フラグを
  一次情報（`.internal/wiki/moves/パワフルエッジ.html`）から記載した。
- 急所ランク補正について、Wikiの判定表に「急所: ○」のみが記載され「急所に当たりやすい」の
  注記が無いこと、`ストーンエッジ` 側の備考に「SVで急所補正を持ついわ技は `ツタこんぼう` のみ」
  との記載があることを確認し、パワフルエッジには急所ランク補正が無い旨を明記した。

## 実装計画書
- 新規作成（`.internal/plan/moves/パワフルエッジ.md`）。`ハイパードリル.md` の構成に倣い、
  `unprotectable` フラグの共通実装（`Move.is_blocked_by_protect` / `_run_protect` /
  `_check_protect_success`）と `slash` フラグ（きれあじによる威力1.5倍）の根拠を明記した。
  急所ランク補正が無いため `critical_rank` を指定しない点も明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ha.py` の `パワフルエッジ` エントリ
  （`flags={"contact", "unprotectable", "slash"}`、`handlers={}`、`critical_rank` 未指定）は
  仕様書・計画書と一致しており、誤りは見つからなかった。
- 急所ランク補正について一次情報を精査したが、パワフルエッジに急所ランク補正は無く、
  `critical_rank` を指定しない現在の実装が正しいことを確認した（`ストーンエッジ` とは異なり
  `critical_rank=1` は付与すべきでない）。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」列は `n/a`。追加効果のない通常の
  物理攻撃技のため専用のリーサル計算ハンドラは不要）。

## テスト
- 変更なし。`tests/moves_attack/test_move_ha.py` の既存3テスト
  （きれあじによる威力1.5倍・まもる中でもダメージが入る・まもる状態が解除されない）が
  仕様と一致しており、過不足は見つからなかった。
- `python -m pytest tests/ -v` で全3963件パス（1件skip）を確認した
  （`.loop/test_logs/パワフルエッジ.log`）。
