# スマートホーン レビュー結果

## 仕様書
- 新規作成（.internal/spec/moves/スマートホーン.md）。.internal/wiki/moves/スマートホーン.html を一次情報として参照。
  タイプ・分類・威力・命中（必中）・PP・対象・優先度・接触・判定フラグを記載。

## 実装計画書
- 新規作成（.internal/plan/moves/スマートホーン.md）。専用ハンドラ不要のデータ定義のみで完結する旨と、
  PPをチャンピオンズ基準（12）にする根拠を明記。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_sa.py` の `MOVES_SA["スマートホーン"]` を修正:
  - `pp=10`（本家Gen9基準）→ `pp=12`（src/jpoke/data/ps-champ-ja/moves.json 準拠。移行漏れバグを修正）
  - `accuracy` が未指定だった（デフォルトNoneで機能上は必中として動作していたが、
    必中技であることが読み取れなかった）→ `accuracy=None,  # 必中` を明記し、
    他の必中技（スピードスター等）と同様の記述に統一
  - `flags={"contact"}` は既存のまま変更なし（一次情報の「直接攻撃：○」と一致）

## リーサル計算
- 対象外（.internal/progress/move.md のリーサル実装列が n/a のため）。

## テスト
- `tests/moves_attack/test_move_sa.py` に以下を追加:
  - `test_スマートホーン_はがねタイプの物理接触技である`: タイプ・分類・威力・contactフラグを確認
  - `test_スマートホーン_相手の回避率が高くても必ず命中する`: 相手の回避率を+6しても命中することを確認
- `python -m pytest tests/ -v` で全3831件PASS（1件skip、無関係）を確認。
  ログ: C:\Users\tmtmp\Documents\pokemon\jpoke\.loop\test_logs\スマートホーン.log
