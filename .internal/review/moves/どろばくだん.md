# どろばくだん レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/どろばくだん.md`）。
- 一次情報（`.internal/wiki/moves/どろばくだん.html`）を参照し、タイプ=じめん・分類=特殊・威力65・
  命中85・PP10・優先度0・非接触・追加効果（30%で相手の命中率を1段階下げる）・`bullet`（弾のわざ）
  フラグ対象であることを確認して記載。
- 類似技 `.internal/spec/moves/だくりゅう.md`（同種の命中率低下追加効果を持つ技）の構成を踏襲した。

## 実装計画書
- 新規作成（`.internal/plan/moves/どろばくだん.md`）。
- 既存実装（`data/move.py` / `handlers/move_attack.py` の `どろばくだん_lower_acc`）が仕様と
  完全に一致していることを確認し、その根拠を記載（変更は発生していない）。

## 実装（handlers/ / data/）
- 変更なし。
- `src/jpoke/data/move.py` の `"どろばくだん"` エントリ（`type="じめん", category="special",
  pp=10, power=65, accuracy=85, flags={"bullet", "secondary_effect"}`）は仕様通り。
- `src/jpoke/handlers/move_attack.py` の `どろばくだん_lower_acc`
  （`modify_defender_stats(battle, ctx, value, stats={"accuracy": -1}, chance=0.3)`）は
  30%発動・命中率-1段階の仕様と一致。`Event.ON_DAMAGE_HIT` での発火もまもる・ぼうだん等での
  無効化時に追加効果が発生しない挙動を自然に満たしており問題なし。
- `handlers/move_attack.py` 内の並び（どろかけ → どろばくだん → どろぼう）は五十音順で
  既に正しい。`data/move.py` 内の並びも五十音順で既に正しい。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」列は `n/a`。命中率ランク低下のみで
  HP分布に影響しない追加効果のためリーサル計算ハンドラは不要）。

## テスト
- 変更なし。
- `tests/moves_attack/test_move_ta.py` の `test_どろばくだん_命中率1段階低下が発動する` は
  `secondary_chance=1.0` で確率を強制した上で命中率ランクが-1になることを確認しており、
  既存の同種テスト（`どろかけ`・`ナイトバースト` 等）と同じパターンで妥当。追加・修正不要。
- `python -m pytest tests/ -v` を実行し、3496 passed, 1 skipped で全件成功を確認
  （ログ: `.loop/test_logs/どろばくだん.log`）。
