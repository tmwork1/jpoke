# いやなおと レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/いやなおと.md`）
- 一次情報（ポケモンWiki）をもとに、タイプ・分類・命中率85・優先度0・直接攻撃×・音のわざフラグ・
  効果（相手の『ぼうぎょ』を2段階下げる）・判定フラグ（まもるで防がれる、みがわり貫通、
  おうじゃのしるし対象外、マジックコートで反射、よこどり不可、オウムがえしでコピー可）を記載
- PPについては一次情報のPP40（Gen9本家基準）ではなく、`src/jpoke/data/ps-champ-ja/moves.json` の
  PP20（ポケモンチャンピオンズの全技PP統一ルール）を採用する旨を明記

## 実装計画書
- 新規作成（`.internal/plan/moves/いやなおと.md`）
- `ON_STATUS_HIT` の subject_spec・priority を同種のランクダウン変化技（あまえる・うそなき等）と
  揃えて明記し、既存共通関数 `modify_defender_stats` を利用する方針を記載

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `いやなおと` エントリで PP が `40`（Gen9本家基準）に
  なっていたバグを発見し、`src/jpoke/data/ps-champ-ja/moves.json` に基づき `20` に修正した
- `handlers/move_status.py` の `いやなおと_lower_defender_def` は既存の共通関数
  `modify_defender_stats` を正しく利用しており、修正不要と判断した
- `flags={"sound"}` は既に設定済みで、みがわり貫通処理（`core/move_executor.py` の sound
  フラグ汎用ルール）も既存の汎用実装でカバーされているため修正不要

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」列が `n/a`）

## テスト
- `tests/moves_status/test_move_a.py` に以下を追加
  - `test_いやなおと_defenderのぼうぎょが2段階下がる`: 命中時に相手の『ぼうぎょ』が2段階下がることを確認
  - `test_いやなおと_みがわりを貫通してぼうぎょが下がる`: soundフラグによりみがわり状態でも
    効果が発動し、みがわりのHPが減らないことを確認
- `python -m pytest tests/ -v` で全4095件（1件skip）が成功することを確認
  （ログ: `.loop/test_logs/いやなおと.log`）
