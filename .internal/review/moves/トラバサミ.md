# トラバサミ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/トラバサミ.md`）。
- 一次情報（`.internal/wiki/moves/トラバサミ.html`）と `src/jpoke/data/ps-champ-ja/moves.json`（はがね/物理/威力35/命中100/PP16）を確認し、
  `.internal/spec/moves/しめつける.md`・`.internal/spec/moves/サンダープリズン.md` と同形式で作成した。
- タイプはChampionsで第八世代の「くさ」から「はがね」に変更されている点、そのためそうしょくによる
  無効化が発生しなくなった点を明記した。
- 併せて `.internal/spec/volatiles/バインド.md` の技一覧表にあった「トラバサミ | くさ | ...」の記載が
  Champions仕様（はがね）に更新されていなかったため、`はがね` に修正した。

## 実装計画書
- 新規作成（`.internal/plan/moves/トラバサミ.md`）。
- `.internal/plan/moves/サンダープリズン.md` と同形式で作成。既存実装（`apply_bind_to_defender` /
  `_apply_bind` の流用）が仕様と一致していることを確認したうえで「変更なし」として記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ta.py` の `トラバサミ` エントリは
  `type="はがね"`, `category="physical"`, `pp=16`, `power=35`, `accuracy=100` で
  `src/jpoke/data/ps-champ-ja/moves.json` と一致しており、`Event.ON_DAMAGE_HIT` に既存の共通バインド
  ハンドラ `apply_bind_to_defender`（`handlers/move_attack.py`）を登録済みで仕様と齟齬なし。
- なお、タスク指示文にあった「トラバサミはこおりタイプ限定でバインド効果を持つ技」という記述は
  一次情報（wiki）・`src/jpoke/data/ps-champ-ja/moves.json`・`.internal/champions/changes_from_sv.md` のいずれとも
  一致しない誤り（実際はChampionsで「はがね」タイプに変更）であり、実装済みの `type="はがね"` が
  正しい値である。誤った記述に基づく修正は行っていない。

## リーサル計算
- 変更なし。`lethal_handlers={LethalEvent.ON_HIT: LethalHandler(l._apply_bind)}` が既に登録されており、
  `handlers/lethal.py` の `_apply_bind`（バインド未付与時のみ付与、ダメージ自体はバインド揮発性状態の
  `ON_TURN_END` リーサルハンドラが処理）と整合している。他のバインド系技（サンダープリズン等）と
  同一パターンのため実装変更は不要と判断した。
- ただし `.internal/progress/move.md` は「リーサルテスト」列が既に `x`（テスト済み）だったにもかかわらず
  `tests/test_lethal.py` にトラバサミのリーサルテストが存在しなかったため、手順6でテストを追加した
  （進捗表と実態の整合を取るための追加）。

## テスト
- `tests/moves_attack/test_move_ta.py` の既存4テスト（バインド中の交代不可、毎ターン1/8ダメージ、
  命中後のバインド付与、攻撃者交代でのバインド解除）を実装と照合し、内容・PP/タイプの前提と齟齬が
  ないことを確認した（変更なし）。
- `tests/test_lethal.py` に `test_トラバサミ_バインド付与` を追加した
  （`test_サンダープリズン_バインド付与` と同パターン。技経由のバインド付与とバインド事前付与で
  ターン終了後の最大到達ダメージが一致することを確認）。
- `python -m pytest tests/ -v` で全3900件（1件skip）がパスすることを確認した。
