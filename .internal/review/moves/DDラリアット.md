# DDラリアット レビュー結果

## 仕様書
- 変更なし（新規作成せず）。DDラリアットは物理・あく・威力85・命中100・PP10・直接攻撃技であり、
  相手のランク補正を無視してダメージを与えるという単一の効果のみを持つ。
  同種の効果を持つせいなるつるぎ（かくとう版）も進捗表で仕様書列を「-」として扱う運用に
  統一されているため、DDラリアットについても専用の仕様書ファイルは作成しない。

## 実装計画書
- 変更なし（新規作成せず）。上記と同じ理由により、専用の計画書ファイルは作成しない。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `"DDラリアット"` エントリ
  （type="あく", category="physical", pp=10, power=85, accuracy=100, flags={"contact"}）は
  一次情報（.internal/wiki/moves/DDラリアット.html: タイプ あく / 分類 物理 / 威力85 / 命中率100 / PP10 /
  直接攻撃○ / 効果「相手のランク補正を無視してダメージを与える」）と一致しており、修正不要と判断した。
- `handlers/move_attack.py` の `DDラリアット_ignore_def_rank` は `Event.ON_CALC_DEF_RANK_MODIFIER` に対して
  `HandlerReturn(value=1)` を返し、防御側のランク補正を1.0に固定する実装であり、`damage.py` の
  `ON_CALC_DEF_RANK_MODIFIER` 発火箇所（防御側ランク補正の計算直後）と整合している。
  同じ効果を持つ `せいなるつるぎ_ignore_def_rank` と実装パターンが一致していることを確認した。

## テスト
- `tests/moves_attack/test_move_ta.py` に以下の既存テストがあり、実装と整合していることを確認した。
  - `test_DDラリアット_相手のぼうぎょランクを無視する`（防御ランク+6でもダメージが変わらないことを確認）
  - `test_DDラリアット_相手のぼうぎょランク低下も無視する`（防御ランク-6でもダメージが変わらないことを確認）
- 上記2テストで正方向・負方向のランク変化を両方カバーしており、追加のテストは不要と判断した。
- `python -m pytest tests/ -v` を実行し、3203 passed, 1 skipped（既存のskipで無関係）で全件成功を確認した
  （ログ: `.loop/test_logs/DDラリアット.log`）。

## 進捗表 (`.internal/progress/move.md`)
- PP列が誤って `12` と記載されていたため、一次情報・実装に合わせて `10` に修正した。
- レビュー列を `-` から `✓` に更新した。
