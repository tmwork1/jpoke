# かげうち レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/かげうち.md`）。
- 一次情報（`.internal/wiki/moves/かげうち.html`）を基に、タイプ（ゴースト）・分類（物理）・威力（40）・
  命中（100）・PP（30）・優先度（+1）・接触あり・追加効果なし・各種判定フラグ（急所/命中判定/まもる/
  おうじゃのしるし/マジックコート/よこどり/オウムがえし）を記載。

## 実装計画書
- 新規作成（`.internal/plan/moves/かげうち.md`）。
- 仕様書をもとに、既存の `data/move.py` エントリが仕様と一致していることを確認し、専用ハンドラ
  不要である根拠（追加効果なし・優先度は `MoveData.priority` の共通処理で解決）を記載。

## 実装（handlers/ / data/）
- 変更なし。`data/move.py` の `"かげうち"` エントリ（type="ゴースト", category="physical", pp=30,
  power=40, accuracy=100, priority=1, flags={"contact"}, handlers={}）は仕様書と完全に一致しており
  修正不要と確認した。

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行は「リーサル実装」「リーサルテスト」ともに `n/a`）。

## テスト
- 変更なし。`tests/moves_attack/test_move_ka.py` の
  `test_かげうち_相手にダメージを与える` は既存の同種先制技テスト
  （マッハパンチ・アクアジェット等）と同一パターンで実装済みであり、追加・修正の必要はないと判断した。

## その他
- `.internal/progress/move.md` の該当行に誤りを発見し修正した：
  - PP列が `20` になっていたが、実装・仕様（PP30）と不一致だったため `30` に修正。
  - 仕様書・計画書列が `-` のままだったが、本レビューで新規作成したため `x` に更新。
  - レビュー列が `-` のままだったが、レビュー完了に伴い `✓` に更新。
  - 効果列に「追加効果なし。」を補足した。
