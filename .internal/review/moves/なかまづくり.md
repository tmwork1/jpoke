# なかまづくり レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/なかまづくり.md`）
- 一次情報（`.internal/wiki/moves/なかまづくり.html`）をもとに、タイプ/分類/威力/命中率/PP/範囲/優先度/
  直接攻撃の有無、効果、判定（まもる○・マジックコート○・おうじゃのしるし×・よこどり×・
  オウムがえし○）、失敗条件（なまけ／protected／uncopyable／同一特性／とくせいガード／
  ダイマックス／スターモービル）、マジックコート反射時の挙動、交代・メガシンカ・
  フォルムチェンジ時の特性復元仕様を記載

## 実装計画書
- 既存の `.internal/plan/moves/なかまづくり.md` を実装・仕様と照合し、以下を修正
  - PP を「15」から「16（champions/move_list.txt準拠）」に修正
  - 「みがわり状態についてはwiki 明記なし」という誤った記述を修正
    （wiki の「技の仕様」節に「みがわり状態に防がれる」と明記されていることを確認）
  - 実装済みの `battle.ability_manager.is_change_blocked(ctx.defender)`
    （とくせいガード等によるブロック）チェックが計画書のコード例・仕様要約・
    ハンドラ構成表に反映されていなかったため追記
  - 「現状の実装状況」表が「未実装」のまま古い記述だったため、実装済みの状態に更新
  - コード例の `category="変化"` → `category="status"`、`pp=15` → `pp=16` に修正
  - とくせいガードとの相互作用に関する節を新規追加

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_na.py` の「なかまづくり」エントリで `pp=15` と
  なっていたバグを `pp=16`（`src/jpoke/data/ps-champ-ja/moves.json` 準拠）に修正
- `handlers/move_status.py` の `なかまづくり_can_apply` /
  `なかまづくり_change_defender_ability` は仕様書・計画書と整合しており、
  ロジック自体の修正は不要（`subject_spec` 未指定＝`attacker:self` デフォルトで
  `AttackContext` のロールと一致、`target` は `MoveData` のデフォルト値 `"foe"` の
  ままで `target="self"` 欠落バグは無し）

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）

## テスト
- `tests/moves_status/test_move_na.py` に以下を追加
  - `test_なかまづくり_まもるで防がれる`（対象のまもる状態で防がれ特性が変化しないことを確認）
  - `test_なかまづくり_みがわり状態の相手には防がれる`（みがわり状態で防がれることを確認）
  - `test_なかまづくり_マジックコートで跳ね返され使用者の特性が変わる`
    （マジックコートで反射され、使用者の特性が相手の特性に書き換わることを確認。
    `target="foe"` が正しく効いていることの回帰防止テストを兼ねる）
- 既存テスト（uncopyable失敗・交代で元に戻る・同一特性失敗・なまけ/protected失敗・
  相手の特性が変わる、とくせいガードでブロック）は仕様と一致しており変更なし
