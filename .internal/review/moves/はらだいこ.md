# はらだいこ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/はらだいこ.md`）。一次情報（`.internal/wiki/moves/はらだいこ.html`）を基に、
  基本情報・効果・失敗条件（こうげきランク最大時／HPが最大HPの半分以下の場合）・あまのじゃくとの
  相互作用・判定表（まもる／マジックコート／よこどり等）・champions向け簡略化事項を記載した。

## 実装計画書
- 既存の計画書は仕様と実装の両方から乖離していたため修正した:
  - PP を 10 → 12（champions基準、`src/jpoke/data/ps-champ-ja/moves.json` 準拠）に修正
  - `target="self"` を明示する必要がある旨を追記（未設定だとデフォルトの `"foe"` になり、
    まもる／マジックコートの判定が誤ってしまうバグがあったため）
  - スタット参照キーを旧仕様の `"A"` から実装の実際のキー `"atk"` に修正
  - HP半分以下チェックを失敗条件に追記（旧計画書には記載がなかった）
  - あまのじゃくとの相互作用の説明を追記

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `はらだいこ` エントリを修正:
  - `pp=10` → `pp=12`（champions基準のPPに修正。Gen9本家基準の値が移行漏れとなっていたバグ）
  - `target="self"` を追加（未設定だとデフォルトの `"foe"` 扱いとなり、`Move.is_blocked_by_protect` /
    `Move.is_reflectable` が誤って真になる。相手の「まもる」で防がれてしまうバグを修正）
- `handlers/move_status.py` の `はらだいこ_can_apply` / `はらだいこ_apply` 自体は仕様通り実装済みで
  修正不要だった（こうげきランク+6判定・HP半分以下判定・`modify_hp(r=-0.5)` による端数切り捨て消費・
  `modify_stats` によるあまのじゃく反転の委譲、いずれも正しく実装されている）。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- `tests/moves_status/test_move_ha.py` に以下を追加:
  - `test_はらだいこ_HPが最大HPの半分以下なら失敗`: HPがちょうど最大HPの半分のとき失敗し、
    ランク・HPともに変化しないことを確認
  - `test_はらだいこ_あまのじゃくならこうげきランクが最低まで下がる`: あまのじゃく特性で
    ランク変化が反転し-6まで下がること、失敗せずHPが通常通り消費されることを確認
  - `test_はらだいこ_まもるで防がれない`: `target="self"` 修正により相手の「まもる」で
    防がれないことを確認（修正前は誤ってブロックされるバグがあった）
- 既存の2テスト（こうげきすでに最大なら失敗／こうげき最大化しHP半分消費）は変更なし
- `python -m pytest tests/ -v` を実行し、4343 passed, 1 skipped で全件成功
  （`tests/test_field.py` の一部テストは無シードの乱数急所判定に依存したフレーキーテストであり、
  本修正とは無関係。再実行で安定してパスすることを確認済み）
