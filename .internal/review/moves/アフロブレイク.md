# アフロブレイク レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/アフロブレイク.md`）
- 一次情報（.internal/wiki/moves/アフロブレイク.html）を確認し、タイプ=ノーマル、分類=物理、威力=120、
  命中率=100、PP=15、対象=相手1体、優先度=0、直接攻撃あり、効果=与ダメの1/4反動、追加効果なしを記載
- 本技は `src/jpoke/data/ps-champ-ja/moves.json` に未収録（チャンピオンズでは現状いずれのポケモンも習得不可）
  であることを明記し、`とっしん`・`からみつく` と同様に汎用ダメージ技として実装する方針を記載
- PPは全技PP統一（8/12/16/20）の対象外であり、本家の値15をそのまま使う旨を明記

## 実装計画書
- 新規作成（`.internal/plan/moves/アフロブレイク.md`）
- ハンドラ構成（`Event.ON_HIT` / priority=100 / `attacker:self`）と、`Event.ON_DAMAGE_HIT` ではなく
  `Event.ON_HIT` を使う理由（`いのちのたま` と同様、実ダメージ0以下でも発火させるため）を明記
- `data/moves/move_a.py` のPP修正内容を記載

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `"アフロブレイク"` エントリの `pp` を `10` → `15` に修正
  （`src/jpoke/data/ps-champ-ja/moves.json` に本技が未収録のため PP統一の対象外であり、本家PPの15が正しい値。
  従来の `10` は初期の一括計画書 `.internal/plan/moves/反動技_025.md` に残っていた誤った暫定値だった）
- `type`・`category`・`power`・`accuracy`・`flags`・ハンドラ登録（`Event.ON_HIT` / `アフロブレイク_recoil`）
  は仕様書と一致しており、修正不要
- `src/jpoke/handlers/move_attack.py` の `アフロブレイク_recoil`（共通ヘルパー `_recoil` を反動比率1/4で
  呼び出す実装）は仕様と一致しており修正不要

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」列は n/a）。反動技は攻撃者自身のHPを減らすのみで
  防御側のHP分布に影響しないため、リーサル計算ハンドラの実装対象にはならない

## テスト
- `tests/moves_attack/test_move_a.py` の既存2テスト
  （`test_アフロブレイク_使用後に攻撃者が反動ダメージを受ける`、
  `test_アフロブレイク_反動ダメージが与ダメの4分の1になる`）を実装と照合し、内容が正しいことを確認
  （変更なし）
- `tests/moves_attack/test_move__group.py` の `test_recoil技グループ_反動量が与ダメのrecoil_ratio倍になる`
  にも `("アフロブレイク", 1/4)` が含まれており、反動比率のテストが重複してカバーされていることを確認
  （変更なし）
- `python -m pytest tests/ -v` で全3658件（1件skip）がパスすることを確認
