# ミラーコート レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ミラーコート.md`）。
- 一次情報（`.internal/wiki/moves/ミラーコート.html`）とカウンター（物理版）の既存仕様書を参照し、
  基本情報・効果・判定フラグ・優先度根拠・未対応エッジケース・Zワザ/ダイマックス情報を整理した。
- 特に「第八世代以前はまねっこでコピー不可だが、SVではコピー可能（カウンターとは異なる）」という
  一次情報の記載を明記した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ミラーコート.md`）。
- ハンドラ構成表・priority根拠（`ON_TRY_MOVE_1` priority=30、`ON_MODIFY_MOVE_DAMAGE` priority=100）を
  カウンターの計画書と同様の形式で記載。
- `non_copycat` フラグを付与しない理由（SVではまねっこでコピー可能なため）を明記した。
- みがわり・物理技のみ受けた場合・連続技・まねっこコピー時の挙動をエッジケースとして記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ma.py` の `ミラーコート` エントリ、
  `src/jpoke/handlers/move_attack.py` の `ミラーコート_check_can_use` / `ミラーコート_modify_damage` は
  カウンターと対称的な実装になっており、仕様書・計画書と整合していることを確認した。
- `flags` に `non_copycat` が付与されていないことを確認（SV仕様として正しい。カウンターとの意図的な差異）。
- 共通処理である `core/move_executor.py` の `last_physical_damage_received` /
  `last_special_damage_received` の「最後の1回分で上書き」ロジック（カウンターレビュー時に修正済み）が
  ミラーコートにも正しく適用されていることを確認した。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`。カウンターと同様、ダメージが試技時点の
  被弾履歴に依存する技のためリーサル計算の対象外として扱う）。

## テスト
- テストの配置ミスを修正: `tests/moves_attack/test_move_ha.py`（は行）に誤って置かれていた
  ミラーコート関連の既存3テストを、正しい行区分の `tests/moves_attack/test_move_ma.py`（ま行）に移動した。
- 既存3テストに加え、カウンターのテストと同等のカバレッジになるよう以下を追加：
  - `test_ミラーコート_みがわりに当たったダメージは参照されない`
  - `test_ミラーコート_連続技を受けた場合は最後の1回分のダメージのみ参照する`
  - `test_ミラーコート_まねっこでコピーできる`（SV固有仕様: カウンターと異なりコピー可能であることの確認）
- `python -m pytest tests/ -v` で全4016件（1件skip、既存のskipで無関係）がパスすることを確認し、
  ログを `.loop/test_logs/ミラーコート.log` に保存した。
