# つのドリル レビュー結果

## 仕様書

- 新規作成（`.internal/spec/moves/つのドリル.md`）。
- `.internal/wiki/moves/つのドリル.html`（タイプ: ノーマル、分類: 物理、威力: -、命中率: 30、PP: 5、
  範囲: 1体選択、優先度: 0、直接攻撃: あり、効果: 一撃必殺技）と
  `.internal/spec/moves/_ohko.md`（一撃必殺技共通仕様）を根拠に、
  `じわれ.md`・`ぜったいれいど.md` と同一構成で作成した。
- つのドリルはノーマルタイプ技のため、既存のタイプ相性判定（ゴーストタイプ無効）に委譲する旨を明記。
  じわれのような専用の無効化ハンドラ（ひこう・ふゆう判定）や、ぜったいれいどのような
  専用の命中率補正は不要であることを確認した。

## 実装計画書

- 新規作成（`.internal/plan/moves/つのドリル.md`）。
- `data/move.py` の既存実装（`ha.ohko_damage` を `Event.ON_MODIFY_MOVE_DAMAGE` に
  `priority=90` で登録、`flags={"ohko", "contact"}`）を仕様書と照合し、誤り・欠落がないことを確認した。
  `じわれ`・`ハサミギロチン`・`ぜったいれいど` と同一パターンであることを明記した。

## 実装（handlers/ / data/）

- 変更なし。`data/move.py` の `つのドリル` エントリ（type=ノーマル, category=physical, pp=5,
  power=0, accuracy=30, flags={"ohko", "contact"}）は仕様書・一次情報と一致しており、
  `handlers/move_attack.py` の共通ハンドラ `ohko_damage`（対象の現在HPを確定ダメージとして
  設定）も既存の一撃必殺技（じわれ・ハサミギロチン・ぜったいれいど）と同一実装で問題なかった。
  がんじょう・きあいのタスキ・まもる等の無効化は `flags={"ohko"}` を参照する共通の
  特性/アイテムハンドラ（`handlers/ability.py`, `handlers/item.py`）で対応済みであることを確認した。

## リーサル計算

- 対象外（`.internal/progress/move.md` の当該行は「リーサル実装: n/a」「リーサルテスト: n/a」）。

## テスト

- 変更なし。`tests/moves_attack/test_move__group.py` の
  `test_一撃必殺技_タイプ相性で無効化される`（つのドリル対ゴース）、
  `test_一撃必殺技_命中時は相手を一撃で倒す`、`test_一撃必殺技_外した時はダメージを与えない`
  で `つのドリル` が代表技として既にカバーされていることを確認した。過不足なし。
- `python -m pytest tests/ -v -p no:randomly` で全3435件（+スキップ1件）がパスすることを確認した。
  なお `-p no:randomly` を付けずに実行した場合、乱数固定を行っていない一部の既存テスト
  （`test_move_ra.py::test_リフレッシュ_まひを治す`、`test_field.py::test_オーロラベール_ひかりのかべとは重複しない`、
  `test_battle_option.py::test_メガシンカ不可オプションでコマンドが出ない` など）で
  実行順序に依存した稀な失敗が発生することを確認したが、これは今回の `つのドリル` の
  実装・変更とは無関係の既存の問題であり、本タスクの対象外とした。

## 進捗表

- `.internal/progress/move.md` の `つのドリル` 行を更新: 仕様書 `x`、計画書 `x`、レビュー `✓`、
  効果列を「相手を『ひんし』状態にする。『ゴースト』タイプには無効だが、それ以外のタイプ相性の
  影響を受けない。」に更新（実装 `x`・テスト `x`・リーサル `n/a`/`n/a` は変更なし）。
