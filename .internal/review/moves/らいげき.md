# らいげき レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/らいげき.md`）。一次情報（`.internal/wiki/moves/らいげき.html`）と
  `.internal/spec/moves/_move_secondary_1.md` を照合し、タイプ=でんき、分類=物理、威力130、
  命中85、PP5、対象=相手1体、優先度0、接触あり、追加効果20%まひを記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/らいげき.md`）。既存実装が仕様と一致していることを確認し、
  ハンドラ構成（`ON_DAMAGE_HIT` / priority100デフォルト / `attacker:self`）と
  おうじゃのしるし・ちからずくとの相互作用を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ra.py` の `らいげき` エントリ（type="でんき",
  category="physical", pp=5, power=130, accuracy=85, flags={"contact",
  "secondary_effect"}, ON_DAMAGE_HIT ハンドラ）と `src/jpoke/handlers/move_attack.py` の
  `らいげき_apply_paralysis_to_defender`（`apply_ailment_to_defender` で20%まひ付与）は
  仕様書・計画書と一致しており、誤り・欠落は見つからなかった。

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行「リーサル実装」列が `n/a`）。

## テスト
- 変更なし。`tests/moves_attack/test_move_yarawa.py` の
  `test_らいげき_まひが発動する`（`secondary_chance=1.0` でまひ付与を確認）は実装と一致して
  おり、過不足なし。`scripts/sort_tests.py` 実行済み（差分なし）、
  `scripts/generate_test_list.py` 実行済み、`python -m pytest tests/ -v` は
  3620 passed, 1 skipped で全テスト成功。
