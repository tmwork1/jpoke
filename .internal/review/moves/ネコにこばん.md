# ネコにこばん レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ネコにこばん.md`）。ノーマル/物理/威力40/命中100/PP20/対象1体、
  非接触（直接攻撃×）、バトル内追加効果なしであることを一次情報（.internal/wiki/moves/ネコにこばん.html）
  から記載した。命中後のお金入手はゲームシステム上の演出であり、対象範囲（チャンピオンズ
  シングルバトル）ではダメージ計算・状態変化に一切影響しないため、追加効果のない通常攻撃技として
  扱う旨を明記した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ネコにこばん.md`）。既存実装（`src/jpoke/data/moves/move_na.py`）が
  仕様と一致していることを確認し、その根拠を記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_na.py` の `"ネコにこばん"` エントリ
  （type=ノーマル, category=physical, pp=20, power=40, accuracy=100, handlers={}）を確認し、
  仕様（威力・命中率・PP・非接触・追加効果なし）と一致していることを検証した。
  `contact` フラグが付与されていない点も「直接攻撃: ×」の一次情報と一致している。
  `src/jpoke/data/ps-champ-ja/moves.json` に本技の記載がないため、PP変更（8/12/16/20統一）の対象外と
  判断し、PP20（Gen9本家基準）をそのまま維持した。

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行は「リーサル実装: n/a」）。

## テスト
- `tests/moves_attack/test_move_na.py` に以下を追加した。
  - `test_ネコにこばん_相手にダメージを与える`: 通常攻撃技で相手にダメージを与えることを確認。
  - `test_ネコにこばん_追加効果が発生しない`: 状態異常・能力ランク変化が発生しないことを確認。
