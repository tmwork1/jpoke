# ゆめくい レビュー結果

## 仕様書
- 変更なし（既存の `.internal/spec/moves/ゆめくい.md` を一次情報（wiki HTML）と照合し、内容の誤り・欠落なしを確認）

## 実装計画書
- 新規作成（`.internal/plan/moves/ゆめくい.md`）

## 実装（handlers/ / data/）
- `handlers/move_status.py` の `ゆめくい_check_sleep` を修正:
  `ctx.defender.has_ailment("ねむり")` を `ctx.defender.ailment.is_sleep` に変更。
  修正前は特性ぜったいねむり（内部的に「ゆめうつつ」状態異常を付与する）のポケモンに対して
  常に失敗しており、仕様「特性 ぜったいねむり のポケモンに対しても効果がある」に反していた。
  同種の判定を行う `いびき_check_sleep` の実装パターンに合わせて修正。
- `handlers/move_attack.py` の `ゆめくい_drain`、`data/moves/move_ya.py` の `"ゆめくい"` エントリは
  仕様と一致しており変更なし（優先度・イベント選択・おおきなねっこ/ヘドロえき/かいふくふうじとの
  連携は共通ヘルパー経由で自動的に成立することを確認）。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」列が `n/a`）

## テスト
- `tests/moves_attack/test_move_yarawa.py` に
  `test_ゆめくい_ぜったいねむり特性の相手にも成功する` を追加し、上記バグ修正を検証。
  既存の2件（ねむり状態でない相手には失敗する／ねむり状態の相手にのみ命中しHPを回復する）は
  実装と一致しており変更なし。
