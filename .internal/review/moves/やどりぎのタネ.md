# やどりぎのタネ レビュー結果

## 仕様書
- `.internal/spec/moves/やどりぎのタネ.md` が存在しなかったため、`.internal/wiki/moves/やどりぎのタネ.html`
  （基本情報表・判定表）をもとに新規作成した（タイプ・分類・威力・命中・PP・対象・優先度・接触の有無、
  急所/命中判定/追加効果/まもる/みがわり/おうじゃのしるし/マジックコート/よこどり/オウムがえしの
  判定を記載。詳細な状態変化仕様は `.internal/spec/volatiles/やどりぎのタネ.md` を参照する形にした）
- `.internal/spec/volatiles/やどりぎのタネ.md`（既存）を一次情報
  （`.internal/wiki/volatiles/やどりぎのタネ_(状態変化).html`）と照合し、以下を修正した
  - 「おおきなねっこ（未実装）」の記載を修正。実際は `item.py` の
    `おおきなねっこ_boost_drain`（`Event.ON_CALC_DRAIN`）で実装済みだったため、
    実装済みである旨と実装箇所を追記した
  - 予防・無効条件の記載を整理し、「みがわり」と「そうしょく」を別々の無効化経路として
    明記した（従来は同一項目内でやや紛らわしい書き方になっていた）

## 実装計画書
- `.internal/plan/moves/やどりぎのタネ.md`（既存）を実装コードと照合し、以下の齟齬を修正した
  - PP を旧記載の10から実装値・`move_list.txt` 準拠の12に修正
  - 「くさタイプ: やどりぎ無効」チェックについて、旧計画書は
    「`ON_TRY_MOVE_2` priority130 でフレームワーク側が処理するため実装不要」と誤って
    記載していたが、実際には `やどりぎのタネ_can_apply`（`Event.ON_BEFORE_APPLY_MOVE`,
    priority=130）という専用ハンドラが実装されている。実装済みハンドラの内容・登録コードを
    計画書に明記し、priorityの根拠（turn.md の値を踏襲しつつ実際のイベントは
    `ON_BEFORE_APPLY_MOVE` に統一する既存の慣例に従う）を追記した
  - 「すでにやどりぎ状態」の失敗は `VolatileManager.apply()` の汎用ルールで処理される旨を明記
  - 揮発性状態（`やどりぎのタネ` volatile）側の実装状況を参考情報として追記

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ya.py` の `やどりぎのタネ` エントリで `pp=10` になっていたのを
  `src/jpoke/data/ps-champ-ja/moves.json`（PP12）に合わせて `pp=12` に修正した
  （`.internal/progress/move.md` のPP列はすでに12で記載されており、実装側の移行漏れだった）
- `handlers/move_status.py` の `やどりぎのタネ_apply` / `やどりぎのタネ_can_apply`、
  `handlers/volatile.py` の `やどりぎのタネ_drain_hp`、`data/volatile.py` の
  `"やどりぎのタネ"` エントリを仕様書・一次情報と照合したが、他に誤りは見つからなかった
  （マジックガード・ヘドロえき・おおきなねっこ・かいふくふうじ・バトンタッチでの引き継ぎ・
  こうそくスピン/キラースピンでの解除・ちからずくによる解除阻止（第九世代仕様）を
  すべて確認し、いずれも仕様と一致していた）

## リーサル計算
- `.internal/progress/move.md` の「リーサル実装」列が `n/a` のため対象外（技自体は直接ダメージを
  与えないため）。なお `やどりぎのタネ` 状態（揮発性状態）側のターン終了時ダメージについては
  `handlers/lethal.py` の `やどりぎのタネ_damage`（`data/volatile.py` の
  `lethal_handlers`）がすでに実装済みで、内容も仕様と一致していることを確認した

## テスト
- 既存テスト（`tests/moves_status/test_move_yarawa.py`,
  `tests/volatiles/test_volatile_hamayarawa.py`, `tests/moves_attack/test_move_ka.py`,
  `tests/items/test_item_a.py` 等）で、状態付与・くさタイプ無効・重複付与失敗・
  ターン終了ダメージ/回復・マジックガード・ヘドロえき・おおきなねっこ・バトンタッチ引き継ぎ・
  こうそくスピン/キラースピンでの解除が広く網羅されていることを確認した
- 未カバーだった「回復先（使用者がいた場所）が交代した場合、新しくその場所に来たポケモンが
  回復し、ベンチに退いた元の使用者は回復しない」というケースのテストを
  `tests/volatiles/test_volatile_hamayarawa.py` に追加した
  （`test_やどりぎのタネ_回復側が交代すると新しい位置のポケモンが回復する`）
- `python -m pytest tests/ -v` で全件パスを確認（4451 passed, 1 skipped）。
  結果は `.loop/test_logs/やどりぎのタネ.log` に保存した
