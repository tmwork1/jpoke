# ミラーショット レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ミラーショット.md`）。.internal/wiki/moves/ミラーショット.html を一次情報として、
  はがねタイプ / 特殊 / 威力65 / 命中85 / PP10（本家基準、champions/move_list.txt に記載なしのため
  過去のずつきレビュー前例に準拠しGen9本家値を採用） / 対象1体 / 優先度0 / 接触なし /
  追加効果30%で相手の命中率を1段階下げる、という内容を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ミラーショット.md`）。既存実装（`ミラーショット_lower_acc` /
  `data/moves/move_ma.py` の `MoveData` エントリ）が仕様書と完全に一致していることを確認し、
  その根拠（`modify_defender_stats` ヘルパーの挙動、イベント優先度、subject_spec のデフォルト理由等）
  を記載した。

## 実装（handlers/ / data/）
- 変更なし。`handlers/move_attack.py` の `ミラーショット_lower_acc`（`modify_defender_stats` で
  `stats={"accuracy": -1}, chance=0.3` を指定）と `data/moves/move_ma.py` の `MoveData`
  （はがね/特殊/pp10/power65/accuracy85/`secondary_effect`フラグ/`ON_DAMAGE_HIT`）は
  いずれも仕様と整合しており、修正不要と判断した。

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行はリーサル実装/リーサルテストともに n/a）。
  命中率低下は確定数計算に影響しないため。

## テスト
- 変更なし。既存の `test_ミラーショット_命中率1段階低下が発動する`
  （`tests/moves_attack/test_move_ma.py`）が `secondary_chance=1.0` で発動を確認しており、
  効果検証として十分と判断した。
