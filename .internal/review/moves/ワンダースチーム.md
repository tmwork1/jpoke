# ワンダースチーム レビュー結果

## 仕様書
- 変更なし（`.internal/spec/moves/_move_secondary_1.md` に既存の複数技共通スペック表として記載済み。
  一次情報（`.internal/wiki/moves/ワンダースチーム.html`）と照合し、対象=defender・確率=0.2・こんらん付与のみで
  誤り・欠落なしを確認した）

## 実装計画書
- `.internal/plan/moves/攻撃技_こんらん系.md` のワンダースチームに関する記述を実装と一致するよう修正した
  - ハンドラ構成表: `ON_HIT` → `ON_DAMAGE_HIT` に修正（実際の登録イベントに合わせる）
  - 実際に `ON_DAMAGE_HIT` は `.internal/spec/turn.md` 未掲載イベントであるため、既存の同種ハンドラ
    （みずのはどう・ロッククライム等、すべて `ON_DAMAGE_HIT` で統一）を根拠として priority=100・
    subject_spec=attacker:self（`MoveHandler` のデフォルト値）を踏襲する旨の注記を追加した
  - MoveData サンプルコードを `labels=[...]` → `flags={...}`、`category="特殊"` → `category="special"`、
    `Event.ON_HIT` → `Event.ON_DAMAGE_HIT` に修正し、実装コードと完全一致させた
  - 他の技（おしゃべり・かかとおとし等）の記述はこのレビュー対象外のため変更していない

## 実装（handlers/ / data/）
- 変更なし
  - `src/jpoke/data/moves/move_wa.py` の `ワンダースチーム` エントリ（type=フェアリー, category=special,
    pp=10, power=90, accuracy=95, flags={"secondary_effect"}, Event.ON_DAMAGE_HIT）は
    一次情報・仕様書と一致することを確認した
  - `pp=10` は `src/jpoke/data/ps-champ-ja/moves.json` に本技のエントリが存在しない（ガラルマタドガスの
    専用技だが Champions では実質使用機会がないため一覧から欠落していると推測される）ため、
    README.md の対象範囲規定に従い第9世代（SV）基準の値をそのまま採用する（変更不要）
  - `src/jpoke/handlers/move_attack.py` の `ワンダースチーム_apply_confusion_to_defender` は
    `handlers/move.py` の共通ヘルパー `apply_confusion_to_defender(chance=0.2)` を正しく利用しており、
    みずのはどう・ロッククライム等の同種実装と一貫している

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列は `n/a`。追加効果のみの技でありダメージ計算への
  影響がないため対象外で正しい）

## テスト
- 変更なし
  - `tests/moves_attack/test_move_yarawa.py::test_ワンダースチーム_こんらんが発動する` が
    `secondary_chance=1.0` でこんらん付与を確認しており、同種の他技のテストと同一パターンで
    過不足なしと判断した

## 進捗表
- `.internal/progress/move.md` のワンダースチーム行を修正した
  - 仕様書列: `-` → `x`（`_move_secondary_1.md` に記載済みのため）
  - 計画書列: `-` → `x`（`攻撃技_こんらん系.md` に記載済みのため）
  - レビュー列: `-` → `✓`
  - タイプ/威力/命中率/PP/対象/効果列: `?` のプレースホルダーを実際の値
    （フェアリー/90/95/10/defender/「追加効果として、20%の確率で相手をこんらん状態にする。」）に更新した
