# サイケこうせん レビュー結果

## 仕様書
- 新規作成（.internal/spec/moves/サイケこうせん.md）
- .internal/wiki/moves/サイケこうせん.html の一次情報（タイプ: エスパー、分類: 特殊、威力65、命中100、PP20、
  追加効果: 10%の確率でこんらん付与、まもるで防がれる、直接攻撃ではない）を反映
- `src/jpoke/data/ps-champ-ja/moves.json` に本技の記載がないことを確認し、`ねんりき`（同様に未収録）の
  既存仕様書を踏襲して本家シリーズの値をそのまま採用する方針を明記

## 実装計画書
- 新規作成（.internal/plan/moves/サイケこうせん.md）
- 既存実装（`data/moves/move_sa.py` の `"サイケこうせん"` エントリ、`handlers/move_attack.py` の
  `サイケこうせん_apply_confusion_to_defender`）は仕様と一致していることを確認し、その内容を計画書に記録

## 実装（handlers/ / data/）
- 変更なし。`data/moves/move_sa.py` の `type="エスパー"`, `category="special"`, `pp=20`, `power=65`,
  `accuracy=100`, `flags={"secondary_effect"}` は一次情報・PP方針（champions未収録技のため本家値20を採用）と一致
- `handlers/move_attack.py` の `サイケこうせん_apply_confusion_to_defender` は `apply_confusion_to_defender`
  （chance=0.1）を呼び出しており、`ねんりき` 用ハンドラと同一パターンで仕様通り

## リーサル計算
- 対象外（.internal/progress/move.md の該当行「リーサル実装」列が n/a。こんらん付与のみでHP分布に影響しない
  追加効果のため対象外）

## テスト
- 変更なし。既存の `test_サイケこうせん_こんらんが発動する`（tests/moves_attack/test_move_sa.py）は
  `ねんりき` の対応テストと同一パターンで secondary_chance=1.0 時にこんらんが付与されることを検証しており、
  仕様と一致
- `python -m pytest tests/ -v` を実行し、3756 passed, 1 skipped を確認
  （途中一度 tests/test_field.py の急所依存テストが乱数起因で5件失敗したが、本レビューの変更対象外であり
  再実行で全て成功することを確認済み。サイケこうせんの実装・テストとは無関係）
