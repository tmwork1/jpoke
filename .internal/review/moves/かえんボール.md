# かえんボール レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/かえんボール.md`）。
- 一次情報（ポケモンWiki）と照合し、基本情報（ほのお/物理/威力120/命中90/PP5/対象1体/優先度0/非接触/弾のわざ）、
  追加効果（10%やけど付与）、および「使用者がこおり状態の場合は治してから攻撃する」効果を明記した。

## 実装計画書
- 新規作成（`.internal/plan/moves/かえんボール.md`）。
- 既存実装（やけど付与ハンドラ）は仕様と一致していたため変更不要と記載。
- 未実装だった使用者側の無条件解凍処理について、`かえんぐるま_thaw_attacker` /
  `もえつきる_thaw_attacker` と同型のハンドラ・priority=5・`Event.ON_TRY_ACTION` での実装方針を明記した。

## 実装（handlers/ / data/）
- **修正**: `かえんボール` はカノニカルな「使用宣言時に自身のこおりを無条件解凍して攻撃する」技
  （フレアドライブ・かえんぐるま・もえつきる等と同種）だが、`Event.ON_DAMAGE_HIT` の
  やけど付与ハンドラ（`かえんボール_apply_burn_to_defender`）のみが実装されており、
  使用者自身のこおり解凍処理が欠落していた。
  - `handlers/move_attack.py` に `かえんボール_thaw_attacker` を新規追加
    （`かえんぐるま_thaw_attacker` と同一パターン）。
  - `data/move.py` の `"かえんボール"` エントリに
    `Event.ON_TRY_ACTION: h.MoveHandler(ha.かえんボール_thaw_attacker, priority=5)` を追加。
  - `scripts/sort_handlers.py src/jpoke/handlers/move_attack.py` と
    `scripts/sort_data/sort_moves.py` を実行済み。
- 威力120・命中90・PP5・タイプほのお・分類物理・弾のわざ（bullet）フラグは実装済みで
  一次情報と一致しており変更なし。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」「リーサルテスト」列は `n/a`）。

## テスト
- `tests/moves_attack/test_move_ka.py` に
  `test_かえんボール_こおり状態で使うと解凍されて攻撃できる` を新規追加
  （`test_かえんぐるま_こおり状態で使うと解凍されて攻撃できる` と同型）。
- 既存の `test_かえんボール_やけどが発動する`（10%やけど付与の確認）は変更なし。
- `scripts/sort_tests.py tests/moves_attack/test_move_ka.py` と
  `scripts/generate_test_list.py` を実行し、`python -m pytest tests/ -v` で全件（3263 passed,
  1 skipped）を確認済み（ログ: `.loop/test_logs/かえんボール.log`）。

## 進捗表
- `.internal/progress/move.md` の該当行を更新:
  - 仕様書: `-` → `x`
  - 計画書: `-` → `x`
  - レビュー: `-` → `✓`
  - 威力: `90` → `120`（一次情報と不一致だった誤記を修正）
  - 命中率: `100` → `90`（同上）
  - PP: `16` → `5`（同上、`かえんほうしゃ` の値が誤って転記されていたと推測）
  - 効果: `?` → 「追加効果として10%の確率で相手を『やけど』状態にする。使用者がこおり状態の場合、こおりを治してから攻撃する。」
