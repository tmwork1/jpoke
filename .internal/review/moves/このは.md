# このは レビュー結果

## 仕様書
- 対象外（一次情報を確認した結果、追加効果なしの通常攻撃技であり、仕様書・計画書の作成が必要な特殊挙動が存在しないため作成しなかった。他の同種の技（`10まんばりき` 等）と同様に `-` のままとする）

## 実装計画書
- 対象外（上記と同じ理由）

## 実装（handlers/ / data/）
- 変更なし（`src/jpoke/data/move.py` の `このは` エントリはタイプ・分類・威力・命中率・PP・ハンドラなし、いずれも一次情報（くさ/物理/40/100/40/追加効果なし）と一致していることを確認した）

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行は `n/a`。追加効果がなくダメージ計算のみのため、既存の汎用リーサル計算で問題ない）

## テスト
- `tests/moves_attack/test_move_ka.py` に `test_このは_相手にダメージを与える` を追加（このはを使用した際に相手にダメージが入ることを確認する基本テスト。既存の `10まんばりき`・`こおりのつぶて` 等の追加効果なし技のテストパターンに準拠）
- `python scripts/sort_tests.py tests/moves_attack/test_move_ka.py` でソート
- `python scripts/generate_test_list.py` でテスト一覧を更新
- `python -m pytest tests/ -v` で全件実行し、3350 passed, 1 skipped を確認（ログ: `.loop/test_logs/このは.log`）
