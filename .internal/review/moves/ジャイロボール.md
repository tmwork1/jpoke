# ジャイロボール レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ジャイロボール.md`）。
  一次情報（`.internal/wiki/moves/ジャイロボール.html`）と対義技エレキボールの仕様書を参考に、
  基本情報・威力計算式（floor(25×相手の実効素早さ÷自分の実効素早さ)+1、最大150、
  自分の実効素早さ0のとき威力1固定）・判定フラグ・リーサル計算の対象外理由を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ジャイロボール.md`）。
  既存実装が仕様書と一致していることを確認した結果と、PP値の修正内容を記録した。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_attack.py` の `ジャイロボール_calc_power` は威力計算式・
  実効素早さの取得方法（`calc_effective_speed()`）ともに仕様書と一致しており、修正不要だった。
- `src/jpoke/data/moves/move_sa.py` の `MoveData` の `pp` が `5`（本家基準の値）になっていたが、
  `src/jpoke/data/ps-champ-ja/moves.json` の値（8）と食い違っていたバグのため `8` に修正した。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装/リーサルテスト列が n/a）。

## テスト
- 変更なし。`tests/moves_attack/test_move_sa.py` の既存4テスト
  （威力上限150、速い/遅い攻撃側の威力比較、まひによる実効素早さ半減時の威力増加）を
  仕様・実装と照合した結果、過不足がなかったため追加・修正は行わなかった。
  PP修正（5→8）はテストの検証内容（威力のみ）に影響しないことを確認した。
