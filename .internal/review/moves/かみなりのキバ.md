# かみなりのキバ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/かみなりのキバ.md`）。一次情報（`.internal/wiki/moves/かみなりのキバ.html`）を確認し、
  タイプ・分類・威力65・命中95・PP15、10%の確率でまひ、10%の確率でひるみ（それぞれ独立判定）、
  かみつき技（がんじょうあご対象）である点を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/かみなりのキバ.md`）。既存実装（`data/move.py` / `handlers/move_attack.py`）が
  仕様と一致していることを確認し、ハンドラ構成・priority根拠・エッジケース（がんじょうあご、ちからずく、
  おうじゃのしるしとの相互作用）を記録した。

## 実装（handlers/ / data/）
- 変更なし。`data/move.py` の `"かみなりのキバ"` エントリ（type=でんき, category=physical, power=65,
  accuracy=95, pp=15, flags={"bite","contact","secondary_effect"}）、`handlers/move_attack.py` の
  `かみなりのキバ_apply_paralysis_to_defender`（10%まひ）・`かみなりのキバ_apply_flinch`（10%ひるみ）は
  いずれも仕様どおりで修正不要と確認した。
- `handlers/item.py` の `_INNATE_FLINCH_MOVES` に「かみなりのキバ」が既に含まれており、おうじゃのしるし・
  するどいキバによるひるみ追加効果の重複が正しく防止されていることも確認した。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が n/a）。

## テスト
- 変更なし。`tests/moves_attack/test_move_ka.py` に既存の
  `test_かみなりのキバ_まひが発動する` / `test_かみなりのキバ_ひるみが発動する` があり、
  `secondary_chance=1.0` でそれぞれの追加効果付与を確認済み。がんじょうあご・ちからずくとの
  相互作用は既存の汎用テスト（`tests/abilities/test_ability_ka.py` の
  `test_がんじょうあご_かみつき技で威力補正1_5倍`、`tests/abilities/test_ability_ta.py` の
  ちからずく関連テスト）でかみつき技・追加効果技全般として網羅されているため追加不要と判断した。
- `python -m pytest tests/ -v` で全3281件（+skip 1件）がパスすることを確認した。
