# こわいかお レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/こわいかお.md`）。.internal/wiki/moves/こわいかお.html を一次情報として、
  タイプ・分類・命中率・PP・対象・判定フラグ・ランク低下無効化との相互作用を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/こわいかお.md`）。ハンドラ構成・priority根拠（`ON_STATUS_HIT` は
  `.internal/spec/turn.md` に記載なしのため既存の同種ハンドラに準拠）・実装箇所・PP修正内容を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ka.py`: こわいかお の `pp` が本家基準の `10` のまま実装されていたため、
  `src/jpoke/data/ps-champ-ja/moves.json`（12）に合わせて `pp=12` に修正した。
- `src/jpoke/handlers/move_status.py`: `こわいかお_modify_defender_stats` にドキュストリングを追加した
  （効果自体・`modify_defender_stats(stats={"spe": -2})` の呼び出しは仕様どおりで変更なし）。

## リーサル計算
- 対象外（ダメージを与えない変化技のため `.internal/progress/move.md` の「リーサル実装」は n/a）。

## テスト
- `tests/moves_status/test_move_ka.py` にテストが存在しなかったため新規追加した:
  - `test_こわいかお_defenderのすばやさが2段階下がる`: 相手のすばやさランクが2段階下がることを確認
  - `test_こわいかお_まもるで防がれる`: まもる状態の相手にはすばやさランクが変化しないことを確認
- `python -m pytest tests/ -v` で全4150件（1件skip）がパスすることを確認した
  （`.loop/test_logs/こわいかお.log` に保存。実行中に無関係な `test_むしくい_ねんちゃく持ちを
  ひんしにさせた場合は奪える` が一度だけフレークしたが、これは麻痺の完全麻痺確率に起因する
  既存の非決定的テストであり、こわいかお の変更とは無関係。単体再実行およびフルスイート再実行で
  パスすることを確認済み）。

## 進捗表
- `.internal/progress/move.md` の こわいかお 行を更新した:
  - タイプ列の誤り「ゴースト」を「ノーマル」に修正
  - 威力列の誤り「90」を「-」に修正（変化技のため威力なし）
  - 仕様書・計画書・レビュー列を `x` / `x` / `✓` に更新
  - 効果列の「？」を「相手の『すばやさ』ランクを2段階下げる。」に更新
