# どろかけ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/どろかけ.md`）。じめん/特殊/威力20/命中100/PP12/追加効果として
  100%の確率で相手の命中率を1段階下げる、という内容で記載。PPはポケモンWiki記載の本家値(10)ではなく
  `src/jpoke/data/ps-champ-ja/moves.json`（PP=12）を優先して採用した。

## 実装計画書
- 新規作成（`.internal/plan/moves/どろかけ.md`）。既存ハンドラ `どろかけ_lower_acc` の実装内容と
  `data/move.py` の `pp` 値の修正点を記載。

## 実装（handlers/ / data/）
- `handlers/move_attack.py` の `どろかけ_lower_acc` は変更なし（`modify_defender_stats` を用いた
  100%命中率-1の実装で仕様と一致）。
- `data/move.py` の `どろかけ` エントリの `pp` を `10` → `12` に修正
  （`src/jpoke/data/ps-champ-ja/moves.json` および `.internal/progress/move.md` の値と一致させた）。
  `scripts/sort_data/sort_moves.py` を実行済み（並び替え自体の変更なし）。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「どろかけ」行はリーサル実装/リーサルテストともに n/a）。

## テスト
- `tests/moves_attack/test_move_ta.py` の `test_どろかけ_命中率1段階低下が発動する` は既存のまま
  問題なし（変更なし）。
