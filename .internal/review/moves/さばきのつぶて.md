# さばきのつぶて レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/さばきのつぶて.md`）。
- 一次情報（ポケモンWiki）をもとに、タイプ・分類・威力・命中率・PP・対象・優先度・接触判定・
  判定フラグ（急所/命中判定/追加効果/まもる/おうじゃのしるし/マジックコート/オウムがえし）を整理。
- 効果として「持たせたプレートに応じてタイプが変わる（プレートなし・アイテム無効化中はノーマル固定）」
  「テラスタルの影響を受けない」を明記。

## 実装計画書
- 新規作成（`.internal/plan/moves/さばきのつぶて.md`）。
- 特性マルチタイプの `PLATE_TO_TYPE` テーブルを再利用する方針、
  `ON_MODIFY_MOVE_TYPE` ハンドラの priority 根拠（既存の同種ハンドラのデフォルト値踏襲）を記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/move.py` の `さばきのつぶて` エントリは登録済みだったが、
  `handlers={}` のままでタイプ変化効果が未実装だったため修正した。
- `src/jpoke/handlers/move_attack.py` に `さばきのつぶて_modify_move_type` を新規実装し、
  `Event.ON_MODIFY_MOVE_TYPE` として登録した。
  - `ctx.attacker.item.name`（アイテム無効化中は空文字を返す）を用いて `PLATE_TO_TYPE` を参照し、
    該当すればタイプを変更、該当しなければ初期値（ノーマル）のまま。
  - `jpoke.data.signature_items` から直接 `PLATE_TO_TYPE` をimportし、循環importを回避
    （`handlers/ability.py` と同じパターン）。
- `python scripts/sort_handlers.py src/jpoke/handlers/move_attack.py` と
  `python scripts/sort_data/sort_moves.py` を実行済み。

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行は「リーサル実装: n/a」「リーサルテスト: n/a」）。

## テスト
- `tests/moves_attack/test_move_sa.py` に以下を追加。
  - `test_さばきのつぶて_せいれいプレートでフェアリータイプになる`
  - `test_さばきのつぶて_プレートなしはノーマルタイプのまま`
  - `test_さばきのつぶて_マジックルーム中はプレートを持っていてもノーマルタイプになる`
- `python scripts/sort_tests.py tests/moves_attack/test_move_sa.py` でソート、
  `python scripts/generate_test_list.py` でテスト一覧を更新。
- `python -m pytest tests/ -v` で全3354件パス（1件skip）を確認
  （ログ: `.loop/test_logs/さばきのつぶて.log`）。
