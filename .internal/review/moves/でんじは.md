# でんじは レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/でんじは.md`）
- 一次情報（ポケモンWiki）を調査した結果、でんじはは変化技でありながら例外的にタイプ相性の
  影響を受け、じめんタイプの相手には無効になる（でんき技がじめんタイプに0倍のため）ことが判明。
  ねらいのまと所持時はこの無効化が解除される点も明記した。

## 実装計画書
- 新規作成（`.internal/plan/moves/でんじは.md`）
- じめんタイプ無効チェックを `ON_BEFORE_APPLY_MOVE`（priority=130、既存の
  `やどりぎのタネ_can_apply` と同priority）で実装する方針を記載

## 実装（handlers/ / data/）
- 既存実装は `ON_STATUS_HIT` で `ailment_manager.apply(defender, "まひ")` を呼ぶのみで、
  でんきタイプへのまひ無効（汎用チェック）はカバーされていたが、
  でんじは固有の「じめんタイプに無効」というタイプ相性判定が欠落していたため追加した。
  - `handlers/move_status.py` に `でんじは_can_apply` を新規実装
    （`battle.damage_calculator.calc_def_type_modifier(ctx) == 0` ならMOVE_IMMUNEDを記録して失敗）
  - `data/moves/move_ta.py` の `"でんじは"` エントリに
    `Event.ON_BEFORE_APPLY_MOVE: h.MoveHandler(hs.でんじは_can_apply, priority=130)` を追加
  - PP=20・命中率90は `src/jpoke/data/ps-champ-ja/moves.json` と一致しており変更なし
  - `subject_spec` は明示なし（MoveHandlerデフォルトの `attacker:self`）で、`target="foe"`
    のデフォルト挙動によりまもる・マジックコートは正しく効く想定（target=self化のバグなし）

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」列が `n/a`）

## テスト
- `tests/moves_status/test_move_ta.py` に4件追加
  - `test_でんじは_じめんタイプには無効`（新規実装のタイプ相性判定を検証）
  - `test_でんじは_でんきタイプには無効`（既存の汎用まひ無効チェックを検証）
  - `test_でんじは_ねらいのまとでじめんタイプへの無効化が解除される`（ねらいのまと連携を検証）
  - `test_でんじは_まひ付与`（基本のまひ付与を検証）
- `python -m pytest tests/ -v` を実行し、4255 passed, 1 skipped で全件成功を確認
  （実行順によりまれに無関係なテスト（`むしくい`・`てんねん`・`オーロラベール`関連等）が
  一時的な乱数状態リークでflakyに失敗することがあるが、`git stash` で本変更を除いた状態でも
  同様に再現するため、本レビューの変更とは無関係な既存の問題と判断した）
