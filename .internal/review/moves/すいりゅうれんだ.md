# すいりゅうれんだ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/すいりゅうれんだ.md`）。
  - 一次情報（ポケモンWiki）をもとに、基本情報・3回固定連続攻撃・確定急所（急所ランク+3）・
    パンチ技フラグ・急所無効化特性（カブトアーマー・シェルアーマー）との関係を記載。

## 実装計画書
- 新規作成（`.internal/plan/moves/すいりゅうれんだ.md`）。
  - 既存実装（`data/move.py`）が仕様と一致していることを確認し、固有ハンドラを持たない旨、
    `critical_rank` / `multi_hit` の汎用機構で処理されることを明記。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の既存定義（`critical_rank=3`, `multi_hit={min:3, max:3,
  check_hit_each_time:False, power_sequence:()}`, `flags={"contact","punch"}`, `handlers={}`）は
  仕様書・計画書と一致しており、修正不要と判断。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装・リーサルテストは `n/a`）。

## テスト
- `tests/moves_attack/test_move_sa.py` に以下を追加:
  - `test_すいりゅうれんだ_3回固定でヒットする`
  - `test_すいりゅうれんだ_必ず急所に当たる`
  - `test_すいりゅうれんだ_相手にダメージを与える`
- `scripts/sort_tests.py` でソート、`scripts/generate_test_list.py` でテスト一覧を更新。
- `python -m pytest tests/ -v` を実行し、全3412件パス（1件skip、既存分）を確認
  （`.loop/test_logs/すいりゅうれんだ.log`）。
