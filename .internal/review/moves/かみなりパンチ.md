# かみなりパンチ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/かみなりパンチ.md`）
  - `.internal/wiki/moves/かみなりパンチ.html` を参照し、基本情報（でんき/物理/威力75/命中100/PP15/直接攻撃/パンチ技）、
    追加効果（10%の確率でまひ）、てつのこぶしによる威力補正、判定フラグを記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/かみなりパンチ.md`）
  - 本技は既存の一括計画書 `.internal/plan/moves/攻撃技_まひ系.md` に含まれていたため、その内容から本技に関する部分を抜粋・整理した。
  - 実際の実装が `Event.ON_HIT` ではなく `Event.ON_DAMAGE_HIT` に登録されている点について、
    `でんじほう`・`ほっぺすりすり`・`かみなりのキバ` 等の既存の同種ハンドラと揃えた登録であることを確認し、計画書に根拠として明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/move_attack.py` の `かみなりパンチ_apply_paralysis_to_defender`（`apply_ailment_to_defender` を使用、chance=0.1）、
  `src/jpoke/data/move.py` の `MoveData`（type="でんき", category="physical", pp=15, power=75, accuracy=100,
  flags={"contact", "punch", "secondary_effect"}, handlers={Event.ON_DAMAGE_HIT: ...}）が
  仕様書・一次情報と一致していることを確認した。
  パンチ技の威力補正（てつのこぶし）は `flags={"punch"}` を通じて特性側ハンドラが汎用的に処理するため、本技側の実装は不要であることも確認した。

## リーサル計算
- 対象外（`n/a`）。まひは行動阻害・素早さ低下効果でありダメージ量そのものには影響しないため、専用のリーサルハンドラは不要と判断した。
  `tests/test_lethal.py` の `test_アッキのみ_ちからずくの対象技では発動しない` では、かみなりパンチを「物理・追加効果あり技」の例として利用しているのみで、
  本技固有のリーサル計算とは無関係であることを確認した。

## テスト
- 変更なし。`tests/moves_attack/test_move_ka.py` の `test_かみなりパンチ_まひが発動する`（`secondary_chance=1.0` で10%効果を確定発動させ、まひ付与を確認）、
  `tests/abilities/test_ability__group.py` の `test_技カテゴリによる威力補正_param`（てつのこぶし所持時に威力4915（75×1.2）へ補正されることを確認）が
  実装と仕様書に一致していることを確認した。
  `python -m pytest tests/ -v` で3281件パス（1件skip）を確認した。
