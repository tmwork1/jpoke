# ないしょばなし レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ないしょばなし.md`）
- 一次情報（`.internal/wiki/moves/ないしょばなし.html`）と `src/jpoke/data/ps-champ-ja/moves.json` を照合。
  `move_list.txt` には「ないしょばなし」単独の行が存在しないが、`ちょうおんぱ`・`なきごえ`・
  `エコーボイス` 等の他の一般的な音技も同様に単独項目を欠いていることを確認し、一覧のスクレイピング漏れと
  判断。第九世代の値（PP20・必中）をそのまま採用する方針を明記した

## 実装計画書
- 新規作成（`.internal/plan/moves/ないしょばなし.md`）
- `unprotectable` フラグの必要性・根拠、`sound` フラグによるみがわり貫通の仕組み、ハンドラ構成を明記

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_na.py`: `ないしょばなし` のバグを修正
  - `flags={"sound"}` → `flags={"sound", "unprotectable"}` に修正
    （一次情報の「技の仕様」節に「まもる状態やダイウォールを無視する。」と明記されているにもかかわらず、
    `unprotectable` フラグが欠落しており、まもる状態の相手に技が通らないバグがあった）
  - `accuracy=None,  # 必中` を明示的に追加（挙動自体はデフォルト値と同じで変化なしだが、
    他の必中技（`とおせんぼう` 等）の記述規約に合わせて明示化した）
  - `handlers/move_status.py` の `ないしょばなし_modify_defender_stats` 自体は仕様と一致しており変更なし

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）

## テスト
- `tests/moves_status/test_move_na.py` に `test_ないしょばなし_まもる状態の相手にも効果が発動する` を追加
  （`unprotectable` フラグ修正の回帰テスト。`とおせんぼう` の同種テストと同じパターン）
- 既存の `test_ないしょばなし_とくこうが1段階下がる` / `test_ないしょばなし_みがわりを貫通してとくこうが下がる`
  は変更なし
- `python -m pytest tests/ -v` で全件パス（4280 passed, 1 skipped）を確認
