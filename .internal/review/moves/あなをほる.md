# あなをほる レビュー結果

## 仕様書
- 変更なし（`.internal/spec/moves/あなをほる.md` は一次情報（wikiダンプ）と一致しており、既存内容に誤り・欠落は見つからなかった）

## 実装計画書
- 新規作成（`.internal/plan/moves/あなをほる.md` が存在しなかったため、`.internal/spec/moves/あなをほる.md` と `.internal/spec/volatiles/あなをほる.md`、既存実装・テストを基に新規作成した。内容は既に実装済みの `そらをとぶ` の計画書の構成に準拠）

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `あなをほる` エントリ（じめん・物理・威力80・命中100・PP10・`ON_MOVE_CHARGE` で `charge_into_volatile` 呼び出し）と、`src/jpoke/data/volatile.py` の `あなをほる` 揮発状態（交代禁止・コマンド強制・威力2倍・回避判定・揮発状態解除の5ハンドラ）、`src/jpoke/handlers/volatile.py` の `あなをほる_boost_power` / `あなをほる_can_hit_hidden_target` / `あなをほる_remove_volatile` / `HIDDEN_MOVE_ALLOWED_MOVES["あなをほる"]` は、すべて仕様書・計画書と一致しており修正不要だった。

## テスト
- 変更なし。`tests/moves_attack/test_move_a.py`（2ターン通しのダメージ確認、じしん命中・威力2倍、じしん/マグニチュード以外は命中しない、通常技回避、マグニチュード関連は未実装のため `pytest.mark.skip`）と `tests/volatiles/test_volatile_a.py` / `tests/volatiles/test_volatile_ka.py`（付与・コマンド固定・PP消費なし・交代禁止・特定技命中・回避のパラメトライズテスト）ですでに網羅的にカバーされていたため、追加・修正は行わなかった。

## 進捗表
- `.internal/progress/move.md` の `あなをほる` 行を修正した：タイプ「ノーマル」→「じめん」、威力「?」→「80」、PP「12」→「10」、効果列の説明文を実装内容に基づいて具体化、レビュー列を「✓」に更新。

## その他
- `python -m pytest tests/` 実行時、`tests/test_megaevol.py::test_メガシンカ_フォルムが変わる` の2件が `KeyError: 'うなぎのぼり'` / `KeyError: 'ほのおのたてがみ'` で失敗した。これは並行して走っている他レビュータスクによる `ABILITIES` データの一時的な未整合が原因であり、`あなをほる` の実装・テストとは無関係。`あなをほる` 関連のテスト（7件、うち1件はマグニチュード未実装のためskip）はすべてPASSしている。
