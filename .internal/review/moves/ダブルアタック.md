# ダブルアタック レビュー結果

## 仕様書
- 変更なし。
- ダブルアタックは「固定2回攻撃技」であり、`.internal/spec/moves/連続技.md`（9.1節）に
  タイプ・分類・威力・命中・PP・直接攻撃の有無・効果が一覧として記載済み。
  追加効果を持たない兄弟技（ダブルウイング等）も個別仕様書を持たない運用のため、
  個別ファイルは新規作成せず既存の共通仕様書を正とした。
- 一次情報（`.internal/wiki/moves/ダブルアタック.html`）と照合し、タイプ=ノーマル、
  分類=物理、威力=35、命中=90、対象=1体選択、優先度=0、直接攻撃=○、
  効果=「2回連続攻撃をする(連続攻撃技)」で共通仕様書の記載と整合していることを確認した
  （PPは本家基準では10だが、後述の通りチャンピオンズ基準では12）。

## 実装計画書
- 対象外（個別計画書なし、共通計画書 `.internal/plan/moves/攻撃技_連続技系.md` の
  「通常型・固定型（追加効果なし）」パターンをそのまま適用する運用のため、
  兄弟技（ダブルウイング等）と同様に新規作成しなかった）。

## 実装（handlers/ / data/）
- 修正: `src/jpoke/data/moves/move_ta.py` の `"ダブルアタック"` エントリの
  `pp` が `10`（Gen9本家基準）になっていたバグを `12` に修正した。
  `src/jpoke/data/ps-champ-ja/moves.json`（466行目: `ダブルアタック ノーマル 物理 35 90 12 接○ 守○`）
  および `.internal/progress/move.md` のPP列が正しく `12` であるのに対し、実装側が
  移行漏れで本家基準の値のままだったもの（`ダブルウイング` で過去に発見された
  同種のバグと同じパターン）。
- その他のフィールド（type/category/power/accuracy/flags/multi_hit）は
  仕様と一致しており変更なし。

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行はリーサル実装・リーサルテストともに `n/a`）。

## テスト
- 追加: `tests/moves_attack/test_move_ta.py` に
  `test_ダブルアタック_タイプ分類威力命中PPが仕様通り` を新規追加し、
  type/category/power/accuracy/pp/flags/multi_hit の各フィールドを検証。
  PP修正のリグレッション防止のため追加（`ダブルウイング` の既存テストと同一パターン）。
- 既存の `tests/moves_attack/test_move__group.py::test_ダブルアタック_2回ヒットする`、
  `tests/items/test_item_ka.py`（かいがらのすず）、`tests/items/test_item_a.py`（いのちのたま）、
  `tests/items/test_item_yarawa.py`、`tests/abilities/test_ability_ha.py`、
  `tests/moves_attack/test_move_ka.py` のダブルアタック関連テストは
  実装と整合しており変更なし。
- `python -m pytest tests/ -v` は全件パス（4068 passed, 1 skipped）。
  結果は `.loop/test_logs/ダブルアタック.log` に保存。
