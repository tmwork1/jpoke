# ヒートスタンプ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ヒートスタンプ.md`）。
  - 一次情報（wiki）を基に、自分/相手の体重比率による威力段階表（40/60/80/100/120）、
    「ちいさくなる」相手への必中・威力2倍の相互作用、判定フラグ、リーサル計算対象外の理由を明記。

## 実装計画書
- 新規作成（`.internal/plan/moves/ヒートスタンプ.md`）。
  - 既存実装（`ヒートスタンプ_calc_power` / 共通関数 `_weight_ratio_to_power`）の威力計算式が
    仕様と一致していることを確認。
  - `MoveData` の PP が `pp=10`（Gen9本家基準）になっており、`src/jpoke/data/ps-champ-ja/moves.json`
    （PP=12）と食い違っていることを検出し、修正方針を記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `"ヒートスタンプ"` エントリを `pp=10` → `pp=12` に修正
  （`src/jpoke/data/ps-champ-ja/moves.json` 準拠。移行漏れバグ）。
- `src/jpoke/handlers/move_attack.py` の `ヒートスタンプ_calc_power` と共通関数
  `_weight_ratio_to_power` は仕様と一致しており、修正不要（ヘビーボンバーと共用の実装で、
  威力段階・ちいさくなる状態でのスキップ処理とも正しい）。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装/リーサルテスト列は n/a）。
  威力は自分・相手双方の体重という固定パラメータにのみ依存し、相手のHP分布
  `StateDist` を左右する要素ではないため、専用のリーサルハンドラは不要。

## テスト
- 変更なし。`tests/moves_attack/test_move_ha.py` に威力段階の境界（比率2未満/2以上3未満/
  3以上4未満/4以上5未満/5以上）を確認するテストと、ちいさくなる状態でのスキップ動作を
  確認するテストが既に一通り揃っており、仕様と実装に一致していることを確認した。
  `tests/volatiles/test_volatile_satana.py` の `minimize_enhance_moves` にもヒートスタンプが
  含まれ、必中化の共通テストでカバーされている。
- `python -m pytest tests/ -v` を実行し、3963 passed, 1 skipped（既存のskipで無関係）で全件成功。
