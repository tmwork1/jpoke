# げんしのちから レビュー結果

## 仕様書
- 変更なし（新規作成せず）。一次情報（`.internal/wiki/moves/げんしのちから.html`）によると
  げんしのちからは特殊・いわタイプ・威力60・命中率100・PP5・対象1体選択で、
  追加効果として10%の確率で自分のこうげき・ぼうぎょ・とくこう・とくぼう・すばやさを
  各1段階上げる、という単一の効果のみを持つ。同種の効果を持つあやしいかぜ・ぎんいろのかぜ
  も含め、進捗表で仕様書列を「-」として扱う運用に統一されているため、げんしのちからに
  ついても専用の仕様書ファイルは作成しない（`.internal/spec/moves/_move_secondary_1.md` の
  attacker系追加効果一覧に `げんしのちから attacker 0.1 1 1 1 1 1 ...`
  （A/B/C/D/S全て+1、確率10%）として記載済みで、一次情報と一致することを確認した）。

## 実装計画書
- 変更なし（新規作成せず）。上記と同じ理由により、専用の計画書ファイルは作成しない。
  `.internal/plan/moves/攻撃技_攻撃側ランク変化系.md` に、げんしのちからを含む全能力+1系技
  （あやしいかぜ・げんしのちから・ぎんいろのかぜ）の実装方針（`Event.ON_HIT`、
  `attacker:self`、`modify_attacker_stats` を使用、priorityは既存パターンに合わせて
  MoveHandlerデフォルトの100を使用）が記載されており、実装済みの内容と一致することを
  確認した。

## 実装（handlers/ / data/）
- `src/jpoke/data/move.py` の `"げんしのちから"` エントリ（type="いわ", category="special",
  pp=5, power=60, accuracy=100, flags={"secondary_effect"}, Event.ON_HIT）を一次情報と
  照合し、誤りがないことを確認した。修正なし。
- `handlers/move_attack.py` の `げんしのちから_boost_all_stats` は共通ヘルパー
  `modify_attacker_stats` を用いて
  `stats={"atk": 1, "def": 1, "spa": 1, "spd": 1, "spe": 1}, chance=0.1` を渡しており、
  一次情報の「10%の確率で自分のすべての能力が1段階ずつ上がる」と一致していることを確認した。
  `Event.ON_HIT`・`attacker:self`（`MoveHandler` のデフォルト）での登録も仕様と整合している。
  `flags={"secondary_effect"}` によりせいしんりょく・ちからずく・くろいヘドロ等の
  既存の追加効果系割り込みハンドラとも正しく連動することを確認した。修正なし。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装・リーサルテスト列は n/a）。

## テスト
- `tests/moves_attack/test_move_ka.py` の `test_げんしのちから_全能力1段階上昇が発動する` が
  `secondary_chance=1.0` で確率を固定し、A/B/C/D/S全てのランクが+1になることを確認しており、
  実装と整合していることを確認した。追加のテストは不要と判断した。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ka.py` を実行したが差分なし。
- `python scripts/generate_test_list.py` を実行したが `げんしのちから` 関連の差分なし。
- `python -m pytest tests/ -v` を実行し、3328 passed, 1 skipped で全件成功を確認した
  （ログ: `.loop/test_logs/げんしのちから.log`）。

## 進捗表（`.internal/progress/move.md`）
- レビュー列を `-` から `✓` に更新した。他の列（仕様書・計画書・実装・テスト・
  リーサル実装・リーサルテスト・分類・タイプ・威力・命中率・PP・対象・効果）はすべて
  実際の状態と一致していたため変更なし。
