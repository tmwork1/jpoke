# にほんばれ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/にほんばれ.md`）
  - タイプ・分類・PP（8、champions/move_list.txt準拠）・対象（全体の場）・効果（5ターンの間、天候をはれにする）を記載
  - はれ自体の一般効果（ほのお技1.5倍/みず技半減/こおり無効化）は `.internal/spec/fields/はれ.md` を参照する形にし、本技固有の実装は不要である旨を明記
  - あついいわによる持続ターン延長（5→8）は item 側の一般処理である旨を明記
  - Zワザ付加効果はポケモンチャンピオンズのルール対象外として明記

## 実装計画書
- 修正した（`.internal/plan/moves/にほんばれ.md`）
  - PP を 5 → 8 に修正（champions/move_list.txt 準拠）
  - 「現状の実装状況」を実態に合わせて修正（`data/moves/move_na.py` に handlers 登録済みだった旨に更新。
    旧記述は「登録済み（handlers 登録なし）」という誤った記載だった）
  - Priority 根拠に「天気変更技: おおひでり等で失敗」（turn.md priority 100）も追記
  - 実装例のコードブロックを実際のファイルパス・内容（`data/moves/move_na.py`、`category="status"`、pp=8）に更新
  - 注意点にあついいわとの相互作用（item側で対応済み、本技側の実装不要）を追記

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_na.py` の `"にほんばれ"` エントリの `pp` を 5 → 8 に修正
  （champions/move_list.txt の値と不一致だったバグ。他の天候始動技 `あまごい`・`すなあらし` は
  いずれも pp=8 で統一されていた）
- `handlers/move_status.py` の `にほんばれ_activate_weather` はロジック自体は正しく、修正なし

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）

## テスト
- 既存テスト（`tests/moves_status/test_move_na.py::test_にほんばれ_天気がはれになる`、
  `tests/moves_status/test_move__grouped.py` の天候始動技パラメタライズテスト群）で
  発動・重複失敗・強天候下での失敗が既にカバーされており、過不足なしと判断（変更なし）
- はれ状態下でのほのお/みず技威力補正・こおり無効化は `tests/test_field.py` で一般仕様として
  既にテスト済み（変更なし）
