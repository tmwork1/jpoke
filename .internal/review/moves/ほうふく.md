# ほうふく レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ほうふく.md`）。
  - `.internal/wiki/moves/ほうふく.html`（一次情報）と `.internal/spec/moves/固定ダメージ.md`（4.3 メタルバースト・ほうふく）を参照して作成。
  - 基本情報（あく / 物理 / 威力- / 命中100 / PP10 / 優先度0 / 接触あり）、効果、判定フラグ、
    特性・道具との相互作用（プレッシャー、よちむ、せいぎのこころ・びびり、さきどり、ゆびをふる）、
    リーサル計算（対象外）を整理。
  - 副産物として `.internal/spec/moves/固定ダメージ.md` と `.internal/spec/moves/_fixed_damage.md`
    （内容が完全に重複した別名ファイル）に存在した「相手の直前の攻撃技ダメージ × 1.5」の
    2行重複記述を1行に修正。

## 実装計画書
- 新規作成（`.internal/plan/moves/ほうふく.md`）。
  - 既存の共通計画書 `.internal/plan/moves/攻撃技_反撃系.md`（カウンター・ミラーコート・ほうふく・メタルバースト）
    の内容をベースに、ほうふく単体の計画書として再構成。
  - ハンドラ構成表（`ON_TRY_MOVE_1` priority=30、`ON_MODIFY_MOVE_DAMAGE` デフォルトpriority）と
    priority根拠、実装済みコードの一致確認、タイプ無効化の扱い、リーサル対象外の理由を記載。

## 実装（handlers/ / data/）
- 変更なし。
  - `src/jpoke/handlers/move_attack.py` の `ほうふく_check_can_use` / `ほうふく_modify_damage`、
    `src/jpoke/data/moves/move_ha.py` の `"ほうふく"` 定義を仕様書・計画書と照合し、
    priority・subject_spec・固定ダメージ計算式（`int(last_damage_received * 1.5)`）・
    失敗判定（`last_damage_received <= 0`）がすべて一致していることを確認した。
  - タイプ無効化については `core/move_executor.py` の `_check_hit_by_type` が
    `ON_MODIFY_MOVE_DAMAGE` より前にタイプ相性0倍を判定して技自体を失敗させるため、
    固定ダメージ計算で無効化がバイパスされる不具合がないことを確認した
    （なお現行のタイプ相性表では、あくタイプの攻撃技が0倍になる防御タイプは存在しない）。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装・リーサルテスト列は `n/a`）。
  ほうふくのダメージ量は攻撃側が受けたダメージに依存するため、防御側HP分布の事前計算要素がない。

## テスト
- 追加: `test_ほうふく_タイプ相性の影響を受けない`（`tests/moves_attack/test_move_ha.py`）
  - フェアリータイプ（あくを0.5倍で受ける）の相手に対しても固定ダメージが半減されず、
    受けたダメージの1.5倍がそのまま通ることを確認するテスト。
  - 当初はエスパータイプへの無効化（0倍）を確認するテストを作成したが、
    実際のタイプ相性表では あく→エスパー は2.0倍（弱点）であり、
    あくタイプの攻撃技を0倍で無効化する防御タイプは存在しないことが判明したため、
    半減無効化（フェアリータイプ）を確認するテストに差し替えた。
- 既存テスト（`test_ほうふく_ダメージを受けていないとき失敗する`、
  `test_ほうふく_物理ダメージを受けた後に1_5倍を返す`、
  `test_ほうふく_特殊ダメージを受けた後も発動する`）は実装と整合していることを確認し、変更なし。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ha.py` でソート、
  `python scripts/generate_test_list.py` で一覧更新、
  `python -m pytest tests/ -v` で全3591件パス（1件skip）を確認
  （ログ: `.loop/test_logs/ほうふく.log`）。
