# えだづき レビュー結果

## 仕様書
- 新規作成（.internal/spec/moves/えだづき.md）。.internal/wiki/moves/えだづき.html を一次情報として、
  タイプ・分類・威力40・命中100・PP40・対象1体・優先度0・接触あり・追加効果なしを記載。

## 実装計画書
- 新規作成（.internal/plan/moves/えだづき.md）。追加効果を持たないため専用ハンドラは不要であることを明記し、
  既存の `data/move.py` エントリが仕様と一致していることを確認する内容とした。

## 実装（handlers/ / data/）
- 変更なし。既存の `data/move.py` の `"えだづき"` エントリ（type="くさ", category="physical",
  pp=40, power=40, accuracy=100, flags={"contact"}, handlers={}）は仕様と一致しており、
  接触フラグ・優先度0（デフォルト）・通常急所判定（デフォルト）もすべて仕様通りだった。

## リーサル計算
- 対象外（.internal/progress/move.md でリーサル実装列が n/a）。

## テスト
- 追加。tests/moves_attack/test_move_a.py に
  `test_えだづき_相手にダメージを与える`（追加効果なしの物理くさ技として、
  ダメージが発生することを確認する基本テスト）を新規追加。
- scripts/sort_tests.py tests/moves_attack/test_move_a.py で並び替え、
  scripts/generate_test_list.py でテスト一覧を更新、
  python -m pytest tests/ -v で3251件成功・1件skip・5件失敗（.loop/test_logs/えだづき.log に保存）。
  失敗5件はいずれも tests/test_field.py の
  `test_オーロラベール_リフレクターとは重複しない` / `test_サイドフィールド_ダメージ軽減`
  （オーロラベール・リフレクター・ひかりのかべの damage_modifier 重複判定）であり、
  えだづき・本タスクの変更とは無関係の既存の不具合（test_field.py・damage.py はどちらも未変更）。
  えだづき自体のテストは単体実行で成功を確認済み。
