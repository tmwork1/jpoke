# だくりゅう レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/だくりゅう.md`）。
  - タイプ: みず / 分類: 特殊 / 威力90（第六世代以降）/ 命中85 / PP10 / 対象: 相手全員
    （シングルのため実質1体） / 優先度0 / 接触なし / `secondary_effect` フラグ。
  - 追加効果: 30%の確率で相手の命中率を1段階下げる。
  - おうじゃのしるし・するどいキバは本技の効果（ひるみでない）と重複せず常に別枠で判定される点を明記。
  - リーサル計算は対象外（命中率ランク低下のみでHPに影響しない）。

## 実装計画書
- 新規作成（`.internal/plan/moves/だくりゅう.md`）。
  既存実装（`data/move.py` の `"だくりゅう"` エントリ、`handlers/move_attack.py` の
  `だくりゅう_lower_acc`）は仕様と一致しており、実装差分はなし。

## 実装（handlers/ / data/）
- 変更なし。
  - `data/move.py`: `type="みず", category="special", pp=10, power=90, accuracy=85,
    flags={"secondary_effect"}` で仕様と一致。
  - `handlers/move_attack.py`: `だくりゅう_lower_acc` が
    `modify_defender_stats(battle, ctx, value, stats={"accuracy": -1}, chance=0.3)` を呼び出し、
    30%発動・命中率-1段階の仕様と一致。
  - `handlers/item.py` の `_INNATE_FLINCH_MOVES` に「だくりゅう」が含まれておらず、
    おうじゃのしるし等の10%ひるみ抽選が正しく独立して働くことを確認。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」列は `n/a`。命中率ランク低下のみで
  HP分布に影響しないため対象外のまま）。

## テスト
- 変更なし。`tests/moves_attack/test_move_ta.py` の
  `test_だくりゅう_命中率1段階低下が発動する` が仕様通りに命中率-1段階を検証しており、
  修正不要と確認。

## その他
- `.internal/progress/move.md` のだくりゅう行に誤りがあったため修正:
  - PP列: `12` → `10`（一次情報・実装ともに基本PPは10）。
  - 仕様書・計画書・レビュー列: `-` → `x` / `✓`（本レビューで新規作成・確認済みのため）。
