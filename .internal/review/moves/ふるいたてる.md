# ふるいたてる レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ふるいたてる.md`）。
- 一次情報（`.internal/wiki/moves/ふるいたてる.html`）に基づき、タイプ（ノーマル）・分類（変化）・
  威力/命中なし・対象（自分）・優先度0・直接攻撃なしを記載。
- PPは `src/jpoke/data/ps-champ-ja/moves.json` に本項目が存在しないため、本家PP30の自己ランク上昇技
  （きあいだめ・つるぎのまい・こうそくいどう等）がすべて champions PP20 に統一されている
  パターンを根拠にPP20と判断し記載した。
- 効果: 自分の『こうげき』『とくこう』を1段階ずつ上げる。

## 実装計画書
- 新規作成（`.internal/plan/moves/ふるいたてる.md`）。
- `.internal/spec/turn.md` に該当イベントの記載がないため、既存の同種ハンドラ（つるぎのまい・
  こうそくいどう等）に倣い `ON_STATUS_HIT` / priority=100（デフォルト） / `attacker:self`
  （デフォルト）を採用する根拠を明記。
- `target="self"` が必須である理由（いたずらごころ・おうごんのからだ・もらいび等の
  「相手対象の変化技」判定への誤反応防止）を明記。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_status.py` の `ふるいたてる_modify_attacker_stats` は仕様通り
  （`modify_attacker_stats` で atk/spa をそれぞれ+1）で変更なし。
- `src/jpoke/data/moves/move_ha.py` を修正:
  - PP を 30 → 20 に修正（champions のPP4段階統一に準拠。上記仕様書の根拠を参照）。
  - `target="self"` を追加（従来はデフォルトの `"foe"` のままになっており、いたずらごころの
    あくタイプ無効化・おうごんのからだ・もらいび・かぜのり等「相手対象の変化技」判定に
    誤反応するバグだった）。

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行は `n/a`）。ダメージを与えない自己バフ技のため
  リーサル計算ハンドラは不要。`handlers/lethal.py` にも該当エントリなしを確認。

## テスト
- 既存の2件（こうげき上限時のとくこう上昇、両ランク+1上昇）は仕様と一致しており変更なし。
- `target="self"` 修正の回帰防止として、
  `test_ふるいたてる_いたずらごころ持ちがあくタイプ相手でも成功する` を
  `tests/moves_status/test_move_fa.py` に追加（いたずらごころ+あくタイプ相手でも技が成功し
  両ランクが上昇することを確認）。
- `python -m pytest tests/ -v` で全4372件パス（1件skip）を確認。ログ:
  `.loop/test_logs/ふるいたてる.log`
