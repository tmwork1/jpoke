# みだれひっかき レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/みだれひっかき.md`）。
  - 一次情報（ポケモンWiki）をもとに、基本情報・2～5回可変連続攻撃・急所各ヒット独立判定・
    追加効果なしの旨を記載。

## 実装計画書
- 新規作成（`.internal/plan/moves/みだれひっかき.md`）。
  - 既存実装（`data/moves/move_ma.py`）が仕様と一致していることを確認し、固有ハンドラを持たない旨、
    `multi_hit`（min=2, max=5, power_sequence なし）の汎用機構で処理されることを明記。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ma.py` の既存定義（`type="ノーマル"`, `category="physical"`,
  `pp=15`, `power=18`, `accuracy=80`, `flags={"contact"}`,
  `multi_hit={min:2, max:5, check_hit_each_time:False, power_sequence:()}`, `handlers={}`）は
  仕様書・計画書と一致しており、修正不要と判断。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装・リーサルテストは `n/a`）。

## テスト
- `tests/moves_attack/test_move_ma.py` に以下を追加:
  - `test_みだれひっかき_複数ヒットする`（`_resolve_hit_count` が2〜5の範囲に収まることを確認）
  - `test_みだれひっかき_相手にダメージを与える`
- `scripts/sort_tests.py`、`scripts/generate_test_list.py` を実行してテスト一覧を更新。
- `python -m pytest tests/ -v` を実行し、3610件パス（1件skip、既存分）を確認
  （`.loop/test_logs/みだれひっかき.log`）。
