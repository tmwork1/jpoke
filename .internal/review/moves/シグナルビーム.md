# シグナルビーム レビュー結果

## 仕様書
- 一次情報（`.internal/wiki/moves/シグナルビーム.html`）を確認した結果、本技は「ソード・シールド」
  「BDSP」「スカーレット・バイオレット」で使用不可能なわざに分類されており（第9世代で新規に
  習得可能なポケモンが存在しない）、`src/jpoke/data/ps-champ-ja/moves.json` にも項目が存在しない。
  README.md の対象範囲定義に該当するため実装対象外と判断し、仕様書をその根拠を明記する形に
  書き直した（`.internal/review/moves/オクタンほう.md` 等と同じ前例）。

（統合ブランチへのマージ時の補足: 本エントリの初回レビューでは「対象範囲の判断は本レビューの
範囲外」として既存実装を維持する結論だったが、これはオクタンほう・グラスミキサー・
シャドーボーンで確立された「SVで使用不可能なわざは実装しない」という前例と矛盾していたため、
マージ時に前例に合わせて実装を削除する方針に修正した。）

## 実装計画書
- 「実装しない」という結論に書き直し、削除した実装内容を記録した。

## 実装（handlers/ / data/）
- 誤って登録されていた実装を削除した。
  - `src/jpoke/data/moves/move_sa.py`: `"シグナルビーム": MoveData(...)` エントリを削除
  - `src/jpoke/handlers/move_attack.py`: `シグナルビーム_apply_confusion_to_defender` ハンドラを削除
  - `src/jpoke/types/move.py`: `MoveName` Literal から `"シグナルビーム"` を削除
    （`generate_move_literal.py` で再生成）

## リーサル計算
- 対象外（実装しないため）。

## テスト
- `tests/moves_attack/test_move_sa.py` の `test_シグナルビーム_こんらんが発動する` を削除した。
- `python -m pytest tests/ -v` で全件成功を確認した。

## 進捗ファイル
- `.internal/progress/move.md` からシグナルビーム行を削除した（`MOVES` 辞書に存在しない技のため）。
