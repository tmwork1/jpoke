# りんごさん レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/りんごさん.md`）。既存の `.internal/spec/moves/_move_secondary_1.md`
  （追加効果一覧）には既に掲載済みだったが、個別仕様書がなかったため、一次情報
  （`.internal/wiki/moves/りんごさん.html`）と `src/jpoke/data/ps-champ-ja/moves.json` /
  `.internal/champions/changes_from_sv.md` を基に新規作成した。

## 実装計画書
- 新規作成（`.internal/plan/moves/りんごさん.md`）。既存実装（`Event.ON_DAMAGE_HIT` /
  `modify_defender_stats` / リーサルハンドラ）は仕様と一致しており変更不要だが、
  `MoveData` の `power` / `pp` に誤りがあったため、その修正内容を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ra.py` の `りんごさん` エントリを修正:
  - `power=80` → `90`（Champions では威力90。SVまでの値80のままになっていた）
  - `pp=10` → `12`（ChampionsではPPが8/12/16/20の4段階に統一されており、りんごさんは12。
    SVまでの値10のままになっていた）
- `handlers/move_attack.py` の `りんごさん_lower_defender_spd`（`Event.ON_DAMAGE_HIT` で
  `modify_defender_stats(..., stats={"spd": -1})`）は同種の `Gのちから_lower_defender_def`
  と同一パターンで、仕様と一致しており変更なし。

## リーサル計算
- 既存実装（`handlers/lethal.py` の `りんごさん_lower_spd`）を確認。
  `Gのちから_lower_def` と同一パターンで `ctx.move_secondary` が真のときのみ
  `ctx.defender.rank["spd"]` を1段階下げており、仕様・実装と整合しているため変更なし。

## テスト
- `tests/moves_attack/test_move_yarawa.py` の `test_りんごさん_とくぼう1段階低下が発動する`、
  `tests/test_lethal.py` の `test_りんごさん_とくぼうダウン_secondary有り` /
  `_secondary無し` を確認。今回の修正（威力・PP）はこれらのアサーションに影響しないため、
  変更なし。`python -m pytest tests/ -v` で全3642件（+skip 1件）がパスすることを確認。
