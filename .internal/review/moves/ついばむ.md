# ついばむ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ついばむ.md`）。
- 一次情報（.internal/wiki/moves/ついばむ.html）と照合し、タイプ（ひこう）・分類（物理）・威力60・命中100・PP20・
  対象1体・優先度0・接触ありを確認。
- 追加効果（相手のきのみを奪って自分が消費する）はむしくいと全く同一であることを明記し、
  ねんちゃく・タンガのみ（ダメージ前消費型きのみ）・みがわり貫通などのエッジケースを記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ついばむ.md`）。
- `src/jpoke/data/move.py` の既存登録内容（`Event.ON_HIT` に `ha.むしくい_steal_and_use_berry` を登録）を
  仕様書と照合し、一致していることを確認。専用ハンドラの新規実装は不要と結論づけた。
- priority根拠は `.internal/spec/turn.md` の `Event.ON_HIT`（ダメージ発生後の処理、みがわりに被弾しても発動）を参照。

## 実装（handlers/ / data/）
- 変更なし。`data/move.py` の `"ついばむ"` エントリ（type/category/pp/power/accuracy/flags/handlers）は
  仕様書と一致しており修正不要。
- `handlers/move_attack.py` の `むしくい_steal_and_use_berry`（むしくい・ついばむ共通ハンドラ）も
  `take_item`（ねんちゃく等のチェックを内包）と `force_trigger_berry` を正しく使っており修正不要。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装・リーサルテスト列は n/a）。
  きのみ強奪効果はダメージ量に影響しないため、リーサル計算ハンドラは不要と判断。

## テスト
- 変更なし。`tests/moves_attack/test_move_ma.py` の
  `test_ついばむ_きのみを奪って攻撃者がHP回復する` が既に存在し、defenderのオボンのみを奪って
  attackerのHPが回復すること、defenderがアイテムを失うことを確認している。
- 共有ハンドラ `むしくい_steal_and_use_berry` のエッジケース
  （アイテムなし・きのみ以外・タンガのみ先行消費・ねんちゃく）は同ファイル内の
  むしくい用テストで既に網羅されているため、ついばむ側での重複追加は行わなかった。
- `python -m pytest tests/ -v` を実行し全件パスを確認（結果: `.loop/test_logs/ついばむ.log`）。
  なお、テストスイート全体に `-v` 実行時のみ発生する既知の非決定的な失敗
  （ついばむ・むしくいとは無関係のテストが実行ごとにランダムに1〜2件失敗する現象）が
  以前から存在することを確認したが、本レビューでの変更とは無関係であり、再実行により
  全件パスするログを採取した。
