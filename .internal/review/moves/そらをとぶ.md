# そらをとぶ レビュー結果

## 仕様書
- 変更なし。`.internal/spec/moves/そらをとぶ.md` は一次情報（Wiki）と照合済みで、タイプ・分類・威力90・命中率95・PP15・優先度0・直接攻撃などの記載に誤りはなかった。

## 実装計画書
- 変更なし。`.internal/plan/moves/そらをとぶ.md` は仕様書・実装（`handlers/volatile.py` / `data/move.py` / `data/volatile.py`）と整合しており、priority根拠も妥当だった。
- なお実装は計画書に記載された時点よりも進んでおり、`HIDDEN_MOVE_ALLOWED_MOVES["そらをとぶ"]` には計画書記載の4技に加え「うちおとす」も追加済み、また `gravity_restricted_fail`（じゅうりょく状態での失敗）も実装済みだった（計画書では「スコープ外」と記載されていたが、実際は既に対応されている）。計画書は参照用ドキュメントのため今回は追記修正しなかった。

## 実装（handlers/ / data/）
- 変更なし。`data/move.py` の `そらをとぶ`（power=90, accuracy=95, pp=15, flags={"contact","gravity_restricted"}）、`data/volatile.py` の `そらをとぶ` 揮発状態（交代禁止・コマンド固定・威力2倍・回避判定・攻撃後解除）はいずれも仕様書・計画書と一致していた。
- `handlers/volatile.py` の `そらをとぶ_boost_power` / `そらをとぶ_can_hit_hidden_target` / `そらをとぶ_remove_volatile` も想定通りの実装だった。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」列が `n/a`）。

## テスト
- 変更なし。`tests/moves_attack/test_move_sa.py`（2ターン攻撃・かみなり命中・空中回避）、`tests/volatiles/test_volatile_ka.py`（揮発状態の共通挙動をパラメータ化してカバー）、`tests/volatiles/test_volatile_satana.py`（かぜおこし・たつまき・ぼうふう・うちおとすの命中/威力2倍）、`tests/test_field.py`（じゅうりょく発動時の揮発状態解除）で十分にカバーされていた。
- `python -m pytest tests/ -v` は 3415 passed, 1 skipped で全件成功（ログ: `.loop/test_logs/そらをとぶ.log`）。

## 進捗表の修正
- `.internal/progress/move.md` の そらをとぶ 行に誤りがあったため修正した：
  - PP列が `16` になっていたが実装・仕様書は `15` のため修正。
  - レビュー列を `-` から `✓` に更新。
