# ドリルくちばし レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ドリルくちばし.md`）。.internal/wiki/moves/ドリルくちばし.html を基に、
  タイプ/分類/威力/命中/PP/対象/優先度/接触/判定フラグを記載。

## 実装計画書
- 新規作成（`.internal/plan/moves/ドリルくちばし.md`）。追加効果なしのため専用ハンドラ不要である旨を記載。
  既存の一括計画書 `.internal/plan/moves/攻撃技_無追加効果系.md` にも同技が含まれているが、内容に矛盾はない。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `"ドリルくちばし"` エントリ（type=ひこう, category=physical,
  pp=20, power=80, accuracy=100, flags={"contact"}, handlers={}）は一次情報・
  `src/jpoke/data/ps-champ-ja/moves.json`（PP20）と完全に一致しており、修正不要。
- `.internal/spec/moves/_move_labels.md` の `contact` ラベル一覧にも登録済みで整合。
- `handlers/move_attack.py` には専用ハンドラなし（追加効果がないため不要、変更なし）。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が n/a）。追加効果のない通常攻撃技のため、
  `handlers/lethal.py` に専用ハンドラは不要。

## テスト
- `tests/moves_attack/test_move_ta.py` の `test_ドリルくちばし_相手にダメージを与える` は既存のまま妥当
  （ダメージが与えられることを確認）であり、修正・追加なし。

## 進捗表
- `.internal/progress/move.md` のドリルくちばし行を仕様書・計画書=x、レビュー=✓、効果列=「追加効果なし」に更新。
- `python -m pytest tests/ -v` を実行し、3917 passed, 1 skipped で全件成功
  （ログ: `.loop/test_logs/ドリルくちばし.log`）。
