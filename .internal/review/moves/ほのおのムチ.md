# ほのおのムチ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ほのおのムチ.md`）。
  - `.internal/wiki/moves/ほのおのムチ.html`（一次情報）と `src/jpoke/data/ps-champ-ja/moves.json` を突き合わせ、
    基本情報（タイプ: ほのお / 分類: 物理 / 威力90〈Champions。SVまでは80〉 / 命中100 /
    PP16〈Champions。SVまでは15〉 / 対象: 相手1体 / 直接攻撃あり）と、
    追加効果（100%の確率で相手の『ぼうぎょ』ランクを1段階下げる）、判定フラグ
    （まもる・オウムがえし対象、マジックコート・よこどり対象外、ちからずく対象技）を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ほのおのムチ.md`）。
  - 既存実装（`handlers/move_attack.py` の `ほのおのムチ_lower_defender_def` /
    `Event.ON_DAMAGE_HIT`）は `Gのちから_lower_defender_def` と同一パターンで問題なし、
    という確認結果と、`data/moves/move_ha.py` の `power` / `pp` の誤りの修正内容を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `ほのおのムチ` エントリを修正した。
  - `power=80` → `90`（Champions仕様。`src/jpoke/data/ps-champ-ja/moves.json` / `.internal/progress/move.md` 準拠。
    SVまでの威力80のままになっていた）。
  - `pp=15` → `16`（Champions仕様。同様にSVまでのPP15のままになっていた）。
- `src/jpoke/handlers/move_attack.py` の `ほのおのムチ_lower_defender_def`、
  `flags={"contact", "secondary_effect"}` は仕様と一致しており変更なし。

## リーサル計算
- 変更なし。`handlers/lethal.py` の `ほのおのムチ_lower_def`（`LethalEvent.ON_HIT`）は
  `Gのちから_lower_def` と同一パターンで `ctx.move_secondary` が真のときのみ
  `ctx.defender.rank["def"]` を1段階下げており、仕様・実装と整合していることを確認した。

## テスト
- 変更なし。`tests/moves_attack/test_move_ha.py::test_ほのおのムチ_ぼうぎょ1段階低下が発動する`、
  `tests/test_lethal.py::test_ほのおのムチ_ぼうぎょダウン_secondary有り` /
  `_secondary無し` で追加効果・リーサル反映を確認済み。今回の威力・PP修正はこれらの
  アサーションに影響しないため追加変更は不要と判断した。
