# せいなるほのお レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/せいなるほのお.md`）。
  - 基本情報（ほのお・物理・威力100・命中95・PP5・相手1体・優先度0・接触なし）。
  - 追加効果: 50%の確率でやけど付与（`.internal/spec/moves/_move_secondary_1.md` 132行目と一致）。
  - 使用者がこおり状態の場合、こおり状態を治してから攻撃する効果を明記
    （かえんぐるま・もえつきる等と同種の「無条件解凍」効果であることを明記）。

## 実装計画書
- 新規作成（`.internal/plan/moves/せいなるほのお.md`）。
  - `かえんぐるま_thaw_attacker` / `もえつきる_thaw_attacker` と同一パターンでの
    `せいなるほのお_thaw_attacker`（`Event.ON_TRY_ACTION`, priority=5）追加を計画。
  - priority=5 の根拠を `.internal/spec/turn.md` および既存実装（かえんぐるま計画書）に基づき記載。

## 実装（handlers/ / data/）
- 修正した内容:
  - `src/jpoke/handlers/move_attack.py` に `せいなるほのお_thaw_attacker` を新規実装
    （使用者がこおり状態のとき無条件で解凍する）。
  - `src/jpoke/data/move.py` の `"せいなるほのお"` エントリに
    `Event.ON_TRY_ACTION: h.MoveHandler(ha.せいなるほのお_thaw_attacker, priority=5)` を追加。
  - `python scripts/sort_handlers.py src/jpoke/handlers/move_attack.py` と
    `python scripts/sort_data/sort_moves.py` を実行し並び替え済み。
  - やけど付与（`せいなるほのお_apply_burn_to_defender`, 50%）は既存実装のままで仕様と一致。
- 併せて `.internal/spec/moves/_move_labels.md` の thaw ラベル一覧に、実装漏れだった
  `かえんほうしゃ`（`flags` に `"thaw"` が設定済みだが一覧に未記載だった）を追加した。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」「リーサルテスト」列は `n/a`）。

## テスト
- `tests/moves_attack/test_move_sa.py` に
  `test_せいなるほのお_こおり状態で使うと解凍されて攻撃できる` を追加
  （`test_かえんぐるま_こおり状態で使うと解凍されて攻撃できる` と同一構成）。
- 既存の `test_せいなるほのお_やけどが発動する` は変更なし。
- `python scripts/sort_tests.py tests/moves_attack/test_move_sa.py` でソート、
  `python scripts/generate_test_list.py` でテスト一覧を更新、
  `python -m pytest tests/ -v` で全3416件パス（1件skip、既存分）を確認
  （ログ: `.loop/test_logs/せいなるほのお.log`）。
