# ボディプレス レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ボディプレス.md`）。
- `.internal/wiki/moves/ボディプレス.html` を一次情報として、基本情報（かくとう・物理・威力80・
  命中100・PP12＝チャンピオンズ基準）、実数値参照入れ替え（自分の『ぼうぎょ』を攻撃力として
  使用）の効果、パラドックス特性・わざわいのおふだ/つるぎ・ファーコート/しんかのきせき・
  パワートリック・てんねん・ちいさくなるとの相互作用、判定フラグをまとめた。
- ワンダールームとの相互作用（実数値は『ぼうぎょ』のまま、ランクは『とくぼう』を参照するという
  本家仕様）は `.internal/spec/fields/ワンダールーム.md` の「範囲外（現行実装非対象）」の既存方針に
  倣い、実装対象外として明記した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ボディプレス.md`）。
- 既存実装（`src/jpoke/core/damage.py` の `_calc_final_attack` 内の `move.name == 'ボディプレス'`
  分岐）の現状整理、発見した PP 不一致バグとその修正方針、パラドックス特性相互作用が既に
  イカサマのレビューで修正済みで流用可能であること、ワンダールーム相互作用をスコープ外とする
  判断根拠を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `"ボディプレス"` エントリの `pp` を `10`（Gen9本家基準）
  から `12`（`src/jpoke/data/ps-champ-ja/moves.json` 基準）に修正した。移行漏れバグ。
- `src/jpoke/core/damage.py` の `_calc_final_attack` における `move.name == 'ボディプレス'` の
  実数値・ランク参照入れ替え、`src/jpoke/handlers/ability_paradox.py` の
  `apply_atk_modifier`（技分類ベースの判定で技名分岐なし）は仕様通りであることを確認した。
  修正なし。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装/リーサルテスト列は `n/a`。攻撃力の参照元
  入れ替えはダメージ計算内で完結し専用のリーサルハンドラを要しない）。

## テスト
- `tests/moves_attack/test_move_ha.py` に以下を追加。
  - `test_ボディプレス_タイプ分類威力命中率PPが仕様通り`: タイプ・分類・威力・命中率・
    Champions基準PP(12)を確認。
  - `test_ボディプレス_パワートリック使用後は入れ替え後の実数値を攻撃として計算する`:
    パワートリックで実数値が入れ替わった後は、入れ替え後の値（元の『こうげき』実数値）を
    攻撃として計算することを確認。
  - `test_ボディプレス_てんねん相手には自分のぼうぎょランクが無視される`: 相手がてんねんの
    場合、使用者自身の『ぼうぎょ』ランク補正が無視されることを確認。
- `tests/abilities/test_ability__group.py` に以下を追加。
  - `test_パラドックス特性_ボディプレス使用時は使用者自身のこうげき強化で補正が乗る`:
    最大ステータスが『こうげき』のポケモンがボディプレス使用時、パラドックス補正（1.3倍）が
    乗ることを確認。
  - `test_パラドックス特性_ボディプレス使用時に強化対象がぼうぎょでも補正は乗らない`:
    最大ステータスが『ぼうぎょ』（＝ボディプレスが参照する実数値と同じ）でも、技分類が物理の
    ため『こうげき』スロットではなく補正が乗らないことを確認。
- 既存テスト（`test_ボディプレス_防御ランク上昇でダメージ増加する`、
  `test_ボディプレス_防御実数値を攻撃として計算する`、
  `test_しんかのきせき_ボディプレスでは攻撃側の防御を1_5倍扱いしない`）は仕様と一致しており
  変更なし。
- `python -m pytest tests/ -v` で全件パス（3997 passed, 1 skipped）を確認。
  結果は `.loop/test_logs/ボディプレス.log` に保存。
  （なお本スイートには一部、乱数シード未固定によりテスト実行順・タイミングで稀に失敗する
  既存の flaky テストが別モジュールに存在することを確認したが、いずれもボディプレスとは
  無関係であり、複数回の再実行で全件パスを確認済み。）
