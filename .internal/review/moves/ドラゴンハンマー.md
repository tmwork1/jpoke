# ドラゴンハンマー レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ドラゴンハンマー.md`）。一次情報（`.internal/wiki/moves/ドラゴンハンマー.html`）を元に、
  タイプ・分類・威力・命中・判定フラグ（急所/命中判定/まもる/おうじゃのしるし/マジックコート/よこどり/
  オウムがえし）を整理して記載した。追加効果なしの通常攻撃であることを確認。
- PPについて: `src/jpoke/data/ps-champ-ja/moves.json` に本技のエントリが存在しないことを確認した。ただし本技を
  覚える`ナッシー(アローラ)`・`トロピウス`・`タケルライコ`はいずれも`pokedex.json`に実在し、
  シードフレア等のようにレギュレーション対象外のポケモン専用技（move_list.txtに載らない理由）には
  該当しない。SV本家PP15を持つ他の技（かえんほうしゃ・10まんボルト・ドラゴンクロー）がいずれも
  move_list.txt上でPP16に変換されていることから、同じ変換規則（changes_from_sv.mdの8/12/16/20統一）
  を適用しPP16を正とした。

## 実装計画書
- 新規作成（`.internal/plan/moves/ドラゴンハンマー.md`）。既存実装が仕様書と一致するようデータ内容
  （type/category/pp/power/accuracy/flags）を明記し、ハンドラ不要である根拠・リーサル対象外の
  理由・テスト方針を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `MOVES_TA["ドラゴンハンマー"]` の `pp` が `15`（SV本家基準の
  値のまま）になっていたバグを発見し、`16`（Champions基準）に修正した。それ以外（type/category/
  power/accuracy/flags/handlers）は仕様書と一致しており誤りはなかった。

## リーサル計算
- 対象外。`.internal/progress/move.md` のリーサル実装列は `n/a`（追加効果を持たない通常攻撃技のため、
  ダメージ計算の共通処理のみで完結し専用ハンドラは不要）。

## テスト
- 新規追加（`tests/moves_attack/test_move_ta.py`、`test_ドラゴンクロー_相手にダメージを与える` の
  直後に挿入）。
  - `test_ドラゴンハンマー_相手にダメージを与える`
- `python -m pytest tests/ -v` を実行し、3918 passed, 1 skipped（既存のskipで本レビューと無関係）で
  全テスト成功を確認した。ログは `.loop/test_logs/ドラゴンハンマー.log` に保存済み。
