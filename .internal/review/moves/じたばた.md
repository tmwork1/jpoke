# じたばた レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/じたばた.md`）。一次情報（wiki）・`きしかいせい.md` の仕様と照合し、
  基本情報（ノーマル/物理/威力可変/命中100/PP15/相手1体/優先度0/接触あり）と威力計算式
  （X = floor(残りHP × 48 / 最大HP) による6段階の威力決定）を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/じたばた.md`）。既存実装（`じたばた_calc_power` および
  共通関数 `_hp_low_to_power`）が仕様書と完全に一致していることを確認し、ハンドラ構成・
  priority の根拠（`ON_CALC_POWER_MODIFIER` はデフォルト優先度、既存の同種ハンドラの慣例に合わせる）
  を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/move_attack.py` の `じたばた_calc_power`、
  `src/jpoke/data/move.py` の `"じたばた"` エントリともに仕様書と一致しており修正不要と判断した。

## リーサル計算
- 対象外（`.internal/progress/move.md` の じたばた 行はリーサル実装/リーサルテストともに n/a）。
  威力は攻撃側自身の現在HPにのみ依存し、相手のHP分布 `StateDist` を直接左右しないため。

## テスト
- `tests/moves_attack/test_move_ma.py` に、`きしかいせい`（`test_move_ka.py`）と同水準の
  境界網羅となるよう中間境界テストを追加した:
  - `test_じたばた_HP極少のとき威力150`（X=2）
  - `test_じたばた_HP約10パーセントのとき威力100`（X=5）
  - `test_じたばた_HP約35パーセントのとき威力80`（X=16）
  - `test_じたばた_HP約68パーセントのとき威力40`（X=32）
- `scripts/sort_tests.py`・`scripts/generate_test_list.py` を実行し、
  `python -m pytest tests/ -v` で全3394件パス（無関係な既存の確率依存フレーキーテスト
  `test_リフレッシュ_まひを治す` は単体では成功することを確認済み）。
