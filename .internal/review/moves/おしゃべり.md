# おしゃべり レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/おしゃべり.md`）。
  一次情報（`.internal/wiki/moves/おしゃべり.html`）をもとに、タイプ・分類・威力（第六世代以降65）・
  命中率・PP・対象・優先度・接触判定・追加効果（100%こんらん）・各種判定フラグ
  （まもる、おうじゃのしるし、マジックコート、よこどり、オウムがえし、さきどり、ちからずく）・
  ねごととの関係（選ばれない技リストに含まれないため `non_negoto` 不要）を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/おしゃべり.md`）。
  既存の共通計画書 `.internal/plan/moves/攻撃技_こんらん系.md` の内容を踏襲しつつ、
  本技専用のハンドラ構成表・priority根拠（`Event.ON_DAMAGE_HIT` は `turn.md` 未掲載イベントのため、
  既存の同種こんらん付与ハンドラ全件に倣うことを明記）・エッジケースを記載した。

## 実装（handlers/ / data/）
- **バグを発見・修正した。** `src/jpoke/data/move.py` の `"おしゃべり"` エントリで `pp=15` と
  誤って登録されていたが、一次情報（wiki）では PP20 であることを確認し、`pp=20` に修正した。
- `handlers/move_attack.py` の `おしゃべり_apply_confusion`（`apply_confusion_to_defender` を呼ぶのみ）は
  仕様と一致しており修正不要。
- タイプ（ひこう）・分類（特殊）・威力（65）・命中率（100）・
  フラグ（`sound`、`secondary_effect`）・非接触は一次情報と一致し修正不要。
- `python scripts/sort_data/sort_moves.py` と `python scripts/sort_handlers.py src/jpoke/handlers/move_attack.py`
  を実行したが、本技に関する並び順の変更はなかった（既に正しい位置にあった）。

## リーサル計算
- 対象外。`.internal/progress/move.md` の「リーサル実装」「リーサルテスト」列がいずれも `n/a` のため。

## テスト
- 変更なし。`tests/moves_attack/test_move_a.py` の `test_おしゃべり_こんらんが発動する` は
  こんらん付与（確率100%）を正しく検証しており、修正不要と判断した。
- `python scripts/sort_tests.py tests/moves_attack/test_move_a.py` と
  `python scripts/generate_test_list.py` を実行した（他の並行レビュー作業による差分を含むが、
  本技に関する差分はなし）。
- `python -m pytest tests/ -v` を実行し、`test_おしゃべり_こんらんが発動する` は PASSED を確認した
  （ログ: `.loop/test_logs/おしゃべり.log`）。
  なお同時実行中の他技レビュー作業（いかりのまえば・いじげんホール・いじげんラッシュ・いびき・
  うたかたのアリア・ちからずく/いっちょうあがり・リーサル計算のがんじょう/きあいのタスキ/マルチスケイル関連）
  により、本技と無関係な15件のテストが一時的に失敗している状態だったが、これらはおしゃべりの変更とは
  無関係であることを確認した。

## 進捗表（`.internal/progress/move.md`）
- 仕様書列・計画書列を `-` から `x` に更新（専用ファイルを新規作成したため）。
- レビュー列を `-` から `✓` に更新。
- タイプ・威力・命中率・PP・対象・効果列の `?` プレースホルダーを実際の値
  （ひこう / 65 / 100 / 20 / defender / 追加効果として100%の確率で相手をこんらん状態にする）に修正した。
