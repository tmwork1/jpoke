# じゃどくのくさり レビュー結果

## 仕様書

- 新規作成（`.internal/spec/moves/じゃどくのくさり.md`）。
  - どくタイプ / 特殊 / 威力100 / 命中100 / PP5 / 対象: 相手1体 / 優先度0 / 直接攻撃なし。
  - 追加効果として50%の確率で相手を『もうどく』状態にする。

## 実装計画書

- 新規作成（`.internal/plan/moves/じゃどくのくさり.md`）。
  - `ON_DAMAGE_HIT` で `apply_ailment_to_defender` を利用してもうどくを付与する既存実装をベースに、
    priorityの根拠・エッジケース（ちからずく・みがわり・りんぷん/おんみつマント）を明記した。

## 実装（handlers/ / data/）

- `handlers/move_attack.py` の `じゃどくのくさり_apply_toxic_to_defender` はロジック自体は正しく、変更なし。
- `data/move.py` の `MoveData` エントリに一次情報（ポケモンWiki）との不一致を発見し修正した。
  - `category="physical"` → `category="special"`（実際は特殊技）
  - `pp=10` → `pp=5`（一次情報ではPP5）
  - `flags={"contact", "secondary_effect"}` → `flags={"secondary_effect"}`（一次情報では直接攻撃「×」のため
    `contact` フラグは誤り）
- `python scripts/sort_data/sort_moves.py` を実行（並び順に変更なし）。

## リーサル計算

- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）。

## テスト

- 既存テスト `test_じゃどくのくさり_もうどくが発動する`（`tests/moves_attack/test_move_sa.py`）は
  もうどく付与の挙動のみを検証しており、category/pp/flags 修正の影響を受けないため変更なし。
  他の同種の単純な追加効果技（例: どくどくのキバ）のテスト密度と一致しており過不足なし。
- `python -m pytest tests/ -v` で全3395件パス（1件skip、無関係）を確認。ログは
  `.loop/test_logs/じゃどくのくさり.log` に保存。
