# かげぬい レビュー結果

## 仕様書

- 新規作成（`.internal/spec/moves/かげぬい.md`）
  - 一次情報（`.internal/wiki/moves/かげぬい.html`）と `src/jpoke/data/ps-champ-ja/moves.json` を基に、
    基本情報（タイプ/分類/威力/命中/PP/対象/優先度/接触）と効果（追加効果としてにげられない状態を付与）、
    ちからずく・りんぷん・おんみつマントとの関係、ゴーストタイプへの無効化、専用技情報を記載。

## 実装計画書

- 新規作成（`.internal/plan/moves/かげぬい.md`）
  - 既存実装との差分（威力・PPがSV基準のまま/追加効果未実装/secondary_effectフラグ未付与）を明記。
  - `apply_volatile_to_defender` ヘルパー（くろいまなざし・とおせんぼうと同パターン）を使う設計を記載。
  - リーサル計算は対象外である旨を明記。

## 実装（handlers/ / data/）

- `src/jpoke/handlers/move_attack.py`: `かげぬい_apply_no_escape` を新規実装
  （`apply_volatile_to_defender` 経由でにげられない状態を付与。ゴーストタイプ無効化は
  `にげられない` 揮発性状態側のON_CHECK_TRAPPEDに委譲）。
- `src/jpoke/data/move.py`: `"かげぬい"` エントリを修正
  - 威力を80→90、PPを10→12に修正（Champions基準。`src/jpoke/data/ps-champ-ja/moves.json` 準拠）。
  - `flags={"secondary_effect"}` を追加（ちからずく対象技として扱うため）。
  - `handlers={Event.ON_DAMAGE_HIT: h.MoveHandler(ha.かげぬい_apply_no_escape)}` を追加
    （従来は `handlers={}` で追加効果が未実装だった）。
- `python scripts/sort_handlers.py src/jpoke/handlers/move_attack.py` と
  `python scripts/sort_data/sort_moves.py` を実行済み。

## リーサル計算

- 対象外（`.internal/progress/move.md` の「リーサル実装」列が `n/a`。にげられない状態の付与はHP分布に影響しないため）。

## テスト

- `tests/moves_attack/test_move_ka.py` に以下を追加:
  - `test_かげぬい_相手にダメージを与える`
  - `test_かげぬい_にげられない状態を付与する`
  - `test_かげぬい_ちからずくで威力上昇しにげられない状態は付与されない`
  - `test_かげぬい_りんぷんの相手にはにげられない状態が付与されない`
  - 防御側はノーマルタイプ（ゴースト技を無効化してしまう）を避け、ピカチュウを使用。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ka.py` と
  `python scripts/generate_test_list.py` を実行済み。
- `python -m pytest tests/ -v` で全件パス（3269 passed, 1 skipped）。ログは
  `.loop/test_logs/かげぬい.log` に保存。
