# アクアジェット レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/アクアジェット.md`）。
- 一次情報（`.internal/wiki/moves/アクアジェット.html`）を基に、タイプ（みず）・分類（物理）・威力（40）・
  命中（100）・PP（20）・優先度（+1）・接触あり・追加効果なし・各種判定フラグ（急所/命中判定/まもる/
  おうじゃのしるし/マジックコート/よこどり/オウムがえし）を記載。

## 実装計画書
- 新規作成（`.internal/plan/moves/アクアジェット.md`）。
- 仕様書をもとに、既存の `data/moves/move_a.py` エントリが仕様と一致していることを確認し、専用ハンドラ
  不要である根拠（追加効果なし・優先度は `MoveData.priority` の共通処理で解決）を記載。
  みずタイプ相性補正・サイコフィールドによる先制技無効化についても既存共通処理で対応済みである旨を追記。

## 実装（handlers/ / data/）
- 変更なし。`data/moves/move_a.py` の `"アクアジェット"` エントリ（type="みず", category="physical",
  pp=20, power=40, accuracy=100, priority=1, flags={"contact"}, handlers={}）は仕様書・
  `src/jpoke/data/ps-champ-ja/moves.json`（PP=20）と完全に一致しており修正不要と確認した。

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行は「リーサル実装」「リーサルテスト」ともに `n/a`）。

## テスト
- 変更なし。`tests/moves_attack/test_move_a.py` の
  `test_アクアジェット_相手にダメージを与える` は既存の同種先制技テスト
  （かげうち・でんこうせっか等）と同一パターンで実装済みであり、追加・修正の必要はないと判断した。
