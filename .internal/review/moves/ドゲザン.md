# ドゲザン レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ドゲザン.md`）。一次情報（`.internal/wiki/moves/ドゲザン.html`）と
  `src/jpoke/data/ps-champ-ja/moves.json` を基に、基本情報・効果・判定フラグをまとめた。
  「急所: ○」（急所ランク+1）と「必ず命中する」の2点を明記。

## 実装計画書
- 新規作成（`.internal/plan/moves/ドゲザン.md`）。既存実装との差分として PP誤り（10→12）と
  `critical_rank=1` の欠落を指摘し、修正方針を記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `"ドゲザン"` エントリを修正した。
  - PPを10から12に修正（`src/jpoke/data/ps-champ-ja/moves.json` の「ドゲザン あく 物理 85 - 12 接○ 守○」
    に準拠。本家Gen9基準の10が移行漏れとして残っていた）。
  - `critical_rank=1` を追加（一次情報の「判定」欄で「急所: ○」となっており、急所に当たり
    やすい技。既存の `つじぎり`（同じ「切る技」であくタイプ）と同様の実装パターン）。
  - `accuracy=None` を明示的に追加（デフォルト値と同じでバグではないが、他の必中技
    `あてみなげ` `でんげきは` `スピードスター` 等と同様、可読性のため明示するスタイルに統一）。
  - `handlers/lethal.py` の変更なし（追加効果を持たない通常攻撃技のため専用ハンドラ不要）。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」列は `n/a`。追加効果を持たない通常の
  物理攻撃技のためリーサル計算ハンドラは不要）。

## テスト
- `tests/moves_attack/test_move_ta.py` に以下を追加した（つじぎり・あてみなげのテストパターンを踏襲）。
  - `test_ドゲザン_タイプ威力PPが仕様通り`: タイプ・分類・威力・命中(None)・PP(12)・
    `critical_rank`(1)・`contact`/`slash`フラグを確認。
  - `test_ドゲザン_相手の回避率が高くても必ず命中する`: 回避率+6でも命中することを確認。
  - `test_ドゲザン_急所ランクが1`: 乱数0で急所が発生することを確認。
  - `test_ドゲザン_急所ランクが1_乱数大で急所なし`: 乱数0.5（>=1/8）で急所が発生しないことを確認。
- `python -m pytest tests/ -v` を実行し、3903 passed, 1 skipped（ログ: `.loop/test_logs/ドゲザン.log`）。
