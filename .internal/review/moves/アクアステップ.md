# アクアステップ レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/アクアステップ.md`）。一次情報（`.internal/wiki/moves/アクアステップ.html`）と
  `.internal/spec/moves/_move_secondary_1.md`（71行目）を参照し、基本情報・追加効果
  （自分のすばやさ+1、100%、ちからずく対象）・判定フラグ・リーサル計算対象外の理由を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/アクアステップ.md`）。既存実装（`src/jpoke/data/moves/move_a.py` の
  `MoveData` と `src/jpoke/handlers/move_attack.py` の `アクアステップ_boost_attacker_spe`）を
  仕様書と照合した結果、PP・flags・ハンドラ登録のいずれも仕様通りで、修正すべき誤り・欠落は
  見つからなかった旨を記載した（同種の `オーラぐるま` / `ニトロチャージ` の計画書と同じ形式）。

## 実装（handlers/ / data/）
- 変更なし。`pp=12`（champions版と一致）、`flags={"contact", "dance", "secondary_effect"}`
  （直接攻撃・おどりの技・ちからずく対象がいずれも一次情報と一致）、
  `Event.ON_HIT: h.MoveHandler(ha.アクアステップ_boost_attacker_spe)` の登録内容を確認したが、
  誤り・欠落は見つからなかった。

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行は「リーサル実装: n/a」「リーサルテスト: n/a」。
  自分のすばやさランクを上げるだけの追加効果は相手へのダメージ確定計算（HP分布）に
  影響しないため）。

## テスト
- 変更なし。`tests/moves_attack/test_move_a.py` に「相手にダメージを与える」「自分のすばやさが
  1段階上がる」の2件が実装済みで、仕様と一致していることを確認した。加えて
  `tests/abilities/test_ability_ta.py`（ちからずくとの相互作用）、
  `tests/abilities/test_ability_a.py`（おやこあいとの相互作用）でも本技を題材にした検証が
  既に存在し、いずれも仕様と一致していることを確認した。過不足なし。
