# すなじごく レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/すなじごく.md`）。一次情報（`.internal/wiki/moves/すなじごく.html`）を参照し、
  現行世代（第五世代以降）の値（威力35・命中85・PP15・接触なし）と効果（バインド付与、4〜5ターン
  毎ターン最大HPの1/8ダメージ）、判定フラグ、備考を `しめつける.md` と同様の構成で記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/すなじごく.md`）。既存のバインド系技共通ハンドラ
  `apply_bind_to_defender`（`handlers/move_attack.py`）・`_apply_bind`（`handlers/lethal.py`）の
  流用方針を記載。priority は `.internal/spec/turn.md` に `ON_DAMAGE_HIT` の記載がないため、
  既存バインド系技と同様にデフォルト priority を採用する根拠を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `すなじごく` エントリ（type=じめん, category=physical,
  pp=15, power=35, accuracy=85, `Event.ON_DAMAGE_HIT: MoveHandler(apply_bind_to_defender)`）は
  一次情報・仕様書と一致しており、`flags` に `contact` が付与されていない点も
  「直接攻撃: ×」という仕様と整合していた。修正不要。

## リーサル計算
- 変更なし。`.internal/progress/move.md` の「リーサル実装」列が `x` であり、`lethal_handlers` に
  `LethalEvent.ON_HIT: LethalHandler(l._apply_bind)` が既に登録済みで、`handlers/lethal.py` の
  `_apply_bind` は他のバインド系技（うずしお・しめつける等）と共通のため実装・仕様と整合していた。

## テスト
- 新規追加。`tests/moves_attack/test_move_sa.py` に以下を追加（サンドパンを使用者として）:
  - `test_すなじごく_バインド中は相手が交代できない`
  - `test_すなじごく_バインド中は相手が毎ターン最大HPの8分の1ダメージを受ける`
  - `test_すなじごく_命中後にバインド状態になる`
  - `test_すなじごく_攻撃者が交代するとバインドが解除される`
  - （じめんタイプのためゴースト無効化テストは対象外。しめつけるはノーマルタイプのため該当テストがあるが、
    すなじごくには不要）
- `tests/test_lethal.py` に `test_すなじごく_バインド付与` を追加（`しめつける` の既存テストと同パターン）。
- `python scripts/sort_tests.py tests/moves_attack/test_move_sa.py tests/test_lethal.py` でソート、
  `python scripts/generate_test_list.py` で一覧更新、`python -m pytest tests/ -v` で全3412件
  （追加した5件を含む）パスを確認。
