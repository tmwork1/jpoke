# みずあめボム レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/みずあめボム.md`）。
  - 基本情報（タイプ・分類・威力・命中・PP・対象・優先度・直接攻撃・弾の技）を明記。
  - 命中率はSVの85ではなくChampionsの90を採用する旨を明記（README の対象範囲の定義に基づく）。
  - 追加効果（あめまみれ100%）の詳細は `.internal/spec/volatiles/あめまみれ.md` を参照する形で記載。
  - りんぷん・おんみつマント・ちからずく・みがわりによる無効化の制約を明記。
  - リーサル計算は対象外（`n/a`）である旨を明記。

## 実装計画書
- 新規作成（`.internal/plan/moves/みずあめボム.md`）。
  - 既存実装の誤り（accuracy=85→90、`bullet` フラグ欠落）を指摘し、修正内容を明記。
  - `Event.ON_DAMAGE_HIT` の priority は `.internal/spec/turn.md` に未掲載のため、既存の同種ハンドラ
    （`ミストボール_lower_spa_C` 等）に倣いデフォルト値（100）を採用する根拠を明記。
  - りんぷん・おんみつマント・ちからずくの無効化は `apply_volatile_to_defender` →
    `battle.resolve_secondary_chance` により汎用的に処理されるため、みずあめボム固有の実装が
    不要である旨を明記。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ma.py` の `"みずあめボム"` エントリを修正。
  - `accuracy=85` → `accuracy=90`（Champions値に修正。SVでは85だが本プロジェクトの対象範囲は
    Championsを優先するため）。
  - `flags={"secondary_effect"}` → `flags={"bullet", "secondary_effect"}`
    （弾の技であり特性ぼうだんで無効化される仕様が未反映だったため追加）。
  - 修正後、`python scripts/sort_data/sort_moves.py` を実行済み。
- `src/jpoke/handlers/move_attack.py` の `みずあめボム_apply_volatile_to_defender` は仕様と一致
  しており変更なし。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装・リーサルテスト列は `n/a`）。

## テスト
- `tests/moves_attack/test_move_ma.py` の既存テストは `accuracy=100` を明示指定しているため、
  base accuracy 修正の影響を受けず変更なし。
- `tests/abilities/test_ability__group.py` の `test_音ラベル無効系_param` に、`bullet` フラグ
  追加を確認するケースとして `("ぼうだん", "", "みずあめボム", True)` /
  `("ぼうだん", "かたやぶり", "みずあめボム", False)` を追加。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ma.py tests/abilities/test_ability__group.py`
  でソート、`python scripts/generate_test_list.py` でテスト一覧を更新、
  `python -m pytest tests/ -v` で全3605件（1 skipped）がパスすることを確認
  （`.loop/test_logs/みずあめボム.log` に結果を保存）。
