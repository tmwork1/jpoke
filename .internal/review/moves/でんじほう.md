# でんじほう レビュー結果

## 仕様書
- 既に `.internal/spec/moves/でんじほう.md` が存在していた（過去のレビューで作成済み）。
  一次情報（`.internal/wiki/moves/でんじほう.html`）と再照合したが、でんき・特殊・威力120（第四世代以降）・
  命中50・PP5・追加効果100%まひ・弾技（ぼうだん対象）の記載に誤り・欠落はなく変更なし。

## 実装計画書
- 既に `.internal/plan/moves/でんじほう.md` が存在していた（過去のレビューで作成済み）。
  実装（`ON_DAMAGE_HIT` / `MoveHandler` デフォルト設定 / `apply_ailment_to_defender`）・
  priority根拠・エッジケース（でんきタイプ無効・ぼうだん無効）の記載を実装と再照合したが変更なし。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/move_attack.py` の `でんじほう_apply_paralysis_to_defender` と
  `src/jpoke/data/move.py` の `MoveData`（`type="でんき"`, `category="special"`, `pp=5`, `power=120`,
  `accuracy=50`, `flags={"bullet", "secondary_effect"}`）は仕様書・計画書と一致しており、五十音順の並びも正しいため修正不要。

## リーサル計算
- 対象外（`n/a`）。まひはダメージ量に影響しないため専用のリーサルハンドラは不要。

## テスト
- `tests/moves_attack/test_move_ta.py` に以下を追加。
  - `test_でんじほう_タイプと分類と威力と命中率とppが正しい`
  - `test_でんじほう_必ず相手をまひ状態にする`
- `python scripts/sort_tests.py tests/moves_attack/test_move_ta.py` でソート、
  `python scripts/generate_test_list.py` で一覧更新、`python -m pytest tests/ -v` で全件パス（3448 passed, 1 skipped）を確認。
- 併せて `.internal/progress/move.md` の でんじほう 行の誤り（PP列が `8` になっていた）を `5` に修正した。
