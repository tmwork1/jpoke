# ねっさのだいち レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ねっさのだいち.md`）。
- 一次情報（`.internal/wiki/moves/ねっさのだいち.html`）から、タイプ=じめん、分類=特殊、威力=70、
  命中=100、PP=10、追加効果30%やけど、使用者がこおり状態の場合は治してから攻撃する、
  相手のこおり状態も治す、という仕様を反映した。
- `.internal/spec/moves/かえんぐるま.md` / `せいなるほのお.md` の既存フォーマットに倣い、
  `"thaw"` フラグの仕様（被弾側の解凍のみカバーし、使用者側の無条件解凍は個別実装が必要）
  を明記した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ねっさのだいち.md`）。
- `ねっさのだいち_thaw_attacker`（`Event.ON_TRY_ACTION`, priority=5）の追加実装を計画し、
  `かえんぐるま_thaw_attacker` 等と同じパターンであることと priority=5 の根拠を明記した。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_attack.py` に `ねっさのだいち_thaw_attacker` を追加。
  使用者がこおり状態のときにこの技を使うと無条件でこおりを解除する処理
  （`かえんぐるま_thaw_attacker` 等と同一パターン）。
- `src/jpoke/data/move.py` の `"ねっさのだいち"` エントリに
  `Event.ON_TRY_ACTION: h.MoveHandler(ha.ねっさのだいち_thaw_attacker, priority=5)` を追加。
  （既存の `Event.ON_DAMAGE_HIT: ねっさのだいち_apply_burn_to_defender` は仕様通りで変更なし。
  また `"thaw"` フラグにより相手のこおり状態を治す効果は `こおり_cure_by_thaw_move` で
  既にカバーされており、変更不要だった。）
- `scripts/sort_handlers.py src/jpoke/handlers/move_attack.py` と
  `scripts/sort_data/sort_moves.py` を実行済み。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」「リーサルテスト」列は `n/a`）。

## テスト
- `tests/moves_attack/test_move_na.py` に以下を追加:
  - `test_ねっさのだいち_こおり状態で使うと解凍されて攻撃できる`
    （使用者がこおり状態のとき、この技を使うと解凍されて攻撃できることを確認）
  - `test_ねっさのだいち_こおり状態の相手に当てると解凍する`
    （相手がこおり状態のとき、この技のダメージを受けると解凍されることを確認）
- `scripts/sort_tests.py tests/moves_attack/test_move_na.py` でソート、
  `scripts/generate_test_list.py` でテスト一覧を更新、
  `pytest tests/` で全件パス（3539 passed, 1 skipped）を確認。

## その他の修正
- `.internal/progress/move.md` の PP 列の誤り修正:
  - `ねっさのだいち`: 12 → 10（実装・一次情報と一致させた）
  - `ねっとう`: 16 → 15（実装・一次情報と一致させた。ねっとう自体は今回のレビュー対象外のため、
    他の列は変更していない）
