# ミストフィールド レビュー結果

## 仕様書

- 新規作成: `.internal/spec/moves/ミストフィールド.md`
  - `.internal/wiki/moves/ミストフィールド.html` の技本体の情報（タイプ: フェアリー、分類: 変化、
    PP: 10→12（wiki 記載は 10 だが `src/jpoke/data/ps-champ-ja/moves.json` の値を優先）、命中率: -、
    範囲: 全体の場、優先度: 0、直接攻撃: ×）を記載
  - 判定（急所/命中判定/追加効果/まもる/マジックコート/よこどり/オウムがえし）を
    `.internal/spec/moves/エレキフィールド.md` と同じ形式で明記
  - フィールド自体の詳細仕様は `.internal/spec/fields/ミストフィールド.md` に委譲することを明記

## 実装計画書

- 修正: `.internal/plan/moves/ミストフィールド.md`
  - PP を 10 から 12（`src/jpoke/data/ps-champ-ja/moves.json` 準拠）に修正
  - `.internal/spec/moves/ミストフィールド.md` への参照を追加し、`エレキフィールド` 計画書と同じ構成
    （仕様要約／現状の実装状況／ハンドラ構成／Priority根拠／実装箇所／注意点）に統一
  - `data/moves/move_ma.py` のサンプルコードの PP も 12 に修正

## 実装（handlers/ / data/）

- `src/jpoke/data/moves/move_ma.py`: **修正あり** — `"ミストフィールド"` エントリの `pp` を
  `10` から `12` に修正（`src/jpoke/data/ps-champ-ja/moves.json` の値と不一致だったバグ）
- `src/jpoke/handlers/move_status.py` の `ミストフィールド_activate_terrain`: 変更なし
  （`battle.terrain_manager.apply("ミストフィールド", 5)` を呼ぶのみで計画書通り）

## リーサル計算

- 対象外（変化技のため `.internal/progress/move.md` のリーサル実装/テスト列は n/a）

## テスト

- 追加: `tests/moves_status/test_move_ma.py`
  - `test_ミストフィールド_すでに同じ地形なら失敗` — すでにミストフィールドが有効な場合に
    再設置されず失敗することを確認
  - `test_ミストフィールド_地形がミストフィールドになる` — 使用後に地形がミストフィールドになり
    5ターン継続することを確認
  - `tests/moves_status/test_move_a.py` の `エレキフィールド` テストと同じパターンで実装
- `python -m pytest tests/ -v` で全件パス（4622 passed, 1 skipped）。ログを
  `.loop/test_logs/ミストフィールド.log` に保存

## 進捗表

- `.internal/progress/move.md` の `ミストフィールド` 行を更新
  - 仕様書: `-` → `x`（`.internal/spec/moves/ミストフィールド.md` 新規作成のため）
  - 計画書: `-` → `x`（`.internal/plan/moves/ミストフィールド.md` が既に存在していたが列が
    未更新だったため修正）
  - レビュー: `-` → `✓`
