# ボルテッカー レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ボルテッカー.md`）。
  - 一次情報（`.internal/wiki/moves/ボルテッカー.html`）、`.internal/spec/moves/_move_secondary_1.md`
    （まひ確率10%）、`.internal/spec/moves/_move_secondary_2.md`（反動比率0.33）、
    `src/jpoke/data/ps-champ-ja/moves.json`（PP16）と照合して作成した。
  - 反動計算・追加効果（まひ）・みがわりとの相互作用・PPのチャンピオンズ仕様差分を明記した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ボルテッカー.md`）。
  - ハンドラ構成（ON_HIT / ON_DAMAGE_HIT、いずれもデフォルトpriority=100）を記載。
  - 既存の legacy な複数技横断計画書（`反動技_025.md` 等）に反動比率0.25という誤記が
    残っていたため、本ファイルで0.33（1/3）が正しい旨を明記した（legacy文書自体は
    対象範囲外のため修正していない）。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の PP を Gen9本家基準の `15` から
  チャンピオンズ基準の `16`（`src/jpoke/data/ps-champ-ja/moves.json` 準拠）に修正した。
- 反動比率（1/3）・まひ追加効果（10%）のロジック自体は仕様通りで、修正なし。
  - `_recoil(battle, ctx, value, 1/3)` で `round_half_down` による端数処理・
    `reason="recoil"`・みがわり与ダメージへのフォールバックが正しく行われていることを確認した。
  - `apply_ailment_to_defender(..., ailment="まひ", chance=0.1)` で `resolve_secondary_chance`
    （ちからずく等）を経由していることを確認した。
  - `ON_DAMAGE_HIT` はみがわりに完全に阻まれた場合発火しないため、まひの追加効果が
    正しくブロックされることを `core/move_executor.py` のロジックで確認した。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列は n/a）。反動技・確率依存の追加効果技であり、
  防御側HP分布には影響しないため対象外と判断した。

## テスト
- `tests/moves_attack/test_move_ha.py` に
  `test_ボルテッカー_みがわりへの与ダメージでも反動が発生する` を追加した
  （みがわりに阻まれた場合も、みがわりへの与ダメージを基準に反動が発生すること、
  かつ追加効果（まひ）は発動しないことを確認）。
- 既存テスト（まひ発動・反動発生・反動量1/3）は仕様と整合しており修正不要。
- `python -m pytest tests/ -v` で3991件全件パス（1件skip）を確認した
  （ログ: `.loop/test_logs/ボルテッカー.log`）。
