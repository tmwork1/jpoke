# くさむすび レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/くさむすび.md`）。一次情報（`.internal/wiki/moves/くさむすび.html`）と
  `.internal/spec/moves/_variable_power.md`（第8節・体重依存威力技）を参照し、基本情報・威力段階表・
  判定フラグ（急所/命中判定/追加効果/まもる/おうじゃのしるし/マジックコート/よこどり/オウムがえし）・
  リーサル計算の扱いを記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/くさむすび.md`）。既存実装（`くさむすび_calc_power` / `_weight_to_power`
  共通関数、`data/move.py` の `MoveData`）が仕様書と完全に一致していることを確認し、ハンドラ構成表
  （イベント・priority・subject_spec）と根拠を明記した。

## 実装（handlers/ / data/）
- 変更なし。`handlers/move_attack.py` の `くさむすび_calc_power`（`_weight_to_power` を
  `けたぐり_calc_power` と共用）、`data/move.py` の `MoveData`（type=くさ, category=special,
  pp=20, power=1(プレースホルダ), accuracy=100, flags={"contact"}）とも一次情報・仕様書と一致。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装/リーサルテスト列は n/a）。
  威力は相手の体重という固定パラメータにのみ依存し、相手のHP分布（`StateDist`）を
  左右する要素ではないため、専用のリーサルハンドラは不要と判断した。

## テスト
- 変更なし。`tests/moves_attack/test_move_ka.py` に威力6段階の境界（10kg未満/10-25kg未満/
  25-50kg未満/50-100kg未満/100-200kg未満/200kg以上）を確認するテストが既に一通り揃っており、
  実装・仕様書と照合した結果、追加・修正は不要だった。

## その他
- `.internal/progress/move.md` の くさむすび行を更新（仕様書: `-`→`x`、計画書: `-`→`x`、
  レビュー: `-`→`✓`、効果列: `？`→ 威力段階の説明文）。
