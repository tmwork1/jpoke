# コスモパワー レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/コスモパワー.md`）
- 一次情報（.internal/wiki/moves/コスモパワー.html）を基に、タイプ・分類・PP（20）・対象（自分）・効果（ぼうぎょ／とくぼうを各1段階上昇）を記載
- 判定フラグ（急所対象外、必中、まもる無効、マジックコート反射不可、よこどり対象、オウムがえし反射不可）を既存の同系統技（めいそう等）と同じ形式で明記

## 実装計画書
- 既存の `.internal/plan/moves/コスモパワー.md` は内容が古い記法だったため修正した
  - 出典を旧 `.internal/spec/moves/status_move.md`（存在しないファイル）から新規作成した `.internal/spec/moves/コスモパワー.md` に修正
  - ハンドラ関数のシグネチャを `EventContext` → `AttackContext` に修正（実装・他の同種計画書と整合）
  - `modify_attacker_stats` の stats キーを `{"B": 1, "D": 1}` から実装済みの正しい `Stat` リテラル `{"def": 1, "spd": 1}` に修正
  - 実装ファイルパスを旧 `src/jpoke/data/move.py` から実際の分割後ファイル `src/jpoke/data/moves/move_ka.py` に修正
  - 挿入位置の記述を実装済みの並び（`こうそくいどう` の後、`コットンガード` の前）に修正
  - リーサル計算セクションを追加し、対象外である理由を明記

## 実装（handlers/ / data/）
- 変更なし。`handlers/move_status.py` の `コスモパワー_modify_attacker_stats` と `data/moves/move_ka.py` の `"コスモパワー"` エントリは仕様・計画と整合しており、`Stat` リテラル（`def`/`spd`）・`MoveHandler` の既定 `subject_spec="attacker:self"`・priority=100（既定）とも一致していることを確認した

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`。自分の能力ランクを上げるだけの変化技で即時のダメージ・HP変化を伴わないため、`handlers/lethal.py` にも該当ハンドラは存在しない）

## テスト
- 変更なし。`tests/moves_status/test_move_ka.py` の `test_コスモパワー_発動前後のランク変化` が通常発動・片方上限・両方上限・両方上限ちょうど・最低ランクからの上昇まで `pytest.mark.parametrize` で網羅しており、追加修正は不要と判断した
- `python -m pytest tests/ -v` で全4468件（1件skip）がパスすることを確認し、結果を `.loop/test_logs/コスモパワー.log` に保存した

## 進捗表
- `.internal/progress/move.md` の コスモパワー 行を更新（仕様書列 `-`→`x`、計画書列 `-`→`x`、レビュー列 `-`→`✓`）
