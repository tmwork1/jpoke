# こうそくいどう レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/こうそくいどう.md`）。一次情報（.internal/wiki/moves/こうそくいどう.html）を基に、
  タイプ・分類・優先度・PP（champions基準の20）・効果（自分の『すばやさ』ランクを2段階上げる）・
  判定フラグ（急所×、命中判定なし、追加効果なし、まもる×、マジックコート×、よこどり○、オウムがえし×）を記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/こうそくいどう.md`）。既存の同種ハンドラ（`つるぎのまい_modify_attacker_stats` 等）に
  合わせて `ON_STATUS_HIT` / priority=100(省略) / `attacker:self`(省略) の構成とした。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_status.py` の `こうそくいどう_modify_attacker_stats`（`modify_attacker_stats(stats={"spe": 2})`）は
  仕様通りで変更なし。
- `src/jpoke/data/moves/move_ka.py` の `pp` を `30`（Gen9本家値）から `20`（champions基準、src/jpoke/data/ps-champ-ja/moves.json）に修正した。

## リーサル計算
- 対象外（.internal/progress/move.md のリーサル実装列が n/a。ダメージを与えない自己ランク変化技のため）。

## テスト
- `tests/moves_status/test_move_ka.py` に以下を追加:
  - `test_こうそくいどう_すばやさが2段階上がる`
  - `test_こうそくいどう_すばやさが上限のとき失敗する`（すばやさランクが+6のとき技が失敗しランクが変化しないことを確認）
