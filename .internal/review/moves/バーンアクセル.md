# バーンアクセル レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/バーンアクセル.md`）
- 一次情報（ポケモンWiki）に基づき、タイプ・分類・威力・命中・PP（本家10→Champions12）・
  「直接攻撃: ×」（物理技だが接触判定なし）・追加効果（30%でやけど）・判定フラグを記載
- 技の仕様欄（アンコール・ものまね・まねっこ・さいはいが失敗する／ゆびをふるで選ばれない）を
  一次情報どおりに記載し、本プロジェクトでの実装可否（ものまね・さいはい・ゆびをふるは
  未実装のため対応フラグなし）を明記

## 実装計画書
- 新規作成（`.internal/plan/moves/バーンアクセル.md`）
- レビュー前の実装の誤り（PP=15、`flags`に不要な`"contact"`、不足していた`"non_encore"`）を
  明記し、修正後のコードを記載
- `Event.ON_DAMAGE_HIT` は `.internal/spec/turn.md` に個別記載がないため、既存の同種やけど付与技
  （ひのこ・かえんほうしゃ等、いずれも `MoveHandler` デフォルトpriority=100）を参照して
  同じ扱いとする根拠を明記
- リーサル計算は `n/a`（進捗表どおり対象外）

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_attack.py` の `バーンアクセル_apply_burn_to_defender` は
  `apply_ailment_to_defender(..., ailment="やけど", chance=0.3)` で仕様（30%）と一致しており、変更なし
- `src/jpoke/data/moves/move_ha.py` の `MoveData` を修正:
  - `pp=15` → `pp=12`（`src/jpoke/data/ps-champ-ja/moves.json` に本技の掲載がないため、同じ本家PP10を
    持つ他の技（10まんばりき・マジカルシャイン・オーラウイング等 → いずれもChampions PP12）の
    変換結果から類推し、PP8/12/16/20の4段階統一ルールに基づき12に修正）
  - `flags` から `"contact"` を削除（一次情報「直接攻撃: ×」により、物理技だが接触判定を
    持たないため誤りだった）
  - `flags` に `"non_encore"` を追加（一次情報「この技に対するアンコールは失敗する」に対応。
    既存の `non_copycat` は既に付与済みで変更なし）
  - `type`, `category`, `power`, `accuracy`, `handlers` はすべて仕様と一致しており変更なし

## リーサル計算
- 対象外（`n/a`）。`.internal/progress/move.md` の該当行が元から `n/a` であり、本技はやけど状態異常を
  付与するのみでダメージ量自体に影響しないため専用のリーサルハンドラは不要と判断した

## テスト
- `tests/moves_attack/test_move_ha.py::test_バーンアクセル_やけどが発動する` は
  仕様（30%でやけど付与）と一致しており修正不要（変更なし）
- `tests/moves_status/test_move_ma.py::test_まねっこ_スターモービル専用技はコピーできない` は
  `non_copycat` フラグの回帰テストとして既に存在しており修正不要（変更なし）
- `flags` から `"contact"` を削除した回帰確認のため、
  `tests/abilities/test_ability_sa.py::test_さめはだ_接触ダメージ` のパラメータに
  `("さめはだ", "バーンアクセル", 0)` を追加（非接触のため接触ダメージが発生しないことを確認）
- `flags` に `"non_encore"` を追加した回帰確認のため、
  `tests/volatiles/test_volatile_a.py` に
  `test_アンコール_バーンアクセルを使った相手には失敗する` を新規追加
  （既存の `test_アンコール_non_encoreラベルの技を使った相手には失敗する` と同様のパターンで
  `defender.executed_move = Move("バーンアクセル")` を用いてアンコールが失敗することを確認）
- `python -m pytest tests/ -v` で全3949件パス（1件skip）を確認
  （ログ: `.loop/test_logs/バーンアクセル.log`）
