# ころがる レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ころがる.md`）。
  - 基本情報（いわ・物理・威力30・命中90・PP20・接触あり）
  - ターンごとの威力表（通常時/まるくなる時）
  - 判定フラグ（急所・命中判定・追加効果・まもる・おうじゃのしるし・マジックコート・よこどり・オウムがえし）
  - 強制行動の仕様（外れる・技無効化・状態異常等で中断、5回命中で終了、PPは初回のみ消費）
  - 特性・道具との相互作用（まるくなる・おやこあい・ばけのかわ/アイスフェイス・メトロノーム）
  - スコープ外事項（ねごと・さきどり・フリーフォールとの相互作用は、それら自体が未実装のため対象外と明記）

## 実装計画書
- 新規作成（`.internal/plan/moves/ころがる.md`）。
  - `あばれる`/`あなをほる` と同じ「forced 揮発性状態 + `force_command`」パターンを踏襲しつつ、
    「命中回数に応じた指数的威力上昇」「外れ・失敗で即終了」という差分を専用揮発性状態 `ころがる` で実装する方針を明記。
  - ハンドラ構成表・priority根拠・subject_spec根拠を記載。
  - `おやこあい`（Parental Bond）が `ころがる`/`アイスボール` を連続攻撃化してしまう既存の不整合
    （仕様書 `.internal/spec/abilities/おやこあい.md` には除外対象と明記されているが実装が対応していなかった）
    を本技の実装に必須の修正として記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/move.py`: `ころがる` に `Event.ON_HIT: ころがる_apply` を登録（従来は `handlers={}` で未実装）。
- `src/jpoke/handlers/move_attack.py`: `ころがる_apply` を新規実装
  （初回命中で揮発性状態を付与、継続命中で count を+1、count>=5で解除）。
- `src/jpoke/data/volatile.py`: 揮発性状態 `ころがる`（`forced=True`）を新規登録。
  - `ON_MODIFY_COMMAND_OPTIONS`: 既存共通関数 `force_command`（行動固定）
  - `ON_CALC_POWER_MODIFIER`: `ころがる_boost_power`（count に応じて 2^count 倍）
  - `ON_TURN_END`（priority=150）: `ころがる_check_interrupt`（このターンの行動が失敗していた場合に解除）
- `src/jpoke/handlers/volatile.py`: `ころがる_boost_power` / `ころがる_check_interrupt` を新規実装。
- `src/jpoke/types/volatile.py`: `python scripts/generate_literals/generate_volatile_literal.py` で `"ころがる"` を追加。
- `src/jpoke/handlers/ability.py`: `おやこあい_modify_hit_count` に `"ころがる"`/`"アイスボール"` の除外を追加
  （既存は `がむしゃら` のみ除外しており、仕様書と不整合だったため修正）。
- `python scripts/sort_handlers.py` / `python scripts/sort_data/sort_moves.py` / `sort_abilities.py` を実行済み。

## リーサル計算
- 対象外（`.internal/progress/move.md` の「リーサル実装」「リーサルテスト」列が `n/a` のためスキップ）。

## テスト
- `tests/moves_attack/test_move_ka.py` に追加:
  - 初回命中時に揮発性状態（count=1）が付与される
  - 継続中の命中で count が+1される
  - count=4 の状態で命中すると5回目として揮発性状態が解除される（強制行動終了）
  - 揮発性状態の count に応じて威力補正が 2^count 倍になる
  - 強制行動中は `Command.FORCED` が返り、解決後の技が `ころがる` になる
  - 外れた場合、ミス直後は揮発性状態が残るが、ターン終了時（`ON_TURN_END`）に解除される
  - おやこあい所持ポケモンが使用しても連続攻撃にならない（count は1のまま、hits_taken=1）
- `tests/abilities/test_ability_a.py` に追加:
  - おやこあい: ころがるには適用しない（既存の `がむしゃら` 除外テストと同じパターンで追加）
- `python scripts/sort_tests.py tests/moves_attack/test_move_ka.py tests/abilities/test_ability_a.py` を実行。
- `python scripts/generate_test_list.py` を実行し `.internal/tests/` を更新。
- `python -m pytest tests/ -v` を実行し、3362 passed, 0 failed, 1 skipped（既存のskip1件は本技と無関係）。
  ログは `.loop/test_logs/ころがる.log` に保存。
