# りゅうせいぐん レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/りゅうせいぐん.md`）。
  - タイプ: ドラゴン / 分類: 特殊 / 威力: 130（第六世代以降。本プロジェクト対象世代） /
    命中: 90 / PP: 5 / 対象: 相手1体 / 優先度: 0 / 接触なし
  - 効果: 命中して成功した後、自分の『とくこう』ランクが2段階下がる（確定効果）。
  - 追加効果には該当しないため、ちからずく等の対象にならないことを明記。

## 実装計画書
- 新規作成（`.internal/plan/moves/りゅうせいぐん.md`）。
  - 既存実装（`handlers/move_attack.py` の `りゅうせいぐん_sharply_lower_spa_C`、
    `data/moves/move_ra.py` の `MoveData` 登録）は仕様と一致しており修正不要と判断。
  - `ON_HIT` イベント・priority 100（デフォルト）を採用した根拠として、既存の同種ハンドラ
    （いじげんラッシュ等、命中直後に自分のランクを下げる実装）を参照したことを記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ra.py` の `MoveData` で `pp=5`（Gen9本家基準）になっていたのを
  `pp=8` に修正した。ポケモンチャンピオンズでは全技のPPが8/12/16/20の4段階に統一
  （`.internal/champions/changes_from_sv.md`）されており、`src/jpoke/data/ps-champ-ja/moves.json:942`
  でも8となっている。
- `src/jpoke/handlers/move_attack.py` の `りゅうせいぐん_sharply_lower_spa_C`
  （`modify_attacker_stats` で確定して自分の『とくこう』を2段階下げる）は仕様通りで誤りなし。
  `secondary_effect` フラグを付与していない点も、自分自身への確定効果は「追加効果」に
  該当しないという実際の仕様（ちからずく対象外）と整合していることを確認した。

## リーサル計算
- 変更なし。`src/jpoke/handlers/lethal.py` の `りゅうせいぐん_lower_spa`
  （命中後に `ctx.attacker.rank["spa"]` を2段階下げる）、`data/moves/move_ra.py` の
  `lethal_handlers={LethalEvent.ON_HIT: LethalHandler(l.りゅうせいぐん_lower_spa)}` は
  いずれも仕様と一致しており誤りなし。確定効果のため `ctx.move_secondary` によるガードが
  不要な点も正しい。

## テスト
- 追加: `tests/moves_attack/test_move_yarawa.py` に
  `test_りゅうせいぐん_とくこう2段階低下が発動する`（命中後、自分の『とくこう』ランクが
  -2になることを確認する通常テスト）を追加。
  従来は `tests/test_lethal.py` の `test_りゅうせいぐん_とくこうダウン`
  （リーサル計算経由のテスト）のみ存在し、通常の攻撃技テストが欠落していたため補った。
- `.internal/progress/move.md` の りゅうせいぐん 行を修正:
  - 仕様書列: `-` → `x`
  - 計画書列: `-` → `x`
  - テスト列: `-` → `x`
  - レビュー列: `-` → `✓`
  - PP列: 実装の `pp=5`（Gen9基準・誤り）を `pp=8`（チャンピオンズ基準）に修正したため、
    既に正しかった `8` のまま維持
  - 効果列: `？` → 「命中して成功した後、自分の『とくこう』ランクが2段階下がる（確定効果）。」
  - リーサル実装・リーサルテスト列は既存の `x` を維持（実装済みで誤りなし）。
- `python -m pytest tests/ -v` を実行し、全件（3643件、1スキップ）がパスすることを確認した
  （テスト実行順序に依存する既存の乱数シード関連の断続的な失敗が別カテゴリのテストで
  観測されたが、`りゅうせいぐん` の変更とは無関係な既知の事象であり、単独実行では
  問題なくパスすることを確認済み）。
