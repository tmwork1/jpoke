# ほうでん レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ほうでん.md`）。.internal/wiki/moves/ほうでん.html を基に、
  タイプ・分類・威力・命中・PP・追加効果（30%まひ）・判定フラグを整理した。

## 実装計画書
- 新規作成（`.internal/plan/moves/ほうでん.md`）。既存実装（`data/moves/move_ha.py` /
  `handlers/move_attack.py`）が仕様書と一致していることを確認し、ハンドラ構成・priority根拠・
  エッジケースを記載した。

## 実装（handlers/ / data/）
- 変更なし。`ほうでん_apply_paralysis_to_defender`（`handlers/move_attack.py`）が
  `apply_ailment_to_defender(..., ailment="まひ", chance=0.3)` を呼び出しており、
  `data/moves/move_ha.py` の `MoveData`（type=でんき, category=special, pp=15, power=80,
  accuracy=100, flags={"secondary_effect"}, ON_DAMAGE_HIT）は仕様書と一致していた。
  10まんボルト・のしかかり等の既存の同種「追加効果」実装と同一パターンであることを確認した。

## リーサル計算
- 対象外。`.internal/progress/move.md` の「リーサル実装」列が `n/a`（まひはダメージに直接影響しないため）。

## テスト
- 変更なし。`tests/moves_attack/test_move_ha.py` に既存のテストで
  「30%でまひが発動する」「でんきタイプにはまひが付与されない」の2ケースが検証済みだった。

## その他
- `.internal/progress/move.md` のほうでん行で PP列が `16` と誤記されていたのを `15`（実装・一次情報と一致）に修正した。
