# ひゃっきやこう レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/ひゃっきやこう.md`）。
- 一次情報（`.internal/wiki/moves/ひゃっきやこう.html`）を参照し、タイプ・分類・命中・PP・対象・
  優先度・追加効果（30%やけど）・威力2倍条件（状態異常時、「たたりめ」と同条件）を記載。
- 威力はSVで60、Pokémon Championsで65に変更されている旨を明記し、本プロジェクトの対象
  （README「対象範囲」＝チャンピオンズ優先）に従い65を採用する方針とした。

## 実装計画書
- 新規作成（`.internal/plan/moves/ひゃっきやこう.md`）。
- 「たたりめ」の計画書を参考に、`ON_CALC_POWER_MODIFIER`（威力2倍判定）と`ON_DAMAGE_HIT`
  （30%やけど付与）の2ハンドラ構成、priority/subject_specの根拠、リーサル計算がn/aである
  根拠を記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `"ひゃっきやこう"` エントリで `power=60`（SV値のまま）
  になっていたのを `power=65`（Champions値）に修正した。
- `pp=15` は仕様と一致（誤りなし）。
- `handlers/move_attack.py` の `ひゃっきやこう_apply_burn_to_defender` /
  `ひゃっきやこう_double_power_when_ailment` は仕様・計画と一致しており変更なし。
- `data/moves/move_ha.py` を修正したため `python scripts/sort_data/sort_moves.py` を実行
  （並び順に変更なし）。

## リーサル計算
- 対象外（n/a）。威力補正のみでダメージ分布そのものを離散化しないため、「たたりめ」と同様に
  `handlers/lethal.py` への専用ハンドラ追加は不要。

## テスト
- `tests/moves_attack/test_move_ha.py` の既存テスト
  `test_ひゃっきやこう_やけどが発動する` / `test_ひゃっきやこう_状態異常のとき威力2倍` は
  威力の実数値に依存しない（`power_modifier` や状態異常付与の有無のみを検証）ため、
  威力修正（60→65）の影響を受けず変更不要と判断した。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ha.py` を実行（並び順に変更なし）。
- `python scripts/generate_test_list.py` を実行（`.internal/tests/` に差分なし）。
- `python -m pytest tests/ -v` は3577件成功・1件スキップで全件パス
  （`.loop/test_logs/ひゃっきやこう.log`）。
  途中で `test_せいなるつるぎ_相手のぼうぎょランクを無視する` 等が偶発的に失敗するケースを
  確認したが、変更前のコード（`git stash`）でも同様の時刻依存シード起因のフレーキー失敗が
  再現することを確認済みで、本タスクの変更とは無関係。
