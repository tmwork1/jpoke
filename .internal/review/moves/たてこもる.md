# たてこもる レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/たてこもる.md`）。.internal/wiki/moves/たてこもる.html を一次情報とし、
  タイプ・分類・PP・対象・効果・判定フラグ（まもる×／マジックコート×）を記載

## 実装計画書
- 修正した内容:
  - PP を 10 → 12 に修正（champions 基準は src/jpoke/data/ps-champ-ja/moves.json により 8/12/16/20 の
    4段階に統一されており、たてこもるは 12。Gen8本家PP=10がそのまま実装に残っていた移行漏れ）
  - `MoveData` に `target="self"` を明示するよう修正（未指定だとデフォルトの `"foe"` になり、
    `is_blocked_by_protect` / `is_reflectable` が誤って True になる）
  - ハンドラのサンプルコードが旧ランク表記（`{"B": 2}`）のままだったため、現行の `Stat` リテラル
    （`{"def": 2}`）に修正し、引数の型注釈も `AttackContext` に修正

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `たてこもる` エントリを修正:
  - `pp=10` → `pp=12`（champions 基準へ修正）
  - `target="self"` を追加（欠落によりまもる／マジックコートに誤反応するバグを修正）
- `handlers/move_status.py` の `たてこもる_modify_attacker_stats` は仕様通り（`modify_attacker_stats`
  で `def` を2段階上げる）であり、変更なし

## リーサル計算
- 対象外（`.internal/progress/move.md` の該当行はリーサル実装／リーサルテストとも n/a。自己ランク変化
  技のためダメージ算出とは無関係）

## テスト
- 既存の `test_たてこもる_ぼうぎょ2段階上がる` は変更なし
- 追加: `test_たてこもる_自分対象のためまもるで防がれない`
  （相手が「まもる」状態でも自分対象技のため効果が発動することを確認。target="self"欠落バグの
  再発防止テスト）
