# うずしお レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/うずしお.md`）。
  - 一次情報（`.internal/wiki/moves/うずしお.html`）に基づき、タイプ・分類・威力(35)・命中(85)・PP(15)・
    対象・優先度・判定フラグを記載。
  - バインド状態の詳細仕様は重複させず `.internal/spec/volatiles/バインド.md` を参照する形にした。
  - ダイビング状態への2倍ダメージは `うずしお` 固有実装ではなく `ダイビング` 揮発性状態側
    （`ダイビング_boost_power`）の共通処理であることを明記した。

## 実装計画書
- 新規作成（`.internal/plan/moves/うずしお.md`）。
  - 既存実装（`apply_bind_to_defender` / `_apply_bind`）が仕様と一致していることを確認し、
    しめつける・すなじごく・ほのおのうずと共通パターンであることを記録した。
  - Priority根拠（`ON_DAMAGE_HIT` / デフォルト100、既存の同種ハンドラに準拠）を明記した。

## 実装（handlers/ / data/）
- 変更なし。
  - `src/jpoke/data/move.py` の `"うずしお"` エントリ（type=みず, category=special, pp=15,
    power=35, accuracy=85, `ON_DAMAGE_HIT: apply_bind_to_defender`）は仕様と一致。
  - `src/jpoke/handlers/move_attack.py` の `apply_bind_to_defender`
    （しめつける・すなじごく・ほのおのうずと共有）も仕様通り。
  - ダイビング状態への2倍ダメージは `src/jpoke/handlers/volatile.py` の `ダイビング_boost_power`
    （`なみのり`・`うずしお` 共通処理）で実装済みであることを確認した。

## リーサル計算
- 変更なし（`.internal/progress/move.md` の「リーサル実装」列は `x`）。
  - `src/jpoke/data/move.py` の `lethal_handlers={LethalEvent.ON_HIT: LethalHandler(l._apply_bind)}`
    と `src/jpoke/handlers/lethal.py` の `_apply_bind` を仕様・実装と照合し、誤り・欠落なしを確認。

## テスト
- 追加: `tests/moves_attack/test_move_a.py` に基本動作テストを4件追加（従来 うずしお 自身の
  攻撃技としての基本動作テストが存在しなかったため）。
  - `test_うずしお_バインド中は相手が交代できない`
  - `test_うずしお_バインド中は相手が毎ターン最大HPの8分の1ダメージを受ける`
  - `test_うずしお_命中後にバインド状態になる`
  - `test_うずしお_攻撃者が交代するとバインドが解除される`
- 既存の関連テストは変更なし（レビューして問題なしと確認）:
  - `tests/moves_attack/test_move_ta.py::test_ダイビング_なみのりうずしおは水中でも命中し威力2倍`
  - `tests/test_lethal.py::test_うずしお_バインド付与`
- `scripts/sort_tests.py tests/moves_attack/test_move_a.py` でソート、
  `scripts/generate_test_list.py` でテスト一覧を更新、
  `python -m pytest tests/ -v` で全3247件（1件skip）が通ることを確認
  （結果: `.loop/test_logs/うずしお.log`）。
  - 途中2回、うずしおと無関係な既存テスト（`test_リフレッシュ_まひを治す` / 
    `test_いかく_交代フェーズの割り込み連鎖で...`）が乱数依存のflakyな失敗を示したが、
    再実行で成功することを確認済み（本タスクの変更とは無関係）。

## 補足
- `.internal/progress/move.md` の うずしお 行の PP が `16` と誤記載されていたため、
  一次情報・実装（`pp=15`）に合わせて `15` に修正した。
