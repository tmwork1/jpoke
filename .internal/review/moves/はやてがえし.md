# はやてがえし レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/はやてがえし.md`）。
- 一次情報（`.internal/wiki/moves/はやてがえし.html`）から基本情報・効果・「技の仕様」節の
  はやてのつばさ／グラススライダー／アンコールとの相互作用・判定フラグをまとめた。

## 実装計画書
- 新規作成（`.internal/plan/moves/はやてがえし.md`）。
- 既存実装の判定ロジック（`command_reserved()` による行動済みチェック、
  `defender_move.is_attack` による変化技チェック、`calc_move_priority` による
  動的な優先度再計算）は仕様と一致していることを確認した。
- `Event.ON_TRY_MOVE_1` の priority 指定漏れ（後述）を修正方針として明記した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `はやてがえし` エントリで、
  `Event.ON_TRY_MOVE_1: h.MoveHandler(ha.はやてがえし_try_move)` に priority が
  指定されておらずデフォルト値（100）になっていたバグを修正し、`priority=30` を明記した。
  同種の「対象行動済み・変化技選択」判定を行う `ねこだまし_check_first_turn` /
  `ふいうち_try_move` / `じんらい_try_move` はいずれも priority=30 で登録されており、
  `.internal/spec/turn.md` の `Event.ON_TRY_MOVE_1` 表にも該当行があるため、これに揃えた。
- `python scripts/sort_data/sort_moves.py` を実行済み（並び順に変化なし）。
- `handlers/move_attack.py` 側の `_はやてがえし_can_apply` / `はやてがえし_try_move` /
  `はやてがえし_apply_flinch` のロジック自体には変更なし（レビューの結果、仕様と一致）。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装/リーサルテスト列は n/a）。
  追加効果はひるみのみでダメージ計算に影響する可変要素を持たないため専用ハンドラは不要。

## テスト
- 既存4件（ひるみ発動、先制変化技への失敗、先制攻撃技への成功、通常攻撃技への失敗）を確認、
  変更なし。
- 以下の2件を追加した。
  - `test_はやてがえし_はやてのつばさが発動しない場合は失敗する`:
    特性はやてのつばさ持ちの相手がHPが満タンでない状態でひこう技を選択した場合、
    優先度が上がらず（優先度0のまま）はやてがえしが失敗することを確認する。
  - `test_はやてがえし_はやてのつばさによる優先度上昇技にも成功する`:
    特性はやてのつばさによりひこう技の優先度が+1になっている場合、
    `calc_move_priority` の動的再計算により正しく成功することを確認する。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ha.py` でソート、
  `python scripts/generate_test_list.py` で `.internal/tests/moves_attack.md` を更新した。
- `python -m pytest tests/ -v` で3569件 passed / 1件 skipped を確認
  （ログ: `.loop/test_logs/はやてがえし.log`）。

## その他
- `.internal/progress/move.md` の はやてがえし 行を更新した:
  - 仕様書列 `-` → `x`、計画書列 `-` → `x`、レビュー列 `-` → `✓`。
  - PP列が `16` になっていたが、一次情報（wiki）・実装（`pp=15`）と一致する `15` に修正した。
