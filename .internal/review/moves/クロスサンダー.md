# クロスサンダー レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/クロスサンダー.md`）。
- 一次情報（`.internal/wiki/moves/クロスサンダー.html`）＋Web検索で「デュアルクロス」の
  発動条件（同ターン中に直前でクロスフレイムが命中していた場合のみ威力2倍。まもる・
  タイプ相性・特性吸収・命中失敗の場合は不成立）を確認して反映した。
- PPは `src/jpoke/data/ps-champ-ja/moves.json` に本技の記載がないため、既存レビュー済みの
  ガリョウテンセイ（同じく未収録・本家PP5→チャンピオンズPP8）の変換規則を適用し、
  本家PP5→チャンピオンズPP8として記載した。

## 実装計画書
- 新規作成（`.internal/plan/moves/クロスサンダー.md`）。
- `Event.ON_CALC_POWER_MODIFIER`（威力2倍判定）と `Event.ON_HIT`（命中ターンの記録）の
  2イベントを使う設計とし、`battle.last_used_move_name` を使えない理由
  （クロスサンダー自身の発動時点で上書きされてしまう／命中でなく失敗しないことしか
  保証しない）を明記した。

## 実装（handlers/ / data/）
- 修正した内容:
  - `src/jpoke/data/moves/move_ka.py`: クロスサンダーの `handlers={}`（追加効果なし、
    誤り）を修正し、`ON_CALC_POWER_MODIFIER` / `ON_HIT` ハンドラを登録。PPを5→8に修正
    （`src/jpoke/data/ps-champ-ja/moves.json` 未収録技の本家PP5→チャンピオンズPP8変換規則、
    ガリョウテンセイと同様）。
  - `src/jpoke/handlers/move_attack.py`: `クロスサンダー_calc_power` /
    `クロスサンダー_record_hit` を新規実装。
  - `src/jpoke/core/battle.py`: `battle.fusion_bolt_used_turn` /
    `battle.fusion_flare_used_turn` フィールドを新規追加（`round_used_turn` と
    同じ配置パターン）。
- 付随修正（クロスサンダーの効果を機能させるために必要な最小限の対応）:
  - クロスフレイム（`src/jpoke/data/moves/move_ka.py`）の `handlers={}` も修正し、
    `クロスフレイム_calc_power` / `クロスフレイム_record_hit`
    （`src/jpoke/handlers/move_attack.py`）を追加登録した。デュアルクロスは
    クロスサンダー・クロスフレイム双方が対になって初めて機能するため、
    クロスフレイム側にも同じ仕組みのハンドラが無いとクロスサンダーの効果が
    永久に発動しない（死んだ実装になる）ことから追加した。
  - クロスフレイム自体の仕様書・計画書・PP修正・`.internal/progress/move.md` の
    クロスフレイム行の更新は本レビュー（クロスサンダー担当）のスコープ外として
    見送った（他レビューとのコンフリクト回避のため、進捗表のクロスフレイム行は
    意図的に変更していない）。

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- 追加した内容（`tests/moves_attack/test_move_ka.py`、クロスサンダー用テスト5件）:
  - 直前にクロスフレイムが命中すると威力が2倍(200)になる
  - 直前に何も技が成功していない場合は威力そのまま(100)
  - 直前がクロスフレイム以外の技の場合は威力そのまま(100)
  - クロスフレイムがまもるで防がれた場合は威力そのまま(100)（命中していないため不成立）
  - ターンをまたぐと効果は持続しない（前のターンの命中は次ターンに影響しない）
- `python -m pytest tests/ -v` を実行し、3726 passed, 1 skipped（既存のskipで新規失敗なし）
  を確認。ログは `C:\Users\tmtmp\Documents\pokemon\jpoke\.loop\test_logs\クロスサンダー.log`
  に保存した。
