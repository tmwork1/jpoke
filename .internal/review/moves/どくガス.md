# どくガス レビュー結果

## 仕様書
- 新規作成（`.internal/spec/moves/どくガス.md`）
- 一次情報（`.internal/wiki/moves/どくガス.html`）をもとに、タイプ/分類/威力/命中率（55→80→90の変遷）/
  対象/優先度/接触の有無、効果（どく付与、どく・はがねタイプ無効）、判定
  （まもる○・マジックコート○・おうじゃのしるし×・よこどり×・オウムがえし○）を記載
- PP欄に、`src/jpoke/data/ps-champ-ja/moves.json` に単独項目が存在しないため、championsのPP圧縮則
  （SV基準PPが20以上の技は一律20に圧縮。`どくのこな` SV35→20、`いとをはく` SV40→20、
  `でんこうせっか` SV30→20で確認済みのパターン）から20を導出した根拠を明記
- どくガスは粉技（`powder`フラグ）ではない点（どくのこなとの違い）を明記

## 実装計画書
- 新規作成（`.internal/plan/moves/どくガス.md`）
- 既存実装の構成（`ON_STATUS_HIT` ハンドラ・`apply_ailment_to_defender` 共通関数の利用）を記載し、
  PP値のバグを明記

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の「どくガス」エントリで `pp=40`（Gen9本家基準のまま）に
  なっていたバグを `pp=20`（championsのPP圧縮則により導出）に修正
- `handlers/move_status.py` の `どくガス_apply_ailment_to_defender` は仕様書・計画書と整合しており
  変更なし（`どくのこな_apply_ailment_to_defender` と同じパターンで `apply_ailment_to_defender` を
  呼び出すのみ。`subject_spec` 未指定＝デフォルトの `attacker:self` で `AttackContext` のロールと一致）

## リーサル計算
- 対象外（`.internal/progress/move.md` のリーサル実装列が `n/a`）

## テスト
- `tests/moves_status/test_move_ta.py` に `test_どくガス_PPは20` を追加
  （`MOVES["どくガス"].pp == 20` を確認。PP修正の回帰テスト）
- 既存テスト（`test_どくガス_どくタイプには無効`・`test_どくガス_どく状態にする`・
  `test_どくガス_命中率90で外れることがある`）は仕様と一致しており変更なし
- `python -m pytest tests/ -v` で全件パス（4289 passed, 1 skipped）を確認
