# かえんのまもり レビュー結果

## 仕様書

- 新規作成: `.internal/spec/volatiles/かえんのまもり.md`
  - 技の基本情報（タイプ・分類・PP・優先度・範囲）を記載
  - 攻撃技のみ防ぐ（変化技は防げない）仕様を明記
  - 接触攻撃技を防いだ場合にやけどを付与する仕様を明記
  - `ぼうごパット` による接触判定無効でやけどにならない仕様を明記
  - `ふかしのこぶし` による守り貫通・やけど無効の仕様を明記
  - ターン処理優先度（ON_TRY_MOVE_1: priority=100、ON_TURN_END: デフォルト）を記載

## 実装計画書

- 新規作成: `.internal/plan/volatiles/かえんのまもり.md`
  - ハンドラ3点（かえんのまもり_protect、かえんのまもり_remove_volatile、かえんのまもり_apply_volatile_to_attacker）の概要と優先度根拠を記載
  - すべて実装済みとしてマーク

## 実装（handlers/ / data/）

- 変更なし
  - `handlers/volatile.py` の `かえんのまもり_protect` は `_run_protect(..., ailment_on_contact="やけど", protect_non_attack=False)` を正しく呼び出している
  - `handlers/volatile.py` の `かえんのまもり_remove_volatile` はターン終了時に状態を解除している
  - `data/volatile.py` の登録は `ON_TRY_MOVE_1 (priority=100, subject_spec="defender:self")` と `ON_TURN_END` で正しく登録されている
  - `handlers/move_status.py` の `かえんのまもり_apply_volatile_to_attacker` と `data/move.py` の技定義も正しい
  - `protect_non_attack=False` により変化技を防がない実装が仕様と一致している
  - `is_contact` チェックにより接触判定がある場合のみやけどを付与（ぼうごパット対応も自動的に処理）

## テスト

- `tests/moves_status/test_move_ka.py` に追加:
  - `test_かえんのまもり_技使用でかえんのまもり状態が付与される`: 技を使うと自分にかえんのまもり揮発性状態が付与されることを確認
- `tests/test_volatile.py` に追加:
  - `test_かえんのまもり_ぼうごパット持ちの接触技はやけどにならない`: ぼうごパットを持つ攻撃者は接触判定が無効になりやけどにならないことを確認
- 既存テスト（変更なし）:
  - `test_かえんのまもり_接触技でやけどを付与`: 正常動作確認済み
  - `test_かえんのまもり_非接触ではやけどにならない`: 正常動作確認済み
  - `test_まもる系_ターン終了で解除` (parametrize: かえんのまもり含む): 正常動作確認済み
  - `test_まもる系_攻撃技を無効化` (parametrize: かえんのまもり含む): 正常動作確認済み
  - `test_まもる系_相手対象変化技への反応` (parametrize: かえんのまもり=False): 正常動作確認済み
- `python scripts/sort_tests.py tests/test_volatile.py tests/moves_status/test_move_ka.py` 実行済み
- `python scripts/generate_test_list.py` 実行済み
- 全 1847 テスト PASS
