# まもる レビュー結果

## 仕様書

新規作成: `.internal/spec/volatiles/まもる.md`

一次情報（`.internal/wiki/volatiles/まもる_(状態変化).html`）をもとに作成。
主な記載内容:
- まもる volatile の付与・消滅条件
- 防御対象（攻撃技・変化技ともに防ぐ / protect_non_attack=True）
- フェイント等による解除仕様（第九世代では次ターンの成功率リセット）
- 連続使用による失敗率上昇（1/3確率 → 未実装・保留として明記）
- ON_TRY_MOVE_1 priority=100、ON_TURN_END で解除

## 実装計画書

新規作成: `.internal/plan/volatiles/まもる.md`

実装の意図・設計根拠を記録。連続使用失敗をスコープ外として明記し、実装するとした場合の設計も記載。

## 実装（handlers/ / data/）

### src/jpoke/handlers/volatile.py

変更なし。

`まもる_protect` は `_run_protect(battle, ctx, value)` を呼び出す。
- `protect_non_attack=True` (デフォルト): 変化技も防ぐ ✓
- 接触時の追加効果なし ✓
- `subject_spec="defender:self"` (攻撃フェーズ = AttackContext) ✓

`まもる_remove_volatile` はターン終了時に解除する。
- `subject_spec="source:self"` (非攻撃フェーズ = EventContext) ✓

### src/jpoke/data/volatile.py

変更なし。

"まもる" の登録:
- `ON_TRY_MOVE_1` priority=100、subject_spec="defender:self" ✓
- `ON_TURN_END` デフォルト priority、subject_spec="source:self" ✓

### 既知の課題（スコープ外）

- **VOLATILES 辞書の順序**: まもる（ま行）が ロックオン（ろ行）より後に配置されており、五十音順違反がある。これは protect 系すべてに共通する既存の構造的問題で、キングシールド等の先行レビュー時から存在する。本レビューの対象外として現状維持。
- **連続使用失敗率**: `ON_TRY_MOVE_1` priority=30 への失敗チェック未実装。Pokemon に protect_streak フィールド追加が必要。仕様書・計画書に記録済み。

### src/jpoke/handlers/move_status.py

変更なし。

`まもる_apply_volatile_to_attacker`: まもる volatile を自分に付与 ✓
`みきり_apply_volatile_to_attacker`: 同じく まもる volatile を付与 ✓

### src/jpoke/data/move.py

変更なし。

「まもる」: ON_STATUS_HIT で apply_volatile_to_attacker (volatile="まもる") ✓
「みきり」: ON_STATUS_HIT で apply_volatile_to_attacker (volatile="まもる") ✓

## テスト

### 追加テスト

`tests/test_volatile.py` に以下を追加:

- `test_まもる_みきりでもまもる状態になる`: みきりを使用すると まもる volatile が付与されること、および付与後に攻撃技が防がれることを確認

### 既存テスト（変更なし）

- `test_シャドーダイブ_まもる中の相手にダメージを与えられる`: シャドーダイブがまもる貫通 ✓
- `test_まもる_相手自身が対象の技は無効化しない`: 自分対象技は防がない ✓
- `test_まもる系_ターン終了で解除[まもる]`: ターン終了で解除 ✓
- `test_まもる系_攻撃技を無効化[まもる]`: 攻撃技を無効化 ✓
- `test_まもる系_相手対象変化技への反応[まもる-True]`: 変化技も防ぐ ✓

### sort / generate_test_list 実行

`python scripts/sort_tests.py tests/test_volatile.py` 実行済み（151 関数ソート）
`python scripts/generate_test_list.py` 実行済み

### テスト結果

全テスト実行: 1876 passed, 6 failed（失敗は test_move_a.py の既存バグ・まもる無関係）
まもる関連テスト: 18 passed / 18
