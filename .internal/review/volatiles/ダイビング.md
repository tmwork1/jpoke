# ダイビング レビュー結果

## 仕様書
- 新規作成: `.internal/spec/volatiles/ダイビング.md`
  - 概要・発生条件・消滅条件・効果（回避・威力2倍・行動固定・交代禁止）・スコープ外を記載
  - 「なみのり・うずしおは命中し、かつダメージが2倍になる（第五世代以降）」を明記

## 実装計画書
- 新規作成: `.internal/plan/volatiles/ダイビング.md`
  - 既存実装済みハンドラ一覧を記載
  - レビュー時に `ON_CALC_POWER_MODIFIER`（ダイビング_boost_power）を追加した旨を記録
  - priority根拠: あなをほる_boost_power / そらをとぶ_boost_power と同種ハンドラで priority=100

## 実装（handlers/ / data/）
- `src/jpoke/handlers/volatile.py`: `ダイビング_boost_power` 関数を追加
  - なみのり・うずしおを受けたとき威力を2倍にする（`ON_CALC_POWER_MODIFIER` / `defender:self`）
  - 実装位置は五十音順（タールショットの後、ちいさくなるの前）
- `src/jpoke/data/volatile.py`: `"ダイビング"` エントリに `ON_CALC_POWER_MODIFIER` ハンドラ登録を追加

## テスト
- `tests/test_volatile.py` に2件追加:
  - `test_ダイビング_なみのりうずしおは威力2倍`: なみのり・うずしお使用時に power_modifier が8192になることを確認（パラメータ化）
  - `test_ダイビング_その他技は威力2倍にならない`: ダイビング状態でないときのなみのりは power_modifier が4096になることを確認
- `python scripts/sort_tests.py tests/test_volatile.py` でソート済み
- `python scripts/generate_test_list.py` でテスト一覧を更新済み
- 全テスト 1865件パス（既存の失敗はパンクロック・タールショット関連で今回のスコープ外）
