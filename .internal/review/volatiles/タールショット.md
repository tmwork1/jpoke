# タールショット レビュー結果

## 仕様書

変更なし。既存の `.internal/spec/volatiles/タールショット.md` はwikiの一次情報と一致しており、誤りや欠落なし。

## 実装計画書

変更なし。既存の `.internal/plan/volatiles/タールショット.md` は仕様書と一致しており、誤りや欠落なし。

## 実装（handlers/ / data/）

変更なし。

- `handlers/volatile.py` の `タールショット_boost_fire` は `AttackContext` を受け取り、`ctx.move.type == "ほのお"` のとき `value *= 2` で正しく実装されている。
- `data/volatile.py` の登録は `Event.ON_CALC_DEF_TYPE_MODIFIER` / `subject_spec="defender:self"` / priority=100 で仕様書・計画書と一致。
- `handlers/move_status.py` の `タールショット_apply` はタールショット状態付与とS-1適用を行い、`どくのいと_apply` と同じパターン（volatile付与結果を無視してS-1を常に適用）で一貫している。
- `data/move.py` の登録は `Event.ON_STATUS_HIT` / type="いわ" / category="変化" / pp=15 / accuracy=100 で仕様書と一致。

## テスト

追加・修正した内容:

- `tests/test_volatile.py` に `test_タールショット_ほのお半減ポケモンが等倍になる` を追加。
  ヒトカゲ（ほのおタイプ、ほのお半減）がタールショット状態でほのお技を受けると等倍（4096）になることを検証。
- `tests/moves_status/test_move_ta.py` に以下2件を追加。
  - `test_タールショット_すばやさ1段階下がりタールショット状態付与`: 通常命中時にタールショット状態付与とS-1が発生することを検証。
  - `test_タールショット_すでにタールショット状態でもSは下がる`: すでにタールショット状態の場合、volatile重複付与はされないがS-1は依然適用されることを検証（どくのいとと同様の挙動）。
- `python scripts/sort_tests.py` および `python scripts/generate_test_list.py` を実行して整合性を維持。
- 全1865件のテストがパスすることを確認。
