# さわがしい レビュー結果

## 仕様書

新規作成: `.internal/spec/volatiles/さわがしい.md`

- さわぐ（使用者）とさわがしい（相手）両状態の効果を記述
- 第五世代以降の仕様として、ねむけは防がれないがねむけ→ねむり移行は防がれる点を明記
- さわぐ VolatileData に `forced=True` が必要な理由を記述

## 実装計画書

新規作成: `.internal/plan/volatiles/さわがしい.md`

- 以下の問題点を特定して修正計画を記述
  1. `さわぐ` VolatileData に `forced=True` がなく Command.FORCED がわるあがきに解決される
  2. ねむけを ON_BEFORE_APPLY_VOLATILE で防いでいる（Gen5+ では不正）
  3. `さわぐ_start` がねむけを解除している（Gen5+ では不正）
  4. `さわぐ_modify_move` がデッドコードになっている

## 実装（handlers/ / data/）

修正した内容:

- `src/jpoke/data/volatile.py`:
  - `"さわぐ"` VolatileData に `forced=True` を追加（Command.FORCED がさわぐ技を正しく解決するようにした）
  - `"さわぐ"` から `ON_BEFORE_APPLY_VOLATILE` (さわぐ_prevent_nemuke) ハンドラを削除（Gen5+: ねむけは防がない）
  - `"さわがしい"` から `ON_BEFORE_APPLY_VOLATILE` (さわぐ_prevent_nemuke) ハンドラを削除

- `src/jpoke/handlers/volatile.py`:
  - `さわぐ_start`: ねむけ解除ロジックを削除（Gen5+: さわぎだし時にねむけは解除しない）
  - `さわぐ_modify_move`: 不要なデッドコードのため削除（`forced=True` 方式で代替）
  - `さわぐ_prevent_nemuke`: 不要のため削除

## テスト

修正・追加した内容:

- `test_さわぐ_技固定`: FORCED コマンド確認に加え、解決後の技名が「さわぐ」になることを追加検証
- `test_さわぐさわがしい_ねむけを防ぐ`: 誤った仕様（Gen3/4 のもの）を検証していたため削除
- `test_さわぐさわがしい_ねむりを防ぐ`: docstring を追加
- `test_さわぐさわがしい_ねむけは防がない`: 新規追加（Gen5+: ねむけは防がない）
- `test_さわぐさわがしい_ねむけからねむりへの移行を防ぐ`: 新規追加（ねむけ終了時にねむりにならない）
- `test_さわぐ技使用_揮発性状態を付与する`: `move_name` キーワード引数エラーを修正（`move_idx` に変更）
- `test_さわぐ_さわぎだしでねむりを起こす`: 同上

全 1854 テスト通過。
