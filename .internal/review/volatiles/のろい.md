# のろい レビュー結果

## 仕様書

変更なし。

`.internal/spec/volatiles/のろい.md` は一次情報（ポケモンWiki）と整合している。
- ターン終了時（Priority 100）に最大HPの1/4ダメージ
- 交代で解除、バトンタッチで引き継ぎ
- マジックガード特性でダメージ無効
- ターン経過で解除されない（永続）
- `subject_spec`: `source:self`（非攻撃フェーズ）

## 実装計画書

変更なし。

`.internal/plan/volatiles/のろい.md` は仕様書と整合している。
ハンドラ実装・登録方法・テスト項目すべて正確。

## 実装（handlers/ / data/）

変更なし。

### handlers/volatile.py（L693-705）

```python
def のろい_damage(battle: Battle, ctx: EventContext, value: Any) -> HandlerReturn:
    """のろい状態のターン終了時ダメージ"""
    battle.modify_hp(ctx.source, r=-1/4)
    return HandlerReturn(value=value)
```

- `battle.modify_hp(r=-1/4)` を使用: `Pokemon.hp` への直接代入なし
- 五十音順配置: ねをはる の後、バインド の前 → 正しい位置

### data/volatile.py（L455-463）

```python
"のろい": VolatileData(
    handlers={
        Event.ON_TURN_END: h.VolatileHandler(
            h.のろい_damage,
            subject_spec="source:self",
            priority=100,
        ),
    }
),
```

- `subject_spec="source:self"`: 非攻撃フェーズの正しい指定
- `priority=100`: `.internal/spec/turn.md` の `ON_TURN_END` のろいダメージ行（Priority 100）と一致

## テスト

変更なし。

`tests/test_volatile.py` に以下の3テストが存在し、すべてパス済み:

| テスト関数名 | 確認内容 |
|---|---|
| `test_のろい_ダメージ` | ターン終了時に最大HPの1/4ダメージを受ける |
| `test_のろい_マジックガードでダメージ無効` | マジックガード特性があるとダメージを受けない |
| `test_のろい_交代で解除` | 交代するとのろい状態が解除される |

テスト実行結果: 3 passed（`.loop/test_logs/のろい.log` 参照）
