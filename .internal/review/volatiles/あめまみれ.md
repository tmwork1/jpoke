# あめまみれ レビュー結果

## 仕様書

- 新規作成: `.internal/spec/volatiles/あめまみれ.md`
  - みずあめボムのwiki（`.internal/wiki/moves/みずあめボム.html`）および`.internal/spec/moves/_all_moves.md`を一次情報として参照
  - 効果（3ターン間、毎ターン終了時にSランク-1）を記載
  - 付与条件（みずあめボムの追加効果、みがわり・りんぷん等で無効）を記載
  - 消滅条件（3ターン経過、交代）を記載
  - 関連インタラクション（クリアボディ、たんじゅん、まけんき、かちき、ミラーアーマー）を記載
  - 実装上の注意（S-1とtickの順序）を記載

## 実装計画書

- 新規作成: `.internal/plan/volatiles/あめまみれ.md`
  - ハンドラ実装の順序（S-1 → tick）の根拠を記載
  - テスト方針を記載

## 実装（handlers/ / data/）

- `src/jpoke/handlers/volatile.py` の `あめまみれ_turn_end` を修正:
  - 修正前: `tick → has_volatile確認 → S-1`（3ターン目はtick後にremoveされS-1されない）
  - 修正後: `S-1 → tick`（count=1の最終ターンにもS-1を発動させる）
  - バグ: count=3でも実際には2回しかSランクが下がらなかった
- `src/jpoke/data/volatile.py` は変更なし

## テスト

- `tests/test_volatile.py` を修正:
  - 既存テスト `test_あめまみれ` を `test_あめまみれ_1ターン経過でSマイナス1` にリネーム（内容は同じ）
  - 追加: `test_あめまみれ_3ターンで合計3回Sが下がる` — count=3で3ターン連続にS-1が発動することを確認
  - 追加: `test_あめまみれ_3ターン後は解除される` — 3ターン経過後に状態が解除されることを確認
  - `python scripts/sort_tests.py` 実行済み
