# さわぐ レビュー結果

## 仕様書

- 新規作成（`.internal/spec/volatiles/さわぐ.md`）
- 第五世代以降（SVシリーズ準拠）の仕様を記載
- さわぎだし時のねむり解除、さわぐ状態中のねむり付与阻止、さわがしい補助状態、
  Champions仕様のねむけ挙動（ねむけからねむりへの移行を防ぐ）を明記

## 実装計画書

- 新規作成（`.internal/plan/volatiles/さわぐ.md`）
- さわぐ技の `ON_HIT` ハンドラが未実装であることを記録
- 各ハンドラの subject_spec・priority・実装状況を明記

## 実装（handlers/ / data/）

- `src/jpoke/handlers/move_attack.py`：`さわぐ_apply` 関数を新規追加
  - さわぐ技命中時に `さわぐ` 揮発性状態（count=3固定、move_name="さわぐ"）を付与
  - 五十音順で `こごえるせかい` の後に配置
- `src/jpoke/data/move.py`：さわぐ技に `ON_HIT` ハンドラ（`さわぐ_apply`）を登録
- `src/jpoke/data/volatile.py`：さわぐ VolatileData に `forced=True` を追加
  - `get_forced_move_name` が `volatile.move_name` を返すために必要
- ねむけ付与阻止（`ON_BEFORE_APPLY_VOLATILE`）は削除された（第五世代仕様に整合）
  - ねむけからねむりへの移行のみ防ぐ仕様に変更

## テスト

- `test_さわぐ技使用_揮発性状態を付与する`：さわぐ技使用でさわぐ状態（count=3）が付与されることを確認
- `test_さわぐ_さわぎだしでねむりを起こす`：さわぎだし時に相手のねむり状態が解除されることを確認
- `test_さわぐ_技固定`：強制コマンドでさわぐが解決される（move.name == "さわぐ"）を確認するアサーションを追加
- `test_さわぐさわがしい_ねむけを防ぐ` を削除し、以下の2テストに分割：
  - `test_さわぐさわがしい_ねむけは防がない`：ねむけ付与は防がれないことを確認
  - `test_さわぐさわがしい_ねむけからねむりへの移行を防ぐ`：ねむけ終了時にねむり移行が防がれることを確認
- 全11テスト通過・全体1854テスト通過
