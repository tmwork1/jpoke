# たくわえる レビュー結果

## 仕様書

- 新規作成した（`.internal/spec/volatiles/たくわえる.md`）
- 一次情報（`.internal/wiki/volatiles/たくわえる_(状態変化).html`）と照合し、以下を仕様書に記載した
  - カウント管理（1〜3、最大3で失敗）
  - はきだす威力テーブル（1回=100、2回=200、3回=300）
  - のみこむ回復量テーブル（1回=1/4、2回=1/2、3回=全回復）
  - たくわえる使用時のぼうぎょ・とくぼう+1
  - のみこむ・はきだす後のランク戻し（クリアボディ無効）
  - バトンタッチ引き継ぎなし

## 実装計画書

- 新規作成した（`.internal/plan/volatiles/たくわえる.md`）
- 実装済みハンドラ一覧・subject_spec・priority 根拠を記載

## 実装（handlers/ / data/）

- 変更なし
- `data/volatile.py`: `"たくわえる": VolatileData(handlers={})` — カウント保持のみ、仕様書と一致
- `handlers/move_status.py`: 7つのハンドラ（たくわえる2、はきだす3、のみこむ2）が実装済み、仕様書と一致
- `data/move.py`: 各技へのハンドラ登録済み、仕様書と一致
- subject_spec はすべて AttackContext の `attacker:self`、規約に準拠

## テスト

- 変更なし
- `tests/moves_status/test_move_ta.py`: たくわえる4件・のみこむ6件、仕様のケースを網羅
- `tests/moves_status/test_move_ha.py`: はきだす5件、仕様のケースを網羅
- test_volatile.py に たくわえる 単体テストなし（volatile 自体はハンドラなしのため不要）
- 全16テストがパス（`python -m pytest tests/ -v` でたくわえる関連16件は PASS）
