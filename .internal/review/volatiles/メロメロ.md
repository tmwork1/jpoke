# メロメロ レビュー結果

## 仕様書

新規作成: `.internal/spec/volatiles/メロメロ.md`

Wiki（`.internal/wiki/volatiles/メロメロ_(状態変化).html`）をもとに作成した。
主な内容:
- 効果: ON_TRY_ACTION（priority=130）で 50% の確率で行動不能
- 付与条件: 相手が異性のときのみ（ON_TRY_MOVE_2、priority=120）
- 消滅条件: 発生させたポケモンの交代・ひんし、メロメロ状態ポケモンの交代、各種アイテム
- 免疫: 同性・性別不明ポケモン、どんかん、アロマベール

## 実装計画書

新規作成: `.internal/plan/volatiles/メロメロ.md`

仕様書をもとに作成した。性別チェック（ON_TRY_MOVE_2、priority=120）の実装が必要と判断し、
追加するハンドラ・データ定義を計画書に明記した。

## 実装（handlers/ / data/）

### 修正あり

**問題点**: 技メロメロの性別チェック（ON_TRY_MOVE_2、priority=120）が未実装だった。
- `.internal/spec/turn.md` の ON_TRY_MOVE_2 テーブルに「120 | メロメロ: 性別同じ・不明」と明記されているが、
  `src/jpoke/data/move.py` の "メロメロ" に ON_TRY_MOVE_2 ハンドラがなく、
  `src/jpoke/handlers/move_status.py` に性別チェック関数もなかった。

**修正内容**:

`src/jpoke/handlers/move_status.py` に `メロメロ_check_gender` を追加（既存の `メロメロ_apply_volatile_to_defender` の直前）:
- 相手が性別不明（""）、または自分が性別不明、または同性の場合は MOVE_FAILED を返して技を失敗させる

`src/jpoke/data/move.py` の "メロメロ" に ON_TRY_MOVE_2 ハンドラを追加（priority=120）:
- `hs.メロメロ_check_gender` を登録

**既存実装（変更なし）**:
- `src/jpoke/handlers/volatile.py`: `メロメロ_try_action`（ON_TRY_ACTION、priority=130）— 問題なし
- `src/jpoke/data/volatile.py`: "メロメロ" VolatileData（ON_TRY_ACTION、priority=130）— 問題なし

## テスト

### 修正・追加あり

既存テスト:
- `test_メロメロ_行動不能`: docstring を追加
- `test_メロメロ_行動可能`: 変更なし

新規追加テスト:
- `test_メロメロ_異性に効く`: オス→メスで技メロメロを使うとメロメロ状態になることを確認
- `test_メロメロ_同性に効かない`: オス→オスでは効かないことを確認
- `test_メロメロ_性別不明に効かない`: オス→性別不明では効かないことを確認

全 5 件のメロメロテストがパスすることを確認済み。
全テスト: 1880 passed, 6 failed（6 件は本修正と無関係な既存失敗: test_move_a.py の LogCode 未インポート）。
