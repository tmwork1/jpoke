# じゅうでん レビュー結果

## 仕様書

- 新規作成（`.internal/spec/volatiles/じゅうでん.md`）
- 一次情報（ポケモンWiki）をもとに第9世代仕様を記述
- 効果: でんきタイプの技の威力×2
- 消滅条件: でんき技選択時（変化技含む）、交代時
- 実装スコープ: ON_CALC_POWER_MODIFIER のでんき技威力補正のみ
- スコープ外として変化技選択時の消滅・状態異常失敗時の消滅を明記

## 実装計画書

- 新規作成（`.internal/plan/volatiles/じゅうでん.md`）
- ON_CALC_POWER_MODIFIER、subject_spec="attacker:self"、priority=100 を明記
- 動作確認ケース・スコープ外を記述

## 実装（handlers/ / data/）

- `src/jpoke/handlers/volatile.py`: `じゅうでん_boost_electric` の型注釈を `EventContext` から `AttackContext` に修正
  - ON_CALC_POWER_MODIFIER は AttackContext で呼ばれるため正しい型に修正
  - `ctx.move.type == "でんき"` の判定ロジック・じゅうでん解除の動作は正常
- `src/jpoke/data/volatile.py`: 変更なし（subject_spec="attacker:self" は正しい）

## テスト

- `tests/test_volatile.py` に以下を追加:
  - `test_じゅうでん_交代で解除`: 交代するとじゅうでん状態が消滅することを確認
- 既存テスト2件（でんき技強化、非でんき技では残る）は正常動作確認済み
- 全3件 PASS
