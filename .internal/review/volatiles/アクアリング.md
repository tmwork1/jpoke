# アクアリング レビュー結果

## 仕様書

- 新規作成 (`.internal/spec/volatiles/アクアリング.md`)
  - 効果: 毎ターン終了時に最大HPの1/16回復（切り捨て、最小1）
  - 消滅条件: 交代で解除（バトンタッチは除く）
  - Priority 70（`.internal/spec/turn.md` と一致確認済み）
  - かいふくふうじ中は回復不可
  - おおきなねっこで回復量1.3倍（未実装、将来対応）
  - バトンタッチで引き継ぎ（未実装、将来対応）

## 実装計画書

- 新規作成 (`.internal/plan/volatiles/アクアリング.md`)
  - 実装済みハンドラの仕様を記録
  - 未実装事項（おおきなねっこ、バトンタッチ）を明記

## 実装（handlers/ / data/）

- `handlers/volatile.py` の `アクアリング_self_heal` を修正
  - 修正前: `battle.modify_hp(ctx.source, r=1/16, source=ctx.source)` を使用（浮動小数点除算）
  - 修正後: `max(1, mon.max_hp // 16)` で整数除算 + 最小値1を保証してから `v=heal` で呼び出すように変更
  - docstring を追加
  - `data/volatile.py` の登録（priority=70, subject_spec="source:self"）は変更なし

## テスト

- `test_アクアリング_交代で解除` を新規追加
  - アクアリング状態で交代すると状態が解除されることを確認
- 既存テスト `test_アクアリング_回復` はそのまま（修正後の実装でも通過）
- 既存テスト `test_アクアリング_かいふくふうじ中は回復しない` はそのまま（変更なし）
- `python scripts/sort_tests.py tests/test_volatile.py` でソート実施
- `python -m pytest tests/ -v` で全1827件パス確認済み
