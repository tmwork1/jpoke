# とくせいなし レビュー結果

## 仕様書

**新規作成** (`.internal/spec/volatiles/とくせいなし.md`)

一次情報 (`.internal/wiki/volatiles/とくせいなし.html`) と照合し、以下を反映:
- 効果: 特性の効果が消えるが特性自体はなくならない
- 付与: いえき技（実装済み）、コアパニッシャー（スコープ外）
- 消滅: 交代のみ（テラレイドの1ターン消滅はスコープ外）
- 予防: gas_proof特性リスト・とくせいガード
- priority: turn.md に ON_VOLATILE_START/END の記載なし → デフォルト100

## 実装計画書

**新規作成** (`.internal/plan/volatiles/とくせいなし.md`)

仕様書をもとに作成。実装済みの2ハンドラーを記録し、docstring未記載の軽微な問題を記録。

## 実装（handlers/ / data/）

### handlers/volatile.py

**修正あり**: `とくせいなし_disable_ability` に docstring を追加した。

```python
def とくせいなし_disable_ability(battle: Battle, ctx: EventContext, value: Any) -> HandlerReturn:
    """とくせいなし付与時に特性を無効化する。"""
    battle.add_ability_disabled_reason(ctx.source, "とくせいなし")
    return HandlerReturn(value=value)
```

その他の実装は仕様書と一致:
- ON_VOLATILE_START: `add_ability_disabled_reason(mon, "とくせいなし")` で特性を無効化 ✓
- ON_VOLATILE_END: `remove_ability_disabled_reason(mon, "とくせいなし")` で特性を有効化 ✓
- subject_spec: 両ハンドラーとも `"source:self"`（非攻撃フェーズ） ✓
- priority: デフォルト100（turn.md に記載なし） ✓
- AbilityDisabledReason Literal に `"とくせいなし"` が定義済み ✓

### data/volatile.py

変更なし。ON_VOLATILE_START / ON_VOLATILE_END ともに正しく登録されている。

## テスト

変更なし。既存2テストが仕様を正しくカバーしている:
- `test_とくせいなし_特性が無効になる`: とくせいなし付与中はちくでんが無効になり電気技のダメージを受ける
- `test_とくせいなし_解除後は特性が有効になる`: 解除後はちくでんが有効に戻り電気技を無効化する

`python scripts/sort_tests.py tests/test_volatile.py` 実行済み。
`python -m pytest tests/ -v` 全1867テスト PASSED。
