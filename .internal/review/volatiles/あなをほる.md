# あなをほる レビュー結果

## 仕様書

- 新規作成: .internal/spec/volatiles/あなをほる.md
  - ポケモンWiki（あなをほる_(状態変化)）を参照して作成
  - 回避効果・威力2倍・行動固定・交代禁止の仕様を記載
  - チャンピオンズ実装スコープ外（天候ダメージ回避・フィールド回避等）を明記

## 実装計画書

- 新規作成: .internal/plan/volatiles/あなをほる.md
  - 実装済みハンドラの一覧を整理
  - 今回追加した ON_CALC_POWER_MODIFIER ハンドラの根拠（ちいさくなる・まるくなると同様のパターン）を記載

## 実装（handlers/ / data/）

- handlers/volatile.py に `あなをほる_boost_power` 関数を追加
  - あなをほる状態の相手に対して じしん・マグニチュード の威力を2倍（4096→8192）にする
  - subject_spec: AttackContext を受け取るが関数シグネチャは AttackContext 型
- data/volatile.py の "あなをほる" エントリに ON_CALC_POWER_MODIFIER ハンドラを追加
  - subject_spec="defender:self", priority=100

## テスト

- tests/test_volatile.py に `test_あなをほる_じしんは2倍ダメージ` を追加
  - あなをほる状態の相手にじしんを使うと power_modifier が 8192 になることを確認
- python scripts/sort_tests.py tests/test_volatile.py を実行してソート済み
- python scripts/generate_test_list.py を実行して .internal/tests/ を更新済み
- python -m pytest tests/ で 1827 tests passed を確認
