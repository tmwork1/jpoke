# うちおとす レビュー結果

## 仕様書

- 新規作成: `.internal/spec/volatiles/うちおとす.md`
  - 浮遊判定の無効化対象（ひこうタイプ、ふゆう、ふうせん、でんじふゆう、テレキネシス）を記載
  - でんじふゆう / テレキネシス状態の解除は技ハンドラ側の処理であることを明記
  - 交代で解除されることを記載
  - スコープ外（テレキネシス未実装、サウザンアロー未実装）を明記

## 実装計画書

- 新規作成: `.internal/plan/volatiles/うちおとす.md`
  - 実装済みハンドラ（ON_CHECK_FLOATING, priority=100）を記録
  - 技「うちおとす」を使ったときの揮発状態付与が未実装である点をスコープ外として記載

## 実装（handlers/ / data/）

- 変更なし
- `handlers/volatile.py` の `うちおとす_check_floating` は正しく実装されている
  - `value=False, stop_event=True` を返すことでふゆう特性・ふうせん・でんじふゆうより確実に優先される
- `data/volatile.py` の `うちおとす` エントリは `ON_CHECK_FLOATING` ハンドラを正しく登録している

## テスト

- 3件追加（`tests/test_volatile.py`）:
  - `test_うちおとす_ふうせんより優先される`: ふうせん持ちでも地面扱いになることを確認
  - `test_うちおとす_ふゆう特性より優先される`: ふゆう特性持ちでも地面扱いになることを確認
  - `test_うちおとす_交代で解除される`: 交代でうちおとす状態が解除されることを確認
- `sort_tests.py` によるソート後、全 1843 件パスを確認
