# ちょうはつ レビュー結果

## 仕様書

- 新規作成: `.internal/spec/volatiles/ちょうはつ.md`
  - 効果（変化技使用禁止）、付与条件（技「ちょうはつ」使用時、count=3固定）を記載
  - 消滅条件（ターン経過・交代）を記載
  - ターン処理の優先度（ON_TRY_ACTION priority=100、ON_TURN_END priority=110）を記載
  - スコープ外（メンタルハーブ・どんかん・アロマベール・おうごんのからだによる予防/解除）を明記

## 実装計画書

- 新規作成: `.internal/plan/volatiles/ちょうはつ.md`
  - 3ハンドラ（ちょうはつ_apply_volatile_to_defender、ちょうはつ_try_action、ちょうはつ_tick_volatile）の仕様を記載
  - priority の根拠を turn.md に基づいて明記
  - ON_TRY_ACTION の priority=200 → 100 の修正が必要である旨を記載

## 実装（handlers/ / data/）

- `src/jpoke/data/volatile.py`の `ON_TRY_ACTION` priority を 200 → 100 に修正
  - 根拠: `.internal/spec/turn.md` の ON_TRY_ACTION 表で「ちょうはつで変化技が出せない」の priority は 100

## テスト

- `tests/test_volatile.py` に以下のテストを追加:
  - `test_ちょうはつ_技を使うと相手がちょうはつ状態になる`: 技「ちょうはつ」が命中すると相手にちょうはつ状態を付与することを確認
  - `test_ちょうはつ_すでにちょうはつ状態の相手には効果なし`: 相手がすでにちょうはつ状態の場合、volatile_manager.apply が False を返し count が更新されないことを確認
- `python scripts/sort_tests.py tests/test_volatile.py` でソート済み
- `python scripts/generate_test_list.py` で .internal/tests/ を更新済み
- 全5件（ちょうはつ関連）、全テスト 1864件 パス（いかりのまえば関連の3件は既存の失敗）
