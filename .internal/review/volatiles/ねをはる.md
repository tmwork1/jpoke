# ねをはる レビュー結果

## 仕様書

新規作成。`.internal/wiki/volatiles/ねをはる_(状態変化).html` をもとに以下の効果を記載：
- ターン終了時に最大HPの1/16回復（Priority 70、`.internal/spec/turn.md` 確認済み）
- 交代・逃げ禁止（ゴーストタイプは例外）
- 地面にいる扱い（浮遊無効）
- かいふくふうじ状態では回復不可

ファイル: `.internal/spec/volatiles/ねをはる.md`

## 実装計画書

新規作成。3つのハンドラ（ON_TURN_END, ON_CHECK_TRAPPED, ON_CHECK_FLOATING）の役割・priority・subject_spec を記載。
HPChangeReason は `""` を使うこと（`"drain"` は使わない）を明記。

ファイル: `.internal/plan/volatiles/ねをはる.md`

## 実装（handlers/ / data/）

### handlers/volatile.py

- `ねをはる_self_heal`: docstring が欠落していたため追加。処理内容（r=1/16, source=ctx.source）は正しい。
- `ねをはる_check_floating`: `HandlerReturn(value=False, stop_event=True)` → 正しい。変更なし。
- `ON_CHECK_TRAPPED` に `check_trapped_not_ghost` を使用 → 正しい。変更なし。

### data/volatile.py

- `ON_TURN_END` priority=70: `.internal/spec/turn.md` と一致。変更なし。
- `ON_CHECK_TRAPPED` subject_spec="source:self": 正しい。変更なし。
- `ON_CHECK_FLOATING` subject_spec="source:self": 正しい。変更なし。

## テスト

### 追加したテスト

- `test_ねをはる_かいふくふうじ中は回復しない`: かいふくふうじ状態では ON_MODIFY_HEAL でブロックされることを確認（欠落していたため追加）

### コメント追加

既存の3テスト（`_交代不可`、`_回復`、`_浮遊無効`）に日本語docstringを追加。

### テスト結果

全テスト 1866 passed（3 failed は test_move_a.py::いかりのまえば 関連でねをはると無関係）。
ねをはる関連テスト 4件 全 PASSED。
