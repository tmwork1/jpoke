# おんねん レビュー結果

## 仕様書

- 新規作成した (`.internal/spec/volatiles/おんねん.md`)
- 一次情報（wiki）と照合し、以下を網羅:
  - PP を 0 にする条件（攻撃技でひんし）
  - 発動しない条件（PP 既に 0、わるあがき、攻撃技以外）
  - 消滅条件（次の自身の行動順 / 交代）

## 実装計画書

- 新規作成した (`.internal/plan/volatiles/おんねん.md`)
- 修正が必要な箇所を明記した（PP=0 チェック不足、わるあがき除外不足）
- ラベル `non_onnen` の追加方針を記載

## 実装（handlers/ / data/）

- `src/jpoke/handlers/volatile.py`: `おんねん_deplete_attacking_move_pp` を修正
  - `ctx.move.pp == 0` のときは発動しないチェックを追加
  - `ctx.move.has_label("non_onnen")` のときは発動しないチェックを追加
- `src/jpoke/utils/type_defs.py`: `MoveLabel` に `"non_onnen"` を追加
- `src/jpoke/data/move.py`: わるあがきの `labels` に `"non_onnen"` を追加
- `src/jpoke/data/volatile.py`: 変更なし（既存登録に問題なし）

## テスト

- 追加したテスト（`tests/test_volatile.py`）:
  - `test_おんねん_PP既に0なら不発`: PP がすでに 0 の技でひんしにしても発動しない
  - `test_おんねん_わるあがきは対象外`: わるあがきでひんしにしても PP 消失しない
  - `test_おんねん_次のターン行動で解除`: 次の行動順が来るとおんねん状態が解除される
  - `test_おんねん_交代で解除`: 交代するとおんねん状態が解除される
- 全 1843 件のテストがパス
