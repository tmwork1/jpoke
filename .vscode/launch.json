{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python デバッガー: 現在のファイル",
            "type": "debugpy",
            "request": "launch",
            //"program": "${workspaceFolder}/tests/run_tests.py", // 全体テスト
            "program": "${workspaceFolder}/tests/ability.py", // ability.pyのデバッグ用
            //"program": "${workspaceFolder}/tests/item.py", // item.pyのデバッグ用
            "console": "integratedTerminal"
        }
    ]
}