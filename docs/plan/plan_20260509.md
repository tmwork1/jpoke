# 作業計画 2026/5/9

-------------------------------------------------------

## 優先度 Critical
### テスト不合格の修正対応

-------------------------------------------------------

## 優先度 H
### EventLogger.rend_get_base_text() を網羅する
テキスト出力に対応していないLogCodeが残っている

### src/data/ の実装スタイルのばらつき
なんでもかんでも partial で汎用関数に情報を注入するのではなく、最低限のパラメータ違いを残して名前付き関数にする。
ability.pyの実装が参考になる。


### test_utils.py
汎用関数の引数の atk_idx を省略不可にして明示させる

### テストケース一覧作成
テストケースの一覧を docs/plan/xxx_test.mdに作成する。

例: ability_test.md
    [] いかく : 場に出たときに相手A-1する

テストスクリプトから一覧表を自動更新できるようにしたい

### Battleクラスのメソッド増加
使用される場面が少ない関数は配下のManagerクラスに任せて中継メソッドは廃止したい。

### その他
- __pycache__をgit管理から外す。削除したはずだが再発している。



-------------------------------------------------------


## 優先度 M
### メガシンカ実装


-------------------------------------------------------


## 優先度 L
### 非公開情報の隠蔽機能(mask)の実装
### 試合開始時の初期化 init_game() で必要な変数をすべて初期化する
### ダメージ乱数固定、急所無効などの対戦オプションを設定できるようにする
### Poke API で図鑑情報を作り直す

### 【全般】attacker, defender の定義を一行にまとめる
'''
    defender = battle.actives[0]
    attacker = battle.actives[1]
    ->
    defender, attacker = battle.actives
'''

