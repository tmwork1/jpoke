# 作業計画 2026/5/26

-------------------------------------------------------

## 優先度 Critical
### テスト不合格の修正対応

-------------------------------------------------------

## 優先度 H
### EventLogger.rend_get_base_text() を網羅する
テキスト出力に対応していないLogCodeのレンダリング実装

### ハンドラ関数名
いつ呼ばれるかではなく、処理内容を関数名で表現したい
'''
Bad - def たべのこし_on_turn_end():
Good - def たべのこし_heal():
'''

### src/data/ の実装方式
data/xxx.py内で汎用関数にpartialでパラメータを直接注入するのではなく、handlers/xxx.pyに名前付き関数を個別に用意したほうがデバッグしやすい。
名前付き関数から汎用関数を呼べばよい。


### test_utils.py
汎用関数の引数の atk_idx を省略不可にして明示させる


### その他
__pycache__をgit管理から外す。削除したはずだが再発している。

-------------------------------------------------------

## 優先度 M

-------------------------------------------------------

## 優先度 L
### 非公開情報の隠蔽機能(mask)の実装

### ダメージ乱数固定、急所無効などの対戦オプションを設定できるようにする

### 【全般】attacker, defender の定義を一行にまとめる
'''
    defender = battle.actives[0]
    attacker = battle.actives[1]
    ->
    defender, attacker = battle.actives
'''

### Battleクラスの肥大化
使用頻度の少ない関数は配下のManagerクラスに任せて中継メソッドは廃止したい。

