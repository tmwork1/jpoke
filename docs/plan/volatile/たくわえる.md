# たくわえる — 実装プラン

## 実装状況

✅ **実装済み**

## 効果概要

1〜3回重ね掛け可能なカウンター。のみこむ・はきだすで実際に効果を発揮。

volatile 本体は handlers={}（カウント管理のみ）。技のハンドラ側で処理。

## テスト状況

## 実装メモ

- volatile 本体はカウント置き場で、実際の効果は `のみこむ` / `はきだす` 側の move ハンドラで消費する。
- `handlers={}` が正しい実装形であり、未実装ではない。

## テスト要件

- [ ] たくわえる回数が保持される
- [ ] のみこむ・はきだす側で回数を参照/消費できる

## 残課題

- volatile 単体ではなく move 側の統合テストが必要。
