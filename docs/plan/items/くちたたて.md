# くちたたて 実装計画

更新日: 2026-06-12

## スコープ

- 仕様書: `docs/spec/items/くちたたて.md`
- 実装状態: 未実装
- 実装方針: `ON_SWITCH_IN` / `ON_ITEM_GAINED` でザマゼンタ(れきせん)をザマゼンタ(たてのおう)にフォルムチェンジする

## 実装ゴール（完了定義）

- ザマゼンタ(れきせん)がくちたたてを持って登場すると、ザマゼンタ(たてのおう)にフォルムチェンジする
- ザマゼンタ以外が持っても追加効果はない
- `removable=False` および `fling_power=0` はすでに ItemData に設定済み

## 実装対象イベント

- `Event.ON_SWITCH_IN` — 登場時にフォルムチェンジを発動する
- `Event.ON_ITEM_GAINED` — バトル中にアイテムを入手したとき

## 変更対象（ファイル/関数単位）

- `src/jpoke/data/item.py` — `くちたたて` に `handlers` を登録する
- `src/jpoke/handlers/item.py` — `くちたたて_form_change` を実装する
- `tests/test_item.py` — テストを追加する

## 実装ステップ

1. `src/jpoke/handlers/item.py` に `くちたたて_form_change` を実装する。
2. `src/jpoke/data/item.py` の `くちたたて` に `ON_SWITCH_IN`・`ON_ITEM_GAINED` handler を登録する。
3. テストを追加し、全テストが通ることを確認する。

## テスト観点

1. ザマゼンタ(れきせん)がくちたたてを持って登場するとザマゼンタ(たてのおう)になる。
2. ザマゼンタ(たてのおう)がすでに場にいるときは再度フォルムチェンジしない。
3. ザマゼンタ以外のポケモンが持っても変化しない。
4. フォルムチェンジ後の種族値が正しく反映される。

## 検証コマンド

```powershell
python -m pytest tests/test_item.py -k "くちたたて" -v
python -m pytest tests/ -v
```

## 疑似コード

```python
def くちたたて_form_change(battle, ctx, value):
    mon = ctx.source
    if mon.name != "ザマゼンタ(れきせん)":
        return HandlerReturn(value=value)
    mon.set_form("ザマゼンタ(たてのおう)")
    return HandlerReturn(value=value)
```

登録:
```python
"くちたたて": ItemData(
    consumable=False,
    removable=False,
    fling_power=0,
    handlers={
        Event.ON_SWITCH_IN: h.ItemHandler(
            h.くちたたて_form_change,
            subject_spec="source:self",
            priority=100,
        ),
        Event.ON_ITEM_GAINED: h.ItemHandler(
            h.くちたたて_form_change,
            subject_spec="source:self",
            priority=100,
        ),
    }
),
```
