# かるいし 実装計画

## 効果概要
持ち主の体重を半分にする（くさむすび・はかりにかけるのダメージ算出に影響）。

## 実装状態

体重半減効果は `src/jpoke/model/pokemon.py` の `weight` プロパティで直接実装済み。
ハンドラ登録は不要。

## テスト観点
- かるいし所持時、`mon.weight` が `mon.data.weight * 0.5` であること
- かるいしなしでは通常体重であること
