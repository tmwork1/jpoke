# マゴのみ 実装計画

## 効果概要
HP が最大値の 25% 以下になった瞬間に最大 HP の 33%（端数切り捨て）を回復する（消費）。
回復と同時に、はやさが上がりにくい性格（あまい味が嫌い）のポケモンはこんらんする。
イアのみ・ウイのみ・フィラのみ・バンジのみと回復量・発動条件は同じで、嫌う味と対応する性格が異なるのみ。

## 実装仕様

| 項目 | 内容 |
|------|------|
| イベント | `Event.ON_HP_CHANGED` |
| subject_spec | `"target:self"` |
| priority | 100（`docs/spec/turn.md` に ON_HP_CHANGED の個別記載はなし。既存の同種ハンドラ `_heal_berry` 系が priority=100（`ItemHandler` のデフォルト）で登録されているため、それに合わせる） |
| once | False（`ItemHandler` は消費アイテムでも once を使わず、`battle.item_manager.consume_item` で明示的に消費する既存の共通パターンに合わせる） |
| 消費条件 | 発動時に `_announce_and_consume_item(battle, mon)`（内部で `battle.item_manager.consume_item` を呼ぶ既存の共通処理） |
| こんらん付与 | 発動時、`mon.nature` が `("ゆうかん", "れいせい", "のんき", "なまいき")` のいずれかなら `battle.volatile_manager.apply_confusion(mon, source=mon)` |

### ハンドラロジック

共通処理 `_heal_berry`（`handlers/item.py`）の `confuse_natures: tuple[str, ...] | None` パラメータに
あまい味が嫌いな性格（はやさが上がりにくい性格）を渡す、マゴのみ専用のラッパー関数
`マゴのみ_heal_on_quarter_hp` を追加する（イアのみ・ウイのみ・フィラのみ・バンジのみと同じ実装パターン）。

```python
def マゴのみ_heal_on_quarter_hp(battle, ctx, value) -> HandlerReturn:
    return _heal_berry(
        battle, ctx, value, denominator=4, heal_r=1/3,
        confuse_natures=("ゆうかん", "れいせい", "のんき", "なまいき"),
    )
```

- あまい味が嫌いな性格は、はやさが上がりにくい（下降補正がかかる）性格＝ゆうかん（こうげき↑）・
  れいせい（とくこう↑）・のんき（ぼうぎょ↑）・なまいき（とくぼう↑）の4種
  （`NATURE_MODIFIER`（`data/nature.py`）でスピード（インデックス5）が 0.9 になっている性格を突合して確認済み）。
- 従来 `data/item.py` では共通関数 `heal_on_quarter_hp`（こんらん未対応）が登録されていたが、これは
  誤り（マゴのみの説明文には現行世代（SV含む第六〜九世代）でも「きらいなあじだとこんらんする」と
  明記されている）。専用ラッパーに差し替える。
- こんらんの発動条件チェック（どんかん／ミストフィールド等の無効化）は `volatile_manager.apply`
  内部の `ON_BEFORE_APPLY_VOLATILE` フックが担うため、本ハンドラ側で個別に判定しない
  （CLAUDE.md の方針・既存の `どくくぐつ_confuse_on_poison` 等と同じ扱い）。
- `source=mon`（自傷）として付与することで、しんぴのまもり等の「相手由来のみ防ぐ」ガードに
  誤って阻止されないようにする（`ctx.is_foe_target()` が false になるため防がれない＝実機仕様と一致）。
- lethal 計算（`handlers/lethal.py` の `マゴのみ_heal`）はHP分布のみを扱うため、こんらん付与は
  対象外（HPを直接変えない効果のため、既存の他アイテムと同様に lethal 側では未実装のままでよい）。

## テスト観点
- HP が 25% 超 → 25% 以下になった瞬間に 33% 回復すること（回復量は既存テストで確認済み）
- 発動後にアイテムが消費されること（既存テストで確認済み）
- はやさが上がりにくい性格（ゆうかん・れいせい・のんき・なまいき）のとき、発動と同時にこんらんすること
- それ以外の性格では発動してもこんらんしないこと
