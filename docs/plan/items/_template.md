# アイテム実装計画: <アイテム名>

更新日: YYYY-MM-DD

## スコープ

- 仕様書: `docs/spec/items/<アイテム名>.md`
- 実装状態: 未実装 / 実装済み / 部分実装
- 実装方針: <一文で方針を記述。例：「ON_HIT で反動ダメージを与える」>

## 実装ゴール（完了定義）

<!-- 「何ができれば完了か」を箇条書きで。テストで検証できる粒度にする -->
- <主効果が正しく発動する>
- <発動しない例外条件で不発になる>
- <既存テストを壊さない>

## 実装対象イベント

<!-- 使用するイベントを列挙。不要なら省略可 -->
- `Event.<イベント名>` — <発動タイミングの説明>

## 変更対象（ファイル/関数単位）

- `src/jpoke/data/item.py` — `<アイテム名>` に handlers を登録する
- `src/jpoke/handlers/item.py` — `<アイテム名>_<効果名>` を実装する
- `tests/test_item.py` — テストを追加する

<!-- 新規イベントが必要な場合 -->
<!-- - `src/jpoke/enums/event.py` — `Event.<新規イベント名>` を追加する -->
<!-- - `src/jpoke/core/*.py` — イベント発火箇所を追加する -->

## 実装ステップ

1. 仕様書 `docs/spec/items/<アイテム名>.md` で発動条件・例外条件を確認する。
2. `src/jpoke/handlers/item.py` に `<アイテム名>_<効果名>` を実装する。
3. `src/jpoke/data/item.py` の `<アイテム名>` に該当イベントの handler を登録する。
4. `tests/test_item.py` にテストを追加し、全テストが通ることを確認する。

<!-- 複雑なアイテムでは手順を細分化する。例： -->
<!-- 2a. 新規イベントを追加する -->
<!-- 2b. イベント発火箇所を実装する -->

## テスト観点

<!-- 番号付きで。「発動する」「発動しない」「解除される」「相互作用」の4軸を基本とする -->
1. 発動条件を満たしたときに効果が出る。
2. 発動条件を満たさないときは効果が出ない。
3. <解除条件があれば記述（消費アイテムなら消費後に無効になるか）>
4. <交代・場状態・特性無効化との相互作用>

## 検証コマンド

```powershell
python -m pytest tests/test_item.py -k "<アイテム名>" -v
python -m pytest tests/ -v
```

---
<!-- 以下は複雑なアイテムのみ使用。不要なら削除する -->

## 事前に固定する仕様値

<!-- 定数化が必要な数値がある場合のみ記載 -->
<!-- - <定数名>: <値>（実装位置候補: `src/jpoke/utils/constants.py`） -->

## 疑似コード

<!-- 実装イメージが非自明な場合のみ記載 -->
```python
# def <アイテム名>_<効果名>(battle, ctx, value):
#     ...
```

## リスクと対策

<!-- 既存ロジックへの副作用や仕様の曖昧点がある場合のみ記載 -->
<!-- - リスク: <内容> -->
<!--   - 対策: <内容> -->
