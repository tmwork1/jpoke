# 特性実装計画: クイックドロウ

更新日: 2026-06-03

## スコープ

- 優先度: H
- 仕様書: `docs/spec/abilities/クイックドロウ.md`
- 実装状態: 未実装
- 実装方針: `DomainEvent.ON_MODIFY_MOVE_PRIORITY` で攻撃技を選択したとき 30% の確率で優先度を +1 する。優先度自体は変化させずに行動順のみ引き上げる。

## 実装ゴール（完了定義）

- 攻撃技選択時に 30% の確率で「クイックドロウで行動が はやくなった!」ログが出て先制する。
- 変化技では発動しない。
- トリックルーム状態でも発動する（行動順が早くなる）。
- 既存テストを壊さない。

## 実装対象イベント

- `DomainEvent.ON_MODIFY_MOVE_PRIORITY` — 攻撃技選択時に 30% で優先度を +1 する

## 変更対象（ファイル/関数単位）

- `src/jpoke/data/ability.py` — `クイックドロウ` に handler を登録する
- `src/jpoke/handlers/ability.py` — `クイックドロウ_modify_priority` を実装する
- `tests/test_ability.py` — テストを追加する

## 実装ステップ

1. 仕様書 `docs/spec/abilities/クイックドロウ.md` で攻撃技判定・トリックルームとの相互作用を確認する。
2. `src/jpoke/handlers/ability.py` に `クイックドロウ_modify_priority` を実装する（`battle.rng.check(0.3)` で 30% 判定、攻撃技のみ対象）。
3. `src/jpoke/data/ability.py` の `クイックドロウ` に handler を登録する。
4. テストを追加し、全テストが通ることを確認する。

## テスト観点

1. 攻撃技使用時に 30% の確率で先制する（確率テストは `accuracy=100` 相当の乱数固定で確認）。
2. 変化技選択時は発動しない。

## 検証コマンド

```powershell
python -m pytest tests/test_ability.py -k "クイックドロウ" -v
python -m pytest tests/ -v
```
