# 特性実装計画: ふとうのけん

更新日: 2026-06-05

## 仕様参照

- `docs/spec/abilities/ふとうのけん.md`

## 実装ゴール

- バトル中一度だけ、場に出たときにこうげきが1段階上がる。
- 2回目以降の入場では発動しない。

## 実装方針

- `Event.ON_SWITCH_IN`、`subject_spec="source:self"` で実装。
- **ふくつのたて** と同一パターン（A↑ か B↑ の違いのみ）。
- `per_battle_once` フラグは data/ability.py に既定済み。

## 変更候補

- `src/jpoke/data/ability.py` — `ふとうのけん` に ON_SWITCH_IN handler を登録する
- `src/jpoke/handlers/ability.py` — `ふとうのけん_on_switch_in` を実装する
- `tests/test_ability.py` — テストを追加する

## テスト観点

1. 初回入場で A が 1 段階上がる。
2. 2回目以降の入場では発動しない。

## 検証コマンド

```powershell
python -m pytest tests/test_ability.py -k "ふとうのけん" -v
python -m pytest tests/ -v
```

## 疑似コード

```python
def ふとうのけん_on_switch_in(battle, ctx, value):
    mon = ctx.source
    if mon.ability.state == "used":
        return HandlerReturn(value=value)
    mon.ability.state = "used"
    if battle.modify_stats(mon, {"A": +1}, source=mon):
        announce_ability_triggered(battle, ctx, value, mon=mon)
    return HandlerReturn(value=value)
```
