# 特性実装計画: とれないにおい

更新日: 2026-06-03

## スコープ

- 優先度: H
- 仕様書: `docs/spec/abilities/とれないにおい.md`
- 実装状態: 未実装
- 実装方針: ミイラと全く同じロジックで実装する。`ON_DAMAGE_HIT` で直接攻撃を受けたとき攻撃者の特性を `とれないにおい` に書き換える。

## 実装ゴール（完了定義）

- 直接攻撃でダメージを受けたとき、攻撃者の特性がとれないにおいに変わる。
- ぼうごパット持ちの攻撃者には発動しない。
- ダメージ 0（みがわり・まもるで防いだ）のときは発動しない。
- 既存テストを壊さない。

## 実装対象イベント

- `Event.ON_DAMAGE_HIT` — 直接攻撃でダメージを受けたとき、攻撃者の特性を書き換える

## Priority

| イベント | Priority | 根拠 |
|---|---|---|
| ON_DAMAGE | 100 | docs/spec/turn.md「へんしょく等（防御側特性）」（直接掲載なし、推定） |

turn.md にとれないにおいの直接掲載なし。ミイラと同ロジックのため `ON_DAMAGE` Priority 100「へんしょく等（防御側特性）」を根拠として Priority 100 と推定する。

## 変更対象（ファイル/関数単位）

- `src/jpoke/data/ability.py` — `とれないにおい` に handler を登録する
- `src/jpoke/handlers/ability.py` — `とれないにおい_on_damage` を実装する（ミイラの実装を参考にする）
- `tests/test_ability.py` — テストを追加する

## 実装ステップ

1. 仕様書 `docs/spec/abilities/とれないにおい.md`（ミイラとの差異）を確認する。
2. `src/jpoke/handlers/ability.py` の `ミイラ` の実装を参考に `とれないにおい_on_damage` を実装する。
   - 発動時のメッセージのみ異なる（「においが うつって とれなくなっちゃった!」）。
3. `src/jpoke/data/ability.py` の `とれないにおい` に handler を登録する。
4. テストを追加し、全テストが通ることを確認する。

## テスト観点

1. 直接攻撃でダメージを受けたとき、攻撃者の特性がとれないにおいに変わる。
2. みがわりで技を防いだとき（ダメージなし）は発動しない。
3. ぼうごパットで接触を防いだ攻撃者には発動しない。
4. 上書きできない特性（uncopyable）の攻撃者には発動しない。

## 検証コマンド

```powershell
python -m pytest tests/test_ability.py -k "とれないにおい" -v
python -m pytest tests/ -v
```
