# 特性実装計画: ヒーリングシフト

更新日: 2026-06-03

## スコープ

- 優先度: H
- 仕様書: `docs/spec/abilities/ヒーリングシフト.md`
- 実装状態: 未実装
- 実装方針: `DomainEvent.ON_MODIFY_MOVE_PRIORITY` で `ctx.move.has_label("heal")` が True のとき優先度を +3 する。

## 実装ゴール（完了定義）

- 仕様書に列挙された回復技・吸収技を使うと優先度が +3 になる。
- アクアリング・やどりぎのタネ等の「状態変化技」は対象外。
- 既存テストを壊さない。

## 実装対象イベント

- `DomainEvent.ON_MODIFY_MOVE_PRIORITY` — 優先度計算時に対象回復技なら +3 する

## Priority

| イベント | Priority | 根拠 |
|---|---|---|
| ON_MODIFY_MOVE_PRIORITY | 未掲載 | docs/spec/turn.md に本イベントの記載なし |

turn.md に `ON_MODIFY_MOVE_PRIORITY` の掲載なし。はやてのつばさなど同イベントを使う他のハンドラを実装時に参照して Priority を決定すること。

## 変更対象（ファイル/関数単位）

- `src/jpoke/data/ability.py` — `ヒーリングシフト` に handler を登録する
- `src/jpoke/handlers/ability.py` — `ヒーリングシフト_modify_priority` を実装する
- `tests/test_ability.py` — テストを追加する

## 実装ステップ

1. 仕様書 `docs/spec/abilities/ヒーリングシフト.md` で有効/除外条件を確認する。
2. `src/jpoke/handlers/ability.py` に `ヒーリングシフト_modify_priority` を実装する（`ctx.move.has_label("heal")` が True のとき value + 3 を返す）。定数リスト不要。
3. `src/jpoke/data/ability.py` の `ヒーリングシフト` に handler を登録する。
4. テストを追加し、全テストが通ることを確認する。

## テスト観点

1. じこさいせい・ドレインパンチなど対象回復技の優先度が +3 になる。
2. やどりぎのタネ・アクアリングなど除外技では優先度が上がらない。
3. ヒーリングシフト対象外の攻撃技は優先度が上がらない。

## 検証コマンド

```powershell
python -m pytest tests/test_ability.py -k "ヒーリングシフト" -v
python -m pytest tests/ -v
```
