# 特性実装計画: <特性名>

更新日: YYYY-MM-DD

## スコープ

- 仕様書: `docs/spec/abilities/<特性名>.md`
- 実装状態: 未実装 / 実装済み / 部分実装
- 実装方針: <一文で方針を記述。例：「ON_TURN_END でHPを確認し回復量を計算する」>

## 実装ゴール（完了定義）

<!-- 「何ができれば完了か」を箇条書きで。テストで検証できる粒度にする -->
- <主効果が正しく発動する>
- <発動しない例外条件で不発になる>
- <既存テストを壊さない>

## 実装対象イベント

<!-- 使用するイベントを列挙。不要なら省略可 -->
- `Event.<イベント名>` — <発動タイミングの説明>

## 変更対象（ファイル/関数単位）

- `src/jpoke/data/ability.py` — `<特性名>` に handlers を登録する
- `src/jpoke/handlers/ability.py` — `<特性名>_<効果名>` を実装する
- `tests/test_ability.py` — テストを追加する

<!-- 新規イベントが必要な場合 -->
<!-- - `src/jpoke/enums/event.py` — `Event.<新規イベント名>` を追加する -->
<!-- - `src/jpoke/core/*.py` — イベント発火箇所を追加する -->

## 実装ステップ

1. 仕様書 `docs/spec/abilities/<特性名>.md` で発動条件・例外条件を確認する。
2. `src/jpoke/handlers/ability.py` に `<特性名>_<効果名>` を実装する。
3. `src/jpoke/data/ability.py` の `<特性名>` に該当イベントの handler を登録する。
4. `tests/test_ability.py` にテストを追加し、全テストが通ることを確認する。

<!-- 複雑な特性では手順を細分化する。例： -->
<!-- 2a. 新規イベントを追加する -->
<!-- 2b. イベント発火箇所を実装する -->

## テスト観点

<!-- 番号付きで。「発動する」「発動しない」「解除される」「相互作用」の4軸を基本とする -->
1. 発動条件を満たしたときに効果が出る。
2. 発動条件を満たさないときは効果が出ない。
3. <解除条件があれば記述>
4. <交代・場状態・他特性との相互作用>

## 検証コマンド

```powershell
python -m pytest tests/test_ability.py -k "<特性名>" -v
python -m pytest tests/ -v
```

---
<!-- 以下は複雑な特性のみ使用。不要なら削除する -->

## 事前に固定する仕様値

<!-- 定数化が必要な数値がある場合のみ記載 -->
<!-- - <定数名>: <値>（実装位置候補: `src/jpoke/utils/constants.py`） -->

## 疑似コード

<!-- 実装イメージが非自明な場合のみ記載 -->
```python
# def <特性名>_<効果名>(battle, ctx, value):
#     ...
```

## リスクと対策

<!-- 既存ロジックへの副作用や仕様の曖昧点がある場合のみ記載 -->
<!-- - リスク: <内容> -->
<!--   - 対策: <内容> -->
