# あんこくきょうだ 実装計画書

## 仕様要約（docs/spec/moves/あんこくきょうだ.md 参照）

* タイプ: あく / 分類: 物理 / 威力: 75 / 命中: 100 / PP: 5
* 対象: 相手1体 / 優先度: 0 / 接触あり / パンチ技
* 効果: 必ず急所に当たる（急所ランク+3）。追加効果なし。

## 現状の実装状況

`src/jpoke/data/move.py` に既に登録済み（`critical_rank=3`、`flags={"contact", "punch"}`）。

```python
"あんこくきょうだ": MoveData(
    type="あく",
    category="physical",
    pp=5,
    power=75,
    accuracy=100,
    critical_rank=3,
    flags={"contact", "punch"},
    handlers={},  # 追加効果なし
),
```

内容は仕様書と一致しており、修正不要。

## ハンドラ構成

急所ランクの効果は `MoveData.critical_rank` フィールドのみで完結する
（`move_executor.py` の `_check_critical` が `ctx.move.critical_rank` を初期値として使用し、
`clamp_critic` 適用後 `CRIT_RATES[3] = 1.0` となるため常に急所となる）。
**専用ハンドラ関数は不要**。`handlers/move_attack.py` への追加コードはない。

`docs/plan/moves/攻撃技_急所系.md` に確定急所グループの共通実装パターンが既に記載されており、
本技もそのパターン（`critical_rank=3` のみで完結）に従っている。

## Priority 根拠

急所ランクは `move_executor.py` の `_check_critical` 内部（ダメージ計算 Interrupt 内）で処理される。
`docs/spec/turn.md` には `ON_CALC_CRITICAL_RANK` イベントの行は独立して記載されていない。
既存の `critical_rank=3` 技（こおりのいぶき・やまあらし・トリックフラワー）と同様のパターンを採用しており、
priority の新規検討は不要（MoveData フィールドのみで完結するため）。

## 実装箇所

### handlers/move_attack.py

追加なし。

### data/move.py

登録済み・修正不要。

## 注意点・エッジケース（review-test 向け）

1. **カブトアーマー / シェルアーマー との相互作用**
   - 確定急所（`critical_rank=3`）であっても、相手の特性カブトアーマー・シェルアーマーが発動すれば急所無効になる。
   - `ON_MODIFY_CRITICAL_RATE`（または同等イベント）でカブトアーマー・シェルアーマー側が急所率を0にする実装になっており、本技側の対応は不要。

2. **パンチ技フラグ**
   - `flags={"contact", "punch"}` により、てつのこぶし・パンチグローブの効果対象となる。実装済み。

3. **テスト方針**
   - `critical_rank=3` は乱数に関係なく急所が確定するため、`fix_random` で命中判定のみ通過させ
     `battle.move_executor.critical is True` を確認するシンプルなテストで十分
     （`こおりのいぶき`・`やまあらし` の既存テストと同様のパターン）。

## 成果物チェックリスト

- [x] 対応する仕様書（`docs/spec/moves/あんこくきょうだ.md`、`docs/spec/critical.md`）を読んだ
- [x] `docs/spec/turn.md` で priority を確認した（急所判定はダメージ計算 Interrupt 内、専用イベントなし）
- [x] ハンドラ構成表に event・priority・subject_spec・役割を記載した（専用ハンドラなし・MoveData フィールドのみ）
- [x] 実装コードが具体的でコピー可能なレベルになっている（既存の `data/move.py` エントリを確認）
- [x] review-test 向けの注意点・エッジケースを列挙した
- [x] `docs/plan/moves/あんこくきょうだ.md` に書き出した
