# 実装計画書: いやしのねがい

## 仕様要約

- タイプ: エスパー / カテゴリ: 変化 / PP: 10 / 命中: なし（必中） / 対象: 自分
- 使用者が **ひんしになる** 代わりに、次に場に出た自分のポケモンの **HP を全回復** し、**状態異常を回復** する
- PP は回復しない（みかづきのまい との違い）
- `heal` ラベルを持つ
- 技使用直後に使用者がひんしになり、死に出し交代が発生する
- 次に場に出たポケモンが「いやしのねがい」効果を受け取った時点でフィールドは消える

## 現状の実装状況

| 項目 | 状態 |
|------|------|
| `data/move.py` エントリ | 登録済み（type="エスパー", category="変化", pp=10, labels=["heal"], handlers なし） |
| `utils/type_defs.py` SideFieldName | `"いやしのねがい"` の追加が必要 |
| `data/field.py` FieldData | 未登録 |
| `handlers/field.py` 関数 | 未実装 |
| `handlers/move_status.py` 関数 | 未実装 |

## ハンドラ構成

| イベント | priority | subject_spec | 役割 | 実装場所 |
|----------|----------|--------------|------|----------|
| `ON_STATUS_HIT` | 100（デフォルト） | `attacker:self` | 自分をひんし状態にし、自陣営に「いやしのねがい」フィールドを設置する | `handlers/move_status.py` → `data/move.py` |
| `ON_SWITCH_IN` | 100 | `source:self` | 場に出たポケモンの HP を全回復・状態異常を回復し、フィールドを解除する | `handlers/field.py` → `data/field.py` |

## Priority 根拠

### ON_STATUS_HIT (priority=100)

`docs/spec/turn.md` に `ON_STATUS_HIT` に関する直接的な記載はないが、同種の類似ハンドラ（`いのちのしずく_heal_self`、`はらだいこ_apply`、`みがわり_apply` 等）はすべて `ON_STATUS_HIT` priority=100（デフォルト）を使用している。`いやしのねがい` も同パターンとする。

### ON_SWITCH_IN (priority=100)

`docs/spec/turn.md` の `Event.ON_SWITCH_IN` セクションに以下の記載がある：

> | 100 | いやしのねがいによる回復 |

priority=100 を明示する。

## 実装箇所

### 1. `src/jpoke/utils/type_defs.py`

`SideFieldName` に `"いやしのねがい"` を追加する。

```python
SideFieldName = Literal["リフレクター", "ひかりのかべ", "しんぴのまもり", "しろいきり", "おいかぜ", "ねがいごと",
                        "まきびし", "どくびし", "ステルスロック", "ねばねばネット", "オーロラベール",
                        "いやしのねがい"]
```

### 2. `src/jpoke/handlers/move_status.py`

五十音順の挿入位置: `いのちのしずく_heal_self` の後、`いやなおと_modify_defender_stats` の前に挿入する。

```python
def いやしのねがい_faint_and_set_side_field(battle: Battle, ctx: EventContext, value: Any) -> HandlerReturn:
    """いやしのねがい: 使用者をひんしにし、自陣営に「いやしのねがい」フィールドを設置する。

    次に場に出たポケモンの HP が全回復し、状態異常が回復する。
    PP は回復しない（みかづきのまいとの違い）。
    """
    mon = ctx.attacker
    side = battle.get_side(mon)
    side.activate("いやしのねがい", 1)
    # 使用者をひんし状態にする
    battle.modify_hp(mon, -mon.max_hp)
    return HandlerReturn(value=value)
```

### 3. `src/jpoke/handlers/field.py`

五十音順の挿入位置: `アクアリング_heal` などの既存関数群の中で、「い」行として適切な位置（`おいかぜ_speed_boost` より前など）に挿入する。

```python
def いやしのねがい_heal_on_switch_in(battle: Battle, ctx: EventContext, value: Any) -> HandlerReturn:
    """いやしのねがい: 場に出たポケモンの HP を全回復し、状態異常を回復する。

    回復後にフィールドを解除する。
    """
    mon = ctx.source
    side = battle.get_side(mon)
    # HP を全回復
    battle.modify_hp(mon, v=mon.max_hp - mon.hp)
    # 状態異常を回復
    battle.ailment_manager.remove(mon)
    # フィールドを解除
    side.deactivate("いやしのねがい")
    return HandlerReturn(value=value)
```

### 4. `src/jpoke/data/field.py`

五十音順の挿入位置: `"ねがいごと"` エントリの前、または「い」行として適切な位置。ただし FIELDS 辞書は既存の順序があるため、五十音順に従い `"いやしのねがい"` を挿入する（実際のキー順序は `おいかぜ` 付近）。

```python
"いやしのねがい": FieldData(
    handlers={
        Event.ON_SWITCH_IN: h.FieldHandler(
            h.いやしのねがい_heal_on_switch_in,
            subject_spec="source:self",
            priority=100,
        ),
    },
),
```

### 5. `src/jpoke/data/move.py`

既存の `"いやしのねがい"` エントリに handlers を追加する。

```python
"いやしのねがい": MoveData(
    type="エスパー",
    category="変化",
    pp=10,
    labels=["heal"],
    handlers={
        Event.ON_STATUS_HIT: h.MoveHandler(
            hs.いやしのねがい_faint_and_set_side_field,
        ),
    }
),
```

## 注意点・エッジケース

- **使用者がひんしになる前提**: `ON_STATUS_HIT` で `side.activate("いやしのねがい", 1)` を先に呼んでからひんし処理（`modify_hp`）を行う。ひんし後に `run_faint_switch` が呼ばれ、死に出しポケモンの `ON_SWITCH_IN` で回復が発動する
- **SideField のカウント管理**: `activate("いやしのねがい", 1)` で 1 ターン分のフィールドを設置するが、`ON_SWITCH_IN` 内で `side.deactivate` を直接呼ぶため、カウントのデクリメントより先にフィールドが消える（`ON_TURN_END` tick は不要）
- **みかづきのまいとの違い**: みかづきのまいは PP も回復するが、いやしのねがいは HP と状態異常のみの回復。両者の実装を混同しないよう注意
- **ailment_manager.remove の uncurable ガード**: `ailment_manager.remove` 内部で `uncurable` フラグをチェックするため、ハンドラ側での追加ガードは不要
- **HP 満タン時の回復**: `modify_hp(mon, v=mon.max_hp - mon.hp)` は HP が満タンの場合 v=0 となり変化しない。`modify_hp` は v=0 のとき早期リターンするため問題ない
- **SideField が設置される陣営**: `battle.get_side(ctx.attacker)` で使用者陣営のフィールドに設置される。ON_SWITCH_IN のハンドラも `ctx.source` から同陣営を取得するため、陣営の一致が保証される
- **次のポケモンが出る前にターンが終了するケース**: 実戦ではほぼないが、理論上は注意が必要。ただし SideField はカウントが残っている限り持続するため、次のターンに出てきたポケモンにも適用される（count=1 で ON_TURN_END に tick を登録しないため、自動消滅しない。ON_SWITCH_IN で明示的に deactivate する設計）
- **「いやしのねがい」フィールドの既存判定**: `ON_APPLY_MOVE`（`ON_BEFORE_APPLY_MOVE`）での失敗判定（`ねがいごと: 前ターン効果残り` に相当）は現段階では実装しない。「いやしのねがい」は使用直後に使用者がひんしになるため、同一ターン内での二重設置は不可能
