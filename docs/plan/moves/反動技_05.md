# 計画書: 反動技（反動量 1/2） — 反動技_05

## 仕様要約

`docs/spec/moves/_move_secondary_2.md` より：与えたダメージの **1/2** を自分が反動として受ける技。

- 反動量: `max(1, round_half_down(damage / 2))`
  （比率1/2の場合、端数は必ず0か0.5となり`int()`切り捨てと`round_half_down`の結果は一致する。
  2026-07 の すてみタックル レビューで共通ヘルパー `_recoil` の端数処理を `round_half_down` に統一した）
- 反動は `reason="recoil"` で処理する（いしあたま特性による無効化のため）
- はめつのひかり・もろはのずつきともに登録済み（両技とも `flags={"recoil"}` とハンドラを持つ）

## 現状の実装状況

| 技名 | data/move.py | labels | ON_HIT ハンドラ |
|------|-------------|--------|----------------|
| はめつのひかり | 登録済み | recoil | ha.はめつのひかり_recoil |
| もろはのずつき | 登録済み | contact, recoil | ha.もろはのずつき_recoil |

（2026-07 のはめつのひかり再レビューで登録済みに更新。ただし当初の登録内容は本計画書の誤記
[タイプ: ゴースト、威力: 150、命中率: 100] をそのまま反映したもので、一次情報
[`docs/wiki/moves/はめつのひかり.html`] と食い違っていたため、下記の通り修正した。）

## ハンドラ構成

| 技名 | イベント | priority | subject_spec | 役割 |
|------|---------|----------|--------------|------|
| はめつのひかり | ON_HIT | 100 | attacker:self | 与ダメの1/2を反動として受ける |
| もろはのずつき | ON_HIT | 100 | attacker:self | 与ダメの1/2を反動として受ける |

## Priority 根拠

`docs/spec/turn.md` の `Event.ON_DAMAGE`（ダメージ付与後の最初の処理）セクションに「priority=100: 反動技による反動ダメージ」と明記されている。

```
## Event.ON_DAMAGE
| 100 | 反動技による反動ダメージ |
```

既存計画書（反動技_025.md）・既存実装（いのちのたまの反動: `data/item.py` で `Event.ON_HIT`, priority=100）いずれも `ON_HIT` に登録しており、本計画書もこれに合わせ `ON_HIT` priority=100 を採用する。`MoveHandler` のデフォルト priority=100 を使用する。

## 実装箇所

### handlers/move_attack.py に追加する2関数

五十音順に従い「は行」の適切な位置に挿入する。

```python
def はめつのひかり_recoil(battle: Battle, ctx: AttackContext, value: Any) -> HandlerReturn:
    recoil = max(1, round_half_down(value / 2))
    battle.modify_hp(ctx.attacker, v=-recoil, reason="recoil", source=ctx.attacker)
    return HandlerReturn(value=value)


def もろはのずつき_recoil(battle: Battle, ctx: AttackContext, value: Any) -> HandlerReturn:
    recoil = max(1, round_half_down(value / 2))
    battle.modify_hp(ctx.attacker, v=-recoil, reason="recoil", source=ctx.attacker)
    return HandlerReturn(value=value)
```

### data/move.py の変更

#### MoveData（はめつのひかり）

`src/jpoke/data/moves/move_ha.py` の `MOVES_HA` 辞書に「はめつのねがい」の次のエントリとして登録済み。

```python
    "はめつのひかり": MoveData(
        type="フェアリー",
        category="特殊",
        pp=5,
        power=140,
        accuracy=90,
        flags={"recoil"},
        handlers={
            Event.ON_HIT: h.MoveHandler(
                ha.はめつのひかり_recoil,
            )
        }
    ),
```

（2026-07 のはめつのひかり再レビューで修正: タイプ `ゴースト` → `フェアリー`、威力 `150` → `140`、
命中率 `100` → `90`。一次情報（`docs/wiki/moves/はめつのひかり.html`）に基づく。フェアリータイプの
専用技であり、ゴーストタイプではない点に注意。また `labels` は `MoveData` のフィールド名としては
`flags` が正しい表記のため修正した。）

#### MoveData（もろはのずつき）

`src/jpoke/data/moves/move_ma.py` の `MOVES_MA` 辞書に登録済み。

```python
    "もろはのずつき": MoveData(
        type="いわ",
        category="物理",
        pp=5,
        power=150,
        accuracy=80,
        flags={"contact", "recoil"},
        handlers={
            Event.ON_HIT: h.MoveHandler(
                ha.もろはのずつき_recoil,
            )
        }
    ),
```

## 注意点・エッジケース

- `reason="recoil"` を指定することで、いしあたま特性（`ON_MODIFY_NON_MOVE_DAMAGE` で `hp_change_reason == "recoil"` を検査）が自動的に反動を無効化する
- `わるあがき_self_damage` は最大HP比（`r=-1/4`）、反動技は与ダメ絶対値（`v=-recoil`）で計算する点が異なる
- `max(1, ...)` によりダメージが2以下の場合でも最低1の反動が発生する
- はめつのひかりは接触技ではないため `flags` に `"contact"` は付けない（一次情報に「直接攻撃: ×」と明記）
- もろはのずつきは接触技（`"contact"`）かつ反動技（`"recoil"`）
- `すてみ` 特性は `has_flag("recoil")` を参照するため、両技に `flags={"recoil"}` を付けることで自動的に威力1.2倍の恩恵を受ける
- turn.md では「反動技による反動ダメージ」は `Event.ON_DAMAGE` のセクションに記載されているが、いのちのたまの反動等と同様に実装上は `Event.ON_HIT` に登録する（既存計画書・実装パターンに準拠）
- 攻撃側がひんしになっても反動処理はハンドラ内で `modify_hp` を呼ぶだけであり、ひんし判定は battle 側で行うため、ハンドラ側に alive ガードは不要
