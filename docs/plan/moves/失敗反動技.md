# 計画書: 失敗反動技

## 仕様要約

「失敗反動（自己HP比）」が 0.5 の技。技が**外れた・失敗した**ときに、自分の最大HPの1/2のダメージを受ける。

- 対象技: かかとおとし / サンダーダイブ / とびげり / とびひざげり
- 発火タイミング: `Event.ON_MISS`（命中判定失敗時）
- ダメージ量: `max(1, attacker.max_hp // 2)`
- reason: `"self_cost"`（いしあたまでは防げない。recoilと区別する）
- subject_spec: `"attacker:self"`（ON_MISS は `AttackContext` を渡す）

## 現状の実装状況

| 技名 | data/move.py 登録 | labels | ON_MISS ハンドラ | 備考 |
|------|------------------|--------|-----------------|------|
| かかとおとし | 登録済み | `["contact", "secondary_effect"]` | なし | ON_DAMAGE_HIT でこんらん効果あり |
| サンダーダイブ | 登録済み | `["minimize", "contact"]` | なし | ハンドラ一切なし |
| とびげり | **未登録** | — | — | 新規 MoveData が必要 |
| とびひざげり | 登録済み | `["contact", "recoil"]` | なし | labels から "recoil" を除く必要あり |

## ハンドラ構成

| 技名 | event | priority | subject_spec | 役割 |
|------|-------|----------|-------------|------|
| かかとおとし | `ON_MISS` | 100 | `attacker:self` | 外れたとき最大HPの1/2ダメージ（self_cost）|
| サンダーダイブ | `ON_MISS` | 100 | `attacker:self` | 外れたとき最大HPの1/2ダメージ（self_cost）|
| とびげり | `ON_MISS` | 100 | `attacker:self` | 外れたとき最大HPの1/2ダメージ（self_cost）|
| とびひざげり | `ON_MISS` | 100 | `attacker:self` | 外れたとき最大HPの1/2ダメージ（self_cost）|

## Priority 根拠

`docs/spec/turn.md` に `Event.ON_MISS` の記載はない（イベント一覧に未掲載）。

既存の `ON_MISS` ハンドラである `からぶりほけん_boost_speed_on_miss`（`data/item.py`）は `ItemHandler` のデフォルト priority=100 で登録されている。また `MoveHandler` のデフォルト priority も 100 である。

**結論: priority=100 を使用する。根拠は MoveHandler デフォルト値と既存の ON_MISS ハンドラ（からぶりほけん）との整合性。**

## 実装箇所

### handlers/move_attack.py に追加する関数（4技分）

五十音順の挿入位置に注意すること（`python scripts/sort_handlers.py` で自動整列）。

```python
def かかとおとし_crash(battle: Battle, ctx: AttackContext, value: Any) -> HandlerReturn:
    """かかとおとしが外れた場合の失敗反動ダメージ。自分の最大HPの1/2を受ける。"""
    battle.modify_hp(ctx.attacker, v=-max(1, ctx.attacker.max_hp // 2), reason="self_cost", source=ctx.attacker)
    return HandlerReturn(value=value)


def サンダーダイブ_crash(battle: Battle, ctx: AttackContext, value: Any) -> HandlerReturn:
    """サンダーダイブが外れた場合の失敗反動ダメージ。自分の最大HPの1/2を受ける。"""
    battle.modify_hp(ctx.attacker, v=-max(1, ctx.attacker.max_hp // 2), reason="self_cost", source=ctx.attacker)
    return HandlerReturn(value=value)


def とびげり_crash(battle: Battle, ctx: AttackContext, value: Any) -> HandlerReturn:
    """とびげりが外れた場合の失敗反動ダメージ。自分の最大HPの1/2を受ける。"""
    battle.modify_hp(ctx.attacker, v=-max(1, ctx.attacker.max_hp // 2), reason="self_cost", source=ctx.attacker)
    return HandlerReturn(value=value)


def とびひざげり_crash(battle: Battle, ctx: AttackContext, value: Any) -> HandlerReturn:
    """とびひざげりが外れた場合の失敗反動ダメージ。自分の最大HPの1/2を受ける。"""
    battle.modify_hp(ctx.attacker, v=-max(1, ctx.attacker.max_hp // 2), reason="self_cost", source=ctx.attacker)
    return HandlerReturn(value=value)
```

### data/move.py の変更内容

#### 1. かかとおとし — ON_MISS ハンドラを追加（既存の ON_DAMAGE_HIT は維持）

```python
"かかとおとし": MoveData(
    type="かくとう",
    category="物理",
    pp=10,
    power=120,
    accuracy=90,
    labels=["contact", "secondary_effect"],
    handlers={
        Event.ON_DAMAGE_HIT: h.MoveHandler(
            ha.かかとおとし_apply_confusion_to_defender,
        ),
        Event.ON_MISS: h.MoveHandler(
            ha.かかとおとし_crash,
        ),
    }
),
```

#### 2. サンダーダイブ — ON_MISS ハンドラを追加

```python
"サンダーダイブ": MoveData(
    type="でんき",
    category="物理",
    pp=15,
    power=100,
    accuracy=95,
    labels=["minimize", "contact"],
    handlers={
        Event.ON_MISS: h.MoveHandler(
            ha.サンダーダイブ_crash,
        ),
    }
),
```

#### 3. とびげり — 新規 MoveData を追加（「と」行、五十音順の適切な位置）

```python
"とびげり": MoveData(
    type="かくとう",
    category="物理",
    pp=10,
    power=85,
    accuracy=90,
    labels=["contact"],
    handlers={
        Event.ON_MISS: h.MoveHandler(
            ha.とびげり_crash,
        ),
    }
),
```

#### 4. とびひざげり — labels を修正し ON_MISS ハンドラを追加

変更点: `labels=["contact", "recoil"]` → `labels=["contact"]`（"recoil" を削除）

```python
"とびひざげり": MoveData(
    type="かくとう",
    category="物理",
    pp=10,
    power=130,
    accuracy=90,
    labels=["contact"],
    handlers={
        Event.ON_MISS: h.MoveHandler(
            ha.とびひざげり_crash,
        ),
    }
),
```

## 注意点・エッジケース（review-test 向け）

### reason="self_cost" の選択理由

- `"recoil"` は反動ダメージ（与ダメージの割合）であり、**いしあたま**特性で無効化される
- `"self_cost"` はみがわり等の自己HP消費と同じ扱いで、いしあたまでは防げない
- マジックガードは `self_cost` を無効化しない（`handlers/ability.py` の `マジックガード_ignore_damage` を確認済み）

### とびひざげり の labels 修正

- 現在の `labels=["contact", "recoil"]` は誤り。失敗反動は「recoil」（与ダメ比）ではなく「失敗反動（自己HP比）」であり、recoil label は不要
- 修正後: `labels=["contact"]` のみ

### ON_MISS の発火条件

- `core/move_executor.py` の `_check_hit` が False を返したときのみ発火する
- 技の使用自体が失敗した場合（ON_TRY_MOVE_1/2 で中断）は ON_MISS は発火しない
- ハンドラ側での追加ガードは不要（move_executor が保証する）

### ひんし後のダメージ

- `battle.modify_hp` はひんし状態のポケモンへの適用をバトル側で管理する
- ハンドラ内で `if not ctx.attacker.alive` のガードは不要

### テスト観点

1. 通常命中時 → ON_MISS ハンドラが発火しないこと（ダメージなし）
2. 外れた時 → 使用者の HP が `max(1, max_hp // 2)` 減ること
3. 最大 HP が奇数のとき → `//` による切り捨てが正しいこと
4. 残 HP が `max(1, max_hp // 2)` 未満でもひんしになること（瀕死処理は battle 側）
5. いしあたまを持つポケモンが使用して外れた場合 → self_cost なのでダメージを受けること
6. とびひざげり が以前の labels="recoil" による誤動作がないこと
7. かかとおとしは外れた場合でも ON_DAMAGE_HIT（こんらん）は発火しないこと（当然だが念のため）
