# 計画書: パワートリック

## 仕様要約

- タイプ: エスパー / カテゴリ: 変化 / PP: 12（`docs/champions/move_list.txt` 準拠。本家 Gen9 基準は10）/ 命中: -（必中）/ 対象: 自分
- 使用者自身の「こうげき（A）」と「ぼうぎょ（B）」の**実数値**を入れ替える
- ランク変化（`rank["atk"]`・`rank["def"]`）は変化しない。実数値のみを入れ替える
- 命中判定: なし（必中）/ まもる: ×（対象が自分自身のため自動的に防がれない）/
  マジックコート: ×（対象が自分自身のため自動的に跳ね返らない）/ よこどり: 未実装（対象外）/
  オウムがえし: ×
- 出典: `docs/spec/moves/パワートリック.md`（一次情報: `docs/wiki/moves/パワートリック.html`）
- 参考: `パワーシフト`（ノーマル・`status_move.md` 203行目 ※旧統合仕様書。現在は個別ファイル化されている）
  と完全に同一効果だが、別の技として独立実装する。`パワーシフト` 自体は `data/move.py` への登録が
  未実施

## 現状の実装状況

| 項目 | 状態 |
|------|------|
| `data/moves/move_ha.py` エントリ | 実装済み（type・category・pp・target・handlers すべて登録済み） |
| `handlers/move_status.py` 関数 | 実装済み（`パワートリック_swap_stats`） |

## ハンドラ構成

| イベント | priority | subject_spec | 役割 | 実装場所 |
|----------|----------|--------------|------|----------|
| `ON_STATUS_HIT` | 100（デフォルト） | `attacker:self`（デフォルト） | こうげき・ぼうぎょ実数値を自分自身で入れ替える | `handlers/move_status.py` |

## Priority 根拠

`docs/spec/turn.md` には `ON_STATUS_HIT` の内部 priority 表は掲載されていない。既存の実数値操作ハンドラ（`スピードスワップ_swap_speed`・`ガードシェア_equalize_stats`・`ガードスワップ_swap_ranks` 等）はすべて priority 100（`MoveHandler` デフォルト）を使用している。パワートリックも同一パターンで priority 100 を採用する。

## 実装箇所

### `src/jpoke/handlers/move_status.py`

挿入位置: 五十音順「は」行 — `はらだいこ_apply` の直後、`ハロウィン_can_apply` の直前に挿入する（はらだいこ → パワートリック → ハロウィン の順）。

実数値の入れ替えは `_stats_manager.stats[1]`（インデックス1 = こうげき）と `_stats_manager.stats[2]`（インデックス2 = ぼうぎょ）を直接操作する。`スピードスワップ` が `stats[5]` を直接操作するパターンを踏襲する。`battle.modify_stat()` はランク変化量加算APIであるため「実数値の入れ替え」には使用しない。

```python
def パワートリック_swap_stats(battle: Battle, ctx: AttackContext, value: Any) -> HandlerReturn:
    """パワートリック: 使用者のこうげきとぼうぎょの実数値を入れ替える。

    ランク変化は行わず、実数値のみを入れ替える。
    インデックス 1 = こうげき、インデックス 2 = ぼうぎょ
    （[HP, 攻撃, 防御, 特攻, 特防, 素早さ]）。
    """
    stats = ctx.attacker._stats_manager.stats
    stats[1], stats[2] = stats[2], stats[1]
    return HandlerReturn(value=value)
```

### `src/jpoke/data/moves/move_ha.py`

既存の `"パワートリック"` エントリに `target="self"` と `handlers` を設定する。
`target` を省略すると `MoveTarget` のデフォルト値 `"foe"` が使われてしまい、
`Move.is_blocked_by_protect`（`target == "foe"` で True）・`Move.is_reflectable`
（`target in ("foe", "foe_side")` で True）が誤って True になり、まもる・マジックコートに
阻害されてしまう。**`target="self"` の明示指定は必須**（`テクスチャー`・`つるぎのまい` 等の
自分対象の変化技と同一パターン）。

```python
"パワートリック": MoveData(
    type="エスパー",
    category="status",
    pp=12,
    target="self",
    handlers={
        Event.ON_STATUS_HIT: h.MoveHandler(
            hs.パワートリック_swap_stats,
        ),
    }
),
```

## 注意点・エッジケース

- **対象は自分のみ**: スピードスワップ（相手と互いに入れ替え）と異なり、パワートリックは使用者自身のこうげきとぼうぎょのみを入れ替える。`ctx.defender` は参照しない
- **`target="self"` の必須性**: 上記の通り、省略するとまもる・マジックコートに誤って阻害される。実装時にレビューで発見・修正した既知の不具合
- **実数値直接操作の根拠**: `スピードスワップ_swap_speed` と同じパターンで `_stats_manager.stats` を直接書き換える。`battle.modify_stat()` はランク変化量加算APIのため「実数値の入れ替え」に使用不可
- **ランク変化は変わらない**: `mon.rank["atk"]`・`mon.rank["def"]` は変化しない。ダメージ計算時には実数値にランク補正がかかる
- **ON_MODIFY_STAT 非発火**: 直接代入のため、ランク変化に反応する特性（かちき・まけんき等）は発動しない。これはゲーム仕様上も正しい（パワートリックは「ランク変化」ではなく「実数値交換」の技）
- **再度使用で元に戻る**: パワートリックを2回使用すると実数値が元の状態に戻る（入れ替え状態は volatile として管理せず、実数値のみを操作するため自動的に対称性が保たれる）
- **交代による解除**: 場を離れると入れ替えた実数値は元に戻る。`Pokemon.reset_on_switch_out()` が `update_stats()` を呼び、種族値ベースで実数値を再計算するため、交代時の復元は自動的に成立している（実装済み・確認済み）
- **ワンダールーム中**: ワンダールームはぼうぎょとくぼうの参照を入れ替えるが、実数値レベルの操作であるパワートリックと干渉した場合の挙動は複雑になる（入れ替えの組み合わせ）。実装シンプル化のため両立する場合は別途検討する
- **パワーシフトとの関係**: ほぼ同一効果（パワーシフトはノーマルタイプ・対象「自分」）だが、別の技として独立したハンドラ関数を実装する。`パワーシフト` 自体は `data/move.py` への登録も未実施のため、将来実装する際に共通関数化を検討してもよい
- **PP について**: `docs/champions/move_list.txt` 準拠で 12（本家 Gen9 基準の 10 ではない）
