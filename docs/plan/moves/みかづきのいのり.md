# 実装計画書: みかづきのいのり

## 仕様要約

- タイプ: エスパー / カテゴリ: 変化 / PP: 5（champions/move_list.txt に本技の行が存在しないため
  Gen9本家値を採用） / 命中: なし（必中） / 対象: 味方全体
- 自分と味方全体の HP をそれぞれ最大 HP の 1/4 ずつ回復し、状態異常を治す（**端数は切り捨て**。
  `いのちのしずく` の四捨五入とは異なる仕様）
- シングルバトルでは「味方全体」＝使用者自身のみが対象
- `MoveData.target="self"` を明示する必要がある（デフォルト値 `"foe"` のままだと、
  `まもる` / `おうごんのからだ` 等の `move.target == "foe"` 判定に誤って合致してしまう）
- 使用者の HP が満タンで、かつ状態異常もない場合は技が失敗する（`ジャングルヒール` と同一の判定。
  一方が満たされなければ成功する）
- `heal` フラグあり（`かいふくふうじ` の対象・`ヒーリングシフト` の優先度+3対象になる）

## 現状の実装状況

| 項目 | 状態 |
|------|------|
| `data/moves/move_ma.py` エントリ | 登録済み（type="エスパー", category="status", pp=5, target="self", flags={"heal"}） |
| `handlers/move_status.py` 関数 | 実装済みだが、HP満タン・状態異常無時の失敗チェックが欠落していた（本レビューで追加） |

## ハンドラ構成

| イベント | priority | subject_spec | 役割 |
|----------|----------|--------------|------|
| `ON_STATUS_HIT` | 100（デフォルト） | `attacker:self`（デフォルト） | HP満タン・状態異常無時の失敗チェック → 自分のHPを最大HPの1/4（切り捨て）回復 → 状態異常を治す |

## Priority 根拠

`docs/spec/turn.md` の `Event.ON_APPLY_MOVE` セクションに以下の記載がある：

> | 100 | ジャングルヒール: HP満タン・状態異常無 |

このイベントはコード上の `ON_STATUS_HIT`（`いのちのしずく` 等の自己回復技と同様のパターン）に
対応する。一次情報によると本技は「タイプとPPを除けば基本的にジャングルヒールと同様の効果」であり、
失敗条件はこの一覧の差異（みがわり・ゆびをふる）に含まれていないため、ジャングルヒールと同一の
priority（100=デフォルト）・判定パターンを踏襲する。

## 実装箇所

### `src/jpoke/handlers/move_status.py`

既存の `みかづきのいのり_apply` を修正し、HP満タン・状態異常無時の失敗チェックを追加する。

```python
def みかづきのいのり_apply(battle: Battle, ctx: AttackContext, value: Any) -> HandlerReturn:
    """みかづきのいのり: 自分のHPを最大HPの1/4回復し、状態異常を治す。

    HPが満タンかつ状態異常もない場合は失敗する（ジャングルヒールと同様の判定。
    docs/spec/turn.md Event.ON_APPLY_MOVE「ジャングルヒール: HP満タン・状態異常無」参照）。
    端数は切り捨て。
    """
    mon = ctx.attacker
    if mon.hp == mon.max_hp and not mon.ailment.is_active:
        return HandlerReturn(value=False, stop_event=True)
    battle.modify_hp(mon, r=1 / 4)
    battle.ailment_manager.remove(mon)
    return HandlerReturn(value=value)
```

### `src/jpoke/data/moves/move_ma.py`

既存の `"みかづきのいのり"` エントリはそのまま（変更なし）:

```python
"みかづきのいのり": MoveData(
    type="エスパー",
    category="status",
    pp=5,
    target="self",
    flags={"heal"},
    handlers={
        Event.ON_STATUS_HIT: h.MoveHandler(hs.みかづきのいのり_apply),
    },
),
```

`target="self"` を明示することで、以下の判定が正しく機能する:

- `Move.is_blocked_by_protect`（`target == "foe"` が前提）→ `まもる` に防がれない
- `おうごんのからだ_block_status_move` など（`target == "foe"` が前提）→ 誤ってブロックされない

## 注意点・エッジケース

- シングルバトルでは「味方全体」の回復対象は使用者自身のみ。ダブルバトルは対象外のため、複数対象
  への回復処理・みがわり判定は実装しない
- 失敗チェックは `mon.hp == mon.max_hp and not mon.ailment.is_active` の両方が真の場合のみ。
  片方だけ満たす場合（例: HP満タンだが状態異常あり）は成功し、状態異常のみ治す
- `HandlerReturn(value=False, stop_event=True)` を返すと `move_executor.py` 内で `MOVE_FAILED`
  ログが記録される
- 端数処理（切り捨て）は `battle.modify_hp(mon, r=1/4)` で行う（`modify_hp` の `r=` 引数は内部で
  `int()` により切り捨てを行うため、追加の丸め処理は不要）
- `かいふくふうじ` 状態のポケモンがこの技を使用した場合の無効化は、`かいふくふうじ` ハンドラ側で
  `heal` フラグを参照して処理されるため、本ハンドラ側での追加チェックは不要
