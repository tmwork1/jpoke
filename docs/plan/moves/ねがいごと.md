# 実装計画書: ねがいごと

## 仕様要約

- タイプ: ノーマル / カテゴリ: status / PP: 12（`docs/champions/move_list.txt` 準拠） / 対象: 自分（target="self"）
- flags: `heal`
- 自陣の場を「ねがいごと」状態にする。使用した次のターン終了時に、その位置の場にいるポケモンのHPが
  **使用者の最大HPの1/2**（小数点以下切り捨て）回復する
- **交代した場合** は同じポジションにいるポケモンが回復する（使用者でなくてよい）
- 同一陣営に既にねがいごとが設置されている場合は **失敗**（重複設置不可）
- ねがいごとフィールドは **カウント 2** で設置し、ON_TURN_END (priority=50) で tick_down される
  - カウント 2 → 1（1ターン目終了）: まだ回復しない
  - カウント 1 → 0（2ターン目終了）: `ON_FIELD_DEACTIVATE` が発火 → HP回復
- 詳細は `docs/spec/moves/ねがいごと.md` / `docs/spec/fields/ねがいごと.md` を参照

## 現状の実装状況

| 項目 | 状態 |
|------|------|
| `types/literals.py` の `SideFieldName` | `"ねがいごと"` が含まれている |
| `model/field.py` の `Field.heal` 属性 | `self.heal: int = 0` として定義済み |
| `data/field/side_field.py` の `"ねがいごと"` エントリ | ON_TURN_END（`ねがいごと_tick`, priority=50）と ON_FIELD_DEACTIVATE（`ねがいごと_heal`）のハンドラが登録済み |
| `handlers/field.py` の `ねがいごと_heal` | `battle.modify_hp(ctx.source, v=value.heal)` で実装済み |
| `handlers/field.py` の `ねがいごと_tick` | 内部ヘルパー `_tick_side_field(battle, ctx, value, name="ねがいごと")` を呼び出して実装済み |
| `data/moves/move_na.py` の `"ねがいごと"` エントリ | `target="self"`, `flags={"heal"}`, `pp=12` で登録済み |
| `handlers/move_status.py` の `ねがいごと_can_apply` / `ねがいごと_set_side_field` | 実装済み |

## ハンドラ構成

| イベント | priority | subject_spec | 役割 | 実装場所 |
|----------|----------|--------------|------|----------|
| `ON_BEFORE_APPLY_MOVE` | 100（デフォルト） | `attacker:self`（デフォルト） | 既にねがいごとフィールドが active なら失敗 | `handlers/move_status.py` → `data/moves/move_na.py` |
| `ON_STATUS_HIT` | 100（デフォルト） | `attacker:self`（デフォルト） | 自陣営に「ねがいごと」を count=2 で設置し、`field.heal` に使用者最大HPの半分を設定する | `handlers/move_status.py` → `data/moves/move_na.py` |
| `ON_TURN_END` | 50 | `source:self` | `_tick_side_field("ねがいごと")` — count=0 になると `ON_FIELD_DEACTIVATE` を発火 | `handlers/field.py` → `data/field/side_field.py` — 実装済み |
| `ON_FIELD_DEACTIVATE` | 100（デフォルト） | `source:self` | 現在場にいるポケモンを `field.heal` 分回復 | `handlers/field.py` → `data/field/side_field.py` — 実装済み |

## Priority 根拠

### ON_BEFORE_APPLY_MOVE (priority=100)

`docs/spec/turn.md` の `Event.ON_APPLY_MOVE` セクション（本実装では `ON_BEFORE_APPLY_MOVE` に対応）に以下の記載がある：

> | 100 | ねがいごと: 前ターン効果残り |

priority=100（MoveHandlerのデフォルト）を採用する。

### ON_STATUS_HIT (priority=100)

`docs/spec/turn.md` に `ON_STATUS_HIT` の直接記載はないが、類似するサイドフィールド設置ハンドラ（`おいかぜ_set_side_field`、`ひかりのかべ_set_side_field`、`しんぴのまもり_set_side_field` 等）はすべて priority=100（デフォルト）を使用している。ねがいごとも同パターンとする。

### ON_TURN_END (priority=50)

`docs/spec/turn.md` の `Event.ON_TURN_END` セクションに以下の記載がある：

> | 50 | ねがいごと |

priority=50 を明示する。`data/field/side_field.py` でも priority=50 が設定済みで一致している。

### ON_FIELD_DEACTIVATE (priority=100)

ねがいごとの `ON_FIELD_DEACTIVATE` ハンドラはカウントが 0 になった時に `_deactivate_field` 内から発火される。priority はデフォルト値 100。`data/field/side_field.py` に登録済み。

## 実装箇所

### 1. `src/jpoke/handlers/move_status.py`

```python
def ねがいごと_can_apply(battle: Battle, ctx: AttackContext, value: Any) -> HandlerReturn:
    """ねがいごとの失敗チェック: すでにねがいごとフィールドが有効なら失敗する。"""
    side = battle.get_side(ctx.attacker)
    if side.get("ねがいごと").is_active:
        battle.add_event_log(
            ctx.attacker, LogCode.MOVE_FAILED,
            payload=FailureLogPayload(move=ctx.move.name, display_reason="ねがいごと")
        )
        return HandlerReturn(value=False, stop_event=True)
    return HandlerReturn(value=value)


def ねがいごと_set_side_field(battle: Battle, ctx: AttackContext, value: Any) -> HandlerReturn:
    """ねがいごとの効果: 自陣営に「ねがいごと」を設置し、回復量を使用者の最大HPの半分に設定する。"""
    mon = ctx.attacker
    side = battle.get_side(mon)
    field = side.get("ねがいごと")
    side.activate("ねがいごと", 2)
    field.heal = mon.max_hp // 2
    return HandlerReturn(value=value)
```

### 2. `src/jpoke/data/moves/move_na.py`

```python
"ねがいごと": MoveData(
    type="ノーマル",
    category="status",
    pp=12,
    target="self",
    flags={"heal"},
    handlers={
        Event.ON_BEFORE_APPLY_MOVE: h.MoveHandler(
            hs.ねがいごと_can_apply,
        ),
        Event.ON_STATUS_HIT: h.MoveHandler(
            hs.ねがいごと_set_side_field,
        ),
    }
),
```

## 注意点・エッジケース

- **PP は `docs/champions/move_list.txt` の値（12）を採用する**。一次情報（Gen9本家基準）の PP10 とは異なる点に注意
- **target は `"self"`**（`"own_side"` や `"field"` ではない）。`champions/move_list.txt` の対象欄が「自分」であり、
  同様に「使用者は自分だが効果は場に残る」性質を持つ `いやしのねがい`（target="self"）と同じ分類にする。
  この engine ではシングルバトルのみが対象のため `self` / `own_side` / `field` の違いは
  `is_blocked_by_protect` / `is_reflectable` の判定結果に影響しない（いずれも `target == "foe"` または
  `target in ("foe", "foe_side")` でのみ判定される）が、`docs/progress/move.md` の対象列や
  今後よこどり等を実装する際の分類のため正しい値を設定する
- **field.heal の設定タイミング**: `side.activate("ねがいごと", 2)` が `False` を返す場合（既にアクティブ）は `ON_BEFORE_APPLY_MOVE` で先に止まるため、`ON_STATUS_HIT` では `activate` が必ず `True` を返す。`field.heal` は `activate` の後で設定する
- **ctx.source の意味**: `ON_FIELD_DEACTIVATE` は ctx=None で emit されるため、`_build_context` が `registered_subject`（Player）から `battle.get_active(player)` を呼ぶ。つまり回復対象はそのポジションの**現在の場のポケモン**になる。これにより「交代した場合は同じ位置のポケモンが回復する」仕様を自動的に実現できる
- **heal 値のリセット**: `field.heal` は `Field.__init__` で `0` に初期化されているため、フィールド解除後の次回 activate 時にゴミ値が残らないよう、`side.activate` が成功したあとで `field.heal = mon.max_hp // 2` を設定する必要がある。`field.count` はリセットされるが `field.heal` は手動設定が必要
- **HP 満タン時の回復**: `battle.modify_hp(mon, v=value.heal)` は内部で `min(max_hp - hp, v)` により満タンを超えない。回復量が 0 になる（満タン）ケースも正常動作する
- **heal フラグによるかいふくふうじ制限**: `flags={"heal"}` が設定されているため、「かいふくふうじ」状態の使用者はねがいごとを選択できない（`かいふくふうじ_try_action` でブロック）。ハンドラ側での追加ガードは不要
- **重複設置の失敗ログ**: `add_event_log(ctx.attacker, LogCode.MOVE_FAILED, ...)` を使用する。他の `_can_apply` ハンドラと同パターンにする
