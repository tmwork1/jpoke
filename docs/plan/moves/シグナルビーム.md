# シグナルビーム 実装計画書

## 結論: 実装しない（対象範囲外）

`docs/spec/moves/シグナルビーム.md` の通り、本技はソード・シールド以降で使用不可能
（第9世代で新規に習得可能なポケモンが存在しない）であり、`docs/champions/move_list.txt`
にも項目が存在しない。README.md の対象範囲定義に該当するため実装しない。

## 削除した実装

過去のレビューで対象範囲確認なしに登録されていた実装を削除した。

- `src/jpoke/data/moves/move_sa.py`: `"シグナルビーム": MoveData(...)` エントリを削除
- `src/jpoke/handlers/move_attack.py`: `シグナルビーム_apply_confusion_to_defender` ハンドラを削除
- `src/jpoke/types/move.py`: `MoveName` Literal から `"シグナルビーム"` を削除
  （`scripts/generate_literals/generate_move_literal.py` で再生成）
- `tests/moves_attack/test_move_sa.py`: `test_シグナルビーム_こんらんが発動する` を削除
- `docs/progress/move.md`: シグナルビーム行を削除（`MOVES` 辞書に存在しない技のため）

## リーサル計算

対象外（実装しないため）。

## 参照

* docs/review/moves/オクタンほう.md（同種の対象範囲外判定・削除手順の前例）
