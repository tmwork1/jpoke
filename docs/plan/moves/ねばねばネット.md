# 実装計画書: ねばねばネット

## 仕様要約

- タイプ: むし / カテゴリ: 変化 / PP: 20 / 対象: 相手の場（target="foe_side"）
- 使用後、相手はポケモンを交代する度に、出てきたポケモンの「すばやさ」ランクが 1 段階下がる
- ひこうタイプや特性「ふゆう」のポケモンは影響を受けない
- 永続効果（ターン制限なし、count=1 で登録）
- turn.md の `ON_SWITCH_IN` priority 100 に「ねばねばネットによる素早さ低下」として記載されている

## 現状の実装状況

| 項目 | 状態 |
|------|------|
| サイドフィールド `ねばねばネット` | 実装済み（docs/progress/side_field.md 確認要） |
| `data/move.py` エントリ | 登録済み（target="foe_side" で handlers なし） |
| `handlers/move_status.py` 関数 | 未実装 |

## ハンドラ構成

| イベント | priority | subject_spec | 役割 |
|----------|----------|--------------|------|
| `ON_STATUS_HIT` | 100（デフォルト） | `attacker:self`（デフォルト） | 相手陣営サイドフィールドに「ねばねばネット」を設定（永続） |

## Priority 根拠

`docs/spec/turn.md` の `Event.ON_APPLY_MOVE` テーブルに「設置技: すでに最大」が priority 100 として記載されている。同種の `ステルスロック_set_field` が priority デフォルト（100）を使用しているため、同一パターンを採用する。

## 実装箇所

### `src/jpoke/handlers/move_status.py`

五十音順の挿入位置: `なみだめ_modify_defender_stats` の後、`にほんばれ_activate_weather` の前（「ね」は「な行」の中で「な」「に」より後）。

```python
def ねばねばネット_set_side_field(battle: Battle, ctx: EventContext, value: Any) -> HandlerReturn:
    """ねばねばネット: 相手陣営に「ねばねばネット」を設定する（永続）。"""
    side = battle.get_side(ctx.defender)
    if not side.activate("ねばねばネット", 1):
        return HandlerReturn(value=False, stop_event=True)
    return HandlerReturn(value=value)
```

### `src/jpoke/data/move.py`

既存の `"ねばねばネット"` エントリに handlers を追加する（target="foe_side" はそのまま）。

```python
"ねばねばネット": MoveData(
    type="むし",
    category="変化",
    pp=20,
    target="foe_side",
    handlers={
        Event.ON_STATUS_HIT: h.MoveHandler(
            hs.ねばねばネット_set_side_field,
        ),
    }
),
```

## 注意点・エッジケース

- `ステルスロック_set_field` と同じく `ctx.defender` の陣営を使う（相手側へ設置）
- `side.activate("ねばねばネット", 1)` の count=1 は「永続」を意味する（ステルスロックと同様）
- ひこうタイプ・ふゆう特性の判定はサイドフィールドの ON_SWITCH_IN ハンドラ側で処理されるため、技ハンドラ側でのガードは不要
- おいかぜ・リフレクター・ひかりのかべ・しんぴのまもりは自陣営（ctx.attacker）を使うが、ねばねばネットは相手陣営（ctx.defender）を使う点に注意
