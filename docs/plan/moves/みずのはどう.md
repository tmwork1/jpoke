# 計画書: みずのはどう

## 仕様要約（docs/spec/moves/みずのはどう.md 参照）

- みずタイプ / 特殊 / 威力60 / 命中100 / PP20 /
  対象: 1体選択 / 優先度0 / 接触なし
- 追加効果として、命中時に20%の確率で相手をこんらん状態にする。
- `pulse` フラグを持つ（波動技。メガランチャー等の対象）。
- 追加効果に分類されるため、ちからずく（威力1.3倍・効果無効化）の対象技になる。
- `docs/champions/move_list.txt:878` に記載あり（みず / 特殊 / 60 / 100 / 20 / 接触なし / まもる有効）。

## 既存実装との差分

既存実装（`data/moves/move_ma.py` の `"みずのはどう"` エントリ、`handlers/move_attack.py` の
`みずのはどう_apply_confusion_to_defender`）は仕様と一致しており、差分なし。

## ハンドラ構成

### handlers/move_attack.py: `みずのはどう_apply_confusion_to_defender`（既存・変更なし）

```python
def みずのはどう_apply_confusion_to_defender(battle: Battle, ctx: AttackContext, value: Any) -> HandlerReturn:
    """みずのはどうの追加効果: 20%の確率で相手をこんらん状態にする。"""
    return apply_confusion_to_defender(battle, ctx, value, chance=0.2)
```

- `apply_confusion_to_defender`（`handlers/move.py`）は内部で `battle.resolve_secondary_chance` を
  呼び出すため、`Event.ON_MODIFY_SECONDARY_CHANCE` 経由でちからずく（確率0化・威力1.3倍）の
  ハンドラが自動的に適用される（`secondary_effect` フラグが前提）。
- イベント: `Event.ON_DAMAGE_HIT`（ダメージが実際に適用された後に発動。
  docs/spec/turn.md の `## Event.ON_DAMAGE` 参照）。
  ダメージが発生しなかった場合（無効化・まもる等）はイベント自体が発火しないため、
  追加効果も発生しない仕様を自然に満たす。
- 優先度: デフォルト100（`MoveHandler` のデフォルト値）。
- subject_spec: `MoveHandler` デフォルト（`attacker:self`）のまま。

### data/moves/move_ma.py（既存・変更なし）

```python
"みずのはどう": MoveData(
    type="みず",
    category="special",
    pp=20,
    power=60,
    accuracy=100,
    flags={"pulse", "secondary_effect"},
    handlers={
        Event.ON_DAMAGE_HIT: h.MoveHandler(
            ha.みずのはどう_apply_confusion_to_defender,
        )
    }
),
```

## リーサル計算

対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`。こんらん付与のみでHP分布に影響しない
追加効果のためリーサル計算ハンドラは不要）。

## 注意点・エッジケース

- ノーマルタイプの相手など、技自体が無効化された場合、`Event.ON_DAMAGE_HIT` 自体が発火しないため
  追加効果も発生しない。個別のガードは不要。
- まもるで防がれた場合も同様にダメージが発生せず `Event.ON_DAMAGE_HIT` が発火しないため、
  追加効果も発生しない。
- 既にこんらん状態の相手に対しては `volatile_manager.apply_confusion` 側で重複付与しない
  （`VolatileManager` の既存ロジックに準拠、本技側での重複ガードは不要）。
- `pulse` フラグにより、メガランチャー等の波動技強化特性の対象となる（本技固有のハンドラ実装は不要、
  特性側のハンドラが `flags` を参照して処理する）。
