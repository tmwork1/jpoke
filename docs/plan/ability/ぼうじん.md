# 特性実装計画: ぼうじん

更新日: 2026-05-03

## 仕様参照

- `spec/ability/ぼうじん.md`

## 実装ゴール

- すなあらしによる天候ダメージを受けない。
- 粉・胞子系の技および特性ほうしを無効化する。
- かたやぶりの効果がある粉技は防げない。
- ゆき天候は第九世代仕様でダメージ自体がないため、ダメージ軽減処理の対象外とする。

## 実装方針

- 天候ダメージ免除: **実装済み（既存）**  
  `src/jpoke/handlers/field.py` の `すなあらし_turn_end` で `ぼうじん` を免除リストに含めている（L87）。  
  `src/jpoke/data/ability.py` や `src/jpoke/handlers/ability.py` への追加は不要。
- 粉技無効: **実装済み（本セッション）**  
  `ON_CHECK_IMMUNE` で `move.has_label("powder")` の技を無効化。
- ほうし無効: 未実装。`ON_BEFORE_APPLY_AILMENT` でほうし起因の状態付与を無効化する（将来タスク）。

## 変更候補（未実装分）

- `src/jpoke/data/ability.py`
- `src/jpoke/handlers/ability.py`
- `tests/test_ability.py`

## テスト観点

1. ~~すなあらし中の天候ダメージを受けない。~~ → field.py で保証済み（テスト: `test_すなあらし_ぼうじん免除` 等で確認）
2. ねむりごな・きのこのほうし等の粉技を無効化する。 **実装済み**
3. ほうし特性による胞子付与も無効化する。 **未実装**
4. かたやぶりの粉技には防げない。 **実装済み**
5. かがくへんかガス発動中は発動しない。
