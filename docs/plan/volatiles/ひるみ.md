# ひるみ — 実装プラン

## 実装状況

✅ **実装済み**

## 効果概要

そのターン、行動できなくなる（次ターンには解除）。

## 使用イベント

| イベント | subject_spec | 効果 |
|---|---|---|
| ON_CHECK_ACTION | ttacker:self | 行動をブロックして状態解除 |

## テスト状況

## 実装メモ

- `ON_CHECK_ACTION` で1回だけ行動を止め、その場で volatile を解除する1ターン状態。
- 行動不能ログも同時に出す。

## テスト要件

- [x] 行動をブロックする
- [x] 発動後に状態が解除される
- [x] `ACTION_BLOCKED` ログが出る

## 残課題

- 特になし。基本挙動は一通り確認済み。
