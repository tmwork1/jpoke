# シャドーダイブ — 実装プラン

## 実装状況

✅ **実装済み** (かくれる volatile として統合実装)

## 効果概要

シャドーダイブ1ターン目：影に潜り姿を隠す（シャドーフォース専用）。

## volatile キー

かくれる（あなをほる・そらをとぶ・ダイビングと同一 volatile）

## テスト状況

## 実装メモ

- `かくれる` volatile の `move="シャドーダイブ"` 分岐で、通常技は回避しつつ同技では命中しない扱いを返す。
- 共通の潜伏拘束は `かくれる` に集約されている。

## テスト要件

- [x] 通常技を回避する
- [x] シャドーダイブ同士では命中しない
- [ ] 潜伏中の交代不可・コマンド固定は `かくれる` 共通として別ファイル参照

## 残課題

- 共通拘束挙動は `あなをほる` 側でのみ直接確認済み。
