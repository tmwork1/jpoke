# そらをとぶ — 実装プラン

## 実装状況

✅ **実装済み** (かくれる volatile として統合実装)

## 効果概要

そらをとぶ1ターン目：空中に飛び上がり姿を隠す。

## volatile キー

かくれる（あなをほる・ダイビング・シャドーダイブと同一 volatile）

## テスト状況

## 実装メモ

- `かくれる` volatile の `move="そらをとぶ"` 分岐で命中例外を判定する。
- 空中技として `かみなり` 等の例外命中を持つ。

## テスト要件

- [x] 通常技を回避する
- [x] かみなりでは命中する
- [ ] 潜伏中の交代不可・コマンド固定は `かくれる` 共通として別ファイル参照

## 残課題

- 共通拘束挙動は `あなをほる` 側でのみ直接確認済み。
