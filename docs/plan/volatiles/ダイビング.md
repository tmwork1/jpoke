# ダイビング — 実装プラン

## 実装状況

✅ **実装済み** (かくれる volatile として統合実装)

## 効果概要

ダイビング1ターン目：水中に潜り姿を隠す。

## volatile キー

かくれる（あなをほる・そらをとぶ・シャドーダイブと同一 volatile）

## テスト状況

## 実装メモ

- `かくれる` volatile の `move="ダイビング"` 分岐で、水中技の命中例外を判定する。
- 共通の潜伏拘束は `かくれる` に集約されている。

## テスト要件

- [x] 通常技を回避する
- [x] なみのりでは命中する
- [ ] 潜伏中の交代不可・コマンド固定は `かくれる` 共通として別ファイル参照

## 残課題

- 共通拘束挙動は `あなをほる` 側でのみ直接確認済み。
