# あめ 実装計画書

## 実装状況

実装済み。本計画書はレビュー時の事後記録として作成。

## ハンドラ一覧

### あめ_power_modifier（ON_CALC_POWER_MODIFIER）

```python
# src/jpoke/handlers/field.py
def あめ_power_modifier(battle: Battle, ctx: AttackContext, value: Any) -> HandlerReturn:
    """雨状態での技威力補正。防御側がばんのうがさを持つ場合は無効。"""
    # 仕様: 晴れ/雨のダメージ補正は防御側の効果とみなされる
    if battle.weather_for(ctx.defender).name == "":
        return HandlerReturn(value=value)
    move_type = ctx.move.type
    if move_type == "みず":
        value = apply_fixed_modifier(value, 6144)  # 1.5倍
    elif move_type == "ほのお":
        value = apply_fixed_modifier(value, 2048)  # 0.5倍
    return HandlerReturn(value=value)
```

- subject_spec: `"attacker:self"` （AttackContext のため）
- priority: デフォルト 100
- `battle.weather_for(ctx.defender)` がばんのうがさ保持時に空天候を返すため、`name == ""` でガード

### tick_weather（ON_TURN_END）

```python
# src/jpoke/handlers/field.py
def tick_weather(battle: Battle, ctx: EventContext, value: Any):
    # 1P側でのみカウントダウンを実行
    if battle.get_player(ctx.source) is battle.players[0]:
        battle.weather_manager.tick_down()
    return HandlerReturn(value=value)
```

- subject_spec: `"source:self"` （EventContext のため）
- priority: 10（turn.md の「にほんばれ等の天気終了」Priority 10）

## data/field.py 登録

```python
"あめ": FieldData(
    handlers={
        Event.ON_CALC_POWER_MODIFIER: h.FieldHandler(
            h.あめ_power_modifier,
            subject_spec="attacker:self",
        ),
        Event.ON_TURN_END: h.FieldHandler(
            h.tick_weather,
            subject_spec="source:self",
            priority=10,
        ),
    },
),
```

## テスト観点

1. みず技 1.5 倍 → `test_はれあめ_タイプ威力補正` でカバー済み
2. ほのお技 0.5 倍 → `test_はれあめ_タイプ威力補正` でカバー済み
3. ばんのうがさ持ち防御側にはみず技補正が無効 → 追加が必要
4. ばんのうがさ持ち防御側にはほのお技補正が無効 → 追加が必要
5. 天候カウントダウン → `test_天候カウント減少` でカバー済み
