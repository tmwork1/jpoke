# ひかりのかべ 実装計画書

## 概要

- カテゴリ: 自陣サイドフィールド
- 実装ファイル: `src/jpoke/handlers/field.py`, `src/jpoke/data/field/side_field.py`
- 技の実装: `src/jpoke/handlers/move_status.py`, `src/jpoke/data/move.py`

## ハンドラ一覧

### フィールド効果ハンドラ（side_field.py への登録）

| ハンドラ関数 | イベント | subject_spec | priority | 効果 |
|---|---|---|---|---|
| `ひかりのかべ_reduce_damage` | ON_CALC_DAMAGE_MODIFIER | defender:self | 100 | 特殊技ダメージ1/2軽減 |
| `ひかりのかべ_tick` | ON_TURN_END | source:self | 130 | カウントダウン |

### 技ハンドラ（move.py への登録）

| ハンドラ関数 | イベント | subject_spec | priority | 効果 |
|---|---|---|---|---|
| `ひかりのかべ_set_side_field` | ON_STATUS_HIT | attacker:self | 100 | 自陣に5ターンのひかりのかべを設定 |

## 実装の詳細

### ひかりのかべ_reduce_damage

```python
def ひかりのかべ_reduce_damage(battle, ctx, value):
    if (
        not ctx.critical
        and not ctx.can_bypass_screen(battle)
        and ctx.move.category == "special"
    ):
        value = apply_fixed_modifier(value, 2048)
    return HandlerReturn(value=value)
```

- 急所（`ctx.critical`）の場合は軽減しない
- すりぬけ等の壁貫通（`ctx.can_bypass_screen`）の場合は軽減しない
- 技カテゴリが "special" の場合のみ軽減する（物理技は軽減しない）
- apply_fixed_modifier(value, 2048) = 1/2倍（2048/4096）

### ひかりのかべ_tick

```python
def ひかりのかべ_tick(battle, ctx, value):
    return _tick_side_field(battle, ctx, value, name="ひかりのかべ")
```

- ターン終了時に「ひかりのかべ」のカウントを1減らす
- priority=130 は docs/spec/turn.md の壁カウントダウン順に従う

### ひかりのかべ_set_side_field（move_status.py）

```python
def ひかりのかべ_set_side_field(battle, ctx, value):
    side = battle.get_side(ctx.attacker)
    if not side.apply("ひかりのかべ", 5, source=ctx.attacker):
        return HandlerReturn(value=False, stop_event=True)
    return HandlerReturn(value=value)
```

- 5ターンを初期値として `side.apply()` を呼ぶ
- `side.apply()` 内で ON_MODIFY_DURATION イベントが発火し、ひかりのねんどがあれば 8ターンに延長される
- すでに発動中の場合は `side.apply()` が False を返し技が失敗する

## 特記事項

- オーロラベールとの重複を防ぐロジックはオーロラベール側ハンドラに実装済み
- ひかりのねんどによる8ターン延長は `data/item.py` のハンドラが担当
