# おおあめ 実装計画書

## 実装状況

実装済み。本計画書はレビュー時の事後記録として作成。

## ハンドラ一覧

### あめ_power_modifier（ON_CALC_POWER_MODIFIER）

```python
# src/jpoke/handlers/field.py
def あめ_power_modifier(battle: Battle, ctx: AttackContext, value: Any) -> HandlerReturn:
    """雨状態での技威力補正。防御側がばんのうがさを持つ場合は無効。"""
    if battle.weather_for(ctx.defender).name == "":
        return HandlerReturn(value=value)
    move_type = ctx.move.type
    if move_type == "みず":
        value = apply_fixed_modifier(value, 6144)  # 1.5倍
    elif move_type == "ほのお":
        value = apply_fixed_modifier(value, 2048)  # 0.5倍
    return HandlerReturn(value=value)
```

- subject_spec: `"attacker:self"`（AttackContext のため）
- priority: デフォルト 100
- おおあめと「あめ」でハンドラを共有する

### おおあめ_block_move（ON_TRY_MOVE_1）

```python
# src/jpoke/handlers/field.py
def おおあめ_block_move(battle: Battle, ctx: AttackContext, value: Any) -> HandlerReturn:
    """おおあめ中にほのおタイプ技を失敗させる（攻撃技・変化技を問わない）"""
    if ctx.move.type == "ほのお":
        battle.add_event_log(
            ctx.attacker, LogCode.MOVE_FAILED,
            payload={"reason": "おおあめ"}
        )
        return HandlerReturn(value=False, stop_event=True)
    return HandlerReturn(value=value)
```

- subject_spec: `"attacker:self"`（AttackContext のため）
- priority: 10（turn.md の ON_TRY_MOVE_1 「おおあめ・おおひでりによる技失敗」Priority 10 と一致）

## data/field.py 登録

```python
"おおあめ": FieldData(
    handlers={
        Event.ON_CALC_POWER_MODIFIER: h.FieldHandler(
            h.あめ_power_modifier,
            subject_spec="attacker:self",
        ),
        Event.ON_TRY_MOVE_1: h.FieldHandler(
            h.おおあめ_block_move,
            priority=10,
            subject_spec="attacker:self",
        ),
    },
),
```

- ON_TURN_END ハンドラなし（カウントダウン不要）

## テスト観点

1. みず技 1.5 倍 → `test_強天候_同タイプ技は威力強化` でカバー済み
2. ほのお技ブロック（攻撃） → `test_強天候_反対タイプ技は失敗` でカバー済み
3. ほのお技ブロック（変化） → `test_強天候_反対タイプ技は失敗` でカバー済み
4. みず技はブロックされない → `test_強天候_同タイプ技はブロックされない` でカバー済み
5. こおりが付与される（防止なし） → `test_おおあめ_こおりが付与される` で追加
