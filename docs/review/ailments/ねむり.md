# ねむり レビュー結果

## 仕様書

- 新規作成: `docs/spec/ailments/ねむり.md`
  - Champions のカウント仕様（通常: count=2 or 3、ねむる: count=3固定）を記述
  - カウント消費の挙動（tick→count=0でremove→行動成功）を図示
  - Champions 説明文（「2度目は1/3の確率で回復する」= count=2が1/3確率）を解説
  - ねむりを無効化する特性・場の状態（ふみん/やるき/エレキフィールド等）を記述
  - 関連インタラクション（はやおき・こんじょう・ナイトメア等）を記述

## 実装計画書

- 新規作成: `docs/plan/ailments/ねむり.md`
  - ON_TRY_ACTION の priority=10 の根拠を明記（turn.md 参照）
  - ねむる技の count=2→3 修正の必要性を記述
  - ねむけ→ねむり移行の count 計算誤り（randint(1,3)）の修正を記述
  - 通常付与時の count=None 問題（永続睡眠バグ）を記述

## 実装（handlers/ / data/）

- `src/jpoke/core/ailment_manager.py` を修正
  - `apply()` でねむり付与時に count=None の場合 Champions 仕様で自動決定（2: 1/3、3: 2/3）する処理を追加

- `src/jpoke/handlers/move_status.py` を修正
  - `ねむる_apply`: count=2 → count=3 に変更（Wikiの「ねむるのカウントは3で固定」に準拠）

- `src/jpoke/handlers/volatile.py` を修正
  - `ねむけ_remove_and_apply_sleep`: `randint(1, 3)` → Champions 仕様（count=2が1/3、count=3が2/3）に変更

## テスト

- `tests/test_ailment.py` に追加:
  - `test_ねむり_カウント3_3ターン目で回復`: count=3 のとき3ターン目で回復することを確認
  - `test_ねむり_通常付与のcountは2か3`: 通常付与（count省略時）のカウントが2または3であることを確認
  - `test_ねむり_いびきは行動できる`: いびきを選択していればねむり中でも行動不能にならないことを確認

- `tests/moves_status/test_move_yarawa.py` を修正:
  - `test_ねむる_ねむり状態になる`: 期待値 count==2 → count==3 に修正（Champions 仕様に合わせる）

- 全1825テスト通過確認済み
