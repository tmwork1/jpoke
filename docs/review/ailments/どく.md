# どく レビュー結果

## 仕様書

- 新規作成: `docs/spec/ailments/どく.md`
  - どくの効果（最大HPの1/8、最小1）
  - タイプ免疫（どくタイプ・はがねタイプ、ふしょく例外）
  - マジックガード・ポイズンヒール等の関連インタラクション
  - ターン処理優先度（ON_TURN_END Priority 90）
  - ダメージ計算の詳細（最小値1の保証）

## 実装計画書

- 新規作成: `docs/plan/ailments/どく.md`
  - `どく_damage` ハンドラの仕様（subject_spec, priority, 計算式）
  - 最小値1保証の必要性を明記
  - マジックガード・ポイズンヒールは特性側で実装済みであることを明記

## 実装（handlers/ / data/）

- `handlers/ailment.py` の `どく_damage` を修正:
  - `-(mon.max_hp // 8)` → `max(1, mon.max_hp // 8)` にして最小ダメージ1を保証
  - docstring を「最大HPの1/8、最小1」に更新
- `もうどく_damage` は既に `max(1, ...)` と上限15ターン制限が実装済みで変更なし
- `data/ailment.py` は変更なし（登録内容は仕様通り）

## テスト

- 変更なし
  - `test_どく_ダメージ`: ターン終了時ダメージを確認 ✓
  - `test_タイプ一致の状態異常は入らない`: どくタイプ・はがねタイプへの付与不可を確認 ✓
  - `test_もうどく_交代でカウントリセット`: 交代で elapsed_turns がリセットされることを確認 ✓（既存テストは通過）
  - 全1821テスト通過
