# やけど レビュー結果

## 仕様書

- 新規作成: `docs/spec/ailments/やけど.md`
  - やけどの効果（最大HPの1/16、最小1 / ぶつりわざ0.5倍）
  - タイプ免疫（ほのおタイプ）
  - マジックガード・たいねつ・こんじょう・からげんき等の関連インタラクション
  - ターン処理優先度（ON_TURN_END Priority 100）
  - ダメージ計算の詳細（最小値1の保証）
  - Champions説明文の記載

## 実装計画書

- 新規作成: `docs/plan/ailments/やけど.md`
  - `やけど_damage` ハンドラの仕様（subject_spec, priority, 計算式）
  - `やけど_modifier` ハンドラの仕様（ON_CALC_BURN_MODIFIER）
  - `r=-1/16` ではなく整数除算 `max_hp // 16` を使う必要性を明記
  - 最小値1保証の必要性を明記
  - マジックガードは特性側で実装済みであることを明記
  - たいねつ特性のやけどダメージ軽減は今後 reason="burn" を使って実装予定である旨を明記

## 実装（handlers/ / data/）

- `handlers/ailment.py` の `やけど_damage` を修正:
  - `r=-1/16`（浮動小数点計算・最小値保証なし）→ `v=-max(1, mon.max_hp // 16)` に変更
  - 最小値1を保証するように修正
  - `reason="burn"` を追加（将来のたいねつ特性のやけどダメージ軽減に向けた識別子）
  - docstring を「最大HPの1/16、最小1」に更新
- `utils/type_defs.py` の `HPChangeReason` に `"burn"` を追加
- `data/ailment.py` は変更なし（登録内容は仕様通り）

## テスト

- 変更なし（既存テストが仕様を正しくカバーしていた）
  - `test_やけど_ダメージ`: ターン終了時ダメージ（max_hp - max_hp // 16）を確認 ✓
  - `test_やけど_物理技ダメージ半減`: burn_modifier == 2048 を確認 ✓
  - `test_やけど_特殊技ダメージは半減しない`: burn_modifier == 4096 を確認 ✓
  - `test_タイプ一致の状態異常は入らない[ヒトカゲ-やけど]`: ほのおタイプへの付与不可を確認 ✓
  - 全25テスト通過（test_ailment.py）
