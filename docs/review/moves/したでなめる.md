# したでなめる レビュー結果

## 仕様書
- 存在しなかったため、docs/wiki/moves/したでなめる.html を参照して新規作成した
  （タイプ: ゴースト / 分類: 物理 / 威力: 30 / 命中: 100 / PP: 30 / 追加効果: 30%の確率でまひ状態にする）。

## 実装計画書
- 存在しなかったため、仕様書をもとに新規作成した。既存実装（`したでなめる_apply_paralysis_to_defender`）が
  仕様と一致していることを確認した。

## 実装（handlers/ / data/）
- `src/jpoke/data/move.py` の `したでなめる` エントリ（タイプ・分類・威力・命中・PP・flags・ハンドラ）は
  仕様と一致しており、修正なし。
- `src/jpoke/handlers/move_attack.py` の `したでなめる_apply_paralysis_to_defender`
  （`apply_ailment_to_defender` を用いて30%の確率でまひを付与）も仕様と一致しており、修正なし。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列は `n/a`）。状態異常付与のみの追加効果であり、
  既存の同種技（かみなりのキバ・10まんボルト 等）と同様にリーサル計算の対象外。

## テスト
- `tests/moves_attack/test_move_sa.py` に既存の `test_したでなめる_まひが発動する` が実装と一致しており、
  修正不要。

## その他
- `docs/progress/move.md` の したでなめる 行に誤りがあったため修正した
  （旧: 仕様書`-`・計画書`-`・レビュー`-`・分類は正しいが タイプ`エスパー`（誤り）・威力`80`（誤り）・
  命中率`90`（誤り）・PP`16`（誤り）・効果`？`（未記入） → 新: 仕様書`x`・計画書`x`・レビュー`✓`・
  タイプ`ゴースト`・威力`30`・命中率`100`・PP`30`・効果「追加効果として、30%の確率で相手をまひ状態にする。」）。
