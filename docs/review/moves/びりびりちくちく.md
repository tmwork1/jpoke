# びりびりちくちく レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/びりびりちくちく.md`）。一次情報（`docs/wiki/moves/びりびりちくちく.html`）に基づき、
  タイプ: でんき / 分類: 物理 / 威力: 80 / 命中: 100 / PP: 10 / 対象: 相手1体 / 直接攻撃: あり、
  および「追加効果として30%の確率で相手をひるみ状態にする」を記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/びりびりちくちく.md`）。既存実装が仕様と一致していることを確認し、
  ハンドラ構成（`ON_DAMAGE_HIT` / デフォルト priority / `apply_volatile_to_defender` 共通ヘルパー）と
  priority根拠（`docs/spec/turn.md` に未掲載のイベントのため既存の同種ハンドラ〈ひょうざんおろし・
  アイアンヘッド等〉に準拠）を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ha.py` の `びりびりちくちく`（type="でんき", category="physical",
  pp=10, power=80, accuracy=100, flags={"contact", "secondary_effect"}）および
  `src/jpoke/handlers/move_attack.py` の `びりびりちくちく_apply_flinch`
  （`apply_volatile_to_defender(..., volatile="ひるみ", chance=0.3)`）は仕様と一致しており修正不要と判断した。
  `handlers/item.py` の `_INNATE_FLINCH_MOVES` にも本技が含まれており、おうじゃのしるし等との
  重複ひるみ付与を正しく回避している。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列・リーサルテスト列は `n/a`）。追加効果がひるみのみで
  HPに影響しないため、リーサル計算ハンドラは不要という既存方針（いわなだれ・つららおとし・
  ひょうざんおろし等）と同様。

## テスト
- 変更なし。`tests/moves_attack/test_move_ha.py` の `test_びりびりちくちく_ひるみが発動する` が
  既存の同種テストパターン（`アイアンヘッド` 等）と一致した形で実装・検証済みであることを確認した。

## 進捗表
- `docs/progress/move.md` の該当行に誤りがあったため修正した（修正前:
  `分類=物理 / タイプ=かくとう / 威力=- / 命中率=- / PP=16`、修正後:
  `分類=物理 / タイプ=でんき / 威力=80 / 命中率=100 / PP=10`）。
  仕様書・計画書・レビュー列も `x` / `x` / `✓` に更新した。

## テスト実行結果
- `python -m pytest tests/ -v` で 3579 件 passed, 1 skipped（全件成功）。ログは
  `.loop/test_logs/びりびりちくちく.log` に保存。
