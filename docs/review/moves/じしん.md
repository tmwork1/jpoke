# じしん レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/じしん.md`）。一次情報（docs/wiki/moves/じしん.html）を確認し、
  タイプ・分類・威力・命中・PP・対象・優先度・接触の有無、および『あなをほる』状態への命中・威力2倍、
  『グラスフィールド』での威力半減について記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/じしん.md`）。じしん自体は追加効果を持たず、
  『あなをほる』状態への威力2倍・命中確定は `handlers/volatile.py` の `あなをほる_boost_power` /
  `HIDDEN_MOVE_ALLOWED_MOVES`、『グラスフィールド』での威力半減は `handlers/field.py` の
  `グラスフィールド_power_modifier` で既に実装済みであることを明記し、じしん固有の重複ハンドラを
  実装しない方針を記した。

## 実装（handlers/ / data/）
- 変更なし。`data/move.py` の `"じしん"` エントリ（タイプ・分類・威力100・命中100・PP10・
  `handlers={}`）は仕様書・計画書と一致していることを確認した。
- 『あなをほる』状態への威力2倍・命中確定（`handlers/volatile.py`）、
  『グラスフィールド』での威力半減（`handlers/field.py`）も既存実装が仕様と一致していることを確認した。

## リーサル計算
- 対象外（`docs/progress/move.md` の リーサル実装/リーサルテスト列がともに `n/a`）。

## テスト
- `tests/moves_attack/test_move_sa.py` に `test_じしん_相手にダメージを与える` を追加した
  （基本ダメージ確認）。
- 『あなをほる』状態への命中・威力2倍は `tests/moves_attack/test_move_a.py` の
  `test_あなをほる_じしんは地中の相手に命中し威力が2倍になる` で、
  『グラスフィールド』での威力半減は `tests/test_field.py` の `test_グラスフィールド_じしん弱化` で
  既にカバーされていることを確認した（変更なし）。
- `scripts/sort_tests.py tests/moves_attack/test_move_sa.py` でソート、
  `scripts/generate_test_list.py` でテスト一覧を更新、`python -m pytest tests/ -v` で全件パス
  （3397 passed, 1 skipped）を確認した。

## 進捗表の修正
- `docs/progress/move.md` のじしん行について、PP列の誤り（12 → 10、一次情報では10）を修正した。
- 仕様書・計画書・テスト・レビュー列を `x` / `✓` に更新した。
