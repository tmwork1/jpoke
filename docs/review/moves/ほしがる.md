# ほしがる レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ほしがる.md`）。ポケモンWiki一次情報（docs/wiki/moves/ほしがる.html）と
  既存のどろぼう仕様書（docs/spec/moves/どろぼう.md）を突き合わせ、タイプ（ノーマル）・威力（60、第五世代以降の値）・
  命中（100）・PP（25、第六世代以降の値）・優先度・接触フラグ・追加効果（自分が道具未所持かつ相手が道具所持の場合に
  奪う）・判定フラグを記載した。発動タイミング・奪えない/発動しないケース・持ち物との発動順序・他の効果との関係は
  どろぼうと完全に共通のため、どろぼう仕様書を参照する形で重複を避けた。

## 実装計画書
- 新規作成（`docs/plan/moves/ほしがる.md`）。既存実装（`src/jpoke/data/moves/move_ha.py`）を確認したところ、
  どろぼうと同一のハンドラ `ha.どろぼう_steal_item` を `ON_DAMAGE_HIT` イベントで登録済みであり、
  priority根拠（docs/spec/turn.md の ON_DAMAGE セクション、はたきおとす等のアイテム効果と同時刻）も
  どろぼうの計画書と共通であることを確認して記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ha.py` の `ほしがる` エントリ（タイプ・分類・PP・威力・命中・接触フラグ・
  ハンドラ登録）は仕様書・計画書と完全に一致しており、修正不要だった。
  `handlers/move_attack.py` の `どろぼう_steal_item` もどろぼう・ほしがる共通のロジックとして
  正しく実装されている。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装・リーサルテスト列は n/a）。
  どろぼうと同様、アイテム奪取効果はダメージ量そのものに影響しないためリーサル計算ハンドラは不要。

## テスト
- `tests/moves_attack/test_move_ha.py` に以下を新規追加（4件）:
  - `test_ほしがる_タイプ威力命中PPが仕様通り`: タイプ・分類・威力・命中・PP・接触フラグを検証。
  - `test_ほしがる_攻撃者がアイテムを得て防御者がアイテムを失う`: 命中時のアイテム奪取を検証。
  - `test_ほしがる_攻撃者がアイテムを持っているとき失敗`: 自分が道具所持時は不発であることを検証。
  - `test_ほしがる_防御者がアイテムを持っていないとき失敗`: 相手が道具未所持時は不発であることを検証。
  - なお `tests/items/test_item_ha.py::test_ホズのみ_ほしがるでは奪われない` で、
    ホズのみ等タイプ半減実消費が奪取より先に発動するケースは既にカバー済みであることを確認した。
  - `python scripts/sort_tests.py tests/moves_attack/test_move_ha.py` でソート、
    `python scripts/generate_test_list.py` で一覧更新、
    `python -m pytest tests/ -v` を実行し3585件パス（1件skip、無関係）を確認。
    ログは `.loop/test_logs/ほしがる.log` に保存。
