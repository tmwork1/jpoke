# ブレイブバード レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ブレイブバード.md`）。
- 一次情報（`docs/wiki/moves/ブレイブバード.html`）から基本情報（ひこう・物理・威力120・
  命中100・接触あり・優先度0）と効果（与ダメの1/3を自分も反動として受ける、追加効果なし）、
  判定フラグを記録した。
- PPは一次情報の15ではなく `docs/champions/move_list.txt`（790行目）の16を正とした。
- 反動ダメージの端数処理は `docs/spec/moves/_move_secondary_2.md` の共通仕様
  （`round_half_down`、五捨五超入）に従うことを明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/ブレイブバード.md`）。
- 既存実装（`ブレイブバード_recoil` ハンドラ・`ON_HIT` 登録）の構成を記録し、
  priorityの根拠（同系統の反動技と同じデフォルトpriority=100）を明記した。
- リーサル計算は対象外（反動技は防御側HP分布に影響しないため）である旨を明記した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `"ブレイブバード"` エントリで `pp=15` に
  なっていたバグを `pp=16`（`docs/champions/move_list.txt` 基準）に修正した。
  Gen9本家基準の値からChampions側のPP統一（8/12/16/20の4段階）への移行漏れ。
- `handlers/move_attack.py` の `ブレイブバード_recoil`（共通ヘルパー `_recoil(..., 1/3)` を
  使用）および `data/moves/move_ha.py` の `Event.ON_HIT` 登録は仕様通りで変更なし。
- 反動ダメージの端数処理（`round_half_down`）・`reason="recoil"` によるいしあたま/
  マジックガード無効化は既存の共通実装（`_recoil` ヘルパー）で正しく処理されていることを確認した。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列は `n/a`）。
  反動技は攻撃者自身のHPのみを減らし、防御側のHP分布には影響しないため対象外。

## テスト
- 既存テストを確認し、仕様と一致していたため変更なし。
  - `tests/moves_attack/test_move_ha.py::test_ブレイブバード_使用後に攻撃者が反動ダメージを受ける`
  - `tests/moves_attack/test_move__group.py::test_recoil技グループ_反動量が与ダメのrecoil_ratio倍になる`
    （`("ブレイブバード", 1/3)` を含むパラメータ化テストで反動比率1/3を検証済み）
- `python -m pytest tests/ -v` を実行し、3990 passed, 1 skipped で全件成功を確認
  （`.loop/test_logs/ブレイブバード.log`）。
