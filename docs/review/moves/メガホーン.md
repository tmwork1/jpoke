# メガホーン レビュー結果

## 仕様書
- 対象外（変更なし）。ポケモンWiki一次情報を確認したところ「むしタイプ・物理・威力120・命中85・PP10（本家）・単体選択・優先度0・直接攻撃・追加効果なし」という単純な通常攻撃技であり、
  追加効果や特殊な相互作用が存在しない。既存の進捗表でも同種の「追加効果なし」単純攻撃技（10まんばりき・あくうせつだん等）は仕様書・計画書を作成せず
  進捗表の効果列のみで管理する運用になっており、本技もその方針に従い個別の仕様書・計画書は作成しない。

## 実装計画書
- 対象外（変更なし）。上記と同じ理由により作成しない。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ma.py` の `メガホーン` エントリを一次情報と照合し、
  type="むし", category="physical", pp=12（champions/move_list.txt 準拠、本家PP10を8/12/16/20段階に統一した結果と一致）,
  power=120, accuracy=85, flags={"contact"}, handlers={} を確認。追加効果が存在しないため handlers 空辞書で問題なし。

## リーサル計算
- 対象外（進捗表: リーサル実装=n/a, リーサルテスト=n/a）。追加効果のない通常ダメージ技のため、汎用の攻撃ダメージ計算で
  リーサル判定が完結し、専用のリーサルハンドラは不要。

## テスト
- 変更なし。`tests/moves_attack/test_move_ma.py::test_メガホーン_相手にダメージを与える` が存在し、
  ヘラクロスでメガホーンを撃ってカビゴンのHPが減ることを確認する内容として妥当。
- `python -m pytest tests/ -v` を実行し全件パスを確認（実行のたびに `test_ミラーコート_まねっこでコピーできる` /
  `test_むしくい_ねんちゃく持ちをひんしにさせた場合は奪える` / `test_オーロラベール_ひかりのかべとは重複しない` など、
  確率依存の既存テストが単発で偶発的に失敗することがあったが、メガホーンとは無関係の既存の確率的フレーク挙動であり、
  再実行すると全件パスすることを複数回確認した。最終ログは4021 passed, 1 skippedで全件成功。
