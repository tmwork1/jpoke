# みずでっぽう レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/みずでっぽう.md`）。一次情報（docs/wiki/moves/みずでっぽう.html）に基づき、
  タイプ: みず / 分類: 特殊 / 威力: 40 / 命中: 100 / PP: 25 / 対象: 相手1体 / 優先度: 0 / 接触なし、
  追加効果なしの通常攻撃技として記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/みずでっぽう.md`）。既存実装が仕様書と一致していることを確認し、
  専用ハンドラが不要である旨を記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ma.py` の `みずでっぽう` エントリ
  （type="みず", category="special", pp=25, power=40, accuracy=100, handlers={}）は
  仕様書・一次情報と完全に一致しており、修正不要と判断した。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行が「リーサル実装: n/a」「リーサルテスト: n/a」のため）。

## テスト
- `tests/moves_attack/test_move_ma.py` に `test_みずでっぽう_相手にダメージを与える` を追加した。
  従来は間接的な参照（他特性・天候テスト等）のみで、みずでっぽう自体を主眼としたダメージ確認テストが
  なかったため補充した。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ma.py` でソート、
  `python scripts/generate_test_list.py` でテスト一覧を更新し、
  `python -m pytest tests/ -v` で全件（3606 passed, 1 skipped）パスを確認した。
