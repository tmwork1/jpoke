# ロックブラスト レビュー結果

## 仕様書
- 個別の仕様書は作成しない（他の追加効果なし連続技と同様、`docs/spec/moves/連続技.md` に集約されている既存方針を踏襲）。
- 一次情報（`docs/wiki/moves/ロックブラスト.html`）と照合し、いわタイプ・物理・威力25・命中90（第五世代以降）・PP10（本家基準）・非接触・2-5回連続攻撃（命中判定は初回のみ、急所は各回）・弾の技である点を確認。既存の `docs/spec/moves/連続技.md` の記載（9.3節）と齟齬なし。

## 実装計画書
- 個別の計画書は作成しない。`docs/plan/moves/攻撃技_連続技系.md` に「ロックブラスト: 設定完了（handler のみ不要）」として既に計画済みであることを確認。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ra.py` の `ロックブラスト` エントリで `pp=10`（第9世代本家基準）になっていたバグを修正し、`pp=12`（チャンピオンズ基準、`src/jpoke/data/ps-champ-ja/moves.json` に準拠）に変更した。
- その他（type/category/power/accuracy/flags/multi_hit）は仕様通りで変更なし。追加効果がないため handlers は空のままで正しい。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列は n/a）。`handlers/lethal.py` にロックブラスト専用ハンドラは存在せず、追加は不要と判断。

## テスト
- `tests/moves_attack/test_move_yarawa.py` に `test_ロックブラスト_タイプ威力命中PPが仕様通り` を追加し、type/category/power/accuracy/pp（12）/flags（bullet かつ非接触）/multi_hit の内容を検証するようにした。
- 既存の `test_ロックブラスト_複数ヒットする`（ヒット回数が2〜5の範囲であることを確認）は変更なし。
