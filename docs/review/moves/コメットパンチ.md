# コメットパンチ レビュー結果

## 仕様書
- 個別ファイルは存在せず、標準的な「攻撃技＋自分ランク上昇追加効果」パターンとして
  `docs/spec/moves/_move_secondary_1.md`（62行目）の共有テーブルに集約されている。
  一次情報（`docs/wiki/moves/コメットパンチ.html`）と照合した結果、内容
  （対象=attacker、確率0.2、こうげき+1）は正しく、変更なし。
- `docs/spec/moves/_move_labels.md`（punch・contact 区分）も一次情報と一致しており変更なし。

## 実装計画書
- `docs/plan/moves/コメットパンチ.md` が存在しなかったため新規作成した。
  一次情報・既存実装の確認結果・修正内容をまとめている。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_attack.py` の `コメットパンチ_boost_attacker_A` は
  `modify_attacker_stats(..., stats={"atk": 1}, chance=0.2)` で正しく実装されており変更なし。
- `src/jpoke/data/moves/move_ka.py` の `MoveData` エントリに PP のバグを発見し修正した。
  - `pp=10`（Gen9本家基準の値）→ `pp=12`
    （`src/jpoke/data/ps-champ-ja/moves.json` の `コメットパンチ はがね 物理 90 90 12 接○ 守○` に合わせる）
  - `flags={"contact", "punch", "secondary_effect"}` は一次情報と一致しており変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行は「リーサル実装: n/a」「リーサルテスト: n/a」）。

## テスト
- `tests/moves_attack/test_move_ka.py` にコメットパンチのテストが存在しなかったため、
  以下を新規追加した（いずれも PASSED）。
  - `test_コメットパンチ_こうげき1段階上昇が発動する`（`secondary_chance=1.0`）
  - `test_コメットパンチ_こうげき1段階上昇が発動しない`（`secondary_chance=0.0`）

## その他
- `docs/progress/move.md` の コメットパンチ 行が、データ破損により
  `こらえる`（優先度+4・HP1残る等）の説明文・タイプ（ノーマル）・対象（attacker）と
  混在していたため、コメットパンチ自身の正しい情報（タイプ：はがね、威力90、命中90、
  対象：defender、効果：20%の確率で自分の『こうげき』ランクを1段階上げる）に修正し、
  仕様書・計画書・実装・テストの各列を x、レビュー列を ✓ に更新した。
