# ステルスロック レビュー結果

## 一次情報

* `docs/wiki/moves/ステルスロック.html` は存在しないため、`docs/champions/move_list.txt`
  （408〜409行目）を一次情報として確認した
  * タイプ: いわ / 分類: 変化 / 威力: - / 命中率: - / PP: 20 / 接× / 守×
  * 効果文: 「使用後、相手はポケモンを交代する度に、出てきたポケモンは最大HPの1/8のダメージを受ける。
    『タイプ』相性の影響を受け、例えば4倍弱点の場合は最大HPの1/2のダメージとなる。」
  * 実装（`data/moves/move_sa.py`・`data/field/side_field.py`・`handlers/move_status.py`・
    `handlers/field.py`）はいずれも一次情報と一致していることを確認した

## 仕様書

* 新規作成: `docs/spec/moves/ステルスロック.md`
  * 技本体（設置行為）の仕様を記載。ステルスロック状態そのものの詳細（ダメージ計算・免疫条件・
    タイプ相性・解除条件）は既存の `docs/spec/fields/ステルスロック.md` に委譲する構成とした
    （`どくびし`・`ねばねばネット`の `moves/` 仕様書と同じパターン）
  * 判定フラグ節で `target="foe_side"` により `まもる`（対象外）・`マジックコート`（反射可能）・
    `よこどり`/`オウムがえし`（未実装のため対象外）の挙動を実装コード（`Move.is_blocked_by_protect`
    / `Move.is_reflectable`）の判定条件と対応付けて明記した
* 既存の `docs/spec/fields/ステルスロック.md` は一次情報（`move_list.txt`）・実装
  （`handlers/field.py` の `ステルスロック_damage`）と齟齬なく、修正不要と判断した

## 実装計画書

* 新規作成: `docs/plan/moves/ステルスロック.md`
  * `どくびし`・`まきびし`・`ねばねばネット`の `plan/moves/` と同じ構成
    （現状の実装状況表・ハンドラ構成表・Priority根拠・実装箇所・注意点）で記載
  * `ON_STATUS_HIT` は `docs/spec/turn.md` に未掲載のイベントであるため、既存の同種ハンドラ
    （`どくびし_set_field`・`まきびし_set_field`・`ねばねばネット_set_side_field`）と同じ
    デフォルト priority（100）を根拠として明記した
* 既存の `docs/plan/fields/ステルスロック.md` は実装（`ステルスロック_damage`）と一致しており修正不要
  * `TYPE_MODIFIER` を直接参照し `ON_CALC_DEF_TYPE_MODIFIER` イベントを経由しない設計により、
    らんきりゅう補正を受けない点、`// 4096` の整数除算バグを避けるため浮動小数点除算
    （`base / (4096 * 8)`）を使う点、いずれも `src/jpoke/core/damage.py` の
    `calc_def_type_modifier` と同じ「タイプごとに `int(base * rate)` を順次乗算する」計算方式
    と整合していることをコードレベルで確認した

## 実装（handlers/ / data/）

* 変更なし。以下を仕様書・計画書と照合し、一致していることを確認した
  * `src/jpoke/data/moves/move_sa.py` の `"ステルスロック"` エントリ
    （type="いわ"、category="status"、pp=20、target="foe_side"）
  * `src/jpoke/handlers/move_status.py` の `ステルスロック_set_field`
    （相手陣営に設置、既設置時は `HandlerReturn(value=False, stop_event=True)` で失敗）
  * `src/jpoke/data/field/side_field.py` の `"ステルスロック"` エントリ（`max_count` 未指定 = 1層のみ）
  * `src/jpoke/handlers/field.py` の `ステルスロック_damage`
    （`is_hazard_immune` によるあつぞこブーツ免疫判定 → `TYPE_MODIFIER["いわ"]` から
    タイプごとに順次乗算してダメージ倍率を算出 → `battle.modify_hp` で反映。
    `reason` 未指定のため `ON_MODIFY_NON_MOVE_DAMAGE` 経由でマジックガードが正しく無効化する）
  * `Move.is_reflectable`（`category=="status" and target in ("foe","foe_side")`）により、
    追加実装なしでマジックコート・マジックミラーの反射対象になっていることを確認した
  * `Move.is_blocked_by_protect`（`target=="foe"` のみ対象）により、`target="foe_side"` の
    本技は自動的にまもるの対象外になっていることを確認した
* ハンドラの五十音順配置を確認するため `python scripts/sort_handlers.py
  src/jpoke/handlers/move_status.py` を実行したところ、`ステルスロック_set_field` 自体の位置に
  問題はなかったが、無関係な `オーロラベール_set_side_field` / `オーロラベール_check_weather` の
  並び順が崩れていたため、実行結果として自動修正された（副次的な整合性修正）

## リーサル計算

* 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`。変化技のためダメージ計算対象外。
  他の設置技（まきびし・どくびし・ねばねばネット）と同様の扱い）

## テスト

* 追加: `tests/moves_status/test_move_sa.py`
  * `test_ステルスロック_相手陣営に設置される`: 使用すると相手陣営にステルスロックが設置される
  * `test_ステルスロック_すでに設置済みなら失敗`: 既設置時は再設置されない（失敗する）
  * `test_ステルスロック_マジックコートで跳ね返り使用者側に設置される`: マジックコートで反射され
    使用者側に設置されることを確認する既存未カバーのエッジケース
    （`どくびし`側の既存テストと同じ構成）
* 既存確認（変更なし）: `tests/test_field.py`
  （`test_ステルスロック_x1`・`x1_2`・`x1_4`・`x4`・`マジックガード無効`。
  タイプ相性ダメージ計算・マジックガード無効化を十分にカバーしている）
* 既存確認（変更なし）: `tests/items/test_item_a.py`
  （`test_あつぞこブーツ_まきびし系ハザード無効`。あつぞこブーツによる免疫を確認）
* 既存確認（変更なし）: `tests/moves_status/test_move_a.py`（おかたづけでの解除）、
  `tests/moves_status/test_move_ka.py`（きりばらいでの解除）、
  `tests/moves_attack/test_move_ka.py`（がんせきアックスによる設置）、
  `tests/abilities/test_ability_ma.py`はいずれも仕様と整合しており変更なし
* `python scripts/sort_tests.py tests/moves_status/test_move_sa.py` で並び替え、
  `python scripts/generate_test_list.py` で一覧を再生成した
* 全テスト: `python -m pytest tests/ -v` で 4480 passed, 1 skipped
