# いびき レビュー結果

## 仕様書
- 変更なし（`docs/spec/moves/いびき.md` は一次情報（Wiki）と照合したが、誤り・欠落は見つからなかった）

## 実装計画書
- 新規作成（`docs/plan/moves/いびき.md`）
  - `ON_TRY_MOVE_1`（priority=30, `attacker:self`）で「自分がねむり状態（ゆめうつつ含む）でない場合は失敗する」チェックを追加する計画を記載
  - `docs/spec/turn.md` の `Event.ON_TRY_MOVE_1` テーブルに記載された「いびき・ねごと: ねむり状態でない」の行を priority 根拠として明記

## 実装（handlers/ / data/）
- 修正した内容:
  - `handlers/move_attack.py` に `いびき_check_sleep` を新規実装。`ctx.attacker.ailment.is_sleep` を見て、ねむり状態でない場合は `MOVE_FAILED` ログを出して失敗させる
    - `is_sleep` は通常の「ねむり」だけでなく `ぜったいねむり` 特性による「ゆめうつつ」状態異常も True になるため、一次情報の「特性ぜったいねむりのポケモンが使用するときは常に成功する」を満たす
  - `data/move.py` の `いびき` エントリに `Event.ON_TRY_MOVE_1`（priority=30, `subject_spec="attacker:self"`）ハンドラを追加
  - 発見した欠落: 従来の実装は `いびき_apply_flinch`（ひるみ付与）のみが登録されており、「自分がねむり状態でない場合は失敗する」という一次情報の必須条件が未実装だった（覚醒中でも通常のダメージ技として使用できてしまう状態）
  - `python scripts/sort_handlers.py src/jpoke/handlers/move_attack.py` と `python scripts/sort_data/sort_moves.py` を実行済み

## リーサル計算
- 対象外（`docs/progress/move.md` の いびき 行「リーサル実装」列は `n/a`）

## テスト
- 追加した内容（`tests/moves_attack/test_move_a.py`）:
  - `test_いびき_ねむり状態でない場合は失敗する`: ねむり状態でない場合、いびきが失敗し相手にダメージを与えないことを確認
  - `test_いびき_ぜったいねむり特性なら成功する`: 特性ぜったいねむり（ゆめうつつ状態）のポケモンは、ねむり状態でなくても成功しひるみを付与できることを確認
  - 既存の `test_いびき_ひるみが発動する` は回帰なし
  - `python scripts/sort_tests.py tests/moves_attack/test_move_a.py` と `python scripts/generate_test_list.py` を実行済み
  - `python -m pytest tests/ -v` 実行結果: 3237 passed, 1 skipped（既存のskipは本変更と無関係）。結果は `.loop/test_logs/いびき.log` に保存

## 追記（並列レビューによる退行の修正・2026-07-05）
- 並行実行していた複数の攻撃技レビューエージェントによる `src/jpoke/data/move.py` への
  競合編集で、本エントリの `Event.ON_TRY_MOVE_1` ハンドラ（`いびき_check_sleep`,
  priority=30, `subject_spec="attacker:self"`）の registration が丸ごと失われ、
  `Event.ON_DAMAGE_HIT`（ひるみ付与）のみのエントリに戻っていた
  （`handlers/move_attack.py` 側の関数実装自体は残っていた）。
  そのため `test_いびき_ねむり状態でない場合は失敗する` が回帰していた。
- `handlers` に `Event.ON_TRY_MOVE_1: h.MoveHandler(ha.いびき_check_sleep,
  subject_spec="attacker:self", priority=30)` を再追加して復旧。実装・仕様に変更なし。
