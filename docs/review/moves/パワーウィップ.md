# パワーウィップ レビュー結果

## 仕様書
- 新規作成（docs/spec/moves/パワーウィップ.md）。docs/wiki/moves/パワーウィップ.html の一次情報
  （くさ・物理・威力120・命中85・PP10（本家）・接触あり・追加効果なし）をもとに作成した。
  Champions基準のPPは docs/champions/move_list.txt の値（12）を採用した。

## 実装計画書
- 新規作成（docs/plan/moves/パワーウィップ.md）。追加効果を持たない攻撃技のため専用ハンドラは
  不要という方針を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ha.py` の `MOVES_HA["パワーウィップ"]` は
  type="くさ", category="physical", pp=12（docs/champions/move_list.txt と一致）,
  power=120, accuracy=85, flags={"contact"}, handlers={} で仕様書・計画書と一致していた。

## リーサル計算
- 対象外（docs/progress/move.md のリーサル実装列は n/a。追加効果を持たない通常攻撃技のため）。

## テスト
- 変更なし。`tests/moves_attack/test_move_ha.py` の
  `test_パワーウィップ_相手にダメージを与える` が既に存在し、仕様と一致する内容だった。
  `python -m pytest tests/ -v` で全件パス（3963 passed, 1 skipped）を確認した。
  （実行中に発生した `test_オーロラベール_ひかりのかべとは重複しない` /
  `test_しんぴのつるぎ_相手のとくぼうランク変化の影響を受けない` の失敗はランダム性由来のflakyな
  既存テストであり、パワーウィップとは無関係。単体再実行では両方パスすることを確認済み。）

## 進捗表
- docs/progress/move.md のパワーウィップ行: 仕様書列・計画書列を `x` に、レビュー列を `✓` に、
  効果列を「追加効果なし」に更新した（実装・テスト列は既に `x` だった）。
