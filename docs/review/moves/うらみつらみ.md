# うらみつらみ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/うらみつらみ.md`）
  - タイプ: ゴースト / 分類: 特殊 / 威力: 75 / 命中: 100 / PP: 10 / 対象: 相手1体 / 優先度: 0 / 非接触
  - 効果: 100%の確率で相手の『こうげき』ランクを1段階下げる（追加効果）
  - Pokémon LEGENDS アルセウス固有の「状態異常時威力2倍」「しもやけ付与」効果は
    第九世代（本プロジェクト対象）には存在しないため対象外と明記

## 実装計画書
- 新規作成（`docs/plan/moves/うらみつらみ.md`）
  - 既存実装（`data/move.py` / `handlers/move_attack.py`）が仕様書と一致することを確認し、
    ハンドラ構成表・priority根拠・注意点を記載

## 実装（handlers/ / data/）
- 変更なし
  - `data/move.py` の `うらみつらみ` エントリ（type=ゴースト, category=special, pp=10, power=75,
    accuracy=100, flags={"secondary_effect"}, Event.ON_DAMAGE_HIT）は仕様と一致
  - `handlers/move_attack.py` の `うらみつらみ_lower_defender_atk`（`modify_defender_stats` で
    defender の atk を -1、chance省略により100%）は仕様と一致
  - 既存の同種ハンドラ（エナジーボール・エレキネット等）と同じ `ON_DAMAGE_HIT` パターンで実装済み

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a）

## テスト
- 変更なし
  - `tests/moves_attack/test_move_a.py::test_うらみつらみ_こうげき1段階低下が発動する` が既に存在し、
    100%発動によるこうげきランク-1を検証できている
  - `python -m pytest tests/ -v` 実行結果: 3256 passed, 1 skipped（既存のskipで無関係）
