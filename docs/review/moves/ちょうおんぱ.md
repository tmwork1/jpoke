# ちょうおんぱ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ちょうおんぱ.md`）
- タイプ: ノーマル / 変化 / 命中率55 / PP20 / 対象1体選択 / 効果「相手をこんらん状態にする」
- PPについて: `docs/champions/move_list.txt` にちょうおんぱ自体のエントリが存在しない
  （`じごくづき` の説明文中で「音系の技」の1つとして名前が挙がるのみ）ため、
  Champions側の上書き値がなく、Gen9本家準拠の20を採用した旨を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/ちょうおんぱ.md`）
- `あやしいひかり`・`てんしのキッス` と同一パターン（`ON_STATUS_HIT` priority 100
  デフォルト、`battle.volatile_manager.apply_confusion` を呼ぶ）で計画した。

## 実装（handlers/ / data/）
- **重大な欠落を修正**: `src/jpoke/data/moves/move_ta.py` の `"ちょうおんぱ"` エントリが
  `handlers={}` （コメント「追加効果なし」）となっており、技の本体効果である「こんらん付与」が
  一切実装されていなかった。以下を追加:
  - `src/jpoke/handlers/move_status.py` に `ちょうおんぱ_apply` を新規実装
    （`ちからをすいとる_can_apply` の直後、`ちょうのまい_modify_attacker_stats` の直前、
    五十音順の位置に挿入）。`battle.volatile_manager.apply_confusion(ctx.defender, source=ctx.attacker)`
    を呼び出す、`あやしいひかり_apply`／`てんしのキッス_apply` と同一実装。
  - `src/jpoke/data/moves/move_ta.py` の `"ちょうおんぱ"` エントリに
    `Event.ON_STATUS_HIT: h.MoveHandler(hs.ちょうおんぱ_apply)` を追加。
- `flags={"sound"}` は既存のまま維持。第六世代以降のみがわり貫通・ぼうおん等の対象判定は
  `move_executor.check_hit_substitute` / `ぼうおん_block_sound` 側で `sound` フラグから汎用的に
  処理されるため、個別の `bypass_substitute` フラグ追加は不要と確認した。
- PP=20・命中率55・type=ノーマル・category=status は一次情報（ポケモンWiki）と一致しており修正なし。

## リーサル計算
- 対象外（ダメージを与えない変化技のため `docs/progress/move.md` のリーサル実装/テスト列は
  `n/a` のまま。変更なし）。

## テスト
- `tests/moves_status/test_move_ta.py` に以下を追加（`ちからをすいとる` 系テストの直後、
  `ちょうのまい` 系テストの直前、五十音順の位置）:
  - `test_ちょうおんぱ_こんらん状態を付与する`
  - `test_ちょうおんぱ_すでにこんらん状態なら失敗`
  - `test_ちょうおんぱ_みがわりを貫通する`
- `python -m pytest tests/ -v` で全4226件パス（1件skip）を確認（ログ: `.loop/test_logs/ちょうおんぱ.log`）。
