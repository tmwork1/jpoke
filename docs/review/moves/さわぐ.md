# さわぐ レビュー結果

## 仕様書

- 新規作成（`docs/spec/moves/さわぐ.md`）
- 基本情報（タイプ・分類・威力・命中率・PP・優先度・直接攻撃）、フラグ（`sound`/`non_negoto`）、
  ちからずく非対象（`secondary_effect`フラグを付けない理由）、みがわり貫通、さいはい失敗の根拠を明記
- 詳細な揮発性状態としての仕様は既存の `docs/spec/volatiles/さわぐ.md` を参照する形で分離
- 併せて `docs/spec/volatiles/さわぐ.md` の記述誤りを修正
  - 「ねむけ自体の付与も防ぐ」という記述が実装（`ON_BEFORE_APPLY_VOLATILE` ハンドラは存在しない）と
    食い違っていたため、「ねむけ付与自体は防がない。ねむけ終了時のねむり移行のみ防ぐ」に修正

## 実装計画書

- 新規作成（`docs/plan/moves/さわぐ.md`）
- 技側（`ON_HIT` での `さわぐ_apply`）の実装状況・priority根拠・登録コードを記載
- 既存の `docs/plan/volatiles/さわぐ.md` の記述不整合も修正
  - 「さわぐ技の `ON_HIT` ハンドラ未実装」となっていたが実際は実装済みだったため状態を修正
  - 存在しない `さわぐ_prevent_nemuke`（`ON_BEFORE_APPLY_VOLATILE`）の項目を削除
  - テスト方針をねむけ挙動の実装に合わせて修正

## 実装（handlers/ / data/）

- 変更なし。`handlers/move_attack.py` の `さわぐ_apply`、`data/move.py` の `"さわぐ"` エントリ
  （type=ノーマル, category=special, pp=10, power=90, accuracy=100,
  flags={"non_negoto", "sound"}, ON_HIT: さわぐ_apply）は仕様・計画と一致していることを確認した。
- `sound` フラグによるみがわり貫通（`move_executor.check_hit_substitute`）、`non_negoto` フラグに
  よるねごと除外・さいはい対象外（`last_used_move_name` 未記録）も確認し、いずれも正しく動作する。
- `secondary_effect` フラグを付与していないため、ちからずくによる威力上昇・追加効果無効化の
  対象にならないことを確認（仕様どおり「追加効果に分類されない」）。

## リーサル計算

- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`）

## テスト

- `tests/moves_attack/test_move_sa.py` に以下を新規追加:
  - `test_さわぐ_命中したときさわぐ揮発性状態を付与する`
  - `test_さわぐ_外れたときさわぐ状態を付与しない`（従来テストされていなかった「外れたときは付与されない」ケースを追加）
- `python scripts/sort_tests.py tests/moves_attack/test_move_sa.py` でソート
- `python scripts/generate_test_list.py` で `docs/tests/` を更新
- `python -m pytest tests/ -v` で全3378件パス（1件skip）を確認

## 進捗表の修正

- `docs/progress/move.md` の `さわぐ` 行を修正:
  - PP列の誤記（12 → 10、`data/move.py` 実装値と一致させた）
  - 仕様書・計画書・テスト・レビュー列を `x` に更新
