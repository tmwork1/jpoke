# ジェットパンチ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ジェットパンチ.md`）。一次情報（`docs/wiki/moves/ジェットパンチ.html`）と
  `src/jpoke/data/ps-champ-ja/moves.json:360` を基に、基本情報・効果・判定フラグ・備考を記載した。
  PPはWiki記載の15（Gen9本家値）ではなく、champions側の16を採用した。

## 実装計画書
- 新規作成（`docs/plan/moves/ジェットパンチ.md`）。既存の同種先制技（でんこうせっか等）の
  計画書フォーマットに倣い、PP修正の根拠とハンドラ不要の理由を明記した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_sa.py` の `ジェットパンチ` エントリで `pp=15`（Gen9本家値の
  移行漏れバグ）になっていたのを `pp=16`（champions基準値）に修正した。
- その他（type, category, power, accuracy, priority, flags={"contact","punch"}, handlers={}）は
  仕様書と一致しており修正不要。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a）。

## テスト
- 変更なし。既存の `test_ジェットパンチ_相手にダメージを与える`（`tests/moves_attack/test_move_sa.py`）は
  他の優先度+1先制技（でんこうせっか等）と同一のテスト形式で、内容・網羅性ともに問題なし。
