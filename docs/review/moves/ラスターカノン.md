# ラスターカノン レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ラスターカノン.md`）。
  - 一次情報（`docs/wiki/moves/ラスターカノン.html`）を参照し、タイプ=はがね、分類=特殊、
    威力80、命中100、対象=相手1体、優先度0、接触なし、追加効果「10%の確率で相手の
    とくぼうを1段階下げる」を記載。
  - PPは `docs/champions/move_list.txt` 準拠で `12`（Gen9本家のPP10とは異なる）と明記。
  - 既存の共有仕様書 `docs/spec/moves/_move_secondary_1.md` に該当行が既に存在することを
    参照として明記。

## 実装計画書
- 新規作成（`docs/plan/moves/ラスターカノン.md`）。
  - 発見した2件のバグ（PP誤り・`secondary_effect`フラグ欠落）と修正内容、
    ハンドラ構成・priority根拠・リーサル対象外の理由・テスト方針を記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ra.py` の `"ラスターカノン"` エントリを修正:
  - `pp=10` → `pp=12`（`docs/champions/move_list.txt` の値に修正。Gen9本家PPからの
    移行漏れバグ）。
  - `flags={"secondary_effect"}` を追加（欠落していた）。これにより、ちからずく所持時の
    威力上昇・追加効果無効化が正しく機能するようになる。
- `src/jpoke/handlers/move_attack.py` の `ラスターカノン_lower_defender_spd`
  （`modify_defender_stats(..., stats={"spd": -1}, chance=0.1)`）はロジック自体は
  仕様と一致しており変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列は `n/a`）。確率発動の相手ランク低下
  効果でありダメージ量自体を変えないため、リーサル計算の対象外。

## テスト
- `tests/moves_attack/test_move_yarawa.py` に以下を追加:
  - `test_ラスターカノン_PPは12`: `MOVES["ラスターカノン"].pp == 12` を確認。
  - `test_ラスターカノン_相手にダメージを与える`: 通常ダメージ確認。
  - `test_ラスターカノン_とくぼう低下が発動しない`: `secondary_chance=0.0` でランク変化なし。
  - `test_ラスターカノン_とくぼう1段階低下が発動する`: `secondary_chance=1.0` でとくぼう-1。
  - `MOVES` のインポートをファイル先頭に追加。
- `python -m pytest tests/ -v` で全4029件パス（1件skip）を確認
  （`.loop/test_logs/ラスターカノン.log` に保存）。
  なお実行順によっては無関係の既存テスト
  `tests/moves_attack/test_move_ma.py::test_むしくい_ねんちゃく持ちをひんしにさせた場合は奪える`
  が時刻ベースの乱数シード依存でまれに失敗することを確認したが、単独実行では常に成功し、
  本レビュー対象（ラスターカノン）の変更とは無関係の既存の flaky テストであるため、
  本タスクの対象外として修正していない。
