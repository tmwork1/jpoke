# いのちのしずく レビュー結果

## 仕様書
- 変更なし。一次情報（`docs/wiki/moves/いのちのしずく.html`）と再照合し、以下を確認した:
  - タイプ / 分類 / PP（12・チャンピオンズ基準） / 命中率（必中） / 対象（味方全員→シングルでは
    使用者自身のみ）/ 優先度 / 直接攻撃の有無、いずれも一次情報と一致
  - 端数処理が四捨五入である旨（`ジャングルヒール`・`みかづきのいのり`の切り捨てとの差異）が
    一次情報の「技の仕様」節と一致
  - `まもる` / `マジックコート` / `おうじゃのしるし` に対する判定（いずれも無効）が一致

## 実装計画書
- 変更なし。仕様書の内容と priority・ハンドラ構成が一致していることを再確認した。

## 実装（handlers/ / data/）
- 変更なし。以下を実装コードから直接確認し、仕様書の記述と矛盾がないことを検証した:
  - `src/jpoke/data/moves/move_a.py` の `いのちのしずく` エントリ（`pp=12`, `target="self"`,
    `flags={"heal"}`）
  - `src/jpoke/handlers/move_status.py` の `いのちのしずく_heal`（`round_half_up(mon.max_hp / 4)`
    による四捨五入回復、HP満タン時の失敗処理）
  - `Move.is_blocked_by_protect` / `_apply_type_absorb`（かんそうはだ等）/
    `おうごんのからだ_block_status_move` / `Move.is_reflectable`（マジックコート）が
    いずれも `move.target == "foe"` を前提とするため、`target="self"` の本技は
    まもる・敵の吸収特性・マジックコートのいずれにも影響されないことをコード上で確認した
  - `MoveHandler` のデフォルト `subject_spec` が `"attacker:self"` であり、仕様書・計画書の
    記載と一致することを確認した

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`。使用者自身を回復する技のため
  対戦相手へのリーサル計算とは無関係）。

## テスト
- 変更なし。`tests/moves_status/test_move_a.py` の以下のテストが実装と一致していることを確認した:
  - HP減少時に最大HPの1/4（四捨五入）回復する
  - HP満タン時は失敗する
  - かいふくふうじ中は回復が無効化される
  - 端数の四捨五入（110/4=27.5→28）
  - 相手のまもるで防がれない
  - 相手のよびみずに引き寄せられず回復できる
- `python -m pytest tests/ -v` で全件（5801 passed, 1 skipped）を確認した。

## 進捗表
- `docs/progress/move.md` の「いのちのしずく」行の「レビュー」列が前回レビュー時に更新漏れで
  `-` のままだったため、今回の再確認をもって `x` に修正した。
