# 10まんばりき レビュー結果

## 仕様書
- 変更なし（新規作成せず）。10まんばりきは物理・じめん・威力95・命中95・PP10・追加効果なしの通常攻撃技であり、
  既存の同種技（はたく、ぶんまわす、りゅうのはどう、アクアテール、ドラゴンクロー、パワージェム、メガホーンなど）
  と同様に、進捗表でも仕様書列は「-」として扱う運用に統一されているため、専用の仕様書ファイルは作成しない。

## 実装計画書
- 変更なし（新規作成せず）。上記と同じ理由により、専用の計画書ファイルは作成しない。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `"10まんばりき"` エントリ
  （type="じめん", category="physical", pp=10, power=95, accuracy=95, flags={"contact"}, handlers={}）は
  一次情報（docs/wiki/moves/10まんばりき.html: タイプ じめん / 分類 物理 / 威力95 / 命中率95 / PP10 / 直接攻撃○ / 追加効果×）
  と一致しており、修正不要と判断した。

## テスト
- `tests/moves_attack/` に10まんばりきの専用テストが存在しなかったため追加した。
  - `tests/moves_attack/test_move_sa.py::test_10まんばりき_相手にダメージを与える`
    （追加効果なしの物理じめん技として相手にダメージを与えることを確認する、既存の同種テストと同形式）
- `python scripts/sort_tests.py tests/moves_attack/test_move_sa.py` でソート済み。
- `python scripts/generate_test_list.py` で `docs/tests/moves_attack.md` を更新済み。
- `python -m pytest tests/ -v` を実行し、3200 passed, 1 skipped（既存のskipで無関係）で全件成功を確認した
  （ログ: `.loop/test_logs/10まんばりき.log`）。

## 進捗表 (`docs/progress/move.md`)
- PP列が誤って `12` と記載されていたため、一次情報・実装に合わせて `10` に修正した。
- テスト列は元々 `x` だったが実際にはテストが存在しなかったため、テストを追加したことで実態と一致させた
  （マーク自体は既に `x` だったため変更なし、実装済みの状態に追いついた）。
