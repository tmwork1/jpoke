# 10まんばりき レビュー結果

## 仕様書
- 新規作成した。既存の仕様書ファイルは存在しなかった（`docs/progress/move.md` の仕様書列も `-`）。
  一次情報（docs/wiki/moves/10まんばりき.html）と src/jpoke/data/ps-champ-ja/moves.json の値
  （じめん・物理・威力95・命中95・PP12・接触あり・追加効果なし）をもとに、
  `docs/spec/moves/はたく.md` と同じ体裁で `docs/spec/moves/10まんばりき.md` を作成した。
  なお過去のレビュー記録（本ファイルの旧版）には「専用の仕様書ファイルは作成しない運用」という
  記述が残っていたが、現在は他の技（はたく等）と同様に個別ファイルを作成する運用に統一されているため、
  その記述は古い内容だった。

## 実装計画書
- 新規作成した。既存の計画書ファイルは存在しなかった。仕様書をもとに、既存実装が仕様と一致することを
  確認する内容として `docs/plan/moves/10まんばりき.md` を作成した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_symbol.py` の `"10まんばりき"` エントリ
  （type="じめん", category="physical", pp=12, power=95, accuracy=95, flags={"contact"},
  handlers={}）は、src/jpoke/data/ps-champ-ja/moves.json（PP12）・一次情報（じめん/物理/威力95/命中95/直接攻撃○/
  追加効果×）と完全に一致しており、修正不要だった。
  （PPについて: src/jpoke/data/ps-champ-ja/moves.json では10まんばりきのPPは12であり、Gen9本家基準の10ではない。
  実装・進捗表ともに既に12で一致しているため、CLAUDE.mdの「PP値はチャンピオンズ基準を正とする」
  というルールに沿った正しい状態だった。過去のレビュー記録がPP10への修正を主張していたのは
  誤りであり、現状の実装（PP12）が正しい。）

## リーサル計算
- 対象外（docs/progress/move.md の「リーサル実装」「リーサルテスト」列がともに `n/a`）。

## テスト
- 変更なし。`tests/moves_attack/test_move_sa.py` の
  `test_10まんばりき_相手にダメージを与える` が既に存在し、追加効果のない物理じめん技として
  ダメージを与えることを検証しており、内容は妥当だった。
- `python -m pytest tests/ -v` を実行し、5801 passed, 0 failed, 1 skipped（既存の無関係なskip）で
  全件成功を確認した（ログ: `.loop/test_logs/10まんばりき.log`）。
