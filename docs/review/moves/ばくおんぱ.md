# ばくおんぱ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ばくおんぱ.md`）
- 一次情報（`docs/wiki/moves/ばくおんぱ.html`）と `src/jpoke/data/ps-champ-ja/moves.json` を突き合わせ、基本情報（タイプ:
  ノーマル / 分類: 特殊 / 威力140 / 命中100 / PP12（Champions値） / 追加効果なし / 音のわざ・みがわり貫通）
  を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/ばくおんぱ.md`）
- 追加効果を持たない技であり、`MoveData`（`type` / `category` / `pp` / `power` / `accuracy` / `flags={"sound"}`）
  の登録のみで挙動が完結することを明記。みがわり貫通は `core/move_executor.py` の
  `check_hit_substitute` が `sound` フラグから汎用処理するため、専用ハンドラは不要と結論づけた。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ha.py` の `ばくおんぱ` エントリ
  （`type="ノーマル"`, `category="special"`, `pp=12`, `power=140`, `accuracy=100`,
  `flags={"sound"}`, `handlers={}`）は仕様書・計画書と一致しており、修正不要と確認した。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列は `n/a`。追加効果を持たない通常のダメージ攻撃技のため）

## テスト
- `tests/moves_attack/test_move_ha.py` に `test_ばくおんぱ_相手にダメージを与える`
  （通常ダメージの確認）を追加。
- 既存の `test_ばくおんぱ_みがわりを貫通して本体にダメージを与える`（音技のみがわり貫通確認）は
  実装・仕様と一致しており変更なし。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ha.py` でソート、
  `python scripts/generate_test_list.py` で一覧更新、
  `python -m pytest tests/ -v` で全件（3574 passed, 1 skipped）を確認した
  （結果は `.loop/test_logs/ばくおんぱ.log` に保存）。
