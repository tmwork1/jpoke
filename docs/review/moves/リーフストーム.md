# リーフストーム レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/リーフストーム.md`）。一次情報（ポケモンWiki）を基に
  タイプ・分類・威力（第六世代以降130）・命中率・PP（Champions基準8）・判定フラグ
  （急所あり・まもるで防がれる・おうじゃのしるし対象外・ちからずく非対象等）・
  同系統技（オーバーヒート等）との整合性を記載。

## 実装計画書
- 新規作成（`docs/plan/moves/リーフストーム.md`）。既存実装の確認結果、PP値の誤り、
  ハンドラ構成・priority根拠、リーサル計算・テストの状況をまとめた。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ra.py` の `リーフストーム` エントリで `pp=5`
  （Gen9本家基準の値）になっていたバグを `pp=8`（Champions基準、
  `src/jpoke/data/ps-champ-ja/moves.json:934` 準拠）に修正。
- `src/jpoke/handlers/move_attack.py` の `リーフストーム_sharply_lower_spa_C` は
  同系統技（オーバーヒート・りゅうせいぐん）と同一パターンの実装であり修正不要。

## リーサル計算
- `src/jpoke/handlers/lethal.py` の `オーバーヒート_lower_attacker_spa` を流用しており、
  `ctx.attacker.rank["spa"]` を確定で2段階下げる処理は仕様・実装と整合している。
  既存の `tests/test_lethal.py::test_リーフストーム_とくこうダウン` で検証済みのため修正不要。

## テスト
- `tests/moves_attack/test_move_yarawa.py` に
  `test_リーフストーム_とくこう2段階低下が発動する` を新規追加（通常の技実行フローで
  自分のとくこうランクが2段階下がることを確認）。
- `tests/test_lethal.py::test_リーフストーム_とくこうダウン`、
  `tests/items/test_item_ka.py::test_クリアチャーム_自分の技の低下は防げない`、
  `tests/items/test_item_sa.py`（しろいハーブとの相互作用）は既存のまま変更なし。
- `python -m pytest tests/ -v` で全4026件パス（1件skip）を確認。
  ログは `.loop/test_logs/リーフストーム.log` に保存。
