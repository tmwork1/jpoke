# かなしばり レビュー結果

## 仕様書

- 新規作成: `docs/spec/moves/かなしばり.md`
  - 基本情報（タイプ/分類/PP/命中率/範囲/優先度/直接攻撃/まもる/マジックコート）を記載
  - 失敗条件（未行動・わるあがき・すでにかなしばり状態）を記載
  - イベントフロー（`ON_BEFORE_APPLY_MOVE` → `ON_STATUS_HIT`）を記載
- 修正: `docs/spec/volatiles/かなしばり.md`
  - 「本実装での対象外（スコープ外）」に記載されていた のろわれボディ特性・メンタルハーブ・
    アロマベール特性の3項目が実際にはすでに実装済みだったため、「関連する特性・アイテムとの
    相互作用」セクションとして実装済みの内容に書き換えた

## 実装計画書

- 新規作成（正確には既存だが実質書き直し）: `docs/plan/moves/かなしばり.md`
  - 「未実装」と記載されていた `handlers/move_status.py` の実装状況を実装済みに更新
  - コード例の関数名を実装と一致するよう修正
    （`かなしばり_apply_volatile_to_defender` → `かなしばり_apply`）
  - データ登録先を実際の `src/jpoke/data/moves/move_ka.py` に修正（`data/move.py` は統合モジュール）
  - `かなしばり_can_apply` の登録イベントを `ON_STATUS_HIT` から `ON_BEFORE_APPLY_MOVE` に修正
    （下記「実装」の修正内容を参照）
- 修正: `docs/plan/volatiles/かなしばり.md`
  - 「未実装事項（将来対応）」に記載されていた のろわれボディ・メンタルハーブ・アロマベールの
    3項目が実装済みだったため、「なし」に修正

## 実装（handlers/ / data/）

- 修正: `src/jpoke/data/moves/move_ka.py`
  - `かなしばり_can_apply` と `かなしばり_apply` が両方とも `Event.ON_STATUS_HIT` に
    登録されていたバグを修正。`docs/spec/turn.md` の `Event.ON_APPLY_MOVE`
    （実装上の `Event.ON_BEFORE_APPLY_MOVE`）に「かなしばり: 失敗条件」が明記されており、
    かつ既存の同種ハンドラ（`アンコール_can_apply`、`いばる_can_apply`、`うらみ_can_apply`）が
    すべて「失敗条件チェックを `ON_BEFORE_APPLY_MOVE`、効果適用を `ON_STATUS_HIT`」の
    2イベント構成を採用していることを確認し、かなしばりも同一パターンに揃えた。
    （`かなしばり_can_apply` を `ON_BEFORE_APPLY_MOVE` に、`かなしばり_apply` は
    `ON_STATUS_HIT` のまま維持）
  - `handlers/move_status.py`・`handlers/volatile.py` の実装内容自体には誤りなし
    （`かなしばり_can_apply` / `かなしばり_apply` / `かなしばり_modify_command_options` /
    `かなしばり_try_action` / `かなしばり_tick_volatile` はいずれも仕様どおり）

## リーサル計算

- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`。変化技でダメージを与えないため）

## テスト

- 変更なし（`tests/moves_status/test_move_ka.py` の既存5件で
  失敗条件（未行動・わるあがき・すでにかなしばり状態）と成功時の付与・行動ブロックを
  十分カバーしていることを確認した）
- `ON_BEFORE_APPLY_MOVE` へのイベント登録変更後も全テストが通過することを確認
  （`python -m pytest tests/ -v` で 4127 passed, 1 skipped）
