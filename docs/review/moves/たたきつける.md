# たたきつける レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/たたきつける.md`）。一次情報（`docs/wiki/moves/たたきつける.html`）より、ノーマルタイプ・物理・威力80・命中75・PP20・接触あり・追加効果なしと確認。

## 実装計画書
- 新規作成（`docs/plan/moves/たたきつける.md`）。追加効果を持たないため専用ハンドラは不要と結論づけ、`docs/plan/moves/いわおとし.md` の形式に倣って記載。

## 実装（handlers/ / data/）
- 変更なし。既存の `data/move.py` の `"たたきつける"` エントリ（type="ノーマル", category="physical", pp=20, power=80, accuracy=75, flags={"contact"}, handlers={}）は仕様書・一次情報と完全に一致していた。

## リーサル計算
- 対象外（追加効果のない通常攻撃技のため `handlers/lethal.py` に専用ハンドラは不要。`docs/progress/move.md` の該当列は既に n/a）。

## テスト
- `tests/moves_attack/test_move_ta.py` に `test_たたきつける_相手にダメージを与える` を追加（テストが存在しなかったため）。`test_たいあたり_相手にダメージを与える` と同様の単純ダメージ確認テスト。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ta.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新。
- `python -m pytest tests/ -v` で全3421件パス（1件skip）を確認。
