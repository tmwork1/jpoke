# チャージビーム レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/チャージビーム.md`）。一次情報（ポケモンWiki）をもとに、
  基本情報（でんき/特殊/威力50/命中90/PP12）・追加効果（70%で自分のとくこう+1）・
  判定フラグ・リーサル計算方針を記載した。
  PPは一次情報のGen9値10ではなく、`docs/champions/move_list.txt` 基準の12を採用した。

## 実装計画書
- 新規作成（`docs/plan/moves/チャージビーム.md`）。既存実装のレビュー結果・修正内容・
  リーサルハンドラの新規実装方針・テスト計画を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `チャージビーム` エントリを修正した。
  - `pp=10` → `pp=12`（`docs/champions/move_list.txt` の基準値に修正。Gen9値からの移行漏れ）
  - `flags={"secondary_effect"}` を追加（追加効果技であり、ちからずくとの相互作用に必要）
  - `lethal_handlers={LethalEvent.ON_HIT: LethalHandler(l.チャージビーム_boost_spa)}` を追加
- `src/jpoke/handlers/move_attack.py` の `チャージビーム_boost_spa_C`（`ON_HIT`、
  `modify_attacker_stats` で70%の確率で自分の`spa`を+1）はロジック上問題なく変更なし。

## リーサル計算
- 新規実装。`src/jpoke/handlers/lethal.py` に `チャージビーム_boost_spa` を追加した
  （`しんぴのちから_boost_spa` / `フレアソング_boost_spa` / `ほのおのまい_boost_spa` と同一パターン。
  `LethalEvent.ON_HIT`, subject="attacker", `ctx.move_secondary` が真のとき
  `ctx.attacker.rank["spa"]` を1段階上げる）。
  `docs/progress/move.md` の「リーサル実装: n/a」は誤りだったため x に修正した。

## テスト
- `tests/moves_attack/test_move_ta.py` に3件追加。
  - `test_チャージビーム_secondary_effectフラグを持つ`
  - `test_チャージビーム_とくこう1段階上昇が発動する`
  - `test_チャージビーム_ちからずくで威力が上がり追加効果が発動しない`
- `tests/test_lethal.py` に2件追加。
  - `test_チャージビーム_とくこうアップ_secondary有り`
  - `test_チャージビーム_とくこうアップ_secondary無し`
- `python -m pytest tests/ -v` で全3877件（3876 passed, 1 skipped）が成功することを確認した。
  ログは `.loop/test_logs/チャージビーム.log` に保存。
