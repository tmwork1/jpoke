# ひょうざんおろし レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ひょうざんおろし.md`）。
- 一次情報（`docs/wiki/moves/ひょうざんおろし.html`）をもとに、基本情報（こおり / 物理 /
  命中85 / PP10 / 対象1体選択 / 優先度0 / 直接攻撃なし）と、威力の世代差
  （SVまでは100、Championsでは120。インフォボックスに明記）、追加効果（30%でひるみ）、
  判定フラグ（急所○・命中判定○・追加効果○・まもる○・おうじゃのしるし×・マジックコート×）を
  まとめた。

## 実装計画書
- 新規作成（`docs/plan/moves/ひょうざんおろし.md`）。
- 既存の共通ひるみ付与ハンドラ（`apply_volatile_to_defender`）を用いた定型実装であることと、
  Championsでの威力変更（100→120）が実装に未反映だった点を明記した。
  `docs/champions/move_list.txt` のPP列（12）は他技（であいがしら・つららおとし・いわなだれ）の
  既存レビューで確認済みのとおり信頼できない値であるため、PPは一次情報どおり10を維持する
  根拠も記載した。

## 実装（handlers/ / data/）
- 誤りを修正した。
  - `src/jpoke/data/moves/move_ha.py`: `ひょうざんおろし` の `power` を `100` → `120` に修正
    （Championsでの威力変更が未反映だった。一次情報のインフォボックスと
    `docs/champions/move_list.txt` の該当行の両方で威力120が確認できる）。
  - `python scripts/sort_data/sort_moves.py` を実行済み（並び替え差分なし、値変更のみ反映）。
  - `src/jpoke/handlers/move_attack.py` の `ひょうざんおろし_apply_flinch`
    （`apply_volatile_to_defender(..., volatile="ひるみ", chance=0.3)`）は変更なし、仕様と一致。
  - `src/jpoke/handlers/item.py` の `_INNATE_FLINCH_MOVES` に `ひょうざんおろし` が含まれており、
    おうじゃのしるし・するどいキバの追加ひるみ効果が重複しない仕様も正しく実装済み。

## リーサル計算
- 対象外。`docs/progress/move.md` のリーサル実装・リーサルテスト列がともに `n/a`。
  単発威力技で追加効果がひるみ（HPに影響しない）のため、専用のリーサル計算ハンドラは不要。

## テスト
- `tests/moves_attack/test_move_ha.py` に `test_ひょうざんおろし_威力は120`
  （`damage_calculator.final_power == 120` を確認）を追加した。既存の
  `test_ひょうざんおろし_ひるみが発動する` は変更なし。
- `tests/moves_status/test_move_ha.py`（変化技専用のテストファイル）に
  `test_ひょうざんおろし_ひるみが発動する` という重複テストが誤って混入していたため削除した
  （ひょうざんおろしは物理攻撃技であり `tests/moves_attack/` 側に同等のテストが既に存在する）。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ha.py tests/moves_status/test_move_ha.py`、
  `python scripts/generate_test_list.py` を実行し、
  `python -m pytest tests/ -v` で全3579件パス（1件skip）を確認した
  （結果は `.loop/test_logs/ひょうざんおろし.log` に保存）。

## 進捗表の修正
- `docs/progress/move.md` の該当行: 仕様書・計画書列を `-` → `x`、レビュー列を `-` → `✓` に更新した。
  PP列の誤り（`12`）を一次情報に基づき `10` に修正し、効果列を
  「追加効果として、30%の確率で相手をひるませる。」に補完した（威力列の `120` は
  Champions基準として正しいため変更なし）。
