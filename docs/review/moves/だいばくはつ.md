# だいばくはつ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/だいばくはつ.md`）。一次情報（`docs/wiki/moves/だいばくはつ.html`）と
  既存の `docs/spec/moves/じばく.md`（威力以外は同一効果）を参照し、威力250のみ差し替えた内容で作成。

## 実装計画書
- 新規作成（`docs/plan/moves/だいばくはつ.md`）。`docs/plan/moves/自爆技.md`（じばく・だいばくはつ・
  ミストバースト共通の暫定計画）と `docs/plan/moves/じばく.md` を参照し、だいばくはつ単体のレビュー用に
  分離した内容で作成。priority根拠は `docs/spec/turn.md` の `Event.ON_PAY_HP`「じばく等」の記載を根拠とした。

## 実装（handlers/ / data/）
- 変更なし。`data/move.py` の `だいばくはつ`（type=ノーマル, category=physical, power=250, accuracy=100,
  pp=5, flags={"explosion"}）と `handlers/move_attack.py` の `だいばくはつ_pay_hp`（`ON_PAY_HP` で
  現在HPを全消費）は仕様書・計画書の内容と完全に一致しており、`じばく_pay_hp` と同一パターン。

## リーサル計算
- 対象外（`docs/progress/move.md` の だいばくはつ 行はリーサル実装・リーサルテストともに `n/a`）。

## テスト
- 変更なし。`tests/moves_attack/test_move_ta.py` に既存の3テスト
  （`test_だいばくはつ_HP消費後も攻撃が相手に届く`、`test_だいばくはつ_しめりけ持ちには技が失敗する`、
  `test_だいばくはつ_使用後に攻撃者がひんしになる`）があり、じばくと同水準のカバレッジを満たしている。
  `tests/test_item.py` の `test_レッドカード_自滅技でひんしになった攻撃者には発動しない` でも
  自滅技としての相互作用が確認済み。追加・修正の必要なし。
