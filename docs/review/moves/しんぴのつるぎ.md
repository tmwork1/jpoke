# しんぴのつるぎ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/しんぴのつるぎ.md`）。
  - タイプ: かくとう / 分類: 特殊 / 威力: 85 / 命中: 100 / PP: 10 / 対象: 相手1体 / 優先度: 0 / 接触なし。
  - 効果: 相手の『とくぼう』ではなく『ぼうぎょ』でダメージ計算をする（分類は特殊のまま、
    サイコショック・サイコブレイクと同種の「物理判定の特殊技」）。追加効果なし。
  - 判定フラグ（急所・命中・まもる・おうじゃのしるし・マジックコート・よこどり・オウムがえし）を一次情報から記載。

## 実装計画書
- 新規作成（`docs/plan/moves/しんぴのつるぎ.md`）。
  - `Query.deals_physical_damage()` が `move.category == "physical"` または
    `move.has_flag("physical_damage")` の場合に防御側の『ぼうぎょ』を参照する既存の仕組みを利用する方針を明記。
  - 新規ハンドラは不要で、`flags` に `"physical_damage"` を追加するだけで済むことを記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/move.py` の `"しんぴのつるぎ"` エントリを修正した。
  - **バグ修正**: `flags={"slash"}` のみで `"physical_damage"` フラグが欠落していたため、
    分類どおり『とくぼう』でダメージ計算されてしまっていた。`flags={"slash", "physical_damage"}` に修正し、
    `Query.deals_physical_damage()` 経由で防御側の『ぼうぎょ』を参照するようにした。
  - `python scripts/sort_data/sort_moves.py` を実行済み。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）。

## テスト
- `tests/moves_attack/test_move_sa.py` に以下を追加した。
  - `test_しんぴのつるぎ_相手のとくぼうランク変化の影響を受けない`
    （相手の『とくぼう』ランクを+6しても与ダメージが変わらないことを確認）
  - `test_しんぴのつるぎ_相手のぼうぎょランク変化の影響を受ける`
    （相手の『ぼうぎょ』ランクを+6すると与ダメージが減ることを確認）
  - `python scripts/sort_tests.py tests/moves_attack/test_move_sa.py` でソート済み。
  - `python scripts/generate_test_list.py` で `docs/tests/moves_attack.md` を更新済み。
  - `python -m pytest tests/ -v` で全件（3388 passed, 1 skipped）を確認済み
    （ログ: `.loop/test_logs/しんぴのつるぎ.log`）。

## 備考
- 調査の過程で、同種の「物理判定の特殊技」であるサイコショック・サイコブレイクにも
  `"physical_damage"` フラグが欠落している同様のバグが存在することを確認した。
  ただし両技は今回のレビュー対象外のため、修正は行っていない
  （それぞれの回のレビュー対象になった際に別途修正が必要）。
