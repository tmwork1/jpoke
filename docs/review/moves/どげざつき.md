# どげざつき レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/どげざつき.md`）。一次情報（`docs/wiki/moves/どげざつき.html`）に基づき、
  タイプ・分類・威力・命中（必中）・PP・対象・優先度・接触・判定フラグ（急所あり、まもるで防がれる、
  おうじゃのしるし発動、マジックコート対象外）を記載。

## 実装計画書
- 新規作成（`docs/plan/moves/どげざつき.md`）。既存の `ドゲザン` `あてみなげ` と同様、
  `accuracy=None` で必中を表現し追加ハンドラ不要である旨を明記。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `MOVES["どげざつき"]` は既に
  `type="あく", category="physical", pp=10, power=80, flags={"contact"}, handlers={}` で登録済みで、
  `accuracy` 未指定（デフォルト `None`）により必中挙動になっており仕様書と一致することを確認した。
  データ順序も `どくびし` と `ドゲザン` の間に正しく配置されている。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装・リーサルテスト列は `n/a`。追加効果を持たない技のため）。

## テスト
- `tests/moves_attack/test_move_ta.py` に以下を追加。
  - `test_どげざつき_威力命中率PPが仕様通り`: タイプ・分類・威力・命中率(None)・PPを確認。
  - `test_どげざつき_相手の回避率が高くても必ず命中する`: 相手の回避率ランクを最大まで上げても命中することを確認。
  - `python scripts/sort_tests.py tests/moves_attack/test_move_ta.py` でソート、
    `python scripts/generate_test_list.py` で一覧を更新、`python -m pytest tests/ -v` で全件パス
    （3496 passed, 1 skipped）を確認。ログは `.loop/test_logs/どげざつき.log` に保存。
