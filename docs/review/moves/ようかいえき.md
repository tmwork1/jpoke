# ようかいえき レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ようかいえき.md`）。一次情報（`docs/wiki/moves/ようかいえき.html`）に基づき、
  タイプ・分類・威力・命中率・PP・対象・優先度・接触判定・追加効果（10%とくぼう-1、第四世代以降の仕様）・
  判定フラグ（まもる／おうじゃのしるし／マジックコート／よこどり／オウムがえし／ちからずく）・
  Zワザ/ダイマックス威力を記載。

## 実装計画書
- 新規作成（`docs/plan/moves/ようかいえき.md`）。既存の `handlers/move_attack.py` の
  `ようかいえき_lower_defender_spd`（`ON_DAMAGE_HIT`、`modify_defender_stats(stats={"spd": -1}, chance=0.1)`）と
  `data/moves/move_ya.py` の登録内容を仕様と照合し、priority 根拠（既存の同種ハンドラ ON_DAMAGE_HIT
  パターンを踏襲）を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/move_attack.py` の `ようかいえき_lower_defender_spd` および
  `src/jpoke/data/moves/move_ya.py` の `"ようかいえき"` エントリ（type=どく, category=special, pp=30,
  power=40, accuracy=100, flags={"secondary_effect"}）は仕様書・計画書と一致しており、修正不要だった。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列は n/a）。確率発動の相手能力ランク低下効果であり、
  ダメージ量自体を変化させないため、リーサル計算（`core/lethal.py`）の対象としないことを確認した。

## テスト
- 既存のテストが存在しなかったため、`tests/moves_attack/test_move_yarawa.py` に以下を新規追加した:
  - `test_ようかいえき_とくぼう1段階低下が発動しない`（`secondary_chance=0.0` で追加効果が発動しないことを確認）
  - `test_ようかいえき_とくぼう1段階低下が発動する`（`secondary_chance=1.0` でとくぼうが1段階下がることを確認）
  - `test_ようかいえき_相手にダメージを与える`（追加効果なしでも通常ダメージが発生することを確認）
- `python scripts/sort_tests.py tests/moves_attack/test_move_yarawa.py` でソート、
  `python scripts/generate_test_list.py` で一覧更新、`pytest tests/` で全件パス（3623 passed, 1 skipped）を確認した。

## 進捗表の修正
- `docs/progress/move.md` のようかいえき行: 仕様書/計画書 `-` → `x`、テスト `-` → `x`、レビュー `-` → `✓`。
