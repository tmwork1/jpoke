# きょじゅうだん レビュー結果

## 仕様書
- 新規作成（docs/spec/moves/きょじゅうだん.md）。docs/wiki/moves/きょじゅうだん.html を一次情報として、
  タイプ・分類・威力100・命中100・PP5・対象1体・優先度0・接触・追加効果なしを記載。
  「相手がダイマックスしているとダメージ2倍」の効果は本プロジェクトがダイマックス非対応のため対象外と明記。
  一次情報に明記された「ものまねでコピー不可」「まねっこは失敗する」「ゆびをふるで選ばれない
  （ゆびをふる自体が本プロジェクト未実装のため対象外）」も記載。

## 実装計画書
- 新規作成（docs/plan/moves/きょじゅうだん.md）。追加効果を持たないため専用ハンドラは不要であることを明記し、
  既存の `data/move.py` エントリに `non_copycat` フラグを追加する方針とその根拠（`まねっこ_can_use` が
  `non_copycat` フラグを見て失敗判定する既存機構をそのまま利用できる）を記載した。

## 実装（handlers/ / data/）
- 修正。`data/move.py` の `"きょじゅうだん"` エントリの `flags` に `non_copycat` を追加した
  （type="はがね", category="physical", pp=5, power=100, accuracy=100 は仕様と一致しており変更なし）。
  一次情報に「この技に対するまねっこは失敗する」と明記されているが、実装済みの `まねっこ_can_use` は
  `non_copycat` フラグの有無のみで判定するため、フラグ追加だけで挙動を修正できた。
  `src/jpoke/types/literals.py` の `non_copycat` フラグのコメントも「スターモービル専用アクセル技、
  きょじゅうだん等」に更新し、この技にも適用されることを明記した。
  `python scripts/sort_data/sort_moves.py` を実行し、辞書順序を確認（`きょじゅうだん` の位置は変更なし）。

## リーサル計算
- 対象外（docs/progress/move.md でリーサル実装列が n/a）。

## テスト
- 追加。tests/moves_attack/test_move_ka.py に
  `test_きょじゅうだん_相手にダメージを与える`（追加効果なしの物理はがね技として、
  ダメージが発生することを確認する基本テスト）を新規追加。
- 追加。tests/moves_status/test_move_ma.py に
  `test_まねっこ_きょじゅうだんはコピーできない`（きょじゅうだんが直前に使われた技の場合、
  まねっこが失敗することを確認するテスト）を新規追加。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ka.py tests/moves_status/test_move_ma.py`
  で並び替え、`python scripts/generate_test_list.py` でテスト一覧を更新、
  `python -m pytest tests/ -v` で全3313件（1件skip）が成功することを確認
  （.loop/test_logs/きょじゅうだん.log に保存）。
