# まとわりつく レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/まとわりつく.md`）。一次情報（`docs/wiki/moves/まとわりつく.html`）を基に、
  基本情報（むし・特殊・威力20・命中100・PP20・接触あり）、バインド付与効果、判定フラグ、
  バインド技グループの備考を記載。むしタイプ技であるためノーマル技のようなゴーストタイプ無効化は
  発生しない旨を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/まとわりつく.md`）。`まきつく` の計画書を参考に、既存のバインド系
  共通ハンドラ（`apply_bind_to_defender` / `_apply_bind`）を流用する方針を記載。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ma.py` にてすでに正しく実装済み（タイプ・威力・命中率・PP・
  `ON_DAMAGE_HIT` ハンドラ・リーサルハンドラすべて仕様と一致）。

## リーサル計算
- 変更なし。`handlers/lethal.py` の `_apply_bind` を既存流用しており、仕様・実装と一致。
  ただし `docs/progress/move.md` 上は「リーサルテスト」列が既に x だったにもかかわらず
  `tests/test_lethal.py` に該当テストが存在しなかったため、テストのみ追加した（下記参照）。

## テスト
- `tests/moves_attack/test_move_ma.py` に以下を追加:
  - `test_まとわりつく_ダメージを与える`
  - `test_まとわりつく_バインド中はターン終了時にダメージを受ける`
  - `test_まとわりつく_命中後にバインド状態になる`
- `tests/test_lethal.py` に `test_まとわりつく_バインド付与` を追加（`まきつく` の既存リーサル
  テストと同パターン、技命中でのバインド付与とバインド事前付与の結果一致を確認）。
