# おたけび レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/おたけび.md`）。`docs/wiki/moves/おたけび.html`（ポケモンWiki）を
  一次情報として、タイプ／分類／命中／PP・効果・判定表（まもる:○、マジックコート:○、
  よこどり:対象外、オウムがえし:○）を記載した。
- PP は `docs/champions/move_list.txt` の値（20）を採用し、本家Gen6以降の基準値（30）との差異を
  注記した。
- 「かいりきバサミ」「まけんき」「かちき」などランク低下無効化・特性相互作用の技仕様欄の記述を、
  実装の汎用ロジック（`battle.modify_stats`）に委譲されることを明記したうえで仕様書に残した。

## 実装計画書
- 既存の `docs/plan/moves/おたけび.md` を仕様書と突き合わせて修正した。
  - 誤り: PP を「30」としていたが、`docs/champions/move_list.txt` 準拠の正しい値「20」に修正。
  - 誤り: ランク変化の `stats` キーを `{"A": -1, "C": -1}` としていたが、実装・型定義
    (`types/literals.py` の `Stat` リテラル) に合わせて `{"atk": -1, "spa": -1}` に修正。
  - 「現状の実装状況」節が「ハンドラ未実装」と記載されていたが、実際には実装済みだったため、
    「PP修正のみ必要」という実態に合わせて記述を更新した。
  - `なみだめ_modify_defender_stats` を「A-1, C-1 の両方を下げる」と誤って参照していた箇所を、
    現行実装の実態（spa -1 のみ）に合わせて参照先を `なきごえ`・`なかよくする` に差し替えた
    （`なみだめ` 自体の実装是非は本タスクの対象外のため変更していない）。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py`: `おたけび` の `pp` を `30` から `20` に修正した
  （`docs/champions/move_list.txt` 準拠。移行漏れのバグ）。
- `src/jpoke/handlers/move_status.py` の `おたけび_modify_defender_stats` は
  `modify_defender_stats(battle, ctx, value, stats={"atk": -1, "spa": -1})` を呼ぶだけの
  正しい実装だったため、変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- `tests/moves_status/test_move_a.py`: 既存の `test_おたけび_こうげきとくこう1段階ずつ下がる` は
  そのまま維持した。
- 新規追加: `test_おたけび_みがわりを貫通してこうげきとくこうが下がる`
  （sound フラグによりみがわり状態の相手にも効果が発動することを確認。既存の
  `test_いやなおと_みがわりを貫通してぼうぎょが下がる` と同じパターンで作成）。
- `python -m pytest tests/ -v` で 4113 passed, 1 skipped を確認
  （結果は `.loop/test_logs/おたけび.log` に保存）。
