# おきみやげ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/おきみやげ.md`）。ポケモンWiki一次情報をもとに、基本情報・効果・
  第五世代以降の発動タイミング仕様（命中/まもる/みがわりで防がれた場合はひんしにならない、
  ランク最低やクリアボディ等で無効化された場合でも使用者はひんしになる等）、判定表、
  Zワザ付加効果（対象外の明記）、チャンピオンズ向け簡略化を記載した。

## 実装計画書
- 既存の計画書には以下の誤りがあったため修正した。
  - PP を `10` としていたが、`src/jpoke/data/ps-champ-ja/moves.json` の正しい値は `12`。
  - ハンドラのコンテキスト型を `EventContext` としていたが、実際は `handlers/move.py` の
    `MoveHandler` 基盤が `AttackContext` を渡すため誤り。関数名・実装例も実際のコード
    （`おきみやげ_apply`、`battle.faint` を使用）に合わせて更新した。
  - 「現状の実装状況」表が「未実装」のままだったため実装済みに更新した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `おきみやげ` エントリの `pp` が `10`（Gen9本家基準の
  移行漏れバグ）になっていたため、`src/jpoke/data/ps-champ-ja/moves.json` の正しい値 `12` に修正した。
- `handlers/move_status.py` の `おきみやげ_apply`（`battle.faint` → `modify_defender_stats`、
  戻り値は常に `value` を維持）は仕様どおりであり変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`。使用者自身がひんしになる技であり
  ダメージベースのリーサル計算対象ではないため）。

## テスト
- 既存の2件（まもるで防がれると使用者はひんしにならない／使用者がひんしになる）は仕様と一致しており変更なし。
- 追加: `test_おきみやげ_相手のランクが共に最低でも使用者はひんしになる`
  （相手のこうげき・とくこうランクが共に-6でランク低下が発生しなくても使用者はひんしになることを確認）。
- 追加: `test_おきみやげ_クリアボディでランク低下が防がれても使用者はひんしになる`
  （相手がクリアボディでランク低下を防いでも使用者はひんしになることを確認）。
- `python -m pytest tests/ -v` は全件（4114 passed, 1 skipped）で成功した。
