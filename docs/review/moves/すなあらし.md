# すなあらし レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/すなあらし.md`）。一次情報（ポケモンWiki）と
  `src/jpoke/data/ps-champ-ja/moves.json` を突き合わせ、タイプ/分類/威力/命中率/範囲/優先度/直接攻撃/
  判定（急所・命中判定・追加効果・まもる・おうじゃのしるし・マジックコート・よこどり・
  オウムがえし）を記載。PPは一次情報の10ではなく `move_list.txt` の8を採用。
  すなあらし天候自体のターン終了ダメージ・いわタイプとくぼう補正は技固有ではなく
  `data/field/weather.py` 側の一般仕様として参照する形にした。

## 実装計画書
- 新規作成（`docs/plan/moves/すなあらし.md`）。`あまごい.md` と同様の構成で、
  `ON_STATUS_HIT`（priority=100 デフォルト）で `weather_manager.apply("すなあらし", 5, ...)`
  を呼ぶ実装パターンを明記。

## 実装（handlers/ / data/）
- **PP修正**: `src/jpoke/data/moves/move_sa.py` の `すなあらし` の `pp` が `10`
  （Gen9本家基準）になっていたバグを `8`（champions/move_list.txt準拠）に修正した。
- `handlers/move_status.py` の `すなあらし_activate_weather`、`target="field"`、
  `flags={"wind"}` は仕様と一致しており変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行「リーサル実装」列が `n/a`。すなあらし技自体は
  直接HPを変動させず、天候によるターン終了ダメージは `data/field/weather.py` 側の
  `すなあらし_damage`（`handlers/lethal.py`）が別途担当しているため、本技のレビュー対象外）。

## テスト
- `tests/moves_status/test_move_sa.py` に
  `test_すなあらし_天気がすなあらしになる`（使用後に天候が「すなあらし」・5ターンになることを確認）
  を追加。
- `tests/moves_status/test_move__grouped.py` の天候始動技グループテストに `すなあらし` を追加:
  - `test_天候始動技_すでに同じ天候なら失敗` に `("すなあらし", "すなあらし")` を追加
  - `test_天候始動技_強天候で失敗` に `("すなあらし", "おおひでり")` を追加
- `python -m pytest tests/ -v` は全件パス（4191 passed, 1 skipped）。
  結果は `.loop/test_logs/すなあらし.log` に保存。
