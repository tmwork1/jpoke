# アイアンテール レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/アイアンテール.md`）。
  - `docs/wiki/moves/アイアンテール.html`（一次情報）と `src/jpoke/data/ps-champ-ja/moves.json` を突き合わせ、
    基本情報（タイプ: はがね / 分類: 物理 / 威力100 / 命中75 / PP16〈Champions。SVまでは15〉 /
    対象: 相手1体 / 直接攻撃あり）と、追加効果（30%の確率で相手の『ぼうぎょ』ランクを1段階下げる）、
    判定フラグ（まもる・オウムがえし対象、マジックコート・よこどり対象外、ちからずく対象技）を記載した。
  - `docs/champions/changes_from_sv.md` に「技のPPが8・12・16・20の4段階に統一」と明記されており、
    SVでのPP15がChampionsでは16になる根拠として引用した。

## 実装計画書
- 新規作成（`docs/plan/moves/アイアンテール.md`）。
  - 既存実装（`handlers/move_attack.py` の `アイアンテール_lower_defender_def` /
    `Event.ON_DAMAGE_HIT`）は `Gのちから_lower_defender_def` / `ほのおのムチ_lower_defender_def`
    と同一パターンで問題なし、という確認結果と、`data/moves/move_a.py` の `pp` の誤りの
    修正内容を記載した。

## 実装（handlers/ / data/）
- **不備を発見し修正した**: `src/jpoke/data/moves/move_a.py` の `アイアンテール` の
  `pp` が `15`（SVまでの値）になっていたが、`src/jpoke/data/ps-champ-ja/moves.json` /
  `docs/champions/changes_from_sv.md`（全技PPが8・12・16・20の4段階に統一）によれば
  Championsでは `16` が正しい値のため `pp=16` に修正した。
- `src/jpoke/handlers/move_attack.py` の `アイアンテール_lower_defender_def`
  （`modify_defender_stats(battle, ctx, value, stats={"def": -1}, chance=0.3)`）、
  `flags={"contact", "secondary_effect"}` は仕様と一致しており変更なし。

## リーサル計算
- 対象外。`docs/progress/move.md` の該当行が「リーサル実装: n/a」「リーサルテスト: n/a」
  のため、今回のレビューではスキップした。

## テスト
- `tests/moves_attack/test_move_a.py` にアイアンテールのテストが1件も存在しなかったため、
  以下を新規追加した。
  - `test_アイアンテール_ぼうぎょ1段階低下が発動しない`（`secondary_chance=0.0`）
  - `test_アイアンテール_ぼうぎょ1段階低下が発動する`（`secondary_chance=1.0`）
- 今回の `pp` 修正（15→16）はダメージ量やPP残数の具体値をハードコードした既存テストに
  影響しないことを確認した。
- `python -m pytest tests/ -v` を実行し、3644 passed, 1 skipped で全件成功。
  結果は `.loop/test_logs/アイアンテール.log` に保存した。
