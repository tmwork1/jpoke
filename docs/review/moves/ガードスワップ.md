# ガードスワップ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ガードスワップ.md`）。一次情報（`docs/wiki/moves/ガードスワップ.html`）を基に、基本情報表・効果・判定（まもる／マジックコート／みがわり）・イベントフローを記載した。

## 実装計画書
- 既存の `docs/plan/moves/ガードスワップ.md` を一次情報・修正後の実装と照合し、以下を修正した。
  - PP を 10 → 12 に修正（`docs/champions/move_list.txt` 準拠。Gen9本家基準の10は移行漏れのバグ）。
  - 命中率を「100」→「必中（`accuracy=None`）」に修正。一次情報の `wgCategories` に「必中わざ」が含まれ、`accuracy=100` では回避率上昇の影響を受けてしまう問題を指摘。
  - マジックコートについて「×」（跳ね返されない）である旨と、そのために `flags={"unreflectable"}` が必要である旨を追記（デフォルトでは対象が `foe` の変化技は跳ね返り対象になってしまうため）。
  - みがわり貫通について一次情報に明記があることを反映し、`flags={"bypass_substitute"}` が必要である旨を確定（従来「wiki不在のため要確認」としていた記述を解消）。
  - ランクキー表記を旧称「B」「D」から実装のキー名 `"def"`／`"spd"` に修正。
  - 実装状況・注意点・エッジケースの記述を実装済み状態に更新。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_status.py` の `ガードスワップ_swap_ranks` はパワースワップ・ハートスワップと同一パターンで実装済みであり、変更なし。
- `src/jpoke/data/moves/move_ka.py` の `"ガードスワップ"` エントリを以下の通り修正した。
  - `pp=10` → `pp=12`（`docs/champions/move_list.txt` 準拠）。
  - `accuracy=100` → `accuracy=None`（必中技。回避率ランク補正の影響を受けないようにするため）。
  - `flags={"unreflectable", "bypass_substitute"}` を追加（マジックコートで跳ね返されず、みがわりを貫通するため）。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）。

## テスト
- `tests/moves_status/test_move_ka.py` に以下のテストを追加した。
  - `test_ガードスワップ_みがわり状態の相手にも効果が発動する`: `bypass_substitute` フラグにより、みがわり状態の相手にもランク入れ替えが成立することを確認。
  - `test_ガードスワップ_マジックコートで跳ね返されない`: `unreflectable` フラグにより、マジックコート状態の相手に使用しても跳ね返されず、使用者側のランクが変化することを確認。
- 既存3テスト（AC不変・BD入れ替え・双方0で変化なし）は修正不要のため変更なし。
- `python -m pytest tests/ -v` は全件パス（4464 passed, 1 skipped）。実行結果を `.loop/test_logs/ガードスワップ.log` に保存した。
