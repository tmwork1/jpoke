# はっぱカッター レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/はっぱカッター.md`）。一次情報（docs/wiki/moves/はっぱカッター.html）と照合し、
  くさ / 物理 / 威力55 / 命中95 / PP25 / 急所ランク+1 / `slash` フラグ / 追加効果なしであることを確認した。
  原作では相手全員が対象の拡散技だが、本プロジェクトはシングルバトルのみ対象のため
  その旨を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/はっぱカッター.md`）。既存実装が仕様と一致していることを確認し、
  専用ハンドラが不要である根拠（`critical_rank` フィールドのみで急所判定ロジックが自動適用される、
  `slash` フラグによりきれあじ特性の威力補正が既存ロジックで処理される）を記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ha.py` の `"はっぱカッター"` エントリ
  （`type="くさ", category="physical", pp=25, power=55, accuracy=95, critical_rank=1,
  flags={"slash"}, handlers={}`）は仕様と完全に一致しており修正不要と判断した。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`。追加効果を持たない通常の
  物理攻撃技のため）。

## テスト
- `tests/moves_attack/test_move_ha.py` に以下を追加した:
  - `test_はっぱカッター_急所ランクが1`（乱数0で急所が発生することを確認）
  - `test_はっぱカッター_急所ランクが1_乱数大で急所なし`（乱数0.5で急所が発生しないことを確認）
- `python scripts/sort_tests.py tests/moves_attack/test_move_ha.py` で並び替え、
  `python scripts/generate_test_list.py` でテスト一覧を更新した。
- `python -m pytest tests/ -v` で全3565件パス（1件スキップ）を確認した
  （実行ログ: `.loop/test_logs/はっぱカッター.log`）。
