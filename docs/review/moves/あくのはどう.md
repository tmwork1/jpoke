# あくのはどう レビュー結果

## 仕様書
- `docs/spec/moves/` に個別の仕様書は存在しない。あくのはどうの追加効果（20%でひるみ）は、
  他の同種の追加効果技（アイアンヘッド、いわなだれ など）と同様に
  `docs/spec/moves/_move_secondary_1.md` の一覧表で管理する方針であり、一次情報（Wiki）と
  照合した結果、既に正しく記載されていた（159行目に `あくのはどう` の行としてひるみ確率0.2で記載）。
  個別ファイルを作成しないという既存の方針（3ぼんのやレビュー等）を踏襲し、新規作成は行っていない。
- 一次情報（`docs/wiki/moves/あくのはどう.html`）で確認したスペック: あくタイプ・特殊・威力80・
  命中率100・PP15・追加効果「20%の確率で相手をひるませる」・非接触技。すべて実装と一致していた。

## 実装計画書
- `docs/plan/moves/あくのはどう.md` は存在しないが、グループ計画書
  `docs/plan/moves/攻撃技_ひるみ系.md` にあくのはどうの実装内容（ON_HIT/ON_DAMAGE_HIT で
  `apply_volatile_to_defender(volatile="ひるみ", chance=0.2)` を `attacker:self` で登録）が
  記載されており、内容は現在の実装と一致していた。同種の単純な追加効果技には個別の計画書を
  作成しない慣例（3ぼんのやレビュー等）に倣い、新規作成はしていない。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `"あくのはどう"` エントリ
  （type="あく", category="special", pp=15, power=80, accuracy=100,
  flags={"pulse", "secondary_effect"}, `Event.ON_DAMAGE_HIT` に
  `ha.あくのはどう_apply_flinch` を `MoveHandler`（デフォルト subject_spec="attacker:self",
  priority=100）で登録）は仕様書・計画書と一致しており、修正不要と判断した。
- `src/jpoke/handlers/move_attack.py` の `あくのはどう_apply_flinch` は共通ヘルパー
  `apply_volatile_to_defender(battle, ctx, value, volatile="ひるみ", chance=0.2)` を
  そのまま呼び出しており、正規の状態変更 API（`battle.volatile_manager.apply`）を経由している。
  五十音順の位置も正しかった。
- `flags` に `"pulse"` が含まれており、メガランチャー特性の波動技威力1.5倍対象になっている点も
  一次情報・既存仕様と一致している。

## テスト
- 変更なし。`tests/moves_attack/test_move_a.py` の `test_あくのはどう_ひるみが発動する`
  （`secondary_chance=1.0` でひるみ付与を確認）が既に存在し、実装と一致する内容だったため
  追加・修正は行わなかった。
- `python -m pytest tests/ -v` を実行し、あくのはどう関連のテストはすべてパスすることを確認した
  （既存の他タスク由来の一時的な失敗が2件あったが、あくのはどうとは無関係のメガシンカ特性
  登録エラーであり、本タスクの対象外のため修正していない）。

## 進捗表
- `docs/progress/move.md` の あくのはどう 行を修正:
  - `PP` 列の誤記`16`を一次情報どおりの`15`に修正
  - `効果` 列の`?`を「追加効果として、20%の確率で相手をひるませる。」に修正
  - `レビュー` 列を`-`から`✓`に更新
