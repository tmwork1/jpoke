# ばくれつパンチ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ばくれつパンチ.md`）。
  `docs/wiki/moves/ばくれつパンチ.html` を一次情報として、基本情報（かくとう/物理/威力100/命中50/PP5/
  パンチ技/直接攻撃）と効果（追加効果100%でこんらん付与、てつのこぶし対象）を記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/ばくれつパンチ.md`）。
  既存実装が仕様と一致しており差分なしのため、既存実装内容の追認として記載した。

## 実装（handlers/ / data/）
- 変更なし。
  `src/jpoke/data/moves/move_ha.py` の `"ばくれつパンチ"` エントリ（type=かくとう, category=physical,
  pp=5, power=100, accuracy=50, flags={"contact","punch","secondary_effect"}）と、
  `src/jpoke/handlers/move_attack.py` の `ばくれつパンチ_apply_confusion_to_defender`
  （`apply_confusion_to_defender` を確率100%で呼び出し、こんらんを2〜5ターン付与）は
  仕様書・計画書と一致しており、修正不要と判断した。
  パンチ技の威力補正（てつのこぶし・パンチグローブ）は共通ハンドラ側で処理されるため、
  本技固有の実装は不要（既存のまま）。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`。
  こんらん付与のみでHP分布に影響しない追加効果のため専用のリーサルハンドラは不要）。

## テスト
- 変更なし。
  `tests/moves_attack/test_move_ha.py` の `test_ばくれつパンチ_こんらんが発動する`
  （100%の確率でこんらんが付与されることを確認）は仕様と一致しており、修正不要と判断した。
- `python -m pytest tests/ -v` を実行し、3574件全て成功（1件skip）を確認した
  （実行結果は `.loop/test_logs/ばくれつパンチ.log` に保存）。
  なお `tests/test_field.py` の急所依存の一部テスト（オーロラベール関連）は本タスクと無関係な
  既知のフレーキーテストであり、再実行で成功することを確認した。

## その他
- `docs/progress/move.md` の該当行を修正した:
  - 仕様書・計画書列を `-` → `x`、レビュー列を `-` → `✓` に更新。
  - PP列を `8`（誤り）→ `5`（実装・一次情報と一致）に修正。
  - 効果列のこんらん継続ターン数を `1～4ターン`（誤り）→ `2～5ターン`
    （`docs/spec/volatiles/こんらん.md` の共通仕様と一致）に修正。
