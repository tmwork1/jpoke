# おいかぜ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/おいかぜ.md`）
- 一次情報（Wiki）とともに `src/jpoke/data/ps-champ-ja/moves.json`（PP=16）、既存の `docs/spec/fields/おいかぜ.md`
  （フィールド効果本体の詳細仕様）を突き合わせて作成した

## 実装計画書
- 既存の `docs/plan/moves/おいかぜ.md` を修正した
  - PP を 15 → 16 に修正（`src/jpoke/data/ps-champ-ja/moves.json` 準拠。SVからの技PP変更で全技8/12/16/20の4段階に統一されているため）
  - コード例の `category="変化"` → `category="status"`、`labels=["wind"]` → `flags={"wind"}` に修正（実際の `MoveData` のフィールド名・値表記に合わせた）
  - ハンドラのコンテキスト型を `EventContext` → `AttackContext` に修正（`ctx.attacker` を参照する攻撃フェーズのハンドラのため）
  - 「現状の実装状況」節を実装済みの状態に更新（実装はすでに完了していた）

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `おいかぜ` エントリの `pp` を 15 → 16 に修正
  （`src/jpoke/data/ps-champ-ja/moves.json` の値が正であり、Gen9本家の15が移行漏れとして残っていた）
- `src/jpoke/handlers/move_status.py` の `おいかぜ_set_side_field` はロジック・`subject_spec`（`attacker:self` デフォルト）・
  五十音順の配置いずれも仕様書・計画書と整合しており、修正なし

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`）

## テスト
- 既存の `tests/moves_status/test_move_a.py` の
  `test_おいかぜ_すでにおいかぜなら失敗` / `test_おいかぜ_自陣営に4ターン設置される` は
  仕様書・実装と整合しており、修正なし
- `python -m pytest tests/ -v` で全4112件パス（1件skip）を確認
  （途中1回だけ既存の無関係な乱数依存テスト `test_むしくい_ねんちゃく持ちをひんしにさせた場合は奪える` が
  たまたま失敗したが、再実行で再現せず、おいかぜの変更とは無関係のフレーキーテストと判断）
