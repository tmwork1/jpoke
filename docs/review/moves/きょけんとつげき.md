# きょけんとつげき レビュー結果

## 仕様書
- `docs/spec/moves/きょけんとつげき.md` は既に作成済み（前回作業分）。
- 一次情報（`docs/wiki/moves/きょけんとつげき.html`）と照合し、内容に誤りがないことを確認した。
  基本情報（ドラゴン・物理・威力120・命中100・PP5・直接攻撃）、効果（次の自分の行動開始まで
  相手からの技が必中・ダメージ2倍）、非発動条件（まもる・フェアリー無効化・命中失敗時は
  デメリット不発）、解除タイミング（次の行動開始時、行動可否・状態異常・こんらん自傷より前に解除）、
  判定フラグ（急所○・命中判定○・追加効果×・まもる○・おうじゃのしるし○・マジックコート×）は
  一次情報と一致しており、修正は行っていない。

## 実装計画書
- `docs/plan/moves/きょけんとつげき.md` は既に作成済み（前回作業分）。
- イベント選定根拠（`ON_HIT` での状態付与、`ON_MODIFY_ACCURACY` での必中化、
  `ON_CALC_DAMAGE_MODIFIER` での2倍化、`ON_TRY_ACTION`（priority=10、`みちづれ_remove`と同一）
  での解除）を実装・既存の類似ハンドラ（`ちいさくなる_guaranteed_hit`、`リフレクター_reduce_damage`、
  `みちづれ_remove`）と突き合わせ、整合していることを確認した。修正は行っていない。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_attack.py` の `きょけんとつげき_apply_self_volatile`、
  `src/jpoke/handlers/volatile.py` の `きょけんとつげき_guaranteed_hit` /
  `きょけんとつげき_double_damage` / `きょけんとつげき_remove`、
  `src/jpoke/data/move.py` / `src/jpoke/data/volatile.py` のエントリは、いずれも計画書通りに
  実装済みで、五十音順の並びも正しいことを確認した。
- `ON_HIT` はまもる等でのブロック（`ON_TRY_MOVE_1`）・タイプ無効化（`ON_TRY_MOVE_2`）・
  命中失敗より後に発火するため、非発動条件を自然に満たしている。
- `calc_damages`（`core/damage.py`）は `move.power` が 0/None の場合に `ON_CALC_DAMAGE_MODIFIER`
  発火前に打ち切られるため、ダメージ固定技（いのちがけ等）が2倍対象外になることも実装で確認した。
- 変更は行っていない（変更なし）。

## リーサル計算
- 対象外（`docs/progress/move.md` のきょけんとつげき行は「リーサル実装: n/a」「リーサルテスト: n/a」）。

## テスト
- `tests/moves_attack/test_move_ka.py` に以下のテストを新規追加した（テストが存在しなかったため）。
  - `test_きょけんとつげき_命中時に自身へ状態を付与する`
  - `test_きょけんとつげき_まもるで防がれたとき状態は付与されない`
  - `test_きょけんとつげき_フェアリータイプに無効化されたとき状態は付与されない`
  - `test_きょけんとつげき_外れたとき状態は付与されない`
  - `test_きょけんとつげき状態_相手の技が必中になる`
  - `test_きょけんとつげき状態_相手の技のダメージが2倍になる`
  - `test_きょけんとつげき状態_ダメージ固定技は2倍にならない`
  - `test_きょけんとつげき状態_次の行動開始時に解除される`
  - `test_きょけんとつげき状態_まひで行動不能になっても解除される`
    （でんき無効のため攻撃者をピカチュウからカビゴンに変更して修正済み）
- `scripts/sort_tests.py`・`scripts/generate_test_list.py` を実行し、`python -m pytest tests/ -v`
  で全3305件（+1 skipped）がパスすることを確認した（`.loop/test_logs/きょけんとつげき.log`）。
