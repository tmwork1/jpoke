# きりさく レビュー結果

## 仕様書
- 変更なし。docs/spec/moves/きりさく.md は一次情報（docs/wiki/moves/きりさく.html）と再照合し、
  タイプ（ノーマル）・分類（物理）・威力（70）・命中（100）・PP（20）・急所ランク+1・直接攻撃・
  切る技（特性きれあじで威力1.5倍）の全項目が一致していることを確認した。誤り・欠落なし。

## 実装計画書
- 変更なし。docs/plan/moves/きりさく.md は仕様書と再照合し、追加効果を持たないため専用ハンドラ
  不要である旨、`critical_rank=1` / `flags={"contact", "slash"}` の根拠が妥当であることを確認した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ka.py` の `MOVES["きりさく"]` は
  `type="ノーマル", category="physical", pp=20, power=70, accuracy=100, critical_rank=1,
  flags={"contact", "slash"}, handlers={}` と正しく登録されており、仕様書・計画書と一致することを
  再確認した。PP20はdocs/champions/moves.md基準とも矛盾しない（きりさく自体はチャンピオンズ技一覧に
  非掲載だが、通常仕様のPP値で実装されており移行漏れバグはない）。

## リーサル計算
- 対象外（追加効果を持たない通常攻撃技のため、docs/progress/move.md のリーサル実装列は n/a）。

## テスト
- 変更なし。tests/moves_attack/test_move_ka.py の3テスト
  `test_きりさく_急所ランクが1` / `test_きりさく_急所ランクが1_乱数大で急所なし` /
  `test_きりさく_相手にダメージを与える` を実装・計画書と再照合し、既存の同種技（急所ランク+1技）の
  テストパターンとも一貫していることを確認した。きれあじとの相互作用は
  tests/abilities/test_ability_ka.py::test_きれあじ_きる技は威力補正1_5倍 で別途カバー済み。
  `python -m pytest tests/ -v` で全5801件（1件skip）がパスすることを確認した。

## 進捗表
- docs/progress/move.md のきりさく行で「レビュー」列が `-` のまま更新漏れになっていたため `x` に修正した
  （仕様書・計画書・実装・テストは前回のレビューで既に `x` 済みだった）。
