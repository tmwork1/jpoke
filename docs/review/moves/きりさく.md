# きりさく レビュー結果

## 仕様書
- 新規作成（docs/spec/moves/きりさく.md）。一次情報（docs/wiki/moves/きりさく.html）と照合し、
  タイプ（ノーマル）・分類（物理）・威力（70）・命中（100）・PP（20）・急所ランク+1・直接攻撃・
  切る技（特性きれあじで威力1.5倍）である旨を記載した。

## 実装計画書
- 新規作成（docs/plan/moves/きりさく.md）。追加効果を持たないため専用ハンドラ不要である旨と、
  `critical_rank=1` / `flags={"contact", "slash"}` の根拠を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `MOVES["きりさく"]` は既に
  `type="ノーマル", category="physical", pp=20, power=70, accuracy=100, critical_rank=1,
  flags={"contact", "slash"}, handlers={}` と正しく登録されており、仕様書・計画書と一致することを確認した。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a のため）。

## テスト
- 追加。`tests/moves_attack/test_move_ka.py` に以下を追加した。
  - `test_きりさく_急所ランクが1`（乱数0で急所発生を確認）
  - `test_きりさく_急所ランクが1_乱数大で急所なし`（乱数が閾値以上で急所にならないことを確認）
  - `test_きりさく_相手にダメージを与える`（通常ダメージを確認）
  - `scripts/sort_tests.py` でソート、`scripts/generate_test_list.py` でテスト一覧を更新し、
    `python -m pytest tests/ -v` で全3309件（1件skip）がパスすることを確認した。
