# はいよるいちげき レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/はいよるいちげき.md`）。一次情報（`docs/wiki/moves/はいよるいちげき.html`）を基に、
  タイプ（むし）・分類（物理）・威力70・命中90・PP10・追加効果100%で相手のとくこうを1段階下げる、
  接触技であること、判定フラグ（急所・命中判定・追加効果・まもる・おうじゃのしるし・マジックコート・
  よこどり・オウムがえし）を記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/はいよるいちげき.md`）。既存の類似技（とびかかる）の計画書構成に倣い、
  ハンドラ構成表・priority根拠・ちからずくとの相互作用・テスト方針を記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `はいよるいちげき` エントリ（type=むし, category=physical,
  pp=10, power=70, accuracy=90, flags={"contact", "secondary_effect"}）、
  `src/jpoke/handlers/move_attack.py` の `はいよるいちげき_lower_spa_C`
  （`modify_defender_stats(battle, ctx, value, stats={"spa": -1})`、chance省略で100%）は
  一次情報・仕様書と一致しており修正不要だった。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」「リーサルテスト」は n/a）。

## テスト
- `tests/moves_attack/test_move_ha.py` に以下を追加した（テストが存在しなかったため新規追加）。
  - `test_はいよるいちげき_secondary_effectフラグを持つ`: `MOVES["はいよるいちげき"].flags` に
    `secondary_effect` が含まれることを確認。
  - `test_はいよるいちげき_とくこう1段階低下が発動する`: 技命中後に防御側の `rank["spa"]` が
    `-1` になることを確認（100%発動のため乱数固定は不要）。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ha.py` でソート、
  `python scripts/generate_test_list.py` で一覧を更新、
  `python -m pytest tests/ -v` で全3546件パス（1件skipped）を確認した。
  結果は `.loop/test_logs/はいよるいちげき.log` に保存した。
- 併せて `docs/progress/move.md` の PP列の誤り（12 → 10）と効果列（？ → 実際の効果説明）を修正した。
