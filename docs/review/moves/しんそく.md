# しんそく レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/しんそく.md`）。一次情報（`docs/wiki/moves/しんそく.html`）を基に、
  タイプ・分類・威力・命中・PP・優先度・接触・判定フラグを記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/しんそく.md`）。既存の同種先制技（であいがしら・フェイント・
  このゆびとまれ 等）と同じパターンで `priority=2` のみで完結することを明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `しんそく` エントリ
  （`type="ノーマル"`, `category="physical"`, `pp=5`, `power=80`, `accuracy=100`,
  `priority=2`, `flags={"contact"}`, `handlers={}`）は仕様書と完全に一致しており、修正不要だった。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装・リーサルテスト列は `n/a`）。

## テスト
- 変更なし。`tests/moves_attack/test_move_sa.py` の
  `test_しんそく_相手にダメージを与える` は既に優先度+2の先制物理技として
  ダメージが発生することを確認しており、追加効果もないため過不足なし。

## その他
- `docs/progress/move.md` の しんそく 行に誤り（PP列が `8` になっていた。正しくは `5`）を
  発見し修正した。あわせて仕様書・計画書列を `x`、レビュー列を `✓` に更新した。
