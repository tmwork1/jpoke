# マジカルシャイン レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/マジカルシャイン.md`）。一次情報（docs/wiki/moves/マジカルシャイン.html）を
  もとに、タイプ・分類・威力・命中・PP・対象・優先度・接触判定・追加効果なし・判定フラグ
  （急所/命中判定/まもる/おうじゃのしるし/マジックコート/オウムがえし）を記載した。
  PPは本家10だが `docs/champions/move_list.txt` で12と明記されているためChampions値の12を採用。

## 実装計画書
- 新規作成（`docs/plan/moves/マジカルシャイン.md`）。既存実装（`handlers={}`）が仕様書と
  完全に一致していることを確認し、専用ハンドラ不要である旨と、ちからずく非連動・
  ダブル専用のスプレッド技威力減衰が本プロジェクト（シングル専用）では対象外であることを
  注意点として記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ma.py` の `"マジカルシャイン"` エントリ
  （type=フェアリー, category=special, pp=12, power=80, accuracy=100, handlers={}）は
  仕様書と一致しており修正不要と判断した。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`）。追加効果を持たない通常攻撃技であり、
  既存の基本ダメージ計算のみで完結するため、専用のリーサルハンドラは不要。

## テスト
- 変更なし。`tests/moves_attack/test_move_ma.py::test_マジカルシャイン_相手にダメージを与える`
  （追加効果なしでダメージを与えることの確認）が既に存在し、実装と仕様に合致している。
  `tests/test_lethal.py` の `test_タイプ半減きのみ_代表種`（ロゼルのみ×マジカルシャイン）も
  既存のまま問題なし。
