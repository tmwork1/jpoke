# スチームバースト レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/スチームバースト.md`）。
  - 基本情報（みず・特殊・威力110・命中95・相手1体・優先度0・接触なし）。
  - PP: Gen9本家は5だが、`docs/champions/changes_from_sv.md` の「全技PPは8/12/16/20の
    4段階に統一」という仕様に基づき8とした。`src/jpoke/data/ps-champ-ja/moves.json` に
    スチームバーストの行自体が存在しない（ボルケニオン専用技のため収録漏れと推測）ため
    直接の出典はないが、Gen9でPP5だった同系統の特殊技（だいもんじ・ハイドロポンプ・
    れんごく・リーフストーム・りゅうせいぐん等）が同ファイル内でいずれもPP=8に
    統一されている一貫したパターンから、PP=8が妥当と判断した。
  - 追加効果: 30%の確率でやけど付与（`docs/spec/moves/_move_secondary_1.md` 131行目と一致）。
  - 使用者がこおり状態の場合、こおり状態を治してから攻撃する効果を明記
    （ねっとう・かえんぐるま等と同種の「無条件解凍」効果であることを明記）。
  - 命中した相手のこおり状態も治す効果（ちからずくで無効化、りんぷんは無効化しない）を明記。

## 実装計画書
- 新規作成（`docs/plan/moves/スチームバースト.md`）。
  - PP=5→8への修正を計画（根拠を`src/jpoke/data/ps-champ-ja/moves.json`内の同系統技のPP統一パターンから記載）。
  - `ねっとう_thaw_attacker` / `シャカシャカほう_thaw_attacker` と同一パターンでの
    `スチームバースト_thaw_attacker`（`Event.ON_TRY_ACTION`, priority=5）追加を計画。
  - priority=5 の根拠を `docs/spec/turn.md` および既存実装（ねっとう計画書）に基づき記載。
  - 被弾側のこおり解除効果（`こおり_cure_by_thaw_move`）は既存の共通実装がそのまま
    正しく適用されるため追加実装不要であることを確認。
  - 申し送り: せいなるほのお（Ho-Oh専用技）も同様にchampions move_list.txtに行がなく
    PP=5のまま。今回のレビュー対象外のため変更しないが、将来の再レビュー時に検討すべき
    旨を記載。

## 実装（handlers/ / data/）
- 修正した内容:
  - `src/jpoke/data/moves/move_sa.py` の `"スチームバースト"` エントリの `pp=5` を `pp=8` に修正。
  - `src/jpoke/handlers/move_attack.py` に `スチームバースト_thaw_attacker` を新規実装
    （使用者がこおり状態のとき無条件で解凍する）。
  - `src/jpoke/data/moves/move_sa.py` の `"スチームバースト"` エントリに
    `Event.ON_TRY_ACTION: h.MoveHandler(ha.スチームバースト_thaw_attacker, priority=5)` を追加。
  - やけど付与（`スチームバースト_apply_burn_to_defender`, 30%）は既存実装のままで仕様と一致。
  - `docs/spec/moves/_move_labels.md` の thaw ラベル一覧には既にスチームバーストが
    記載済みだった（変更不要）。
  - 五十音ソート（sort_handlers.py / sort_data/sort_moves.py）は今回実行しない
    （マージ後に一括整形するため）。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」「リーサルテスト」列は `n/a`）。

## テスト
- `tests/moves_attack/test_move_sa.py` に以下を追加:
  - `test_スチームバースト_こおり状態で使うと解凍されて攻撃できる`
    （`test_ねっとう_こおり状態で使うと解凍されて攻撃できる` と同一構成）。
  - `test_スチームバースト_こおり状態の相手に当てると解凍する`
    （`test_ねっとう_こおり状態の相手に当てると解凍する` と同一構成、
    `secondary_chance=0.0` でやけど付与の乱数干渉を避ける）。
- 既存の `test_スチームバースト_やけどが発動する` は変更なし。
- sort_tests.py / generate_test_list.py は今回実行しない（マージ後に一括実行）。
- `python -m pytest tests/ -v` で3831件パス（1件skip、既存分）を確認
  （ログ: `.loop/test_logs/スチームバースト.log`）。失敗・flakyなテストはなし。
