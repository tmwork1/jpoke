# おどろかす レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/おどろかす.md`）。ポケモンWiki一次情報を基に、タイプ・分類・威力・命中・PP・判定フラグ・追加効果を記載。

## 実装計画書
- 新規作成（`docs/plan/moves/おどろかす.md`）。既存実装は仕様と一致しているため「修正不要」と結論づけ、根拠を記載。

## 実装（handlers/ / data/）
- 変更なし。`handlers/move_attack.py` の `おどろかす_apply_flinch`（`apply_volatile_to_defender` 経由で30%ひるみ付与）、
  `data/move.py` の `"おどろかす"` エントリ（type=ゴースト, category=physical, pp=15, power=30, accuracy=100,
  flags={"contact", "secondary_effect"}）ともに仕様書と一致しており、修正不要と確認した。

## リーサル計算
- 対象外（`docs/progress/move.md` の「おどろかす」行はリーサル実装/リーサルテストともに n/a）。
  ひるみは行動阻害効果でありHP分布に影響しないため。

## テスト
- 変更なし。`tests/moves_attack/test_move_a.py::test_おどろかす_ひるみが発動する` は
  `secondary_chance=1.0` でひるみ付与を確認しており、仕様・実装と一致している。

## その他
- `docs/progress/move.md` の該当行で PP が誤って `16` と記載されていたため `15`（実装・仕様と一致する値）に修正した。
- `tests/moves_status/test_move_a.py` にも同名の `test_おどろかす_ひるみが発動する`（既存の重複テスト）が存在するが、
  同ファイルには他の攻撃技の追加効果テスト（`アイアンヘッド`・`あくのはどう` 等）も同様に重複しており、
  本タスクの対象範囲（`tests/moves_attack/`）外の既存の構成のため今回は変更しない。
- `tests/` 全体を実行した際、`tests/moves_status/test_move_ra.py::test_リフレッシュ_まひを治す` が
  まひの25%全体麻痺判定の乱数を固定していないことに起因して稀に失敗する（本タスクとは無関係の既存の flaky テスト）。
  再実行では全テストが通過することを確認済み。
