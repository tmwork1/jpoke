# かえんほうしゃ レビュー結果

## 仕様書
- 専用の `docs/spec/moves/かえんほうしゃ.md` は作成していない（あおいほのお等と同じく、単純な追加効果技は個別仕様書を持たず `docs/spec/moves/_move_secondary_1.md` のカタログ表に集約する既存方針に合わせた）。
- `docs/spec/moves/_move_secondary_1.md` の `かえんほうしゃ` 行（`defender 0.1 ... やけど1 ...`）は一次情報（ポケモンWiki）の「10%の確率で相手をやけど状態にする」と一致しており、変更なし。

## 実装計画書
- `docs/plan/moves/かえんほうしゃ.md` は作成していない（上記と同じ理由。単純な追加効果技のため個別計画書を持たない既存方針）。

## 実装（handlers/ / data/）
- `src/jpoke/data/move.py` の `かえんほうしゃ` エントリを一次情報・`docs/champions/move_list.txt` と照合し、以下2点の誤りを修正した。
  - `pp=15`（本家シリーズのPP）→ `pp=16`（`docs/champions/move_list.txt` 準拠のChampions PP。`うたかたのアリア`等の既存レビュー方針と同じく、Championsの一覧を正とする）。
  - `flags={"secondary_effect"}` → `flags={"secondary_effect", "thaw"}` を追加。`docs/spec/ailments/こおり.md`に「ほのおタイプの攻撃技ダメージを受けると回復する」と明記されており、`かえんほうしゃ`はほのおタイプの攻撃技であるため、被弾した相手のこおり状態を解凍する必要があるが、`thaw`フラグが欠落していたため被弾しても解凍されない状態だった（`handlers/ailment.py`の`こおり_cure_by_thaw_move`は`ctx.move.has_flag("thaw")`で判定するため）。
  - `handlers={Event.ON_DAMAGE_HIT: ha.かえんほうしゃ_apply_burn_to_defender}` によるやけど付与（10%）自体は既存実装で正しく、変更なし。
  - `src/jpoke/handlers/move_attack.py` の `かえんほうしゃ_apply_burn_to_defender`（`apply_ailment_to_defender(..., ailment="やけど", chance=0.1)`）は正しく、変更なし。
- `python scripts/sort_data/sort_moves.py` を実行して並び順を整えた（`handlers/move_attack.py`は関数追加なしのため`sort_handlers.py`は未実行）。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）。

## テスト
- `tests/moves_attack/test_move_ka.py` に `test_かえんほうしゃ_こおり状態の相手に当てると解凍する`（`ねっとう`の解凍テストと同じパターンで、`secondary_chance=0.0`にしてやけど付与を無効化し、こおり状態の相手に命中させて解凍されることを確認）を追加。
- 既存の `test_かえんほうしゃ_やけどが発動する`（10%やけど付与のテスト）は変更なし。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ka.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新。
- `python -m pytest tests/ -v` を実行し、結果を `.loop/test_logs/かえんほうしゃ.log` に保存。3258 passed, 1 skipped, 3 failed。
  失敗した3件（`test_リフレッシュ_まひを治す`、`test_だっしゅつパック_わるいてぐせに奪われた場合は発動しない`、`test_だっしゅつボタン_ちからずくの効果が発動した技を受けたときは発動しない`）はいずれも `かえんほうしゃ` と無関係な特性・アイテム（リフレッシュ、わるいてぐせ、ちからずく、だっしゅつパック／ボタン）に関するもので、他エージェントが並行作業中の実装途中状態によるものと判断した。本レビューの変更（`かえんほうしゃ`のPP・thawフラグ・テスト追加）による影響ではないことを確認済み。

## 進捗表
- `docs/progress/move.md` の `かえんほうしゃ` 行を更新: レビュー=✓、タイプ=ほのお、威力=90、命中率=100、PP=16、対象=defender、効果=「10%の確率で相手を『やけど』状態にする。」に修正。
