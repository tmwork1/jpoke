# フレアソング レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/フレアソング.md`）。
  一次情報（`docs/wiki/moves/フレアソング.html`）をもとに、タイプ・分類・威力・命中率・
  対象・優先度・接触なし・音技フラグ・「100%の確率で自分のとくこうを1段階上げる」
  効果、判定フラグ（急所・命中判定・追加効果・まもる・おうじゃのしるし・マジックコート）
  を記載した。PPは一次情報の10ではなく `docs/champions/move_list.txt` 基準の12を採用する
  旨を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/フレアソング.md`）。
  既存実装（`フレアソング_boost_spa_C` / `フレアソング_boost_spa`）がしんぴのちから・
  ほのおのまいと同一パターンで実装済みであることを確認し、priorityをデフォルト（100）の
  まま維持する根拠（`docs/spec/turn.md` の記載priority=10は他の同種技の既存実装との
  一貫性を優先し採用しない）を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `フレアソング` エントリの `pp` を `10` → `12` に修正した。
  `docs/champions/move_list.txt`（`フレアソング ... 12`）が正であり、Gen9本家基準の
  値がChampions移行時に修正されずに残っていたバグと判断した。
  ハンドラ本体（`フレアソング_boost_spa_C`、`ON_HIT` / `attacker:self` / デフォルトpriority）
  は仕様・既存の類似技（しんぴのちから・ほのおのまい）と一致しており修正不要。

## リーサル計算
- 変更なし。`src/jpoke/handlers/lethal.py` の `フレアソング_boost_spa`
  （`LethalEvent.ON_HIT`, subject="attacker"）は仕様・実装と一致しており修正不要。

## テスト
- 変更なし。既存テスト（`tests/moves_attack/test_move_ha.py::test_フレアソング_特攻1段階上昇が発動する`、
  `tests/test_lethal.py::test_フレアソング_とくこうアップ_secondary有り` /
  `_secondary無し`）は実装と一致しており、全件パスを確認した。

## 進捗表
- `docs/progress/move.md` の フレアソング 行を更新した。
  - 仕様書・計画書列を `-` → `x`
  - PP列を `10` → `12`（Champions基準）
  - レビュー列を `-` → `✓`
