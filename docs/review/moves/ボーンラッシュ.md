# ボーンラッシュ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ボーンラッシュ.md`）。
- 一次情報（`docs/wiki/moves/ボーンラッシュ.html`）と `docs/champions/move_list.txt` を照合し、
  タイプ・分類・威力（Champions基準30）・命中率（90）・PP（Champions基準12）・対象・優先度・
  接触判定なし・連続攻撃技としての判定仕様（急所各回、命中判定は初回のみ、追加効果なし、まもる有効、
  おうじゃのしるし有効、マジックコート/よこどり対象外、オウムがえし可能）を記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/ボーンラッシュ.md`）。
- 既存実装（`multi_hit={min:2, max:5, check_hit_each_time:False, power_sequence:()}`、固有ハンドラなし）
  を前提に、汎用連続攻撃機構（`core/move_executor.py`）で処理される旨と、修正したPP・威力の根拠を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `ボーンラッシュ` エントリを修正した。
  - `pp=10` → `pp=12`（Gen9本家値のまま移行漏れになっていたバグ。`docs/champions/move_list.txt` の
    `ボーンラッシュ	じめん	物理	30	90	12	接×	守○` に合わせて修正）
  - `power=25` → `power=30`（同上。Champions基準では威力30に変更されている）
  - `accuracy=90`、`multi_hit`（min2/max5/check_hit_each_time=False/power_sequence空）、
    `flags`（接触フラグなし）は既存のまま正しかったため変更なし。
- `handlers/move_attack.py` への追加・修正は不要（固有ハンドラを持たない汎用連続攻撃技のため）。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`。連続攻撃技全般はリーサル計算の対象外）。

## テスト
- `tests/moves_attack/test_move_ha.py` に以下を追加した。
  - `test_ボーンラッシュ_PPと威力はChampions基準`: `MOVES["ボーンラッシュ"].pp == 12` および
    `power == 30` を確認（`test_ネズミざん_PPは12` と同様のパターン）。
  - `test_ボーンラッシュ_命中判定は1発目のみ`: 命中率0で0回ヒットすることを確認。
  - `test_ボーンラッシュ_相手にダメージを与える`: ヒット時に相手のHPが減ることを確認。
- 既存の `test_ボーンラッシュ_複数ヒットする`（2～5回ヒット確認）は変更なし。
