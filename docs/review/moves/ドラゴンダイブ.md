# ドラゴンダイブ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ドラゴンダイブ.md`）。`docs/wiki/moves/ドラゴンダイブ.html` および
  `src/jpoke/data/ps-champ-ja/moves.json` を参照し、タイプ・分類・威力100・命中75・PP12・
  20%ひるみ・`minimize`（ちいさくなる状態に必中・威力2倍）の仕様をまとめた。

## 実装計画書
- 新規作成（`docs/plan/moves/ドラゴンダイブ.md`）。既存の集約計画書
  `docs/plan/moves/攻撃技_ひるみ系.md` にはドラゴンダイブの記載があるが、
  イベント（`ON_HIT`）・ハンドラ名（`_apply_volatile_to_defender`）が実装の実態
  （`ON_DAMAGE_HIT` / `_apply_flinch`）と乖離しており、集約計画書側の記載は
  古い設計案として残存していたもの（実装時に他の同種ひるみ技と統一する形で
  `ON_DAMAGE_HIT` + `_apply_flinch` 命名規則に変更された）。個別レビュー方針に合わせ、
  実装の実態に即した個別計画書を新規作成した（集約計画書自体は本レビューの対象外として未変更）。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `pp` を 10（Gen9本家基準の移行漏れ）から
  12（`src/jpoke/data/ps-champ-ja/moves.json` の値）に修正した。
- `handlers/move_attack.py` の `ドラゴンダイブ_apply_flinch`（20%ひるみ、
  `apply_volatile_to_defender` 経由）は仕様と一致しており修正不要。
- `minimize` フラグによる必中・威力2倍は `handlers/volatile.py` の汎用ハンドラで処理されており、
  修正不要。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装・リーサルテストは n/a）。

## テスト
- 既存テスト `tests/moves_attack/test_move_ta.py::test_ドラゴンダイブ_ひるみが発動する`
  （20%ひるみ発動）と `tests/volatiles/test_volatile_satana.py` の
  `minimize_enhance_moves` パラメータ化テスト（ドラゴンダイブを含む必中・威力2倍の確認）で
  既に十分カバーされていたため、追加のテストは行わなかった。
- `python -m pytest tests/ -v` を実行し、全件パス（3917 passed, 1 skipped）を確認した。
  ログは `.loop/test_logs/ドラゴンダイブ.log` に保存した。
