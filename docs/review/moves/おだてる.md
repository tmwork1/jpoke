# おだてる レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/おだてる.md`）。`docs/wiki/moves/おだてる.html` と
  `src/jpoke/data/ps-champ-ja/moves.json` を参照し、`いばる`（こうげき+2版）の仕様書と対になる形式で作成。
  - あくタイプ / 変化 / 命中100 / PP16 / 対象1体選択
  - 相手のとくこうを1段階上げ、こんらん状態にする
  - 失敗条件（とくこう+6かつこんらん済みで技全体が失敗）を明記
  - マイペース・しんぴのまもり・ミストフィールドによるこんらん無効化（Champions対応範囲外のため未実装）を記載

## 実装計画書
- 修正した。
  - PP を Gen9本家値の 15 から `src/jpoke/data/ps-champ-ja/moves.json` の正しい値 16 に修正
    （Champions は全技PPを8/12/16/20に統一しているため）
  - 失敗条件判定用の `ON_BEFORE_APPLY_MOVE`（`おだてる_can_apply`）ハンドラが計画に欠落していたため追加
    （`いばる_can_apply` と同型のパターン）
  - `ON_STATUS_HIT` ハンドラの実装イメージを、実際の実装（`AttackContext` + `volatile_manager.apply_confusion`）
    に合わせて修正（旧計画は `EventContext` + 手動 `randint` を記載していたが不整合だった）

## 実装（handlers/ / data/）
- 修正した。
  - `handlers/move_status.py`: `おだてる_can_apply` を新規追加（相手のとくこうランクが+6かつ
    こんらん済みの場合に技全体を失敗させる。`いばる_can_apply` と同型）
  - `data/moves/move_a.py`: `"おだてる"` エントリの `pp` を 15 → 16 に修正、
    `Event.ON_BEFORE_APPLY_MOVE` に `おだてる_can_apply` を追加登録
  - 修正前は失敗条件を判定するハンドラが存在せず、とくこう+6かつこんらん済みのケースで
    見かけ上は数値変化がないものの `battle.move_executor.move_applied` が `True` のままになる
    バグがあった（`いばる`は明示的に `False` にする実装だった）

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`。変化技のためダメージなし）

## テスト
- 修正・追加した（`tests/moves_status/test_move_a.py`）。
  - 既存3テストに `battle.move_executor.move_applied` の検証を追加
    （成功時は `True`、失敗時は `False` になることを確認）
  - `test_おだてる_こんらん済みでもとくこう最大未満なら成功` を新規追加
    （`いばる_こんらん済みでもこうげき最大未満なら成功` と対応するケース）
  - `python -m pytest tests/ -v` で全4113件パス（1件skip）。実行ログは `.loop/test_logs/おだてる.log`
