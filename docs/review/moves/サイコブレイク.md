# サイコブレイク レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/サイコブレイク.md`）。
  - タイプ: エスパー / 分類: 特殊 / 威力: 100 / 命中: 100 / PP: 10 / 対象: 相手1体 /
    優先度: 0 / 接触なし。
  - 効果: 相手の『とくぼう』ではなく『ぼうぎょ』でダメージ計算をする（分類は特殊のまま、
    サイコショック・しんぴのつるぎと同種の「物理判定の特殊技」）。追加効果なし。
  - 判定フラグ（急所・命中・まもる・おうじゃのしるし・マジックコート・よこどり・オウムがえし）を一次情報から記載。
  - PPについて: `docs/champions/move_list.txt` に本技のエントリが存在しない
    （ミュウツー専用技のため）ことを確認し、一次情報（Wiki）記載の値 10 をそのまま採用する旨を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/サイコブレイク.md`）。
  - `Query.deals_physical_damage()` が `move.category == "physical"` または
    `move.has_flag("physical_damage")` の場合に防御側の『ぼうぎょ』を参照する既存の仕組みを利用する方針を明記。
  - 新規ハンドラは不要で、`flags` に `"physical_damage"` を追加するだけで済むことを記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_sa.py` の `"サイコブレイク"` エントリを修正した。
  - **バグ修正**: `flags` が未指定（`physical_damage` フラグが欠落）で、分類どおり
    『とくぼう』でダメージ計算されてしまっていた。`flags={"physical_damage"}` を追加し、
    `Query.deals_physical_damage()` 経由で防御側の『ぼうぎょ』を参照するようにした。
    - `docs/spec/moves/カテゴリ.md` の「物理として判定される / physical」区分には
      サイコブレイクが既に掲載されており、`docs/review/moves/しんぴのつるぎ.md` の「備考」で
      既知バグとして記録されていたものを今回修正した。
  - 五十音ソートスクリプト（sort_handlers.py / sort_data/*.py）は指示によりこの回では実行していない
    （マージ後に一括整形予定）。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）。

## テスト
- `tests/moves_attack/test_move_sa.py` に以下を追加した。
  - `test_サイコブレイク_相手のとくぼうランク変化の影響を受けない`
    （相手の『とくぼう』ランクを+6しても与ダメージが変わらないことを確認）
  - `test_サイコブレイク_相手のぼうぎょランク変化の影響を受ける`
    （相手の『ぼうぎょ』ランクを+6すると与ダメージが減ることを確認）
  - sort_tests.py / generate_test_list.py は指示によりこの回では実行していない
    （マージ後に一括実行予定）。
  - `python -m pytest tests/ -v` で全件（3776 passed, 1 skipped）を確認済み
    （ログ: `.loop/test_logs/サイコブレイク.log`）。
