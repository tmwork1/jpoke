# えんまく レビュー結果

## 仕様書
- 存在しなかったため新規作成した（`docs/spec/moves/えんまく.md`）
- 一次情報（ポケモンWiki）に基づき、タイプ=ノーマル、分類=変化、PP=20、命中率=100、
  範囲=相手1体選択、優先度=0、直接攻撃=×、効果=「相手の命中率ランクを1段階下げる」を記載
- `src/jpoke/data/ps-champ-ja/moves.json` に本技の記載がないことを確認し、その旨と一次情報基礎値PP=20を
  採用する根拠を明記した
- Zワザ付加効果・コンテスト効果・バトルパレス判定はチャンピオンズ対象外として明記

## 実装計画書
- 存在しなかったため新規作成した（`docs/plan/moves/えんまく.md`）
- 既存の類似実装（`すなかけ`, `あまえる` の防御側ランクダウン変化技ハンドラ）を参照し、
  `ON_STATUS_HIT` / `subject_spec="attacker:self"`（デフォルト） / `priority=100` の根拠を明記

## 実装（handlers/ / data/）
- 既存実装（`handlers/move_status.py` の `えんまく_modify_defender_stats`、
  `data/moves/move_a.py` の `MoveData` 登録）は仕様書・計画書と完全に一致しており、
  `すなかけ` と同一パターンの共通関数 `modify_defender_stats` を正しく利用していたため
  変更なし

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`。ダメージを与えない変化技のため）

## テスト
- テストが存在しなかったため `tests/moves_status/test_move_a.py` に
  `test_えんまく_相手の命中率1段階下がる` を追加（`つぶらなひとみ` 等の既存パターンに準拠し、
  `defender.rank["accuracy"] == -1` を確認）
- `python -m pytest tests/` で全4094件（+1 skip）がパスすることを確認
