# でんじふゆう レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/でんじふゆう.md` が存在しなかったため、
  `docs/wiki/moves/でんじふゆう.html` と `docs/champions/move_list.txt` を基に作成）
- PP=12（champions基準）、対象=自分、まもる/マジックコート無効、
  ねをはる・うちおとす状態での失敗、じゅうりょく中の使用不可などを明記

## 実装計画書
- 修正した（既存の `docs/plan/moves/でんじふゆう.md` を仕様書・実装の変更内容に合わせて更新）
  - PP を10→12に修正（champions/move_list.txt準拠）
  - `target="self"` の指定漏れとその修正理由を追記
  - `でんじふゆう_check_can_use`（ねをはる・うちおとす状態での失敗）ハンドラの新規追加を記載

## 実装（handlers/ / data/）
- 修正した
  - `src/jpoke/data/moves/move_ta.py`: `でんじふゆう` の `pp` を10→12に修正
    （`docs/champions/move_list.txt` 準拠。Gen9本家のPP10が移行漏れで残っていたバグ）
  - `src/jpoke/data/moves/move_ta.py`: `target="self"` が欠落していたため追加
    （欠落していると相手のまもる・マジックコートに誤反応するバグ）
  - `src/jpoke/handlers/move_status.py`: `でんじふゆう_check_can_use` を新規実装し、
    ねをはる・うちおとす状態のポケモンが使用すると失敗するようにした
    （`docs/wiki/moves/でんじふゆう.html` の「技の仕様」節に記載があるが未実装だった）
  - `src/jpoke/data/moves/move_ta.py`: `ON_TRY_MOVE_1` に上記ハンドラを
    `gravity_restricted_fail` と同じ priority=30 で追加登録

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行が n/a。ダメージを与えない変化技のため）

## テスト
- 追加した（`tests/moves_status/test_move_ta.py`）
  - `test_でんじふゆう_PPは12`
  - `test_でんじふゆう_まもるで防がれない`
  - `test_でんじふゆう_マジックコートで跳ね返されない`
  - `test_でんじふゆう_ねをはる状態なら失敗`
  - `test_でんじふゆう_うちおとす状態なら失敗`
  - `test_でんじふゆう_じゅうりょく状態では失敗`
- 既存の2件（すでにでんじふゆう状態なら失敗／でんじふゆう状態を付与する）は変更なし
- `python -m pytest tests/ -v` で4257件パス（1件skip、既存分）
