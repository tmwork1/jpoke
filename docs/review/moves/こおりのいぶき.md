# こおりのいぶき レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/こおりのいぶき.md`）。一次情報（`docs/wiki/moves/こおりのいぶき.html`）に基づき、
  タイプ・分類・威力（第六世代以降60）・命中90・PP10・対象・優先度・非接触・確定急所（急所ランク3）・
  追加効果なしを明記。

## 実装計画書
- 新規作成（`docs/plan/moves/こおりのいぶき.md`）。`やまあらし`（同じく `critical_rank=3` のみで
  確定急所を表現する既存実装）と同様のパターンであることを確認し、専用ハンドラ不要である根拠を記載。

## 実装（handlers/ / data/）
- 変更なし。`data/move.py` の `"こおりのいぶき"` エントリ（`type="こおり"`, `category="special"`,
  `pp=10`, `power=60`, `accuracy=90`, `critical_rank=3`, `handlers={}`）は一次情報・仕様書と一致していた。

## リーサル計算
- 対象外（`docs/progress/move.md` でリーサル実装・リーサルテストとも n/a）。
  追加効果・確率分岐を持たない通常攻撃で、急所ランク3は `calc_lethal` 呼び出し側の `critical` 引数
  で表現される既存の枠組みに委ねるため、本技固有のリーサルハンドラは不要と判断。

## テスト
- 変更なし。`tests/moves_attack/test_move_ka.py::test_こおりのいぶき_確定急所` が
  `critical_rank=3` による確定急所発生を検証しており、実装・仕様と一致していることを確認した。

## 進捗表の修正
- `docs/progress/move.md` の こおりのいぶき 行:
  - PP列の誤り（12 → 10）を修正。
  - 仕様書・計画書・レビュー列を `-` → `✓` に更新（今回作成・確認済みのため）。
  - 効果列に「直接攻撃ではない」を補足。
- 作業中に気づいた別行の誤り: `やまあらし` 行の PP列も誤って12と記載されていたため、10に修正した
  （実装は `pp=10`）。
