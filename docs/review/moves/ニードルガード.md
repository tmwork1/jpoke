# ニードルガード レビュー結果

## 仕様書
- 新規作成: `docs/spec/moves/ニードルガード.md`（基本情報・効果・判定フラグ・priority根拠・既知の未実装範囲）
- 新規作成: `docs/spec/volatiles/ニードルガード.md`（揮発性状態側の詳細仕様。まもる/トーチカの
  既存スペックと同じフォーマットで作成し、マジックガード・ぼうごパット・フェイント等との相互作用を明記）

## 実装計画書
- 既存の `docs/plan/moves/ニードルガード.md` は「未実装」を前提とした記述になっており、実際には
  既に実装済みだったため、実装済み状態を正しく反映する内容に全面更新した。
  - PP を計画書内の記載・コード例ともに `10` → `8`（champions基準、move_list.txt 628行目）に修正
  - `_run_protect` の接触判定コード例を `battle.query.is_contact(ctx)` → 実際の実装である
    `battle.query.is_contact_reaction(ctx)` に修正（ぼうごパット等の反応阻害を考慮する関数）
  - 関数名の記載を実装と一致させた（`ニードルガード_apply_volatile_to_attacker` → `ニードルガード_apply`）
  - 「実装状況」テーブルをすべて実装済みに更新し、フェイント等による解除の実装箇所（`move_attack.py`
    の `_FAINT_REMOVE_VOLATILES`）を追記

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_na.py` の `"ニードルガード"` の `pp` を `10` → `8` に修正
  （champions基準の PP は `docs/champions/move_list.txt` 628行目より 8。他は仕様書・計画書と一致しており修正なし）
- `handlers/volatile.py` の `ニードルガード_protect` / `ニードルガード_remove_volatile`、
  `handlers/move_status.py` の `ニードルガード_apply`、`data/volatile.py` の `VOLATILES["ニードルガード"]`
  は仕様書・計画書と照合し、誤り・欠落なし（変更なし）

## リーサル計算
- 対象外（`docs/progress/move.md` のニードルガード行「リーサル実装」列が `n/a`。まもる・キングシールド・
  トーチカ等の他の守る系技も同様に `n/a` であり、自己対象の防御技のためリーサル計算の対象外）

## テスト
- 既存テスト（`tests/moves_status/test_move_na.py` の揮発状態付与・攻撃技無効化・変化技無効化・
  接触ダメージ・ターン終了解除・非接触ダメージなし、`tests/moves_status/test_move_ma.py` の連続使用失敗、
  `tests/moves_attack/test_move_ha.py` のフェイントによる解除）は実装と照合し、誤りなし
- 追加: `tests/moves_status/test_move_na.py` に以下の2件を新規追加
  - `test_ニードルガード_ぼうごパット所持者にはダメージを与えない`
  - `test_ニードルガード_マジックガード所持者にはダメージを与えない`
- `python -m pytest tests/ -v` を実行し、全件パスを確認（結果: `.loop/test_logs/ニードルガード.log`）

## その他
- `docs/progress/volatile.md` にニードルガード揮発状態の行が欠落していたため追加した
  （とくせいなし行の後、にげられない行の前。他の守る系揮発状態と同じ `x x x x n/a n/a x ?` パターン）
