# サイコブレイド レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/サイコブレイド.md`）。
- 一次情報（Wiki）から、エレキフィールド中の威力1.5倍効果（接地判定なし・技威力への補正として適用）と、
  切る技（きれあじ対象）である点を整理して記載。
- PPについて、`src/jpoke/data/ps-champ-ja/moves.json` に該当行が欠落していることを明記した上で、
  本家PP15の他技（シャドークロー・そらをとぶ・サンダーダイブ・シザークロス）がいずれもPP16に
  変換されている実例から、サイコブレイドもPP16と推定する根拠を記載。

## 実装計画書
- 新規作成（`docs/plan/moves/サイコブレイド.md`）。
- 既存実装の不備（PP・flags・威力補正ハンドラ未実装）を整理し、`ライジングボルト_calc_power` /
  `ワイドフォース_calc_power` との違い（接地判定を行わない）を明記した上で実装方針を記載。

## 実装（handlers/ / data/）
- `handlers/move_attack.py` に `サイコブレイド_calc_power` を新規実装
  （エレキフィールド中のみ威力1.5倍、接地判定なし）。
- `data/moves/move_sa.py` の `"サイコブレイド"` エントリを修正:
  - `pp=15` → `pp=16`（チャンピオンズPP換算値。move_list.txt に欠落しているため既存PP15技からの
    変換パターンで推定）
  - `flags={"contact"}` → `flags={"contact", "slash"}`（切る技。きれあじの威力補正判定に必要）
  - `handlers={}` → `Event.ON_CALC_POWER_MODIFIER` に `サイコブレイド_calc_power` を登録
    （エレキフィールド中の威力1.5倍効果が未実装だったため）

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）。

## テスト
- `tests/moves_attack/test_move_sa.py` に5件追加:
  - `test_サイコブレイド_エレキフィールドで威力1_5倍になる`
  - `test_サイコブレイド_フィールドなしのとき威力補正なし`
  - `test_サイコブレイド_使用者が浮いていても威力補正が乗る`（接地判定を行わない仕様の確認）
  - `test_サイコブレイド_対象が浮いていても威力補正が乗る`（同上）
  - `test_サイコブレイド_きれあじで威力1_5倍`（slashフラグとの連動確認）
- `python -m pytest tests/ -v` 実行結果: 3779 passed, 1 skipped（既存の無関係なskip）。
  ログは `.loop/test_logs/サイコブレイド.log` に保存。
