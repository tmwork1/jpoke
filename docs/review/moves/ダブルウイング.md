# ダブルウイング レビュー結果

## 仕様書
- 変更なし。
- ダブルウイングは「固定2回攻撃技」であり、`docs/spec/moves/連続技.md`（9.1節）に
  タイプ・分類・威力・命中・PP・直接攻撃の有無・効果が一覧として記載済み。
  同じ扱いの兄弟技（ダブルアタック等）も個別仕様書を持たない運用のため、
  個別ファイルは新規作成せず既存の共通仕様書を正とした。
- 一次情報（`docs/wiki/moves/ダブルウイング.html`）と照合し、タイプ=ひこう、
  分類=物理、威力=40、命中=90、直接攻撃=○、効果=2回連続攻撃（連続攻撃技）で
  共通仕様書の記載と整合していることを確認した。

## 実装計画書
- 対象外（個別計画書なし、共通仕様書 `連続技.md` の固定2回攻撃技パターンを
  そのまま適用する運用のため、兄弟技と同様に新規作成しなかった）。

## 実装（handlers/ / data/）
- 修正: `src/jpoke/data/moves/move_ta.py` の `"ダブルウイング"` エントリの
  `pp` が `10`（Gen9本家基準）になっていたバグを `12` に修正した。
  `docs/champions/move_list.txt`（468行目: `ダブルウイング ひこう 物理 40 90 12 接○ 守○`）
  および `docs/progress/move.md` のPP列が正しく `12` であるのに対し、実装側が
  移行漏れで本家基準の値のままだったもの。
- その他のフィールド（type/category/power/accuracy/flags/multi_hit）は
  仕様と一致しており変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行はリーサル実装・リーサルテストともに `n/a`）。

## テスト
- 追加: `tests/moves_attack/test_move_ta.py` に
  `test_ダブルウイング_タイプ分類威力命中PPが仕様通り` を新規追加し、
  type/category/power/accuracy/pp/flags/multi_hit の各フィールドを検証。
  PP修正のリグレッション防止のため追加。
- 既存の `tests/moves_attack/test_move__group.py::test_ダブルウイング_2回ヒットする`
  および `tests/items/test_item_a.py` の いかさまダイス関連パラメトライズテストは
  実装と整合しており変更なし。
- `python -m pytest tests/ -v` は全件パス（3873 passed, 1 skipped）。
  なお `tests/moves_attack/test_move_ma.py::test_むしくい_ねんちゃく持ちをひんしにさせた場合は奪える`
  は本レビュー対象と無関係な既存の確率依存テスト（まひの「動けない」判定を固定していない）
  でまれに失敗することを確認したが、ダブルウイングの変更とは無関係のため本タスクでは
  修正対象外とした。
