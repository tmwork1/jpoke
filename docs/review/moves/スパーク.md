# スパーク レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/スパーク.md`）。一次情報（`docs/wiki/moves/スパーク.html`）と照合し、
  タイプ/分類/威力/命中/PP、まひ追加効果30%、判定フラグ（急所・命中判定・まもる・おうじゃのしるし・
  マジックコート・よこどり・オウムがえし）を記載。
  `src/jpoke/data/ps-champ-ja/moves.json` にスパークのエントリが存在しないため、PPは第九世代基準の20を
  そのまま採用する旨を明記した（既に統一4段階 8/12/16/20 のひとつであり、
  `changes_from_sv.md` の技個別変更点一覧にも記載がないため）。

## 実装計画書
- 新規作成（`docs/plan/moves/スパーク.md`）。既存実装（`data/moves/move_sa.py` /
  `handlers/move_attack.py`）が仕様書と一致していることを確認し、ハンドラ構成・priority根拠・
  PPの扱いに関する根拠を記載。

## 実装（handlers/ / data/）
- 変更なし。`data/moves/move_sa.py` の `スパーク` エントリ（type=でんき, category=physical, pp=20,
  power=65, accuracy=100, flags={"contact", "secondary_effect"}, ON_DAMAGE_HIT ハンドラ）は
  仕様と一致。
- `handlers/move_attack.py` の `スパーク_apply_paralysis_to_defender`（chance=0.3）も仕様と一致。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- 追加: `tests/moves_attack/test_move_sa.py` に `test_スパーク_まひが発動しない`
  （secondary_chance=0.0でまひを付与しないことを検証）を追加。
  既存の `test_スパーク_まひが発動する` と合わせ、10まんボルト・のしかかり等の同系統技と
  同じ「発動する/発動しない」の対をなすテスト構成にした。
- `python -m pytest tests/ -v` を実行し、3830 passed, 1 skipped で全テスト成功を確認
  （初回実行時に急所判定の乱数由来と思われる無関係な既存テスト6件が偶発的に失敗したが、
  個別再実行および全体再実行で再現せず、本レビューの変更とは無関係と判断した）。

## その他の修正
- `docs/progress/move.md` の スパーク 行が全列 `?` の未記入状態だったため、
  タイプ=でんき、威力=65、命中率=100、PP=20、対象=defender、効果欄を記入した。
  合わせて 仕様書/計画書 列を `x`、レビュー列を `✓` に更新。
