# バレットパンチ レビュー結果

## 仕様書
- 新規作成（docs/spec/moves/バレットパンチ.md）。docs/wiki/moves/バレットパンチ.html を一次情報として、
  タイプ・分類・威力・命中・PP・優先度・判定フラグ・備考を記載した。
- PPは本家30だが、docs/champions/move_list.txt の値（20）を正とした。

## 実装計画書
- 新規作成（docs/plan/moves/バレットパンチ.md）。追加効果なし・専用ハンドラ不要である旨と、
  優先度+1は `MoveData.priority` の共通処理で対応すること、パンチ技フラグ・てつのこぶし補正は
  特性/アイテム側の共通ロジックで処理されることを明記した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `"バレットパンチ"` エントリで `pp=30`（本家Gen9基準の移行漏れ）
  になっていたため、champions基準の `pp=20` に修正した。
- `priority=1`（優先度+1）、`flags={"contact", "punch"}` は仕様と一致しており変更なし。
- `handlers={}`（追加効果なし）も仕様と一致しており変更なし。

## リーサル計算
- 対象外（n/a）。追加効果のない固定威力技のため、専用のリーサル計算ハンドラは不要。

## テスト
- `tests/moves_attack/test_move_ha.py` に以下を追加・修正した。
  - `test_バレットパンチ_タイプ威力命中PP優先度が仕様通り`: type/category/power/accuracy/pp/priority/flags を検証（新規追加）。
  - `test_バレットパンチ_相手にダメージを与える`: 既存のまま変更なし。
  - `test_バレットパンチ_優先度がプラス1のためすばやさが高い相手より先制する`: 優先度+1により
    すばやさの高い相手より先に行動することを `get_action_order` で検証（新規追加）。
- `python -m pytest tests/ -v` を実行し、3949 passed, 1 skipped で全件成功を確認
  （ログ: `.loop/test_logs/バレットパンチ.log`）。
