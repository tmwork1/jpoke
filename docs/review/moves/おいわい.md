# おいわい レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/おいわい.md`）。一次情報（ポケモンWiki）を元に、
  タイプ・分類・PP・範囲（自分）・判定（まもる/マジックコート/よこどり/
  オウムがえし/ねごと/まねっこ）・実装方針を記載した。
- PPは `src/jpoke/data/ps-champ-ja/moves.json` に記載がないため、配布限定の効果なし技で
  あることからPP標準化の対象外とみなし、本家の値（40）をそのまま採用する旨を
  明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/おいわい.md`）。現状の実装状況（`target` 未設定・
  `non_copycat` フラグ欠落）と修正方針を記載した。効果を持たない技のため
  ハンドラは不要である旨、および `is_reflectable` / `is_blocked_by_protect`
  が `MoveData.target` を参照する実装であることの根拠も記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `"おいわい"` エントリを修正した。
  - `target="self"` を追加（デフォルトの `"foe"` のままでは、
    `Move.is_reflectable` / `Move.is_blocked_by_protect` が `target` を
    参照する実装のため、マジックコートで跳ね返される・まもるで防がれる
    という誤った挙動になっていた。仕様は「まもる: ×」「マジックコート: ×」
    であり、`target="self"` にすることで正しく解決される）。
  - `flags` に `non_copycat` を追加（一次情報に「この技に対する さいはい・
    まねっこ は失敗する」と明記されているため、既存の `まねっこ_can_use`
    ハンドラの `non_copycat` フラグ判定の仕組みをそのまま利用する）。
  - `handlers={}` は変更なし（効果のないわざのため副作用は存在しない）。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）。

## テスト
- `tests/moves_status/test_move_a.py` に以下を追加。
  - `test_おいわい_まもるで防がれない`
  - `test_おいわい_マジックコートで跳ね返されない`
  - `test_おいわい_使用してもHPやランクなど戦闘状態が変化しない`
- `tests/moves_status/test_move_ma.py` に `test_まねっこ_おいわいはコピーできない`
  を追加し、`non_copycat` フラグによりまねっこでのコピーが失敗することを検証した。
- `python -m pytest tests/ -v` で全4116件（1件skip）がパスすることを確認した
  （ログ: `.loop/test_logs/おいわい.log`）。
