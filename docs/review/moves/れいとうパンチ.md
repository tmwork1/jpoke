# れいとうパンチ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/れいとうパンチ.md`）。一次情報（`docs/wiki/moves/れいとうパンチ.html`）とシブリング技（`ほのおのパンチ`・`かみなりパンチ`）の仕様書を参照し、こおりタイプ・物理・威力75・命中100・PP15・追加効果10%こおり・パンチ技（てつのこぶし対象）として記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/れいとうパンチ.md`）。既存実装（`src/jpoke/handlers/move_attack.py` の `れいとうパンチ_apply_freeze_to_defender`、`src/jpoke/data/moves/move_ra.py` の `MoveData` 登録）を事後記録する形で作成した。ハンドラは `Event.ON_DAMAGE_HIT` / `MoveHandler` デフォルト（`subject_spec="attacker:self"`, `priority=100`）で、`かみなりパンチ`・`ほのおのパンチ`・`れいとうビーム` と同じ構成。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/move_attack.py` の `れいとうパンチ_apply_freeze_to_defender` は共通関数 `apply_ailment_to_defender(battle, ctx, value, ailment="こおり", chance=0.1)` を使用しており、`れいとうビーム` と同一パターン。`src/jpoke/data/moves/move_ra.py` の `MoveData` も type/category/pp/power/accuracy/flags（`contact`, `punch`, `secondary_effect`）がすべて一次情報と一致していることを確認した。

## リーサル計算
- 対象外（`n/a`）。こおりは行動阻害の状態異常であり、ダメージ量そのものには影響しないため、本技専用のリーサルハンドラは不要と判断した（`docs/progress/move.md` のリーサル実装列も `n/a`）。

## テスト
- 変更なし。`tests/moves_attack/test_move_yarawa.py` の `test_れいとうパンチ_こおりが発動する` は `secondary_chance=1.0` で確定発動を検証しており、シブリング技（`ほのおのパンチ`・`かみなりパンチ`）と同じテストパターンで妥当と判断した。

## その他
- `docs/progress/move.md` の れいとうパンチ 行に以下の誤りがあったため修正した：
  - PP列が `?` になっていた → `15`（実装・一次情報と一致）に修正
  - 対象列が `attacker`（誤り）になっていた → `defender`（他の攻撃技と同じ表記）に修正
  - 仕様書・計画書・レビュー列を `-` から `x` / `✓` に更新
