# てっぺき レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/てっぺき.md`）。docs/wiki/moves/てっぺき.html の情報を基に、
  基本情報・効果・判定フラグ（まもる/マジックコート/よこどり等）・エッジケースを記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/てっぺき.md`）。`からをやぶる`/`つるぎのまい` の計画書を参考に、
  ハンドラ構成・priority根拠・実装箇所・注意点を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `"てっぺき"` エントリを修正:
  - `pp=15` → `pp=16`（docs/champions/move_list.txt 基準。旧Gen9値からの移行漏れバグ）
  - `target="self"` を追加（欠落していたため、デフォルト値 `"foe"` により
    `is_blocked_by_protect` / `is_reflectable` が誤って `True` を返す状態だった。
    自分対象の変化技のため、まもる・マジックコートに反応してはいけない）
- `src/jpoke/handlers/move_status.py` の `てっぺき_modify_attacker_stats`（ぼうぎょ+2）は
  実装内容として問題なし。変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行「リーサル実装」列が `n/a`）。

## テスト
- `tests/moves_status/test_move_ta.py` に以下を追加:
  - `test_てっぺき_ぼうぎょ2段階上がる`: 使用後にぼうぎょランクが+2になることを確認
  - `test_てっぺき_自分対象のためまもるで防がれない`: 相手がまもる状態でも効果が発動することを確認
    （`target="self"` 修正の回帰防止）
  - `test_てっぺき_すでに最大なら失敗する`: ぼうぎょランクが+6のとき、ランクが変化しないことを確認
- `python -m pytest tests/ -v` を実行し、4253 passed, 1 skipped で全件パス
  （ログ: `.loop/test_logs/てっぺき.log`）
