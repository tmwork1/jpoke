# 3ぼんのや レビュー結果

## 仕様書
- `docs/spec/moves/` に個別の仕様書は存在しない。3ぼんのやの2つの追加効果
  （50%で相手の『ぼうぎょ』-1、30%でひるみ）は、他の同種の追加効果技
  （アイアンヘッド、かみなりのキバ など）と同様に `docs/spec/moves/_move_secondary_1.md`
  の一覧表で管理する方針であり、一次情報（Wiki）と照合した結果、既に正しく
  記載されていた（14行目・157行目に `３ぼんのや` として記載）。個別ファイルは
  作成しないという既存の方針を踏襲し、新規作成は行っていない。
- 急所ランク+1（急所に当たりやすい）は `MoveData.critical_rank` フィールドで
  表現される標準項目であり、個別のドキュメント化は不要（他の急所ランク技と同様）。

## 実装計画書
- `docs/plan/moves/3ぼんのや.md` は存在しない。同種の単純な追加効果技
  （アイアンヘッド、かみなりのキバ など）にも個別の計画書は存在せず、
  慣例に倣い新規作成はしていない。

## 実装（handlers/ / data/）
- **不備を発見し修正した**: 一次情報（Wiki）および `_move_secondary_1.md` によると
  3ぼんのやは「50%で相手の『ぼうぎょ』を1段階下げる」効果と「30%でひるませる」
  効果の**独立判定の2つの追加効果**を持つが、実装は「30%でひるみ」のみが
  登録されており、防御ダウン効果が完全に欠落していた。
- `src/jpoke/handlers/move_attack.py` に `_3ぼんのや_lower_defender_def`
  （`modify_defender_stats` で防御-1、chance=0.5）を追加した。
- `src/jpoke/data/move.py` の `"3ぼんのや"` エントリの `Event.ON_DAMAGE_HIT` を
  単一ハンドラから、防御ダウン→ひるみの順のリスト（`かみなりのキバ` などと
  同じ並び方）に変更した。
- `critical_rank=1`（急所に当たりやすい）は既に正しく実装されていたため変更なし。
- `python scripts/sort_handlers.py src/jpoke/handlers/move_attack.py` と
  `python scripts/sort_data/sort_moves.py` を実行済み。

## テスト
- `tests/moves_attack/test_move_a.py` に以下を追加した:
  - `test_3ぼんのや_ぼうぎょ低下が発動する`（新規実装した防御ダウン効果の確認）
  - `test_3ぼんのや_急所ランクが1`
  - `test_3ぼんのや_急所ランクが1_乱数大で急所なし`
  （急所ランク+1が未検証だったため、既存の `アクアカッター` 等と同様のテストを追加）
- 既存の `test_3ぼんのや_ひるみが発動する` は変更なし。
- `python scripts/sort_tests.py tests/moves_attack/test_move_a.py` でソート、
  `python scripts/generate_test_list.py` で一覧更新、
  `python -m pytest tests/ -v` で全3203件（1件skip、既存分）がパスすることを確認した。

## 進捗表
- `docs/progress/move.md` の3ぼんのや行を更新:
  - `PP` 列を誤記の `12` から一次情報どおりの `10` に修正
  - `レビュー` 列を `-` から `✓` に更新
