# タネばくだん レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/タネばくだん.md`）。`docs/wiki/moves/タネばくだん.html` を一次情報として、
  タイプ（くさ）・分類（物理）・威力80・命中100・PP16（チャンピオンズ基準）・非接触・弾（bullet）技・
  追加効果なしを記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/タネばくだん.md`）。ハンドラ不要で `MoveData` 登録のみで完結する方針を明記した。
- なお `docs/plan/moves/攻撃技_無追加効果系.md`（旧・15技一括の計画書）にも本技が含まれていたが、
  `labels=[...]`／カテゴリを日本語表記（`"物理"`）で記述するなど現行コードの表記（`flags={...}`・
  `category="physical"`）と乖離した古い記録であり、現行の `data/moves/move_ta.py` の実装とは既に一致していた
  （実質的にこの一括計画は完了扱いだったと判断）。本レビューでは対象外のため修正していない。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ta.py` の `"タネばくだん"` エントリは
  `type="くさ"` `category="physical"` `pp=16` `power=80` `accuracy=100` `flags={"bullet"}` `handlers={}`
  であり、一次情報・`src/jpoke/data/ps-champ-ja/moves.json`（PP16）と一致していることを確認した。
  追加効果がないため専用ハンドラも不要で、実装は仕様通り。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行でリーサル実装列・リーサルテスト列がともに `n/a`）。

## テスト
- `tests/moves_attack/test_move_ta.py` に `test_タネばくだん_くさタイプの物理弾技である`
  （type/category/power/pp/flags の直接検証）を追加した。既存の
  `test_タネばくだん_相手にダメージを与える`（ダメージが発生することの確認）は変更なし。
- `python -m pytest tests/ -v` を実行し、3843 passed, 1 skipped で全テスト成功を確認した
  （ログ: `C:\Users\tmtmp\Documents\pokemon\jpoke\.loop\test_logs\タネばくだん.log`）。
