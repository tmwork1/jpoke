# いたみわけ レビュー結果

## 仕様書
- 変更なし（既存の `docs/spec/moves/いたみわけ.md` は一次情報と一致しており誤り・欠落なし）

## 実装計画書
- 新規作成（`docs/plan/moves/いたみわけ.md` が存在しなかったため、仕様書と実装内容をもとに作成）

## 実装（handlers/ / data/）
- 変更なし。`handlers/move_status.py` の `いたみわけ_equalize_hp` と
  `data/moves/move_a.py` のエントリを仕様書と照合したが、以下の点はすべて既存実装で正しく成立していることを確認した
  - PP=20（`src/jpoke/data/ps-champ-ja/moves.json` と一致）
  - `accuracy` 未設定＝`None` により必中（第三世代以降の仕様と一致）
  - `target` 未設定＝デフォルト`"foe"`により相手1体を対象
  - `flags` 未設定により直接攻撃（`direct`）扱いにならない
  - 最大HPを超えないクランプは `battle.modify_hp` → `Pokemon.modify_hp` が汎用的に処理
  - まもる／みがわり／おうごんのからだ／マジックガード／かいふくふうじとの相互作用はすべて
    技共通のフレームワーク処理に委譲されており、本ハンドラでの重複実装は不要と確認

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行は「リーサル実装」「リーサルテスト」ともに `n/a`）

## テスト
- 追加: `tests/moves_status/test_move_a.py` に以下の3件を追加（既存の
  `いえき`/`いとをはく` の間、五十音順の位置に挿入）
  - `test_いたみわけ_HPを均等にする`: 自分と相手のHPが `(a+b)//2` に均等化されることを確認
  - `test_いたみわけ_端数は切り捨てられる`: 合計が奇数の場合に端数が切り捨てられることを確認
  - `test_いたみわけ_最大HPを超えて回復しない`: 種族値差の大きい相手（カビゴン）に対して使用した際、
    使用者（ピチュー）のHPが自身の最大HPを超えず頭打ちになることを確認
- 既存の `tests/volatiles/test_volatile_ka.py::test_かいふくふうじ_いたみわけは防がない` と
  `tests/abilities/test_ability_ma.py::test_マジックガード_いたみわけを受ける` は変更なし（内容は仕様と一致）
- `python -m pytest tests/ -v` で全4075件（+1 skipped）がパスすることを確認
  （ログ: `.loop/test_logs/いたみわけ.log`）
