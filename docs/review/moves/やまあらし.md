# やまあらし レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/やまあらし.md`）。
- 一次情報（`docs/wiki/moves/やまあらし.html`）と `docs/spec/critical.md` を基に、基本情報（タイプ:
  かくとう / 物理 / 威力60(第六世代以降) / 命中100 / PP10 / 接触あり）、必ず急所に当たる効果、
  判定フラグ（急所・命中判定・まもる・おうじゃのしるし・マジックコート・よこどり・オウムがえし）を記載。
- SVでは技マシン未収録で使用不可という一次情報の記載があるが、`docs/champions/move_list.txt` には
  登録されているため、本プロジェクト（ポケモンチャンピオンズシングルバトル）では使用可能技として扱う旨を明記。

## 実装計画書
- 新規作成（`docs/plan/moves/やまあらし.md`）。
- `docs/plan/moves/攻撃技_急所系.md`・`docs/plan/moves/あんこくきょうだ.md`・
  `docs/plan/moves/こおりのいぶき.md` の既存パターン（`critical_rank=3` のみで確定急所を表現し、
  専用ハンドラは不要）に倣って作成。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ya.py` の既存エントリ
  （`type="かくとう"`, `category="physical"`, `pp=10`, `power=60`, `accuracy=100`,
  `critical_rank=3`, `flags={"contact"}`, `handlers={}`）は仕様書・計画書と一致しており、
  修正不要と確認した。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）。

## テスト
- 変更なし。`tests/moves_attack/test_move_yarawa.py` の
  `test_やまあらし_確定急所`（急所ランク3のため `fix_random` の値によらず
  `battle.move_executor.critical is True` となることを確認）は、
  同種の確定急所技（`あんこくきょうだ`・`こおりのいぶき`）のテストと同一パターンで
  実装済みであり、修正不要と確認した。
- `python -m pytest tests/ -v` は 3620 passed, 1 skipped（既存の無関係なskip）で全件成功。
