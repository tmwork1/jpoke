# じならし レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/じならし.md`）
  - タイプ・分類・威力・命中・PP・優先度・接触なしを一次情報（`docs/wiki/moves/じならし.html`）に基づき記載
  - 追加効果（100%ですばやさ-1）・グラスフィールドでの威力半減・あなをほるに当たらない点を記載

## 実装計画書
- 新規作成（`docs/plan/moves/じならし.md`）
  - `がんせきふうじ` の計画書を参考に、`flags={"secondary_effect"}` 欠落バグの修正内容を記載
  - ハンドラ構成表・priority根拠・リーサル対象外の理由・review-test向けエッジケースを記載

## 実装（handlers/ / data/）
- `src/jpoke/data/move.py` の `"じならし"` エントリに `flags={"secondary_effect"}` を追加した
  - 欠落していたため、特性ちからずくによる威力1.3倍化・追加効果無効化が発動しない不具合があった
  - 同種の `あわ`・`からみつく`・`がんせきふうじ` は既に `flags={"secondary_effect"}` を持っており、
    じならしのみ欠落していた
  - `handlers/move_attack.py` の `じならし_lower_defender_spd`（`modify_defender_stats` 経由ですばやさ-1）は
    仕様と一致しており修正不要
  - グラスフィールドでの威力半減（`handlers/field.py` の `グラスフィールド_power_modifier`）、
    あなをほるに当たらない仕様（`handlers/volatile.py` の `あなをほる` ヒット対象リストに
    `じならし` が含まれない）はいずれも既存実装で対応済みで修正不要
  - `python scripts/sort_data/sort_moves.py` を実行済み

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）
  - 本技の追加効果はすばやさランク低下のみでHPに影響しないため、専用のリーサルハンドラは不要

## テスト
- `tests/moves_attack/test_move_sa.py` に以下を追加
  - `test_じならし_すばやさ低下が発動する`: 100%の確率ですばやさが1段階下がることを確認
  - `test_じならし_ちからずくで威力上昇しすばやさ低下は発動しない`: ちからずく所持時に威力補正が
    1.3倍（5325）になり、すばやさ低下が発動しないことを確認（`flags={"secondary_effect"}` 追加の回帰確認）
  - `python scripts/sort_tests.py tests/moves_attack/test_move_sa.py` でソート
  - `python scripts/generate_test_list.py` でテスト一覧を更新
  - `python -m pytest tests/ -v` で全3397件パス（1件skip）を確認（ログ: `.loop/test_logs/じならし.log`）
