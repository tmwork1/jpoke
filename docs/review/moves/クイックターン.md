# クイックターン レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/クイックターン.md`）。
  `docs/wiki/moves/クイックターン.html` の一次情報（タイプ:みず、物理、威力60、命中100、PP20、
  対象1体選択、優先度0、直接攻撃、急所通常判定、追加効果なし、まもるで防がれる、おうじゃのしるし対応、
  マジックコート対象外）と、ウィキが参照を指示する「とんぼがえり#技の仕様」の交代発動条件を反映した。
  クイックターン固有の記述として、Wiki内「特性かんそうはだ/ちょすい/よびみずで技が無効化されたときは
  交代できない」を明記した（みずタイプの技のため、とんぼがえり・ボルトチェンジの仕様書にはない条件）。

## 実装計画書
- 新規作成（`docs/plan/moves/クイックターン.md`）。
  既存の `とんぼがえり`・`ボルトチェンジ` と同じ共通ハンドラ `pivot`（`handlers/move_attack.py`）を
  `Event.ON_HIT` に登録済みであることを確認し、priority根拠・エッジケースを記載した。
  かんそうはだ/ちょすい/よびみずによる無効化は既存の `_apply_type_absorb` 共通処理が
  `Event.ON_HIT` の発火自体を止めるため、本技固有の実装は不要と判断した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ka.py` の `クイックターン` エントリ
  （type=みず, category=physical, pp=20, power=60, accuracy=100, flags={"contact"},
  handlers={Event.ON_HIT: pivot}）は仕様書・`src/jpoke/data/ps-champ-ja/moves.json`
  （`クイックターン みず 物理 60 100 20 接○ 守○`）と一致しており、修正不要だった。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a）。

## テスト
- 新規追加（`tests/moves_attack/test_move_ka.py`）。とんぼがえりの既存テストパターンに倣い、以下7件を追加：
  - `test_クイックターン_ダメージを与える`
  - `test_クイックターン_まもるで防がれた場合は交代しない`
  - `test_クイックターン_命中しなかった場合は交代しない`
  - `test_クイックターン_控えがいない場合は交代しない`
  - `test_クイックターン_攻撃後に交代可能状態になる`
  - `test_クイックターン_相手を倒した場合でも交代する`
  - `test_クイックターン_ちょすいで無効化されると交代しない`（みずタイプ技固有のケースとして新規追加）
  - `Interrupt` のインポートを追加。
  - `python -m pytest tests/ -v` で全3728件成功（1件skip、無関係）を確認。
