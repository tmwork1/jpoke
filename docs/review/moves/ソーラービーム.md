# ソーラービーム レビュー結果

## 仕様書
- 変更なし（既存の `docs/spec/moves/ソーラービーム.md` は一次情報（`docs/wiki/moves/ソーラービーム.html`）と照合済みで、天候による溜めスキップ・威力半減・ばんのうがさ/ノーてんき/エアロックとの関係を含め誤り・欠落は無かった）

## 実装計画書
- 新規作成（`docs/plan/moves/ソーラービーム.md`）
  - 既存実装のハンドラ構成・priority根拠を整理して明記
  - PPバグ（後述）を計画書内に記録

## 実装（handlers/ / data/）
- 修正: `src/jpoke/data/moves/move_sa.py` の `ソーラービーム` の `pp` を `10`（Gen9本家基準）から `12`（`src/jpoke/data/ps-champ-ja/moves.json` 準拠）に修正
  - `docs/champions/changes_from_sv.md` の PP統一ルール（8/12/16/20）への移行漏れバグ
  - `ソーラーブレード`（同ファイル内・pp=10のまま）は今回のレビュー対象外のため未修正（別レビューで対応予定）
- その他のハンドラ（`ソーラービーム_charge` / `ソーラービーム_weather_skip` / `ソーラービーム_halve_power`、`data/volatile.py` の `ソーラービーム` 揮発状態、`handlers/volatile.py` の `ソーラービーム_remove_volatile`）は仕様書・計画書と照合の上、誤り・欠落なしと確認（変更なし）
  - にほんばれ/おおひでり下の溜めスキップ、あめ/おおあめ/すなあらし/ゆき下の威力半減（らんきりゅうは対象外）、ばんのうがさ・ノーてんき/エアロックとの相互作用、パワフルハーブとの優先順位（priority=90で天候スキップを先に判定）はすべて既存実装で正しく担保されていることを確認

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`）

## テスト
- 追加: `tests/moves_attack/test_move_sa.py` に以下を新規追加（従来はソーラービーム本体の攻撃技テストが皆無で、`tests/items/test_item_ha.py` のパワフルハーブ絡みのテストのみ存在していた）
  - `test_ソーラービーム_1ターン目は溜めてダメージを与えない`
  - `test_ソーラービーム_2ターン目に攻撃して揮発状態が解除される`
  - `test_ソーラービーム_はれ_おおひでり下では溜めずに1ターンで攻撃する`（パラメタライズ）
  - `test_ソーラービーム_あめ_おおあめ_すなあらし_ゆき下では威力が半減する`（パラメタライズ、`damage_roll="最大"` + `critical_mode="確定のみ"` で決定的に威力比0.5を検証）
  - `test_ソーラービーム_らんきりゅう下では威力が変化しない`
- `python -m pytest tests/ -v` は最終的に全3851件成功・1件skip
  - 1回目の実行では `test_むしくい_ねんちゃく持ちをひんしにさせた場合は奪える`（乱数依存のまひ判定に起因する既知のフレーキーテスト）が偶発的に失敗したが、単体実行では再現せず、2回目のフルスイート実行でも成功したため、本レビューの変更とは無関係と判断
