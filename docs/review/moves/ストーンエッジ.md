# ストーンエッジ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ストーンエッジ.md`）。一次情報（docs/wiki/moves/ストーンエッジ.html）より
  タイプ・威力・命中・急所ランク+1・追加効果なし・接触なし等を記載。
  PPは本家基準5、チャンピオンズでは4段階統一ルールにより8であることを明記。

## 実装計画書
- 新規作成（`docs/plan/moves/ストーンエッジ.md`）。既存実装との差分として PP の移行漏れバグを指摘。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_sa.py` の `"ストーンエッジ"` エントリの `pp` を `5` から `8` に修正。
  （`src/jpoke/data/ps-champ-ja/moves.json` 410行目「ストーンエッジ	いわ	物理	100	80	8	接×	守○」および
  `docs/champions/changes_from_sv.md` の全技PP4段階統一ルールに基づく移行漏れバグ）
  それ以外の項目（type, category, power, accuracy, critical_rank, target, flags）は仕様通りで変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`。追加効果を持たない通常の
  物理攻撃技のためリーサル計算ハンドラは不要）。

## テスト
- `tests/moves_attack/test_move_sa.py` にテストを新規追加（既存テストは存在しなかった）。
  - `test_ストーンエッジ_急所ランクが1`
  - `test_ストーンエッジ_急所ランクが1_乱数大で急所なし`
  - `test_ストーンエッジ_相手にダメージを与える`
- `python -m pytest tests/ -v` 実行し 3832 passed, 1 skipped（スキップは既存の無関係なテスト）で全テスト成功。
  ログは `C:\Users\tmtmp\Documents\pokemon\jpoke\.loop\test_logs\ストーンエッジ.log` に保存。
