# いとをはく レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/いとをはく.md` は未存在だったため、一次情報（`docs/wiki/moves/いとをはく.html`）をもとに作成した）
- タイプ: むし / 分類: 変化 / 命中率95 / 相手のすばやさランクを2段階下げる（第五世代までは1段階）ことを明記
- PP はポケモンチャンピオンズ準拠で20（Gen9本家は40）である旨を明記
- まもる○・マジックコート○・直接攻撃×を記載
- コンテスト効果・バトルパレス判定・Zワザ付加効果は対象範囲外として明記

## 実装計画書
- 既存の `docs/plan/moves/いとをはく.md` は内容が実装と食い違っていたため修正した
  - PP を40→20に修正（`docs/champions/move_list.txt` 準拠）
  - ハンドラ関数名を計画時の想定 `いとをはく_modify_defender_stats` に合わせて実装側も統一（後述）
  - stats キーを `{"S": -2}`（誤り）から `{"spe": -2}`（`Stat` 型準拠）に修正
  - subject_spec/priority の根拠説明を実際の同種ハンドラ（`あまえる_modify_defender_stats` 等）を参照する形に更新
  - まもる・マジックコートは `modify_defender_stats` / `マジックコート_reflect` 側で汎用的に処理されるため個別対応不要である旨を明記

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py`: `いとをはく` の `pp` を40→20に修正
  （`docs/champions/move_list.txt` は20。Gen9本家の40がそのまま移行されていたバグ）
- `src/jpoke/handlers/move_status.py`: 関数名を `いとをはく_reduce_defender_spe` から
  `いとをはく_modify_defender_stats` にリネーム（他の同種ハンドラの命名規則
  `<技名>_modify_defender_stats` に統一。ロジック自体（`stats={"spe": -2}`）は元々正しかった）
- `data/moves/move_a.py` の登録箇所も新関数名に合わせて修正

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`。変化技のためダメージ計算に関与しない）

## テスト
- 既存の `tests/moves_status/test_move_a.py::test_いとをはく_すばやさ2段階下がる` を確認し、
  内容は仕様（すばやさ2段階ダウン）と一致していたため変更なし
- PP修正・関数名リネームに伴う既存テストへの影響はなし（`python -m pytest tests/ -v` で全件パス、
  4083 passed, 1 skipped）
