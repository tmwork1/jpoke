# どくのこな レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/どくのこな.md`）。一次情報（`docs/wiki/moves/どくのこな.html`）を
  もとに、基本情報・効果（どく状態付与、粉技によるくさタイプ／ぼうじん／ぼうじんゴーグル無効）・
  判定フラグ（まもる○、マジックコート○、オウムがえし○、追加効果×等）を記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/どくのこな.md`）。既存実装の構成（`ON_STATUS_HIT` で
  `apply_ailment_to_defender` を呼び出す）を記録し、PP値の不整合（下記）を明記した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `どくのこな` エントリで `pp=35`（Gen9本家基準）に
  なっていたバグを修正し、`src/jpoke/data/ps-champ-ja/moves.json` に準拠した `pp=20` に修正した。
- `handlers/move_status.py` の `どくのこな_apply_ailment_to_defender` はハンドラの実装として
  仕様と一致しており修正なし（`ON_STATUS_HIT` で `ctx.defender` にどく状態を付与する）。
- `powder` フラグはくさタイプ・特性『ぼうじん』・道具『ぼうじんゴーグル』への無効化を
  `core/move_executor.py` / `handlers/ability.py` / `handlers/item.py` が汎用的に処理しており、
  技側の追加実装は不要（既存実装のまま問題なし）。
- 対象は相手1体（`MoveData.target` のデフォルト値 `"foe"`）であり、`target="self"` 欠落の
  問題は該当しない。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`。変化技で確定ダメージなし）。

## テスト
- `tests/moves_status/test_move_ta.py` に以下を追加した（`どくのいと` テストの後、
  `どくびし` テストの前に挿入）。
  - `test_どくのこな_どく状態にする`
  - `test_どくのこな_命中率75で外れることがある`
  - `test_どくのこな_くさタイプには無効`（粉技によるくさタイプ無効化の確認）
- `python -m pytest tests/ -v` で全4282件（+スキップ1件）がパスすることを確認し、
  `.loop/test_logs/どくのこな.log` に保存した。
