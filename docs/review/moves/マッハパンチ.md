# マッハパンチ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/マッハパンチ.md`）。一次情報（`docs/wiki/moves/マッハパンチ.html`）と
  類似のパンチ優先技である `バレットパンチ.md` の形式を踏襲。
  タイプ: かくとう / 分類: 物理 / 威力40 / 命中100 / 優先度+1 / 接触あり / パンチ技あり を記載。
  PPは本家30・champions基準20の両方を明記（champions側を正とする注記付き）。

## 実装計画書
- 新規作成（`docs/plan/moves/マッハパンチ.md`）。追加効果を持たないため専用ハンドラは不要で、
  `MoveData(priority=1, flags={"contact", "punch"})` のみで仕様を満たすことを明記。
  Priority根拠・リーサル計算（n/a）・エッジケース（てつのこぶし補正は特性側で処理）を記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ma.py` の `"マッハパンチ"` エントリで `pp=30`（Gen9本家値）に
  なっていたバグを修正し、`pp=20`（`src/jpoke/data/ps-champ-ja/moves.json` 準拠）に変更した。
- その他（type, category, power, accuracy, priority, flags）は仕様と一致しており修正なし。
- `handlers/`側の変更なし（追加効果がないため専用ハンドラ不要、`handlers={}` のままで正しい）。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a。追加効果のない固定威力技のため
  専用のリーサル計算ハンドラは不要）。

## テスト
- `tests/moves_attack/test_move_ma.py` に
  `test_マッハパンチ_素早さで劣っていても先制する` を追加。
  素早さで劣るカビゴンがマッハパンチで、素早さに勝るピカチュウ（HP1）を優先度+1により
  先に倒し、ピカチュウが行動できずカビゴンがダメージを受けないことを検証する。
  優先度+1の効果を確認するテストがこれまで存在しなかったため補完した。
- 既存の `test_マッハパンチ_相手にダメージを与える`（基本ダメージ確認）、
  `tests/abilities/test_ability_ta.py` の `test_てつのこぶし_パンチ技以外は補正なし`
  （パンチ技威力補正の確認）は変更なし。
- `python -m pytest tests/ -v` で全4010件パス（1件skip）を確認した。
