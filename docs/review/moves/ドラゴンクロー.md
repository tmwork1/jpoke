# ドラゴンクロー レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ドラゴンクロー.md`）。一次情報（Wiki）と `src/jpoke/data/ps-champ-ja/moves.json`、
  `docs/champions/changes_from_sv.md` を基に、基本情報・急所ランク+1・切る技（slashフラグ、きれあじ対象）
  を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/ドラゴンクロー.md`）。既存実装との差分（`critical_rank` 欠落、
  `slash` フラグ欠落）を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `"ドラゴンクロー"` エントリを修正した。
  - `critical_rank=1` を追加（一次情報「急所に当たりやすい」に対応する急所ランク補正が欠落していた）。
  - `flags` に `slash` を追加（`docs/champions/changes_from_sv.md` に「ドラゴンクロー　切る技(切り技)に
    追加」と明記されており、Championsではきれあじの対象。既存実装は `contact` のみで
    `きれあじ_modify_power` の `has_flag("slash")` 判定が通らず、威力補正が発動しない不備があった）。
  - PPは既に16でチャンピオンズ基準通り正しかったため変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）。

## テスト
- `tests/moves_attack/test_move_ta.py` の `test_ドラゴンクロー_相手にダメージを与える` に加えて、
  以下を新規追加した（`シャドークロー` の既存テストと同一パターン）。
  - `test_ドラゴンクロー_PPは16`
  - `test_ドラゴンクロー_きれあじで威力1_5倍`
  - `test_ドラゴンクロー_急所ランクが1`
  - `test_ドラゴンクロー_急所ランクが1_乱数大で急所なし`
- `python -m pytest tests/ -v` で全件（3921件）パスを確認（`.loop/test_logs/ドラゴンクロー.log`）。
