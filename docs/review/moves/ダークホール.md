# ダークホール レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ダークホール.md`）。
- `docs/wiki/moves/ダークホール.html` を一次情報として、基本情報（あく・変化・命中率50・PP10・
  相手全員/シングルでは相手1体）と、第七世代以降の種族制限（ダークライ以外が使用すると必ず失敗す
  る）、マジックコートで跳ね返された場合は種族制限を受けない旨、へんしん・ものまね・イリュージョン
  との相互作用をまとめた。
- `src/jpoke/data/ps-champ-ja/moves.json` に本技の記載がないこと（ダークライが本プロジェクトの `POKEDEX` に
  未収録のため）を明記し、PP・命中率は第9世代の値をそのまま採用する方針とした。

## 実装計画書
- 新規作成（`docs/plan/moves/ダークホール.md`）。
- `ON_TRY_MOVE_1`（priority=30、`docs/spec/turn.md` の「とっておき: 未使用技がある」と同枠の使用者
  自身の前提条件チェック）で種族チェックを行い、`ON_STATUS_HIT`（デフォルトpriority）でねむり状態を
  付与する2ハンドラ構成とした。
- マジックコート反射時に種族チェックが再評価されないこと（`ON_CHECK_REFLECT` は `ON_TRY_MOVE_1` より
  後段のため）、へんしん・イリュージョンとの整合性（`Pokemon.name` が種族名を正しく反映するため追加
  実装不要）を根拠とともに記載した。

## 実装（handlers/ / data/）
- 修正した。既存実装は `handlers={}`（コメント「追加効果なし」）という誤った空実装だったため、以下を
  追加・修正した。
  - `src/jpoke/handlers/move_status.py`: `ダークホール_check_species`（種族制限チェック、失敗時
    `LogCode.MOVE_FAILED` を記録）と `ダークホール_apply_sleep`（`apply_ailment_to_defender` で相手
    をねむり状態にする）を追加。
  - `src/jpoke/data/moves/move_ta.py`: `ダークホール` の `MoveData.handlers` に
    `Event.ON_TRY_MOVE_1`（priority=30）と `Event.ON_STATUS_HIT` を登録。
  - PP=10・命中率=50・タイプ=あく・分類=status は元々正しかったため変更なし。

## リーサル計算
- 対象外（`n/a`）。ダメージを与えない変化技のため。

## テスト
- 追加した。`tests/moves_status/test_move_ta.py` に
  `test_ダークホール_ダークライ以外が使用すると失敗する` を追加し、非ダークライ使用時に失敗して
  ねむり状態が付与されないことを確認した。
- 本プロジェクトの `POKEDEX` にダークライが未収録のため、成功ケース（ダークライ使用時にねむり状態を
  付与すること）は現時点でテストできない。ダークライ実装後に追加テストが必要（計画書にも明記）。
- `python -m pytest tests/ -v` で全4522件パス（1件skip、ダークホールとは無関係の既存skip）を確認し、
  ログを `.loop/test_logs/ダークホール.log` に保存した。
