# たきのぼり レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/たきのぼり.md`）。
  一次情報（`docs/wiki/moves/たきのぼり.html`）に基づき、みずタイプ・物理・威力80・命中100・PP15・
  追加効果20%ひるみ（第四世代以降）・接触技であることを明記。
  判定フラグ（急所/命中判定/追加効果/まもる/おうじゃのしるし/マジックコート/よこどり/オウムがえし/ちからずく）は
  既存の `ずつき.md` の記載パターンに準拠して整理した。

## 実装計画書
- 新規作成（`docs/plan/moves/たきのぼり.md`）。
  既存実装（`data/move.py` の `"たきのぼり"` エントリ、`handlers/move_attack.py` の
  `たきのぼり_apply_flinch`）は仕様と一致しており、変更不要であることを確認して記録した。

## 実装（handlers/ / data/）
- 変更なし。
  `data/move.py` の `"たきのぼり"` エントリ（`type="みず", category="physical", pp=15, power=80,
  accuracy=100, flags={"contact", "secondary_effect"}`）、
  `handlers/move_attack.py` の `たきのぼり_apply_flinch`
  （`apply_volatile_to_defender(..., volatile="ひるみ", chance=0.2)`）はいずれも仕様通り。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`。ひるみ付与のみでHP分布に
  影響しない追加効果のため）。

## テスト
- `tests/moves_status/test_move_ta.py` に誤って重複登録されていた
  `test_たきのぼり_ひるみが発動する`（たきのぼりは物理攻撃技であり変化技ではないため誤配置）を削除した。
  `tests/moves_attack/test_move_ta.py` 側の同名テスト（`secondary_chance=1.0` を使用）が
  正しい配置であり、そちらは変更なし。
- `python scripts/sort_tests.py tests/moves_status/test_move_ta.py tests/moves_attack/test_move_ta.py`
  でソート、`python scripts/generate_test_list.py` でテスト一覧を更新。
- `python -m pytest tests/ -v` で全3415件パス（1件skip）。
  結果は `.loop/test_logs/たきのぼり.log` に保存。

## 進捗ファイル
- `docs/progress/move.md` の該当行を更新:
  - 仕様書列: `-` → `✓`
  - 計画書列: `-` → `✓`
  - レビュー列: `-` → `✓`
  - PP列: `16`（誤り） → `15`（実装・仕様と一致する正しい値）
  - 効果列: `？` → `追加効果として、20%の確率で相手をひるませる。`
