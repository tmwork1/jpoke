# ミサイルばり レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ミサイルばり.md`）。一次情報（`docs/wiki/moves/ミサイルばり.html`）を
  参照し、タイプ（むし）・分類（物理）・威力（25、第六世代以降）・命中率（95、第六世代以降）・
  PP（20）・対象（相手1体）・接触なし・2-5回連続攻撃技・追加効果なし・急所各ヒット独立判定・
  おうじゃのしるし発動・まもる有効・オウムがえし対象などを記載した。
- 既存の `docs/spec/moves/_multi_hit.md` §9.3（2-5回攻撃技グループ）の記載と整合していることを確認した。

## 実装計画書
- 新規作成（`docs/plan/moves/ミサイルばり.md`）。`みだれひっかき` の計画書を参考に、
  既存実装（`multi_hit` による汎用機構利用、固有ハンドラなし）が仕様と一致していることを明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ma.py` の `ミサイルばり` エントリ
  （type=むし, category=physical, pp=20, power=25, accuracy=95,
  multi_hit={min:2, max:5, check_hit_each_time:False, power_sequence:()}, handlers={}）は
  仕様書・`src/jpoke/data/ps-champ-ja/moves.json`（威力25・命中95・PP20）と一致しており、修正不要と判断した。
  `flags` に `contact` を含めない点も「接触なし」の仕様と一致している。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a）。

## テスト
- 変更なし。`tests/moves_attack/test_move_ha.py` の
  `test_ミサイルばり_複数ヒットする` が既に存在し、`_resolve_hit_count` の戻り値が2〜5の範囲に
  収まることを確認している（`みだれひっかき`・`スイープビンタ` と同一パターン）。追加の観点なしと判断した。
- `python -m pytest tests/ -p no:randomly` で全件パス（4009 passed, 1 skipped）を確認した。
  （`-p no:randomly` を外すと `pytest-randomly` のシード順序に起因する既存の無関係な
  フレーキーテスト（だっしゅつパック／だっしゅつボタン／むしくい／field 系）が発生することがあるが、
  ミサイルばりとは無関係の既知の問題であるため対象外とした。）
