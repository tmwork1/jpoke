# アクアテール レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/アクアテール.md`）。ポケモンWiki一次情報（docs/wiki/moves/アクアテール.html）に基づき、
  みずタイプ・物理・威力90・命中90・PP12（チャンピオンズ基準。`src/jpoke/data/ps-champ-ja/moves.json` 準拠、Gen9本家は10）・
  対象1体・優先度0・接触あり・追加効果なし、として記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/アクアテール.md`）。追加効果を持たないため専用ハンドラは不要であり、
  `handlers={}` のみで基本ダメージ計算に委ねる方針を記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_a.py` の `"アクアテール"` エントリは
  `type="みず"`, `category="physical"`, `pp=12`, `power=90`, `accuracy=90`, `flags={"contact"}`,
  `handlers={}` であり、仕様書・チャンピオンズのPP値（12）と完全に一致していた。修正不要。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）。追加効果のない通常攻撃技のため、
  専用のリーサル計算ハンドラは不要。

## テスト
- 変更なし。`tests/moves_attack/test_move_a.py` の `test_アクアテール_相手にダメージを与える` は
  カメックス対カビゴンでアクアテールを撃ちダメージが与えられることを確認しており、
  仕様（追加効果なしの通常攻撃）と一致していた。修正不要。
