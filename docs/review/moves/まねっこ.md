# まねっこ レビュー結果

## 仕様書
- 変更なし。`docs/spec/moves/まねっこ.md` は一次情報（`docs/wiki/moves/まねっこ.html`）と照合し、タイプ・分類・威力・命中率・PP・範囲・優先度・直接攻撃・効果文・技の仕様（コピー不可技の判定ルール・選ばれる技のルール）が全て一致していることを確認した。誤り・欠落なし。

## 実装計画書
- `docs/plan/moves/まねっこ.md` を実装・一次情報と照合し、以下を修正した。
  - `core/move_executor.py` の `last_used_move_name` 更新タイミングの記述が誤っていた（`ON_TRY_MOVE_1` 通過後と記載されていたが、実装は `ON_TRY_MOVE_2` 通過後・`ON_PAY_HP` 発火前）。実装に合わせて修正。
  - 「コピー禁止技の除外」節を新設し、Wikiの詳細表（×表記技一覧）と実装の対応関係（`non_negoto` フラグによる記録スキップ／`protect` フラグ／個別 `non_copycat` フラグの3経路）を明記した。
  - 「既知の限界」として曖昧なままだった `non_copycat` の適用範囲を、実際に洗い出して個別付与する形に更新し、限界の記述を削除・実態に合わせて書き換えた。

## 実装（handlers/ / data/）
- `handlers/move_status.py` の `まねっこ_can_use` を修正し、`non_copycat` フラグに加えて `protect` フラグ（守る系技）も判定するようにした。まもる・みきり・こらえる・キングシールド・ニードルガード・トーチカ・かえんのまもりは、Wikiの詳細表で全世代 × と明記されているにもかかわらず、修正前はコピー可能になっていたバグを修正。
- Wikiの詳細表（第九世代列）で × と明記されているにもかかわらず `non_copycat` / `non_negoto` / `protect` のいずれのフラグも持たず、まねっこでコピー可能になってしまっていた実装済み技に `non_copycat` フラグを追加した:
  - `ふきとばし`（move_ha.py）
  - `ほえる`（move_ha.py）
  - `ともえなげ`（move_ta.py）
  - `ドラゴンテール`（move_ta.py）
  - `トリック`（move_ta.py）
  - `すりかえ`（move_sa.py）
  - `どろぼう`（move_ta.py）
  - `ほしがる`（move_ha.py）
  - `へんしん`（move_ha.py、未実装だが将来実装時のために付与）
  - `みちづれ`（move_ma.py）
  - `わるあがき`（move_symbol.py）
  - `きょじゅうざん`（move_ka.py。きょうだい技の`きょじゅうだん`は既に付与済みだった）
  - `このゆびとまれ`（move_ka.py）
- 上記の修正作業中に発見した、まねっことは無関係の既存データ不整合も同じ箇所の編集ついでに修正した（champions基準PPからの移行漏れ）:
  - `どろぼう`: PP 25 → 20（`src/jpoke/data/ps-champ-ja/moves.json` 準拠）
  - `ほしがる`: PP 25 → 20（同上）
  - `みちづれ`: PP 5 → 8（同上）
  - なお `docs/progress/move.md` の `どろぼう` / `ほしがる` / `みちづれ` 行は指示によりまねっこ以外の行を編集しない方針のため未更新のまま。PP列が実装（20 / 20 / 8）と食い違って見える点は次回それらの技がレビュー対象になった際に追随修正が必要。
- `おしゃべり`・`トラップシェル`・`キングシールド`（世代9列が `-` 表記でコピー可否の確証が得られなかった技）は今回のフラグ付与を見送った（キングシールドは `protect` フラグで別途カバー済みのため実質的に問題なし）。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- `tests/moves_status/test_move_ma.py` に以下のテストを追加した。
  - `test_まねっこ_まもるはコピーできない`（新設した `protect` フラグ判定の回帰テスト）
  - `test_まねっこ_トリックはコピーできない`（新規追加した `non_copycat` フラグの回帰テスト）
  - `test_まねっこ_すりかえはコピーできない`（同上）
- `tests/moves_attack/test_move_ha.py` の `test_ほしがる_タイプ威力命中PPが仕様通り` を PP20 の期待値に修正（champions基準PP修正に追随）。
- `python -m pytest tests/ -v` で全4415件パス（1件skip）を確認し、結果を `.loop/test_logs/まねっこ.log` に保存した。
