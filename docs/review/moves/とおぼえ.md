# とおぼえ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/とおぼえ.md`）
  - タイプ/分類/威力/命中率/範囲/優先度/直接攻撃/判定を一次情報（`docs/wiki/moves/とおぼえ.html`）と `docs/champions/move_list.txt` から整理
  - PPは `docs/champions/move_list.txt` 準拠で20（Gen9本家は40）
  - 対象は第8世代以降「味方全員」だが、本プロジェクトはシングルバトルのみ対象のため実質「自分」のみである旨を明記
  - `まもる`／`マジックコート` に影響されない（自分が対象のため）ことを明記

## 実装計画書
- 既存の `docs/plan/moves/とおぼえ.md` を修正
  - PPを40→20に修正（`docs/champions/move_list.txt` 準拠）
  - `MoveData` に `target="self"` を追加する記載を追加（欠落するとまもる・マジックコートの判定が誤る旨を明記）
  - ハンドラのstatsキー表記を誤り（`{"A": 1}`）から実装と一致する `{"atk": 1}` に修正
  - 実装状況表を「未実装」から「実装済み」に更新

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `"とおぼえ"` エントリを修正
  - `pp=40` → `pp=20`（`docs/champions/move_list.txt` の値に合わせて修正。Gen9本家値からの移行漏れバグ）
  - `target="self"` を追加（欠落していたため `MoveData.target` のデフォルト値 `"foe"` が適用され、`Move.is_blocked_by_protect` / `Move.is_reflectable` が誤ってTrueを返し、まもる・マジックコートに誤反応する不具合があった）
- `src/jpoke/handlers/move_status.py` の `とおぼえ_modify_attacker_stats` は `stats={"atk": 1}` で実装済みであり、変更なし

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a のため変化技として対象外）

## テスト
- `tests/moves_status/test_move_ta.py` に以下を追加
  - `test_とおぼえ_相手のまもる状態に影響されず成功する`: 相手が `まもる` 状態でも自分の「こうげき」が1段階上がることを確認（`target="self"` 修正の回帰テスト）
  - `test_とおぼえ_マジックコートで跳ね返されない`: 相手が `マジックコート` 状態でも跳ね返されず自分の「こうげき」が1段階上がることを確認（`target="self"` 修正の回帰テスト）
- 既存の `test_とおぼえ_こうげき1段階上がる` は変更なし
- `python -m pytest tests/ -v` は 4260 passed, 1 skipped（ログ: `.loop/test_logs/とおぼえ.log`）
