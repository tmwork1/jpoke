# くろいきり レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/くろいきり.md`）。
  - `docs/wiki/moves/くろいきり.html` の「技の仕様」節を参照し、以下を明記した。
    - PP は `docs/champions/move_list.txt` 準拠で 20（実装の旧値 30 は誤り、後述の実装修正で対応）。
    - 対象は「全体の場」で、みがわり状態・姿を隠しているポケモンのランクもリセット対象。
    - しろいきり（場の状態）・クリアボディ等のランク低下防止特性を無視してリセットする。
    - あまのじゃく／たんじゅんの影響を受けず、まけんき／かちき／びんじょう／
      ものまねハーブ／しっとのほのお／うっぷんばらしも発動しない。
    - きゅうしょランクはリセット対象外。
    - 第一世代限定の状態異常回復効果は対象世代外のため実装しない旨を明記。

## 実装計画書
- 既存の `docs/plan/moves/くろいきり.md` を仕様書と実装の実態に合わせて修正した。
  - PP を 30→20 に修正（`docs/champions/move_list.txt` 準拠）。
  - 「未実装」と誤記されていた実装状況を「実装済み」に修正（`handlers/move_status.py` の
    `くろいきり_reset_all_ranks` と `data/moves/move_ka.py` への登録はすでに存在していた）。
  - コード例のログ出力を実装済みの `StatChangePayload(stats=..., display_reason=...)` 形式に修正
    （旧記述は存在しない `dict` 直書き形式だった）。
  - `Stat` リテラルの表記を実装と一致する `"hp", "atk", "def", "spa", "spd", "spe", "accuracy",
    "evasion"` に修正（旧記述は存在しない `"H", "A", "B", ...` 表記だった）。
  - あまのじゃく／たんじゅん・ランク変化検知系特性の非発動に関する注意点を追記。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ka.py` の `くろいきり` エントリで PP が 30 になっていたバグを修正し、
  `docs/champions/move_list.txt` 準拠の 20 に修正した。
- `handlers/move_status.py` の `くろいきり_reset_all_ranks` 自体は仕様通りで変更なし
  （`battle.actives` を直接走査し `Pokemon.rank` を直接書き換えることで、しろいきり・
  ランク低下防止特性・あまのじゃく等を自然にバイパスする設計は妥当）。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`。くろいきりはダメージを与えない
  変化技のためリーサル計算の対象外）。

## テスト
- `tests/moves_status/test_move_ka.py` に `test_くろいきり_しろいきりを無視してリセットする` を追加し、
  相手側が `しろいきり` 状態でもランクリセットが機能することを確認した。
- 既存の `test_くろいきり_使用者のランクがゼロにリセットされる` /
  `test_くろいきり_双方のランクが同時にリセットされる` は実装と一致しており変更なし。
- `python -m pytest tests/ -v` を実行し、4149 件パス（1 skip）を確認
  （ログ: `.loop/test_logs/くろいきり.log`）。
