# ずつき レビュー結果

## 仕様書

- `docs/spec/moves/ずつき.md` が存在しなかったため、`docs/wiki/moves/ずつき.html` を参照して新規作成した。
  - タイプ: ノーマル / 分類: 物理 / 威力: 70 / 命中: 100 / PP: 15 / 対象: 相手1体 / 優先度: 0 / 接触あり
  - 追加効果: 30%の確率で相手をひるませる
  - `docs/champions/move_list.txt` に本技のエントリが存在しない点を明記した
    （しねんのずつき・もろはのずつきは掲載されているが、ずつき単体は掲載なし。本家のPP15をそのまま採用）

## 実装計画書

- `docs/plan/moves/ずつき.md` が存在しなかったため、既存実装（`しねんのずつき` の計画書を参考形式）に
  倣って新規作成した。既存実装は仕様と完全に一致しており、修正は不要と結論づけた。

## 実装（handlers/ / data/）

- 変更なし。
  - `src/jpoke/data/move.py` の `"ずつき"` エントリ: `type="ノーマル", category="physical", pp=15,
    power=70, accuracy=100, flags={"contact", "secondary_effect"}` で仕様と一致。
  - `src/jpoke/handlers/move_attack.py` の `ずつき_apply_flinch`:
    `apply_volatile_to_defender(battle, ctx, value, volatile="ひるみ", chance=0.3)` で
    30%ひるみ付与の仕様と一致。`resolve_secondary_chance` 経由でちからずくにも対応済み。

## リーサル計算

- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`。ひるみ付与のみでHP分布に影響しないため）。

## テスト

- `tests/moves_attack/test_move_sa.py::test_ずつき_ひるみが発動する` が既に存在し、
  30%発動の仕様と実装内容に一致していることを確認した（修正不要）。
- `docs/progress/move.md` の `ずつき` 行が実態と乖離していた
  （タイプ=くさ・威力=- ・命中率=- ・対象=attacker・仕様書/計画書/レビュー列が未更新の誤ったプレースホルダー）ため、
  正しい値（タイプ=ノーマル・威力=70・命中率=100・対象=defender・仕様書=✓・計画書=✓・レビュー=✓・効果欄）に修正した。
