# あまごい レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/あまごい.md`）
- 一次情報（`docs/wiki/moves/あまごい.html`）を基に、タイプ/分類/威力/命中率/範囲/優先度/直接攻撃/判定を記載
- PPは一次情報の5ではなく、champions/move_list.txt準拠の8を採用し、その旨を明記
- あめの一般効果（威力補正・しめったいわ延長・強天候上書き不可等）は `docs/spec/fields/あめ.md` を参照する形にし、あまごい固有の実装が不要である点を明記
- Zワザによる付加効果（すばやさ+1）はポケモンチャンピオンズにZワザが存在しないため対象外と明記

## 実装計画書
- PP を 5 → 8（champions/move_list.txt準拠）に修正
- ハンドラのシグネチャを `ctx: EventContext` → `ctx: AttackContext` に修正
  （実装済みコードは `AttackContext` を使用しており、`EventContext` には `attacker` 属性が存在しないため、計画書の記載が誤りだった）
- `data/move.py` → 実際のファイルパス `data/moves/move_a.py` に修正し、`category="変化"` → `category="status"`、`pp=5` → `pp=8` に修正

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `あまごい` エントリで `pp=5` となっていたのを `pp=8`（champions/move_list.txt準拠）に修正
- `src/jpoke/handlers/move_status.py` の `あまごい_activate_weather` は仕様通りで変更なし
  （`battle.weather_manager.apply("あめ", 5, source=ctx.attacker)` を返すのみ、AttackContext/attacker:self の使用も既存の天候技パターンと一致）

## リーサル計算
- 対象外（`docs/progress/move.md` の リーサル実装列が `n/a`。ダメージを与えない変化技のため）

## テスト
- `tests/moves_status/test_move__grouped.py` に `test_天候始動技_すでに同じ天候なら失敗`
  （にほんばれ・あまごいをパラメータ化）を追加し、既に同じ天候が有効な場合に再発動しない
  （カウントが変化しない）ことを検証
- 既存の `test_あまごい_天気があめになる`（発動確認）、
  `test_天候始動技_強天候で失敗`（おおひでり中に失敗する）は実装と一致しており変更なし
- `python -m pytest tests/ -v` で全件パス（4074 passed, 1 skipped）を確認
  （`test_むしくい_ねんちゃく持ちをひんしにさせた場合は奪える` は乱数依存のまひ判定に起因する
  既存の断続的な失敗があるが、あまごいの変更とは無関係。再実行で解消することを確認済み）
