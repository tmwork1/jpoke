# こおりのつぶて レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/こおりのつぶて.md`）。
- 一次情報（`docs/wiki/moves/こおりのつぶて.html`）と照合し、タイプ: こおり / 分類: 物理 /
  威力: 40 / 命中: 100 / PP: 30 / 優先度: +1 / 非接触 / 追加効果なしを確認した。
- 判定表（急所○・命中判定○・追加効果×・まもる○・おうじゃのしるし○・マジックコート×・
  よこどり×・オウムがえし○）も反映した。

## 実装計画書
- 新規作成（`docs/plan/moves/こおりのつぶて.md`）。
- 追加効果を持たない技であり、`MoveData` の宣言のみで完結するため専用ハンドラは不要である旨を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の既存エントリ（`type="こおり"`, `category="physical"`,
  `pp=30`, `power=40`, `accuracy=100`, `priority=1`, `handlers={}`）は仕様書と完全一致していた。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列・リーサルテスト列は `n/a`）。
  追加効果のない通常ダメージ技のため、既存の汎用ダメージ計算経路でカバーされる。

## テスト
- `tests/moves_attack/test_move_ka.py` に以下を追加した:
  - `test_こおりのつぶて_相手にダメージを与える`
  - `test_こおりのつぶて_素早さが低い側でも優先度により先制する`
- `python scripts/sort_tests.py tests/moves_attack/test_move_ka.py` でソート、
  `python scripts/generate_test_list.py` で `docs/tests/` を更新した。
- `docs/progress/move.md` の該当行を修正した：
  - 仕様書列・計画書列を `-` → `x` に更新
  - PP列の誤り（20 → 30）を修正
- 全テスト（3349件）がパスすることを確認した（`.loop/test_logs/こおりのつぶて.log`）。
  なお、実行中に本技と無関係な既存テスト（例: こがらしあらし・てんねん・リフレッシュ・
  だっしゅつパック等）がランダムに失敗する既知の実行順依存フレーキネスを観測したが、
  こおりのつぶてに関する変更とは無関係であり、再実行で全件パスすることを確認済み。
