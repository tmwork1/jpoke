# じばく レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/じばく.md`）。
  - 一次情報（`docs/wiki/moves/じばく.html` / `docs/wiki/moves/だいばくはつ.html`）をもとに、基本情報・
    効果・HP消費とひんし判定の順序（`ON_PAY_HP` がヒット処理より前に発火し、外れ・タイプ無効・まもるでも
    必ずひんしになる点）・しめりけによる失敗（`flags={"explosion"}`）・判定フラグを記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/じばく.md`）。既存の共通計画書 `docs/plan/moves/自爆技.md`（じばく・
  だいばくはつ・ミストバースト共通）からじばく分のみを抽出し、仕様書へのpriority根拠を明記した独立ファイル
  として整備。`自爆技.md` の冒頭に「じばくは個別ファイルへ分離済み」の注記を追加し、だいばくはつ・
  ミストバーストは従来どおり `自爆技.md` を暫定計画書として参照する形にした。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `じばく` エントリ（type=ノーマル, category=physical, pp=5,
  power=200, accuracy=100, flags={"explosion"}, `Event.ON_PAY_HP` → `じばく_pay_hp`,
  `subject_spec="attacker:self"`）は仕様書・計画書と一致しており、修正不要と判断した。
  `handlers/move_attack.py` の `じばく_pay_hp` も現在HP全消費のみを行う正しい実装だった。

## リーサル計算
- 対象外（`docs/progress/move.md` の じばく 行はリーサル実装・リーサルテストともに `n/a`）。
  使用者自身がひんしになる技で、相手へのダメージ算出は通常の物理技と同一のため専用ハンドラは不要。

## テスト
- 変更なし。`tests/moves_attack/test_move_sa.py` に既存の3テスト
  （HP消費後も攻撃が届く／しめりけで失敗する／使用後にひんしになる）があり、いずれも仕様と一致していた。
  `tests/moves_attack/test_move__group.py` のパラメタライズテスト（じばく・だいばくはつ・ミストバースト共通）
  と `tests/abilities/test_ability_sa.py` のしめりけ関連テストも合わせて確認し、過不足なしと判断した。
  `python -m pytest tests/ -v` は 3397 passed, 1 skipped で全件成功（ログ: `.loop/test_logs/じばく.log`）。

## その他の修正（進捗表の記述整合性）
- `docs/progress/move.md` の じばく 行: 仕様書・計画書列を `x` に、レビュー列を `✓` に更新。
  PP列が実装（pp=5）と異なる `8` になっていた誤りを `5` に修正。効果列を、まもる・命中失敗時も
  必ずひんしになる旨・しめりけで失敗する旨を含む記述に更新。
- レビュー中に気づいた誤りとして、`だいばくはつ`・`ミストバースト` 行のPP列も同様に `8` となっており
  実装（pp=5）と食い違っていたため、あわせて `5` に修正した（この2技自体の仕様書・計画書・レビュー列は
  未実施のため据え置き）。
