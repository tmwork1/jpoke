# フラワーヒール レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/フラワーヒール.md`）。`docs/wiki/moves/フラワーヒール.html` の一次情報と、
  類似技 `いやしのはどう` の仕様書テンプレートを基に作成。
- PPは `docs/champions/move_list.txt` に本技単体の行が存在しないため（サイコノイズの説明文中に
  技名として言及されているのみ）、原作PPが同じ10で単体HP回復技として近い `いやしのはどう`
  （原作PP10 → champions PP12）に倣い12とし、その根拠を仕様書内に明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/フラワーヒール.md`）。`いやしのはどう.md` の構成に倣い、
  ハンドラ構成・Priority根拠（`docs/spec/turn.md` の `ON_STATUS_HIT`/HP満タン判定の行を参照）・
  端数処理・かいふくふうじ/ヒーリングシフトとの相互作用を明記。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_status.py` の `フラワーヒール_heal_defender` を修正。
  - 旧実装は `battle.modify_hp(mon, r=r)`（内部で `int()` による切り捨て）を使っており、
    一次情報が明記する「通常時は切り上げ」「グラスフィールド中は五捨五超入」という丸め方向と
    食い違っていた。
  - `(mon.max_hp + 1) // 2`（切り上げ）と `round_half_down(mon.max_hp * 2732 / 4096)`
    （五捨五超入、`いやしのはどう` のメガランチャー分岐と同じ `round_half_down` を使用）に修正し、
    `battle.modify_hp(mon, v=heal)` で反映するよう変更した。
- `src/jpoke/data/moves/move_ha.py` の `フラワーヒール` の `pp` を `10`（SV原作基準の移行漏れ）から
  `12`（champions基準、上記根拠）に修正した。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`。本技は変化技でダメージを与えないため）

## テスト
- `tests/moves_status/test_move_fa.py` の既存3テストを修正・追加。
  - `test_フラワーヒール_相手のHPを半分回復する`: 期待値を `int(max_hp * 1/2)`（切り捨て）から
    `(max_hp + 1) // 2`（切り上げ）に修正。
  - `test_フラワーヒール_グラスフィールドで2_3回復する` を
    `test_フラワーヒール_グラスフィールドで2732_4096回復する` に改名し、期待値を
    `int(max_hp * 2/3)`（切り捨て）から `round_half_down(max_hp * 2732 / 4096)`（五捨五超入）に修正。
  - `test_フラワーヒール_満タンなら失敗する`: 変更なし。
  - `test_フラワーヒール_かいふくふうじ中は回復が無効化される` を新規追加（`いやしのはどう` の
    対応テストに倣う）。
- `python -m pytest tests/ -v` で全4621件（1件skip）がパスすることを確認（
  `.loop/test_logs/フラワーヒール.log`）。
