# キノコのほうし レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/キノコのほうし.md`）。一次情報（`docs/wiki/moves/キノコのほうし.html`）を
  元に、タイプ・分類・威力・命中・PP・判定フラグ（急所/命中判定/まもる/マジックコート/よこどり/
  オウムがえし）、粉・胞子技としての無効化条件（くさタイプ・ぼうじん・ぼうじんゴーグル）、ねむり
  無効化系特性・場の効果、特性『そうしょく』との相互作用を整理して記載した。
- PPについて: `src/jpoke/data/ps-champ-ja/moves.json` に本技のエントリが存在しないことを確認した。たいあたり・
  テレポート・アロマセラピー等の多くの汎用技も同様に未掲載であり、move_list.txt はチャンピオンズの
  全技を網羅したデータではなく部分的なデータと判断し、Gen9本家基準のPP `15` を正として採用した
  （`シードフレア` レビュー時に確立した同様の判断パターンを踏襲）。

## 実装計画書
- 新規作成（`docs/plan/moves/キノコのほうし.md`）。既存実装（`data/moves/move_ka.py` /
  `handlers/move_status.py`）が仕様書と一致していることを確認し、ハンドラ構成・priority根拠・
  リーサル対象外の理由・テスト方針を記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ka.py` のキノコのほうしのエントリ（type/category/pp=15/
  accuracy=100/flags={"powder"}）と、`src/jpoke/handlers/move_status.py` の
  `キノコのほうし_apply_ailment_to_defender`（`apply_ailment_to_defender` でねむり付与）は
  仕様書と完全に一致しており、誤り・欠落はなかった。

## リーサル計算
- 対象外。`docs/progress/move.md` のリーサル実装列が `n/a`（状態異常付与のみでダメージ量そのものを
  変化させないため）。

## テスト
- 変更なし。既存の `tests/moves_status/test_move_ka.py` の
  `test_キノコのほうし_くさタイプには無効化される`・`test_キノコのほうし_相手がねむり状態になる`
  の2件が粉技無効化・ねむり付与の主要挙動を正しく検証しており、過不足はなかった。
  特性『そうしょく』との相互作用は `tests/abilities/test_ability__group.py` で技種別を問わない
  くさ技共通ロジックとして既にテスト済みのため追加不要と判断した。
- `python -m pytest tests/ -v` を実行し、4468 passed, 1 skipped（既存のskipで本レビューと無関係）
  で全テスト成功を確認した。ログは `.loop/test_logs/キノコのほうし.log` に保存済み。

## 進捗表
- `docs/progress/move.md` の該当行を更新: 仕様書 `-`→`x`、計画書 `-`→`x`、レビュー `-`→`✓`、
  PP列 `20`→`15`（Gen9本家基準に修正、move_list.txt未掲載のため champions 独自変換は不適用）。
