# ダストシュート レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ダストシュート.md`）。
  一次情報（ポケモンWiki）に基づき、タイプ・分類・威力・命中率・優先度・接触の有無・
  追加効果（30%でどく付与）・判定フラグ（急所/命中判定/追加効果/まもる/おうじゃのしるし/
  マジックコート/よこどり/オウムがえし）・リーサル計算対象外の理由を記載した。
  PPは一次情報のPP5ではなく `docs/champions/move_list.txt` の値（PP8）を正とした。

## 実装計画書
- 新規作成（`docs/plan/moves/ダストシュート.md`）。
  既存実装（`ダストシュート_apply_poison_to_defender`、`Event.ON_DAMAGE_HIT`）が
  仕様と一致していること、PPのみ修正が必要だったことを記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `"ダストシュート"` エントリの `pp` を `5` → `8` に修正した。
  一次情報（ポケモンWiki）のPP5がそのまま実装されており、`docs/champions/move_list.txt`
  （PP8）への移行漏れバグだったため。
- `handlers/move_attack.py` の `ダストシュート_apply_poison_to_defender`
  （`apply_ailment_to_defender(..., ailment="どく", chance=0.3)`）は仕様通りで修正不要。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装／リーサルテスト列は `n/a`。
  固定威力・状態異常追加効果のみの通常攻撃技のため専用ハンドラ不要）。

## テスト
- 変更なし。既存の `test_ダストシュート_どくが発動する`
  （`tests/moves_attack/test_move_ta.py`）が secondary_chance=1.0 でどく付与を確認しており、
  仕様と実装に一致しているため追加修正不要。
