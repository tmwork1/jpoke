# バブルこうせん レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/バブルこうせん.md`）。
- `docs/wiki/moves/バブルこうせん.html` を参照し、タイプ（みず）・分類（特殊）・威力65・命中100・
  PP20（Gen9基準）・追加効果（10%の確率で相手の『すばやさ』を1段階下げる）・判定フラグ
  （急所通常／まもる有効／おうじゃのしるし対応／マジックコート対象外／オウムがえし対象／
  ちからずく対象）を記載した。
- `docs/champions/move_list.txt` に本技の記載がないため、PPは本家Gen9基準の20を採用した
  （20はチャンピオンズのPP統一4段階に既に含まれる値のため変換不要。既存の同種技
  `オーロラビーム.md` と同じ扱い）。

## 実装計画書
- 新規作成（`docs/plan/moves/バブルこうせん.md`）。
- 既存実装（`data/moves/move_ha.py` の `バブルこうせん` エントリ、
  `handlers/move_attack.py` の `バブルこうせん_lower_defender_spd`）が仕様と一致していることを確認し、
  ハンドラ構成（`ON_DAMAGE_HIT` / priority 100 / `modify_defender_stats` 経由）と
  priority根拠（既存の同種ハンドラ・オーロラビーム等と同一パターン）を記載した。

## 実装（handlers/ / data/）
- 変更なし。`data/moves/move_ha.py` の `type/category/pp/power/accuracy/flags` および
  `handlers/move_attack.py` の `バブルこうせん_lower_defender_spd`（`modify_defender_stats` で
  `stats={"spe": -1}, chance=0.1` を指定）は仕様書・計画書と一致しており、修正不要と判断した。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列は `n/a`）。確率発動のランク低下効果であり、
  ダメージ量自体を変化させないため。

## テスト
- `tests/moves_attack/test_move_ha.py` に3件追加：
  - `test_バブルこうせん_すばやさ1段階低下が発動しない`（`secondary_chance=0.0` で相手のS変化なしを確認）
  - `test_バブルこうせん_すばやさ1段階低下が発動する`（`secondary_chance=1.0` で相手のSが-1になることを確認）
  - `test_バブルこうせん_相手にダメージを与える`（みずタイプ特殊技でダメージが入ることを確認）
- `python -m pytest tests/ -v` で3950 passed, 1 skippedを確認（`tests/moves_attack/test_move_ma.py::test_むしくい_ねんちゃく持ちをひんしにさせた場合は奪える` は本タスクと無関係な既存の乱数依存フレーキーテストで、単体実行では成功することを確認済み）。
