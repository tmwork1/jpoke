# わるだくみ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/わるだくみ.md`）
- 一次情報（`docs/wiki/moves/わるだくみ.html`）と `src/jpoke/data/ps-champ-ja/moves.json` を照合し、
  タイプ:あく／分類:変化／PP:20／命中率なし（必中）／範囲:自分／優先度:0／直接攻撃:× を記載
- 効果は「自分のとくこうランクを2段階上げる」
- 判定表（急所・命中判定・追加効果・まもる・マジックコート・おうじゃのしるし・よこどり・
  オウムがえし）を一次情報どおりに反映

## 実装計画書
- 新規作成（`docs/plan/moves/わるだくみ.md`）
- `ON_STATUS_HIT` の subject_spec は `MoveHandler` のデフォルト値 `"attacker:self"`、priority は
  同種の自分対象ランクアップ変化技（りゅうのまい・ロックカット等）に合わせて100とする根拠を明記

## 実装（handlers/ / data/）
- 変更なし
- `src/jpoke/handlers/move_status.py` の `わるだくみ_modify_attacker_stats` は
  共通ユーティリティ `handlers/move.py` の `modify_attacker_stats(stats={"spa": 2})` を呼ぶのみで、
  仕様書・計画書と一致していることを確認
- `src/jpoke/data/moves/move_wa.py` の `MOVES_WA["わるだくみ"]`（type=あく, category=status, pp=20,
  `Event.ON_STATUS_HIT` に登録）も仕様書・計画書と一致

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列は n/a。わるだくみは相手にダメージを与えない
  自分対象の変化技のため対象外）

## テスト
- `tests/moves_status/test_move_yarawa.py` に以下を追加:
  - `test_わるだくみ_とくこう2段階上がる`: 使用後に `attacker.rank["spa"] == 2` を確認
  - `test_わるだくみ_とくこう最大なら失敗する`: とくこうが既に+6の場合、ランクが変化しないことを確認
- `python -m pytest tests/ -v` で全4453件（+スキップ1件）がパスすることを確認
  （結果は `.loop/test_logs/わるだくみ.log` に保存）
