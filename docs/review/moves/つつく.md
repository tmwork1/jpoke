# つつく レビュー結果

## 仕様書
- 対象外。一次情報（`docs/wiki/moves/つつく.html`）を確認したところ、つつくは
  「ひこう・物理・威力35・命中100・PP35・優先度0・直接攻撃○・追加効果なし」という
  純粋な通常攻撃技であり、`MoveData`（type/category/power/accuracy/pp/flags）のみで
  挙動が完結する。同種の追加効果なし通常攻撃技（`たいあたり`・`このは`・`しんくうは` など）
  も同様の理由で専用の仕様書・計画書を持たない運用のため、本技についても新規作成は行わなかった。

## 実装計画書
- 対象外（理由は仕様書の項と同じ）。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `"つつく"` エントリ
  （type="ひこう", category="physical", pp=35, power=35, accuracy=100, flags={"contact"},
  handlers={}）は一次情報と完全に一致していることを確認した。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a。追加効果がなくダメージ計算のみのため、
  既存の汎用リーサル計算で問題ない）。

## テスト
- `tests/moves_attack/test_move_ta.py` に `test_つつく_相手にダメージを与える` を追加
  （つつくを使用した際に相手にダメージが入ることを確認する基本テスト。既存の
  `たいあたり`・`たたきつける` 等の追加効果なし技のテストパターンに準拠）。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ta.py` でソート。
- `python scripts/generate_test_list.py` でテスト一覧を更新。
- `.venv/Scripts/python.exe -m pytest tests/ -p no:randomly` で全件実行し、
  3431 passed, 1 skipped を確認（ログ: `.loop/test_logs/つつく.log`）。
- `docs/progress/move.md` の つつく 行を修正した。
  - テスト列: `-` → `x`
  - レビュー列: `-` → `✓`
  - 効果列: `？` → `追加効果なし。`
