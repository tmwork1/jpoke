# いやしのねがい レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/いやしのねがい.md`）。`docs/wiki/moves/いやしのねがい.html` を一次情報として基本情報・効果・判定・第五世代以降/第八世代以降の発動タイミングを整理。
- PP はチャンピオンズ仕様に合わせて 12（`docs/champions/move_list.txt` 準拠）と明記。
- ダブル/トリプルバトル固有の仕様（サイドチェンジ・トリプルバトル MOVE 等）は本プロジェクト（シングルバトル専用）の対象外である旨を明記。

## 実装計画書
- 既存の `docs/plan/moves/いやしのねがい.md` を実装の実態に合わせて修正した。
  - PP を 10 → 12 に修正（`docs/champions/move_list.txt` 準拠）。
  - 計画書に記載のなかった `target="self"` の必要性を明記（`Move.is_blocked_by_protect` が `target=="foe"` のときのみ true を返すため、未指定だと `まもる` が誤って `いやしのねがい` を防いでしまう）。
  - 計画書に記載のなかった「控えのポケモンがいない場合の失敗判定」をハンドラ構成・エッジケースに追記。
  - ハンドラ名を実装済みの実名（`いやしのねがい_apply` / `いやしのねがい_heal`）に合わせて修正（計画書では `いやしのねがい_faint_and_set_side_field` / `いやしのねがい_heal_on_switch_in` という別名だった）。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py`
  - PP を 10 → 12 に修正（`docs/champions/move_list.txt` の値に合わせた。移行漏れバグ）。
  - `target="self"` を追加（欠落していたバグ。未指定だと既定値 `"foe"` になり、`Move.is_blocked_by_protect` が true を返して `まもる` に阻まれてしまう。Wiki 上は `まもる: ×` であり、自分専用技のため防がれてはいけない）。
- `src/jpoke/handlers/move_status.py::いやしのねがい_apply`
  - 控えのポケモンがいない（`battle.query.can_switch(player)` が false）場合に失敗させる判定を追加（欠落していたバグ。Wiki 上は「控えのポケモンがいないときに使用した場合は失敗し、使用者はひんしにならない」と明記されている。`しっぽきり_check` 等と同じ API `battle.query.can_switch` を使用）。
- `src/jpoke/handlers/field.py::いやしのねがい_heal`、`src/jpoke/data/field/side_field.py` は仕様と一致しており変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）。

## テスト
- `tests/moves_status/test_move_a.py` に以下を追加。
  - `test_いやしのねがい_控えがいないと失敗し使用者はひんしにならない`: 控えなしで使用した場合、失敗して使用者がひんしにならず、フィールドも設置されないことを確認。
  - `test_いやしのねがい_自分対象のためまもるで防がれない`: 相手が `まもる` を張っていても、自分専用技のため効果が正常に発動する（使用者がひんしになりフィールドが設置される）ことを確認。
- 既存テスト（HP満タン時の保留、状態異常回復、死に出し時の全回復、フィールド解除等）は実装と整合しており変更なし。
- `python -m pytest tests/ -v` で全件パス（4085 passed, 1 skipped）。ログ: `.loop/test_logs/いやしのねがい.log`
  - 補足: 本リポジトリのテストはランダムな実行順序により、いやしのねがいと無関係な既存テスト（`test_field.py` のオーロラベール系、`test_ability_ta.py` のてんねん系など）が稀に順序依存で失敗する既知の不安定挙動があることを確認した（本タスクの変更前から再現するプリエグジスティングな問題であり、いやしのねがいの実装とは無関係）。
