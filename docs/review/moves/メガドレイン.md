# メガドレイン レビュー結果

## 仕様書
- 存在しなかったため新規作成した（docs/spec/moves/メガドレイン.md）。
- docs/wiki/moves/メガドレイン.html を一次情報とし、タイプ（くさ）・分類（特殊）・威力（40）・
  命中率（100）・PP（第四世代以降15、`src/jpoke/data/ps-champ-ja/moves.json` に記載なしのためGen9本家値を採用）・
  接触なし・効果（与えたダメージの1/2を回復するドレイン技）・判定フラグ・Zワザ威力（120）・
  ダイマックスわざ威力（90）を記載。
- ドレイン技共通仕様は docs/spec/moves/ギガドレイン.md・きゅうけつ.md を参照する形で記述。

## 実装計画書
- 存在しなかったため新規作成した（docs/plan/moves/メガドレイン.md）。
- ギガドレインの計画書を踏襲し、共通関数 `_drain_hp`（handlers/move_attack.py）を利用する方針、
  `Event.ON_HIT` priority=20（docs/spec/turn.md 準拠）、テスト方針、リーサル対象外の旨を明記。

## 実装（handlers/ / data/）
- 変更なし。既存実装（`data/moves/move_ma.py` の `メガドレイン`、
  `handlers/move_attack.py` の `メガドレイン_drain`）は仕様書・計画書と一致しており、
  タイプ・分類・PP（15）・威力（40）・命中率（100）・flags（heal）・priority（20）・
  共通処理 `_drain_hp(heal_ratio=0.5)` の呼び出しいずれも正しいことを確認した。

## リーサル計算
- 対象外（docs/progress/move.md のリーサル実装列は n/a）。
  他のドレイン技と同様、ドレイン技グループとして専用のリーサル計算ハンドラは未実装の方針を維持。

## テスト
- 変更なし。既存の `tests/moves_attack/test_move__group.py`
  `test_drain技グループ_回復量が与ダメのheal_ratio倍になる`（`("メガドレイン", 0.5)` を含む）と、
  `tests/moves_attack/test_move_ma.py` の
  `test_メガドレイン_使用後に攻撃者のHPが回復する` が既に実装と整合しており、追加・修正は不要と判断した。

## 補足（進捗表の修正）
- docs/progress/move.md のメガドレイン行が、隣接する別技（メガキック/メガトンキック相当のデータ:
  タイプ=ノーマル、威力=120、命中率=75、PP=8）の値と混同されており誤っていたため修正した。
  タイプ=くさ、威力=40、命中率=100、PP=15、効果列を実装内容に合わせて修正し、
  仕様書・計画書列を x、レビュー列を ✓ に更新した。
