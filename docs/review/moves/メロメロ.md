# メロメロ レビュー結果

## 仕様書

新規作成: `docs/spec/moves/メロメロ.md`

`docs/wiki/moves/メロメロ.html` をもとに作成した。主な内容:
- タイプ・分類・命中率・PP（16、`docs/champions/move_list.txt` 基準）・対象・優先度・直接攻撃の有無
- 効果: 相手が異性の場合のみメロメロ状態を付与、みがわり状態の相手にも成功する
  （`bypass_substitute`）
- どんかん・アロマベール・あかいいと・重複付与の扱いはリンク先（`docs/spec/volatiles/メロメロ.md`、
  アイテム側実装）に委譲する旨を明記

## 実装計画書

新規作成: `docs/plan/moves/メロメロ.md`

既存の実装（`src/jpoke/data/moves/move_ma.py`、`src/jpoke/handlers/move_status.py`）を反映して
作成した。旧計画書（存在しなかった）とは別に、以下を明記した:
- `ON_TRY_MOVE_2`（priority=120）で性別チェック、`ON_STATUS_HIT` でメロメロ付与
- `flags={"bypass_substitute"}` が必要である根拠

## 実装（handlers/ / data/）

### 修正あり

**問題点1**: PPが本家Gen9基準の15のままで、Pokémon Champions基準（`docs/champions/move_list.txt`）
の16に移行していなかった。
- `src/jpoke/data/moves/move_ma.py` の `"メロメロ"` を `pp=15` → `pp=16` に修正。

**問題点2**: ポケモンWiki「技の仕様」に「みがわり状態のポケモンに対しても成功する」と明記されて
いるにもかかわらず、`flags={"bypass_substitute"}` が設定されておらず、みがわりに阻まれる実装に
なっていた。
- `src/jpoke/data/moves/move_ma.py` の `"メロメロ"` に `flags={"bypass_substitute"}` を追加。

**既存実装（変更なし）**:
- `src/jpoke/handlers/move_status.py` の `メロメロ_check_gender`（`ON_TRY_MOVE_2`、priority=120）
- `src/jpoke/handlers/move_status.py` の `メロメロ_apply`（`ON_STATUS_HIT`、`apply_volatile_to_defender`
  ラッパー）
- `src/jpoke/data/volatile.py` の `"メロメロ"` VolatileData、`どんかん`・`アロマベール`・`あかいいと`
  の各特性・アイテム側実装は仕様と一致しており変更なし。

## リーサル計算

対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）。

## テスト

### 追加あり

`tests/moves_status/test_move_ma.py` に以下を追加:
- `test_メロメロ_みがわり状態の相手にも効果が発動する`: `bypass_substitute` フラグにより、
  みがわり状態の相手にもメロメロが付与されることを確認（`ちょうはつ` の同種テストと同じパターン）

既存テスト（`test_メロメロ_すでにメロメロ状態なら失敗`、`test_メロメロ_性別の組み合わせ`、
`tests/volatiles/test_volatile_hamayarawa.py` の `test_メロメロ_同性に効かない` 等、
`tests/items/test_item_a.py` のあかいいと関連テスト、`tests/abilities/test_ability__group.py` の
どんかん・アロマベール免疫テスト）は実装と一致しており変更なし。

`python -m pytest tests/ -v` を実行し、4631 passed, 1 skipped（既存の無関係な skip）で
全件パスすることを確認した（ログ: `.loop/test_logs/メロメロ.log`）。
