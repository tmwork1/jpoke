# つばめがえし レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/つばめがえし.md`）。一次情報（`docs/wiki/moves/つばめがえし.html`）を基に、
  タイプ/分類/威力/命中(必中)/PP/対象/優先度/接触/slashラベル・判定フラグを記載。

## 実装計画書
- 新規作成（`docs/plan/moves/つばめがえし.md`）。追加効果なしのため専用ハンドラは不要である旨と、
  `accuracy=None` による必中実装の根拠（`move_executor._check_hit`）、はりきりとの相互作用の安全性を明記。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `"つばめがえし"` エントリ
  （`type="ひこう"`, `category="physical"`, `pp=20`, `power=60`, `accuracy` 省略（必中）,
  `flags={"contact", "slash"}`, `handlers={}`）は一次情報・仕様書と一致していることを確認した。
- `move_executor.py` の命中判定（`accuracy is None` で即 `True` を返す）が必中仕様と一致していることを確認。
- `はりきり_modify_accuracy` の `ctx.move.accuracy is not None` ガードにより、必中技には
  はりきりの命中率ペナルティが適用されないことを確認（`ON_MODIFY_ACCURACY` 自体発火しないため冗長だが安全）。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列は `n/a`。追加効果のない通常攻撃技のため）。

## テスト
- `tests/moves_attack/test_move_ta.py` に以下を追加。
  - `test_つばめがえし_相手にダメージを与える`
  - `test_つばめがえし_相手の回避ランクが高くても必ず命中する`（回避ランク+6でも命中することを確認）
- `scripts/sort_tests.py`・`scripts/generate_test_list.py` を実行し、`python -m pytest tests/ -v` で
  全3435件（+1 skip）がパスすることを確認（`.loop/test_logs/つばめがえし.log`）。
