# わたほうし レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/わたほうし.md`）。一次情報（`docs/wiki/moves/わたほうし.html`）と
  `src/jpoke/data/ps-champ-ja/moves.json` を照合し、以下を明記した。
  - タイプ: くさ / 分類: 変化 / PP: 20（champions基準。Gen9本家は40） / 命中率: 100 /
    範囲: 相手全員（シングルでは相手1体）/ 優先度: 0 / 直接攻撃: ×
  - 効果: 相手のすばやさランクを2段階下げる（粉技のためくさタイプ・特性『ぼうじん』・
    道具『ぼうじんゴーグル』を持つ相手には無効）
  - 判定（まもる○・マジックコート○・追加効果系特性対象外・オウムがえし○）

## 実装計画書
- 既存の `docs/plan/moves/わたほうし.md` は内容が実装と食い違っていたため修正した。
  - PP を 40 → 20（champions基準）に修正
  - すばやさ低下量を「1段階」→「2段階」に修正（実装は元々 -2 で正しく、計画書側の記述が誤りだった）
  - ハンドラのシグネチャ・登録先パス（`data/move.py` → `data/moves/move_wa.py`）を実装の
    実態に合わせて修正
  - リーサル計算は対象外である旨を明記

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_status.py` の `わたほうし_modify_defender_stats` はすでに
  `stats={"spe": -2}` で正しく実装済みだったため変更なし。
- `src/jpoke/data/moves/move_wa.py` の PP が `40`（Gen9本家値）になっていたバグを
  `20`（champions/move_list.txt準拠）に修正した。

## リーサル計算
- 対象外（ダメージを与えない変化技のため `docs/progress/move.md` は n/a）。

## テスト
- `tests/moves_status/test_move_yarawa.py`
  - 既存テスト `test_わたほうし_相手のすばやさ1段階下がる` は関数名・docstringが「1段階」に
    なっていたが、アサーションは `-2` を検証しており実態と食い違っていたため、
    `test_わたほうし_相手のすばやさ2段階下がる` に改名・docstring修正した。
  - 粉技のくさタイプ無効を確認する `test_わたほうし_くさタイプには無効` を新規追加した
    （他の粉技 `どくのこな`・`ねむりごな` と同様のパターン）。
