# かげぶんしん レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/かげぶんしん.md`）。一次情報（`docs/wiki/moves/かげぶんしん.html`）に基づき、タイプ・分類・威力・命中率・PP・範囲・優先度・直接攻撃・効果・判定（急所/命中判定/追加効果/まもる/おうじゃのしるし/マジックコート/よこどり/オウムがえし）を記載した。
- PP は `src/jpoke/data/ps-champ-ja/moves.json` 準拠で 16（Gen9本家は15）と明記した。

## 実装計画書
- `docs/plan/moves/かげぶんしん.md` は既存だったが、以下を修正した。
  - PP を 15 → 16（champions 準拠）に修正。
  - 「現状の実装状況」節が既に実装済みの状態を反映していなかった（未実装として記述）ため削除。
  - ハンドラのコンテキスト型を `EventContext` → `AttackContext` に修正（`ON_STATUS_HIT` は `MoveHandler` 経由で `AttackContext` が渡される。実際の実装と整合させた）。
  - 関数名を `かげぶんしん_modify_attacker_stats` → 実装済みの `かげぶんしん_boost_attacker_evasion` に修正。
  - `stats={"EVA": 1}` → 実装済みの `stats={"evasion": 1}` に修正（`Stat` 型のキーは小文字）。
  - `data/move.py` への参照を実体に合わせて `data/moves/move_ka.py` に修正し、`category="変化"` → `category="status"` に修正。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ka.py` の `かげぶんしん` の PP を 15 → 16 に修正（champions/move_list.txt 準拠）。
- `src/jpoke/handlers/move_status.py` の `かげぶんしん_boost_attacker_evasion` はハンドラ構成・`subject_spec`（`MoveHandler` デフォルトの `attacker:self`）とも仕様書・計画書に整合しており、修正なし。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列は `n/a`。ダメージを与えない変化技のため対象外）。

## テスト
- `tests/moves_status/test_move_ka.py` の `test_かげぶんしん_回避率1段階上がる` は実装と一致しており、変更なし。

## その他
- `docs/progress/move.md` の かげぶんしん 行の仕様書・計画書列を `-` → `x`、レビュー列を `-` → `✓` に更新（PP列は既に16で正しかったため変更なし）。
