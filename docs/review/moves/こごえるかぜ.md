# こごえるかぜ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/こごえるかぜ.md`）。一次情報（`docs/wiki/moves/こごえるかぜ.html`）と
  `docs/champions/move_list.txt` に基づき、タイプ/分類/威力/命中/PP/追加効果（100%すばやさ1段階ダウン）・
  フラグ（`wind`, `secondary_effect`）を記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/こごえるかぜ.md`）。既存実装との差分（PPの誤り、`secondary_effect` フラグの欠落）
  を明記し、修正方針を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/move.py` の `"こごえるかぜ"` エントリを修正した。
  - `pp=15` → `pp=16`（`docs/champions/move_list.txt` 準拠。本家のPP15はChampionsでは16に変更されている）。
  - `flags={"wind"}` → `flags={"wind", "secondary_effect"}`（100%の追加効果＝すばやさダウンを持つため、
    `あわ` / `あやしいかぜ` / `かみなりあらし` 等と同様にちからずくの対象技として扱う必要があった）。
  - `handlers/move_attack.py` の `こごえるかぜ_lower_defender_spd`
    （`modify_defender_stats(battle, ctx, value, stats={"spe": -1})`）は仕様通り実装済みのため変更なし。
  - `data/move.py` の修正に伴い `scripts/sort_data/sort_moves.py` を実行した。

## リーサル計算
- 対象外。`docs/progress/move.md` の「リーサル実装」列は事前は `-` だったが、本技の追加効果（すばやさランクダウン）は
  HP分布に影響しないため（`あわ` / `あやしいかぜ` 等の同種効果技と同様の扱い）、`n/a` に修正した
  （新規のリーサルハンドラは実装していない）。

## テスト
- `tests/moves_attack/test_move_ka.py` に以下を追加した。
  - `test_こごえるかぜ_相手にダメージを与える`
  - `test_こごえるかぜ_すばやさが下がる`
  - `test_こごえるかぜ_ちからずくで威力上昇しすばやさダウンは発動しない`
- `scripts/sort_tests.py tests/moves_attack/test_move_ka.py` でソートし、
  `scripts/generate_test_list.py` で `docs/tests/` を更新した。
- `python -m pytest tests/ -v` で全件（3346 passed, 1 skipped）を確認した
  （ログ: `.loop/test_logs/こごえるかぜ.log`）。
