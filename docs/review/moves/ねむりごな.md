# ねむりごな レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ねむりごな.md`）。ポケモンWiki一次情報（タイプ: くさ / 分類: 変化 /
  威力: - / 命中率: 75 / 判定各種）と `src/jpoke/data/ps-champ-ja/moves.json` の PP(16) を反映。
  `さいみんじゅつ.md` / `どくのこな.md` の記述形式に合わせた。

## 実装計画書
- 新規作成（`docs/plan/moves/ねむりごな.md`）。ハンドラ構成・priority根拠・実装箇所・注意点を記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_na.py` の `ねむりごな` エントリで PP が 15
  （Gen9本家基準）になっていたバグを修正し、Pokémon Champions 基準
  （`src/jpoke/data/ps-champ-ja/moves.json` の 16）に修正した。
- `handlers/move_status.py` の `ねむりごな_apply_ailment_to_defender` はそのまま
  `apply_ailment_to_defender(..., ailment="ねむり")` を呼び出す実装で仕様と一致しており修正不要。
- `flags={"powder"}` により、くさタイプ/特性『ぼうじん』/道具『ぼうじんゴーグル』への無効化は
  `move_executor.py` / `ability.py` / `item.py` の汎用処理で担保されており技側の実装は不要。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行が `n/a`。状態異常付与のみの変化技のためダメージ
  リーサル計算の対象外）。

## テスト
- `tests/moves_status/test_move_na.py` に以下を追加:
  - `test_ねむりごな_相手をねむり状態にする`
  - `test_ねむりごな_くさタイプには無効`
  - `test_ねむりごな_命中率75で外れることがある`
- 特性『ぼうじん』・道具『ぼうじんゴーグル』・かたやぶりでの無効化については
  `tests/abilities/test_ability__group.py` / `tests/items/test_item_ha.py` に
  既存のカバーがあることを確認済み（追加不要）。
- `python -m pytest tests/ -v` は 4316 passed, 1 skipped（既存の無関係なskip）で全件成功。
  結果は `.loop/test_logs/ねむりごな.log` に保存。
