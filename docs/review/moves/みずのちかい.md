# みずのちかい レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/みずのちかい.md`）。一次情報（`docs/wiki/moves/みずのちかい.html`）および
  既存の同系統仕様書（`docs/spec/moves/くさのちかい.md` / `docs/spec/moves/ほのおのちかい.md`）を参照し、
  タイプ・分類・威力（第五世代50→第六世代以降80）・命中・PP・対象・優先度・接触なし・判定フラグ・
  コンビネーションわざ（ダブルバトル以上限定のため本プロジェクトでは対象外）・よびみず/ひらいしん/そうでん状態との
  関係を記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/みずのちかい.md`）。仕様書をもとに、専用ハンドラ不要（`MoveData` フィールドのみで完結）
  であることと注意点・エッジケースを記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ma.py` に既に
  `type="みず", category="special", pp=10, power=80, accuracy=100, handlers={}` として登録済みで、
  仕様書・計画書と一致していることを確認した。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行「リーサル実装」列が `n/a`）。

## テスト
- `tests/moves_attack/test_move_ma.py` に以下を新規追加:
  - `test_みずのちかい_威力80`
  - `test_みずのちかい_追加効果なしでダメージのみ`
  （`くさのちかい`/`ほのおのちかい`の既存テストと同等の構成）
- `scripts/sort_tests.py` でソート、`scripts/generate_test_list.py` でテスト一覧を更新し、
  `python -m pytest tests/ -v` で全件パス（3608 passed, 1 skipped）を確認した。
