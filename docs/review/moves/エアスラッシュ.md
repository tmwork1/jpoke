# エアスラッシュ レビュー結果

## 仕様書
- 変更なし。個別の仕様書ファイルは存在しないが、一次情報（`docs/wiki/moves/エアスラッシュ.html`）と
  照合した結果、既存の共通仕様表に内容が正しく反映されている
  （ひこう / 特殊 / 威力75 / 命中95 / 範囲1体選択（遠隔）/ 優先度0 / 直接攻撃×）。
  追加効果「30%の確率でひるみ」は `docs/spec/moves/_move_secondary_1.md` の163行目
  （`エアスラッシュ defender 0.3 ... 1`）に既に記載されており正しい。
  一次情報の「急所:○」「命中判定:○」「まもる:○」「おうじゃのしるし: 第四世代のみ有効・第五世代以降無効」
  「マジックコート:×」「よこどり:×」「オウムがえし:○」も既存の
  `docs/spec/items/おうじゃのしるし.md` の記載（59行目）と一致することを確認した。
  「切る技」（きれあじで威力1.5倍）であることも `docs/spec/abilities/きれあじ.md` に記載済み。

## 実装計画書
- 変更なし。個別の計画書は存在しないが、同種の「確率でひるみを付与する」技
  （いわなだれ・かみつく・あくのはどう等）にも個別計画書は作成されておらず、
  共通ヘルパー `apply_volatile_to_defender` を使う定型実装のため計画書省略は既存方針と整合している。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `エアスラッシュ` エントリで PP が `15`
  （Gen9本家基準の値）になっていたバグを発見し、`src/jpoke/data/ps-champ-ja/moves.json`（122行目）の
  値に基づき `16` に修正した（チャンピオンズではPPは8/12/16/20の4段階に統一されているため）。
  `docs/progress/move.md` のPP列は既に `16` で正しかった。
- それ以外は変更なし。type=ひこう, category=special, power=75, accuracy=95,
  flags={"slash", "secondary_effect"}（"wind" フラグは一次情報上エアスラッシュには
  該当しないため付与されておらず正しい）、`Event.ON_DAMAGE_HIT` に
  `エアスラッシュ_apply_flinch` を登録している構成は仕様と一致する。
  `src/jpoke/handlers/move_attack.py` の `エアスラッシュ_apply_flinch` は
  `apply_volatile_to_defender(battle, ctx, value, volatile="ひるみ", chance=0.3)` を呼び出しており、
  30%の確率で相手を『ひるみ』状態にする仕様と一致する。五十音順の位置も既に正しいため
  `sort_handlers.py` / `sort_moves.py` の実行差分はなし（本レビューでは未実行、マージ後に一括整形）。
  `src/jpoke/handlers/item.py` の `_INNATE_FLINCH_MOVES` に `エアスラッシュ` が含まれており、
  おうじゃのしるし・するどいキバの追加ひるみ効果が重複しない仕様も正しく実装済み。

## リーサル計算
- 対象外。`docs/progress/move.md` のリーサル実装・リーサルテスト列がともに `n/a`。

## テスト
- 変更なし。`tests/moves_attack/test_move_a.py` の `test_エアスラッシュ_ひるみが発動する`
  （secondary_chance=1.0 でひるみ付与を確認）は既存実装と一致しており修正不要。
- `python -m pytest tests/ -v` で全3674件パス（1件skip）を確認
  （ログ: `.loop/test_logs/エアスラッシュ.log`）。

## その他
- `docs/progress/move.md` の該当行のレビュー列を `✓` に更新した（他は全列一致のため変更なし）。
