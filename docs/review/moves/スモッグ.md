# スモッグ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/スモッグ.md`）。
  - 一次情報（docs/wiki/moves/スモッグ.html）に基づき、タイプ=どく、分類=特殊、威力=30（第六世代以降）、
    命中=70、PP=20、追加効果=40%でどく状態、直接攻撃=×、優先度=0 を記載。
  - `docs/champions/move_list.txt` に本技のエントリが存在しないため、第九世代基準のPP20を
    そのまま採用する旨と根拠を明記。

## 実装計画書
- 新規作成（`docs/plan/moves/スモッグ.md`）。
  - 既存実装（`data/moves/move_sa.py` の `スモッグ` エントリ、`handlers/move_attack.py` の
    `スモッグ_apply_poison_to_defender`）が仕様書と一致していることを確認し、実装コード・
    ハンドラ構成表・priority根拠・エッジケースを記載。

## 実装（handlers/ / data/）
- 変更なし。既存の `スモッグ_apply_poison_to_defender`（`apply_ailment_to_defender` を
  `chance=0.4` で呼び出す実装）および `MoveData`（どく/特殊/pp20/power30/accuracy70/
  `secondary_effect`）はいずれも一次情報・仕様書と一致しており、修正不要と判断した。

## リーサル計算
- 対象外。`docs/progress/move.md` のスモッグ行は「リーサル実装」「リーサルテスト」ともに n/a。

## テスト
- 追加: `tests/moves_attack/test_move_sa.py` に
  `test_スモッグ_どくが発動しない`（`secondary_chance=0.0` でどくが付与されないことを確認）を追加。
  既存の `test_スモッグ_どくが発動する` は発動ケースのみだったため、10まんボルト・スパーク等の
  同系統技に合わせて非発動ケースを補った。
- `python -m pytest tests/ -v` は 3843 passed, 1 skipped（全て成功）。
  1回目の実行で `test_だっしゅつパック_わるいてぐせに奪われた場合は発動しない` /
  `test_だっしゅつボタン_ちからずくの効果が発動した技を受けたときは発動しない` /
  `test_むしくい_ねんちゃく持ちをひんしにさせた場合は奪える` の3件が失敗したが、個別実行では
  全て成功し、再実行したフルスイートでも全件成功したため、乱数依存の既知の偶発的失敗であり
  本レビューの変更とは無関係と判断した。
