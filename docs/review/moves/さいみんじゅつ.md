# さいみんじゅつ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/さいみんじゅつ.md`）。`docs/wiki/moves/さいみんじゅつ.html` の一次情報
  （タイプ: エスパー / 分類: 変化 / 命中率: 60 / PP: 20 / 優先度: 0 / 直接攻撃: ×、判定:
  急所×・命中判定○・追加効果×・まもる○・マジックコート○・よこどり×・オウムがえし○）を反映。
  `あくまのキッス` 仕様書と同一パターンで作成し、Pokémon Champions の PP（`docs/champions/move_list.txt`
  で20、本家Gen9と同値）・ねむりカウント仕様・音系/粉系ラベルなしの点を補足した。

## 実装計画書
- 変更なし。既存の `docs/plan/moves/さいみんじゅつ.md` を一次情報・仕様書と照合したが、priority根拠
  （`ON_STATUS_HIT` は `docs/spec/turn.md` に未掲載のため既存の同種ハンドラ `あくまのキッス`・
  `ねむりごな` に合わせてデフォルトpriority=100を採用）を含め誤り・欠落なし。

## 実装（handlers/ / data/）
- 変更なし。`handlers/move_status.py` の `さいみんじゅつ_apply_ailment_to_defender` は共通ヘルパー
  `apply_ailment_to_defender(battle, ctx, value, ailment="ねむり")` を呼び出す実装で、
  `あくまのキッス` と同一パターン。`data/moves/move_sa.py` の `MoveData`（type="エスパー",
  category="status", pp=20, accuracy=60、`ON_STATUS_HIT` ハンドラ登録）も仕様書・計画書と一致。
  `subject_spec` はデフォルトの `attacker:self` で `AttackContext` のロールと一致。

## リーサル計算
- 対象外（進捗表の該当列は n/a。ダメージを与えない変化技のため）。

## テスト
- 変更なし。`tests/moves_status/test_move_sa.py` の `test_さいみんじゅつ_ねむり付与`
  （accuracy=100固定で相手がねむり状態になることを確認）は `あくまのキッス` のテストと同一パターンで
  十分な検証内容と判断。
