# ちいさくなる レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ちいさくなる.md`）。一次情報（`docs/wiki/moves/ちいさくなる.html`、
  `docs/wiki/volatiles/ちいさくなる_(状態変化).html`）をもとに作成。PPは
  `docs/champions/move_list.txt`（12）を正とした。

## 実装計画書
- 新規作成（`docs/plan/moves/ちいさくなる.md`）。既存実装のバグ（PP・target欠落）を明記した
  修正計画として記述。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `ちいさくなる` エントリを修正:
  - `pp` を `10` → `12` に修正（`docs/champions/move_list.txt` 準拠。Gen9本家値が残っていた移行漏れ）
  - `target="self"` を追加（未指定だとデフォルトの `"foe"` になり、
    `Move.is_blocked_by_protect` / `Move.is_reflectable` が `target == "foe"` を条件にしているため、
    まもる・マジックコートに誤反応していた）
- `handlers/move_status.py` の `ちいさくなる_apply`、`handlers/volatile.py` の
  `ちいさくなる_guaranteed_hit` / `ちいさくなる_boost_power`、`data/volatile.py` の
  `"ちいさくなる"` VolatileData は仕様通りで変更なし。
- minimizeラベルの対象技（ふみつけ・のしかかり・ドラゴンダイブ・ヒートスタンプ・
  ヘビーボンバー・フライングプレス・サンダーダイブ）は既に各技データに `flags={"minimize", ...}`
  で正しく登録されており、champions側の対象技リストと一致していることを確認。変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行が `n/a`。ダメージを与えない変化技のため）。

## テスト
- `tests/moves_status/test_move_ta.py` に3件追加:
  - `test_ちいさくなる_回避率2段階上がりちいさくなる状態を付与する`
  - `test_ちいさくなる_まもるで防がれない`（target="self"修正の回帰確認）
  - `test_ちいさくなる_マジックコートで跳ね返されない`（target="self"修正の回帰確認）
- 揮発状態側の必中化・威力2倍効果は `tests/volatiles/test_volatile_satana.py` に
  既存テスト（`test_ちいさくなる_minimize技が必中化して威力2倍` 等）があり、確認済み。変更なし。
- `python -m pytest tests/ -v` 実行、4220 passed, 1 skipped で全件パス。
  結果は `.loop/test_logs/ちいさくなる.log` に保存。
