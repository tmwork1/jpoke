# がんせきほう レビュー結果

## 仕様書

- 新規作成（`docs/spec/moves/がんせきほう.md`）。
  - 一次情報（`docs/wiki/moves/がんせきほう.html`）より、タイプ いわ / 分類 物理 / 威力150 / 命中90 / PP5 /
    弾のわざ（ぼうだんで無効化）/ 命中後に反動で次のターン行動不能になる効果、判定フラグ（急所・命中・
    まもる・おうじゃのしるし・マジックコート・よこどり・オウムがえし）を記載した。

## 実装計画書

- 新規作成（`docs/plan/moves/がんせきほう.md`）。
  - 既存実装（`data/move.py` の `がんせきほう`、`handlers/move_attack.py` の `リチャージ_apply` 共用ハンドラ）を
    仕様書と照合し、ハンドラ構成表・priority根拠・エッジケースを記載した。

## 実装（handlers/ / data/）

- 変更なし。`src/jpoke/data/move.py` の `がんせきほう`（type=いわ, category=physical, pp=5, power=150,
  accuracy=90, flags={"bullet"}, `Event.ON_DAMAGE_HIT` で `リチャージ_apply` を使用）は仕様書と一致しており、
  修正不要と判断した。

## リーサル計算

- 対象外。`docs/progress/move.md` の「リーサル実装」「リーサルテスト」列がともに `n/a`。
  反動で行動できなくなる効果はHP依存の状態変化を伴わないため、専用のリーサルハンドラは不要。

## テスト

- 変更なし。`tests/moves_attack/test_move_ka.py` に既存の5テストがあり、いずれも実装と一致し全て成功することを
  確認した（まもるで防がれた場合／交代で解除／命中後に付与／外れた場合は付与されない／次ターン行動不能）。

## 付随して発見した誤り（進捗表）

- `docs/progress/move.md` の対象行（`がんせきほう`）のPP列が誤って `8` と記載されていたため `5` に修正した
  （`data/move.py` の実装値と一致させた）。
- 同じ「リチャージ」系技（同一ハンドラを共用）である `はかいこうせん` / `ギガインパクト` / `ハイドロカノン` /
  `ハードプラント` / `ブラストバーン` も同様にPP列が `8` と誤記載されていたため `5` に修正した。
  あわせて `ギガインパクト` / `ハードプラント` の効果列が `？` のままだったため
  「使用した次のターンは行動できない。」に修正した。
- `がんせきアックス` の効果列は既存のまま（変更していないが確認のためPP列 `16`→`15` に修正、実装値と一致させた）。
