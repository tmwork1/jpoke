# シャカシャカほう レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/シャカシャカほう.md`）
- `docs/wiki/moves/シャカシャカほう.html` を一次情報として、基本情報（タイプ=くさ／特殊／威力80／
  命中90／対象=相手全員）と効果（ダメージの半分をHP吸収、20%でやけど付与、使用者・相手の
  こおり状態を治す）を記載した。
- 「相手の防御実数値の高さに応じて命中率が変動する」という情報は一次情報に存在せず、
  命中率は固定90であることを確認した（該当の想定は誤りだったため反映していない）。
- 複数対象時の吸収順序・みちづれ絡みの仕様は、本プロジェクトがシングルバトルのみを対象と
  するためスコープ外と明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/シャカシャカほう.md`）
- `docs/plan/moves/ねっとう.md` の構成に倣い、PP修正・使用者自身の無条件解凍ハンドラ追加の
  2点を計画として記載した。
- 被弾側（相手）のこおり解除とちからずくによる無効化は、`ねっとう` レビュー時に
  `こおり_cure_by_thaw_move`（`src/jpoke/handlers/ailment.py`）へ既に汎用実装済みであり、
  シャカシャカほうはタイプ=くさ・`"thaw"` フラグ設定済みのため、追加実装なしでそのまま
  正しく適用されることを確認し、計画書にその根拠を明記した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_sa.py`: `"シャカシャカほう"` の `pp` を `15` → `16` に修正
  （`docs/champions/move_list.txt` 342行目のchampions基準値に合わせた。Gen9本家基準値の
  移行漏れバグ）。
- `src/jpoke/data/moves/move_sa.py`: `Event.ON_TRY_ACTION`（priority=5）に
  `シャカシャカほう_thaw_attacker` を追加登録（使用者自身がこおり状態のとき、
  攻撃前に無条件解凍する処理が未実装だったため）。
- `src/jpoke/handlers/move_attack.py`: `シャカシャカほう_thaw_attacker` を新規実装
  （`ねっとう_thaw_attacker` / `ねっさのだいち_thaw_attacker` と同一パターン）。
- 既存の `シャカシャカほう_apply_burn_to_defender`（20%やけど）・`シャカシャカほう_drain`
  （heal_ratio=0.5）は仕様と一致しており修正不要だった。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」「リーサルテスト」列が `n/a`）。

## テスト
- `tests/moves_attack/test_move_sa.py` に以下を追加：
  - `test_シャカシャカほう_こおり状態で使うと解凍されて攻撃できる`
    （使用者自身の無条件解凍を検証）。
  - `test_シャカシャカほう_こおり状態の相手に当てると解凍する`
    （被弾した相手のこおり解除を検証。既存実装だが未テストだったため追加）。
- 既存の3テスト（やけど付与2件・ドレイン回復1件）は変更なし。
- `python -m pytest tests/ -v` で全3817件（1件skip）が成功することを確認
  （ログ: `C:\Users\tmtmp\Documents\pokemon\jpoke\.loop\test_logs\シャカシャカほう.log`）。
