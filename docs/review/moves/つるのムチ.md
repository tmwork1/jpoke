# つるのムチ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/つるのムチ.md`）。前回レビュー（2026-07-05）では類似の追加効果
  なし技と同様に個別仕様書を作成しない方針としていたが、既存の `はたく.md` など同種の技には
  個別仕様書が存在するため、方針を改め一次情報（docs/wiki/moves/つるのムチ.html）をもとに
  新規作成した。タイプ=くさ、分類=物理、威力=45（第六世代以降）、命中=100、PP=25（第六世代
  以降の値、docs/champions/moves.md の慣例と一致）、接触あり・追加効果なしであることを確認した。

## 実装計画書
- 新規作成（`docs/plan/moves/つるのムチ.md`）。追加効果を持たないため専用ハンドラ不要、
  `handlers={}` で基本ダメージ計算に委ねる方針を明記した（`はたく.md` の計画書を参考に作成）。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ta.py` の `"つるのムチ"` エントリ
  （type="くさ", category="physical", pp=25, power=45, accuracy=100,
  flags={"contact"}, handlers={}）は仕様書・計画書と一致しており誤りなし。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行で `n/a` のためスキップ）。

## テスト
- 変更なし。`tests/moves_attack/test_move_ta.py` の
  `test_つるのムチ_相手にダメージを与える`（前回レビューで追加済み）が実装・仕様と一致して
  いることを確認した（追加効果なしの物理くさ技として、相手のHPが減ることのみを検証）。
- `python -m pytest tests/ -v` を実行し、5801 passed, 1 skipped, 0 failed を確認した。
  ログは `.loop/test_logs/つるのムチ.log` に保存済み。
