# つるのムチ レビュー結果

## 仕様書
- 対象外（追加効果のない通常攻撃技のため、他の同種の技（あくうせつだんなど）と同様に個別の仕様書は作成しない方針を踏襲）

## 実装計画書
- 対象外（追加効果のない通常攻撃技のため、他の同種の技と同様に個別の計画書は作成しない）

## 実装（handlers/ / data/）
- 変更なし
  - `src/jpoke/data/move.py` の `つるのムチ` エントリ（くさ・物理・威力45・命中100・PP25・`contact` フラグ・`handlers={}`）は一次情報（docs/wiki/moves/つるのムチ.html）の現行世代仕様と一致しており、誤り・欠落なし

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`）

## テスト
- 追加: `tests/moves_attack/test_move_ta.py` に `test_つるのムチ_相手にダメージを与える` を新規追加（追加効果のない物理くさ技としてダメージのみ発生することを確認）
- `python scripts/sort_tests.py tests/moves_attack/test_move_ta.py` でソート
- `python scripts/generate_test_list.py` で `docs/tests/moves_attack.md` を更新
- `python -m pytest tests/ -v` で全件実行し、3436 passed, 1 skipped を確認（`test_リフレッシュ_まひを治す` は乱数固定なしのまひ確率依存で稀に失敗する既存の無関係なフレーキーテストであり、単体実行では成功することを確認済み）
