# どくどく レビュー結果

## 仕様書
- 存在しなかったため新規作成した（`docs/spec/moves/どくどく.md`）。
- 一次情報（`docs/wiki/moves/どくどく.html`）をもとに以下を記載:
  - 基本情報（どくタイプ・変化・命中90・PP12・対象1体）
  - 使用者がどくタイプの場合は必中になる効果（第六世代以降の仕様）
  - へんげんじざい/リベロによるタイプ変化後の必中判定、マジックコートで跳ね返した際の
    必中判定はあくまで元の使用者のタイプに依存する点
  - 判定フラグ（まもる/みがわりで防がれる、マジックコート・オウムがえしで反射可能 等）

## 実装計画書
- 存在しなかったため新規作成した（`docs/plan/moves/どくどく.md`）。
- 修正点（PP誤り・必中判定未実装）を明記し、ハンドラ構成・priority根拠・エッジケースを記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py`: `どくどく` の `pp` を `10` → `12` に修正
  （`src/jpoke/data/ps-champ-ja/moves.json` 基準。Gen9本家の10になっていた移行漏れバグ）。
- `src/jpoke/data/moves/move_ta.py`: `Event.ON_MODIFY_ACCURACY` ハンドラを追加登録
  （使用者がどくタイプの場合に必中にする効果が未実装だったため）。
- `src/jpoke/handlers/move_status.py`: `どくどく_accuracy` を新規追加
  （`ctx.attacker.types` にどくタイプが含まれる場合、`HandlerReturn(value=None)` を返して
  必中にする。既存の `かみなり_accuracy` と同じパターン）。
- `どくどく_apply_ailment_to_defender`（もうどく付与）は既存実装のまま変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）。

## テスト
- `tests/moves_status/test_move_ta.py` に以下を追加:
  - `test_どくどく_どくタイプ使用時は必中になる`: どくタイプの使用者（マタドガス）が
    命中率90では本来外れる乱数でも命中することを確認。
  - `test_どくどく_どくタイプでない場合は命中率90で外れることがある`: どくタイプでない
    使用者（ピカチュウ）は通常どおり命中率90で外れることがあることを確認。
  - `test_どくどく_命中すると相手をもうどく状態にする`: 命中時に相手が『もうどく』状態に
    なることを確認。
- 既存の `tests/abilities/test_ability_ma.py`（ミラクルスキンで変化技の命中率を50%に固定する
  テスト、攻撃側はどくタイプでないカビゴン）には影響がないことを確認済み。
- `python -m pytest tests/ -v` で全4263件パス（1件skip、既存分）。
  ログ: `.loop/test_logs/どくどく.log`
