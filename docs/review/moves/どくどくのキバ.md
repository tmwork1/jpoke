# どくどくのキバ レビュー結果

## 仕様書

- 新規作成（`docs/spec/moves/どくどくのキバ.md`）。ポケモンWiki（`docs/wiki/moves/どくどくのキバ.html`）
  を参照し、タイプ: どく / 分類: 物理 / 威力50 / 命中100 / PP15 / 追加効果50%もうどく（第五世代までは
  30%だったが現行仕様は50%）を記載。かみつき技（がんじょうあご対象）である旨、まもる・オウムがえし・
  ちからずくとの関係も記載した。

## 実装計画書

- 新規作成（`docs/plan/moves/どくどくのキバ.md`）。実装は既に仕様どおり完成済みだったため、
  現状実装の確認結果とハンドラ構成・priority根拠・エッジケースを記載した。

## 実装（handlers/ / data/）

- 変更なし。`src/jpoke/data/move.py` の `どくどくのキバ`（type=どく, category=physical, pp=15,
  power=50, accuracy=100, flags={"bite", "contact", "secondary_effect"}）と
  `src/jpoke/handlers/move_attack.py` の `どくどくのキバ_apply_toxic_to_defender`
  （`apply_ailment_to_defender(..., ailment="もうどく", chance=0.5)`）は仕様と一致していることを確認した。

## リーサル計算

- 対象外。`docs/progress/move.md` の当該行「リーサル実装」列は `n/a`。

## テスト

- 変更なし。`tests/moves_attack/test_move_ta.py` の `test_どくどくのキバ_もうどくが発動する` が
  `secondary_chance=1.0` で50%発動判定を強制し、もうどく付与を確認しており仕様と一致することを確認した。
  `sort_tests.py` / `generate_test_list.py` を再実行し、全テスト（3484 passed, 1 skipped）を確認した。

## その他

- `docs/progress/move.md` の当該行に誤り（PP列が16と記載されていたが正しくは15）を発見し修正した。
  また仕様書・計画書を新規作成したため「仕様書」「計画書」列を `x`、「レビュー」列を `✓` に更新した。
