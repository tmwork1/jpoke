# アイアンヘッド レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/アイアンヘッド.md`）。一次情報（`docs/wiki/moves/アイアンヘッド.html`）を
  確認し、タイプ=はがね・分類=物理・威力=80・命中100・PP=16（Champions基準）、追加効果は
  20%の確率で相手をひるませる（本家シリーズでは30%だが、Championsでは20%に変更されている点を
  Wikiの「説明文」節 Champions の記載から確認）ことを記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/アイアンヘッド.md`）。既存実装（`アイアンヘッド_apply_flinch` /
  `MoveData` エントリ）を仕様書と照合した結果、`chance=0.2`・`power=80`・`accuracy=100`・
  `pp=16`・`flags={"contact", "secondary_effect"}` はすべて仕様と一致しており修正不要と判断した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/move_attack.py` の `アイアンヘッド_apply_flinch` および
  `src/jpoke/data/moves/move_a.py` の `MoveData` エントリはいずれも仕様と一致しており誤りはなかった。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）。

## テスト
- `tests/moves_status/test_move_a.py` に誤って配置されていた `test_アイアンヘッド_ひるみが発動しない` /
  `test_アイアンヘッド_ひるみが発動する` を削除した（アイアンヘッドは物理技であり、
  「変化技ハンドラの単体テスト」ファイルである `moves_status` 側に置くべきではなく、
  `moves_attack` 側に既に同等のテストが存在する重複だったため）。
- `tests/moves_attack/test_move_a.py` に `test_アイアンヘッド_ひるみが発動しない`
  （`secondary_chance=0.0` で追加効果不発時の非発動を確認するテスト）を新規追加し、
  既存の `test_アイアンヘッド_ひるみが発動する` と合わせて発動・非発動の両方をカバーするようにした。
