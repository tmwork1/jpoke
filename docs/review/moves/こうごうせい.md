# こうごうせい レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/こうごうせい.md`）。一次情報（ポケモンWiki）を確認し、
  一次情報の「技の仕様」節が「基本的にあさのひざしと同様の効果を持つ」と参照委譲していたため、
  `あさのひざし` の一次情報（回復割合・ばんのうがさ/ノーてんき/エアロック/かいふくふうじとの
  相互作用）を確認したうえで作成した既存の `docs/spec/moves/あさのひざし.md` を土台に、
  タイプ（くさ）・PP（8）を こうごうせい 用に差し替えて作成した。

## 実装計画書
- 既存の `docs/plan/moves/こうごうせい.md` を仕様書・実装と照合し、以下を修正した。
  - PP の記載を 5 → 8 に修正（`docs/champions/move_list.txt` 準拠）。
  - サンプルコードのフィールド名を `labels=[...]` → `flags={...}`（`MoveData` の実際のフィールド名）に修正。
  - 実装が `こうごうせい_heal_self` を新規実装せず既存の `あさのひざし_heal_self` を
    そのまま共用している実態に合わせて記述を修正（`つきのひかり` も同様の共用パターン）。
  - `MoveData.target="self"` が未設定だと `まもる`/`マジックコート` の判定に使われる
    `move.target == "foe"` がデフォルト値のまま誤判定される、という注意点を追記。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ka.py` の `こうごうせい` エントリを修正。
  - PP を `5` → `8` に修正（champions基準PPへの移行漏れバグ）。
  - `target="self"` を追加（未設定のままだとデフォルト値 `"foe"` になり、相手の `まもる` に
    誤って防がれたり `マジックコート` に誤って跳ね返されたりするバグがあった）。
  - `handlers/move_status.py` 側の変更なし（`あさのひざし_heal_self` をそのまま共用しており、
    `battle.weather_for(mon)` によりばんのうがさ/ノーてんき/エアロックを正しく考慮済みのため）。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」「リーサルテスト」列がいずれも `n/a`）。

## テスト
- `tests/moves_status/test_move_ka.py` の こうごうせい テストを見直し、以下に変更した。
  - あめ/はれ/通常天候/まんたん失敗の4個別テストを、`あさのひざし` と同様の
    `@pytest.mark.parametrize` による天候別回復量テスト（通常1/2, あめ/おおあめ/すなあらし/ゆき1/4,
    はれ/おおひでり2/3 の全7ケース）に統合し、未検証だった おおあめ・すなあらし・おおひでり・ゆき
    のケースを追加した。
  - `まんたんなら失敗` テストは維持。
  - `target="self"` バグを検出するための `まもるで防がれない` テストを追加。
  - `target="self"` バグを検出するための `マジックコートで跳ね返されない` テストを追加。
  - 追加・修正後のテストは全て pass（`まもるで防がれない`/`マジックコートで跳ね返されない` は
    修正前の実装だと失敗していたことを確認済み）。
- `python -m pytest tests/ -v` を実行し、4154 passed, 1 skipped（既存の無関係なskip）を確認。
  ログは `.loop/test_logs/こうごうせい.log` に保存。
