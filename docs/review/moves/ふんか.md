# ふんか レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ふんか.md`）。一次情報（`docs/wiki/moves/ふんか.html`）に基づき、
  タイプ=ほのお、分類=特殊、威力=可変（1〜150、`floor(150×残りHP÷最大HP)`）、命中100、PP5、
  対象=相手全員（本プロジェクトはシングルバトルのみのため相手1体）、優先度0、接触なしと明記した。
  しおふき・ドラゴンエナジーと同一の威力計算式であることも記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/ふんか.md`）。既存実装（`ふんか_calc_power`）が仕様書と完全一致することを確認し、
  修正不要と結論づけた。ハンドラ構成表（イベント・priority・subject_spec）を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/move_attack.py` の `ふんか_calc_power`（`modifier = floor(HP比率 × 4096)`、
  最小値1保証）は仕様書と一致しており、修正不要。
  `src/jpoke/data/moves/move_ha.py` の `MoveData`（type=ほのお/category=special/pp=5/power=150/accuracy=100）も
  一次情報と一致しており、修正不要。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装/リーサルテスト列は n/a）。
  威力は攻撃側自身の現在HPにのみ依存し、相手のHP分布（`StateDist`）を直接左右する要素ではないため、
  `lethal.py` 側の専用ハンドラは不要。

## テスト
- 変更なし。`tests/moves_attack/test_move_ha.py` の既存2件
  （`test_ふんか_HP満タンのとき威力150`, `test_ふんか_HP半分のとき威力が低下する`）は
  しおふきの既存テストと同一パターンで、仕様・実装と整合しているため修正不要。
  `python -m pytest tests/ -v` で全3579件（+skip 1件）パスを確認（`.loop/test_logs/ふんか.log`）。

## 進捗表の修正
- `docs/progress/move.md` の ふんか 行について、以下を修正した。
  - 仕様書列・計画書列を `-` → `x`
  - レビュー列を `-` → `✓`
  - PP列を `8` → `5`（一次情報・実装の `pp=5` と不一致だったため）
  - 効果列を、実際の計算式・条件（floor・最小値1）を明記した文言に修正
