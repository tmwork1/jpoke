# おにび レビュー結果

## 仕様書
- PP を 15 → 16 に修正（`src/jpoke/data/ps-champ-ja/moves.json` の値を正とする。championsではPPが
  8/12/16/20の4段階に統一されており、Gen9本家の15はそのまま踏襲されない）
- 「失敗条件」節を修正: 実装は技固有の `ON_BEFORE_APPLY_MOVE` ハンドラを持たず、
  `ailment_manager.apply` 内部の汎用チェック（既存状態異常・タイプ相性）で失敗を判定している
  ことを明記
- 失敗時のログ出力は `MoveExecutor._execute_status_hit` が `ON_STATUS_HIT` の戻り値の偽を
  検知して汎用的に `LogCode.MOVE_FAILED` を記録することを明記
- 「防がれる条件」節にもらいび特性の挙動を追記し、`ON_BEFORE_APPLY_AILMENT` 経由の汎用判定
  であることを明記
- 「イベントフロー」節を実装（`ON_STATUS_HIT` のみ）に合わせて修正

## 実装計画書
- PP を 15 → 16 に修正
- 実際には存在しない `おにび_can_apply`（`ON_BEFORE_APPLY_MOVE` priority=130）ハンドラの
  記載を削除。`ailment_manager.apply` が既存状態異常チェック・タイプ相性チェック・
  `ON_BEFORE_APPLY_AILMENT` イベントを汎用的に処理するため、技固有の失敗チェックハンドラは
  不要であることを明記（`どくどく` `さいみんじゅつ` `でんじは` 等の既存実装と同じパターン）
- ハンドラ構成表・実装コード例を実際の実装（`おにび_apply_burn` のみを `ON_STATUS_HIT` に登録）
  に合わせて修正
- エッジケース節をもらいび特性・`_can_apply_by_type` の実際の判定方法に合わせて修正

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `おにび` MoveData の `pp` を 15 → 16 に修正
  （`src/jpoke/data/ps-champ-ja/moves.json` は既に16であり、実装側がGen9本家値のまま移行漏れしていた
  バグ）
- `handlers/move_status.py` の `おにび_apply_burn`（`apply_ailment_to_defender` 呼び出し）自体は
  仕様・既存パターンと整合しており変更なし

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a。おにびはダメージを与えない変化技のため）

## テスト
- `tests/moves_status/test_move_a.py` に `test_おにび_ほのおタイプには無効` を追加
  （対象がほのおタイプの場合はやけどが付与されず失敗することを確認）
- 既存の `test_おにび_すでに状態異常なら失敗` `test_おにび_やけど付与` は仕様と整合しており変更なし
- `tests/abilities/test_ability__group.py` の `test_状態異常無効_かたやぶりで無効`
  （みずのベール×おにび）、`tests/items/test_item_ta.py` の
  `test_チーゴのみ_やけど付与直後に即時回復する`、`tests/test_field.py` の
  `test_強天候_反対タイプ技は失敗`（おおあめ×おにび）は既存のまま変更なし
- `python -m pytest tests/ -v` で全4122件パス（1件スキップ）を確認。結果は
  `.loop/test_logs/おにび.log` に保存
