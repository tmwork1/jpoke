# しっぽをふる レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/しっぽをふる.md`）。一次情報（`docs/wiki/moves/しっぽをふる.html`）に基づき、
  ノーマルタイプ・変化・命中100・PP30・優先度0・非接触・「相手の『ぼうぎょ』ランクを1段階下げる」
  という基本情報と判定フラグ（急所なし・追加効果なし・まもる○・マジックコート○・オウムがえし○・
  よこどり×）を記載した。
- `docs/champions/move_list.txt` に本技のエントリが存在しないことを確認した（TM/わざマシン非対応の
  初期習得わざのため）。既にレビュー済みの同種ケース（`ひっかく`・`なきごえ`）と同じ扱いとして、
  Gen9基準のPP30をそのまま採用する旨を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/しっぽをふる.md`）。既存実装（`src/jpoke/data/moves/move_sa.py`・
  `src/jpoke/handlers/move_status.py`）が仕様と一致していることを確認した内容、
  `ON_STATUS_HIT` の subject_spec（`attacker:self`）・priority（100、同種ハンドラとの整合）の根拠、
  リーサル計算が対象外である理由を記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/move_status.py` の `しっぽをふる_modify_defender_stats` は
  共通ユーティリティ `modify_defender_stats(battle, ctx, value, stats={"def": -1})` を呼び出しており、
  `にらみつける`・`あまえる` など同種の防御側ランクダウン変化技と同じ実装パターンで、
  仕様書と一致している。
- `src/jpoke/data/moves/move_sa.py` の `しっぽをふる` エントリ（`type="ノーマル"`, `category="status"`,
  `pp=30`, `accuracy=100`）も仕様書と一致しており、修正不要と判断した。
  `target` 未指定により `MoveData` のデフォルト `"foe"`（相手1体）が適用され、
  仕様の「対象: 相手1体（シングル）」とも一致する。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行が「リーサル実装: n/a」）。ダメージを与えない変化技のため
  専用のリーサルハンドラは不要。

## テスト
- `tests/moves_status/test_move_sa.py` に以下を追加した。
  - `test_しっぽをふる_ぼうぎょが1段階下がる`: 使用すると相手のぼうぎょランクが1段階下がることを確認。
  - `test_しっぽをふる_ぼうぎょが最低値のとき変化なし`: ぼうぎょランクがすでに-6のときは変化しないことを確認。
- `python -m pytest tests/ -v` を実行し、4163 passed, 1 skipped で全件パスを確認した
  （結果は `.loop/test_logs/しっぽをふる.log` に保存。1回だけ `test_むしくい_...` がタイムアウト系の
  乱数由来でフレーキーに失敗したが、単体実行・再実行では常に成功しており本タスクの変更とは無関係）。
