# ビルドアップ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ビルドアップ.md`）。既存の `めいそう.md` / `つるぎのまい.md` と同じ形式で作成。
- 基本情報（タイプ: かくとう、変化、PP20、対象: 自分、優先度0、直接攻撃なし）と効果（こうげき・ぼうぎょをそれぞれ1段階上げる）、判定フラグ（急所×、命中判定なし、追加効果判定なし、まもる×、マジックコート×、よこどり○、オウムがえし×）を一次情報（`docs/wiki/moves/ビルドアップ.html`）から記載。

## 実装計画書
- 既存の `docs/plan/moves/ビルドアップ.md` の出典参照を修正（存在しない `docs/spec/moves/status_move.md` 行208 →新規作成した `docs/spec/moves/ビルドアップ.md`）。
- ハンドラ構成・priority根拠・実装箇所の記述は既存実装と整合していたため変更なし。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_status.py` の `ビルドアップ_modify_attacker_stats` は `modify_attacker_stats(..., stats={"atk": 1, "def": 1})` を呼ぶのみで、既存の `めいそう_modify_attacker_stats` 等と同じ実装パターンであり問題なし。
- `src/jpoke/data/moves/move_ha.py` の `"ビルドアップ"` エントリに **`target="self"` が欠落しているバグを発見・修正**。
  - `Move.is_blocked_by_protect` / `Move.is_reflectable`（`src/jpoke/model/move.py`）は `move.target` を参照しており、デフォルト値は `"foe"`。
  - `target` を明示していなかったため、修正前は「まもるで防がれる」「マジックコートで跳ね返される」という誤った判定になっていた（仕様書のまもる×・マジックコート×と矛盾）。
  - `target="self"` を追加し、`かたくなる` 等と同じ扱いに修正。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行「リーサル実装」列が `n/a`）。

## テスト
- `tests/moves_status/test_move_ha.py` に以下を追加:
  - `test_ビルドアップ_マジックコートで跳ね返されない`
  - `test_ビルドアップ_まもるで防がれない`
  - （`target="self"` 欠落バグの回帰防止。修正前はこれらのテストが失敗することを確認済み）
- 既存の `test_ビルドアップ_発動前後のランク変化`（ランク上限を含む網羅的パラメトライズテスト）は変更なし。
- `python -m pytest tests/ -v` で全4597件（+1 skipped）がパス。結果は `.loop/test_logs/ビルドアップ.log` に保存。
