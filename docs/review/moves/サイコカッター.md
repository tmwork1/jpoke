# サイコカッター レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/サイコカッター.md`）。
  ポケモンWiki一次情報（`docs/wiki/moves/サイコカッター.html`）をもとに、
  タイプ=エスパー、分類=物理、威力=70、命中=100、PP=20（`src/jpoke/data/ps-champ-ja/moves.json` と一致）、
  対象=相手1体選択、優先度0、接触なし（`slash`フラグのみ、`contact`なし）、
  効果=急所ランク+1（追加効果なし）を記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/サイコカッター.md`）。
  既存実装（`data/moves/move_sa.py`）が仕様と完全に一致していたため、計画書には
  「差分なし」として実装内容をそのまま記載した。

## 実装（handlers/ / data/）
- 変更なし。
  `src/jpoke/data/moves/move_sa.py` の `"サイコカッター"` エントリ（`type="エスパー",
  category="physical", pp=20, power=70, accuracy=100, critical_rank=1, flags={"slash"},
  handlers={}`）は仕様書・計画書と一致しており、修正は不要だった。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`。追加効果を持たない
  通常の物理攻撃技のため）。

## テスト
- `tests/moves_attack/test_move_sa.py` に以下の3件を追加:
  - `test_サイコカッター_急所ランクが1` — 乱数0で急所判定が発生することを確認
  - `test_サイコカッター_急所ランクが1_乱数大で急所なし` — 乱数が閾値(1/8)以上のとき急所にならないことを確認
  - `test_サイコカッター_きれあじで威力1_5倍` — `slash`フラグにより特性きれあじで威力1.5倍になることを確認
- `python -m pytest tests/ -v` 実行結果: 3759 passed, 1 skipped（既存の1件のみ、本レビューとは無関係）。
