# はさむ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/はさむ.md`）。一次情報（ポケモンWiki）に基づき、ノーマルタイプ・物理・威力55・命中100・PP30・直接攻撃・追加効果なしの内容を記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/はさむ.md`）。既存の単純な物理技（`いわおとし`等）の計画書を参考に、専用ハンドラ不要である旨と実装済みエントリの整合性を記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ha.py` の `"はさむ"` エントリ（type=ノーマル, category=physical, pp=30, power=55, accuracy=100, flags={"contact"}, handlers={}）は仕様書と完全に一致しており、修正不要だった。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）。

## テスト
- 追加。`tests/moves_attack/test_move_ha.py` に `test_はさむ_相手にダメージを与える` を追加し、既存の単純ダメージ確認テスト（`はたく`等）と同様のパターンで通常攻撃によるダメージ発生を確認した。
- `scripts/sort_tests.py` でソート、`scripts/generate_test_list.py` で一覧を更新、`python -m pytest tests/ -v` で全件（3560 passed, 1 skipped）を確認した。
