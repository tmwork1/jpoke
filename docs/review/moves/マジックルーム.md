# マジックルーム レビュー結果

## 仕様書
- 変更なし
- `docs/spec/moves/マジックルーム.md`（一次情報の生スクレイプ）は wiki 本文と照合し内容一致を確認済み（誤りなし）。
- `docs/spec/fields/マジックルーム.md`（もちもの無効化の詳細を含むキュレーション済み仕様）も
  `docs/wiki/global_fields/マジックルーム_(場の状態).html` と照合し内容一致を確認済み（誤りなし）。

## 実装計画書
- 新規作成: `docs/plan/moves/マジックルーム.md`
  - `docs/plan/moves/トリックルーム.md`（同種の全体の場発動技）を参考に、技データ（タイプ/PP/優先度/対象）・
    `ON_STATUS_HIT` の subject_spec と priority の根拠・ハンドラ構成・注意事項を記載した。

## 実装（handlers/ / data/）
- 修正: `src/jpoke/data/moves/move_ma.py` の `"マジックルーム"` エントリで `pp=10`（Gen9本家基準の
  移行漏れ）となっていたのを `pp=12`（`src/jpoke/data/ps-champ-ja/moves.json` 準拠）に修正した。
- `handlers/move_status.py` の `マジックルーム_activate_global_field`、`handlers/field.py` の
  `マジックルーム_apply` / `マジックルーム_remove` / `マジックルーム_tick`、
  `data/field/global_field.py` の `GLOBAL_FIELD["マジックルーム"]` は仕様と照合し誤りなし
  （`target="field"` によりまもる・マジックコートを正しく無効化、`ON_SWITCH_IN` での再判定、
  `ItemManager.add_disabled_reason`/`remove_disabled_reason` と `Item.reset_on_switch_out` の
  組み合わせにより、ベンチに退避したポケモンの無効化フラグも交代時に正しくリセットされることを確認した）。
- 優先度: `MoveData.priority` は未指定（デフォルト0）で、第六世代以降の優先度0仕様と一致しているため
  変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a。ダメージを与えない全体の場発動の変化技のため）

## テスト
- 追加: `tests/moves_status/test_move_ma.py` に以下を追加（`トリックルーム` の同種テストを参考に作成）
  - `test_マジックルーム_PPは12`
  - `test_マジックルーム_マジックコートで跳ね返されない`
  - `test_マジックルーム_まもるで防がれない`
  - `test_マジックルーム_場が発動する`
  - `test_マジックルーム_発動中に再使用すると解除される`
  - `test_マジックルーム_発動すると使用者のもちものも無効化される`
- 既存の `tests/test_field.py` のフィールド側テスト（交代後のもちもの無効化・再発動での即解除・
  もちもの効果無効化）は変更なし。
- `python -m pytest tests/ -v` で 4626 passed, 1 skipped を確認（ログ: `.loop/test_logs/マジックルーム.log`）。
