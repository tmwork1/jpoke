# ほろびのうた レビュー結果

## 仕様書
- 変更なし（`docs/spec/moves/ほろびのうた.md` は一次情報（wiki）と照合済みで内容に誤りなし）

## 実装計画書
- 以下を修正した：
  - PP を 5 → 8 に修正（`docs/champions/move_list.txt` の値が正。第九世代本家PPからの移行漏れ）
  - `ON_BEFORE_APPLY_MOVE` の失敗判定を「`ctx.defender` のみ確認」から
    「`battle.actives` 全員がすでにほろびのうた状態の場合のみ失敗」に修正
    （実際の実装・テストと整合させた。片方だけがすでに状態でも、もう片方に
    効果があれば技は成功するのが正しい仕様）
  - 「まもる系を貫通する」を「計画書スコープ外」から実装対象に変更し、
    `MoveData.flags` に `unprotectable` を追加する方針を明記
    （既存の `ないしょばなし`（`flags={"sound", "unprotectable"}`）と同じパターン）
  - `data/move.py` → `data/moves/move_ha.py`、`labels=` → `flags=` など
    ファイル分割後の実態に合わせて記述を更新
  - コード例から未使用の `ctx=ctx` 引数を削除し、実装と一致させた

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `"ほろびのうた"` MoveData を修正：
  - `pp=5` → `pp=8`（`docs/champions/move_list.txt` 準拠）
  - `flags={"sound"}` → `flags={"sound", "unprotectable"}`
    （仕様書に「まもる/ワイドガード/トリックガード/ダイウォールなどあらゆるガード状態を
    無視して有効」とあるが、`unprotectable` フラグが未設定だったためまもるでブロックされる
    バグがあった。`Move.is_blocked_by_protect` は `target == "foe" and not
    has_flag("unprotectable")` で判定されるため、フラグ追加により修正）
  - `handlers/move_status.py` の `ほろびのうた_apply` / `ほろびのうた_can_apply` は
    仕様・計画と整合しており変更なし

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装・リーサルテスト列は n/a）

## テスト
- `tests/moves_status/test_move_ha.py` に以下を追加：
  - `test_ほろびのうた_PPは8`: PP=8 であることを確認
  - `test_ほろびのうた_まもる状態の相手にも効果が発動する`: unprotectableフラグ修正の回帰テスト
  - `test_ほろびのうた_みがわりを貫通して付与される`: soundフラグによるみがわり貫通の確認
- 既存テスト（3ターン後瀕死・使用者と相手への付与・全員済みで失敗・片方だけ済みで成功）は
  実装と整合しており変更なし
- `python -m pytest tests/ -v` で 4398 passed, 1 skipped を確認
  （結果: `.loop/test_logs/ほろびのうた.log`）
