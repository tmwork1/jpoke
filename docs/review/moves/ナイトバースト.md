# ナイトバースト レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ナイトバースト.md`）。
  - `docs/wiki/moves/ナイトバースト.html` および `docs/champions/move_list.txt` を参照し、
    タイプ=あく、分類=特殊、威力=90（Champions値）、命中=95、PP=12（Champions値）、
    対象=相手1体、優先度=0、接触なし、`secondary_effect` フラグを記載。
  - 効果: 追加効果として40%の確率で相手の命中率を1段階下げる（wiki記載値どおり。
    タスク説明文に記載された「20%」はwiki一次情報と不一致のため採用しなかった）。
  - 判定フラグ（急所・命中判定・追加効果・まもる・おうじゃのしるし・マジックコート・
    よこどり・オウムがえし・ちからずく）をwikiの判定表に基づき明記。

## 実装計画書
- 新規作成（`docs/plan/moves/ナイトバースト.md`）。
  - 既存実装が本家シリーズ値（`power=85, pp=10`）のままChampions移行漏れであったことを記録し、
    `power=90, pp=12` への修正内容を明記。
  - 追加効果ハンドラ（`ナイトバースト_lower_acc`）・`flags`・イベント（`Event.ON_DAMAGE_HIT`）は
    仕様通りで変更不要である旨を記載。
  - リーサル計算は対象外である旨、およびおうじゃのしるし等のエッジケースを記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_na.py` の `"ナイトバースト"` エントリを修正。
  - `pp=10` → `pp=12`、`power=85` → `power=90` に修正（`docs/champions/move_list.txt` の
    ナイトバースト行: 威力90・命中95・PP12 に合わせた。Gen9本家基準値からの移行漏れバグ）。
  - `accuracy=95` は変更なし（wiki・champions値と一致）。
- `src/jpoke/handlers/move_attack.py` の `ナイトバースト_lower_acc`
  （`modify_defender_stats(..., stats={"accuracy": -1}, chance=0.4)`）は40%発動・
  命中率-1段階の仕様と一致しており、修正不要と確認した。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`。命中率ランク低下のみで
  HP分布に影響しないため）。

## テスト
- 既存テスト `tests/moves_attack/test_move_na.py::test_ナイトバースト_命中率1段階低下が発動する`
  を確認。`secondary_chance=1.0` で命中率ランクが-1になることを検証しており、仕様と一致するため
  変更なし。
- `python -m pytest tests/ -v` を実行し、3933 passed, 1 skipped で全件パスを確認
  （`.loop/test_logs/ナイトバースト.log` に保存）。
  - 実行途中で `test_むしくい_ねんちゃく持ちをひんしにさせた場合は奪える` が1回失敗したが、
    まひによる行動不能（25%）のランダム性に起因する既存の非決定的テストであり、ナイトバーストの
    変更とは無関係。単体再実行では成功することを確認した。
