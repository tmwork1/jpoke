# ようせいのかぜ レビュー結果

## 仕様書
- 新規作成（`docs/wiki/moves/ようせいのかぜ.html` に基づき、基本情報・効果（追加効果なし）・
  判定フラグ・風技フラグの由来を記載）

## 実装計画書
- 新規作成

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ya.py` の `ようせいのかぜ` エントリ
  （`type="フェアリー"`, `category="special"`, `power=40`, `accuracy=100`, `pp=30`,
  `flags={"wind"}`, `handlers={}`）は仕様と一致しており修正不要。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a。追加効果を持たない通常攻撃技のため）

## テスト
- 新規追加。`tests/moves_attack/test_move_yarawa.py::test_ようせいのかぜ_相手にダメージを与える`
  を追加し、追加効果なしの通常ダメージ技として機能することを確認した。
