# へんしん レビュー結果

## 仕様書
- 既存の仕様書は初期の生wikiダンプ形式だったため、現行の構成（基本情報/効果/成功条件・失敗条件/
  技の仕様/判定フラグ/実装状況/参照）に全面的に書き直した。
- PPをChampions基準の12（`docs/champions/move_list.txt`）で明記し、旧記載の「PP10」は本家世代の
  値であることを明確化した。
- 対象範囲を本プロジェクトが扱う第五世代以降/SVルールに絞り、コピーする/しないものの一覧、
  失敗条件（みがわり・イリュージョン・スターモービル・テラスタル関連等）、技の仕様
  （まもる貫通・こだわり系との相互作用・かなしばり継続・アンコール不可等）を整理した。
- 本技が「実装保留」である理由（大規模な変身機構が必要で、特性かわりものと共有基盤である旨）を
  明記した。

## 実装計画書
- 存在しなかったため新規作成した（`docs/plan/moves/へんしん.md`）。
- 実装保留の判断理由（`Pokemon`モデルへの大規模な変更が必要な点を4項目で具体的に整理）、
  `data/moves/move_ha.py`の修正内容、将来実装時の設計メモ、リーサル計算・テストが対象外である
  旨を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py`の`へんしん`エントリを修正した。
  - `pp`を10から12に修正（Champions基準の`move_list.txt`に合わせた。旧値はSV本家基準の移行漏れ）。
  - `accuracy=100`を削除（必中技のため他の必中変化技と同様にデフォルト`None`に統一。従来の
    `accuracy=100`だと回避率補正の影響を受けてしまう誤りだった）。
  - `flags={"non_encore"}`は仕様（`docs/spec/volatiles/アンコール.md`）と一致しているため変更なし。
  - `handlers={}`は実装保留のまま維持し、保留理由を示すコメントを追加した。

## リーサル計算
- 対象外（`docs/progress/move.md`のリーサル実装列を`n/a`に修正。実装保留のため）。

## テスト
- 変更なし（技自体が実装保留のため、`tests/moves_status/`への追加は行わない。既存4395件の
  全テストが通ることを確認した）。
