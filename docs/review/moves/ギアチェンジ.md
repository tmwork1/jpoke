# ギアチェンジ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ギアチェンジ.md`）。
- docs/wiki/moves/ギアチェンジ.html を参照し、効果（自分のこうげき+1・すばやさ+2）、
  判定フラグ（必中・追加効果なし・まもる無効・マジックコート反射不可・よこどり対象）を記載。
- PPは10（src/jpoke/data/ps-champ-ja/moves.json に該当行が存在しないため、Wiki値かつ現行実装値の10を採用し、
  その旨を仕様書に明記した）。

## 実装計画書
- 新規作成（`docs/plan/moves/ギアチェンジ.md`）。
- `りゅうのまい` / `ちょうのまい` / `つるぎのまい` と同じ自己強化ランクアップパターンとして
  `ON_STATUS_HIT`（priority 100 = デフォルト）で `modify_attacker_stats` ヘルパーを利用する構成を明記。

## 実装（handlers/ / data/）
- **バグ修正**: 実装が `handlers={}`（コメント「追加効果なし」）の空スタブになっており、
  本来の効果（こうげき+1・すばやさ+2）が一切実装されていなかった。
  - `src/jpoke/handlers/move_status.py` に `ギアチェンジ_modify_attacker_stats` を追加
    （`きんぞくおん_modify_defender_stats` の後に配置。最終並びは sort_handlers.py で整形予定）。
  - `src/jpoke/data/moves/move_ka.py` の `"ギアチェンジ"` エントリに `target="self"` と
    `Event.ON_STATUS_HIT` ハンドラ登録を追加。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行は `n/a`）。ダメージを与えない自己バフ技のため
  リーサル計算ハンドラは不要。

## テスト
- `tests/moves_status/test_move_ka.py` に4件追加（`きんぞくおん` テストの後、`くすぐる` テストの前）。
  - `test_ギアチェンジ_こうげき1段階すばやさ2段階上がる`
  - `test_ギアチェンジ_こうげき最大でもすばやさは上昇する`
  - `test_ギアチェンジ_マジックコートで跳ね返されない`
  - `test_ギアチェンジ_自分対象のためまもるで防がれない`
- `python -m pytest tests/ -v` は 4466 passed, 1 skipped で全件成功
  （ログ: `.loop/test_logs/ギアチェンジ.log`）。
