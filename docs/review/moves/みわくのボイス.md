# みわくのボイス レビュー結果

## 仕様書
- 新規作成（`docs/wiki/moves/みわくのボイス.html` に基づき、基本情報・追加効果
  （そのターンにランクが上がった相手を100%でこんらん）・判定フラグ・こんらん付与の
  無効化条件を記載。しっとのほのお仕様書と対応する内容で統一）

## 実装計画書
- 新規作成

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ma.py` の `みわくのボイス` エントリ
  （`type="フェアリー"`, `power=80`, `accuracy=100`, `pp=10`,
  `flags={"sound", "secondary_effect"}`、`Event.ON_DAMAGE_HIT` に
  `みわくのボイス_apply_confusion_to_defender` を登録）と、`handlers/move_attack.py` の
  `みわくのボイス_apply_confusion_to_defender`（`ctx.defender.stat_raised_this_turn` を
  確認した上で `apply_confusion_to_defender` を呼ぶ）は仕様と一致しており修正不要。
  `sound` フラグにより `core/move_executor.py` の汎用ルートでみがわりを貫通する点も
  個別ハンドラ不要で正しく処理されている。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が保留。こんらん付与のみで HP 分布に
  影響しない追加効果のため）

## テスト
- 変更なし。`tests/moves_attack/test_move_ma.py` の
  `test_みわくのボイス_ランクが上がった相手にこんらんが付与される` と
  `test_みわくのボイス_ランクが上がっていない相手にはこんらんが付与されない` が
  既に追加効果の両分岐を検証しており過不足なし。
