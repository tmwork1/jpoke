# はなふぶき レビュー結果

## 仕様書

- 新規作成（`docs/spec/moves/はなふぶき.md`）。
- 一次情報（`docs/wiki/moves/はなふぶき.html`）に基づき、タイプ・分類・威力・命中率・対象・
  優先度・接触の有無・判定フラグ（急所／命中判定／追加効果／まもる／おうじゃのしるし／
  マジックコート／よこどり／オウムがえし）・風の技（wind）フラグの扱いを整理した。
- PPについては一次情報がPP15（第9世代基準）だが、`docs/champions/changes_from_sv.md` の
  「すべての技のPPが8・12・16・20の4段階に統一」ルールにより、本プロジェクトが対象とする
  ポケモンチャンピオンズでは基礎PP15が16に丸められることを明記した
  （`src/jpoke/data/ps-champ-ja/moves.json` の記載値16と一致）。

## 実装計画書

- 新規作成（`docs/plan/moves/はなふぶき.md`）。
- 追加効果なしのため新規ハンドラは不要であることを確認し、その根拠を記載した。
- PP16が誤りではなく、ポケモンチャンピオンズのPP統一ルールに基づく正しい値であることを
  検証した経緯を記録した（一度PP15への修正を検討したが、レビュー中に
  `docs/champions/changes_from_sv.md` を確認し誤りだったことに気づいたため、
  実装は変更していない）。

## 実装（handlers/ / data/）

- 変更なし。`src/jpoke/data/moves/move_ha.py` の `はなふぶき`
  （`type="くさ"`, `category="physical"`, `pp=16`, `power=90`, `accuracy=100`,
  `flags={"wind"}`, `handlers={}`）は仕様書・一次情報と整合しており、修正不要と判断した。
- `flags={"wind"}` により `かぜのり_absorb_wind` / `ふうりょくでんき_on_damage`
  （`src/jpoke/handlers/ability.py`）の対象判定に正しく含まれることを確認した。

## リーサル計算

- 対象外。`docs/progress/move.md` の はなふぶき 行はリーサル実装／リーサルテストとも `n/a`。
  固定威力・追加効果なしの通常攻撃技のため専用ハンドラは不要。

## テスト

- 変更なし。`tests/moves_attack/test_move_ha.py` の
  `test_はなふぶき_相手にダメージを与える` で基本的なダメージ発生を確認済み。
  風技フラグの相互作用（かぜのり／ふうりょくでんき）は特性側テスト
  （`tests/abilities/test_ability_ka.py`）で別技を用いて検証済みのため追加不要と判断した。
- `python -m pytest tests/ -v` で全3567件パス（1件skip）を確認した
  （ログ: `.loop/test_logs/はなふぶき.log`）。

## 進捗表の更新

- `docs/progress/move.md` の はなふぶき 行: 仕様書列・計画書列を `-` → `x`、レビュー列を
  `-` → `✓` に更新した（実装・テスト・PP・その他列は変更なし、既存の記載どおり正しかった）。
