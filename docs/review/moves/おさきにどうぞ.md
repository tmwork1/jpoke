# おさきにどうぞ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/おさきにどうぞ.md`）
- 一次情報（`docs/wiki/moves/おさきにどうぞ.html`）と `src/jpoke/data/ps-champ-ja/moves.json`（L146）を照合し、
  基本情報（ノーマル/変化/威力-/命中-〈必中〉/PP16/対象自分以外の1体/優先度0/直接攻撃×/
  ゆびをふる対象外）および本来の効果（対象のすばやさ・優先度・特性あとだし/きんしのちから・
  持ち物こうこうのしっぽ/まんぷくおこう・さきおくりの効果を無視して使用者の直後に行動させる、
  第八世代以降は元々次に行動予定だった相手にも成功する、行動順が変わっても対象の技の優先度
  情報自体は保持される、すでに行動済み〈さいはい/おどりこ経由を含む〉の対象には失敗する）
  を明記した。
- 本プロジェクト（シングルバトル専用、`battle.actives` は常に2体以下）では「使用者の直後に
  割り込ませる」効果自体が意味を持たないダブル/トリプル専有効果であり、
  `docs/progress/move.md` で本技が全列「ダブル専用」として扱われていることと整合させ、
  対象外である旨を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/おさきにどうぞ.md`）
- `docs/spec/turn.md`（`Event.ON_APPLY_MOVE` priority 100「おさきにどうぞ: 失敗条件」）の
  失敗条件チェックのみを実装したとしても、行動順操作自体が未実装のためシングルバトルで
  観測可能な効果は生まれず、このゆびとまれ・いかりのこな・てだすけ・さきおくり等の
  同種ダブル専有技との一貫性を欠くことから、実装を見送る判断を明記した
  （`docs/review/moves/いかりのこな.md` の判断を踏襲）。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `"おさきにどうぞ"` エントリを仕様書と照合し、
  **PP の誤り（15 → 16）を修正**した（`src/jpoke/data/ps-champ-ja/moves.json` L146 の値 16 が正）。
- `handlers={}`（追加効果なし）はダブル専用のため対象外という方針と一致しているため据え置き、
  コメントを「追加効果なし」から「ダブル専用（本プロジェクトはシングルバトル専用のため対象外）」に
  変更して意図を明確化した。
- `handlers/move_status.py` への追加は行っていない（追加効果を実装しない方針のため不要）。

## リーサル計算
- 対象外（`docs/progress/move.md` のおさきにどうぞ行「リーサル実装」「リーサルテスト」列が
  いずれも「ダブル専用」）。本技はダメージを与えず、HP分布にも影響しないため、
  仮に実装対象であってもリーサル計算への影響はない。

## テスト
- 対象外。本プロジェクトのスコープでは本技に観測可能な効果がなく（`handlers={}` のまま）、
  他の全ダブル専用技（このゆびとまれ・いかりのこな・さきおくり等）にも専用テストが
  存在しないことを確認した上で、本技についても新規テストの追加は行わなかった。
- `python -m pytest tests/ -v` を実行し、4112 passed, 1 skipped で全件成功。
  結果は `.loop/test_logs/おさきにどうぞ.log` に保存した。

## 進捗表
- `docs/progress/move.md` のおさきにどうぞ行の「レビュー」列を `ダブル専用` から `✓` に更新した
  （他の列はダブル専用のまま据え置きが正しいと判断したため変更していない。PP列は実装修正に
  伴い既存の値16のまま据え置き）。
