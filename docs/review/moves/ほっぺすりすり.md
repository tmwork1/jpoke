# ほっぺすりすり レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ほっぺすりすり.md`）。一次情報（ポケモンWiki）に基づき、でんきタイプ・物理・威力20・命中100・PP20・直接攻撃、追加効果100%まひを記載。

## 実装計画書
- 新規作成（`docs/plan/moves/ほっぺすりすり.md`）。`でんじほう` の計画書を参考に、ハンドラ構成・priority根拠・エッジケースを記載。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_attack.py` の `ほっぺすりすり_apply_paralysis_to_defender` は仕様通り（`apply_ailment_to_defender` で100%まひ）で変更なし。
- `src/jpoke/data/moves/move_ha.py` の `MoveData` に `flags={"contact"}` のみが設定されており、追加効果100%を持つにもかかわらず `secondary_effect` フラグが欠落していた（`でんじほう` 等の他の100%追加効果技と比較して不整合）。
  `flags={"contact", "secondary_effect"}` に修正し、`ちからずく`（威力1.3倍・追加効果無効化）や `てんのめぐみ` 等の判定と正しく連動するようにした。
- `python scripts/sort_data/sort_moves.py` を実行し、並び順を確認済み（変更なし）。

## リーサル計算
- 対象外（`n/a`）。まひはダメージ量に影響しないため専用のリーサルハンドラは不要。

## テスト
- `tests/moves_attack/test_move_ha.py` に以下を追加:
  - `test_ほっぺすりすり_タイプと分類と威力と命中率とppが正しい`
  - `test_ほっぺすりすり_でんきタイプにはまひが付与されない`
  - `test_ほっぺすりすり_必ず相手をまひ状態にする`
- `python scripts/sort_tests.py tests/moves_attack/test_move_ha.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新。
- `python -m pytest tests/ -v` で全件パスを確認（3585 passed, 1 skipped）。
