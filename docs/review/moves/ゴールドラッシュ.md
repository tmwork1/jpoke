# ゴールドラッシュ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ゴールドラッシュ.md`）。
- 一次情報（`docs/wiki/moves/ゴールドラッシュ.html`）をもとに、タイプ・分類・威力・命中・PP・効果・判定フラグを整理した。
- Championsでの下降修正（命中100→95、命中後のとくこうランク低下1段階→2段階）を明記した。
- `docs/champions/move_list.txt` にゴールドラッシュ自体の行が存在しないため、同系統技
  （オーバーヒート・りゅうせいぐん、いずれもSV基準PP5→Champions PP8）と同じ変換規則を根拠として
  PP8を採用する旨を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/ゴールドラッシュ.md`）。
- 修正が必要な箇所（PP・命中率・`secondary_effect`フラグの誤付与・ランク変化量・リーサル未実装）を洗い出し、
  修正後のコードと根拠を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ka.py` の `ゴールドラッシュ` エントリを修正:
  - `pp=5` → `pp=8`（Championsの8/12/16/20統一ルールに基づく修正。Gen9本家基準のPPが移行漏れのバグだった）
  - `accuracy=100` → `accuracy=95`（Championsでの下降修正を反映）
  - `flags={"secondary_effect"}` を削除（自分自身への確定ランク低下は「追加効果」ではなく、
    ちからずく等の対象にしてはならないため誤り）
  - `lethal_handlers` を追加（`LethalEvent.ON_HIT: LethalHandler(l.ゴールドラッシュ_lower_spa)`）
- `src/jpoke/handlers/move_attack.py` の `ゴールドラッシュ_lower_spa_C` を修正:
  - `stats={"spa": -1}` → `stats={"spa": -2}`（Championsでの下降修正を反映。SVでは1段階だが誤ってそのまま実装されていた）
  - 関数名を `ゴールドラッシュ_sharply_lower_spa_C` に変更（同系統の `りゅうせいぐん_sharply_lower_spa_C` と命名を統一）

## リーサル計算
- 新規実装。`docs/progress/move.md` のリーサル実装列が `-` だったため、
  `src/jpoke/handlers/lethal.py` に `ゴールドラッシュ_lower_spa` を新規実装し
  （オーバーヒート・りゅうせいぐんと同じパターンで、命中後に攻撃側のとくこうランクを2段階下げる）、
  `data/moves/move_ka.py` の `ゴールドラッシュ` エントリに `lethal_handlers` として登録した。

## テスト
- `tests/moves_attack/test_move_ka.py` の既存テストを修正:
  - `test_ゴールドラッシュ_特攻1段階低下が発動する` → `test_ゴールドラッシュ_特攻2段階低下が発動する` に改名し、
    期待値を `attacker.rank["spa"] == -1` → `== -2` に修正（Championsの下降修正に合わせる）。
- `tests/test_lethal.py` に `test_ゴールドラッシュ_とくこうダウン` を新規追加
  （`test_オーバーヒート_とくこうダウン` と同じパターンで、2発目のダメージが減少することを確認）。
- `python -m pytest tests/ -v` を実行し、3757 passed / 0 failed / 1 skipped を確認。
