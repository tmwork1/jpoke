# ねっさのあらし レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ねっさのあらし.md`）。
  一次情報（`docs/wiki/moves/ねっさのあらし.html`）および既存の同型技
  `docs/spec/moves/こがらしあらし.md` を参考に、基本情報・追加効果（20%やけど）・
  天候によるあめ時必中・そらをとぶ状態への非命中・判定フラグを記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/ねっさのあらし.md`）。
  `こがらしあらし`・`かみなりあらし`と同型のパターンに沿って、
  `ON_MODIFY_ACCURACY` ハンドラの追加計画とpriority根拠を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_attack.py` に `ねっさのあらし_accuracy` を新規追加
  （あめ天候時に必中となる `ON_MODIFY_ACCURACY` ハンドラ。`かみなりあらし_accuracy`・
  `こがらしあらし_accuracy` と同様のパターン）。
- `src/jpoke/data/move.py` の `ねっさのあらし` エントリに
  `Event.ON_MODIFY_ACCURACY: h.MoveHandler(ha.ねっさのあらし_accuracy, subject_spec="attacker:self")`
  を追加登録。
  - 修正前は、やけど付与ハンドラ（`ねっさのあらし_apply_burn_to_defender`, chance=0.2）のみ実装済みで、
    「天気が『あめ』のときは必ず命中する」仕様が未実装だった（一次情報・進捗表の効果欄に
    記載があるにもかかわらず抜けていたバグ）。
  - タイプ（じめん）・分類（特殊）・威力（100）・命中率（80）・PP（10）・
    フラグ（`wind`, `secondary_effect`）は一次情報と一致しており修正不要だった。
- `そらをとぶ` へのHIDDEN_MOVE_ALLOWED_MOVES登録は元々未登録（`こがらしあらし`と同様、
  あめ必中でも命中しない仕様）で正しいため変更なし。
- `python scripts/sort_handlers.py src/jpoke/handlers/move_attack.py` と
  `python scripts/sort_data/sort_moves.py` を実行済み。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行は「リーサル実装」「リーサルテスト」ともに `n/a`）。

## テスト
- `tests/moves_attack/test_move_na.py` に `test_ねっさのあらし_あめ中は必中になる` を追加
  （`こがらしあらし`・`かみなりあらし`の同種テストと同じパターンで、あめ天候中に
  命中率80では外れるはずの乱数（0.9）でも必中になることを確認）。
- 既存の `test_ねっさのあらし_やけどが発動する` は内容が正しいため変更なし。
- `python scripts/sort_tests.py tests/moves_attack/test_move_na.py` と
  `python scripts/generate_test_list.py` を実行済み。
- `python -m pytest tests/ -v` で全件パス（3533 passed, 1 skipped）を確認。
