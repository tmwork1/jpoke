# おかたづけ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/おかたづけ.md`）
  - 一次情報（`docs/wiki/moves/おかたづけ.html`）の基本データ・効果・技の仕様（解除順序、ランク最大時の挙動、姿を隠している状態やすりぬけでも解除可能、ゆびをふるで選ばれない）を反映
  - PPは `docs/champions/move_list.txt` の値（12）を採用し、Gen9本家値（10）は移行漏れとして併記

## 実装計画書
- 修正した（`docs/plan/moves/おかたづけ.md` は既存だったが、以下を更新）
  - PP値を10→12に修正（champions値との不整合を解消）
  - 「現状の実装状況」を「未実装」から「実装済み」に更新（既に実装されていたため）
  - 実装されなかった `LogCode.MOVE_CUSTOM`（「かたづけ おわり!」メッセージ）や `cleaned` フラグの記述を削除し、
    実際の実装内容に合わせて更新
  - ランク変化のstatsキーを `{"A": 1, "S": 1}` から実装と一致する `{"atk": 1, "spe": 1}` に修正
  - 技全体の成否判定が `modify_attacker_stats` の戻り値のみで決まる点（きりばらいと同一の設計判断）を
    エッジケースとして明記

## 実装（handlers/ / data/）
- 修正した
  - `src/jpoke/data/moves/move_a.py`: `おかたづけ` の `pp` を `10` → `12` に修正
    （`docs/champions/move_list.txt` 準拠、Gen9本家値からの移行漏れバグ）
  - `src/jpoke/handlers/move_status.py` の `おかたづけ_cleanup` はロジック自体は仕様通り
    （両陣営のみがわり除去・両陣営トラップ除去・こうげき/すばやさ+1）で変更なし

## リーサル計算
- 対象外（変化技のためリーサル計算なし。`docs/progress/move.md` の該当行は `n/a`）

## テスト
- 追加した（`tests/moves_status/test_move_a.py`）
  - `test_おかたづけ_自分のみがわりも除去する`: 使用者自身のみがわりも除去されることを確認
    （既存テストは相手のみがわり除去のみをカバーしていた）
  - `test_おかたづけ_ランクが最大でもみがわりを除去する`: こうげき・すばやさが+6でも、
    みがわりの除去効果自体は発動することを確認（wiki仕様のエッジケース）
  - `test_おかたづけ_両陣営のトラップを除去する`（parametrize: まきびし/どくびし/ステルスロック/ねばねばネット）:
    自陣・相手陣営双方のトラップが除去されることを確認（既存テストは相手陣営のまきびしのみをカバーしていた）
