# シャドークロー レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/シャドークロー.md`）。
- 一次情報（Wiki）から、基本ステータス（ゴースト/物理/威力70/命中100/PP16）、
  急所ランク+1、切る技（きれあじ対象）である点を整理して記載。
- Wikiの「技の仕様」節に「本家SVでは切る技に分類されておらず、きれあじの対象外だが、
  Championsでは切る技の1つとして扱われる」という世代差が明記されている点を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/シャドークロー.md`）。
- 既存実装の不備（PP・flags未対応）を整理し、`critical_rank=1` は既に正しく実装済みで
  追加ハンドラ不要であることを明記した上で実装方針を記載。

## 実装（handlers/ / data/）
- `data/moves/move_sa.py` の `"シャドークロー"` エントリを修正:
  - `pp=15` → `pp=16`（`src/jpoke/data/ps-champ-ja/moves.json` に
    `シャドークロー ゴースト 物理 70 100 16 接○ 守○` と明記されているにもかかわらず、
    本家基準の値のまま残っていた移行漏れのバグ）
  - `flags={"contact"}` → `flags={"contact", "slash"}`
    （Championsでは切る技として扱われ、きれあじの威力補正判定に必要。一次情報に明記）
- `docs/spec/moves/カテゴリ.md` と `docs/spec/moves/_move_labels.md` の「切り / slash」一覧に
  シャドークローが欠落していたため追加した。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）。

## テスト
- `tests/moves_attack/test_move_sa.py` に5件追加:
  - `test_シャドークロー_相手にダメージを与える`
  - `test_シャドークロー_急所ランクが1`
  - `test_シャドークロー_急所ランクが1_乱数大で急所なし`
  - `test_シャドークロー_きれあじで威力1_5倍`（slashフラグとの連動確認）
  - `test_シャドークロー_PPは16`
  - 防御側はカビゴン（ノーマルタイプ、ゴースト技が無効）だと0ダメージ・急所判定なしになるため、
    ピカチュウ（でんきタイプ、等倍）に変更して実装した。
- `python -m pytest tests/ -v` 実行結果: 3820 passed, 1 skipped（既存の無関係なskip）。
  ログは `.loop/test_logs/シャドークロー.log` に保存。
