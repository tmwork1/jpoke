# みだれづき レビュー結果

## 仕様書
- 対象外（新規作成しなかった）
- みだれづきは連続攻撃技の一種であり、`docs/spec/moves/_multi_hit.md` の「9.3 2-5回攻撃技」テーブルに
  タイプ=ノーマル、分類=物理、威力=15、命中=85、PP=20、直接攻撃=○、追加効果=なし として既に正しく記載されていることを一次情報（ポケモンWiki）と照合し確認した。
  個別の仕様書は他の同種の無効果連続技（とげキャノン、たまなげ、ミサイルばりなど）にも存在しないため、
  みだれづきについても新規作成不要と判断した。誤り・欠落なし。

## 実装計画書
- 対象外（新規作成しなかった）
- 連続攻撃の実処理は `core/move_executor.py` の共通ロジック（`_resolve_hit_count` 等）に集約されており、
  みだれづき固有のハンドラは不要（追加効果なし）。個別の計画書を持つ同種技も存在しないため、新規作成不要と判断した。

## 実装（handlers/ / data/）
- 変更なし
- `src/jpoke/data/moves/move_ma.py` の `MoveData("みだれづき")` を確認。
  type="ノーマル", category="physical", pp=20, power=15, accuracy=85, flags={"contact"},
  multi_hit={"min": 2, "max": 5, "check_hit_each_time": False, "power_sequence": ()}, handlers={} であり、
  一次情報（威力15・命中85・PP20・直接攻撃○・連続攻撃技2-5回・命中判定は初回のみ・追加効果なし）と完全に一致。
  兄弟技のみずしゅりけん・むしくいなどと同じ構造で実装されており、誤り・欠落なし。

## リーサル計算
- 対象外
- `docs/progress/move.md` のみだれづき行のリーサル実装列は n/a のため対象外。

## テスト
- 追加した
- `tests/moves_attack/test_move_ma.py` に以下を新規追加:
  - `test_みだれづき_相手にダメージを与える`: 追加効果のない通常攻撃技として相手にダメージを与えることを確認。
  - `test_みだれづき_複数ヒットする`: `battle.move_executor._resolve_hit_count()` の結果が2〜5回の範囲であることを確認
    （兄弟技`test_みずしゅりけん_複数ヒットする`と同じ検証パターン）。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ma.py` でソート、
  `python scripts/generate_test_list.py` でテスト一覧を更新、
  `python -m pytest tests/ -v` で全3610件（+1 skipped）が成功することを確認。
