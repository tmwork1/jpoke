# もえあがるいかり レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/もえあがるいかり.md`）。
  `docs/wiki/moves/もえあがるいかり.html` を一次情報として、タイプ（あく）・分類（特殊）・
  威力90・命中100・PP10・優先度0・非接触・追加効果（20%ひるみ）・判定フラグ（まもるで防がれる、
  おうじゃのしるし対象外、マジックコート反射不可、ちからずく対象）を記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/もえあがるいかり.md`）。
  既存実装（`data/moves/move_ma.py` / `handlers/move_attack.py`）が仕様と完全に一致していることを
  確認し、あくのはどうと同一の実装パターンであることを根拠として明記した。

## 実装（handlers/ / data/）
- 変更なし。`data/moves/move_ma.py` の `もえあがるいかり` エントリ（type="あく", category="special",
  pp=10, power=90, accuracy=100, flags={"secondary_effect"}）と、`handlers/move_attack.py` の
  `もえあがるいかり_apply_flinch`（`apply_volatile_to_defender` で20%ひるみ付与）は仕様書と一致して
  いた。`handlers/item.py` の `_INNATE_FLINCH_MOVES` にも登録済みで、おうじゃのしるし等との重複判定
  も正しく処理されていることを確認した。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`。ひるみは状態変化のみでダメージ量に
  影響しないため）。

## テスト
- 誤って `tests/moves_attack/test_move_yarawa.py`（や・ら・わ行用ファイル）に置かれていた
  `test_もえあがるいかり_ひるみが発動する` を、五十音順の配置ルールに従い
  `tests/moves_attack/test_move_ma.py`（ま行用ファイル）へ移動した。テスト内容自体（あくのはどうと
  同じ `secondary_chance=1.0` パターンでひるみ付与を確認）は妥当だったため変更していない。
- `scripts/sort_tests.py` で両ファイルを整列、`scripts/generate_test_list.py` でテスト一覧を更新、
  `python -m pytest tests/ -v` で全3620件（+skip 1件）が成功することを確認した。
