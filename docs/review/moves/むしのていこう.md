# むしのていこう レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/むしのていこう.md`）。
  一次情報（`docs/wiki/moves/むしのていこう.html`）に基づき、
  タイプ・分類・威力（第六世代以降50）・命中・PP・対象・優先度・接触判定・
  追加効果（100%でとくこうランク-1）・判定フラグを記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/むしのていこう.md`）。
  `docs/spec/moves/_move_secondary_1.md` の既存記載（確率1でC=-1）とも整合していることを確認し、
  ハンドラ構成表・priority根拠・実装済みコード・注意点（ちからずくとの相互作用）・
  リーサル対象外の理由を記載した。

## 実装（handlers/ / data/）
- `handlers/move_attack.py` の `むしのていこう_lower_spa_C` は既存実装のまま修正不要
  （`modify_defender_stats` で `spa: -1` を確定発動、`ムーンフォース_lower_spa_C` 等と同型）。
- `data/moves/move_ma.py` の `MoveData` に **`flags={"secondary_effect"}` が欠落していた不具合を修正**。
  このフラグがないと、ちからずく特性による威力1.3倍化・追加効果無効化（確率0化）が機能しない
  （`ひやみず`・`とびかかる`・`はいよるいちげき` 等のレビューで見つかった同種の欠落と同じパターン）。
  `python scripts/sort_data/sort_moves.py` を実行済み（並び替えによる変更なし）。
  `python scripts/sort_handlers.py src/jpoke/handlers/move_attack.py` を実行済み（並び替えによる変更なし）。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が n/a）。
  相手のとくこうランクを下げる効果は次ターン以降の攻撃力にのみ影響し、
  本技自体のダメージによるHP分布計算には影響しないため。

## テスト
- `tests/moves_attack/test_move_ma.py` に以下を追加:
  - `test_むしのていこう_secondary_effectフラグを持つ`: `MOVES["むしのていこう"].flags` に
    `secondary_effect` が含まれることを確認（今回の修正内容の回帰防止）。
  - `test_むしのていこう_とくこう1段階低下が発動する`: 100%発動でdefenderの `rank["spa"] == -1` を確認。
  - `python scripts/sort_tests.py tests/moves_attack/test_move_ma.py` を実行済み（並び替えによる変更なし）。
  - `python scripts/generate_test_list.py` を実行し `docs/tests/moves_attack.md` を更新。
  - `python -m pytest tests/ -v` を実行し、3610件成功・1件スキップを確認
    （結果ログ: `.loop/test_logs/むしのていこう.log`）。
    なお、初回の全体実行時に `tests/items/test_item_ta.py` の
    `test_だっしゅつパック_わるいてぐせに奪われた場合は発動しない` /
    `test_だっしゅつボタン_ちからずくの効果が発動した技を受けたときは発動しない`
    が失敗したが、`Battle` のデフォルトシードが `int(time.time())`（秒単位の現在時刻）であることに起因する
    既存のタイミング依存フレーキーテストであり、本レビューの変更内容とは無関係であることを確認済み
    （変更を `git stash` して元のコードで全体実行しても3608件全て成功、その後の複数回の再実行では
    本レビューの変更込みでも安定して全件成功した）。
