# はたく レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/はたく.md`）。docs/wiki/moves/はたく.html を基に、タイプ/分類/威力/命中/PP/対象/優先度/接触/判定フラグを記載。

## 実装計画書
- 新規作成（`docs/plan/moves/はたく.md`）。追加効果なしのため専用ハンドラ不要である旨と、PP の修正内容を記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `"はたく"` エントリで `pp=20` となっていたバグを修正し、一次情報（PP35）に合わせて `pp=35` に修正した。
- それ以外（type/category/power/accuracy/flags/handlers）は仕様と一致しており変更なし。
- `handlers/move_attack.py` には専用ハンドラなし（追加効果がないため不要、変更なし）。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a）。追加効果のない通常攻撃技のため、`handlers/lethal.py` に専用ハンドラは不要。

## テスト
- `tests/moves_attack/test_move_ha.py` の `test_はたく_相手にダメージを与える` は既存のまま妥当（ダメージが与えられることを確認）であり、修正・追加なし。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ha.py` 実行済み（並び替え差分なし）。
- `python scripts/generate_test_list.py` 実行済み。
- `python -m pytest tests/ -v` 実行し、3563 passed, 1 skipped で全件成功（ログ: `.loop/test_logs/はたく.log`）。
