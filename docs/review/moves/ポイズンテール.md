# ポイズンテール レビュー結果

## 仕様書

- `docs/spec/moves/ポイズンテール.md` が存在しなかったため、`docs/wiki/moves/ポイズンテール.html` を参照して新規作成した。
  - タイプ: どく / 分類: 物理 / 威力: 50 / 命中: 100 / PP: 25 / 対象: 相手1体 / 優先度: 0 / 接触あり
  - 追加効果: 急所ランク+1（急所に当たりやすい）。10%の確率で相手を『どく』状態にする。
  - `src/jpoke/data/ps-champ-ja/moves.json` に本技のエントリが存在しない点を明記した
    （からてチョップ・ずつき等と同様、掲載なし。`ずつき` レビューの先例に倣い本家Gen9のPP25をそのまま採用）

## 実装計画書

- `docs/plan/moves/ポイズンテール.md` が存在しなかったため、既存実装（`クロスポイズン` の計画書を参考形式）に
  倣って新規作成した。既存実装は仕様と完全に一致しており、修正は不要と結論づけた。

## 実装（handlers/ / data/）

- 変更なし。
  - `src/jpoke/data/moves/move_ha.py` の `"ポイズンテール"` エントリ: `type="どく", category="physical",
    pp=25, power=50, accuracy=100, critical_rank=1, flags={"contact", "secondary_effect"}` で仕様と一致。
  - `src/jpoke/handlers/move_attack.py` の `ポイズンテール_apply_poison_to_defender`:
    `apply_ailment_to_defender(battle, ctx, value, ailment="どく", chance=0.1)` で
    10%どく付与の仕様と一致。`resolve_secondary_chance` 経由でちからずくにも対応済み。
    `critical_rank=1` は `core/move_executor.py` の既存急所判定ロジックが参照するため個別実装不要。

## リーサル計算

- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`。どく状態異常の付与のみでHP分布に影響しないため）。

## テスト

- `tests/moves_attack/test_move_ha.py::test_ポイズンテール_どくが発動する` が既に存在し、
  10%発動の仕様と実装内容に一致していることを確認した（修正不要）。
- 急所ランク+1の効果を確認するテストが存在しなかったため、クロスポイズンの既存パターンに倣い
  `test_ポイズンテール_急所ランクが1`（乱数0で急所発生）・
  `test_ポイズンテール_急所ランクが1_乱数大で急所なし`（乱数0.5で急所なし）を追加した。
- `docs/progress/move.md` の `ポイズンテール` 行が未整備（仕様書・計画書が `-`、レビューが `-`、
  分類以外の列が `?` プレースホルダー）だったため、正しい値
  （仕様書=x・計画書=x・レビュー=✓・タイプ=どく・威力=50・命中率=100・PP=25・対象=defender・効果欄）に修正した。
