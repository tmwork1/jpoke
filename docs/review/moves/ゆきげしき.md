# ゆきげしき レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ゆきげしき.md`）。`docs/wiki/moves/ゆきげしき.html` の一次情報と
  `docs/spec/fields/ゆき.md`（ゆき天候の一般仕様）、`あまごい.md`/`にほんばれ.md` の記述形式に
  倣って作成した。PP は一次情報の10ではなく `docs/champions/move_list.txt` 準拠の8を採用。

## 実装計画書
- `docs/plan/moves/ゆきげしき.md` は既存だったが、以下を修正した。
  - PP を誤って一次情報基準の10と記載していたため、champions基準の8に修正
  - データ登録先のパスを `src/jpoke/data/move.py`（旧構成）から実際の
    `src/jpoke/data/moves/move_ya.py` に修正
  - つめたいいわとの相互作用（持続ターン延長）に関する注意点を追記

## 実装（handlers/ / data/）
- `handlers/move_status.py` の `ゆきげしき_activate_weather` は仕様通り実装済みで変更なし。
- `data/moves/move_ya.py` の `"ゆきげしき"` エントリで `pp=10` となっていたバグを修正し、
  `docs/champions/move_list.txt` 準拠の `pp=8` に修正した（Gen9本家基準からの移行漏れ）。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`。天候変化技のためダメージ計算対象外）。

## テスト
- 既存の `test_ゆきげしき_おおひでり中は失敗する`・`test_ゆきげしき_天気がゆきになる` は
  実装・仕様と整合しており変更なし。こおりタイプ防御1.5倍・つめたいいわ延長は
  `tests/test_field.py`・`tests/items/test_item_misc.py` の汎用天候テストで既に検証済みのため
  追加不要と判断した。
- `python -m pytest tests/ -v` で 4450 passed, 1 skipped を確認（ログ: `.loop/test_logs/ゆきげしき.log`）。

## 進捗表
- `docs/progress/move.md` の ゆきげしき 行を更新: 仕様書 `-`→`x`、計画書 `-`→`x`、
  レビュー `-`→`✓`、対象 `defender`→`field`（誤記修正）。
