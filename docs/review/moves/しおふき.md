# しおふき レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/しおふき.md`）。
  - 一次情報（`docs/wiki/moves/しおふき.html`）をもとに、タイプ・分類・威力（可変1〜150）・命中率100・PP5・
    対象・優先度0・非接触の基本情報と、威力計算式（`floor(150 × 残りHP ÷ 最大HP)`、最小値1）を記載した。
  - ドラゴンエナジーと同一の威力計算式であることを明記した。
  - リーサル計算が対象外（n/a）である理由を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/しおふき.md`）。
  - 既存実装（`しおふき_calc_power`、`data/move.py` の MoveData）が仕様書と完全に一致していることを確認し、
    その根拠（4096基準の固定小数点補正、`round_half_down` による丸め、最小値1の保証）を記録した。
  - `Event.ON_CALC_POWER_MODIFIER` の priority について、既存の同種ハンドラの慣例（デフォルト値）に
    合わせる根拠を明記した。
  - シングルバトル専用プロジェクトのため、範囲技（相手全員）であっても `target` を単体技のデフォルトのまま
    扱ってよいことを明記した。

## 実装（handlers/ / data/）
- 変更なし。
  - `src/jpoke/handlers/move_attack.py` の `しおふき_calc_power`、`src/jpoke/data/move.py` の
    `MoveData`（type=みず/category=special/pp=5/power=150/accuracy=100）を一次情報・仕様書と照合し、
    誤りは見つからなかった。
  - ドラゴンエナジー（`data/move.py`）が同じハンドラを再利用している点も、威力計算式が同一のため妥当と確認した。

## リーサル計算
- 対象外（`docs/progress/move.md` のしおふき行は「リーサル実装: n/a」「リーサルテスト: n/a」）。
  威力が使用者自身の残りHPにのみ依存し、相手のHP分布（`StateDist`）を直接左右しないため、
  専用のリーサルハンドラは不要と判断した。

## テスト
- 変更なし。
  - `tests/moves_attack/test_move_sa.py` の既存テスト2件
    （`test_しおふき_HP半分のとき威力約75`、`test_しおふき_HP満タンのとき威力150`）を実装と照合し、
    威力計算式（modifier=floor(HP比率×4096)、final_power=round_half_down(150×modifier/4096)）と
    整合していることを確認した。追加・修正は不要と判断した。

## 進捗表の修正
- `docs/progress/move.md` のしおふき行について、以下の誤りを修正した。
  - PP列: `8` → `5`（一次情報・実装ともに5）
  - 仕様書列: `-` → `x`（新規作成のため）
  - 計画書列: `-` → `x`（新規作成のため）
  - レビュー列: `-` → `✓`
  - 効果列: 「自分のHPが少ないほど技の威力が下がる。」→
    「自分の残りHPが多いほど威力が大きくなる。威力=floor(150×残りHP÷最大HP)（最小1）。」
    （より正確な向き・計算式の記述に修正）
