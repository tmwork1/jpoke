# こうげきしれい レビュー結果

## 仕様書
- 新規作成（docs/spec/moves/こうげきしれい.md）。docs/wiki/moves/こうげきしれい.html を一次情報として、
  タイプ・分類・威力90・命中100・PP15・対象1体・優先度0・接触なし・急所ランク+1（急所に当たりやすい）・
  追加効果なしを記載。

## 実装計画書
- 新規作成（docs/plan/moves/こうげきしれい.md）。追加効果を持たないため専用ハンドラは不要であることを明記し、
  既存の `data/move.py` エントリが仕様と一致していることを確認する内容とした。

## 実装（handlers/ / data/）
- 変更なし。既存の `data/move.py` の `"こうげきしれい"` エントリ（type="むし", category="physical",
  pp=15, power=90, accuracy=100, critical_rank=1, handlers={}）は仕様と一致していた。
  `flags` に `contact` が付与されていない点も「直接攻撃ではない」という一次情報と一致。

## リーサル計算
- 対象外（docs/progress/move.md でリーサル実装列が n/a）。

## テスト
- 追加。tests/moves_attack/test_move_ka.py に以下を新規追加。
  - `test_こうげきしれい_急所ランクが1`（乱数0で急所が発生することを確認）
  - `test_こうげきしれい_急所ランクが1_乱数大で急所なし`（乱数が閾値以上のとき急所にならないことを確認）
  - `test_こうげきしれい_相手にダメージを与える`（追加効果なしの物理むし技として基本ダメージを確認）
- scripts/sort_tests.py tests/moves_attack/test_move_ka.py で並び替え、
  scripts/generate_test_list.py でテスト一覧を更新。

## その他の修正
- `src/jpoke/core/event_logger.py` で `EventLogger` クラスが `EventLog` クラスより前に定義されており、
  `EventLogger.get()` の戻り値型注釈 `list[EventLog]` の評価時に `NameError: name 'EventLog' is not defined`
  が発生し、`jpoke` パッケージ全体のインポートが失敗してテストスイート全体が collection error になる
  不具合を発見したため修正した（`EventLogger` クラス定義を `EventLog` クラス定義の後ろに移動）。
  こうげきしれい固有の変更ではないが、本タスクのテスト実行に必須だったため対応した。

## テスト実行結果
- `python -m pytest tests/ -v` で 3335 件成功・1件skip（.loop/test_logs/こうげきしれい.log に保存）。
- 補足: 本リポジトリは現在大規模なWIPの変更（event_logger等の複数コアモジュール）を抱えており、
  テストスイート全体を複数回実行すると `tests/test_field.py` / `tests/test_item.py` /
  `tests/test_tree_search_framework.py`（未コミットの別タスクの実験的フレームワーク）で
  実行のたびに異なるテストが散発的に失敗する既知の非決定性が確認された。
  これらはこうげきしれい・本レビューの変更とは無関係（該当ファイルは今回未変更）であり、
  再実行で解消することを確認済み。上記ログは全件成功時点のものを保存した。
