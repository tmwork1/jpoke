# みちづれ レビュー結果

## 仕様書
- 変更なし（`docs/spec/moves/みちづれ.md` は一次情報（ポケモンWiki `docs/wiki/moves/みちづれ.html`）と照合済みで、記述に誤り・欠落なし）
- `docs/spec/volatiles/みちづれ.md`（付与される揮発性状態側の仕様書）も既存内容を確認し、誤りなし

## 実装計画書
- 新規作成: `docs/plan/moves/みちづれ.md` が存在しなかったため、仕様書をもとに新規作成
  - 技側のハンドラ構成（`ON_TRY_MOVE_2` の連続使用失敗チェック、`ON_STATUS_HIT` の状態付与）と
    priority根拠、実装済みコードを記載
  - `docs/spec/moves/みちづれ.md` の「第七世代以降」節で説明されている各エッジケース
    （状態異常での行動不能、きあいパンチの集中力切れ、道具使用、逃走失敗など）が
    `executed_move` ベースの判定でどのように自然に満たされるかを明記
  - おどりこによる強制ダンス技実行時の未対応（既知の限界）も明記
- `docs/plan/volatiles/みちづれ.md`: 「実装状況」節が古い記述のまま（`みちづれ_remove` 未実装・
  `ON_TRY_ACTION` 未登録と記載）だったが、実際にはどちらも実装済みだったため修正

## 実装（handlers/ / data/）
- **欠落を発見・修正**: 原作Wiki「技の仕様」節の「第七世代以降」に明記されている
  「みちづれが成功した直後にもう一度みちづれを使うと、必ず失敗する」という連続使用制限が
  未実装だったため、新規に実装した
  - `handlers/move_status.py` に `みちづれ_連続使用失敗チェック` を追加
    （`まもる系_連続使用失敗チェック` / `れんぞくぎり_reset_on_turn_end` と同じ
    `mon.executed_move.name == "みちづれ"` 判定パターン）
  - `data/moves/move_ma.py` の `"みちづれ"` エントリに `ON_TRY_MOVE_2` ハンドラとして登録
  - 失敗時は `executed_move` を `None` にリセットするため、連続使用失敗の直後にもう一度使えば
    成功する（原作仕様通り）
- `みちづれ_apply`（`ON_STATUS_HIT`）・`data/volatile.py` の `"みちづれ"` エントリ
  （`ON_MOVE_KO` / `ON_TRY_ACTION`）は既存実装のまま変更なし（仕様と一致していることを確認）
- PP=8 は `docs/champions/move_list.txt` の値と一致していることを確認（champions基準、変更なし）
- `flags={"non_copycat"}` のみで `non_negoto` を付与していない点も、原作Wiki「ねごとでみちづれを
  出すことはできる」の記述と一致していることを確認（変更なし）

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行で「リーサル実装」「リーサルテスト」は `n/a`。
  ダメージを与えない変化技のため）

## テスト
- `tests/moves_status/test_move_ma.py` に以下を追加（みずびたし系テストとミラータイプ系テストの間、
  五十音順の位置に配置）
  - `test_みちづれ_使用すると自分にみちづれ状態を付与する`
  - `test_みちづれ_2ターン連続で使用すると2ターン目は失敗する`
  - `test_みちづれ_2ターン目失敗後3ターン目は再使用できる`
  - `test_みちづれ_別の技をはさむと再使用できる`
- `tests/volatiles/test_volatile_hamayarawa.py` の既存のみちづれ揮発性状態テスト
  （発動条件・不発・自身行動での解除）は実装と照合し、修正不要と確認
- `python -m pytest tests/ -v` で全4433件パス（1件skip）を確認し、
  `.loop/test_logs/みちづれ.log` に保存
