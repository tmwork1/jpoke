# ぶんまわす レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ぶんまわす.md`）。一次情報（ポケモンWiki）に基づき、
  タイプ・分類・威力・命中・PP・対象・優先度・接触・判定フラグを整理した。
  対象は原作では「自分以外の全員」だが、本プロジェクトはシングルバトルのみを対象とするため
  実質的に相手1体である旨を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/ぶんまわす.md`）。追加効果を持たない通常攻撃技のため
  専用ハンドラは不要であることを記載し、`data/move.py` の既存エントリが仕様と一致することを確認した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ha.py` の `"ぶんまわす"` エントリ
  （type="あく", category="physical", pp=20, power=60, accuracy=100,
  flags={"contact"}, handlers={}）は仕様書と一致しており、修正不要だった。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- 変更なし。既存の `test_ぶんまわす_相手にダメージを与える`
  （`tests/moves_attack/test_move_ha.py`）は仕様と一致しており修正不要だった。
- `python -m pytest tests/ -v` を実行し、3577 passed, 1 skipped で全件パスを確認した。
