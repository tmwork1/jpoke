# ふういん レビュー結果

## 仕様書
- 新規作成した（`docs/spec/moves/ふういん.md`）。
  一次情報（`docs/wiki/moves/ふういん.html`, `docs/wiki/volatiles/ふういん_(状態変化).html`）を基に、
  基本情報（タイプ・分類・PP・範囲・優先度）、効果、失敗条件、まもる/マジックコートの判定
  （対象が自分自身のため無効）を整理した。
- PP は `docs/champions/move_list.txt` の値（12）を正として記載した（Gen9本家は10）。
- 揮発性状態側の仕様（`docs/spec/volatiles/ふういん.md`）はすでに存在し、内容も一次情報と一致していたため変更なし。

## 実装計画書
- 新規作成した（`docs/plan/moves/ふういん.md`）。
  ハンドラ構成（`ON_STATUS_HIT` に `ふういん_apply`）、priority根拠、
  今回のレビューで修正した不備（`target="self"` 未設定・PP誤り）を明記した。
- 揮発性状態側の計画書（`docs/plan/volatiles/ふういん.md`）はすでに存在し、内容も妥当だったため変更なし。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `"ふういん"` エントリを修正した。
  - `target="self"` が未設定だった（デフォルトの `"foe"` のままだった）ため追加した。
    これにより `Move.is_blocked_by_protect` / `Move.is_reflectable` が誤って `True` を
    返す状態になっていた（まもる・マジックコートで防げてしまうバグ）。target="self" 追加により
    どちらも自動的に False（防がれない・跳ね返されない）になる。
  - `pp=10`（Gen9本家値）になっていたが、Champions基準の `pp=12`
    （`docs/champions/move_list.txt` の値）に修正した。
- `handlers/move_status.py` の `ふういん_apply`、`data/volatile.py` / `handlers/volatile.py` の
  `ふういん_try_action`（相手の共通技をブロックするロジック）は仕様と一致しており修正不要。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列は `n/a`。ダメージを与えない変化技のため）。

## テスト
- `tests/moves_status/test_move_ha.py` に、技「ふういん」自体の使用に関するテストを追加した
  （揮発性状態側のブロック挙動は `tests/volatiles/test_volatile_hamayarawa.py` に既存のテストがあり、
  変更なし）。
  - `test_ふういん_すでにふういん状態なら失敗する`
  - `test_ふういん_使用者がふういん状態になる`
  - `test_ふういん_まもるで防がれない`（target="self"修正の回帰テスト）
  - `test_ふういん_マジックコートで跳ね返されない`（target="self"修正の回帰テスト）
- `python -m pytest tests/ -v` を実行し、4353 passed, 1 skipped（既存のskipで無関係）で
  全件成功を確認した（ログ: `.loop/test_logs/ふういん.log`）。
