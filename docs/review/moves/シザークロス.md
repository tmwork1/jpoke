# シザークロス レビュー結果

## 仕様書
- 新規作成（docs/spec/moves/シザークロス.md）。docs/wiki/moves/シザークロス.html を一次情報として、
  基本情報（タイプ/分類/威力/命中/PP/対象/優先度/接触）、判定フラグ（急所・命中判定・追加効果・
  まもる・おうじゃのしるし・マジックコート・よこどり・オウムがえし）、きれあじとの相互作用を記載。
  本編シリーズでは急所ランク補正が無い（LEGENDS アルセウス限定の効果）ことをwiki備考欄で確認し明記。

## 実装計画書
- 新規作成（docs/plan/moves/シザークロス.md）。仕様書をもとに、追加効果なしのため専用ハンドラ不要である旨、
  および発見したPPバグの修正内容（後述）を記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_sa.py` の `MOVES_SA["シザークロス"]` の `pp` を `15`（Gen9本家基準）から
  `16`（champions基準、docs/champions/move_list.txt 準拠）に修正した。全技PPが8/12/16/20の4段階に
  統一されているchampionsルールへの移行漏れバグ。
- その他（type/category/power/accuracy/flags/handlers）は仕様と一致しており修正不要だった。
  `critical_rank` 未設定（デフォルト0）も、本編シリーズで急所ランク補正が無い仕様と一致することを確認した。

## リーサル計算
- 対象外（docs/progress/move.md の「リーサル実装」列は `n/a`。追加効果なしの確定ダメージ技のため）。

## テスト
- `tests/moves_attack/test_move_sa.py` に `test_シザークロス_相手にダメージを与える` を新規追加。
  追加効果・急所ランク補正のいずれも持たない技のため、ダメージが発生することのみを確認する
  最小限のテストとした（きれあじとの相互作用は既存の `test_ability_ka.py` の
  `test_きれあじ_きる技は威力補正1_5倍` 等で汎用的に網羅済みのため個別追加は不要と判断）。
- `python -m pytest tests/ -v` で全3816件パス（1件は既存のskip、シザークロス起因ではない）。
  ログは `.loop/test_logs/シザークロス.log` に保存。
