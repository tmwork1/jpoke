# かいでんぱ レビュー結果

## 仕様書
- PP を 15 → 16 に修正（`src/jpoke/data/ps-champ-ja/moves.json` の値に合わせた。ポケモンチャンピオンズでは全技PPが8/12/16/20の4段階に統一されているため、Gen9本家基準の15はバグ）。

## 実装計画書
- PP のサンプルコード（`data/move.py` 抜粋）を pp=15 → pp=16 に修正。
- ハンドラのサンプルコードの型ヒントを `ctx: EventContext` → `ctx: AttackContext` に修正（実装・他の変化技ハンドラ（`いやなおと` 等）との整合を取った。ON_STATUS_HIT は MoveHandler 経由で AttackContext を受け取るため）。
- `modify_defender_stats({"C": -2})` のスタブコード内のキー名を `"C"` → `"spa"` に修正（`Stat` 型のリテラルは `"spa"`/`"spd"` 等であり `"C"` は存在しない）。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ka.py` の `かいでんぱ` MoveData の `pp=15` を `pp=16` に修正（実装漏れのバグ。`src/jpoke/data/ps-champ-ja/moves.json` 158行目で PP=16 と定義されている）。
- `handlers/move_status.py` の `かいでんぱ_modify_defender_stats` 関数自体はロジック上問題なし（`modify_defender_stats` で `stats={"spa": -2}` を渡すのみ）。
- 一次情報（ポケモンWiki）で言及される でんきエンジン／ちくでん／ひらいしん によるでんき技無効化、クリアボディ等によるランク低下防止、かちき特性によるランク変化反転は、いずれも `handlers/ability.py` 側の汎用ハンドラ（`でんきエンジン_absorb_electric` 等）で既に対応済みのため、かいでんぱ側の追加実装は不要と確認した。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行で リーサル実装 = n/a, リーサルテスト = n/a。かいでんぱはダメージを与えない変化技のため）。

## テスト
- 変更なし。既存の `tests/moves_status/test_move_ka.py::test_かいでんぱ_相手の特攻が2段階下がる` で基本動作（相手の特攻が-2ランクになること）を確認済み。他の能力低下技（`うそなき` 等）と同様、みがわり貫通の有無を確認するテストは不要（かいでんぱは音技ではなくみがわりで通常通り防がれるため）。
