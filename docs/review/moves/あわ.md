# あわ レビュー結果

## 仕様書
- 変更なし。個別の仕様書ファイルは存在しないが、標準的な追加効果（確率発動のすばやさ1段階ダウン）は
  `docs/spec/moves/_move_secondary_1.md` の一覧表（41行目）に既に記載されており、
  一次情報（`docs/wiki/moves/あわ.html`）と照合した結果、内容は正しい
  （みず / 特殊 / 威力20→40（第六世代以降）/ 命中100 / PP30 / 対象1体 / 追加効果10%ですばやさ1段階ダウン / 直接攻撃×）。

## 実装計画書
- 変更なし。個別の計画書は存在しないが、同種の「確率で能力ランクを下げる」技
  （アクアブレイク・いわくだき等）にも個別計画書は作成されておらず、
  共通ヘルパー `modify_defender_stats` を使う定型実装のため計画書省略は既存方針と整合している。

## 実装(handlers/ / data/)
- 変更なし。`src/jpoke/data/move.py` の `あわ` エントリ
  （type=みず, category=special, pp=30, power=40, accuracy=100, flags={"secondary_effect"},
  `Event.ON_DAMAGE_HIT` に `あわ_lower_defender_spd` を登録）は仕様と一致。
  `src/jpoke/handlers/move_attack.py` の `あわ_lower_defender_spd` は
  `modify_defender_stats(battle, ctx, value, stats={"spe": -1}, chance=0.1)` を呼び出しており、
  10%の確率で相手のすばやさを1段階下げる仕様と一致する。既に五十音順の位置に実装済みのため
  `python scripts/sort_handlers.py` の実行は不要と判断（差分なしを確認済み）。

## テスト
- `tests/moves_attack/test_move_a.py` に `あわ` のテストが存在しなかったため、
  `test_あわ_すばやさ低下が発動する`（secondary_chance=1.0 ですばやさランク-1を確認）を新規追加した。
  `python scripts/sort_tests.py tests/moves_attack/test_move_a.py` でソート、
  `python scripts/generate_test_list.py` でテスト一覧を更新した。
- `python -m pytest tests/ -v` で全3219件パス（1件skip）を確認（ログ: `.loop/test_logs/あわ.log`）。

## その他
- `docs/progress/move.md` の該当行で 威力 列が空欄になっていたため `40` に修正し、
  テスト列を `x`、レビュー列を `✓` に更新した。
