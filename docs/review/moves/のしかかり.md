# のしかかり レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/のしかかり.md`）。一次情報（`docs/wiki/moves/のしかかり.html`）と照合し、
  タイプ/分類/威力/命中/PP、まひ追加効果30%、ちいさくなる状態の相手への必中化・威力2倍（第六世代以降）を記載。

## 実装計画書
- 新規作成（`docs/plan/moves/のしかかり.md`）。既存実装（`data/move.py` / `handlers/move_attack.py`）が
  仕様書と一致していることを確認し、ハンドラ構成・priority根拠・minimizeフラグの共通処理との関係を記載。

## 実装（handlers/ / data/）
- 変更なし。`data/move.py` の `のしかかり` エントリ（type=ノーマル, category=physical, pp=15, power=85,
  accuracy=100, flags={"minimize", "contact", "secondary_effect"}, ON_DAMAGE_HIT ハンドラ）は仕様と一致。
- `handlers/move_attack.py` の `のしかかり_apply_paralysis_to_defender`（chance=0.3）も仕様と一致。
- ちいさくなる状態相手への必中化・威力2倍は `handlers/volatile.py` の
  `ちいさくなる_boost_power` / `ちいさくなる_guaranteed_hit` による minimize フラグの共通処理で対応済みのため、
  本技固有の実装は不要（正しく未実装のまま）。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- 変更なし。`tests/moves_attack/test_move_na.py` の `test_のしかかり_まひが発動しない` /
  `test_のしかかり_まひが発動する` でまひ追加効果を検証済み。
- minimize相互作用（必中化・威力2倍）は `tests/volatiles/test_volatile_satana.py` の
  `test_ちいさくなる_minimize技が必中化して威力2倍`（パラメータ化テストに「のしかかり」を含む）で検証済み。

## その他の修正
- `docs/progress/move.md` の のしかかり 行に誤り（PP列が16と誤記載、正しくは15）があったため修正。
  合わせて 仕様書/計画書 列を `x`、レビュー列を `✓` に更新。
