# てんしのキッス レビュー結果

## 仕様書
- 存在しなかったため新規作成した（`docs/spec/moves/てんしのキッス.md`）。
- `docs/wiki/moves/てんしのキッス.html` を一次情報として、タイプ=フェアリー・分類=変化・威力=-・
  命中率=75・PP=12（Champions基準）・範囲=1体選択・優先度=0・直接攻撃=×・
  まもる/マジックコート/オウムがえし=○、よこどり=×、追加効果=×、急所=× を記載。
- PP は原作の10ではなく `docs/champions/move_list.txt` の12（Champions基準）を採用する旨を明記。

## 実装計画書
- `docs/plan/moves/てんしのキッス.md` は存在していたが、実際の実装（`battle.volatile_manager.apply_confusion`
  を呼ぶパターン）と乖離した古い内容（`battle.volatile_manager.apply` を直接呼ぶ独自実装案、PP10）に
  なっていたため、実装済みコードに合わせて修正した。
  - ハンドラ関数名を実装済みの `てんしのキッス_apply` に修正
  - `apply_confusion`（`VolatileManager` 内で2〜5ターンの乱数を決定）を使う旨に修正
  - PP を 12（Champions基準）に修正
  - 挿入ファイルの記述を `data/move.py` → `data/moves/move_ta.py` に修正

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_status.py` の `てんしのキッス_apply` は仕様と一致（変更なし）。
- `src/jpoke/data/moves/move_ta.py` の `MoveData` エントリで `pp=10` になっていたバグを `pp=12`
  （`docs/champions/move_list.txt` 基準）に修正した。
- `target` は明示指定がなく `MoveData` のデフォルト `"foe"` を使用しており、まもる/マジックコートの
  対象判定に問題はないことを確認した（target="self" 欠落バグには該当しない）。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- `tests/moves_status/test_move_ta.py` の既存2件（こんらん付与・重複付与失敗）を実装と照合し、
  問題なしを確認（変更なし）。
- `python -m pytest tests/ -v` を複数回実行し、全4250件パス・1件skipを確認
  （`.loop/test_logs/てんしのキッス.log`）。
  実行中に無関係な項目（`test_だっしゅつパック_*`、`test_だっしゅつボタン_*`、
  `test_ミラーコート_まねっこでコピーできる`）で乱数依存とみられる散発的な失敗が観測されたが、
  てんしのキッスの変更とは無関係かつ再実行で再現しないフレーキーな既存問題のため、本レビューの
  スコープ外として対応していない。
