# ドラゴンアロー レビュー結果

## 仕様書
- 変更なし。
  - `docs/spec/moves/ドラゴンアロー.md` という個別ファイルは存在しないが、本技は追加効果を持たない
    単純な「2回固定ヒット技」であり、`docs/spec/moves/_multi_hit.md`（9.1 節）・
    `docs/spec/moves/連続技.md`（9.1 節）に既に一覧掲載されている。
    同種の追加効果なし連続技（ダブルアタック、ダブルウイング、ツインビーム、つららばり、
    ボーンラッシュ、ミサイルばり、ロックブラスト、ネズミざん等）も同様に個別仕様書を持たず
    `docs/progress/move.md` 上で「仕様書: -」のまま扱われており、これがプロジェクトの既定の
    ドキュメント方針である。個別仕様書の新規作成は行わず、既存の共有仕様書のままとした。
  - 一次情報（`docs/wiki/moves/ドラゴンアロー.html`）を確認し、以下を検証済み:
    タイプ=ドラゴン、分類=物理、威力=50（本家基準。2発合計100）、命中率=100、
    PP=10（第9世代本家基準）、直接攻撃=×、優先度=0、範囲=1体選択、
    急所判定=通常、まもるで防がれる、おうじゃのしるし発動、マジックコート対象外。
  - 「そうでん状態時にでんきタイプへ変化する」効果は本プロジェクトで既に汎用実装済み
    （`src/jpoke/handlers/volatile.py` の `そうでん_move_type`、`ON_MODIFY_MOVE_TYPE` で
    技単位ではなく攻撃側の状態として処理されるため、ドラゴンアロー固有の実装は不要）。
  - タスク指示にあった「2回目のゴーストタイプ判定」について一次情報を確認したが、
    ドラゴンアローにゴーストタイプへ変化する仕様は存在しない（そうでん状態によるでんき
    タイプ化のみ）。誤情報と判断し、対応する実装は行っていない。
  - 本プロジェクトは対象がシングルバトルのみのため、相手2体時の対象振り分け仕様
    （Wiki「攻撃対象」節）は実装対象外。既存実装は常に固定2回ヒットとして扱われており、
    この方針と整合している。

## 実装計画書
- 変更内容: `docs/plan/moves/攻撃技_連続技系.md` のドラゴンアローのコード例の PP を
  `10` → `12` に修正し、チャンピオンズ基準である旨のコメントを追記した
  （下記実装のPPバグと対応させるため）。
  - `docs/plan/moves/ドラゴンアロー.md` という個別計画書は存在しないが、仕様書と同様の理由
    （追加効果なしの単純な連続技）で、共有計画書 `docs/plan/moves/攻撃技_連続技系.md` に
    既に実装方針が記載されている。個別計画書の新規作成は行わなかった。

## 実装（handlers/ / data/）
- 修正した内容:
  - `src/jpoke/data/moves/move_ta.py` の `"ドラゴンアロー"` エントリで `pp=10` になっていた
    バグを `pp=12` に修正した。`src/jpoke/data/ps-champ-ja/moves.json`（586行目）では
    `ドラゴンアロー	ドラゴン	物理	50	100	12` となっており、`docs/progress/move.md` の
    PP列（12）とも整合する。第9世代本家基準の10のまま移行漏れしていたバグ。
  - `multi_hit`（min=2, max=2, check_hit_each_time=False, power_sequence=()）、
    タイプ・威力・命中率・`flags`（`contact` なし）はいずれも仕様通りで変更なし。
  - `handlers/move_attack.py` には追加効果がないため固有ハンドラは存在せず、それが正しい
    （追加効果なしの連続技と同一パターン）。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装/リーサルテスト列がいずれも n/a）。

## テスト
- 追加した内容（`tests/moves_attack/test_move_ta.py`）:
  - `test_ドラゴンアロー_タイプ威力PPが仕様通り`: タイプ・分類・威力・命中率・PP（12）・
    非接触・`multi_hit` 設定値を検証する新規テスト。
  - `test_ドラゴンアロー_2発とも威力が変わらない`: `power_sequence` が空のため、2発目も
    威力50のまま変化しないことを検証する新規テスト。
  - 既存の `test_ドラゴンアロー_2回ヒットする`（必ず2回ヒットする）は変更なし。
  - `python scripts/sort_tests.py tests/moves_attack/test_move_ta.py` を実行したが、
    ファイルは既に五十音順のため実質的な差分は追加分のみだった。
  - `python -m pytest tests/ -v` を実行し、3919 passed, 1 skipped で全件成功を確認
    （ログ: `.loop/test_logs/ドラゴンアロー.log`）。
