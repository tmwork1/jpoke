# ブレイブチャージ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ブレイブチャージ.md`）。
  - `docs/wiki/moves/ブレイブチャージ.html` を一次情報として、基本情報（タイプ/分類/威力/命中/PP/
    対象/優先度/直接攻撃/専用技）と効果（とくこう・とくぼうランク+1、状態異常回復、両効果の独立性、
    技自体は失敗しないこと、ねごと経由でねむりを解除できること）、判定フラグを記載した。
  - PPについて: `docs/champions/move_list.txt` に本技のエントリが存在しないことを確認した。
    フィオネ・マナフィの専用技であり、両者はチャンピオンズのシングル対戦で実戦投入されないため
    championsのPP再編対象から漏れている（対象外）と判断し、第九世代基準のPP15を維持する旨を明記した。

## 実装計画書
- 既存の `docs/plan/moves/ブレイブチャージ.md` は「効果不明」の暫定プレースホルダーだったため、
  仕様書確定後の内容に全面的に書き直した。
  - ハンドラ構成（`ON_STATUS_HIT` priority=100、`attacker:self` 省略）、priority根拠、
    実装済みコードの引用、リーサル対象外の理由、テスト一覧、PP未収録に関する注記を記載。

## 実装（handlers/ / data/）
- 変更なし。`handlers/move_status.py` の `ブレイブチャージ_apply`、
  `data/moves/move_ha.py` の `MOVES_HA["ブレイブチャージ"]` は仕様書と整合していることを確認した。
  - `modify_attacker_stats` の戻り値を使わず常に `value` を返すことで、ランク上限・状態異常なしの
    いずれの場合でも技自体を失敗させない仕様を正しく満たしている。
  - `battle.ailment_manager.remove()` は状態異常がない場合に安全にno-opするため、無条件呼び出しで問題ない。
  - `target="self"`, `accuracy` 省略（必中）, `priority` 省略（0）, `flags` 空（直接攻撃なし）も
    仕様と一致。

## リーサル計算
- 対象外（`n/a`）。自分の能力ランク変化・状態異常回復のみで相手のHPに影響しないため。

## テスト
- 既存3件（`test_ブレイブチャージ_とくこうとくぼうが上がり状態異常が治る`,
  `test_ブレイブチャージ_ランク上限でも技は失敗しない`,
  `test_ブレイブチャージ_状態異常がなくても技は失敗しない`）を確認し、仕様と整合していることを確認した。
- 欠落していた複合エッジケース（ランク上限かつ状態異常なしでも技は失敗しない）を検証する
  `test_ブレイブチャージ_ランク上限かつ状態異常がなくても技は失敗しない` を追加した。
- `python -m pytest tests/ -v` で全4621件（+1 skipped）がパスすることを確認した
  （ログ: `.loop/test_logs/ブレイブチャージ.log`）。
