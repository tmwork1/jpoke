# トリックルーム レビュー結果

## 仕様書
- PP列に Champions での実値（8）を注記（`docs/champions/move_list.txt` 準拠、本家は5）。
  それ以外は一次情報（ポケモンWiki）と一致しており変更なし。

## 実装計画書
- `docs/plan/moves/トリックルーム.md` が存在しなかったため、`docs/plan/moves/じゅうりょく.md`
  と同じ構成（技データ・`ON_STATUS_HIT` の subject_spec/priority 根拠・ハンドラ構成・注意事項）で
  新規作成した。
- 併せて `docs/plan/fields/トリックルーム.md`（場の状態側の計画書）に実装関数名との不一致
  （`トリックルーム_reverse_speed`→実装は`トリックルーム_reverse_spe`、
  `トリックルーム_tick_global_field`→実装は`トリックルーム_tick`）を発見したため修正した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `"トリックルーム"` エントリで `pp=5`（本家Gen4-9の値）に
  なっていたのを `pp=8`（`docs/champions/move_list.txt` 準拠）に修正した。移行漏れのバグ。
- `handlers/move_status.py` の `トリックルーム_activate_global_field`（発動中なら
  `deactivate` でトグル解除、未発動なら `activate` で5ターン発動）、
  `data/field/global_field.py` の `GLOBAL_FIELD["トリックルーム"]`（`ON_CHECK_SPEED_REVERSE` で
  素早さ反転、`ON_TURN_END` priority=140 でカウントダウン）は仕様書・場の状態仕様書と整合してお
  り、他に修正なし。
- `target="field"` が明示されており、まもる／マジックコートで防がれない一次情報の判定表
  （まもる: ×、マジックコート: ×）とも一致していることを確認した。

## リーサル計算
- 対象外（`docs/progress/move.md` の リーサル実装／リーサルテスト列は n/a）。

## テスト
- `tests/moves_status/test_move_ta.py` に技単体のテストが存在しなかったため、
  `じゅうりょく` のテストパターンに倣って以下を追加した:
  - `test_トリックルーム_PPは8`
  - `test_トリックルーム_マジックコートで跳ね返されない`
  - `test_トリックルーム_まもるで防がれない`
  - `test_トリックルーム_場が発動する`
  - `test_トリックルーム_発動中に再使用すると解除される`（トグル解除の確認。既存の場の状態側
    テスト `tests/test_field.py::test_トリックルーム_再使用で解除` と重複しない技視点のテスト）
- `tests/test_field.py`（素早さ逆順・技優先度優先・再使用解除）、`tests/items/test_item_yarawa.py`
  （ルームサービス）、`tests/abilities/test_ability_a.py`（あとだし）の既存テストは実装と齟齬が
  なく変更なし。
- `python -m pytest tests/ -v` を実行し、4545 passed, 1 skipped で全件成功を確認した
  （ログ: `.loop/test_logs/トリックルーム.log`）。
