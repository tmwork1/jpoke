# エナジーボール レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/エナジーボール.md`）。一次情報（`docs/wiki/moves/エナジーボール.html`）と
  `src/jpoke/data/ps-champ-ja/moves.json` を参照し、タイプ・分類・威力・命中・PP・追加効果（10%でとくぼう-1）
  ・判定フラグ・ちからずくとの相互作用を記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/エナジーボール.md`）。既存の類似技（きあいだま）の計画書パターンを踏襲。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `エナジーボール` エントリを修正:
  - `pp` が `10`（Gen9本家基準の値）になっていたバグを `12`（`src/jpoke/data/ps-champ-ja/moves.json` の正しい値）
    に修正。
  - `flags` に `"secondary_effect"` が付与されておらず、ちからずく特性（追加効果技の威力1.3倍化・
    追加効果無効化）が正しく機能しないバグを修正し、`flags={"bullet", "secondary_effect"}` とした。
- `src/jpoke/handlers/move_attack.py` の `エナジーボール_lower_defender_spd` 関数は仕様と一致しており
  変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列は `n/a`）。確率発動の相手能力ランク低下効果であり
  ダメージ量そのものを変化させないため。

## テスト
- `tests/moves_attack/test_move_a.py` に以下のテストを追加:
  - `test_エナジーボール_とくぼう1段階低下が発動しない`
  - `test_エナジーボール_とくぼう1段階低下が発動する`
  - `test_エナジーボール_相手にダメージを与える`
- `python -m pytest tests/ -v` で全3677件パス（1件skip）を確認。
