# レイジングブル レビュー結果

## 仕様書
- PP欄を修正: 一次情報（ポケモンWiki）記載の `10` はそのまま残しつつ、`docs/champions/move_list.txt` に基づく実際のPPが `12` である旨を注記に追加（本家シリーズとChampionsでPPが異なるため、`かげぬい.md` 等の既存表記形式に合わせた）。

## 実装計画書
- 仕様要約・実装コード例のPP表記を `10` → `12` に修正（`docs/champions/move_list.txt` 準拠）。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ra.py` の `"レイジングブル"` エントリで `pp=10` となっていたバグを `pp=12`（`docs/champions/move_list.txt` 準拠）に修正。
- `handlers/move_attack.py` の `レイジングブル_modify_move_type` / `レイジングブル_break_screens` は計画書通りに実装されており、`subject_spec` はデフォルトの `attacker:self`（`MoveHandler` のデフォルト）で仕様と一致。`ON_HIT` は命中・無効化判定後に発火するため「無効化時は壁を解除しない」仕様を正しく満たしている。壁解除は追加効果扱いにしていない（`ON_MODIFY_SECONDARY_CHANCE` 非経由）点も仕様通り。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装・リーサルテスト列は `n/a`。威力固定の通常ダメージ技でリーサル計算に影響する固有条件がないため、`lethal_handlers` 未実装のままで問題なし）。

## テスト
- 既存5件（かくとう/みず/ほのおタイプ変化、ひかりのかべ解除、リフレクター解除）は実装と一致しており変更なし。
- 以下4件を追加:
  - `test_レイジングブル_オーロラベールを解除する`（オーロラベールの解除確認。既存テストはリフレクター/ひかりのかべのみだった）
  - `test_レイジングブル_ケンタロス通常はノーマルタイプになる`（通常フォルムのケースが未カバーだった）
  - `test_レイジングブル_まねっこでケンタロス以外が使用してもノーマルタイプになる`（ケンタロス以外が使用した場合にノーマルタイプ固定になる仕様の確認）
  - `test_レイジングブル_まもるで無効化されたときは壁を解除しない`（まもる無効化時に壁が解除されない仕様の確認）
- `python -m pytest tests/ -v` で全4029件（1件skip）がパスすることを確認。ログは `.loop/test_logs/レイジングブル.log` に保存。
