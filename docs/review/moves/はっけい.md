# はっけい レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/はっけい.md`）。一次情報（`docs/wiki/moves/はっけい.html`）と照合し、かくとうタイプ・物理・威力60・命中100・PP10・直接攻撃・追加効果30%まひ、の内容で作成した。

## 実装計画書
- 新規作成（`docs/plan/moves/はっけい.md`）。既存の類似技（かみなりパンチ・フリーズボルト・のしかかり等）と同じ `apply_ailment_to_defender` 共通ハンドラを使う構成であることを確認し、その通りに記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/move_attack.py` の `はっけい_apply_paralysis_to_defender`（`apply_ailment_to_defender(..., ailment="まひ", chance=0.3)`）と `src/jpoke/data/moves/move_ha.py` の `MoveData`（type=かくとう, category=physical, pp=10, power=60, accuracy=100, flags={"contact", "secondary_effect"}, `Event.ON_DAMAGE_HIT` に登録）は仕様・計画と一致しており、既存の同系統技（かみなりパンチ等）と同じパターンだったため修正不要だった。

## リーサル計算
- 対象外。まひは行動阻害・素早さ低下効果でダメージ量に影響しないため、`docs/progress/move.md` のリーサル実装/リーサルテストは `n/a` のまま。

## テスト
- 変更なし。`tests/moves_attack/test_move_ha.py::test_はっけい_まひが発動する` が既に存在し、`secondary_chance=1.0` で確率発動を検証しており、他の類似技（フリーズボルト・フレアドライブ等）と同じテストパターンで十分と判断した。

## その他
- `docs/progress/move.md` の はっけい 行のうち、未記入だった タイプ/威力/命中率/PP/対象/効果 列と 仕様書/計画書/レビュー 列を実態に合わせて更新した（仕様書=x, 計画書=x, レビュー=✓, タイプ=かくとう, 威力=60, 命中率=100, PP=10, 対象=defender, 効果=追加効果として、30%の確率で相手をまひ状態にする。）。
