# しんくうは レビュー結果

## 仕様書
- 対象外。一次情報（`docs/wiki/moves/しんくうは.html`）を確認したところ、しんくうはは
  「かくとう・特殊・威力40・命中100・PP30・優先度+1・直接攻撃×・追加効果なし」という
  純粋な先制攻撃技であり、`MoveData`（type/category/power/accuracy/pp/priority/flags）のみで
  挙動が完結する。同種の先制攻撃技（しんそく・でんこうせっか・アクアジェット・マッハパンチ・
  バレットパンチなど）も同様の理由で専用の仕様書・計画書を持たない運用のため、本技についても
  新規作成は行わなかった。

## 実装計画書
- 対象外（理由は仕様書の項と同じ）。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `"しんくうは"` エントリ
  （type="かくとう", category="special", pp=30, power=40, accuracy=100, priority=1,
  handlers={}, flags未設定＝直接攻撃なし）は一次情報と完全に一致していることを確認した。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a）。

## テスト
- 変更なし。`tests/moves_attack/test_move_sa.py` の
  `test_しんくうは_相手にダメージを与える` は既存実装と一致しており、
  他の先制攻撃技（アクアジェット・マッハパンチ等）と同一パターンのテストになっていることを確認した。
- `python -m pytest tests/ -v` で全件（3384 passed, 1 skipped）を確認した
  （ログ: `.loop/test_logs/しんくうは.log`）。

## その他
- `docs/progress/move.md` の しんくうは 行に誤りがあったため修正した。
  - PP列: `20` → `30`（実装・一次情報と一致させた）
  - 効果列: `？` → `必ず先制できる(優先度:+1)。追加効果なし。`
  - レビュー列: `-` → `✓`
