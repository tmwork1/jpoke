# ハイドロポンプ レビュー結果

## 仕様書
- 新規作成（docs/spec/moves/ハイドロポンプ.md）。
  - 一次情報（docs/wiki/moves/ハイドロポンプ.html）と src/jpoke/data/ps-champ-ja/moves.json を参照し、
    タイプ・分類・威力（110、第五世代までは120）・命中率（80）・PP（Champions仕様の8）・
    対象・優先度・接触判定・各種判定フラグ（急所/命中判定/まもる/おうじゃのしるし/
    マジックコート/よこどり/オウムがえし）を記載。
  - 追加効果がない通常攻撃技であることを明記。

## 実装計画書
- 新規作成（docs/plan/moves/ハイドロポンプ.md）。
  - いわおとし.md 等の「追加効果なしパターン」に倣い、ハンドラ不要である旨と
    既存 `data/moves/move_ha.py` の実装が仕様と一致していることを記載。
  - Priority 検討対象イベントが存在しないこと、リーサル計算も不要（n/a）である旨を明記。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ha.py` の `"ハイドロポンプ"` エントリ
  （type="みず", category="special", pp=8, power=110, accuracy=80, handlers={}）は
  仕様書・src/jpoke/data/ps-champ-ja/moves.json と完全に一致しており、修正不要と判断。

## リーサル計算
- 対象外（n/a）。追加効果を持たない通常攻撃技のため、専用のリーサル計算ハンドラは不要。

## テスト
- 変更なし。`tests/moves_attack/test_move_ha.py` の
  `test_ハイドロポンプ_相手にダメージを与える` が、追加効果なしの通常ダメージ技として
  適切に検証しており、修正不要と判断。
- `python -m pytest tests/ -v` は 3933 passed, 1 skipped で全件成功
  （.loop/test_logs/ハイドロポンプ.log に記録）。
