# みをけずる レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/みをけずる.md`）。
  一次情報（docs/wiki/moves/みをけずる.html）から以下を確認して記載：
  - タイプ: ノーマル / 分類: 変化 / 威力: - / 命中率: - / PP(Gen9本家): 10 / 対象: 自分 /
    優先度: 0 / 直接攻撃: × / 専用技: ミガルーサ
  - 効果: 自分の最大HPの1/2（切り捨て）を消費して、こうげき・とくこう・すばやさを2段階ずつ上げる
  - 失敗条件: HPが最大HPの半分以下、または3ランクすべてが既に上限（+6）
  - 技の仕様: ダメおし威力2倍にならない・ゆびをふるで選ばれない
  - PPはchampionsの `src/jpoke/data/ps-champ-ja/moves.json` に本技のエントリが存在しないため、
    Gen9本家PP10の他の変化技（あくび・どくどく・はらだいこ等）が軒並みchampionsで
    12へ変換されている規則性から類推し、12と結論付けた。

## 実装計画書
- 新規作成（`docs/plan/moves/みをけずる.md`）。ハンドラ構成・priority根拠・実装コード例・
  エッジケース（ダメおし非該当、あまのじゃく相互作用、PP修正根拠）を記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ma.py`: `pp=10` → `pp=12` に修正
  （champions基準のPP。Gen9本家基準のまま移行されていなかったバグ）。
- `src/jpoke/handlers/move_status.py` の `みをけずる_apply`: ランク変化の有無に関わらず
  無条件にHPを消費していたバグを修正。`modify_attacker_stats` の返り値
  （`HandlerReturn.value` ＝実際に変化したランクの辞書）が空でない場合のみ
  `battle.modify_hp` を呼ぶように変更した。
  - 修正前: 3ランクすべてが+6で変化しなくても常にHPを消費していた
    （一次情報「すでに攻撃・特攻・素早さが最大まで上がっている場合、HPは減らない」に反する）。
  - 修正後: ランクが1つも変化しない場合はHPを消費せず、
    フレームワーク側（`move_executor._execute_status_hit`）が自動的に
    `MOVE_FAILED` ログ・`move_success=False` を設定する（`つるぎのまい` 等、他の
    ランク変化技と同じ挙動パターン）。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行はリーサル実装・リーサルテストともに `n/a`）。

## テスト
- `tests/moves_status/test_move_ma.py`:
  - `test_みをけずる_ランクが上限でも技自体は成功しHPが減る` を
    `test_みをけずる_3ランクすべて上限なら技が失敗しHPも減らない` に置き換え
    （3ランクすべて+6のとき、ランク変化なし・HP消費なし・`move_success is False` を検証）。
  - `test_みをけずる_一部のランクのみ上限なら残りが上昇しHPが減る` を新規追加
    （こうげきのみ+6のとき、とくこう・すばやさは通常通り+2し、HPは消費されることを検証）。
- `tests/moves_attack/test_move_ta.py`:
  - `test_ダメおし_相手がみをけずるでHPを消費しても威力は2倍にならない` を新規追加
    （みをけずるによる自傷HP減少が `hits_taken` に加算されず、ダメおしの威力2倍判定に
    影響しないことを確認）。
- `python -m pytest tests/ -v` で全4439件パス（1件skip、既存分）を確認。
  ログ: `.loop/test_logs/みをけずる.log`
