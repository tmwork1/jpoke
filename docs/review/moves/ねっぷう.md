# ねっぷう レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ねっぷう.md`）。一次情報（`docs/wiki/moves/ねっぷう.html`）に基づき、
  基本情報（ほのお・特殊・威力95(第六世代以降)・命中90・PP10・相手全員・優先度0・接触なし・
  風の技）と効果（10%の確率でやけど付与）、判定フラグを整理した。

## 実装計画書
- 新規作成（`docs/plan/moves/ねっぷう.md`）。既存実装（`data/move.py` の `"ねっぷう"` エントリ、
  `handlers/move_attack.py` の `ねっぷう_apply_burn_to_defender`）が仕様と一致していることを確認し、
  追加実装は不要と結論。

## 実装（handlers/ / data/）
- 変更なし。`data/move.py` の `"ねっぷう"` エントリ（type="ほのお", category="special", pp=10,
  power=95, accuracy=90, flags={"wind", "secondary_effect"}）と、
  `handlers/move_attack.py` の `ねっぷう_apply_burn_to_defender`（`apply_ailment_to_defender` を
  `ailment="やけど", chance=0.1` で呼び出すのみ）は仕様書と一致しており、修正不要と判断した。

## リーサル計算
- 対象外。`docs/progress/move.md` の「リーサル実装」「リーサルテスト」列は `n/a`
  （確率的な追加効果のみの技であり、既存の同種技と同様に対象外）。

## テスト
- 変更なし。`tests/moves_attack/test_move_na.py` の `test_ねっぷう_やけどが発動する`
  （`secondary_chance=1.0` で確定発動させ、やけどの付与を確認）が既存し、仕様と一致していることを確認した。

## その他
- `docs/progress/move.md` の該当行にて PP 列が誤って `12` になっていた（実際のPPは10）ため `10` に修正した。
  また 仕様書・計画書・レビュー列を新規作成・レビュー完了を反映して更新した
  （仕様書: x, 計画書: x, レビュー: ✓）。
