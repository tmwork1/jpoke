# なかよくする レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/なかよくする.md`）
  - 一次情報（`docs/wiki/moves/なかよくする.html`）をもとに作成
  - タイプ: ノーマル、分類: 変化、命中率: 必中、PP: 20、範囲: 相手1体選択、優先度: 0、直接攻撃: ×
  - 効果: 相手の「こうげき」ランクを1段階下げる。まもる・みがわり状態を無視して発動する
  - 判定: まもるで防がれない、みがわりを貫通する、マジックコートで跳ね返される、オウムがえしでコピー可能、
    よこどりは対象外、トリックガードで防がれる（本ライブラリ未実装のため対象外）

## 実装計画書
- 新規作成（`docs/plan/moves/なかよくする.md`）
  - `ON_STATUS_HIT` の subject_spec / priority を既存の同種ハンドラ（`あまえる` 等）に合わせて明記
  - `flags={"unprotectable", "bypass_substitute"}` の指定が必要な根拠（`model/move.py` の
    `is_blocked_by_protect`、`core/move_executor.py` の `check_hit_substitute`）を記載

## 実装（handlers/ / data/）
- 【バグ修正】`handlers/move_status.py` の `なかよくする_modify_defender_stats` が
  `stats={"atk": -2}`（2段階下げ）になっていたが、正しくは1段階下げのため `stats={"atk": -1}` に修正。
  docstringも修正。
  - 誤りの原因と推測: ウィキの「こんなときに使おう」節に登場する類似技「あまえる」（2段階下げ）の
    実装と混同したと見られる
- 【欠落修正】`data/moves/move_na.py` の `なかよくする` エントリに
  `flags={"unprotectable", "bypass_substitute"}` が指定されておらず、まもる・みがわりを無視する
  効果が実装されていなかったため追加
- `accuracy=None,  # 必中` を明示的に追加（従来はフィールド省略によりデフォルトのNoneに依存していたが、
  `ないしょばなし` 等の他の必中技と表記を揃えた。挙動自体に変化なし）

## リーサル計算
- 対象外（変化技のため `docs/progress/move.md` のリーサル実装列は `n/a`）

## テスト
- `tests/moves_status/test_move_na.py`
  - `test_なかよくする_こうげきが2段階下がる` を `test_なかよくする_こうげきが1段階下がる` に修正し、
    アサーションを `defender.rank["atk"] == -1` に変更（実装バグに追随していた誤ったテストを修正）
  - `test_なかよくする_まもる状態を無視してこうげきを下げる` を新規追加（unprotectableフラグの検証）
  - `test_なかよくする_みがわりを貫通してこうげきを下げる` を新規追加（bypass_substituteフラグの検証）
  - `python -m pytest tests/ -v` で全4290件（1件skip）が通ることを確認
    （結果は `.loop/test_logs/なかよくする.log` に保存）

## 備考
- `src/jpoke/data/ps-champ-ja/moves.json` に `なかよくする` のエントリが存在しなかった
  （`なかまづくり` と `なげつける` の間に本来あるべき行が欠落している）。
  既存実装のPP=20は一次情報（ポケモンWiki: PP 20、Gen6以降不変）および
  `changes_from_sv.md` の「全技PPは8/12/16/20の4段階に統一」ルールに照らして妥当な値
  （元のPPが既に20のため変更不要）であることを確認し、そのまま維持した。
