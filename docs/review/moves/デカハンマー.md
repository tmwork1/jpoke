# デカハンマー レビュー結果

## 仕様書
- 変更なし（既存の `docs/spec/moves/デカハンマー.md` を一次情報（ポケモンWiki）と照合し、
  基本情報（はがね・物理・威力160・命中100・PP5・直接攻撃なし）、「2回連続で選べない」
  という技の仕様、判定（急所○/命中判定○/追加効果×/まもる○/おうじゃのしるし○/マジックコート×）
  に誤り・欠落がないことを確認した）。

## 実装計画書
- 変更なし（既存の `docs/plan/moves/デカハンマー.md` を仕様書と照合し、ハンドラ構成
  （`ON_TRY_MOVE_1` での揮発付与、`ON_MODIFY_COMMAND_OPTIONS` でのコマンド除外、
  `ON_TURN_END` でのtick、count=2の根拠）が実装と一致していることを確認した）。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ta.py` の `"デカハンマー"` の `pp` を `5` から `8` に修正した。
  `src/jpoke/data/ps-champ-ja/moves.json` ではPP8（Champions版の8/12/16/20統一ルール）だが、
  実装がGen9本家基準の5のままになっていたバグ（move_list.txtへの移行漏れ）。
  `docs/progress/move.md` のPP列は元々8で正しかった。
- `handlers/move_attack.py` の `デカハンマー_apply_reuse_block`、`handlers/volatile.py` の
  `デカハンマー_modify_command_options` / `デカハンマー_tick_volatile`、`data/volatile.py` の
  `"デカハンマー"` VolatileData を確認。既存の `いちゃもん_modify_command_options` /
  `ブラッドムーン_apply_reuse_block` 等と同一パターンで実装されており、`subject_spec` も
  `AttackContext`/`EventContext` のロールと一致しているため、それ以外の修正なし。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装・リーサルテストは `n/a`）。

## テスト
- `tests/moves_attack/test_move_ta.py` の既存4テスト
  （2ターン後は再び選択可能／まもるで防がれた場合も次のターンは選択不可／
  使用後は次のターンに選択不可／通常使用でダメージを与える）を確認。実装と整合しているため変更なし。
- 未カバーだった「PP未消費（まひによる行動不能）時は揮発状態が付与されず、次のターンも選択できる」
  というエッジケースのテスト `test_デカハンマー_まひで行動不能だった場合はPP未消費で次のターンも選択可能`
  を追加した（`battle.test_option.trigger_ailment = True` でまひによる行動失敗を確定させ、
  PP・揮発状態・次ターンの選択肢を検証）。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ta.py` を実行（並び替えは
  発生せず、新規テストは元々正しい位置に追加済みだった）。
- `python -m pytest tests/ -v` を実行し、全3872件パス・1件skip（既存分）を確認
  （`.loop/test_logs/デカハンマー.log`）。1回目の実行で `test_てんねん_攻撃側は防御ランク補正を無視する`
  が2件失敗したが、デカハンマーの変更とは無関係な既存の乱数依存フレーキーテストであることを
  個別再実行・全体再実行で確認済み（本タスクの変更によるものではない）。

## 進捗表
- `docs/progress/move.md` の該当行を更新
  （レビュー列を `-` から `✓` に、効果列を「自分が最後にPPを消費した技がこの技だった場合、
  次のターンは選ぶことができない。」に更新。PP列は元々8で正しかったため変更なし）。
