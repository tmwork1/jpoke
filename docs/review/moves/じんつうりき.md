# じんつうりき レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/じんつうりき.md`）。
  - docs/wiki/moves/じんつうりき.html と docs/champions/move_list.txt を参照し、
    エスパー / 特殊 / 威力80 / 命中100 / PP20 / 対象1体 / 優先度0 / 接触なし、
    追加効果10%ひるみの仕様をまとめた。

## 実装計画書
- 新規作成（`docs/plan/moves/じんつうりき.md`）。
  - 既存実装（`data/move.py` の `"じんつうりき"`、`handlers/move_attack.py` の
    `じんつうりき_apply_flinch`）は仕様と完全に一致しており、差分なし。

## 実装（handlers/ / data/）
- 変更なし。`じんつうりき_apply_flinch` は `apply_volatile_to_defender` を用いて
  `Event.ON_DAMAGE_HIT` で10%の確率でひるみを付与しており、仕様・既存の同種技
  （しねんのずつき等）と実装パターンが一致していることを確認した。
  `data/move.py` の type/category/pp/power/accuracy/flags もすべて一次情報と一致。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列は `n/a`。
  ひるみ付与のみでHP分布に影響しない追加効果のためリーサル計算ハンドラは不要）。

## テスト
- 変更なし。`tests/moves_attack/test_move_sa.py` の
  `test_じんつうりき_ひるみが発動する` が既に仕様通りの内容（10%確定発動でひるみ付与を確認）
  であることを確認した。
- `python -m pytest tests/ -v` で全3400件（1 skipped）がパスすることを確認した
  （`.loop/test_logs/じんつうりき.log`）。
  途中、`test_field.py::test_オーロラベール_ひかりのかべとは重複しない` が
  一度失敗したが、これは `Battle` のデフォルトseedが `time.time()` 由来で、
  当該テストが急所発生を固定していないことに起因する既存の flaky テストであり、
  じんつうりきの実装・変更とは無関係（再実行で解消）。
