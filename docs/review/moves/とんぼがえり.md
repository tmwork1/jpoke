# とんぼがえり レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/とんぼがえり.md`）。
- 一次情報（`docs/wiki/moves/とんぼがえり.html`）をもとに、基本情報（むし/物理/威力70/命中100/PP20/接触あり）、
  交代の発動条件・非発動条件（まもる・命中失敗・控えなし・レッドカード・だっしゅつボタン・
  にげごし/ききかいひ）、交代が発動するケース（みがわり・ばけのかわ/アイスフェイス・とらわれ状態無視・
  相手を倒した場合の交代順序）を整理して記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/とんぼがえり.md`）。
- 既存の `handlers/move_attack.py` の `pivot`（`ボルトチェンジ` と共通）を使用しており、
  新規実装は不要であることを確認・記載した。
- `docs/spec/turn.md` の `Event.ON_HIT` を確認し、priority はデフォルト（100）のままで
  問題ないことを根拠とともに記載した。
- レビューで確認したエッジケース（控えなし、まもる／命中失敗、相手を倒した場合の交代順序、
  レッドカード等との相互作用、攻撃側がゴツゴツメット等の反動で瀕死になった場合の
  `Interrupt.PIVOT` の扱いに関する既知の制約）を記録した。

## 実装（handlers/ / data/）
- 変更なし。`data/move.py` の `とんぼがえり` エントリ（`type="むし"`, `category="physical"`,
  `pp=20`, `power=70`, `accuracy=100`, `flags={"contact"}`,
  `Event.ON_HIT: h.MoveHandler(ha.pivot)`）は仕様書と一致しており、既存の `pivot` 関数
  （`handlers/move_attack.py`）も動作を手動シミュレーションで確認し、以下がすべて正しく
  動作することを確認した。
  - 通常時: ダメージを与えた後 `Interrupt.PIVOT` が設定される。
  - 控えがいない場合: ダメージは与えるが `Interrupt.PIVOT` は設定されない。
  - まもるで防がれた場合・命中しなかった場合: ダメージなし・交代も発動しない。
  - 相手を倒した場合: 攻撃側の交代 → 防御側の瀕死交代、の順で処理される（仕様通り）。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- `tests/moves_attack/test_move_ta.py` に以下を新規追加。
  - `test_とんぼがえり_ダメージを与える`
  - `test_とんぼがえり_攻撃後に交代可能状態になる`
  - `test_とんぼがえり_控えがいない場合は交代しない`
  - `test_とんぼがえり_まもるで防がれた場合は交代しない`
  - `test_とんぼがえり_命中しなかった場合は交代しない`
  - `test_とんぼがえり_相手を倒した場合でも交代する`
- `python scripts/sort_tests.py tests/moves_attack/test_move_ta.py` でソート、
  `python scripts/generate_test_list.py` で一覧更新、
  `python -m pytest tests/ -v` で全3493件パス（1件skip）を確認した
  （ログ: `.loop/test_logs/とんぼがえり.log`）。
