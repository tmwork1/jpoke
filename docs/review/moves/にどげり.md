# にどげり レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/にどげり.md`）。
  - 一次情報（ポケモンWiki）をもとに、基本情報・2回固定連続攻撃・命中判定は1発目のみ・
    追加効果なし・各種判定フラグ（おうじゃのしるし、オウムがえし等）を記載。

## 実装計画書
- 新規作成（`docs/plan/moves/にどげり.md`）。
  - 既存実装（`data/move.py`）が仕様と一致するよう修正した上で、固有ハンドラを持たない旨、
    `multi_hit` の汎用機構で処理されることを明記。

## 実装（handlers/ / data/）
- `src/jpoke/data/move.py` の `にどげり` エントリを修正。
  - `multi_hit.check_hit_each_time` が `True` になっており、一次情報（命中判定は初回のみ）と
    矛盾していたため `False` に修正。
    - 実際の命中判定は `flags` に `check_hit_each_time` が含まれるかどうかで決まる
      （`core/move_executor.py`）ため、`flags={"contact"}` のみのこの技は修正前から挙動自体は
      仕様通り（1発目のみ判定）だったが、`multi_hit` 辞書内の値が実際の挙動と矛盾しており
      誤解を招くため修正した。
  - `power_sequence` を `(30, 30)` から `()` に修正（威力は固定のため `ダブルアタック` /
    `ダブルウイング` と同一パターンに統一。`_resolve_hit_power` の挙動は変わらない）。
  - `scripts/sort_data/sort_moves.py` を実行し、`MOVES` 辞書の並び順を確認。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装・リーサルテストは `n/a`）。

## テスト
- 既存の `tests/moves_attack/test_move_na.py::test_にどげり_命中判定1回で2回ヒットする` を確認。
  `ダブルアタック` 等と同一パターンの基本テスト（2回ヒットすることを確認）であり、
  現状の実装・仕様と整合しているため変更なし。
- `python -m pytest tests/ -v` を実行し、全3533件パス（1件skip、既存分）を確認
  （`.loop/test_logs/にどげり.log`）。

## 進捗表
- `docs/progress/move.md` の該当行を更新
  （仕様書 `x`、計画書 `x`、テスト `x`、レビュー `✓`、効果列を「1ターンに2回連続で攻撃する。」に更新）。
