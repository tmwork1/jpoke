# イナズマドライブ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/イナズマドライブ.md`）。
  - `docs/wiki/moves/イナズマドライブ.html`（一次情報）を基に、基本情報（タイプ: でんき /
    分類: 特殊 / 威力100 / 命中100 / PP5 / 対象: 相手1体 / 直接攻撃あり）と、効果
    （効果がバツグンのとき威力が4/3倍〈正確には5461/4096倍〉になる）、判定フラグ
    （まもる対象、マジックコート対象外、ゆびをふるで選ばれない等）を記載した。
  - PPについては `src/jpoke/data/ps-champ-ja/moves.json` にミライドンの専用技である本技自体の掲載は
    ないが、同じ本家PP5の `ソウルビート`（line 434）が move_list.txt で8と記載されており
    「全技PP8・12・16・20統一」ルールの対象になっていることを確認できるため、8を採用する
    旨を明記した（`ソウルビート` の実装コード自体は現時点で `pp=5` のままだが、これは
    未レビューによる既知の移行漏れであり、本技の判断根拠には影響しない）。

## 実装計画書
- 新規作成（`docs/plan/moves/イナズマドライブ.md`）。
  - 既存実装（`handlers/move_attack.py` の共有ハンドラ `効果抜群時威力ブースト`、
    `Event.ON_CALC_POWER_MODIFIER`）が一次情報の「正確な補正率は5461/4096倍」と一致して
    おり問題なしという確認結果を記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_a.py` の `イナズマドライブ` エントリ
  （`type="でんき"`, `category="special"`, `pp=5`, `power=100`, `accuracy=100`,
  `flags={"contact"}`）は一次情報と一致していた。
- `src/jpoke/handlers/move_attack.py` の `効果抜群時威力ブースト`
  （`damage_calculator.calc_def_type_modifier(ctx)` が4096超のとき
  `apply_fixed_modifier(value, 5461)` を適用）も仕様と一致しており、
  `アクセルブレイク`（かくとうタイプ版）と共有する実装として妥当。

## リーサル計算
- 対象外。`docs/progress/move.md` の該当行が「リーサル実装: n/a」「リーサルテスト: n/a」
  のため、今回のレビューではスキップした。

## テスト
- 変更なし。`tests/moves_attack/test_move_a.py` に以下のテストが既に存在し、実装・仕様と
  整合していることを確認した。
  - `test_イナズマドライブ_効果抜群のとき威力が4_3倍になる`（ジバコイル vs カメックス、
    でんき→みず2倍、威力100→133）
  - `test_イナズマドライブ_等倍のとき威力倍率なし`（ジバコイル vs カビゴン、でんき→ノーマル
    等倍、威力100のまま）
- `python -m pytest tests/ -v` を実行し、3658 passed, 1 skipped で全件成功。
  結果は `.loop/test_logs/イナズマドライブ.log` に保存した。
- `docs/progress/move.md` のイナズマドライブ行を
  「仕様書=x / 計画書=x / 実装=x / テスト=x / レビュー=✓」に更新し、「効果」欄の
  プレースホルダ「？」を実際の効果説明に更新した（他行は変更していない）。
