# シャドーレイ レビュー結果

## 仕様書
- 新規作成（docs/spec/moves/シャドーレイ.md）
- 一次情報（docs/wiki/moves/シャドーレイ.html）と照合し、基本情報・判定フラグ・技の仕様
  （無視できる特性はかたやぶりと同一対象、ファントムガード／プリズムアーマーは対象外、
  第七世代限定の自身特性無視は第八世代以降撤廃）をまとめた。
- 事前に懸念されていた「タイプ相性を無視する技」という説は一次情報で否定。
  実際は「相手の特性を無視して攻撃する」技であり、実装（`ignore_ability` フラグ + かたやぶりと
  同じ `mold_breaker_ignorable` 判定）は正しい方向性だった。
- PP について: src/jpoke/data/ps-champ-ja/moves.json にシャドーレイの記載が存在しないため
  （シャドーダイブ・シャドーボーンなど他の専用技も同様に未記載）、champions 値への
  上書きは行わず Gen9 本家準拠の PP5 を維持する方針を明記した。

## 実装計画書
- 新規作成（docs/plan/moves/シャドーレイ.md）
- 既存実装（`handlers/move_attack.py` の `シャドーレイ_disable_defender_ability` /
  `シャドーレイ_restore_defender_ability`、`data/moves/move_sa.py` の `ON_BEGIN_MOVE` /
  `ON_END_MOVE` 登録）の設計根拠を記録。特性「かたやぶり」の実装パターンを踏襲している
  ことを確認し、priority は turn.md に記載がないため既存の かたやぶり 実装（デフォルト）に
  合わせる方針とした。

## 実装（handlers/ / data/）
- 変更なし。既存実装が仕様と一致していることを確認した。
  - `mold_breaker_ignorable` フラグを持つ特性のみ無効化（ハードロック等は対象、
    ファントムガード／プリズムアーマーは対象外＝フラグ未付与で正しく除外）。
  - 攻撃終了後に `remove_ability_disabled_reason` で確実に復元。
  - 使用者自身の特性は無視しない（defender のみ対象）＝第八世代以降・SV基準として正しい。
  - 技呼び出し系（ゆびをふる等）経由での無効化除外は、当該機構自体が本プロジェクトで
    未実装（`ゆびをふる` は `handlers={}` の TODO）のため対応不要と判断。

## リーサル計算
- 対象外（docs/progress/move.md のリーサル実装／リーサルテスト列がともに n/a）。

## テスト
- 変更なし。既存の2テスト
  （`test_シャドーレイ_ハードロックの効果抜群軽減を無視する`、
  `test_シャドーレイ_攻撃後に相手の特性が有効に戻る`）が
  同系技フォトンゲイザー・メテオドライブと同一パターンで実装されており、
  効果の確認として十分と判断した。
- `python -m pytest tests/ -v` を実行し、3815 passed, 1 skipped で全テスト成功を確認
  （ログ: C:\Users\tmtmp\Documents\pokemon\jpoke\.loop\test_logs\シャドーレイ.log）。
