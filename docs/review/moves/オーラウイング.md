# オーラウイング レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/オーラウイング.md`）
- 一次情報（ポケモンWiki）に基づき、タイプ・分類・威力・命中・PP（本家）・急所ランク+1・
  追加効果（自分のすばやさ+1、100%）・判定フラグを記載
- PLA固有の「早業・力業」に伴う行動順補正等は本プロジェクト対象外として明記

## 実装計画書
- 新規作成（`docs/plan/moves/オーラウイング.md`）
- 既存の同種ハンドラ（`はがねのつばさ_boost_attacker_B` 等の自分ランク上昇追加効果技）を
  参照し、`Event.ON_HIT` + `MoveHandler` デフォルト（`subject_spec="attacker:self"`, priority=100）で
  登録する方針を明記
- リーサル計算は対象外（`n/a`）である根拠を明記

## 実装（handlers/ / data/）
- `src/jpoke/handlers/move_attack.py` の `オーラウイング_boost_attacker_spe` は仕様通り
  `modify_attacker_stats(battle, ctx, value, stats={"spe": 1})`（デフォルトchance=1で100%）であり、変更なし
- `src/jpoke/data/moves/move_a.py` の `MoveData` を修正: `pp=10` → `pp=12`
  - `src/jpoke/data/ps-champ-ja/moves.json` には「オーラウイング」自体の掲載がないため、同じ本家PP10を持つ
    他の技（`10まんばりき`→12、`ラスターカノン`→12、`れいとうビーム`→12）の変換結果から類推し、
    Championsルール（PP8/12/16/20の4段階統一）に基づき12に修正した
  - `type`, `category`, `power`, `accuracy`, `critical_rank`, `flags`, `handlers` はすべて仕様と一致しており変更なし

## リーサル計算
- 対象外（`n/a`）。自分自身のすばやさランク上昇はダメージ量に直接影響しないため、専用の
  リーサルハンドラは元々実装されておらず、今回も追加不要と判断した

## テスト
- `tests/moves_attack/test_move_a.py::test_オーラウイング_素早さ1段階上昇が発動する` は
  仕様（命中時100%で自分のSが1段階上昇）と一致しており、修正不要（変更なし）
- `python -m pytest tests/ -v` で全3692件パス（1件skip）を確認
  （ログ: `.loop/test_logs/オーラウイング.log`）
