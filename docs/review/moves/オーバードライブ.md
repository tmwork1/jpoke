# オーバードライブ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/オーバードライブ.md`）。
  - 一次情報（`docs/wiki/moves/オーバードライブ.html`）に基づき、タイプ・分類・威力・命中・対象・
    追加効果なし・音技フラグ等を記載。
  - PPは `src/jpoke/data/ps-champ-ja/moves.json` に本技の記載がなかったため、同じ本家基準PP=10を持つ
    ハイパーボイス・むしのさざめき・フレアソング（champions側でいずれもPP=12）から類推し、
    12と判断した。

## 実装計画書
- 新規作成（`docs/plan/moves/オーバードライブ.md`）。
  - 仕様書の内容を要約し、PP修正（10→12）の根拠を記載。
  - 追加効果がないため専用ハンドラは不要である旨を明記。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `pp` を `10`（Gen9本家基準の値が残っていた）から
  `12`（champions基準の類推値）に修正した。
- その他（type/category/power/accuracy/flags/handlers）は仕様と一致しており変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列は n/a）。

## テスト
- `tests/moves_attack/test_move_a.py` に以下を追加。
  - `test_オーバードライブ_PPが12`: champions側のPP変換値が12であることを確認。
  - `test_オーバードライブ_相手にダメージを与える`: 通常の特殊ダメージが発生することを確認。
  - 使用ポケモンは `ストリンダー(ハイ)`（本技の専用習得ポケモン）。
- `python -m pytest tests/ -v` で3694件全てパス（1件skip）を確認。
