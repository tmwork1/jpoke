# スピードスター レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/スピードスター.md`）
- 一次情報（`docs/wiki/moves/スピードスター.html`）と照合し、以下を明記した
  - 基本情報（ノーマル/特殊/威力60/命中率必中/PP20/対象は相手全員だが本プロジェクトはシングル専用のため
    実質相手1体/優先度0/接触なし）
  - 命中判定に関する補足（まもる等の防御効果や、あなをほる・そらをとぶ等の回避状態には通常のダメージ技と
    同様に命中しない。必中フラグは命中率・回避率の判定のみをスキップするものであるため）
  - `src/jpoke/data/ps-champ-ja/moves.json` に本技が掲載されていない件について、別エージェントによる調査結果を
    踏まえ、「Champions実装対象外」を意味するものではないと明記した
    （同ファイルはSVからChampionsへのPP等の数値変更点のみを転記した表であり、Gen9の全技一覧ではない。
    本技のWikiカテゴリタグに「ソード・シールドで使用不可能なわざ」等の除外タグが無く、SVでも
    わざマシン・おしえわざとして習得可能な現行技であることを確認した）

## 実装計画書
- 新規作成（`docs/plan/moves/スピードスター.md`）
- `handlers={}` のみで完結する方針を明記し、`accuracy=None` による必中処理はcore側
  （`move_executor.py`）が既存の同種必中技（`あてみなげ`等）と同様に処理するため追加実装不要と記載した

## 実装（handlers/ / data/）
- 変更なし
- `src/jpoke/data/moves/move_sa.py` の `MOVES["スピードスター"]`（`type="ノーマル"`, `category="special"`,
  `pp=20`, `power=60`, `accuracy=None`, `handlers={}`）は仕様書・計画書と一致しており誤り・欠落なし
- PPについて: `src/jpoke/data/ps-champ-ja/moves.json` に本技の記載が無いため、champions値との照合はできないが、
  既存PP=20は同ファイルで統一されている4段階（8/12/16/20）の一つに合致しており、Gen9(SV)本家の
  PP=20とも一致するため、変更の必要なし

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a）

## テスト
- `tests/moves_attack/test_move_sa.py` に `test_スピードスター_相手の回避率が高くても必ず命中する` を追加
  （`あてみなげ`の同種テストを参考に、相手の回避ランクを+6にしても必ずダメージが発生することを確認）
- `python -m pytest tests/ -v` 実行、3830 passed, 1 skipped（全テスト成功、結果は
  `.loop/test_logs/スピードスター.log` に保存）
