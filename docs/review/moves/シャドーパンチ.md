# シャドーパンチ レビュー結果

## 仕様書
- 新規作成（docs/spec/moves/シャドーパンチ.md）。一次情報（docs/wiki/moves/シャドーパンチ.html）に基づき、
  タイプ（ゴースト）・分類（物理）・威力（60）・命中（必中）・PP（20）・接触あり・パンチ技・
  追加効果なし・各種判定フラグ（急所あり／まもる可／おうじゃのしるし発動／マジックコート・よこどり対象外／
  オウムがえしコピー可）を記載。

## 実装計画書
- 新規作成（docs/plan/moves/シャドーパンチ.md）。追加効果を持たない攻撃技のため専用ハンドラは不要と判断し、
  `MoveData` の各フィールド（type/category/pp/power/accuracy=None/flags={"contact","punch"}）のみで
  実装が完結する方針を記載。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_sa.py` の `MOVES["シャドーパンチ"]` は
  `type="ゴースト"`, `category="physical"`, `pp=20`, `power=60`, `accuracy` 未指定（必中）,
  `flags={"contact", "punch"}`, `handlers={}` となっており、仕様書・計画書と一致していることを確認した。
  PP値（20）は docs/champions/move_list.txt と一致（Gen9基準からの変更なし）。

## リーサル計算
- 対象外（docs/progress/move.md の該当行が n/a）。

## テスト
- 新規追加（tests/moves_attack/test_move_sa.py）。
  - `test_シャドーパンチ_相手の回避率が高くても必ず命中する`: 相手の回避率を+6しても必ず命中することを確認。
    ノーマルタイプはゴースト技を無効化するため、対象ポケモンをカビゴンからピカチュウに変更して検証。
  - `test_シャドーパンチ_タイプ分類威力が正しく反映される`: type/category/power がゴースト・物理・60で
    あることを確認。
- `python -m pytest tests/ -v` を実行し、3817 passed, 1 skipped（既存の無関係なskip）で全テスト成功を確認。
