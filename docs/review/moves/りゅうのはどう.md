# りゅうのはどう レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/りゅうのはどう.md`）。
- 一次情報（`docs/wiki/moves/りゅうのはどう.html`）に基づき、タイプ・分類・威力
  （85、第五世代までは90）・命中率・PP・対象・優先度・接触の有無・判定フラグ・
  メガランチャーによる威力1.5倍の効果を記載。

## 実装計画書
- 新規作成（`docs/plan/moves/りゅうのはどう.md`）。
- 追加効果を持たない技のため専用ハンドラは不要である旨と、既存実装の
  PP不一致（後述）の修正方針を記載。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ra.py` の `"りゅうのはどう"` エントリは
  `pp=12` で、`docs/champions/move_list.txt:944` および `docs/champions/changes_from_sv.md`
  （全技PPを8/12/16/20の4段階に統一）と整合していることを確認した。
  - レビュー作業中に一度 `pp=10`（Gen9基準）へ誤って変更したが、本プロジェクトの対象は
    ポケモンチャンピオンズのみであり、champions move_list.txt のPP値が正であるため
    `pp=12` に差し戻した。
  - その他（type・category・power・accuracy・flags={"pulse"}・handlers空）は
    仕様と一致しており変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`。HP依存の追加効果・
  威力変動を持たない通常のダメージ技のため）。

## テスト
- 既存テスト（`tests/moves_attack/test_move_yarawa.py::test_りゅうのはどう_相手にダメージを与える`、
  `tests/test_field.py` のフィールド威力補正テスト、`tests/test_lethal.py`、
  `tests/abilities/test_ability__group.py` の各テスト）を確認し、PP修正の影響を受けないことを確認。
  変更なし。
