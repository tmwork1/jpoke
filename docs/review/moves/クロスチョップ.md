# クロスチョップ レビュー結果

## 仕様書
- 新規作成（docs/spec/moves/クロスチョップ.md）
  - docs/wiki/moves/クロスチョップ.html を一次情報として、基本情報（かくとう・物理・威力100・
    命中80・PP8・対象1体・優先度0・接触あり・急所ランク+1）と判定フラグをまとめた。
  - PPは docs/champions/move_list.txt（8）を採用し、Gen9本家基準（5）は不採用と明記した。

## 実装計画書
- 新規作成（docs/plan/moves/クロスチョップ.md）
  - 追加効果を持たない技のため専用ハンドラ不要、`MoveData` の設定のみで完結する方針を記載。
  - 既存実装の `pp=5` がチャンピオンズ基準（8）への移行漏れバグであることを明記。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ka.py` の `MOVES["クロスチョップ"]` の `pp` を `5` → `8` に修正した
  （docs/champions/move_list.txt の値と不一致だったバグ）。
  それ以外（type/category/power/accuracy/critical_rank/flags/handlers）は仕様書と一致しており変更なし。

## リーサル計算
- 対象外（docs/progress/move.md でリーサル実装=n/a, リーサルテスト=n/a）。

## テスト
- `tests/moves_attack/test_move_ka.py` に `test_クロスチョップ_相手にダメージを与える` を追加した
  （既存の急所判定テスト2件のみで、通常ダメージの確認テストが欠けていたため）。
- 既存の急所ランク+1関連テスト（乱数0で急所発生／乱数0.5で急所なし）は仕様と一致しており変更なし。
