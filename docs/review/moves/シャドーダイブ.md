# シャドーダイブ レビュー結果

## 仕様書
- 変更なし。`docs/spec/moves/シャドーダイブ.md` を一次情報（ポケモンWiki `docs/wiki/moves/シャドーダイブ.html`）と照合したが、誤り・欠落は無かった（タイプ・分類・威力・命中率・PP・優先度・直接攻撃・効果・技の仕様の記載すべて一致）。
- `docs/spec/volatiles/シャドーダイブ.md`（揮発状態側）も既存内容のままで一次情報と齟齬なし。

## 実装計画書
- 新規作成: `docs/plan/moves/シャドーダイブ.md`
  - 既存実装（`ON_MOVE_CHARGE` による2ターン技化・`unprotectable` フラグ）を仕様書と照合し、記録した。
  - `src/jpoke/data/ps-champ-ja/moves.json` に「シャドーダイブ」の行が存在しないことを確認し、その旨と根拠（TMが存在しない専用技であるため掲載対象外と推測されること）、および README.md の対象範囲定義（champions側の情報がない場合はGen9本家仕様を採用）に基づき既存PP値（5）を変更しない判断を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_sa.py` の `MoveData("シャドーダイブ")` は既に以下の通り正しく実装済みだった:
  - `ON_MOVE_CHARGE` ハンドラで `h.charge_into_volatile(b, c, v, "シャドーダイブ")` を呼び出し、2ターン技として機能する。
  - `flags={"contact", "unprotectable"}` によりまもる系を無視して攻撃する。
  - `pp=5`, `power=120`, `accuracy=100`, `type="ゴースト"`, `category="physical"` は一次情報と一致。
  - ゴーストダイブのレビューで発見されたバグ（`ON_MOVE_CHARGE` 未登録・`unprotectable` フラグ欠落・PP移行漏れ）はシャドーダイブ側には存在しなかった。
- 揮発状態側（`src/jpoke/data/volatile.py` の `"シャドーダイブ"` エントリ、`src/jpoke/handlers/volatile.py` の `シャドーダイブ_can_hit_hidden_target` / `シャドーダイブ_remove_volatile`）も変更なし。

## PPについて（付記）
- `src/jpoke/data/ps-champ-ja/moves.json` を全文検索したが「シャドーダイブ」の行は存在しなかった（シャドークロー・シャドーパンチ・シャドーボールはTM経由で習得できるため掲載されているが、TMの存在しない専用技であるシャドーダイブ・シャドーボーン・シャドーレイは掲載対象外になっている模様）。
- `docs/champions/changes_from_sv.md` の「全技PPが8/12/16/20の4段階に統一（move_list.txt参照）」ルールの参照先に本技の記載がなく、champions側の意図した値を確認できないため、README.md の対象範囲定義に従い既存のGen9本家/Wiki準拠のPP値（5）をそのまま維持した（推測で8等に変更しない）。

## リーサル計算
- 対象外（`docs/progress/move.md` のシャドーダイブ行は「リーサル実装」「リーサルテスト」ともに `n/a`）。

## テスト
- `tests/moves_attack/test_move_sa.py` に `test_シャドーダイブ_2ターンで攻撃する` を追加した（`test_シャカシャカほう_使用後に攻撃者のHPが回復する` と `test_シャドーボーン_ぼうぎょ1段階低下が発動する` の間、五十音順の暫定位置）。
  - 1ターン目はダメージなし・`"シャドーダイブ"` 揮発状態が付与されること、2ターン目でダメージが発生し揮発状態が解除されることを確認（ゴーストダイブの対応するテストと同型。防御側はゴースト技を無効化しないどくタイプの「ドガース」を使用）。
  - このテストが欠落しており、シャドーダイブ単体の「2ターン技として攻撃が成立する」ことを move レベルで検証するテストが存在しなかったための追加。
- 既存テスト（変更なし、そのまま有効であることを確認）:
  - `tests/volatiles/test_volatile_satana.py::test_シャドーダイブ_まもる中の相手にダメージを与えられる`（まもる貫通の確認）
  - `tests/volatiles/test_volatile_ka.py` の `hidden_move_name` パラメトライズテスト群（`"シャドーダイブ"` を含む。回避・コマンド固定・交代禁止などの揮発状態共通仕様を確認）
- `python -m pytest tests/ -v` を実行し、3816 passed, 1 skipped（既存の無関係なskip）で全テスト成功を確認した。ログは `C:\Users\tmtmp\Documents\pokemon\jpoke\.loop\test_logs\シャドーダイブ.log` に保存済み。
