# ヘドロウェーブ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ヘドロウェーブ.md`）。
  - タイプ: どく / 分類: 特殊 / 威力95 / 命中100 / PP12（Champions基準。src/jpoke/data/ps-champ-ja/moves.json を正とした）
  - 対象: 自分以外全体（本プロジェクトはシングル戦のみのため相手1体と同義）
  - 追加効果: 10%の確率で相手を『どく』状態にする
  - Zワザ威力175・ダイマックスわざ威力90は対象範囲外として明記

## 実装計画書
- 新規作成（`docs/plan/moves/ヘドロウェーブ.md`）。
  - `docs/plan/moves/攻撃技_どく系.md`（グループ計画書）の記載内容と矛盾しないことを確認しつつ、
    ヘドロウェーブ単体の個別計画書として切り出した。
  - グループ計画書には `Event.ON_HIT` と記載されていたが、実際の実装・テストは
    `Event.ON_DAMAGE_HIT`（priority=100・`MoveHandler` デフォルト）を使用しており、
    同グループの `ヘドロこうげき`・`ヘドロばくだん`・`どくばり` 等の既存実装と一貫していることを確認。
    本ファイルでは実装済みの `Event.ON_DAMAGE_HIT` を正としてPriority根拠を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `ヘドロウェーブ` の `pp` が `10`（Gen9本家基準）に
  なっていたバグを発見し `pp=12`（Champions基準・src/jpoke/data/ps-champ-ja/moves.json）に修正した。
- `src/jpoke/handlers/move_attack.py` の `ヘドロウェーブ_apply_poison_to_defender`
  （`apply_ailment_to_defender(..., ailment="どく", chance=0.1)` を呼ぶだけの実装）は
  仕様と一致しており変更なし。どく・はがねタイプへの無効化やめんえき等の免疫チェックは
  `ailment_manager.apply` 内部で処理されるため、重複ガードもなく適切。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a）。

## テスト
- 変更なし。既存の `test_ヘドロウェーブ_どくが発動する`
  （`tests/moves_attack/test_move_ha.py`）は `secondary_chance=1.0` で確定発動させ
  `defender.ailment.name == "どく"` を確認しており、同グループの
  `ヘドロこうげき`・`ヘドロばくだん` のテストと一貫したパターンになっている。
  PP修正後も `python -m pytest tests/ -v` で全件パス（3990 passed, 1 skipped）を確認した。
