# マジカルリーフ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/マジカルリーフ.md`）。ポケモンWiki一次情報（くさ・特殊・威力60・必中・PP20・接触なし・追加効果なし）をもとに作成。既存の類似技（スピードスター）の仕様書を参考に判定フラグ・実装状況・備考を整理した。

## 実装計画書
- 新規作成（`docs/plan/moves/マジカルリーフ.md`）。専用ハンドラ不要（`accuracy=None`のみで必中を実現）である旨と、テスト方針・リーサル対象外の根拠を記載した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ma.py` の `MOVES["マジカルリーフ"]` は既に `type="くさ"`, `category="special"`, `pp=20`, `power=60`, `accuracy=None`, `handlers={}` と仕様に一致しており、修正不要と確認した。PPも `docs/champions/move_list.txt` に掲載がないためGen9本家値（20）をそのまま採用（4段階基準にも合致）。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a）。

## テスト
- `tests/moves_attack/test_move_ma.py` に以下を追加:
  - `test_マジカルリーフ_相手にダメージを与える`
  - `test_マジカルリーフ_相手の回避率が高くても必ず命中する`（回避ランク+6でも命中することを確認）
