# シェルブレード レビュー結果

## 仕様書
- 新規作成（docs/spec/moves/シェルブレード.md）。一次情報（docs/wiki/moves/シェルブレード.html）を
  もとに基本情報・効果・判定フラグ・Zワザ/ダイマックスわざ威力を記載した。
  PP は champions基準（src/jpoke/data/ps-champ-ja/moves.json）の12を採用し、一次情報の本家基準10との
  差異を注記した。

## 実装計画書
- 新規作成（docs/plan/moves/シェルブレード.md）。共通ハンドラ `modify_defender_stats` を使う方針、
  必要な `flags`（contact / slash / secondary_effect）、リーサル計算対象外である旨を明記した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_sa.py` の `MOVES["シェルブレード"]` の `pp` が `10`
  （Gen9本家基準）になっていたバグを `12`（champions基準・src/jpoke/data/ps-champ-ja/moves.json および
  docs/progress/move.md と整合）に修正した。
- `flags={"contact", "slash", "secondary_effect"}` と
  `handlers/move_attack.py` の `シェルブレード_lower_defender_def`
  （`modify_defender_stats(..., stats={"def": -1}, chance=0.5)`）は仕様と一致しており変更なし。

## リーサル計算
- 対象外（docs/progress/move.md のシェルブレード行「リーサル実装」「リーサルテスト」列が n/a）。

## テスト
- `tests/moves_attack/test_move_sa.py` にテストが存在しなかったため新規追加。
  - `test_シェルブレード_ぼうぎょ1段階低下が発動しない`
  - `test_シェルブレード_ぼうぎょ1段階低下が発動する`
  - `test_シェルブレード_相手にダメージを与える`
- `python -m pytest tests/ -v` は 3777 passed, 1 skipped で全件成功
  （.loop/test_logs/シェルブレード.log に保存）。
