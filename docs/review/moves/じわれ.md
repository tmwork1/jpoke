# じわれ レビュー結果

## 仕様書

- 新規作成（`docs/spec/moves/じわれ.md`）。
  - 既存の共通仕様書 `docs/spec/moves/_ohko.md`（一撃必殺技共通仕様）を踏まえつつ、
    じわれ固有の情報（じめんタイプ技であることに由来する無効化条件：ひこうタイプ・
    特性「ふゆう」・「でんじふゆう」、および本プロジェクト未実装の「テレキネシス」）を
    一次情報（`docs/wiki/moves/じわれ.html`）から追記した。

## 実装計画書

- 新規作成（`docs/plan/moves/じわれ.md`）。
  - 実装は既に完了していたため、既存実装（`ha.ohko_damage` を `Event.ON_MODIFY_MOVE_DAMAGE`
    priority=90 で登録する、`つのドリル`・`ハサミギロチン`・`ぜったいれいど` と同一パターン）を
    根拠とともに記録した。

## 実装（handlers/ / data/）

- `src/jpoke/data/move.py` の `じわれ` エントリに `power=0` を追加した
  （`つのドリル`・`ハサミギロチン`・`ぜったいれいど` との表記統一。`MoveData.power` の
  デフォルトは `None` で `damage.py` の `calc_damages` は `if not move.power: return [0]` により
  `None`/`0` を同一に扱うため、挙動への影響はなし）。
- `python scripts/sort_data/sort_moves.py` を実行（並び替えの実質的な差分なし）。
- ハンドラ本体（`handlers/move_attack.py` の `ohko_damage`）・じめんタイプによる無効化
  （ひこう・ふゆう・でんじふゆう）は既存の共通実装で正しく動作することを確認した。誤りなし。

## リーサル計算

- 対象外（`docs/progress/move.md` の じわれ 行は「リーサル実装: n/a」「リーサルテスト: n/a」）。

## テスト

- `tests/moves_attack/test_move__group.py` の
  `test_一撃必殺技_タイプ相性で無効化される` にて、コメントアウトされていた
  `("じわれ", "ピジョン")` のパラメータを有効化した（現在の実装で正しく無効化されることを確認済み）。
- `tests/test_item.py` の `test_ひるみ付与アイテム_一撃必殺技には効果がない` を修正し、
  防御側に「きあいのタスキ」を持たせた上で HP=1 で生存することも直接検証するよう強化した
  （旧docstringは「一撃必殺技ときあいのタスキの組み合わせに既知の不具合がありHP1で耐えられない」
  という、既に修正済み（`ohko_damage` priority=90 化）で解消済みの内容を記載していたため、
  実際に動作確認のうえ更新した）。
- `python scripts/sort_tests.py tests/moves_attack/test_move__group.py tests/test_item.py` を実行。
- `python scripts/generate_test_list.py` を実行（差分なし）。
- `python -m pytest tests/ -v` で全3404件成功（1件skip）を確認。ログは
  `.loop/test_logs/じわれ.log` に保存。
