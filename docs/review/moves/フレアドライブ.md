# フレアドライブ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/フレアドライブ.md`）。
  一次情報（`docs/wiki/moves/フレアドライブ.html`）・`docs/champions/move_list.txt`・
  `docs/spec/moves/_move_labels.md`・`docs/spec/moves/_move_secondary_1.md` を参照して作成。
  反動ダメージ（1/3）、追加効果のやけど（10%）、使用者がこおり状態のときの無条件解凍効果、
  ちからずくとの相互作用（やけどのみ抑制され、反動・解凍は発生する）を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/フレアドライブ.md`）。
  かえんぐるま・かえんボール・もえつきるのレビュー時に確立された「使用者側の無条件解凍は
  個別実装が必要」というパターンに倣い、`フレアドライブ_thaw_attacker` の追加計画を記載。
  priorityの根拠（`docs/spec/turn.md` の ON_CHECK_ACTION 節と既存実装の比較）も明記した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `"フレアドライブ"` エントリを修正:
  - `pp=15` → `pp=16`（`docs/champions/move_list.txt` 774行目の値に合わせる。Gen9本家PP15の
    移行漏れバグ）。
  - `Event.ON_TRY_ACTION: h.MoveHandler(ha.フレアドライブ_thaw_attacker, priority=5)` を追加
    （使用者がこおり状態のときに無条件解凍してから攻撃する効果が未実装だったため）。
- `src/jpoke/handlers/move_attack.py` に `フレアドライブ_thaw_attacker` を新規実装
  （`かえんぐるま_thaw_attacker` / `もえつきる_thaw_attacker` と同じパターン）。
- 反動ダメージ（`フレアドライブ_recoil`, 1/3）とやけど付与（`フレアドライブ_apply_burn_to_defender`,
  10%）は既存実装のまま仕様と一致しており修正不要だった。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装・リーサルテスト列は `n/a`）。

## テスト
- `tests/moves_attack/test_move_ha.py` に
  `test_フレアドライブ_こおり状態で使うと解凍されて攻撃できる` を追加
  （`かえんぐるま`/`かえんボール` の同種テストと同じ構成）。
- 既存の `test_フレアドライブ_やけどが発動する` / `test_フレアドライブ_使用後に攻撃者が反動ダメージを受ける`
  は変更なし。
- `python -m pytest tests/ -v` を実行し、全件パス（3974 passed, 1 skipped）を確認
  （`.loop/test_logs/フレアドライブ.log` に保存）。
  ※ 実行順序によっては本レビューと無関係な既存テスト
  （`test_ウェザーボール_らんきりゅう中はタイプも威力も変化しない` /
  `test_しんぴのつるぎ_相手のとくぼうランク変化の影響を受けない` 等、乱数状態に依存した
  順序依存の既存テスト）が単発で失敗することがあるが、単体実行では成功しフレアドライブの
  変更とは無関係であることを確認済み。
