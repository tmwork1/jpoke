# はどうだん レビュー結果

## 仕様書
- 新規作成（`docs/wiki/moves/はどうだん.html` を基に、必中技であること・弾/波動フラグ・
  判定フラグ（あなをほる等の回避状態には必中でも命中しない）を記載）

## 実装計画書
- 新規作成

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `はどうだん` エントリに `accuracy` フィールドが
  未指定だった。`MoveData.accuracy` のデフォルトが `None`（必中）のため実際の挙動には
  問題なかったが、`あてみなげ` 等の他の必中技に倣い `accuracy=None` を明示的に追加した
  （可読性・保守性のための修正、挙動に変化なし）。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a）

## テスト
- `tests/moves_attack/test_move_ha.py` に以下を追加:
  - `test_はどうだん_相手の回避率が高くても必ず命中する`
  - `test_はどうだん_かくとうタイプの特殊技としてダメージを与える`
