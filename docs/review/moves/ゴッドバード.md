# ゴッドバード レビュー結果

## 仕様書
- 変更なし（`docs/spec/moves/ゴッドバード.md` は既存の内容が一次情報（ポケモンWiki）と整合していることを確認した）

## 実装計画書
- 新規作成（`docs/plan/moves/ゴッドバード.md`）
  - 2ターン溜め技としての仕様要約、既存実装の欠落点（`ON_MOVE_CHARGE` 未登録・PP誤り）、ハンドラ構成、priority根拠、あなをほる系・ソーラービーム系との違いを記載

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ka.py`: ゴッドバードが2ターン溜め技として全く実装されていなかった不具合を修正。
  - `Event.ON_MOVE_CHARGE` ハンドラ（`charge_into_volatile` 経由でゴッドバード揮発状態へ移行）を追加
  - PP を `5`（base game 値）から `8`（`src/jpoke/data/ps-champ-ja/moves.json` 準拠のチャンピオンズ値）に修正
- `src/jpoke/handlers/volatile.py`: `ゴッドバード_remove_volatile`（攻撃後に揮発状態を解除）を新規追加
- `src/jpoke/data/volatile.py`: `ゴッドバード` の `VolatileData`（`ON_MODIFY_COMMAND_OPTIONS` でコマンド強制、`ON_HIT` で揮発状態解除）を新規追加
- `src/jpoke/types/volatile.py`: `scripts/generate_literals/generate_volatile_literal.py` を実行し `VolatileName` に `"ゴッドバード"` を自動反映
- 追加効果（ひるみ30%・`ON_DAMAGE_HIT`）、急所ランク+1（`critical_rank=1`）、`_INNATE_FLINCH_MOVES` へのおうじゃのしるし重複無効登録は既存実装のままで仕様通りだったため変更なし

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）

## テスト
- `tests/moves_attack/test_move_ka.py`:
  - 既存の `test_ゴッドバード_ひるみが発動する` が単発 `run_move` 前提になっており、2ターン溜め技実装後は機能しなくなるため、1ターン目でチャージさせてから2ターン目で判定するよう修正
  - `test_ゴッドバード_1ターン目は溜めてダメージを与えない`、`test_ゴッドバード_2ターン目に攻撃して揮発状態が解除される`、`test_ゴッドバード_急所ランクが1`、`test_ゴッドバード_急所ランクが1_乱数大で急所なし` を新規追加
- `tests/moves_status/test_move_ka.py`: ゴッドバード（攻撃技）のひるみテストが変化技テストファイルに誤って重複配置されていたため削除（`tests/moves_attack/` 側に統合済み）
- `python -m pytest tests/ -v` を実行し、3759 passed, 1 skipped（ゴッドバードとは無関係の既存skip）を確認。ログは `C:\Users\tmtmp\Documents\pokemon\jpoke\.loop\test_logs\ゴッドバード.log` に保存
