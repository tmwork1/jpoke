# くすぐる レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/くすぐる.md`）。ポケモンWiki一次情報（`docs/wiki/moves/くすぐる.html`）をもとに、
  基本情報（タイプ/分類/威力/命中/PP/対象/優先度/接触/音技）、効果、判定フラグ（急所・命中判定・追加効果・
  まもる・マジックコート・よこどり・オウムがえし・おうじゃのしるし）、ランク低下無効化系特性・道具との相互作用
  （クリアボディ・メタルプロテクト・しろいけむり・フラワーベール・しろいきり・かいりきバサミ・まけんき・かちき）を
  「おたけび」仕様書と同じ構成でまとめた。

## 実装計画書
- 既存（`docs/plan/moves/くすぐる.md`）と実装が既に一致していたが、記述が古いままだったため修正した。
  - 「`handlers` キーが未設定（未実装）」という記述を「実装済み」に修正
  - コードスニペットを実装の実体（`category="status"`、`data/moves/move_ka.py`、`ctx: AttackContext`、
    `stats={"atk": -1, "def": -1}`）に合わせて修正（旧記述は `category="変化"` / `EventContext` /
    `stats={"A": -1, "B": -1}` という誤った内容だった）
  - 実装箇所の挿入位置の説明を実際の並び（`くさわけ_*` の後、`くちばしキャノン_*` の前）に修正
  - 冒頭に仕様書への参照リンクを追加

## 実装（handlers/ / data/）
- 変更なし。`handlers/move_status.py` の `くすぐる_modify_defender_stats` は共通関数
  `modify_defender_stats` を通じて `battle.modify_stats()` を呼び出しており、正規の状態変更APIを
  使用している。`subject_spec` はデフォルトの `attacker:self`（`MoveHandler` のデフォルト）で
  `AttackContext` のロールと一致しており問題なし。`data/moves/move_ka.py` の登録内容
  （type/category/pp/accuracy/handlers）も仕様書・PP一覧（`src/jpoke/data/ps-champ-ja/moves.json`）と一致している。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列は `n/a`。くすぐるはダメージを与えない
  ランク低下技のためリーサル計算の対象外）。

## テスト
- 変更なし。既存の2件（`test_くすぐる_こうげきが最低でもぼうぎょは下がる`、
  `test_くすぐる_こうげきとぼうぎょ1段階ずつ下がる`）で基本効果と下限到達時の独立性を確認できており、
  仕様（こうげき・ぼうぎょをそれぞれ1段階下げる、片方が下限でももう片方は下がる）を十分カバーしている。
  `python -m pytest tests/ -v` は全4148件パス（1件skip）。
