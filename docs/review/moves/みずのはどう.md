# みずのはどう レビュー結果

## 仕様書
- 新規作成（`docs/wiki/moves/みずのはどう.html` と `docs/champions/move_list.txt:878` に基づき、
  基本情報・追加効果（20%こんらん）・判定フラグ・イベント優先度を記載）

## 実装計画書
- 新規作成

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ma.py` の `みずのはどう` エントリ
  （`flags={"pulse", "secondary_effect"}`、`Event.ON_DAMAGE_HIT` に
  `みずのはどう_apply_confusion_to_defender` を登録）と、`handlers/move_attack.py` の
  `みずのはどう_apply_confusion_to_defender`（`apply_confusion_to_defender(..., chance=0.2)`）は
  仕様と一致しており修正不要。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a。こんらん付与のみで HP 分布に
  影響しない追加効果のため）

## テスト
- 変更なし。`tests/moves_attack/test_move_ma.py::test_みずのはどう_こんらんが発動する` が
  既に追加効果の発動を検証しており過不足なし。
