# コットンガード レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/コットンガード.md`）
  - 一次情報（docs/wiki/moves/コットンガード.html）とdocs/champions/move_list.txtを基に、
    タイプ・分類・PP・対象・優先度・効果・判定フラグ（まもる/マジックコート/よこどり等）を記載

## 実装計画書
- 新規作成（`docs/plan/moves/コットンガード.md`）
  - ハンドラ構成・priority根拠・実装箇所・注意点を記載
  - `target="self"` を明示指定する必要がある旨とその理由（省略時のバグ）を明記

## 実装（handlers/ / data/）
- `handlers/move_status.py` の `コットンガード_modify_attacker_stats`（`def: 3`）は仕様通りで変更なし
- `data/moves/move_ka.py` の `"コットンガード"` エントリを修正
  - PP を `10`（Gen9本家値のまま）から `12`（champions基準、docs/champions/move_list.txt準拠）に修正
    - Champions は全技PPを8/12/16/20に統一しており、本家PP10は12へ繰り上げ
  - `target="self"` を追加（欠落していたバグ）
    - `target` を省略するとデフォルトの `"foe"` になり、`Move.is_blocked_by_protect` /
      `MoveExecutor.check_hit_substitute`（みがわり判定）が「相手を対象とする技」として
      誤判定し、相手の「まもる」「みがわり」で本来防がれないはずの自己バフが防がれてしまう
      不具合があった。`かたくなる`・`こうごうせい` 等と同じ target="self" パターンに合わせて修正

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行はリーサル実装・リーサルテストともに `n/a`。
  ダメージを与えない自己バフ技のためリーサル計算ハンドラは不要）

## テスト
- `tests/moves_status/test_move_ka.py` に以下を追加（`test_コスモパワー_発動前後のランク変化` の後、
  `test_こらえる_2ターン連続で使用すると2ターン目は失敗する` の前に挿入）
  - `test_コットンガード_ぼうぎょが3段階上がる`
  - `test_コットンガード_ぼうぎょが上限のとき失敗する`
  - `test_コットンガード_まもるで防がれない`
  - `test_コットンガード_マジックコートで跳ね返されない`
  - `test_コットンガード_みがわりに防がれない`
  （まもる・マジックコート・みがわりのテストは `target="self"` 修正のリグレッション防止を兼ねる）
- `python -m pytest tests/ -v` は全件パス（4473 passed, 1 skipped）。
  ログ: `.loop/test_logs/コットンガード.log`
