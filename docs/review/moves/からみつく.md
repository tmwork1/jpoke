# からみつく レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/からみつく.md`）。
- 一次情報（`docs/wiki/moves/からみつく.html`）に基づき、タイプ=ノーマル、分類=物理、威力=10、命中=100、
  PP=35、追加効果=10%の確率で相手の『すばやさ』ランクを1段階下げる、として記載した。
- 本技は `src/jpoke/data/ps-champ-ja/moves.json` に未収録（現行スカーレット・バイオレットでは使用不可）であることも明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/からみつく.md`）。
- ハンドラ構成（`ON_DAMAGE_HIT` / priority 100 / `からみつく_lower_defender_spd`）、ちからずくとの相互作用、
  リーサル計算が対象外である根拠を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/move.py` の `からみつく` エントリが、実際のゲームデータと大きく異なっていた不具合を修正した。
  - 修正前: `type="むし"`, `category="special"`, `power=55`, `pp=20`
  - 修正後: `type="ノーマル"`, `category="physical"`, `power=10`, `pp=35`
  - `accuracy=100` および `flags={"secondary_effect"}` は変更なし。
- `handlers/move_attack.py` の `からみつく_lower_defender_spd`（10%で相手の『すばやさ』を1段階下げる）は
  一次情報・仕様書と一致しており、修正なし。
- `python scripts/sort_data/sort_moves.py` を実行し、辞書の並び順を確認した（対象エントリの並び順に変化なし）。

## リーサル計算
- 対象外（n/a）。
- 修正前の `docs/progress/move.md` では「リーサル実装」列が `-`（未整理）だったが、本技の追加効果は
  相手の『すばやさ』ランクを下げるのみでHPに影響しないため、同種のスタットダウン技（`あわ` 等）と同様に
  `n/a` へ修正した。専用のリーサル計算ハンドラは実装していない。

## テスト
- `tests/moves_attack/test_move_ka.py` に `test_からみつく_すばやさ低下が発動する`（`secondary_chance=1.0` で
  相手の『すばやさ』ランクが -1 になることを確認）を追加した。
- `python scripts/sort_tests.py tests/moves_attack/test_move_ka.py` でソート、
  `python scripts/generate_test_list.py` でテスト一覧を更新、
  `python -m pytest tests/ -v` で全3284件（1件skip）がパスすることを確認した
  （`.loop/test_logs/からみつく.log` に保存）。
