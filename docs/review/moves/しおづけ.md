# しおづけ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/しおづけ.md`）。
  - 一次情報（`docs/wiki/moves/しおづけ.html`、`docs/wiki/volatiles/しおづけ_(状態変化).html`）に基づき、
    基本情報（いわ・物理・威力40・命中100・PP15・対象1体・優先度0・直接攻撃なし・専用技）、
    追加効果100%でしおづけ状態を付与すること、判定フラグ（急所・命中判定・追加効果あり／まもる可／
    おうじゃのしるし可／マジックコート不可／よこどり対象外／ゆびをふる対象外）を記載した。
  - Champions版のしおづけ状態ダメージ量（1/16、みず・はがね1/8）はSVと異なる旨も明記。

## 実装計画書
- 新規作成（`docs/plan/moves/しおづけ.md`）。
  - 既存実装（`handlers/move_attack.py` の `しおづけ_apply_volatile_to_defender`、`data/move.py` の
    `しおづけ` エントリ、`handlers/lethal.py` の `しおづけ_apply_volatile`）を仕様書と対応付けて記録した。
  - `ON_DAMAGE_HIT` の priority 根拠（`docs/spec/turn.md` 未掲載のため、同種ハンドラ `シェルブレード_lower_defender_def` に倣いデフォルト priority を採用）を明記。
  - ちからずく・りんぷん・おんみつマント・みがわりが `secondary_effect` フラグ経由で汎用的に処理される点を明記。

## 実装（handlers/ / data/）
- 変更なし。既存実装（`handlers/move_attack.py`、`data/move.py`、`handlers/volatile.py`、`data/volatile.py`）は
  仕様書・一次情報と整合していることを確認した。
  - `apply_volatile_to_defender` 経由でちからずく（威力1.3倍・追加効果無効化）、りんぷん・おんみつマント
    （追加効果無効化）が汎用的に処理される。
  - みがわり状態の相手に技が防がれた場合、`move_executor._execute_hit` の共通処理で `actual_damage <= 0` のとき
    `ON_DAMAGE_HIT` 自体が発火しないため、しおづけ状態にできない仕様が自動的に満たされている。
  - `flags={"secondary_effect"}` のみで `contact` フラグは付与されておらず、直接攻撃技ではない仕様と一致。

## リーサル計算
- 変更なし。`docs/progress/move.md` のリーサル実装列は既に `x`。
  - `handlers/lethal.py` の `しおづけ_apply_volatile`（`LethalEvent.ON_HIT`, subject=None）は
    `ctx.move_secondary` が真かつ未付与のときのみしおづけ状態を仮想付与し、ターン終了時ダメージ計算は
    揮発性状態側の `しおづけ_damage`（`LethalEvent.ON_TURN_END`, subject="defender"）に委ねる構成を確認し、
    仕様・実装と整合していることを確認した。

## テスト
- `tests/moves_attack/test_move_sa.py::test_しおづけ_ターン終了後にダメージを受ける` を修正。
  - 修正前は「しおづけ状態のポケモンはターン終了時に最大HPの1/8のダメージを受ける」という誤った docstring・
    アサーションになっていた（カビゴンはみず・はがねタイプではないため、正しくは1/16）。また技のダメージと
    しおづけダメージが合算された閾値判定になっており、しおづけダメージ自体を検証できていなかった。
  - 技命中直後のHPとターン終了後のHPを比較し、しおづけダメージ単体が `max_hp // 16` であることを検証するよう修正。
- `docs/progress/move.md` の しおづけ 行の PP 列が `16` になっていた誤りを `15`（一次情報・実装と一致）に修正。
- `python scripts/sort_tests.py tests/moves_attack/test_move_sa.py` でソート、
  `python scripts/generate_test_list.py` でテスト一覧を更新、
  `python -m pytest tests/ -v` で全3374件（1件skip）がパスすることを確認した
  （ログ: `.loop/test_logs/しおづけ.log`）。
