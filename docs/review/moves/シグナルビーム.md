# シグナルビーム レビュー結果

## 仕様書
- 存在しなかったため、docs/wiki/moves/シグナルビーム.html を参照して新規作成した
  （タイプ: むし / 分類: 特殊 / 威力: 75 / 命中: 100 / PP: 15 / 追加効果: 10%の確率で相手をこんらん状態にする）。
- docs/champions/move_list.txt に本技の記載がないため、既存実装と同じGen9本家基準の値を採用する旨を明記した。
  なおポケモンWiki上では本技は「ソード・シールド／BDSP／スカーレット・バイオレットで使用不可能なわざ」
  （現行世代で新規に習得可能なポケモンが存在しない）に分類されているが、対象範囲の判断（実装継続の可否）
  は本レビューの範囲外と判断し、既存実装（PP・威力・命中・効果が第9世代本家基準と一致）を維持した。

## 実装計画書
- 存在しなかったため、仕様書をもとに新規作成した。既存実装（`シグナルビーム_apply_confusion_to_defender`）が
  仕様と一致していることを確認した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_sa.py` の `シグナルビーム` エントリ（タイプ・分類・威力・命中・PP・flags・ハンドラ）は
  仕様と一致しており、修正なし。
- `src/jpoke/handlers/move_attack.py` の `シグナルビーム_apply_confusion_to_defender`
  （`apply_confusion_to_defender` を用いて10%の確率でこんらんを付与）も仕様と一致しており、修正なし。
- `handlers/item.py` の `_INNATE_FLINCH_MOVES`（おうじゃのしるし等の重複除外リスト）に
  シグナルビームが含まれていないことを確認した（本技はひるみの追加効果を持たないため正しい）。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列は `n/a`）。こんらん付与のみの追加効果であり、
  既存の同種技（みずのはどう・したでなめる 等）と同様にリーサル計算の対象外。

## テスト
- `tests/moves_attack/test_move_sa.py` に既存の `test_シグナルビーム_こんらんが発動する` が実装と一致しており、
  修正不要。
- `python -m pytest tests/ -v` を実行し、3815 passed, 1 skipped（既存の無関係なskip）で全テスト成功を確認した。
