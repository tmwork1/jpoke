# どくばり レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/どくばり.md`）。
  - docs/wiki/moves/どくばり.html を一次情報として、タイプ:どく、分類:物理、威力15、命中100、
    PP35、対象:相手1体、優先度0、接触なし、追加効果30%『どく』を記載。
  - 既存の `どくどくのキバ.md` の構成（判定フラグ・Zワザ/ダイマックス威力・参照）に揃えた。

## 実装計画書
- 新規作成（`docs/plan/moves/どくばり.md`）。
  - `src/jpoke/data/move.py` の `MoveData` 定義と `src/jpoke/handlers/move_attack.py` の
    `どくばり_apply_poison_to_defender` が仕様と一致していることを確認し、修正不要である旨を記載。
  - priorityは既存の同種ハンドラ（どくどくのキバ・どくづき・どくばりセンボン等）と同じ
    `ON_DAMAGE_HIT` / デフォルトpriorityに従うことを根拠として明記。

## 実装（handlers/ / data/）
- 変更なし。既存実装は仕様と完全に一致していることを確認した。
  - `data/move.py`: type="どく", category="physical", pp=35, power=15, accuracy=100,
    flags={"secondary_effect"} — wiki記載値と一致。
  - `handlers/move_attack.py`: `apply_ailment_to_defender(battle, ctx, value, ailment="どく", chance=0.3)`
    — 30%の追加効果確率が仕様通り。汎用ヘルパー経由でちからずく等の相互作用も正しく処理される。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行「リーサル実装」列は `n/a`）。
  どく状態異常の付与はターン経過依存の追加効果であり、リーサル計算対象外の方針に合致。

## テスト
- 変更なし。既存テスト `test_どくばり_どくが発動する`
  （`tests/moves_attack/test_move_ta.py`）が `secondary_chance=1.0` で30%付与を確認しており、
  仕様・実装と整合していることを確認した。追加・修正は不要と判断。
- `python -m pytest tests/ -v` で全件（3484件）パス、1件skipを確認した
  （ログ: `.loop/test_logs/どくばり.log`）。
  ※ 実行中断続的に無関係な既存テスト（`だっしゅつパック`関連、`てんねん`関連、`リフレッシュ`関連等）
  が失敗する事象を観測したが、`Battle` が `seed` 未指定時に `int(time.time())` を用いるため、
  同一秒内に生成された無関係なテスト同士が同一乱数列を共有し偶発的に衝突する既存の仕様上の
  副作用であり、どくばりの実装・変更とは無関係であることを確認済み（単体実行では常にパスする）。
