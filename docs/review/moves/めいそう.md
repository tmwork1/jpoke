# めいそう レビュー結果

## 仕様書
- 存在しなかったため新規作成した（`docs/spec/moves/めいそう.md`）。
- 一次情報（docs/wiki/moves/めいそう.html）と `src/jpoke/data/ps-champ-ja/moves.json` を照合し、
  タイプ: エスパー、分類: 変化、威力: -、命中: -、PP: 20、対象: 自分、優先度: 0、直接攻撃: いいえ、
  効果: 自分の『とくこう』『とくぼう』ランクをそれぞれ1段階ずつ上げる、を記載。
  判定フラグ（急所対象外・必中・追加効果判定なし・まもる無効・マジックコート対象外・よこどり対象・
  オウムがえし対象外）は既存の同型技（こうそくいどう）の記載形式に合わせて明記した。

## 実装計画書
- 既存の `docs/plan/moves/めいそう.md` を仕様書と実装コードに合わせて修正した。
  - 出典を存在しない `docs/spec/moves/status_move.md` から新規作成した `docs/spec/moves/めいそう.md` に修正。
  - サンプルコードの能力キーを誤記の `"C"/"D"` から実装と一致する `"spa"/"spd"` に修正。
  - サンプルコードの `ctx: EventContext` を実装と一致する `ctx: AttackContext` に修正。
  - 実装ファイルの記述を、存在しない `src/jpoke/data/move.py` から実際の配置先
    `src/jpoke/data/moves/move_ma.py` に修正し、`category="変化"`（誤記）を実装と一致する
    `category="status"` に修正。
  - 挿入位置の記述を実際のコード上の位置（`みをけずる_can_apply` の後、`メロメロ_apply` の前）に修正。

## 実装（handlers/ / data/）
- 変更なし。`handlers/move_status.py` の `めいそう_modify_attacker_stats` は
  `modify_attacker_stats(battle, ctx, value, stats={"spa": 1, "spd": 1})` を呼ぶのみで、
  `コスモパワー_modify_attacker_stats` 等と同一パターンであり、仕様と一致している。
  `data/moves/move_ma.py` の登録内容（type/category/pp/handlers/priority省略=100/
  subject_spec省略=attacker:self）も仕様・計画書と整合している。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`。攻撃技ではないため）。

## テスト
- 変更なし。`tests/moves_status/test_move_ma.py` の
  `test_めいそう_発動前後のランク変化` が通常・片方上限・両方上限・両方上限まで1段階・
  最低ランクからの上昇まで parametrize で網羅しており、追加の必要なし。
