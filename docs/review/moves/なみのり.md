# なみのり レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/なみのり.md`）。
  - 一次情報（`docs/wiki/moves/なみのり.html`）と `src/jpoke/data/ps-champ-ja/moves.json` を突合し、
    基本情報（みず・特殊・威力90・命中100・PP16・非接触）、判定フラグ、
    『ダイビング』状態の相手への威力2倍効果を記載した。
  - PPは本家15だがChampions仕様（8/12/16/20の4段階統一）により16が正しいことを
    `src/jpoke/data/ps-champ-ja/moves.json`（622行目）で確認した。

## 実装計画書
- 新規作成（`docs/plan/moves/なみのり.md`）。
  - `じしん` の計画書（同種の「追加効果なしの単純攻撃技＋相手側揮発状態ハンドラで威力2倍を処理する」
    パターン）を参考に作成した。
  - なみのり自体は `handlers={}` のみで、ダイビング状態への威力2倍は
    `handlers/volatile.py` の `ダイビング_boost_power`（`ダイビング` volatile側に登録済み）で
    処理されることを明記した。

## 実装（handlers/ / data/）
- 変更なし。
  - `data/move.py` の `なみのり` エントリ（type=みず, category=special, pp=16, power=90,
    accuracy=100, handlers={}）は仕様と一致していることを確認した。
  - `handlers/volatile.py` の `ダイビング_boost_power` / `ダイビング_can_hit_hidden_target` /
    `HIDDEN_MOVE_ALLOWED_MOVES["ダイビング"] = ["なみのり", "うずしお"]` により
    ダイビング状態の相手への命中・威力2倍が既に正しく実装されていることを確認した
    （`data/volatile.py` の `ダイビング` エントリ側に登録済み）。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装・リーサルテスト列は `n/a`）。
  追加効果を持たない単純な攻撃技のため、リーサル計算での固有ハンドラは不要。

## テスト
- 変更なし。
  - `tests/moves_attack/test_move_na.py` の `test_なみのり_相手にダメージを与える` で
    基本ダメージ確認済み。
  - ダイビング状態への威力2倍・命中確定は `tests/volatiles/test_volatile_satana.py` の
    `test_ダイビング_その他技は威力2倍にならない` / `test_ダイビング_なみのりうずしおは威力2倍`
    で既にカバーされていることを確認した。
  - `python -m pytest tests/ -v` を実行し、3528 passed, 1 failed（`tests/moves_attack/test_move_na.py::test_にぎりつぶす_相手HP半分のとき威力59`）, 1 skipped。
    この失敗は「にぎりつぶす」（別技、`docs/progress/move.md` で未レビュー `-` 扱い）の
    威力計算に関する既存の不具合であり、なみのりの実装・変更とは無関係のため本タスクでは
    修正対象外とした。
