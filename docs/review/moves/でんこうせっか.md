# でんこうせっか レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/でんこうせっか.md`）。一次情報（ポケモンWiki）と照合し、
  タイプ: ノーマル / 分類: 物理 / 威力: 40 / 命中: 100 / PP: 30 / 優先度: +1 / 接触あり、
  追加効果なしの先制攻撃技であることを確認した。

## 実装計画書
- 新規作成（`docs/plan/moves/でんこうせっか.md`）。既存の同種先制技（しんそく）の計画書を参考に、
  専用ハンドラ不要である旨と priority の根拠を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `でんこうせっか` エントリ
  （`type="ノーマル"`, `category="physical"`, `pp=30`, `power=40`, `accuracy=100`,
  `priority=1`, `flags={"contact"}`, `handlers={}`）は仕様書と完全に一致しており、修正不要だった。

## リーサル計算
- 対象外（`docs/progress/move.md` の該当行は「リーサル実装」「リーサルテスト」ともに `n/a`。
  追加効果を持たないため専用ハンドラ不要で、既存の対象外判定と一致）。

## テスト
- 変更なし。`tests/moves_attack/test_move_ta.py` の
  `test_でんこうせっか_相手にダメージを与える` が優先度+1の物理技として
  ダメージを与える挙動を確認しており、`tests/moves_attack/test_move_ha.py` /
  `test_move_sa.py` / `test_move_ka.py` でも他技（ふいうち等）の優先度検証に
  でんこうせっかが利用されている。いずれも実装内容と整合しており追加・修正は不要と判断した。
  `docs/progress/move.md` の該当行は PP が誤って `20` と記載され、効果列が未記入（`？`）
  だったため、実際の値（PP 30・追加効果なし）に修正し、仕様書・計画書・レビュー列を
  `x` / `x` / `✓` に更新した。
