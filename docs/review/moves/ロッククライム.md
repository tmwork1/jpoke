# ロッククライム レビュー結果

## 仕様書
- 新規作成（docs/spec/moves/ロッククライム.md）。ポケモンWiki（docs/wiki/moves/ロッククライム.html）を
  一次情報として、タイプ・分類・威力90・命中85・PP20・追加効果（20%こんらん）・各種判定フラグを記載。
  `src/jpoke/data/ps-champ-ja/moves.json` に本技の掲載がない（SVで使用不可の技）ため、威力・命中・PPは
  ポケモンWiki記載の値（第四世代以降変更なし）を採用する旨を明記した。

## 実装計画書
- 新規作成（docs/plan/moves/ロッククライム.md）。既存実装（`data/moves/move_ra.py` の
  `"ロッククライム"` エントリ、`handlers/move_attack.py` の `ロッククライム_apply_confusion_to_defender`）
  が仕様と一致していることを確認し、差分なしとして記録した。

## 実装（handlers/ / data/）
- 変更なし。`data/moves/move_ra.py` の type=ノーマル / category=physical / pp=20 / power=90 /
  accuracy=85 / flags={contact, secondary_effect} と、`handlers/move_attack.py` の
  `ロッククライム_apply_confusion_to_defender`（`apply_confusion_to_defender(chance=0.2)` 呼び出し）は
  仕様書・計画書と完全に一致しており、修正は不要だった。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列は `n/a`）。こんらん付与のみでHP分布に
  影響しない追加効果のため、リーサル計算ハンドラは不要。

## テスト
- 変更なし。`tests/moves_attack/test_move_yarawa.py` の
  `test_ロッククライム_こんらんが発動する` が既に存在し、`secondary_chance=1.0` でこんらん付与を
  確認する内容として仕様と一致している。`python -m pytest tests/ -v` で全4033件パス（1件skip）を確認。
