# メテオビーム レビュー結果

## 仕様書
- 既存の `docs/spec/moves/メテオビーム.md`（ポケモンWikiの内容をそのまま転記したもの）は
  一次情報（`docs/wiki/moves/メテオビーム.html`）と照合し、タイプ・分類・威力・命中率・範囲・
  優先度・直接攻撃の有無・判定（急所/命中判定/追加効果/まもる/おうじゃのしるし/マジックコート）
  すべて一致していることを確認した。
- 冒頭に「Champions固有の注意点」節を追加し、PP は `docs/champions/move_list.txt` の値である
  12 が正であること（本家Gen9では10だが、Championsでは全技PPが8/12/16/20の4段階に統一されて
  いるため）を明記した（表内のPP=10はポケモンWikiの本家仕様の転記のままとし、注記で補足する
  方式にした）。

## 実装計画書
- `docs/plan/moves/メテオビーム.md` の仕様要約・コード例中の PP を 10 → 12 に修正した
  （champions/move_list.txt の値に合わせ、根拠のコメントも追記）。
- その他の設計内容（ON_MOVE_CHARGE の priority=50 でとくこう上昇を先行させる根拠、
  パワフルハーブとの相互作用、揮発状態の構造）は実装と一致しており変更なし。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ma.py` の `"メテオビーム"` MoveData の `pp=10` を `pp=12` に修正した
  （champions/move_list.txt の値との不一致を検出。実装漏れのバグ）。
- そのほかの実装（`handlers/move_attack.py` の `メテオビーム_boost_spa` / `メテオビーム_charge`、
  `handlers/volatile.py` の `メテオビーム_remove_volatile`、`data/volatile.py` の `"メテオビーム"`
  VolatileData、`types/move.py` / `types/volatile.py` への Literal 追加）は仕様・計画書と一致して
  おり、`エレクトロビーム`（同系統の溜め技）の実装パターンとも整合していることを確認した。
- `docs/spec/turn.md` の `Event.ON_MOVE_CHARGE` セクションで「100 | メテオビーム: とくこう上昇」
  となっていた記載を、実際の実装（priority=50、パワフルハーブより先に実行するための調整）に
  合わせて「50 | メテオビーム: とくこう上昇（パワフルハーブ〈priority=100〉より確実に先に実行
  するため）」に修正し、Priority昇順の並びに合わせて位置も移動した。

## リーサル計算
- 変更なし。`handlers/lethal.py` の `メテオビーム_boost_spa`（`ctx.move_secondary` でとくこう+1を
  適用）は `data/moves/move_ma.py` の `lethal_handlers={LethalEvent.ON_BEFORE_MOVE: LethalHandler(...)}`
  として登録されており、同系統の `エレクトロビーム_boost_spa` と同一パターンで実装されている
  ことを確認した。誤り・欠落はなし。

## テスト
- `tests/moves_attack/test_move_ma.py` に既存の3テスト
  （1ターン目のとくこう上昇／2ターンで攻撃・揮発状態解除／パワフルハーブ使用時1ターン攻撃）を
  確認し、実装と一致していることを確認した。
- 新規追加: `test_メテオビーム_ちからずくでも威力は上がらずとくこうは上昇する`
  （ちからずく所持時も追加効果扱いされないため威力補正がなく、とくこう上昇は通常通り発動する
  ことを確認）。
- `tests/test_lethal.py` に `docs/progress/move.md` でリーサルテスト済み（x）と記載されていたが
  実体がなかったため、`test_メテオビーム_とくこうアップ_secondary有り` /
  `test_メテオビーム_とくこうアップ_secondary無し` を `エレクトロビーム` の既存テストと同じ
  パターンで新規追加した。
- `python -m pytest tests/ -v` を実行し、4028 passed, 1 skipped で全件成功を確認した
  （ログ: `.loop/test_logs/メテオビーム.log`）。
