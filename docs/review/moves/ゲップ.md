# ゲップ レビュー結果

## 仕様書
- 一次情報（docs/wiki/moves/ゲップ.html）と照合し、以下を修正・整理して再構成した。
  - 既存の仕様書は一次情報テーブルの生の書き写し（進化・コンテスト・アニメ登場等の雑多な情報を含む）で、
    Champions固有の仕様が欠落していたため、他の技（かげぬい等）と同じ「基本情報／効果／判定フラグ／
    イベント」形式に書き直した。
  - PRの目玉として、Championsにおける固有仕様を追記した: 本家シリーズでは使用条件を満たさない場合
    ゲップの選択自体がキャンセルされる（わるあがき扱い）が、Championsでは選択自体は可能で、
    自分の行動時点までにきのみを食べれば成功する（一次情報の「技の仕様」節に記載があるが旧仕様書には
    転記されていなかった）。
  - まねっこ・ねごと等どの「技を選ぶ／コピーする」効果からゲップが除外されるかを整理し、本プロジェクトで
    実装済みのまねっこ（Copycat）との相互作用（`non_copycat`フラグ）を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/ゲップ.md`）。実装は既に存在していたため、既存実装と仕様の差分
  （PP・`non_copycat`フラグ欠落）を明記する形で作成した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ka.py` の `"ゲップ"` エントリを修正:
  - PPを `10` → `12` に修正（`docs/champions/move_list.txt` 準拠。本家Gen9基準の値が移行漏れとなっていた）。
  - `flags` に `non_copycat` を追加（一次情報でまねっこにより選ばれないと明記されているが未実装だった）。
- `src/jpoke/handlers/move_attack.py` の `ゲップ_check_ate_berry` は仕様・`docs/spec/turn.md`
  （`Event.ON_TRY_MOVE_1` priority=30）と整合しており、変更なし。
- `ate_berry` の管理（`model/pokemon.py` の `memory["battle"]` スコープ、
  `item_manager.consume_item` / `force_trigger_berry` での更新）は交代・バトンタッチ・へんしん・
  むしくい/ついばむによる強奪など一次情報の各ケースと突き合わせて確認し、いずれも仕様通りだったため
  変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a）。

## テスト
- `tests/moves_status/test_move_ma.py` に `test_まねっこ_ゲップはコピーできない` を追加し、
  `non_copycat` フラグ追加の回帰テストとした（既存の `test_まねっこ_カウンターはコピーできない` /
  `test_まねっこ_きょじゅうだんはコピーできない` と同じパターン）。
- `tests/moves_attack/test_move_ka.py` の既存ゲップテスト（きのみ未食での失敗・きのみ消費後の発動・
  やきつくすで焼かれた場合・交代後も使えること）は実装・仕様と整合しており、変更なし。
- `python -m pytest tests/ -v` で全3757件成功（1件skip）を確認。
