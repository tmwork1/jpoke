# ブリザードランス レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ブリザードランス.md`）。
  - `docs/wiki/moves/ブリザードランス.html` の一次情報（タイプ: こおり、分類: 物理、
    威力: 130（第八世代）→120（第九世代）、命中100、PP5（Gen9本家）、対象: 相手全員、
    直接攻撃なし、追加効果なし、まもるで防げる、おうじゃのしるし対応、マジックコート対象外）
    を反映。
  - 急所判定について、ストーンエッジ等の「急所に当たりやすい（急所ランク+1）」の記載が
    一次情報に存在しないことを確認し、ブリザードランスには急所ランク補正がない旨を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/ブリザードランス.md`）。
  - 追加効果を持たない攻撃技のためハンドラ不要とし、`MoveData` のフィールド設定のみで
    完結する方針を記載。
  - PPをChampions基準の8に統一する根拠（`src/jpoke/data/ps-champ-ja/moves.json` には本技の記載が
    ないが、同じ本家PP5の`アストラルビット`・`でんじほう`・`はかいこうせん`・`じばく`・
    `だいばくはつ`等が軒並み8へ変換されていることから同様に扱う）を明記。
  - `critical_rank`は設定しない（急所ランク補正なしのため）ことを明記。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `ブリザードランス` エントリを修正。
  - `pp=5` → `pp=8` に修正（Gen9本家PPの移行漏れ。Championsでは全技PPが8/12/16/20の
    4段階に統一されており、本家PP5の技は8に変換される。同一パターンの`アストラルビット`
    は既に`pp=8`で正しく実装されていたため、本技の`pp=5`は移行漏れのバグと判断）。
  - `critical_rank` は既に未設定（デフォルト0）であり、急所ランク補正を持たない仕様と
    一致していることを確認した。誤って`critical_rank=1`等が付与されていないかを重点的に
    確認したが、既存実装は正しかった。
  - `handlers={}`（追加効果なし）は仕様と一致しており変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`。追加効果を持たない通常攻撃技
  のため専用のリーサルハンドラは不要）。

## テスト
- `tests/moves_attack/test_move_ha.py` に以下を追加。
  - `test_ブリザードランス_Champions基準のPPを持つ`: タイプ・分類・威力・命中・PP・
    急所ランク（0）を検証。
  - `test_ブリザードランス_タイプ分類威力命中が正しく反映されダメージのみ与える`:
    バドレックス（はくばじょうのすがた）が使用してダメージのみを与え、ランク変化・
    状態異常が発生しないことを検証。
  - `python -m pytest tests/ -v` で全3975件（1件skip）がパスすることを確認
    （`.loop/test_logs/ブリザードランス.log`）。
