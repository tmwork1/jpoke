# テラバースト レビュー結果

## 仕様書
- 変更なし（`docs/spec/moves/テラバースト.md` は既存。一次情報（`docs/wiki/moves/テラバースト.html`）と
  照合したが、タイプ変化・分類変化・ステラ威力補正・自己ランクダウン・プレッシャーPP消費・
  はやてのつばさ非発動など、内容に誤り・欠落は無かった）

## 実装計画書
- 新規作成（`docs/plan/moves/テラバースト.md` が存在しなかったため、既存実装
  （`handlers/move_attack.py` / `data/moves/move_ta.py`）と仕様書を突き合わせて作成した）
- ハンドラ構成表・Priority根拠（`ON_MODIFY_MOVE_TYPE` / `ON_MODIFY_MOVE_CATEGORY` /
  `ON_CALC_POWER_MODIFIER` は turn.md 未掲載のため既存の同種実装＝フォトンゲイザーを参照して
  デフォルト優先度を採用、`ON_HIT` は turn.md に priority=10 の記載があるが既存の
  オーバーヒート・リーフストームと一貫性を取りデフォルト優先度を維持）を明記した
- スキン系特性・はやてのつばさとの相互作用について、実際に動作確認（アドホックスクリプト）した
  上でコード変更が不要であることと根拠をエッジケース節に記載した

## 実装（handlers/ / data/）
- `handlers/move_attack.py` の `テラバースト_modify_move_type` /
  `テラバースト_modify_move_category` / `テラバースト_stellar_power` /
  `テラバースト_stellar_stat_drop` は仕様書と一致しており修正なし
- `data/moves/move_ta.py` の `MoveData`（タイプ・分類・威力80・命中100・PP10）も仕様と一致
- PP=10 は `docs/champions/move_list.txt` に該当行が存在しない（タイプ・分類・威力が可変の技は
  一次情報のテーブルから対象外になっていると考えられ、テラクラスターも同様に対象外）ため、
  Gen9基準の値をそのまま維持（変更なし）
- `data/moves/move_ta.py` に `lethal_handlers`（`LethalEvent.ON_HIT`）を新規追加し、
  `l.テラバースト_lower_attacker_atk_spa` を登録した

## リーサル計算
- 新規実装
  - `handlers/lethal.py` に `テラバースト_lower_attacker_atk_spa` を追加
    （ステラタイプにテラスタルして命中した場合のみ、こうげき・とくこうを1段階ずつ下げる）
  - ダメージ計算自体（タイプ・分類・ステラ威力補正）は `battle.calc_damages()` が通常の
    `ON_MODIFY_MOVE_TYPE` 等のハンドラをそのまま経由するため専用実装は不要と判断した
  - `data/moves/move_ta.py` に `lethal_handlers={LethalEvent.ON_HIT: LethalHandler(...)}` を追加

## テスト
- 追加
  - `tests/test_lethal.py`
    - `test_テラバースト_ステラでこうげきとくこうダウン`
    - `test_テラバースト_ステラ以外はランクが下がらない`
  - `tests/moves_attack/test_move_ta.py`
    - `test_テラバースト_テラスタイプがノーマルでもスキン特性の対象にならない`
      （フェアリースキン所持ポケモンがノーマルタイプにテラスタルした場合、
      技タイプがフェアリーではなくノーマルのままになることを確認する回帰テスト）
- 既存の3テスト（ステラ補正、テラスタイプへのタイプ変化、分類判定）は実装と一致しており修正なし
- `python -m pytest tests/ -v` で3874件パス・1件skip（テラバースト無関係の既存skip）を確認し、
  `.loop/test_logs/テラバースト.log` に保存した
