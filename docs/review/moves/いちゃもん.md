# いちゃもん レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/いちゃもん.md`）
  - 基本情報（あく/変化/PP16/命中100/相手1体/優先度0/直接攻撃×/まもる可）
  - 効果（相手を「いちゃもん」揮発性状態にする。`ctx.defender.executed_move` を禁止技として記録）
  - 対象が未行動でも付与自体は成功する旨を明記
  - まもる・マジックコートは既存の汎用処理で対応済みであることを明記
  - ダイマックス関連はプロジェクト対象外（Gen9基準のためダイマックス機構自体が存在しない）

## 実装計画書
- 新規作成（`docs/plan/moves/いちゃもん.md`）
  - `いちゃもん_apply` の実装方針（`かなしばり_apply` と同じパターンで `ctx.defender.executed_move` から `move_name` を取得）
  - priority根拠（`ON_STATUS_HIT` は turn.md に記載なし、既存の同種ハンドラに合わせてデフォルト priority=100）
  - PP修正の根拠（`src/jpoke/data/ps-champ-ja/moves.json` 準拠で16へ修正）

## 実装（handlers/ / data/）
- **バグ修正**: `handlers/move_status.py` の `いちゃもん_apply` が `apply_volatile_to_defender` 呼び出し時に
  `move_name` を渡していなかったため、いちゃもん状態の `move_name` が常に空文字列 `""` になり、
  技「いちゃもん」を使用しても相手の技が一切禁止されない状態になっていた（揮発状態そのもの
  `handlers/volatile.py` の `いちゃもん_modify_command_options` は正しく実装されていたが、
  技側から正しい `move_name` が渡されていなかった）。
  `かなしばり_apply` と同様に `ctx.defender.executed_move` を参照して `move_name` を渡すよう修正。
- **PP修正**: `data/moves/move_a.py` の `いちゃもん` の `pp` が `15`（Gen9本家基準の移行漏れ）に
  なっていたため、`src/jpoke/data/ps-champ-ja/moves.json` 準拠の `16` に修正。
  `docs/progress/move.md` のPP列はもともと `16` で正しく、実装側のみが誤っていた。

## リーサル計算
- 対象外（`docs/progress/move.md` の いちゃもん 行はリーサル実装・リーサルテストともに `n/a`）

## テスト
- `tests/moves_status/test_move_a.py` に以下を追加:
  - `test_いちゃもん_すでにいちゃもん状態の相手には失敗する`
  - `test_いちゃもん_使用で相手にいちゃもん状態が付与される`
  - `test_いちゃもん_直前に使用した技が選択肢から除外される`
  - `test_いちゃもん_未行動の相手にも成功する`
  - いずれも `move_name` が正しく記録される・コマンド選択肢から正しく除外されることを確認する
    （修正前のバグでは `test_いちゃもん_直前に使用した技が選択肢から除外される` が失敗していたことを確認済み）
- `python -m pytest tests/ -v` は 4076 passed, 1 skipped で全件成功（ログ: `.loop/test_logs/いちゃもん.log`）
