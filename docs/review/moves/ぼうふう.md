# ぼうふう レビュー結果

## 仕様書
- 存在しなかったため新規作成した（`docs/spec/moves/ぼうふう.md`）。
- ポケモンWiki（`docs/wiki/moves/ぼうふう.html`）と `src/jpoke/data/ps-champ-ja/moves.json` を突き合わせ、
  タイプ・分類・威力・命中率・PP・追加効果（30%こんらん）・天候による命中率補正（あめ必中／にほんばれ50）・
  そらをとぶ状態への命中・風の技フラグ・判定フラグ・リーサル対象外の根拠をまとめた。
- PPはポケモンWiki（10）と `src/jpoke/data/ps-champ-ja/moves.json`（12）で差異があったため、`ふぶき`・`なみのり` の
  既存仕様書と同様にChampions側の値（PP4段階統一〈8/12/16/20〉）を正として採用した。

## 実装計画書
- 存在しなかったため新規作成した（`docs/plan/moves/ぼうふう.md`）。
- `かみなり`・`ふぶき` の計画書パターンに倣い、ハンドラ構成・priority根拠・エッジケース・テスト方針を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ha.py` の `ぼうふう` エントリで `pp=10`（一次情報＝ポケモンWikiの値）に
  なっていた誤りを検出し、`src/jpoke/data/ps-champ-ja/moves.json` 準拠の `pp=12` に修正した。
- `handlers/move_attack.py` の `ぼうふう_accuracy` / `ぼうふう_apply_confusion_to_defender` は
  `かみなり_accuracy` / `ふぶき_accuracy` と同一パターンで実装済みであり、修正不要だった。
- `handlers/volatile.py` の `HIDDEN_MOVE_ALLOWED_MOVES["そらをとぶ"]` に `ぼうふう` が登録済みであることを確認した。
- `flags={"wind", "secondary_effect"}` が設定済みで、かぜのり／ふうりょくでんきの対象になっていることを確認した。
- `python scripts/sort_data/sort_moves.py` を実行したが差分は発生しなかった（既に五十音順）。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装・リーサルテスト列は n/a）。
  固定威力・追加効果（ダメージ量に影響しない状態異常付与のみ）の通常攻撃技のため、専用ハンドラは不要。

## テスト
- 既存テスト（`test_ぼうふう_こんらんが発動する`、`tests/abilities/test_ability_ka.py` の
  かぜのり関連テスト、`tests/volatiles/test_volatile_satana.py` の
  `test_そらをとぶ_ぼうふうが命中する`）を確認し、いずれも実装と整合していた。
- 天候による命中率補正のテストが未整備だったため、`かみなり` の既存テストパターンに倣い
  `tests/moves_attack/test_move_ha.py` に以下を追加した。
  - `test_ぼうふう_あめ中は必中になる`
  - `test_ぼうふう_にほんばれ中は命中率が50になる`
- `python scripts/sort_tests.py tests/moves_attack/test_move_ha.py` でソート、
  `python scripts/generate_test_list.py` でテスト一覧を更新、
  `python -m pytest tests/ -v` で全件パス（3597 passed, 1 skipped）を確認した。
  結果は `.loop/test_logs/ぼうふう.log` に保存した。
