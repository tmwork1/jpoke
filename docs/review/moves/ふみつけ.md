# ふみつけ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ふみつけ.md`）。
  `docs/wiki/moves/ふみつけ.html` を一次情報として、タイプ・分類・威力65・命中100・PP20・
  接触・優先度0・追加効果（30%ひるみ）・ちいさくなる状態の相手への威力2倍/必中（`minimize`フラグ）
  を記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/ふみつけ.md`）。
  既存実装（`ふみつけ_apply_flinch` / `apply_volatile_to_defender`）が仕様と一致することを確認し、
  ちいさくなるとの相互作用は `handlers/volatile.py` の汎用ハンドラ
  （`ちいさくなる_boost_power` / `ちいさくなる_guaranteed_hit`）が `minimize` フラグ経由で
  処理する旨を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ha.py` の `"ふみつけ"` エントリ
  （`type="ノーマル", category="physical", pp=20, power=65, accuracy=100,
  flags={"minimize", "contact", "secondary_effect"}`）および
  `src/jpoke/handlers/move_attack.py` の `ふみつけ_apply_flinch`
  （`apply_volatile_to_defender(..., volatile="ひるみ", chance=0.3)`）は仕様書・計画書と一致しており
  修正不要と判断した。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`。ひるみ付与のみでHP分布に
  影響しない追加効果のため）。

## テスト
- 変更なし。`tests/moves_attack/test_move_ha.py` の
  `test_ふみつけ_ひるみが発動する` で30%ひるみ付与を確認済み。
  `tests/volatiles/test_volatile_satana.py` の `minimize_enhance_moves` パラメータ化テスト
  （`test_ちいさくなる_minimize技が必中化して威力2倍`）に `ふみつけ` が含まれており、
  ちいさくなる状態への威力2倍・必中化も既にカバーされていることを確認した。
  `python -m pytest tests/ -v` で3579件パス・1件skipを確認（`.loop/test_logs/ふみつけ.log`）。
