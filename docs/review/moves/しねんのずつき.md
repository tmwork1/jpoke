# しねんのずつき レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/しねんのずつき.md`）。
  一次情報（`docs/wiki/moves/しねんのずつき.html`）と `src/jpoke/data/ps-champ-ja/moves.json` を照合し、
  タイプ=エスパー、分類=物理、威力=80、命中=90、PP=16（Champions基準）、
  追加効果=20%でひるみ付与、`contact`/`secondary_effect` フラグを明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/しねんのずつき.md`）。
  既存実装とのPP差分（Champions基準の16でなく本家基準の15になっていた点）を明記し、
  ハンドラ構成・イベント/優先度・リーサル対象外の根拠を記載した。

## 実装（handlers/ / data/）
- `data/move.py` の `"しねんのずつき"` エントリの `pp` を `15` → `16` に修正
  （`src/jpoke/data/ps-champ-ja/moves.json` 準拠）。
- `handlers/move_attack.py` の `しねんのずつき_apply_flinch`（`apply_volatile_to_defender` を使用した
  20%ひるみ付与）は仕様通りで変更なし。
- `flags={"contact", "secondary_effect"}` は仕様と一致しており変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`。ひるみ付与のみでHP分布に影響しない
  追加効果のため）。

## テスト
- `tests/moves_status/test_move_sa.py` に誤って重複登録されていた
  `test_しねんのずつき_ひるみが発動する`（しねんのずつきは物理攻撃技のため moves_status には該当しない）
  を削除。`tests/moves_attack/test_move_sa.py` 側の同名テスト（`secondary_chance=1.0` を使用する
  正規の実装）はそのまま維持。
- `python scripts/sort_tests.py` でソート後、`python scripts/generate_test_list.py` で
  `docs/tests/` を更新。
- `python -m pytest tests/ -v` で全件パス（3384 passed, 1 skipped）を確認
  （`.loop/test_logs/しねんのずつき.log` に保存）。
