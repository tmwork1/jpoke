# いわおとし レビュー結果

## 仕様書
- 新規作成（docs/spec/moves/いわおとし.md）。docs/wiki/moves/いわおとし.html を一次情報として、
  タイプ・分類・威力50・命中90（第一世代のみ65）・PP15・対象1体・優先度0・非接触・追加効果なしを記載。

## 実装計画書
- 新規作成（docs/plan/moves/いわおとし.md）。追加効果を持たないため専用ハンドラは不要であることを明記し、
  既存の `data/move.py` エントリが仕様と一致していることを確認する内容とした。

## 実装（handlers/ / data/）
- 変更なし。既存の `data/move.py` の `"いわおとし"` エントリ（type="いわ", category="physical",
  pp=15, power=50, accuracy=90, handlers={}）は仕様と一致しており、非接触（flags未指定）・
  優先度0（デフォルト）・通常急所判定（デフォルト）もすべて仕様通りだった。

## リーサル計算
- 対象外（docs/progress/move.md でリーサル実装列が n/a）。

## テスト
- 追加。tests/moves_attack/test_move_a.py に
  `test_いわおとし_相手にダメージを与える`（追加効果なしの物理いわ技として、
  ダメージが発生することを確認する基本テスト）を新規追加。
- scripts/sort_tests.py tests/moves_attack/test_move_a.py で並び替え、
  scripts/generate_test_list.py でテスト一覧を更新、
  python -m pytest tests/ -v で全3235件（1件skip）が成功することを確認
  （.loop/test_logs/いわおとし.log に保存）。
