# いっちょうあがり レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/いっちょうあがり.md`）
- 一次情報（`docs/wiki/moves/いっちょうあがり.html`）とちからずく仕様書（`docs/spec/abilities/ちからずく.md`）を照合し、以下を明記した
  - 基本情報（ドラゴン/物理/威力80/命中100/PP10/対象1体/優先度0/接触なし、ヘイラッシャ専用技）
  - ヘイラッシャが特性しれいとうでシャリタツを飲み込んでいる場合の自分能力アップ効果は、しれいとうがダブル専用
    （`docs/progress/ability.md` で全列「ダブル専用」）のため、シングル専用の本プロジェクトでは実装対象外
  - ちからずくとの相互作用：本技は「追加効果は無いがちからずく対象」フラグを持ち、シングルでもちからずく所持者が
    使用すると威力が1.3倍になる（エレクトロビームと異なりChampinsでの除外記載が無いため対象）

## 実装計画書
- 新規作成（`docs/plan/moves/いっちょうあがり.md`）
- `flags={"secondary_effect"}` の追加のみで完結する方針を明記し、しれいとう連携部分は対象外とした

## 実装（handlers/ / data/）
- `src/jpoke/data/move.py` の `"いっちょうあがり"` エントリに `flags={"secondary_effect"}` を追加
  （修正前は `handlers={}` のみでフラグ未設定だったため、ちからずく所持者が使用しても威力が上がらない不具合があった）
- `handlers/` への追加コードなし（専用ハンドラ不要）
- `python scripts/sort_data/sort_moves.py` を実行済み

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a）

## テスト
- `tests/abilities/test_ability_ta.py` に `test_ちからずく_いっちょうあがりは追加効果がなくても威力が1_3倍になる` を追加
  - ちからずく所持ポケモンがいっちょうあがりを使用した際に `power_modifier == 5325`（1.3倍）となり、
    かつ能力ランクは変化しない（しれいとう連携は未実装のため）ことを確認
  - `python scripts/sort_tests.py tests/abilities/test_ability_ta.py` を実行済み
  - `tests/moves_attack/` には本技固有の追加効果（ランクアップ等）が実装されないため新規テストは追加していない
- `python scripts/generate_test_list.py` 実行済み
- `python -m pytest tests/ -v` 実行、3234 passed, 1 skipped（全テスト成功、結果は `.loop/test_logs/いっちょうあがり.log` に保存）
