# アクセルブレイク レビュー結果

## 仕様書
- 新規作成した。`docs/wiki/moves/アクセルブレイク.html` を取得しようとしたが、
  wiki.pokemonwiki.com がレビュー実施時に521/522エラー・タイムアウトを繰り返し
  一次情報を取得できなかったため、`docs/champions/moves.md`・`docs/spec/damage.md`・
  双子技である `docs/spec/moves/イナズマドライブ.md`（でんきタイプ版・同一仕様）を
  根拠として作成した。

## 実装計画書
- 新規作成した（仕様書をもとに、既存実装との照合結果と修正内容を記載）。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_a.py` の `アクセルブレイク` エントリで `pp=5`（Gen9本家基準）
  になっていたのを `pp=8`（チャンピオンズのPP統一ルール）に修正した。
- `src/jpoke/handlers/move_attack.py` の `効果抜群時威力ブースト`（`イナズマドライブ`と共有）は
  仕様と一致しており変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」「リーサルテスト」列が `n/a`）。

## テスト
- 変更なし。`tests/moves_attack/test_move_a.py` の
  `test_アクセルブレイク_効果抜群のとき威力が4_3倍になる` /
  `test_アクセルブレイク_等倍のとき威力倍率なし` は実装と整合しており修正不要と確認した。
