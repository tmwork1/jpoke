# ほのおのちかい レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ほのおのちかい.md`）。
- 基本情報（タイプ・分類・威力80(第六世代以降)・命中100・PP10・対象1体・優先度0・非接触）を一次情報から記載。
- コンビネーションわざ（くさのちかい・みずのちかいとの合体効果）はダブルバトル以上でのみ成立するため、
  シングルバトルのみを対象とする本プロジェクトでは対象外である旨を明記（くさのちかい仕様書と同方針）。
- ひらいしん・そうでん状態・ほのおのジュエルとの関係についても、いずれも本プロジェクトの対象外である理由を記載。

## 実装計画書
- 新規作成（`docs/plan/moves/ほのおのちかい.md`）。
- 現状の実装（`handlers={}`, 追加効果なしの単純な特殊技）が仕様書と一致していることを確認。
- 専用ハンドラ・priority検討が不要である根拠、コンビネーションわざ等が対象外である根拠を記載。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ha.py` の `"ほのおのちかい"` エントリ
  （`type="ほのお"`, `category="special"`, `pp=10`, `power=80`, `accuracy=100`, `handlers={}`）は
  仕様書・計画書と一致しており修正不要と判断した。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装・リーサルテスト列は n/a）。

## テスト
- `tests/moves_attack/test_move_ha.py` に以下を追加:
  - `test_ほのおのちかい_威力80`
  - `test_ほのおのちかい_追加効果なしでダメージのみ`
- `python scripts/sort_tests.py tests/moves_attack/test_move_ha.py` でソート済み。
- `python scripts/generate_test_list.py` でテスト一覧を更新済み。
- `python -m pytest tests/ -v` で全3593件パス（1件skip、既存のもの）を確認。
