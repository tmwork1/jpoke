# すなかけ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/すなかけ.md`）
- 一次情報（`docs/wiki/moves/すなかけ.html`）に基づき、タイプ（じめん／第一世代のみノーマル）、
  分類（変化）、PP15、命中率100、対象（相手1体）、効果（相手の命中率ランクを1段階下げる）、
  判定（まもる○／マジックコート○／オウムがえし○／よこどり×）を記載

## 実装計画書
- 新規作成（`docs/plan/moves/すなかけ.md`）
- `あまえる`（同種の防御側ランクダウン変化技）の計画書パターンに倣い、`ON_STATUS_HIT` /
  `subject_spec="attacker:self"`（デフォルト） / `priority=100` の根拠を明記
- `src/jpoke/data/ps-champ-ja/moves.json` に `すなかけ` の記載が無いため、PPは第9世代基準の15を
  採用する旨を注意事項に明記

## 実装（handlers/ / data/）
- 変更なし。既存実装（`handlers/move_status.py` の `すなかけ_modify_defender_stats` が
  共通ユーティリティ `modify_defender_stats` を用いて `accuracy: -1` を適用、
  `data/moves/move_sa.py` の `MoveData` は `type="じめん"`, `category="status"`, `pp=15`,
  `accuracy=100`, `target` はデフォルトの `"foe"`）は仕様書・計画書と一致しており、
  target欠落によるまもる／マジックコート誤反応も無いことを確認した
- PPについて: `src/jpoke/data/ps-champ-ja/moves.json` に `すなかけ` のエントリが存在しないため、
  同リストとの照合はできない。同リストは全技を網羅したものではなく、記載の無い技は
  README.md の対象範囲定義に従い第9世代基準の値（15）を採用するのが妥当と判断し、
  既存実装のPP=15はそのまま維持した（変更なし）

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が n/a）

## テスト
- `tests/moves_status/test_move_sa.py` に以下を追加
  - `test_すなかけ_相手の命中率が1段階下がる`: 命中率ランクが1段階下がることを確認
  - `test_すなかけ_まもるで防がれる`: まもる状態の相手には効果が及ばないことを確認
