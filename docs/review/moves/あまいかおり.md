# あまいかおり レビュー結果

## 仕様書
- 新規作成（docs/spec/moves/あまいかおり.md）。docs/wiki/moves/あまいかおり.html を一次情報とし、
  タイプ・分類・威力・命中率・PP・範囲・優先度・直接攻撃・効果・判定を記載。
  PPはchampions/move_list.txtの20を採用（Gen9本家の20と一致）。

## 実装計画書
- 新規作成（docs/plan/moves/あまいかおり.md）。イベント・priority（ON_STATUS_HIT, デフォルト100）、
  ハンドラ実装方針、対象がMoveDataのデフォルト値`"foe"`のままでよい理由を明記。

## 実装（handlers/ / data/）
- 変更なし。既存実装（`あまいかおり_modify_defender_stats` で `modify_defender_stats(stats={"evasion": -2})`
  を呼び出す実装、data/moves/move_a.py のMoveData定義）は仕様書・計画書と一致しており、
  まもる/マジックコート/おうじゃのしるし等は汎用処理で対応済みのため個別実装は不要と確認した。

## リーサル計算
- 対象外（n/a）。ダメージを与えない変化技のため。

## テスト
- tests/moves_status/test_move_a.py に `test_あまいかおり_defenderの回避率が2段階下がる` を追加
  （`test_あまえる_defenderのこうげきが2段階下がる` の直前、五十音順）。
  相手（defender）の回避率ランクが-2になることを検証。
