# ヘドロこうげき レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ヘドロこうげき.md`）。
  一次情報（`docs/wiki/moves/ヘドロこうげき.html`）をもとに、タイプ・分類・威力・命中・PP・
  追加効果（30%でどく状態）・判定フラグ・Zワザ威力などを記載した。
  `docs/champions/move_list.txt` に本技の記載が存在しないことを確認し、Gen9本家PP(20)が
  Champions側PP4段階(8/12/16/20)の最大値と一致するため変換不要である旨を「Champions固有仕様」
  節に明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/ヘドロこうげき.md`）。
  仕様書・既存実装（`data/moves/move_ha.py` の `MoveData`、`handlers/move_attack.py` の
  `ヘドロこうげき_apply_poison_to_defender`）を突き合わせ、priority根拠・エッジケース
  （ちからずく相互作用・世代差・PP扱い・テスト方針）を記載した。

## 実装（handlers/ / data/）
- 変更なし。既存実装（`data/moves/move_ha.py` の `type="どく"`, `category="special"`,
  `pp=20`, `power=65`, `accuracy=100`、`handlers/move_attack.py` の
  `apply_ailment_to_defender(battle, ctx, value, ailment="どく", chance=0.3)`）は
  一次情報・仕様書と完全に一致しており、修正不要と判断した。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列は `n/a`）。
  どく状態異常の付与はターン経過依存の追加効果であり、リーサル計算対象外の方針に合致する。

## テスト
- 変更なし。既存テスト `tests/moves_attack/test_move_ha.py::test_ヘドロこうげき_どくが発動する`
  は `secondary_chance=1.0` で30%のどく付与を確定発動させて確認しており、仕様と一致している。
  `python -m pytest tests/ -v` を実行し、3990 passed, 1 skipped で全件パスを確認した
  （ログ: `.loop/test_logs/ヘドロこうげき.log`）。
