# ギガインパクト レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ギガインパクト.md`）。docs/wiki/moves/ギガインパクト.html を参照し、
  タイプ・分類・威力・命中・PP（本家基準5、champions基準8を注記）・効果・判定フラグ・Zワザ威力・
  ダイマックスわざ威力を記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/ギガインパクト.md`）。既存の類似技（はかいこうせん）の計画書を参考に、
  ハンドラ構成・priority根拠・実装箇所・エッジケース・リーサル計算対象外の理由を記載した。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ka.py` の `ギガインパクト` の PP を `5`（Gen9本家基準の移行漏れ）から
  `8`（`docs/champions/move_list.txt` 基準）に修正した。
- ハンドラ実装（`リチャージ_apply` / `リチャージ_block_action`）自体は既存の共用実装で正しく、修正不要。

## リーサル計算
- 対象外（`docs/progress/move.md` で「リーサル実装: n/a」「リーサルテスト: n/a」）。
  反動で動けなくなる効果はHP依存でないため、リーサル計算上は通常の固定威力技として扱われる。

## テスト
- `tests/moves_attack/test_move_ka.py` に以下のテストを追加し、はかいこうせん（`test_move_ha.py`）と
  同等のカバレッジに揃えた:
  - `test_ギガインパクト_まもるで防がれた場合はリチャージ状態にならない`
  - `test_ギガインパクト_交代するとリチャージ状態が解除される`
  - `test_ギガインパクト_相手をひんしにしてもリチャージ状態が付与される`
- 既存の以下3件は変更なし:
  - `test_ギガインパクト_命中後にリチャージ状態が付与される`
  - `test_ギガインパクト_外れた場合はリチャージ状態にならない`
  - `test_ギガインパクト_次ターン行動不能になる`
- `python -m pytest tests/ -v` で全3710件（1 skipped）がパスすることを確認した
  （`.loop/test_logs/ギガインパクト.log`）。
