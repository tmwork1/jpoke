# すいとる レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/すいとる.md`）。
  `docs/wiki/moves/すいとる.html` の基本情報（くさ・特殊・威力20・命中100・PP25（第四世代以降）・
  対象1体・優先度0・非接触）と判定フラグ（おうじゃのしるし発動可否の世代差など）、
  ドレイン技共通仕様（回復量計算、おおきなねっこ、ヘドロえき、みがわり吸収）を記載した。
  既存の `docs/spec/moves/きゅうけつ.md` の構成に合わせた。

## 実装計画書
- 新規作成（`docs/plan/moves/すいとる.md`）。
  既存の `docs/plan/moves/きゅうけつ.md` を参考に、`MoveData`・ハンドラ・
  `Event.ON_HIT` priority=20 の根拠・テスト方針・リーサル計算方針（対象外）を記載した。

## 実装（handlers/ / data/）
- 変更なし。`data/move.py` の `すいとる`（type=くさ, category=special, pp=25, power=20,
  accuracy=100, flags={"heal"}, `Event.ON_HIT` に `すいとる_drain` を priority=20 で登録）は
  仕様書と一致していた。`handlers/move_attack.py` の `すいとる_drain` も共通関数 `_drain_hp`
  （heal_ratio=0.5）を正しく呼び出しており、他のドレイン技（ギガドレイン・きゅうけつ等）と
  同一パターンで実装済みだった。修正なし。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列は n/a）。
  他のドレイン技と同様、ドレイン技グループとして専用のリーサル計算ハンドラは未実装の方針。

## テスト
- 変更なし。既存の以下のテストで十分にカバーされていた:
  - `tests/moves_attack/test_move_sa.py::test_すいとる_使用後に攻撃者のHPが回復する`
  - `tests/moves_attack/test_move__group.py::test_drain技グループ_回復量が与ダメのheal_ratio倍になる`
    （`("すいとる", 0.5)` を含む）
  - `python scripts/generate_test_list.py` を実行し `docs/tests/moves_attack.md` を
    最新のテスト状態（他タスクで追加された分も含む）に同期した。
  - `python -m pytest tests/ -v` で3400件全てパス（1件skip）を確認した。
