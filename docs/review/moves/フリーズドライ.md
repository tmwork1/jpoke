# フリーズドライ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/フリーズドライ.md`）。
- 一次情報（`docs/wiki/moves/フリーズドライ.html`）と `docs/champions/changes_from_sv.md` を照合し、
  基本情報（こおり/特殊/威力70/命中100/PP20）・みずタイプへの特例的な効果抜群・
  **Championsではこおり付与の追加効果（SVまで10%）が削除されている**点を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/フリーズドライ.md`）。
- ハンドラ構成表・priority根拠（`ON_CALC_DEF_TYPE_MODIFIER` は `docs/spec/turn.md` 未掲載のため
  既存の同種ハンドラ `フライングプレス_add_flying_type` に準拠）・4倍補正の数学的根拠・
  Champions での追加効果削除に伴う実装方針を記載した。

## 実装（handlers/ / data/）
- **バグ修正**: 実装が `docs/champions/changes_from_sv.md`（「フリーズドライ 追加効果のこおり状態を削除」）に
  反して、SV仕様のまま10%でこおり状態を付与する `ON_DAMAGE_HIT` ハンドラ
  （`フリーズドライ_apply_freeze_to_defender`）と `flags={"secondary_effect"}` を保持していたため削除した。
  - `src/jpoke/data/moves/move_ha.py`: `フリーズドライ` エントリから `ON_DAMAGE_HIT` ハンドラと
    `secondary_effect` フラグを削除。
  - `src/jpoke/handlers/move_attack.py`: 不要になった `フリーズドライ_apply_freeze_to_defender` 関数を削除。
  - みずタイプへのタイプ相性補正ハンドラ（`フリーズドライ_water_effectiveness`、4倍補正による
    1/2倍→2倍化）は仕様通りであることを確認し、変更なし。複合タイプ（みず/ひこう等）でも
    「みず成分のみ2倍に置換」する演算として数学的に正しいことを確認した。

## リーサル計算
- 対象外（`n/a`）。追加効果削除により、通常のダメージ計算のみで完結するため。

## テスト
- `tests/moves_attack/test_move_ha.py`: `test_フリーズドライ_こおりが発動する`（Champions仕様と矛盾する
  誤ったテスト）を `test_フリーズドライ_こおり付与効果はChampionsで削除されている` に修正し、
  `secondary_chance=1.0` を強制してもこおり状態が付与されないことを確認するテストに変更した。
- `tests/moves_attack/test_move_ha.py`: `test_フリーズドライ_みずタイプに効果抜群`（タイプ相性2倍の確認）は
  実装・仕様と一致しており変更なし。
- `tests/items/test_item_yarawa.py`: ヤチェのみ関連テストは実装・仕様と一致しており変更なし。
- `python -m pytest tests/ -v` を実行し、3973 passed, 1 skipped（既存の無関係なskip）を確認した。
  ログ: `.loop/test_logs/フリーズドライ.log`
