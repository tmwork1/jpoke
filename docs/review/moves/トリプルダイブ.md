# トリプルダイブ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/トリプルダイブ.md`）。
  - 一次情報（`docs/wiki/moves/トリプルダイブ.html`）より、タイプ:みず / 分類:物理 / 威力:30（固定） /
    命中:95 / PP:10（Wiki準拠） / 対象:相手1体 / 優先度:0 / 接触あり を確認。
  - 「技の仕様」節の「1発目が当たれば攻撃は必ず3回続く」という記述から、本技は
    **命中判定を1発目のみ行う固定3回攻撃技**であることを確認した
    （`docs/spec/moves/_multi_hit.md` の「9.2 3回攻撃固定技」分類・`すいりゅうれんだ` と同一パターン）。
    タスク指示文には「各ヒットごとに命中判定する技」とあったが、一次情報・既存の
    `_multi_hit.md` の分類のいずれとも矛盾するため誤りと判断し、実際の仕様（1発目のみ判定）で記載した。
  - `docs/champions/move_list.txt` に該当行が存在しないため（ウミトリオ専用技でTMが存在しないため
    掲載対象外と推測。`すいりゅうれんだ`・`シャドーダイブ` 等の専用技も同様）、
    README.md の対象範囲定義に従い第9世代本家/Wiki準拠のPP値（10）を採用する旨を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/トリプルダイブ.md`）。
  - 既存実装が仕様と一致していることを確認した内容と、PPが champions/move_list.txt に
    掲載されていない理由、review-test 向けのエッジケース（命中判定1発目のみ・急所は通常判定）を記載。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/moves/move_ta.py` の `MoveData("トリプルダイブ")` は
  `multi_hit={"min":3,"max":3,"check_hit_each_time":False,"power_sequence":()}` かつ
  `flags={"contact"}`（`check_hit_each_time` フラグなし）となっており、仕様書
  （命中判定は1発目のみ・威力固定30・急所は通常判定）と一致していることを確認した。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- `tests/moves_attack/test_move_ta.py` に以下を追加した（今回はテストが存在しなかったため新規作成）。
  - `test_トリプルダイブ_3回固定でヒットする`: `_resolve_hit_count` が常に3を返すことを確認。
  - `test_トリプルダイブ_命中判定は1発目のみで残りは必ず命中する`: `_check_hit` を
    「1発目のみ命中」に差し替えても3ヒットとも発生することを確認（命中判定が1発目のみである検証）。
  - `test_トリプルダイブ_相手にダメージを与える`: 基本的なダメージ発生を確認。
  - `python -m pytest tests/ -v` で全件パス（3902 passed, 1 skipped）を確認した
    （`test_move_ma.py::test_むしくい_ねんちゃく持ちをひんしにさせた場合は奪える` は
    攻撃側にまひを付与したまま `fix_random` していない既存の無関係なテストで、
    まひによる行動不能が発生すると偶発的に失敗する既存のflakyテスト。本レビューの対象外のため
    修正していない）。
