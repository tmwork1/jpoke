# つばさでうつ レビュー結果

## 仕様書
- 変更なし。
- つばさでうつは追加効果を持たない単純な物理ひこう技（「つつく」の上位互換）であり、
  他の追加効果なし単純技（あくうせつだん・いわおとし・えだづき等）と同様に専用の仕様書
  （`docs/spec/moves/つばさでうつ.md`）は新規作成しなかった。
- 一次情報（`docs/wiki/moves/つばさでうつ.html`）で以下を確認し、実装と整合していることを確認した。
  - タイプ: ひこう / 分類: 物理 / 威力: 60（第二世代以降。第一世代は35） / 命中率: 100 / PP: 35
  - 範囲: 1体選択（遠隔） / 優先度: 0 / 直接攻撃: ○（接触技）
  - 効果: 通常攻撃（追加効果なし）

## 実装計画書
- 変更なし。
- 上記と同じ理由で、単純な追加効果なし技のため専用の計画書
  （`docs/plan/moves/つばさでうつ.md`）は作成しなかった。

## 実装（handlers/ / data/）
- 変更なし。
- `src/jpoke/data/move.py` の `つばさでうつ` エントリを確認:
  - `type="ひこう"`, `category="physical"`, `pp=35`, `power=60`, `accuracy=100`,
    `flags={"contact"}`, `handlers={}`（追加効果なし）
  - `target` は指定なし（デフォルトの `"foe"` = 1体選択）で一致
- 一次情報と完全に一致しており、修正の必要はなかった。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装列が `n/a`）。

## テスト
- `tests/moves_attack/test_move_ta.py` につばさでうつ専用のテストが存在しなかったため、
  以下を新規追加した。
  - `test_つばさでうつ_相手にダメージを与える`: 追加効果なしの物理ひこう技で相手にダメージを
    与えることを確認
- `python scripts/sort_tests.py tests/moves_attack/test_move_ta.py` でソート、
  `python scripts/generate_test_list.py` でテスト一覧を更新した。
- `python -m pytest tests/ -v` を実行し、3433 passed, 1 skipped で全件パスしたことを確認した
  （`.loop/test_logs/つばさでうつ.log` に保存）。
