# しっとのほのお レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/しっとのほのお.md`）。
- 一次情報（`docs/wiki/moves/しっとのほのお.html`）に基づき、基本情報（ほのお/特殊/威力70/命中100/PP5）、
  追加効果（そのターンにランクが上がった相手を100%の確率でやけど状態にする）、
  対象外となるランク上昇の種類（パワースワップ系、バトンタッチ引き継ぎ、へんしんコピー、
  ターン終了時発動効果、急所ランク上昇等）、死に出し/1ターン目/後出しのタイミング仕様、
  やけど無効化条件（りんぷん・おんみつマント・ほのおタイプ）を記載した。

## 実装計画書
- 新規作成（`docs/plan/moves/しっとのほのお.md`）。
- `data/move.py`・`handlers/move_attack.py` の既存実装が仕様と一致していることを確認し、
  ハンドラ構成表（`ON_DAMAGE_HIT` / priority 100 / attacker:self）と priority 根拠
  （既存の同種ハンドラ「したでなめる」「みわくのボイス」と同一パターン）を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/move.py` の `しっとのほのお` エントリ（pp=5, power=70, accuracy=100）、
  `src/jpoke/handlers/move_attack.py` の `しっとのほのお_apply_burn_to_defender` は
  仕様書・計画書と一致しており、修正不要と判断した。
  `ctx.defender.stat_raised_this_turn`（`Battle.modify_stats()` で汎用的に管理されるフラグ）を
  参照する実装であり、能力上昇の発生源を問わず一律で判定できる設計になっている。

## リーサル計算
- 対象外。`docs/progress/move.md` の「リーサル実装」列が `保留` のため、手順5の対象外としてスキップした。

## テスト
- 変更なし。`tests/moves_attack/test_move_sa.py` に既存の
  `test_しっとのほのお_ランクが上がった相手にやけどが付与される` /
  `test_しっとのほのお_ランクが上がっていない相手にはやけどを付与しない` の2件があり、
  仕様の主要ケース（付与される/されない）を検証できている。追加不要と判断した。

## その他
- `docs/progress/move.md` の しっとのほのお 行に誤り（PP列が8になっていたが正しくは5）があったため修正した。
  併せて 仕様書・計画書 列を `x`、レビュー列を `✓` に更新した。
