# パラボラチャージ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/パラボラチャージ.md`）。
  - `docs/wiki/moves/パラボラチャージ.html` を一次情報として基本情報（タイプ: でんき、分類: 特殊、
    威力: 65（第七世代以降の値。本プロジェクトはこちらを採用）、命中100、対象defender、
    優先度0、接触なし）と効果（与ダメの半分を自分が回復・追加効果なし）を記載した。
  - PPは `src/jpoke/data/ps-champ-ja/moves.json`（718行目）の値20を採用（Gen9本家とも同値のため差異なし）。
  - HP吸収技グループの共通計画書 `docs/plan/moves/HP吸収技.md` および
    `docs/spec/moves/_move_secondary_2.md`（HP吸収(与ダメ比)=0.5）への参照を明記した。
  - おおきなねっこ・ヘドロえき・かいふくふうじとの相互作用を、既存の汎用実装
    （`おおきなねっこ_boost_drain` / `ヘドロえき_reverse_drain` / `かいふくふうじ_try_action`）
    への参照として記載した。
  - ダブルバトル以上を前提とする複数対象への吸収順序仕様は、シングルバトル専用の本プロジェクトの
    スコープ外として明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/パラボラチャージ.md`）。
  - 既存実装（`_drain_hp` ヘルパー再利用、`heal_ratio=0.5`）をそのまま計画書として文書化した。
  - `Event.ON_HIT` priority=20 の根拠を `docs/spec/turn.md`（「HP吸収技による回復」）から明記した。
  - おおきなねっこ・ヘドロえき・かいふくふうじは汎用実装で対応済みのため専用実装不要である旨を記載。
  - リーサル計算は対象外（n/a）である理由（通常のダメージ計算のみでリーサル判定が成立する回復技）
    を明記した。

## 実装（handlers/ / data/）
- 変更なし。
  - `src/jpoke/handlers/move_attack.py` の `パラボラチャージ_drain`（`_drain_hp(heal_ratio=0.5)` を
    呼び出すのみ）を確認。仕様書のheal_ratio=0.5、四捨五入・最低1回復に一致。
  - `src/jpoke/data/moves/move_ha.py` の `MoveData`（でんき / special / pp=20 / power=65 /
    accuracy=100 / flags={"heal"} / `Event.ON_HIT` priority=20）を確認。PPはchampions基準の
    move_list.txt（718行目、20）と一致。`flags={"heal"}` によりかいふくふうじでの選択禁止も
    正しく機能する。`contact` フラグが付与されていないことも wiki の「直接攻撃: ×」と一致。
  - ヘドロえき（`ヘドロえき_reverse_drain`）・おおきなねっこ（`おおきなねっこ_boost_drain`）は
    `hp_change_reason == "drain"` / `Event.ON_CALC_DRAIN` を用いた汎用実装であり、本技固有の
    追加実装は不要であることを確認した。

## リーサル計算
- 対象外（n/a）。`docs/progress/move.md` のリーサル実装・リーサルテスト列がともに n/a であることを
  確認し、変更なし。

## テスト
- 変更なし。
  - `tests/moves_attack/test_move_ha.py::test_パラボラチャージ_使用後に攻撃者のHPが回復する`
    （HP1から使用してHPが増加することを確認）を確認。
  - `tests/moves_attack/test_move__group.py::test_drain技グループ_回復量が与ダメのheal_ratio倍になる`
    のパラメータ化テストに `("パラボラチャージ", 0.5)` が含まれており、与ダメ100に対して回復量50
    となることを検証済み。
  - `python -m pytest tests/ -v` を実行し、3963 passed, 1 skipped で全件パスを確認
    （`.loop/test_logs/パラボラチャージ.log`）。
