# れんごく レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/れんごく.md`）
  - 一次情報（`docs/wiki/moves/れんごく.html`）と `docs/spec/moves/_move_secondary_1.md`（146行目）に基づき、
    タイプ・分類・威力・命中・PP・対象・優先度・接触の有無・追加効果（100%やけど）・判定フラグを記載。
  - ほのおタイプの攻撃技として、被弾した相手のこおりを解凍する仕様（`docs/spec/ailments/こおり.md`）も明記。

## 実装計画書
- 新規作成（`docs/plan/moves/れんごく.md`）
  - 既存実装（type/category/pp/power/accuracy/やけど付与ハンドラ/リーサルハンドラ）は仕様と一致しており
    変更不要である旨を記載。
  - `flags` に `"thaw"` が欠落していたため、追加が必要である旨と priority 根拠（`だいもんじ` 等と同型）を記載。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ra.py`: `"れんごく"` の `flags` に `"thaw"` を追加した。
  - れんごくはほのおタイプの特殊攻撃技であり、`こおり_cure_by_thaw_move`（`handlers/ailment.py`）が
    `flags` の `"thaw"` を見て被弾した相手のこおりを解凍する仕様のため、既存の `かえんほうしゃ` 等と
    同様にこのフラグが必要（欠落は実装ミス）。
- `handlers/move_attack.py` の `れんごく_apply_burn_to_defender`（100%やけど付与）は仕様通りで変更なし。
- `handlers/lethal.py` の `れんごく_apply_やけど`（リーサル計算用ハンドラ）は仕様通りで変更なし。

## リーサル計算
- 変更なし。`handlers/lethal.py` の `れんごく_apply_やけど`（`LethalEvent.ON_HIT`）は
  追加効果有効時に防御側へやけどを確定付与しており、`キラースピン_apply_どく` 等の既存パターンと
  同型で仕様と一致している。`tests/test_lethal.py` の `test_れんごく_やけど付与_secondary有り` も
  既存のまま妥当。

## テスト
- `tests/moves_attack/test_move_yarawa.py` に `test_れんごく_こおり状態の相手に当てると解凍する`
  を追加した（`かえんほうしゃ` の同種テストと同じ構成、`flags` に `"thaw"` を追加したことの検証）。
- 既存の `test_れんごく_やけどが発動する`（burn付与テスト）は変更なし。
