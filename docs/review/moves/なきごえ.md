# なきごえ レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/なきごえ.md`）。一次情報（`docs/wiki/moves/なきごえ.html`）を基に、
  タイプ・分類・命中率・PP・対象・優先度・効果・各種判定フラグを整理した。
- PPは `docs/champions/move_list.txt` に単独項目が存在しないため、既存の前例
  （`ちょうおんぱ`・`ないしょばなし`）に倣い Gen9本家準拠の40を採用（現行実装と一致、変更なし）。

## 実装計画書
- 新規作成（`docs/plan/moves/なきごえ.md`）。仕様書を基に、ハンドラ構成・priority根拠・
  sound フラグによる汎用処理・まもる/マジックコートとの相互作用・リーサル対象外の理由を明記した。

## 実装（handlers/ / data/）
- `handlers/move_status.py` の `なきごえ_modify_defender_stats` に docstring を追加（同系統の
  `なかよくする_modify_defender_stats` と体裁を合わせた）。ロジック自体は仕様に合致しており
  変更なし。
- `data/moves/move_na.py` の `なきごえ` エントリ（type/category/pp/accuracy/flags={"sound"}/
  ON_STATUS_HIT ハンドラ登録）は仕様書・計画書と一致しており変更なし。

## リーサル計算
- 対象外（変化技のためダメージを与えない。`docs/progress/move.md` のリーサル実装/
  リーサルテスト列は n/a）。

## テスト
- `tests/moves_status/test_move_na.py` に以下を追加:
  - `test_なきごえ_こうげきが1段階下がる`
  - `test_なきごえ_まもるで防がれる`
  - `test_なきごえ_みがわりを貫通してこうげきが下がる`
- `python scripts/sort_tests.py tests/moves_status/test_move_na.py` で五十音順に整列。
- `python scripts/generate_test_list.py` で `docs/tests/` を更新。
- `python -m pytest tests/ -v` で全件パス（4291 passed, 1 skipped）。ログは
  `.loop/test_logs/なきごえ.log` に保存。
  - 実行中、なきごえと無関係な既存テスト（`DDラリアット`・`ミラーコート`・`てんねん` 等）が
    実行順序・乱数状態依存で稀に失敗する既存のflakinessを確認したが、本レビューの変更とは
    無関係であり対象外とした（複数回再実行するといずれも単独では成功する）。
