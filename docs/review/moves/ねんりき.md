# ねんりき レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/ねんりき.md`）。
- `docs/wiki/moves/ねんりき.html` を一次情報として、エスパー/特殊/威力50/命中100/PP25/
  対象1体/優先度0/非接触/追加効果として10%の確率でこんらん付与、という内容を記載した。
- `src/jpoke/data/ps-champ-ja/moves.json` には本技の記載がないため、本家シリーズ（Wiki）の値を
  そのまま採用する旨を明記した。

## 実装計画書
- 新規作成（`docs/plan/moves/ねんりき.md`）。
- 既存実装（`data/move.py` の `"ねんりき"` エントリ、`handlers/move_attack.py` の
  `ねんりき_apply_confusion_to_defender`）との差分がないことを確認し、ハンドラ構成・
  イベント（`Event.ON_DAMAGE_HIT`）・優先度・subject_spec・リーサル対象外の根拠を記載した。

## 実装（handlers/ / data/）
- 変更なし。`data/move.py` の `"ねんりき"` エントリ（type=エスパー, category=special,
  pp=25, power=50, accuracy=100, flags={"secondary_effect"}）は一次情報と一致していた。
- `handlers/move_attack.py` の `ねんりき_apply_confusion_to_defender` は
  `apply_confusion_to_defender(battle, ctx, value, chance=0.1)` を呼び出しており、
  ちからずく対応（`resolve_secondary_chance` 経由）・こんらん付与ロジックとも仕様と一致していた。

## リーサル計算
- 対象外（`docs/progress/move.md` の「リーサル実装」列が `n/a`）。
  こんらん付与のみでHP分布に影響しない追加効果のため、リーサル計算ハンドラは不要と判断した。

## テスト
- 変更なし。`tests/moves_attack/test_move_na.py` の
  `test_ねんりき_こんらんが発動する`（secondary_chance=1.0 でこんらん付与を確認）が既存し、
  実装と一致していることを確認した。
- `python -m pytest tests/ -v` で 3537 passed, 1 skipped を確認（`.loop/test_logs/ねんりき.log`）。

## 進捗表
- `docs/progress/move.md` の ねんりき 行を更新: 仕様書=x, 計画書=x, レビュー=✓、
  タイプ=エスパー, 威力=50, 命中率=100, PP=25, 対象=defender,
  効果=「追加効果として、10%の確率で相手をこんらん状態にする。」に補完した
  （それまで `?` のプレースホルダーだった）。
