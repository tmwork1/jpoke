# メガトンキック レビュー結果

## 仕様書
- 新規作成（`docs/spec/moves/メガトンキック.md`）。ノーマルタイプ/物理/威力120/命中75/PP（Gen9本家5、本プロジェクトはchampions基準で8）/対象1体/優先度0/接触あり、追加効果なしの通常攻撃であることを記載。

## 実装計画書
- 新規作成（`docs/plan/moves/メガトンキック.md`）。追加効果を持たないため専用ハンドラ不要、`handlers={}` のみで基本ダメージ計算に委ねる方針を明記。

## 実装（handlers/ / data/）
- `src/jpoke/data/moves/move_ma.py` の `"メガトンキック"` エントリの PP を `5` から `8` に修正（`docs/champions/move_list.txt` で PP=8 と定義されており、Gen9本家値からの移行漏れだったため）。
- その他（type/category/power/accuracy/flags）は仕様と一致しており変更なし。

## リーサル計算
- 対象外（`docs/progress/move.md` のリーサル実装/リーサルテスト列は `n/a`）。

## テスト
- `tests/moves_attack/test_move_ma.py` に `test_メガトンキック_相手にダメージを与える` を新規追加（従来テストが存在しなかったため）。
