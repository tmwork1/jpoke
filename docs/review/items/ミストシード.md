# ミストシード レビュー結果

## 仕様書
- `docs/spec/items/_フィールドシード.md` のミストシード節が未記入だったため、他のフィールドシード（グラスシード・エレキシード・サイコシード）の節と一次情報（wiki）に基づき、効果・詳細な仕様・なげつける威力・ウッウロボ素材情報を補完した。

## 実装計画書
- `docs/plan/items/ミストシード.md` が旧フォーマット（`Event.ON_SWITCH_IN` の priority=100、`once=True`、`battle.consume_item`、ランクキー "D" 表記）のまま残っており、実装・他シード系の計画書と乖離していたため、`docs/plan/items/サイコシード.md` の構成（イベント1: 登場時 priority=120／イベント2: フィールド変化時／イベント3: アイテム有効化時、`_terrain_seed_boost` 共通ロジック、消費条件の明記）に合わせて全面的に書き直した。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/item.py` の `ミストシード_boost_spdef`（共通関数 `_terrain_seed_boost` を呼び出す実装）自体は正しく、修正不要だった。
- `src/jpoke/data/item.py` の `"ミストシード"` エントリで `Event.ON_SWITCH_IN` の登録に `priority=120` が指定されておらず、`docs/spec/turn.md`（ON_SWITCH_IN: 「120 ミストシードの発動」）およびグラスシード・エレキシード・サイコシードの実装と不整合だったため追加した。
- `python scripts/sort_data/sort_items.py` を実行し、ITEMS辞書の並び順を確認（変更なし、既に五十音順）。

## テスト
- `tests/test_item.py` には `test_フィールドシード_フィールドなしでは発動しない` / `test_フィールドシード_ランク最大時は消費しない` / `test_フィールドシード_発動` がエレキシード・グラスシード・サイコシード・ミストシードの4アイテムをパラメトライズしてカバーしており、ミストシードのテストは既に十分だったため追加・修正なし。
- `python scripts/sort_tests.py tests/test_item.py` を実行（変更なし、既にソート済み）。
- `python scripts/generate_test_list.py` を実行（変更なし）。
- `python -m pytest tests/ -v` を実行し、3083 passed, 1 skipped を確認（結果は `.loop/test_logs/ミストシード.log` に保存）。
