# くちたたて レビュー結果

## 仕様書
- 変更なし。一次情報（`docs/wiki/items/くちたたて.html`）と照合し、既存の `docs/spec/items/くちたたて.md` の内容（効果・なげつけるの失敗・トリック/すりかえの失敗・ふしょくガス無効・ほしがる/どろぼう/マジシャン/わるいてぐせでの奪取不可・はたきおとすの効果なし・れきせんのゆうしゃザマゼンタへの譲渡不可）に誤り・欠落は見つからなかった。

## 実装計画書
- 修正した。既存の計画書は「フォルムチェンジのみ」を実装方針としており、仕様書に明記されている「奪取/交換/除去の禁止」「れきせんのゆうしゃザマゼンタへの譲渡禁止」が計画から漏れていた。姉妹アイテム `くちたけん`（ザシアン版）の実装済みハンドラ構成に合わせ、`Event.ON_CHECK_ITEM_CHANGE` を用いた実装方針・実装ステップ・テスト観点・疑似コード・リスク欄を追記した。

## 実装（handlers/ / data/）
- 修正した。`src/jpoke/data/item.py` の `くちたたて` エントリには `ON_SWITCH_IN` / `ON_ITEM_GAINED` によるフォルムチェンジハンドラのみが登録されており、仕様書に記載されている奪取/交換/除去の禁止処理が未実装だった（姉妹アイテムの `くちたけん` には実装済みだったため、実装漏れと判断）。
  - `src/jpoke/handlers/item.py` に `くちたたて_prevent_item_change`（ザマゼンタが持っている間は `ON_CHECK_ITEM_CHANGE` を `False` にして奪取/交換/除去を禁止）と `くちたたて_prevent_transfer_to_hero`（れきせんのゆうしゃザマゼンタへの譲渡を禁止）を追加した。
  - `src/jpoke/data/item.py` の `くちたたて` に `Event.ON_CHECK_ITEM_CHANGE` ハンドラ（`target:self` / `target:foe`）を登録した。
  - `python scripts/sort_handlers.py src/jpoke/handlers/item.py` と `python scripts/sort_items.py` を実行済み。

## テスト
- 追加した。`tests/test_item.py` に `くちたけん` の既存テストと対になる以下のテストを追加した。
  - `test_くちたたて_ザマゼンタから奪えない`（はたきおとす・どろぼう × れきせん/たてのおう）
  - `test_くちたたて_ザマゼンタ以外は奪われる`
  - `test_くちたたて_トリックでザマゼンタから奪えない`
  - `test_くちたたて_れきせんのゆうしゃザマゼンタへ渡せない`
  - `python scripts/sort_tests.py tests/test_item.py`、`python scripts/generate_test_list.py` を実行し、`python -m pytest tests/ -v` で全2834件（1件skip、対象外）がパスすることを確認した。ログは `.loop/test_logs/くちたたて.log` に保存。
