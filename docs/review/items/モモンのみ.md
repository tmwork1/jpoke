# モモンのみ レビュー結果

## 仕様書
- 変更なし（`docs/spec/items/_状態回復きのみ.md` に既存記載あり。一次情報（`どく・もうどく状態を回復させる`、なげつける威力10）と一致していることを確認した）

## 実装計画書
- 修正した: `docs/plan/items/モモンのみ.md` が `ON_TURN_END` のみの古い内容だったため、実際の実装（`ON_APPLY_AILMENT` による即時回復・`ON_ITEM_ENABLED`・`ON_FORCE_BERRY_TRIGGER`・`ON_TURN_END` の4イベント構成）に合わせて全面的に書き直した。他の状態異常回復きのみ（クラボのみ等）の計画書と同じ体裁に統一。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/item.py` の `モモンのみ` エントリと `src/jpoke/handlers/item.py` の `モモンのみ_cure_poison` / `モモンのみ_cure_poison_on_apply`（共通ヘルパー `_cure_ailment_berry` / `_cure_ailment_berry_on_apply` を利用）は他の状態異常回復きのみと同一パターンで実装済みであり、仕様と齟齬なし。なげつけるの効果（`なげつける_apply_item_effect` 内の `モモンのみ` 分岐）も既に実装済みで対象外。

## テスト
- 追加: `test_モモンのみ_どく付与直後に即時回復する`（`tests/test_item.py`）を新規追加。他の状態異常回復きのみ（カゴのみ・クラボのみ・チーゴのみ・ナナシのみ）には ON_APPLY_AILMENT による即時回復を確認するテストが存在していたが、モモンのみだけ欠落していたため補充した。
- 既存の `test_状態異常回復きのみ_ターン終了で回復` / `test_状態異常回復きのみ_対象外状態では発動しない`（パラメタライズ）でモモンのみのターン終了時回復・対象外状態異常での不発動は確認済み。
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新、`python -m pytest tests/ -v` で全3104件（+skip1件）通過を確認した。
