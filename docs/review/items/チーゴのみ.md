# チーゴのみ レビュー結果

## 仕様書
- `docs/spec/items/_状態回復きのみ.md`（カゴのみ・クラボのみ・チーゴのみ・ナナシのみ・モモンのみ・キーのみ・ラムのみの共通仕様書）に既存記載あり。一次情報（`docs/wiki/items/チーゴのみ.html`）と照合し「効果:やけど状態を回復させる」の記載に誤りがないことを確認した。変更なし。

## 実装計画書
- `docs/plan/items/チーゴのみ.md` は既存だったが、回復対象の状態異常が誤って「こおり」と記載されていた（一次情報・仕様書・実装はいずれも「やけど」）。以下の内容に修正した。
  - 効果概要・実装仕様の対象状態異常を「こおり」→「やけど」に修正
  - 実装が実際に登録している4イベント（`ON_APPLY_AILMENT` / `ON_ITEM_ENABLED` / `ON_FORCE_BERRY_TRIGGER` / `ON_TURN_END`）を明記（旧計画書は `ON_TURN_END` のみの記載だった）
  - ハンドラ疑似コードを実装の `_cure_ailment_berry` / `_cure_ailment_berry_on_apply` 呼び出しに合わせて更新
  - テスト観点に「やけど付与直後の即時回復」を追加

## 実装（handlers/ / data/）
- `src/jpoke/handlers/item.py` の `チーゴのみ_cure_burn` / `チーゴのみ_cure_burn_on_apply`、`src/jpoke/data/item.py` の `"チーゴのみ"` エントリはいずれも「やけど」を正しく対象としており、カゴのみ・クラボのみと同一パターン（`ON_TURN_END` / `ON_APPLY_AILMENT` / `ON_ITEM_ENABLED` / `ON_FORCE_BERRY_TRIGGER`、priority 50）で実装済み。修正不要（変更なし）。
- 誤りがあったのは計画書のみで、実装自体は一次情報・仕様書と一致していた。

## テスト
- `tests/test_item.py` に既存だったのは以下2件のみ（パラメータ化テスト内）:
  - `test_状態異常回復きのみ_ターン終了で回復`（チーゴのみ／やけど）
  - `test_状態異常回復きのみ_対象外状態では発動しない`（チーゴのみ／まひでは発動しない）
- 兄弟アイテムのカゴのみ・クラボのみには存在する「付与直後の即時回復（`ON_APPLY_AILMENT`）」を確認するテストがチーゴのみに欠けていたため、`test_チーゴのみ_やけど付与直後に即時回復する`（`おにび`でやけどを付与し即座に回復・消費されることを確認）を追加した。
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新し、`python -m pytest tests/ -v` で全2944件（+skip1件）がパスすることを確認した（ログ: `.loop/test_logs/チーゴのみ.log`）。
