# りゅうのキバ レビュー結果

## 仕様書
- 新規作成: `docs/spec/items/りゅうのキバ.md` を作成した（`docs/wiki/items/りゅうのキバ.html` の一次情報に基づく）。
  - 効果: 持たせるとドラゴンタイプの技の威力が1.2倍になる（第四世代以降）。なげつける威力70。
  - 詳細な仕様: 他のタイプ強化アイテムと同様、タイプが変わる技を含め判定時点の技タイプで判定される旨を記載。

## 実装計画書
- 更新: `docs/plan/items/りゅうのキバ.md` を更新した。
  - 仕様書リンクを未作成から実際の作成済みファイルパスに修正。
  - 実装方針（共通実装 `_modify_power_by_type` の再利用、イベント・subject_spec の根拠）を明記。
  - 実装済み内容・テスト状況を具体的に記載。

## 実装（handlers/ / data/）
- 変更なし。既存実装（`src/jpoke/data/item.py` の `りゅうのキバ` エントリ、`src/jpoke/handlers/item.py` の `りゅうのキバ_modify_power_by_type`）は他のタイプ強化アイテム（じしゃく・くろおび・やわらかいすな等）と同一の共通実装 `_modify_power_by_type` を使用しており、`fling_power=70`・威力補正1.2倍(4915/4096)・`subject_spec="attacker:self"` とも仕様と一致していることを確認した。

## テスト
- 変更なし。`tests/test_item.py` の `test_タイプ強化アイテム` パラメータ化テストに `("りゅうのキバ", "ドラゴン")` が既に含まれており、威力補正1.2倍(4915/4096)の検証が行われている。
- `python -m pytest tests/ -v` を実行し、3142 passed, 1 skipped（既存の無関係なskip）で全件パスを確認した。
