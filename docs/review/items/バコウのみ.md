# バコウのみ レビュー結果

## 仕様書
- 変更なし（`docs/spec/items/_タイプ半減きのみ.md` のバコウのみ節は wiki 一次情報（`docs/wiki/items/バコウのみ.html`）と一致していることを確認した）
  - ついばむを効果抜群で受けた場合、技の効果よりバコウのみの効果が優先されダメージが半減する旨
  - フライングプレスが効果抜群になる場合でも発動するのはヨプのみでありバコウのみは発動しない旨
  - 上記いずれも一次情報どおり記載済みで誤り・欠落なし

## 実装計画書
- 修正した（`docs/plan/items/バコウのみ.md` を作成し直した）
  - 旧版は「仕様書未作成」と誤った記述のままだったため、実際の仕様書（共有仕様書）を参照するよう修正
  - `ON_CALC_DAMAGE_MODIFIER` での実装方針、ついばむとの相互作用（アイテム消費が先に走るため`ON_HIT`の盗み効果が不発になる理由）、フライングプレスで発動しない理由（`ctx.move.type == "ひこう"` 判定によりかくとう技のフライングプレスは対象外になる）を明記
  - リーサル実装・登録内容・テスト内容を実態に合わせて記載

## 実装（handlers/ / data/）
- 変更なし
  - `src/jpoke/handlers/item.py` の `バコウのみ_modify_super_effective_damage`（`_modify_super_effective_damage(..., type_="ひこう", modifier=2048/4096)`）を確認。`ctx.move.type != type_` 判定により、フライングプレス（技タイプ`かくとう`）は自動的に対象外になっており、仕様（バコウのみは発動しない）と一致している
  - `src/jpoke/handlers/lethal.py` の `バコウのみ_resist_flying`（`_type_resist_berry(..., "ひこう")`）も同様の判定で仕様と一致
  - ついばむ（`src/jpoke/data/move.py` の `むしくい_steal_and_use_berry` ハンドラ、`ON_HIT`）は `defender.has_item()` を確認してから盗み処理を行うため、`ON_CALC_DAMAGE_MODIFIER` で先にバコウのみが消費されていれば自然に盗み処理が不発になる。既存の実装で仕様通りの挙動になっていることを確認した
  - `src/jpoke/data/item.py` の `"バコウのみ"` エントリ（`fling_power=10`、`handlers`、`lethal_handlers`）に誤り・欠落なし

## テスト
- 追加した（`tests/test_item.py`）
  - `test_バコウのみ_ついばむで奪われる前に消費されダメージ半減`: ついばむを効果抜群で受けたときダメージが半減し、バコウのみが消費済みのため攻撃側に奪われないことを確認
  - `test_バコウのみ_フライングプレスでは発動しない`: フライングプレスがひこう複合相性で抜群になってもバコウのみは発動せず消費されないことを確認
  - 既存の `test_タイプ半減実`（パラメータ化）に含まれる `("バコウのみ", "ひこう", "コジョフー")` ケースは変更なし
  - `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` で一覧更新、`python -m pytest tests/ -v` で全件（3153 passed, 1 skipped）確認済み
