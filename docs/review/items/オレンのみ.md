# オレンのみ レビュー結果

## 仕様書
- 変更なし。`docs/spec/items/_HP回復ピンチきのみ.md` にオレンのみの節が既に存在し、
  一次情報（`docs/wiki/items/オレンのみ.html`）と照合した結果、内容は正確だった。
  - 効果:HPを10回復する。ポケモンに持たせると戦闘中にHPが1/2以下になった時、HPを10回復する。
  - 詳細な仕様は「回復量以外の効果はオボンのみと共通する部分が多い」とし、オボンのみの節を参照する形になっており、一次情報の記述と一致。

## 実装計画書
- 変更なし。`docs/plan/items/オレンのみ.md` は既存で、`Event.ON_HP_CHANGED` / `subject_spec="target:self"` /
  priority 100 / once=True という仕様が仕様書・実装と整合していることを確認した。
  疑似コードは実装時に `_heal_berry` 共通ヘルパーへリファクタリングされているが、
  ロジック（発動条件・回復量・消費）は計画書の意図と一致している。

## 実装（handlers/ / data/）
- 変更なし。
  - `src/jpoke/handlers/item.py` の `オレンのみ_heal_on_half_hp`（共通ヘルパー `_heal_berry(denominator=2, heal_v=10)` を使用）は、
    HPが最大HPの1/2以下になったときに10回復し、`_announce_and_consume_item` でアイテムを消費する。
  - `src/jpoke/data/item.py` で `Event.ON_HP_CHANGED` に `subject_spec="target:self"` で登録済み。オボンのみと同一パターン。
  - リーサル計算用の `src/jpoke/handlers/lethal.py` の `オレンのみ_heal`（`_heal_at_pinch(v=10, threshold_rate=1/2, ...)`）も整合していることを確認した。
  - きんちょうかん・じんばいったい・マジックルーム等によるきのみ使用禁止は、アイテム共通の
    使用可否判定（`item_manager` の disabled reason 機構等）で一括処理されるため、本アイテム側で
    個別に判定する必要はない（既存の共通実装を確認済み）。
  - じゅくせい（回復量2倍）は特性側が未実装（`AbilityData()` が空）のため、現状は反映されない。
    これは特性実装側のスコープであり、本アイテムの実装漏れではない。

## テスト
- 変更なし。`tests/test_item.py` に以下2件が既に存在し、実装と一致することを確認した。
  - `test_オレンのみ_HP50以下で10回復`
  - `test_オレンのみ_HP50超では発動しない`
  - `tests/test_lethal.py::test_オレンのみ_HP2分の1以下で10回復` も存在し、リーサル実装と整合。
- `python -m pytest tests/ -v` を実行し、2713件全て成功（1件skip）を確認した。
  1回目の実行で急所（ダメージ計算の乱数依存）関連の5件が失敗したが、無関係と判断し再実行したところ全件成功した。
  ログは `.loop/test_logs/オレンのみ.log` に保存。
