# マゴのみ レビュー結果

## 仕様書
- `docs/spec/items/_HP回復ピンチきのみ.md` のマゴのみ節を修正した。
  - 「詳細な仕様」に、説明文が第六〜九世代（SV含む）で一貫して「きらいなあじだとこんらんする」と
    明記されている旨（一次情報のwikiページで確認済み）を追記。
  - 「備考」に、あまい味が嫌いな性格（ゆうかん・れいせい・のんき・なまいき＝はやさが上がりにくい性格）
    を追記（プレースホルダのコメントのみだった箇所を埋めた）。

## 実装計画書
- `docs/plan/items/マゴのみ.md` を修正した（従来はイア/フィラ/ウイ/バンジのみに追従して汎用関数
  `heal_on_quarter_hp`（こんらん未対応）のまま据え置く計画になっていたが、これを撤回し他の4種と
  同じ専用ラッパー方式に切り替える計画へ更新した）。
  - `マゴのみ_heal_on_quarter_hp` 専用ハンドラを追加し、`confuse_natures=("ゆうかん", "れいせい", "のんき", "なまいき")`
    を渡す方針を明記。
  - 性格とこんらんの対応関係の根拠（`data/nature.py` の `NATURE_MODIFIER` ですばやさ補正が0.9の性格を
    突合）を明記。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/item.py`
  - `マゴのみ_heal_on_quarter_hp` を新規追加（`_heal_berry` に `confuse_natures=("ゆうかん", "れいせい", "のんき", "なまいき")` を渡す）。
  - 汎用の `heal_on_quarter_hp`（こんらん判定なし）はどこからも参照されなくなったため削除した。
  - `python scripts/sort_handlers.py src/jpoke/handlers/item.py` を実行済み。
- `src/jpoke/data/item.py`
  - `"マゴのみ"` の `ON_HP_CHANGED` ハンドラ登録を `h.heal_on_quarter_hp` から `h.マゴのみ_heal_on_quarter_hp` に変更。
  - **不備の内容**: 従来の実装はマゴのみが本来持つ「あまい味が嫌いな性格（ゆうかん・れいせい・のんき・
    なまいき）はこんらんする」効果が未実装だった（イア・ウイ・フィラ・バンジのみは既に専用ラッパーで
    対応済みだったが、マゴのみだけ汎用関数のまま取り残されていた）。今回のレビューで修正した。

## テスト
- `tests/test_item.py` に以下を追加した。
  - `test_マゴのみ_はやさが上がりにくい性格でこんらんする`（`ゆうかん`・`れいせい`・`のんき`・`なまいき` をパラメタライズ）
  - `test_マゴのみ_それ以外の性格ではこんらんしない`（`まじめ` で発動してもこんらんしないことを確認）
- 既存の共通パラメタライズテスト（`test_HP4分の1以下で回復するきのみ` 等）はそのまま変更なし。
- `python scripts/sort_tests.py tests/test_item.py` → `python scripts/generate_test_list.py` を実行済み。
- `python -m pytest tests/ -v` を実行し、3088 passed, 1 skipped で全件パス（ログ: `.loop/test_logs/マゴのみ.log`）。
