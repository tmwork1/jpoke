# ノーマルジュエル レビュー結果

## 仕様書
- 変更なし（`docs/spec/items/ノーマルジュエル.md` は一次情報と照合済みで誤り・欠落なし）

## 実装計画書
- 新規作成（既存の `docs/plan/items/ノーマルジュエル.md` は仕様書未整備・テスト未実装と誤記されていたため全面更新）
  - priority: `ON_CALC_POWER_MODIFIER` は turn.md 未掲載イベントのため、既存の同種ハンドラ（`のろいのおふだ_modify_power_by_type` 等）に倣いデフォルト priority を採用する旨を明記
  - `no_fling` フラグ追加方針を明記

## 実装（handlers/ / data/）
- `src/jpoke/handlers/item.py` の `ノーマルジュエル_modify_power_by_type`: 威力補正倍率が `6144`（1.5倍、第五世代の値）になっていたバグを `5325`（1.3倍、第六世代以降=現行仕様の正確な値）に修正
- `src/jpoke/data/models.py` の `ItemData` に `no_fling: bool = False` フィールドを追加
- `src/jpoke/data/item.py` の `ノーマルジュエル` エントリに `no_fling=True` を追加
- `src/jpoke/handlers/move_attack.py` の `なげつける_check_item` を修正し、`item_data.no_fling` が真の場合もなげつけるを失敗させるようにした
  - 仕様書に明記されている「ジュエルを持つポケモンはなげつけるを使用しても失敗し、投げることができない」（表示上のなげつける威力は30だが実際は投げられない）という既知の例外挙動が未実装だったため
- `docs/spec/moves/なげつける.md` の失敗条件にジュエル全種の例外を追記
- 上記修正後 `python scripts/sort_handlers.py src/jpoke/handlers/item.py`、`python scripts/sort_data/sort_items.py` を実行

## テスト
- `test_ノーマルジュエル_ノーマル技威力1_5倍` を `test_ノーマルジュエル_ノーマル技威力1_3倍` に改名し、期待値を `6144` → `5325` に修正
- `test_ノーマルジュエル_なげつけるが失敗しアイテムを消費しない` を新規追加（なげつけるが失敗し、ダメージを与えずアイテムも消費されないことを確認）
- `python scripts/sort_tests.py tests/test_item.py`、`python scripts/generate_test_list.py` を実行
- `python -m pytest tests/ -v` で全2985件（+1 skipped）がパスすることを確認（ログ: `.loop/test_logs/ノーマルジュエル.log`）
