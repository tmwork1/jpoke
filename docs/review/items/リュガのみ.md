# リュガのみ レビュー結果

## 仕様書
- `docs/spec/items/_ランク上昇ピンチきのみ.md` に既存の記述あり。一次情報（ポケモンWiki「リュガのみ」）と照合し、内容（HP1/4以下でぼうぎょ+1、ランク最大時不発、じきゅうりょく/くだけるよろい/みずがための後に発動判定、ほおばる消費時の特例）に誤り・欠落なし。変更なし。

## 実装計画書
- 既存の `docs/plan/items/リュガのみ.md` が旧式（`_boost_on_quarter_hp` 共通処理導入前のpriority/onceベースの疑似コード）のままで、実際の実装（共通ヘルパー経由、こんらんの自傷ダメージ除外対応済み）と乖離していたため、`チイラのみ.md` と同じ最新フォーマットに更新した。
  - 効果概要にほおばる等の強制発動・こんらんの自傷ダメージ除外の記述を追加
  - ハンドラロジックを実際の `_boost_on_quarter_hp(battle, ctx, value, stat="def", amount=+1)` 呼び出しに合わせて記載
  - テスト観点にランク上限不発・こんらんの自傷ダメージ除外を追加

## 実装（handlers/ / data/）
- `src/jpoke/handlers/item.py` の `リュガのみ_boost_defense` は共通処理 `_boost_on_quarter_hp` を呼び出しており、他のピンチきのみ（チイラのみ・カムラのみ・ヤタピのみ・ズアのみ）と同一パターン。こんらんの自傷ダメージ除外（`ctx.hp_change_reason == "self_attack"`）も反映済み。
- `src/jpoke/data/item.py` の登録内容（`fling_power=10`, `subject_spec="target:self"`）も仕様・他の同系統きのみと一致。
- 変更なし（修正不要と判断）。

## テスト
- `tests/test_item.py` には既にリュガのみを含むパラメトライズテスト（`test_HP25以下でランク上昇するきのみ`、`test_HP25以下でランク上昇するきのみ_こんらんの自傷では発動しない`）が存在し、基本発動・こんらんの自傷ダメージ除外は網羅済みだった。
- 一方で、他の同系統きのみ（チイラのみ・カムラのみ・ヤタピのみ・ズアのみ）には個別に「ランクが最大のとき発動しない」テストが存在するのに、リュガのみのみ欠落していたため `test_リュガのみ_ぼうぎょランクが最大のとき発動しない` を追加した。
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新、`python -m pytest tests/ -v` で全3116件パス（1件skip）を確認した。
