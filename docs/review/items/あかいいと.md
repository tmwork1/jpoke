# あかいいと レビュー結果

## 仕様書
- 変更なし（`docs/wiki/items/あかいいと.html` の一次情報と照合し、バトル仕様・遺伝仕様ともに誤り・欠落なし）
- 備考: 一次情報に記載の「ふしぎなバリアに守られている相手はあかいいとの効果を防ぐ」はマックスレイドバトル固有の仕様であり、本プロジェクトの対象（ポケモンチャンピオンズのシングルバトル）には該当しないため未実装のままで問題なし

## 実装計画書
- 修正: 登録コード例に存在しない `consumable=False` フィールドが記載されていたため削除し、`ItemData` に `consumable` フィールドが存在しない旨と「消費処理を呼び出さないことで消費されないことを担保する」旨の注記を追加

## 実装（handlers/ / data/）
- 変更なし
  - `src/jpoke/handlers/item.py` の `あかいいと_infatuate_foe`（`ON_VOLATILE_START`、value が「メロメロ」の場合のみ `battle.foe(ctx.source)` に `volatile_manager.apply` でメロメロを付与）は仕様書・計画書と一致
  - `src/jpoke/data/item.py` の `あかいいと` エントリ（`fling_power=10`、`ON_VOLATILE_START` ハンドラ登録、`subject_spec="source:self"`）も仕様通り
  - どんかん・アロマベールによるブロックは `ON_BEFORE_APPLY_VOLATILE` の既存の `_prevent_volatile` 経由で担保されており、あかいいと側の追加実装は不要

## テスト
- 追加: `test_あかいいと_アロマベール持ちには付与されない`（相手がアロマベールを持つ場合にメロメロが付与されないことを確認。既存はどんかんのみ検証しており、仕様書に明記されたもう一方の無効化特性のカバレッジが欠けていたため追加）
- 既存4件（アイテム消費されない／どんかん持ちには付与されない／メロメロ被弾で相手もメロメロ／他の揮発状態では発動しない）は実装と一致しており変更なし
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新
- `python -m pytest tests/ -v` 実行結果: 2654 passed, 1 skipped（`.loop/test_logs/あかいいと.log` に保存）
