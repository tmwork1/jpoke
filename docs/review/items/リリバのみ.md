# リリバのみ レビュー結果

## 仕様書
- 変更なし。`docs/spec/items/_タイプ半減きのみ.md`（タイプ半減きのみ共通仕様書）内の「リリバのみ」節が
  一次情報（ポケモンWiki `docs/wiki/items/リリバのみ.html`）と一致していることを確認した。
  - 効果:効果バツグンのはがねわざを受けたときに消費して、1度だけダメージを1/2にする。
  - 詳細な仕様: はめつのねがいの攻撃に対しては、第四世代までは発動しない。第五世代以降は発動する。

## 実装計画書
- 全面更新（`docs/plan/items/リリバのみ.md`）。旧内容は「仕様書未整備・テスト未実装」という
  古い定型文のままだったため、共通仕様書の参照先・イベント（`ON_CALC_DAMAGE_MODIFIER` /
  `LethalEvent.ON_BEFORE_HIT`）・priority根拠（`docs/spec/turn.md` に個別記載がないため既存の
  同種きのみハンドラに合わせた既定値を使う旨）を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/item.py` の `リリバのみ` は `ON_CALC_DAMAGE_MODIFIER` ハンドラと
  `lethal_handlers`（`LethalEvent.ON_BEFORE_HIT`）を登録済みで、五十音順の並びも正しい。
- `handlers/item.py` の `リリバのみ_modify_super_effective_damage` は共通処理
  `_modify_super_effective_damage(..., type_="はがね", modifier=2048/4096)` を正しく呼び出している。
  この共通処理は直前の並行レビュー（ヨロギのみ・ナモのみ）で判定条件（`> 4096`）と
  消費処理（`_announce_and_consume_item`）の不具合が修正済みであり、リリバのみにも
  正しく適用されていることを確認した。
- `handlers/lethal.py` の `リリバのみ_resist_steel` も共通処理 `_type_resist_berry(..., "はがね")`
  を正しく呼び出しており、修正済みの判定条件（`> 4096`）を利用している。

## テスト
- 変更なし。`tests/test_item.py` の `test_タイプ半減実` パラメータに
  `("リリバのみ", "はがね", "ピッピ")` が存在する。
  - 直前の「ヨロギのみ」レビューで判明した誤り（旧パラメータの `マリル` は みず/フェアリー複合で
    はがねタイプに対し相殺され等倍になってしまう）は、単タイプのフェアリーである `ピッピ` に
    既に差し替え済みであることを確認した（はがねタイプの技はフェアリー単タイプに対し
    素直に効果バツグンになる）。
  - `damage_modifier == 2048`（1/2倍）およびアイテム消費（`not has_item()`）を検証しており、
    追加の修正は不要と判断した。
- `python scripts/sort_tests.py tests/test_item.py` を実行したが差分なし（既にソート済み）。
- `python scripts/generate_test_list.py` で `docs/tests/` を更新した。
- `python -m pytest tests/ -v` で全件パス（3151 passed, 1 skipped）を確認した。
  結果は `.loop/test_logs/リリバのみ.log` に保存済み。
