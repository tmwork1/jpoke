# するどいツメ レビュー結果

## 仕様書
- 変更なし。一次情報（ポケモンWiki「するどいツメ」）と照合したが、記載内容（急所ランク+1、なげつける威力80、ピントレンズとの比較）に誤り・欠落なし。

## 実装計画書
- 変更なし。`Event.ON_CALC_CRITICAL_RANK` / `subject_spec="attacker:self"` / priority 100 は既存の同種ハンドラ（ピントレンズ・ちきゅうなげ由来の急所ランク系特性・揮発性状態）と揃っており妥当。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/item.py` の `するどいツメ_boost_critical_rank`（急所ランク+1を返すのみ）、`src/jpoke/data/item.py` の `ItemData(fling_power=80, handlers={Event.ON_CALC_CRITICAL_RANK: ...})` は仕様書・計画書と一致。

## テスト
- 変更なし。`tests/test_item.py::test_するどいツメ_急所ランク加算` が既に存在し、急所ランク+1を検証している。過不足なし。

## その他
- `docs/progress/item.md` の するどいツメ 行が「テスト -」「レビュー -」「効果 ?」のまま未更新だったため、実態（テスト実装済み・レビュー済み）に合わせて更新した。
