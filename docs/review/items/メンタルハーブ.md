# メンタルハーブ レビュー結果

## 仕様書
- 変更なし。一次情報（`docs/wiki/items/メンタルハーブ.html`）と `docs/spec/items/メンタルハーブ.md` を照合し、内容は正確であることを確認した。

## 実装計画書
- 新規作成に近い形で全面的に修正した。
  - 従来の計画書は対象揮発性状態として誤って「なりきり」「さしおさえ」を挙げていた（正しくは「メロメロ」「かいふくふうじ」）。
  - `subject_spec` を実装済みの `"source:self"` に修正し、`VolatileManager.apply()` が
    `EventContext(source=target)` で `ON_VOLATILE_START` を発火する仕様を明記した。
  - なげつけるでの追加効果（`handlers/move_attack.py` の `なげつける_apply_item_effect`）についても記載を追加した。
  - どんかんとの発動順・バトンタッチでのかいふくふうじ引き継ぎ（第6・7世代限定）は現行世代（SV）を
    対象とする本プロジェクトのスコープ外として明記した。

## 実装（handlers/ / data/）
- バグを修正した。
  - `handlers/item.py` の `メンタルハーブ_cure_mental_volatile` が対象とする揮発性状態から
    「メロメロ」「かいふくふうじ」が漏れていたため追加した
    （修正前: `いちゃもん・アンコール・かなしばり・ちょうはつ` のみ）。
  - `handlers/move_attack.py` の `なげつける_apply_item_effect` 内のメンタルハーブ分岐にも同様の漏れがあり、
    同じく「メロメロ」「かいふくふうじ」を追加した。docstring も合わせて修正した。
  - `data/item.py` のハンドラ登録（`ON_VOLATILE_START` / `subject_spec="source:self"` / `fling_power=10`）は
    仕様通りで変更なし。

## テスト
- `tests/test_item.py` を修正した。
  - `test_メンタルハーブ_いちゃもんを即解除` は既存のまま維持。
  - 新規にパラメータ化テスト `test_メンタルハーブ_対象の揮発状態を即解除` を追加し、
    メロメロ・アンコール・かなしばり・ちょうはつ・かいふくふうじの全対象で即解除されることを確認。
  - `test_メンタルハーブ_対象外の揮発状態には反応しない`（こんらん）は既存のまま維持。
- `tests/moves_attack/test_move_na.py` に、なげつけるでのメンタルハーブ追加効果のうち
  抜けていた「メロメロ」「かいふくふうじ」を解除するテストを追加した
  （`test_なげつける_メンタルハーブでメロメロを解除する`、`test_なげつける_メンタルハーブでかいふくふうじを解除する`）。
- `python scripts/sort_tests.py tests/test_item.py tests/moves_attack/test_move_na.py` でソート、
  `python scripts/generate_test_list.py` でテスト一覧を更新、
  `python -m pytest tests/ -v` で全3098件（1スキップ）がパスすることを確認した
  （ログ: `.loop/test_logs/メンタルハーブ.log`）。
