# ズアのみ レビュー結果

## 仕様書
- 変更なし。`docs/spec/items/_ランク上昇ピンチきのみ.md` の「ズアのみ」節が既に存在し、一次情報（`docs/wiki/items/ズアのみ.html`）と照合した結果、内容（HP1/4以下でとくぼう+1・消費、とくぼうランク最大時は不発、詳細な仕様はチイラのみを参照）に誤りは無かった。

## 実装計画書
- 変更なし。`docs/plan/items/ズアのみ.md` の内容（`Event.ON_HP_CHANGED` / `subject_spec="target:self"` / priority100 / once消費）は仕様書と一致しており、実際の実装（共通ヘルパー `_boost_on_quarter_hp` を利用）とも整合していた。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/item.py` の `"ズアのみ"` エントリは `Event.ON_HP_CHANGED` に `h.ズアのみ_boost_spdef`（`subject_spec="target:self"`）を登録済みで、優先度は `ItemHandler` のデフォルト値100（`docs/spec/turn.md` の「きのみの発動」優先度100と一致）。
- `src/jpoke/handlers/item.py` の `ズアのみ_boost_spdef` は共通処理 `_boost_on_quarter_hp(battle, ctx, value, stat="spd", amount=+1)` を呼び出しており、チイラのみ・カムラのみ・ヤタピのみ・リュガのみと同じ実装パターンで、HP1/4以下判定・ランク最大時の不発・消費処理（`_announce_and_consume_item`）が正しく行われている。修正不要と判断。

## テスト
- `tests/test_item.py` に既存の `test_HP25以下でランク上昇するきのみ`（パラメータ化）でズアのみの基本発動（HP1/4以下でとくぼう+1・消費）は確認済みだったが、「とくぼうランクが最大のとき発動しない」個別ケース（カムラのみには既存）がズアのみには無かったため、`test_ズアのみ_とくぼうランクが最大のとき発動しない` を追加した。
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` で一覧更新、`python -m pytest tests/ -v` を実行し全件（2881 passed, 1 skipped）成功を確認（ログ: `.loop/test_logs/ズアのみ.log`）。

## 進捗表
- `docs/progress/item.md` のズアのみ行を更新: テスト列 `-` → `x`、レビュー列 `-` → `x`、効果列 `?` → 「HPが最大HPの1/4以下になった瞬間にとくぼう+1して消費する。すでにとくぼうランクが最大のときは発動せず消費されない。なげつける威力10。」
