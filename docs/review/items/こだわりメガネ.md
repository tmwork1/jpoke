# こだわりメガネ レビュー結果

## 仕様書
- 変更なし。`docs/spec/items/_こだわり系.md` に既存の記載があり、一次情報（`docs/wiki/items/こだわりメガネ.html`）と一致していることを確認した。
  - 効果: とくこう1.5倍・最初に選択した技しか使用不可（こだわりロック）
  - なげつける威力: 10
  - 詳細な仕様: こだわり系共通仕様を参照

## 実装計画書
- 変更なし。`docs/plan/items/こだわりメガネ.md` は仕様書・実装内容と整合していることを確認した。

## 実装（handlers/ / data/）
- 変更なし。以下を確認した。
  - `src/jpoke/data/item.py` の `"こだわりメガネ"` エントリ: `fling_power=10`、`Event.ON_CALC_ATK_MODIFIER`（`attacker:self`）で `こだわりメガネ_boost_special`、`Event.ON_MOVE_END`（`attacker:self`）で共通の `こだわり_lock_move` を登録。
  - `src/jpoke/handlers/item.py` の `こだわりメガネ_boost_special`: 特殊技のときのみ `apply_fixed_modifier(value, 6144)`（×1.5）を適用。
  - こだわりロック本体は `こだわり_lock_move`（アイテム側）と `こだわり_restrict_commands`（`handlers/volatile.py`、こだわりスカーフ・ハチマキ・メガネ共通のボラタイル）で実装されており、交代でロック解除される。
  - こだわり系3アイテム（スカーフ・ハチマキ・メガネ）は同一パターンで実装されており、整合性を確認した。

## テスト
- 変更なし。`tests/test_item.py` に以下の既存テストがあり、実装と一致していることを確認した。
  - `test_こだわり系_交代でロック解除`（パラメータ化、こだわりメガネ含む）
  - `test_こだわり系_技ロック`（パラメータ化、こだわりメガネ含む）
  - `test_こだわり系_火力補正`（こだわりメガネの物理技無補正/特殊技1.5倍を確認）
- `python scripts/sort_tests.py tests/test_item.py` と `python scripts/generate_test_list.py` を実行したが、差分なし（既にソート・生成済みの状態だった）。
- `python -m pytest tests/ -v` を実行し、2839 passed, 1 skipped（無関係）で全件パスを確認した。ログは `.loop/test_logs/こだわりメガネ.log` に保存。

## 進捗表
- `docs/progress/item.md` の こだわりメガネ 行のレビュー列を `-` → `x` に更新し、効果列に説明文を追記した。
