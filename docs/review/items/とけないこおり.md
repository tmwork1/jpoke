# とけないこおり レビュー結果

## 仕様書
- 新規作成: `docs/spec/items/とけないこおり.md` を追加した。
  - 効果（こおりタイプの技の威力を1.2倍、なげつける威力30）、詳細仕様（タイプが変わる技・フリーズスキン特性でタイプが変わった技も対象になる旨）、関連項目を記載。
  - `docs/spec/items/_タイプ強化アイテム.md`（旧・アイテム横断調査ファイル）内の記述と内容は齟齬なし。

## 実装計画書
- `docs/plan/items/とけないこおり.md` を修正した。
  - 仕様書未作成前提の記述を削除し、`docs/spec/items/とけないこおり.md` を参照するよう更新。
  - `くろおび.md` 等の既存プランと同様に、実装方針（`ON_CALC_POWER_MODIFIER` ハンドラ、priority、`subject_spec`、`fling_power`）を明記。
  - 「今後の対応」を「特になし」に更新（テスト未実装の記述は誤りだったため修正）。

## 実装（handlers/ / data/）
- 変更なし。
  - `src/jpoke/data/item.py` の `とけないこおり` エントリは `ON_CALC_POWER_MODIFIER` ハンドラ（`subject_spec="attacker:self"`）と `fling_power=30` を正しく登録済み。
  - `src/jpoke/handlers/item.py` の `とけないこおり_modify_power_by_type` は共通ヘルパー `_modify_power_by_type(ctx.move, value, type_="こおり", modifier=4915)` を呼び出しており、他のタイプ強化アイテム（くろおび等）と実装パターンが一致している。

## テスト
- 変更なし。
  - `tests/test_item.py` の `test_タイプ強化アイテム`（パラメトライズ）に `("とけないこおり", "こおり")` が既に含まれており、威力補正（`power_modifier == 4915`）を検証済み。
  - `python -m pytest tests/ -v` で全3140件（+skip 1件）がパスすることを確認（ログ: `.loop/test_logs/とけないこおり.log`）。

## 進捗表
- `docs/progress/item.md` の `とけないこおり` 行を更新（レビュー列を `-` → `x`、効果列を `?` から具体的な説明文に変更）。
