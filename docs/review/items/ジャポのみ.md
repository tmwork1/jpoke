# ジャポのみ レビュー結果

## 仕様書
- 変更なし（一次情報（docs/wiki/items/ジャポのみ.html）と照合済み。効果・詳細な仕様の記述に誤り・欠落なし）

## 実装計画書
- ハンドラロジックの疑似コードを実装の実態（`_retaliate_on_category` 共通関数、レンブのみと共有）に合わせて修正
- 「マジックガードなどで実際にダメージが通らなかった場合（攻撃者がすでにひんしの場合を含む）は発動・消費しない」という仕様上の注意点を明記
- テスト観点に「攻撃者がマジックガード持ちの場合は発動せず、アイテムも消費されないこと」を追加

## 実装（handlers/ / data/）
- `src/jpoke/handlers/item.py` の `_retaliate_on_category`（ジャポのみ・レンブのみ共通処理）を修正。
  従来は `battle.modify_hp(ctx.attacker, r=-1/8)` の戻り値を確認せず常に `_announce_and_consume_item` を呼んでいたため、
  攻撃者がマジックガード持ちの場合や既にひんしだった場合でも発動・消費されてしまうバグがあった。
  `_boost_on_attack_category` や `_apply_contact_item_chip`（ゴツゴツメット等）と同様に、
  `modify_hp` の戻り値（実際に変化したHP量）が真の場合のみ発動・消費するよう修正した。
- `data/item.py` の変更はなし（登録内容は既に仕様通り）。
- `python scripts/sort_handlers.py src/jpoke/handlers/item.py` を実行済み。

## テスト
- `test_ジャポのみ_マジックガードには発動しない` を追加（攻撃者がマジックガード持ちの場合、ダメージを与えず、アイテムも消費されないことを確認）。
- 既存の `test_ジャポのみ_物理被弾で攻撃者にダメージ` / `test_ジャポのみ_特殊技では発動しない` は変更なし。
- `python scripts/sort_tests.py tests/test_item.py` でソート済み。
- `python scripts/generate_test_list.py` で `docs/tests/` を更新済み。
- `python -m pytest tests/ -v` 実行結果: 2857 passed, 1 skipped（ジャポのみ関連は全てPASSED）。ログは `.loop/test_logs/ジャポのみ.log` に保存。
