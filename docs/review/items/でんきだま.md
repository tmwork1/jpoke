# でんきだま レビュー結果

## 仕様書
- 変更なし（一次情報 `docs/wiki/items/でんきだま.html` と照合し、既存の `docs/spec/items/でんきだま.md` の記述に誤り・欠落がないことを確認した）

## 実装計画書
- 以下を補足した:
  - 実装ゴールに「キョダイマックスするピカチュウ（`ピカチュウ(キョダイ)`）も有効」「こんらんの自傷ダメージ（`_こんらん`）には影響しない（Champions仕様＝第五世代以降準拠）」を明記
  - 疑似コードを実装（対象フォームの明示的集合 + `_こんらん` 除外）に合わせて更新
  - 「事前に固定する仕様値」に対象フォームの正確な集合と除外技を明記
  - テスト観点をピカチュウ(キョダイ)の有効確認・ピチュー/ライチュウの無効確認に更新

## 実装（handlers/ / data/）
- `src/jpoke/handlers/item.py` の `でんきだま_boost_atk` を修正:
  - 対象を `{"ピカチュウ"}`（完全一致のみ）から `{"ピカチュウ", "ピカチュウ(キョダイ)"}` に拡張し、キョダイマックスするピカチュウにも効果が及ぶよう修正（仕様書「詳細な仕様」の記載に対応、これまで未対応だった）
  - こんらんの自傷ダメージ（`ctx.move.name == "_こんらん"`）を対象から除外。こだわりハチマキ等の既存実装（`こだわりハチマキ_boost_physical`）と同じ方針で、Champions仕様（第五世代以降）に合わせて自傷ダメージにはでんきだまの補正がかからないよう修正（これまで未対応で、確率的にダメージ計算が誤っていたバグ）
  - `data/item.py` の登録内容自体に変更はなし
  - `python scripts/sort_handlers.py src/jpoke/handlers/item.py` を実行済み

## テスト
- `tests/test_item.py` を修正:
  - `test_専用アイテム攻撃補正` のパラメータに `("でんきだま", "ピカチュウ(キョダイ)", "たいあたり", 8192)` を追加
  - `test_でんきだま_ピチューライチュウには効果なし`（ピチュー・ライチュウ）を新規追加
  - `test_でんきだま_こんらん自傷ダメージには補正なし` を新規追加
  - `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新
  - `python -m pytest tests/ -v` を実行し、2955 passed, 1 skipped で全件パス（`.loop/test_logs/でんきだま.log` に保存）
