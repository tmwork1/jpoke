# ナナシのみ レビュー結果

## 仕様書
- `docs/spec/items/_状態回復きのみ.md`（カゴのみ・クラボのみ・チーゴのみ・ナナシのみ・モモンのみ・キーのみ・ラムのみの共通仕様書）に既存記載あり。一次情報（`docs/wiki/items/ナナシのみ.html`）と照合し「効果:こおり状態を回復させる」「なげつけるの効果(威力10)」の記載に誤りがないことを確認した。変更なし。

## 実装計画書
- `docs/plan/items/ナナシのみ.md` は既存だったが、回復対象が誤って「こんらん（揮発性状態）」と記載され、`Event.ON_TURN_END` 単独でこんらんを解除する疑似コードになっていた（一次情報・仕様書・実装はいずれも「こおり」状態異常の回復）。キーのみの計画書からの流用ミスとみられる。以下の内容に修正した。
  - 効果概要・実装仕様の対象を「こんらん解除」→「こおり状態の回復」に修正
  - 実装が実際に登録している4イベント（`ON_APPLY_AILMENT` / `ON_ITEM_ENABLED` / `ON_FORCE_BERRY_TRIGGER` / `ON_TURN_END`）を明記（旧計画書は `ON_TURN_END` のみの記載だった）
  - ハンドラ疑似コードを実装の `_cure_ailment_berry` / `_cure_ailment_berry_on_apply` 呼び出しに合わせて更新
  - テスト観点に「こおり付与直後の即時回復」を追加

## 実装（handlers/ / data/）
- `src/jpoke/handlers/item.py` の `ナナシのみ_cure_freeze` / `ナナシのみ_cure_freeze_on_apply`、`src/jpoke/data/item.py` の `"ナナシのみ"` エントリはいずれも「こおり」を正しく対象としており、カゴのみ・クラボのみ・チーゴのみと同一パターン（`ON_TURN_END` / `ON_APPLY_AILMENT` / `ON_ITEM_ENABLED` / `ON_FORCE_BERRY_TRIGGER`、priority 50）で実装済み。修正不要（変更なし）。
- 誤りがあったのは計画書のみで、実装自体は一次情報・仕様書と一致していた。

## テスト
- `tests/test_item.py` に既存だったのは以下2件のみ（パラメータ化テスト内）:
  - `test_状態異常回復きのみ_ターン終了で回復`（ナナシのみ／こおり）
  - `test_状態異常回復きのみ_対象外状態では発動しない`（ナナシのみ／まひでは発動しない）
- 兄弟アイテムのカゴのみ・クラボのみ・チーゴのみには存在する「付与直後の即時回復（`ON_APPLY_AILMENT`）」を確認するテストがナナシのみに欠けていたため、`test_ナナシのみ_こおり付与直後に即時回復する`（`battle.ailment_manager.apply` でこおりを直接付与し即座に回復・消費されることを確認。こおりを100%付与できる技が存在しないため直接付与で代替）を追加した。
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新し、`python -m pytest tests/ -v` を実行した。ナナシのみ関連のテスト（新規含む）は全てパス。ただし `test_ノーマルジュエル_ノーマル技威力1_5倍` が1件失敗しているが、これは同時進行中の別カテゴリのループタスクが `src/jpoke/handlers/item.py` の `ノーマルジュエル` ハンドラを未コミットで変更中のためであり、本タスク（ナナシのみ）の変更とは無関係（ログ: `.loop/test_logs/ナナシのみ.log`）。
