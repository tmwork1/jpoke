# あついいわ レビュー結果

## 仕様書
- 新規作成（`docs/spec/items/あついいわ.md` が存在しなかったため、一次情報を基に作成）
  - にほんばれ状態にする技使用時に持続ターンが8ターンに伸びる効果
  - 特性ひでり・ひひいろのこどう発動時にも持続ターンが8ターンに伸びる効果（第六世代以降）
  - なげつける威力60、ウッウロボ素材情報、ダイマックスわざでの適用、じょうきょう画面表示の詳細仕様を記載

## 実装計画書
- 修正した内容
  - `ON_MODIFY_DURATION` が `docs/spec/turn.md` に未掲載であることを明記し、priority をデフォルト値100とする根拠として
    既存の同種ハンドラ（さらさらいわ・しめったいわ・つめたいいわ・ひかりのねんど・グランドコート）が
    いずれも priority を明示指定していない事実を追記
  - にほんばれ（技）・ひでり／ひひいろのこどう（特性）のどちらの発生源でも `WeatherManager.apply()` が
    唯一の発動経路であるため区別なく延長されることを明記

## 実装（handlers/ / data/）
- 変更なし
  - `src/jpoke/handlers/item.py` の `あついいわ_resolve_field_count`（`_resolve_field_count(value, "はれ", additonal_count=3)`）
  - `src/jpoke/data/item.py` の `ON_MODIFY_DURATION` ハンドラ登録（`subject_spec="source:self"`）
  - いずれも仕様・計画書と一致し、さらさらいわ等の兄弟アイテムと同一パターンで実装済み

## テスト
- 変更なし
  - `tests/test_item.py::test_天候延長アイテム`（パラメータ化: さらさらいわ／あついいわ／しめったいわ／つめたいいわ）で
    `weather_manager.apply` 経由の持続ターン延長（5→8）を検証済み。技・特性いずれの発生源でも
    `WeatherManager.apply()` を通るため、このテストで両方のケースをカバーしている
  - `python scripts/sort_tests.py tests/test_item.py`、`python scripts/generate_test_list.py`、
    `python -m pytest tests/ -v` を実行し、2654 passed, 1 skipped で全テスト成功を確認
