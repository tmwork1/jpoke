# こうかくレンズ レビュー結果

## 仕様書
- 変更なし（一次情報（ポケモンWiki）と照合し、命中率1.1倍・なげつける威力10の記載に誤りなし）

## 実装計画書
- 変更なし（イベント `ON_MODIFY_ACCURACY`・subject_spec `attacker:self`・固定倍率4506/4096の内容は仕様書と実装に一致）

## 実装（handlers/ / data/）
- 変更なし（`handlers/item.py` の `こうかくレンズ_modify_accuracy` が `apply_fixed_modifier(value, 4506)` で命中率を1.1倍にしており、`data/item.py` の `ON_MODIFY_ACCURACY` 登録・`subject_spec="attacker:self"`・`fling_power=10` も仕様と一致。既存の特性しょうりのほし・アイテムパンチグローブと同一の4506係数を使用しており、四捨五入も含め整合していることを確認）

## テスト
- `tests/test_item.py` に以下2件を追加
  - `test_こうかくレンズ_命中率が1_1倍になる`: さいみんじゅつ使用時、命中率が `move.accuracy * 4506 // 4096` になることを確認
  - `test_こうかくレンズ_非所持では命中率が変化しない`: 持っていない場合は命中率が変化しないことを確認
- `scripts/sort_tests.py tests/test_item.py` でソート、`scripts/generate_test_list.py` でテスト一覧を更新
- `python -m pytest tests/ -v` で全2838件パス（1件skip、既存の無関係なskip）を確認、ログを `.loop/test_logs/こうかくレンズ.log` に保存
