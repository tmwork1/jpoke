# パンチグローブ レビュー結果

## 仕様書
- 変更なし。一次情報（Wiki）と照合し、効果・威力補正倍率・対象パンチ技一覧・詳細仕様（てつのこぶしとの重複、えんかく類似の接触判定無効化、わるいてぐせ／もふもふ／かたいツメ／ふかしのこぶしへの波及）に誤り・欠落なし。

## 実装計画書
- 変更なし。イベント（`ON_CALC_POWER_MODIFIER` / `ON_CHECK_CONTACT`）、subject_spec（`attacker:self`）、priority（デフォルト100）は仕様と整合しており、`Handler` のデフォルト値とも一致することを確認した。

## 実装（handlers/ / data/）
- 変更なし。`handlers/item.py` の `パンチグローブ_boost_punch_power`（4506/4096 ≒ 1.1倍）と `パンチグローブ_negate_punch_contact`（`ON_CHECK_CONTACT` を `False` で確定させる）を確認。
- `battle.query.is_contact()` 経由で接触判定を参照する `かたいツメ`・`もふもふ`・`わるいてぐせ`・`ふかしのこぶし` の各ハンドラは `ON_CHECK_CONTACT` イベントの結果を利用しているため、パンチグローブによる接触無効化が自動的に波及することをコードレベルで確認した（仕様書の詳細仕様と一致）。
- `てつのこぶし`（`ON_CALC_POWER_MODIFIER` に別途1.2倍を乗算）とは同一イベント上で独立した固定倍率を適用するため、累積して1.32倍になる仕様と整合していることを確認した。
- `data/item.py` の登録内容（`fling_power=30`）も仕様書の「なげつける:威力30」と一致。

## テスト
- 変更なし。`tests/test_item.py` に以下2件が既存で、実装と一致していることを確認した。
  - `test_パンチグローブ_パンチ技の威力1_1倍`: `power_modifier == 4506` を検証
  - `test_パンチグローブ_パンチ技の接触判定を無効化`: ゴツゴツメット所持相手にパンチ技を当てても反動ダメージを受けないことを検証
- `python scripts/sort_tests.py tests/test_item.py` と `python scripts/generate_test_list.py` を実行したが差分なし（既にソート済み）。
- `python -m pytest tests/ -v` で3012 passed, 1 skipped を確認（`.loop/test_logs/パンチグローブ.log` に保存）。
