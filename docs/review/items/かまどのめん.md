# かまどのめん レビュー結果

## 仕様書
- 変更なし（`docs/spec/items/_おめん.md` のかまどのめん節は、いどのめん・いしずえのめんと同内容で既に正しく整備済みだった）

## 実装計画書
- 修正した: `docs/plan/items/かまどのめん.md` が「物理技のみ（特殊技は未対応）」という古い記述のまま放置されていた。
  共有ハンドラ `オーガポンのめん_boost_atk` は並行レビュー（いどのめん）で `ctx.move.category == "物理"` 判定から
  `ctx.move.is_attack` 判定（物理・特殊問わず）に修正済みだったため、計画書の効果概要・実装状態・ハンドラロジック・
  テスト観点を最新実装に合わせて更新した（いどのめん.md と同内容、対象アイテム名のみ差し替え）。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/item.py` の `オーガポンのめん_boost_atk` は既に `ctx.move.is_attack` ベースに修正済みで、
  いどのめん・かまどのめん・いしずえのめんの3アイテムが同じ関数を共有している。
  `src/jpoke/data/item.py` の `かまどのめん` 登録（`Event.ON_CALC_ATK_MODIFIER`, `subject_spec="attacker:self"`,
  `removable=False`, `fling_power=60`）も他2アイテムと同一構成で問題なし。

## テスト
- 追加した: `tests/test_item.py` の `test_専用アイテム攻撃補正` パラメータに
  `("かまどのめん", "オーガポン(かまど)", "エナジーボール", 4915)` を追加。
  いしずえのめん・いどのめんには物理技（たいあたり）・特殊技（エナジーボール）の両方のテストケースがあったが、
  かまどのめんは物理技（たいあたり）のケースしかなく、特殊技での1.2倍補正を検証するケースが欠落していたため補充した。
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` でテスト一覧を更新、
  `python -m pytest tests/ -v` を実行し、2752 passed, 1 skipped で全テスト成功を確認（ログ: `.loop/test_logs/かまどのめん.log`）。
