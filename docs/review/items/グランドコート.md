# グランドコート レビュー結果

## 仕様書
- `docs/spec/items/グランドコート.md` は既に存在していたが「## 効果」節が空だったため、一次情報（ポケモンWiki）をもとに以下を追記した。
  - 持たせたポケモンが場をエレキフィールド・グラスフィールド・ミストフィールド・サイコフィールドに変えたとき、持続ターンが8ターンに伸びる（5ターン→8ターン）
  - なげつける威力60
  - 非消費アイテムである旨
- 「詳細な仕様」節（技/特性いずれでも発動可、地面にいない状態でも効果あり、状況画面での表示挙動）は既に一次情報と一致しており修正なし。

## 実装計画書
- `docs/plan/items/グランドコート.md` は既に存在し、仕様書と整合していたため変更なし。
  - `Event.ON_MODIFY_DURATION` / `subject_spec="source:self"` / 非消費 という設計は、あついいわ・ひかりのねんど等の既存の同パターン実装と一致している。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/item.py` の `グランドコート_resolve_field_count` と `src/jpoke/data/item.py` の `"グランドコート"` エントリは既に実装済みで、仕様書・計画書と整合していることを確認した。修正なし。
  - 共通ヘルパー `_resolve_field_count` を使い、対象フィールド名（エレキ/グラス/ミスト/サイコ）に一致する場合のみ持続ターンを+3する実装で、あついいわ・ひかりのねんど等と同一パターン。
  - `subject_spec="source:self"` により、フィールドを発動させた本人がグランドコートを持っている場合のみ発動するため、「地面にいないポケモンでも効果がある」という仕様も自然に満たされている。

## テスト
- `tests/test_item.py` に既存のテスト（4種フィールドすべてで8ターン延長されること、非所持時は5ターンのままであること）があり、実装と一致していることを確認した。修正なし。
- `python -m pytest tests/ -v` で全3168件（+skip 1件）がパスすることを確認した（`.loop/test_logs/グランドコート.log`）。

## 進捗表
- `docs/progress/item.md` のグランドコート行が実装・テスト・レビュー未完了（`-`/`?`）のまま古くなっていたため、実態（仕様書・計画書・実装・テストすべて完了）に合わせて更新した。
