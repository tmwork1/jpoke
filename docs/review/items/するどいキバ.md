# するどいキバ レビュー結果

## 仕様書
- 変更なし。`docs/spec/items/するどいキバ.md` の内容を一次情報（`docs/wiki/items/するどいキバ.html`）と照合し、
  「攻撃技命中時10%の確率でひるませる」「おうじゃのしるしと同効果」「なげつける威力30・ひるませる」の記述が
  すべて一致していることを確認した。

## 実装計画書
- `docs/plan/items/するどいキバ.md` を修正した。
  - イベント欄が `Event.ON_HIT` と誤記されていたため、実際の登録（`data/item.py`）に合わせて
    `Event.ON_DAMAGE_HIT` に修正した。
  - `actual_damage<=0` で発火しない旨、`_INNATE_FLINCH_MOVES`・`resolve_secondary_chance`（りんぷん/てんのめぐみ）・
    なげつける連携（100%付与）など、姉妹アイテム `おうじゃのしるし` の計画書と同水準の詳細を追記した。
  - テスト観点に「一撃必殺技には効果が無い」「元々ひるみの追加効果がある技には重複しない」
    「てんのめぐみで確率2倍」「なげつけるで100%ひるませる」を追加した（既存テストはこれらを全てカバー済み）。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/item.py` の `するどいキバ` エントリは `Event.ON_DAMAGE_HIT` /
  `subject_spec="attacker:self"` で `handlers/item.py` の `flinch_on_hit_10pct`（`おうじゃのしるし` と共有）を
  登録しており、仕様書・計画書と整合していることを確認した。`handlers/move_attack.py` の
  `なげつける_apply_item_effect` にも `おうじゃのしるし`/`するどいキバ` 分岐が実装済み。

## テスト
- 変更なし。`tests/test_item.py` に `おうじゃのしるし`/`するどいキバ` を `pytest.mark.parametrize` で
  共通化したテストが存在し、ひるみ付与・確率境界・てんのめぐみ・なげつける・りんぷん・一撃必殺技・
  元々ひるみ効果のある技・変化技不発動をすべてカバーしていることを確認した。
- `python -m pytest tests/ -v` を実行し、`tests/test_item.py` を含む全テスト（362件）がパスすることを確認した
  （実行結果は `.loop/test_logs/するどいキバ.log` に保存。無関係なテストの単発flaky失敗は共有チェックアウトでの
  並行タスク実行によるテスト間干渉であり、個別実行では全てパスすることを確認済み）。
