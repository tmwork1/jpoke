# チイラのみ レビュー結果

## 仕様書
- 変更なし。`docs/spec/items/_ランク上昇ピンチきのみ.md` の「チイラのみ」節が一次情報（`docs/wiki/items/チイラのみ.html`）と整合していることを確認した。
  - 「こんらんの自傷ダメージを受けたときはHPが発動圏内になっても発動しない」（第五世代以降の仕様）も正しく記載済み。

## 実装計画書
- `docs/plan/items/チイラのみ.md` を修正した。
  - 旧内容は「hp_before/hp_after の閾値跨ぎ判定」という実装と異なる疑似コードのままだった（実際の実装は共有ヘルパー `_boost_on_quarter_hp` を使用し、こんらん自傷ダメージの除外・強制発動・ランク上限時不消費を扱う）。
  - 実際の共有ヘルパーのロジックに合わせて記述を更新し、priority・once の根拠、たんじゅん/じゅくせい/あまのじゃくの扱い（既存の `ON_BEFORE_MODIFY_STAT` 経由の共通処理に委譲）を明記した。
  - テスト観点に「こんらんの自傷ダメージでは発動しないこと」を追加した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/item.py` の `チイラのみ_boost_attack` は共有ヘルパー `_boost_on_quarter_hp(battle, ctx, value, stat="atk", amount=+1)` を呼び出しており、直前の「スターのみ」レビューで修正済みの `ctx.hp_change_reason == "self_attack"` 除外ロジックを反映している。
- `src/jpoke/data/item.py` の登録内容（`fling_power=10`, `Event.ON_HP_CHANGED`, `subject_spec="target:self"`）も他のピンチきのみ系アイテム（カムラのみ等）と一致しており、問題なし。

## テスト
- `tests/test_item.py` に以下を追加した。
  - `test_HP25以下でランク上昇するきのみ_こんらんの自傷では発動しない`（チイラのみ・カムラのみ・ヤタピのみ・リュガのみ・ズアのみをパラメトライズ）: こんらんの自傷ダメージ(`reason="self_attack"`)ではHPが1/4以下になっても発動せず消費されないこと、その後通常のダメージで発動することを確認。
  - `test_チイラのみ_こうげきランクが最大のとき発動しない`: こうげきランクが+6のときはHP1/4以下でも発動・消費されないことを確認（カムラのみ・ズアのみに既存する同種テストとの整合を取った）。
- `python scripts/sort_tests.py tests/test_item.py` でソート、`python scripts/generate_test_list.py` で一覧を更新し、`python -m pytest tests/ -v` で全件（2944 passed, 1 skipped）通過を確認した。

## その他
- `docs/progress/item.md` のチイラのみ行を更新（テスト・レビュー列を `x`、効果列に説明文を追加）。
- 併せて、同じ共有ヘルパーを使うカムラのみ・ズアのみの効果列に「こんらんの自傷ダメージでは発動しない」の記載が抜けていたため追記した（実装は既に正しく動作しており、記述漏れのみの修正）。
