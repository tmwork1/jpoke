# ユーザーレビュー

2026/5/9

## src/data/ の実装スタイルのばらつき
ability.pyの実装がよい
- lambda 式は使わず名前付き関数を優先する
- partial で汎用関数に全情報を注入するのではなく、最低限のパラメータ違いを残して名前付き関数に集約する


## ログ書き込み
battle.add_event_log()を使うパターンと、battle.event_logger.add()を直接呼び出すパターンが混在している。battle.add_event_log()に統一してコード量を減らす。

## Battleクラスにweather, terrain, global field, side field を設定・解除するラッパー関数を持たせる
テストやユーザーカスタマイズに便利

## テスト
### 補正係数の検証では、数値を左辺に置く
数値が見切れず読みやすい
'''
4096 == assert t.calc_damage_modifier(battle, Event.ON_CALC_DAMAGE_MODIFIER)
'''

### かたやぶり
かたやぶりによる無効化テストは、_activate_mold_breaker()を使って疑似的に再現するより、
battle.move_executor.run_move() を使ってなるべく実環境に近い条件でテストしたい。
run_move() で検証しづらいテストは_activate_mold_breaker()を使う。

### test_utils.py
汎用関数の引数の atk_idx を省略不可にして明示させる

### 補正係数の検証では、数値を左辺に置く
'''
4096 == assert t.calc_damage_modifier(battle, Event.ON_CALC_DAMAGE_MODIFIER)
'''

### attacker, defender の定義を一行にまとめる
'''
    defender = battle.actives[0]
    attacker = battle.actives[1]
    ->
    defender, attacker = battle.actives
'''

## その他
__pycache__をgit管理から外す
コード中のTODOコメントを確認
