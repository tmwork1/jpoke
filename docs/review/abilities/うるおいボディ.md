# うるおいボディ レビュー結果

## 仕様書
- 変更なし（`docs/wiki/abilities/うるおいボディ.html` の一次情報と照合し、効果・特性の仕様（発動タイミング、あめが止むターンでの不発、ねむけ/どくどくだま等との前後関係、ノーてんき・エアロック・ばんのうがさでの不発）が正確に記載されていることを確認した）

## 実装計画書
- 変更なし（`Event.ON_TURN_END` / `subject_spec="source:self"` / `priority=60` の根拠が `docs/spec/turn.md` の該当行と一致していることを確認した）

## 実装（handlers/ / data/）
- 変更なし。`handlers/ability.py` の `うるおいボディ_cure_ailment_in_rain` は `battle.weather_for(mon).rainy` で天候判定（`weather_for` がエアロック・ノーてんき・ばんのうがさを内包）し、`battle.ailment_manager.remove()` で正規の解除APIを使用しており、仕様・計画書と整合していることを確認した。`data/ability.py` の登録内容（priority=60）も `docs/spec/turn.md` の ON_TURN_END 表と一致している。

## リーサル計算
- 対象外（`docs/progress/ability.md` の「うるおいボディ」行はリーサル実装・リーサルテストとも `n/a`）

## テスト
- `tests/abilities/test_ability_a.py` の既存テスト（あめ/はれでの回復可否）に加えて以下を追加した:
  - `test_うるおいボディ_天候別に状態異常を回復する` に `おおあめ` ケースを追加（`rainy` プロパティが「あめ・おおあめ」両方を対象とすることの確認）
  - `test_うるおいボディ_状態異常がなければ何も起きない`（状態異常なし時の無害な no-op を確認）
  - `test_うるおいボディ_あめが止むターンでは発動しない`（天候カウントが当該ターンで尽きる場合に発動しないことを確認。`ON_TURN_END` 内での天候終了処理 priority=10 が うるおいボディ priority=60 より先に実行されるため）
