# バリアフリー レビュー結果

## 仕様書
- 変更なし（`docs/wiki/abilities/バリアフリー.html` の一次情報と照合し、誤り・欠落なしと確認）

## 実装計画書
- 実装方針・疑似コードを実際のコードに合わせて修正
  - `SideFieldManager._deactivate_field(field)`（private）という古い記述を、実際に使われている
    公開 API `SideFieldManager.deactivate(name)` に修正
  - priority を明記しない方針の根拠（`docs/spec/turn.md` ON_SWITCH_IN「100 バリアフリー（特性）」
    = Handler のデフォルト優先度と一致）を追記
  - 参照テストファイルを `tests/test_ability.py`（存在しない）から実在の
    `tests/abilities/test_ability_ha.py` に修正
  - テスト観点に「しんぴのまもり・しろいきり・設置技は解除しない」を追加

## 実装（handlers/ / data/）
- 変更なし。`handlers/ability.py` の `バリアフリー_remove_screens` と
  `data/ability.py` の `Event.ON_SWITCH_IN` 登録（`subject_spec="source:self"`、priority未指定＝
  デフォルト100）はいずれも仕様書・`docs/spec/turn.md` と整合していた

## リーサル計算
- 対象外（`docs/progress/ability.md` の リーサル実装/リーサルテスト列が n/a）

## テスト
- 既存の2件（`test_バリアフリー_入場で壁を解除`、`test_バリアフリー_壁がない場合アナウンスなし`）は
  実装と一致しており変更なし
- 不足していた観点を補うテストを2件追加（`tests/abilities/test_ability_ha.py`）
  - `test_バリアフリー_オーロラベールも解除される` — 仕様書「効果」に明記されたオーロラベールの
    解除を単独で検証（既存テストはリフレクター・ひかりのかべのみだった）
  - `test_バリアフリー_しんぴのまもりしろいきり設置技は解除しない` — 仕様書「備考」の
    「しんぴのまもりやしろいきり、設置技の効果は解除されない」を検証
- `python -m pytest tests/ -v` で全5157件 pass・1件 skip（バリアフリーと無関係な既存skip:
  `test_あなをほる_マグニチュードは地中の相手に命中し威力が2倍になる`）を確認。flakyな挙動は
  観測されなかった
