# スナイパー レビュー結果

## 仕様書
- 変更なし（一次情報と一致していることを確認した）

## 実装計画書
- 新規作成（`docs/plan/abilities/スナイパー.md` が存在しなかったため、仕様書と既存実装をもとに作成した）

## 実装（handlers/ / data/）
- `handlers/ability.py` の `スナイパー_boost_critical` で `getattr(ctx, "critical", False)` を `ctx.critical` に修正した（`AttackContext.critical` は必須フィールドで常に存在するため、他の急所関連ハンドラ（`core/damage.py`, `handlers/field.py`）と同様に直接参照するのが一貫している）
- ロジック自体（`Event.ON_CALC_DAMAGE_MODIFIER` で急所時に `apply_fixed_modifier(value, 6144)` を適用）は仕様書どおりで、`core/damage.py` の急所基礎倍率1.5倍と乗算されることで通常時と比べ2.25倍のダメージになることを確認した

## リーサル計算
- 対象外（`docs/progress/ability.md` の「リーサル実装」列が n/a）。`core/lethal.py` の `_calc_damage_dist` は `battle.calc_damages(..., critical=ctx.critical)` を通じて通常のダメージ計算パイプライン（`Event.ON_CALC_DAMAGE_MODIFIER` を含む）を再利用しており、スナイパー専用のリーサルハンドラを追加する必要はないことを確認した

## テスト
- 変更なし（既存の `test_スナイパー_急所時の最終ダメージを1_5倍`（確定急所技「トリックフラワー」使用、`damage_modifier == 6144` を検証）で十分カバーされていることを確認した）
- `python -m pytest tests/ -v` を実行し、4927 passed, 1 skipped（既知flakyの単発失敗は再実行で解消し、本レビューの変更とは無関係と確認）
