# サーフテール レビュー結果

## 仕様書
- 変更なし（一次情報（Wiki HTML）と内容が一致していることを確認した）

## 実装計画書
- 変更なし（仕様書・実装方針・すいすいとの対比が一致していることを確認した）

## 実装（handlers/ / data/）
- 変更なし
  - `handlers/ability.py` の `サーフテール_modify_speed` は `battle.terrain.name == "エレキフィールド"` のとき素早さを2倍にする実装で、仕様書・計画書と一致している
  - `data/ability.py` の登録（`DomainEvent.ON_CALC_SPEED`, `subject_spec="source:self"`）は、同種の `すいすい` / `すなかき` / `ゆきかき` / `ようりょくそ` と同一パターンで priority も未指定（デフォルト）で揃っている
  - 「所有者が地面におらず、エレキフィールドの効果を受けられない場合でも発動する」という仕様通り、実装は接地判定を行わず常にフィールド名のみで判定しており正しい

## リーサル計算
- 対象外（`docs/progress/ability.md` の「リーサル実装」列が `n/a` のためスキップ）

## テスト
- 変更なし
  - `tests/abilities/test_ability_sa.py` に `test_サーフテール_エレキフィールド中にS2倍` と `test_サーフテール_他フィールドでは変化なし` が存在し、実装内容と一致することを確認した
- `python -m pytest tests/ -v` で全4850件パス（1件skip）を確認した。実行中に `test_てんねん_攻撃側は防御ランク補正を無視する` や `test_ごりむちゅう_特殊技には効果がない` の単発失敗が見られたが、いずれも急所・命中判定の乱数固定漏れによる既知flakyで、該当テストのみ再実行するとパスすることを確認した（サーフテールの変更とは無関係）
