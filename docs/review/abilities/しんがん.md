# しんがん レビュー結果

## 仕様書
- 変更なし。一次情報（docs/wiki/abilities/しんがん.html）と照合し、以下の記載が正確であることを確認した。
  - ゴーストタイプに対するノーマル・かくとうタイプ技の相性が無効ではなく等倍になる
  - 他のポケモンから命中率のランク補正を下げられない
  - 相手の回避率ランクを無視して攻撃できる
  - きもったまと異なりいかくを防ぐ効果はない
  - ガチグマ(アカツキ)の専用特性

## 実装計画書
- 変更なし。仕様書と整合しており、実装対象イベント（ON_CALC_DEF_TYPE_MODIFIER / ON_BEFORE_MODIFY_STAT / ON_GET_STAT_RANK）・既存関数の再利用方針も実装内容と一致していることを確認した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/ability.py` の `しんがん` エントリは、`mold_breaker_ignorable` フラグと以下の handlers を登録済みで仕様書通りだった。
  - `Event.ON_CALC_DEF_TYPE_MODIFIER`: `しんがん_ghost_immune_bypass`（`subject_spec="attacker:self"`）
  - `Event.ON_BEFORE_MODIFY_STAT`: `するどいめ_block_ACC_drop`（`subject_spec="target:self"`）
  - `Event.ON_GET_STAT_RANK`: `するどいめ_ignore_evasion`（`subject_spec="attacker:self"`）
- `handlers/ability.py` の `しんがん_ghost_immune_bypass` は `きもったま_ghost_immune_bypass` と同一ロジック（ゴースト以外のタイプ相性を保ったまま等倍に書き換え）で、複合タイプの扱いも正しかった。いかく無効化の handler（`きもったま_block_intimidate` 相当）は登録されておらず、きもったまとの差異（いかくは防がない）も仕様通り実装されていた。

## リーサル計算
- 対象外（docs/progress/ability.md の「しんがん」行はリーサル実装・リーサルテストともに n/a）。

## テスト
- 既存の3テスト（ゴーストタイプへのノーマル技命中、相手の命中ランク低下を防ぐ、相手の回避ランクを無視する）は実装と整合しており妥当だった。
- 差異点（いかくを無効化しない）と複合タイプでの相性処理の網羅性を高めるため、`tests/abilities/test_ability_sa.py` に以下を追加した。
  - `test_しんがん_いかくは無効化されない`: きもったまと異なりいかくによる攻撃ランク低下が発生することを確認
  - `test_しんがん_かくとう技がゴースト複合に抜群`（parametrize）: かくとう技はゴースト/はがね複合に通常通り効果抜群になり、ノーマル技はゴーストに等倍になることを確認（きもったまの既存テストと対応するパターン）
- `python -m pytest tests/ -v` は 4917 passed, 1 skipped で全件パス（実行中に発生した無関係な既知flakyテスト3件の単発失敗は再実行でパスすることを確認済み）。
