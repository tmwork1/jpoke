# シンクロ レビュー結果

## 仕様書
- 変更なし。`docs/spec/abilities/シンクロ.md` は一次情報（`docs/wiki/abilities/シンクロ.html`）と照合したが、バトル効果・フィールド効果・特性の仕様（第四世代まで／第五世代以降の相違点、無効化要因、みがわり無視、かたやぶり許容等）の記載に誤り・欠落は見つからなかった。

## 実装計画書
- 変更なし。`docs/plan/abilities/シンクロ.md` は仕様書と整合しており、`Event.ON_APPLY_AILMENT` を使う実装方針・優先度の根拠（既存ハンドラ参照）も妥当。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/ability.py` の `シンクロ_return_ailment` と `src/jpoke/data/ability.py` の登録（`subject_spec="target:self"`）を確認した。
  - `_SYNC_AILMENTS = {"どく", "もうどく", "まひ", "やけど"}` で対象状態異常を限定しており、ねむり・こおりでは発動しない。
  - `ctx.is_foe_target()` と `foe is None` の二重チェックにより、`source=None`（自傷アイテム等）でも誤発動しない。
  - `battle.ailment_manager.apply()` 側の既存ロジック（タイプ耐性、既存状態異常チェック、`ON_BEFORE_APPLY_AILMENT` を介した特性・フィールド耐性判定、みがわり非考慮）を再利用しており、仕様書の無効化条件を満たしている。
  - 重複ガード等の問題は見つからなかった。

## リーサル計算
- 対象外（`docs/progress/ability.md` の「リーサル実装」列は `n/a`）。

## テスト
- `tests/abilities/test_ability_sa.py` の既存テスト（こんらんは伝染しない・状態異常が相手に伝染する・自発的な状態異常は伝染しない）を確認し、実装と整合していることを確認した。
- 以下のテストを追加した:
  - `test_シンクロ_技によるまひが相手に伝染する` — `でんじは` を使った統合テスト（技経由での付与でも正しく反応することを確認）
  - `test_シンクロ_相手が既に状態異常のときは発動しない` — 相手が既に状態異常のとき、シンクロによる状態異常の付与が失敗し伝染しないことを確認
- `python -m pytest tests/ -v` で全件（4923 passed, 1 skipped）を確認した。ログは `.loop/test_logs/シンクロ.log` に保存。
