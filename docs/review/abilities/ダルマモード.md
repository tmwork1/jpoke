# ダルマモード レビュー結果

## 仕様書
- 変更なし。一次情報（docs/wiki/abilities/ダルマモード.html）と照合したが、既存の
  `docs/spec/abilities/ダルマモード.md` は効果・特性の仕様・備考ともに一次情報と
  齟齬なく正確に記載されていた。

## 実装計画書
- 修正した。既存の `docs/plan/abilities/ダルマモード.md` は仕様書作成前のテンプレ
  ート状態のまま放置されており（実装ゴール・実装対象イベント・テスト観点が「仕様
  書作成後に記載する」のプレースホルダのまま）、既に実装・テストが完了している実
  態と乖離していた。以下の内容で全面更新した。
  - 実装状態を「実装済み」に更新。
  - 実装ゴール（完了定義）を実際のフォルムチェンジ挙動に基づいて明記。
  - `docs/spec/turn.md` L648 に基づき `Event.ON_TURN_END` priority=160 を採用す
    る根拠を明記。あわせて、一次情報の「はらぺこスイッチより早い」という記述と、
    現行の `はらぺこスイッチ` 実装（priority未指定でデフォルト100）が矛盾してい
    ることを発見したが、これは `はらぺこスイッチ` 側の実装課題であり本タスクのス
    コープ外と判断し、ダルマモード側は turn.md の値をそのまま採用する旨を記載し
    た（`はらぺこスイッチ` 自体は未修正）。
  - 「既知の制約」節を新設し、ヒヒダルマ(ガラルのすがた)のダルマモード発動後専用
    フォルムが `pokedex.json`（`zukan.json` 由来）に存在しないため実装が通常のヒ
    ヒダルマのみを対象としている旨を明記。

## 実装（handlers/ / data/）
- 修正した（コメント追加のみ、ロジック変更なし）。
  - `handlers/ability.py` の `ダルマモード_update_form` / `ダルマモード_revert_form`
    に、ヒヒダルマ(ガラルのすがた)が対象外である理由（専用フォルムデータの欠落）
    を明記する Note を追加した。
  - ロジック自体（ON_TURN_END priority=160 でのHP判定フォルムチェンジ、
    ON_SWITCH_OUT での強制解除、flags={"uncopyable", "protected", "gas_proof"}）
    は仕様書・turn.md と一致しており誤りはなかった。
- 発見した既知の制約（今回は未対応、上記の通り計画書に記録）:
  - ヒヒダルマ(ガラルのすがた)がダルマモードを隠れ特性として持つが、
    `pokedex.json` にダルマモード発動後のガラル専用フォルム（ほのおタイプ単一、
    こうげき/すばやさ上昇）のエントリが存在しない。データソース (`zukan.json`)
    がこの環境に存在せず種族値を検証できないため、新規フォルムデータの追加は
    見送った。現状、ガラルのすがたのヒヒダルマがダルマモードを持っていてもフォ
    ルムチェンジは発動しない（サイレントに何もしない）。

## リーサル計算
- 対象外（`docs/progress/ability.md` のリーサル実装列が n/a）。

## テスト
- 追加した。`tests/abilities/test_ability_ta.py` に以下のテストを1件追加。
  - `test_ダルマモード_HP1_2以下で場に出た直後は変化しない`: HPが最大HPの1/2以下
    の状態であらかじめ生成したポケモンを場に出しても、そのターンの終了前ではダ
    ルマモードへ変化せず、ターン終了後に変化することを確認する（「発動にはター
    ンの終了まで待つ必要がある」という仕様の裏付け）。
  - 既存の3テスト（ノーマル→ダルマ、ダルマ→ノーマル、交代で解除）は実装と一致
    しており変更なし。
- `python -m pytest tests/ -v` を実行し、4986 passed, 1 skipped で全テスト成功を
  確認した（ログ: `C:\Users\tmtmp\Documents\pokemon\jpoke\.loop\test_logs\ダルマ
  モード.log`）。
