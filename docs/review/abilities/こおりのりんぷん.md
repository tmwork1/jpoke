# こおりのりんぷん レビュー結果

## 仕様書
- 変更なし（一次情報（Wikiページ）と照合し、効果・特性の仕様・備考いずれも一致していることを確認した）

## 実装計画書
- 新規作成（`docs/plan/abilities/こおりのりんぷん.md` が存在しなかったため、仕様書をもとに
  新規作成した。ダメージ固定技・分類変化技・ワンダールーム・かたやぶりとの相互作用について、
  実装上なぜ自然に満たされるかの根拠を明記した）

## 実装（handlers/ / data/）
- 変更なし（`handlers/ability.py` の `こおりのりんぷん_reduce_special_damage` は
  `ctx.move.category == "special"` のときのみ `apply_fixed_modifier(value, 2048)` を適用しており
  仕様と一致。`data/ability.py` の登録も `Event.ON_CALC_DAMAGE_MODIFIER` /
  `subject_spec="defender:self"` / `flags={"mold_breaker_ignorable"}` で他の damage-reducing
  特性（ハードロック・マルチスケイル・もふもふ等）と一貫していることを確認した。
  ダメージ固定技は `power=0` により `calc_damages` の通常フロー・`ON_CALC_DAMAGE_MODIFIER` を
  経由しないため、ハンドラ側の追加ガードは不要と判断した）

## リーサル計算
- 対象外（`docs/progress/ability.md` の「リーサル実装」「リーサルテスト」列がともに `n/a`）

## テスト
- 追加: `test_こおりのりんぷん_ぼうぎょ参照の特殊技には効果が乗る`
  （サイコショックのようにぼうぎょを参照しつつ分類は special のままの技にも半減効果が
  乗ることを確認するテストを追加。既存の `test_こおりのりんぷん_かたやぶりで無効` /
  `test_こおりのりんぷん_特殊技のダメージ半減` は変更なし）
- `python -m pytest tests/ -v` を実行し、4829 passed, 1 skipped で全件パスを確認した
  （ログ: `.loop/test_logs/こおりのりんぷん.log`）
