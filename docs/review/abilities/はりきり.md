# はりきり レビュー結果

## 仕様書
- 変更なし（一次情報（ポケモンWiki `docs/wiki/abilities/はりきり.html`）と照合し、内容・命中率補正値（3277/4096）・一撃必殺技/必中技の除外・イカサマ/ボディプレス/パワートリック時の挙動・分類が変わる技の扱いなど、全項目で誤り・欠落なしを確認した）

## 実装計画書
- 変更なし（仕様書・実装と整合していることを確認した。ON_CALC_ATK_MODIFIER/ON_MODIFY_ACCURACY は `docs/spec/turn.md` に未掲載のダメージ計算内部イベントであり、既存の同種特性（ちからもち・ノーガード等）と同じくpriority省略で問題ないことを確認した）

## 実装（handlers/ / data/）
- 変更なし。`はりきり_modify_atk`（ON_CALC_ATK_MODIFIER, attacker:self）と `はりきり_modify_accuracy`（ON_MODIFY_ACCURACY, attacker:self）を確認し、以下の一次情報記載の細かな挙動もコード変更なしで自然に満たされていることを検証した：
  - イカサマ使用時: `damage.py._calc_final_attack` が相手のこうげきを`final_attack`に代入した上でON_CALC_ATK_MODIFIERを発火するため、category=="physical"判定のみで正しく1.5倍される
  - ボディプレス使用時: 同様に防御力が`final_attack`に代入されるため、category=="physical"判定のみで正しく1.5倍される
  - 相手からイカサマを受ける場合: subject_spec="attacker:self"のため、はりきり所持者が防御側のときは発動しない
  - ノーガード/ロックオン/テレキネシス/ちいさくなるとの併用: これらのハンドラは value=None, stop_event=True で無条件に上書きするため、ON_MODIFY_ACCURACYの実行順に関わらず必ず必中になる（既存の回帰テストで確認済み）
  - 一撃必殺技: has_flag("ohko")で命中率補正から除外済み

## リーサル計算
- 対象外（`docs/progress/ability.md` のはりきり行「リーサル実装」列がn/a）

## テスト
- `tests/abilities/test_ability_ha.py::test_はりきり_命中率が低下` を `test_はりきり_一撃必殺技は命中率が下がらない` にリネーム。
  - 修正理由: このテストは「つのドリル」（一撃必殺技）使用時に命中率が30のまま変化しないことを確認するテストだが、関数名が「命中率が低下」となっており、実際のアサーション内容（命中率は下がらない）と矛盾していたため、確認内容に合わせて改名した。
- その他のテスト（`test_はりきり_物理技が1_5倍になる`、`test_はりきり_物理技の命中率が下がる`、`test_ノーガード_はりきりの命中率低下を無視して必中になる`）は実装・仕様と整合しており変更なし。
- `python -m pytest tests/ -v` を実行し、5157 passed, 1 skipped（はりきりと無関係のskip）で全テスト成功を確認した。flakyなテストは確認されなかった。
