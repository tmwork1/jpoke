# かんつうドリル レビュー結果

## 仕様書
- 変更なし。一次情報（docs/wiki/abilities/かんつうドリル.html）と照合し、内容（接触技使用時にまもる状態の相手へダメージを1/4にして与える／Championsで効果が変更されたふかしのこぶしと同じ効果／メガドリュウズ専用特性）は正確だった。

## 実装計画書
- 変更なし。ふかしのこぶしと同一ハンドラ（`ふかしのこぶし_bypass_protect` / `ふかしのこぶし_reduce_damage`）を共有する計画になっており、仕様書と整合していた。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/ability.py` の `かんつうドリル` エントリは `Event.ON_CHECK_PROTECT` / `Event.ON_CALC_PROTECT_MODIFIER` に `ふかしのこぶし_bypass_protect` / `ふかしのこぶし_reduce_damage` を `subject_spec="attacker:self"` で登録しており、`ふかしのこぶし` と完全に同じ実装だった。誤り・欠落なし。

## リーサル計算
- 対象外（`docs/progress/ability.md` の「リーサル実装」列が `n/a`）。

## テスト
- `tests/abilities/test_ability__group.py` に既存のまもる貫通テスト（パラメータ化）で `かんつうドリル` がカバーされていたが、「非接触技はまもるを貫通しない」テストが `ふかしのこぶし` のみだったため、`かんつうドリル` も含むパラメータ化テストに拡張した。
