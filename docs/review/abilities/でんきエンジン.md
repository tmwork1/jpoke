# でんきエンジン レビュー結果

## 仕様書
- 変更なし。一次情報（docs/wiki/abilities/でんきエンジン.html）と照合したが誤り・欠落はなかった。
  - 効果（でんきタイプの技を無効化しすばやさ1段階上昇）、ちくでんと共通の発動条件（変化技も対象／自身対象技・場対象技には非発動／みがわり状態でも発動／まもる等で防いだ場合は非発動／連続攻撃技は無効化1回のみ・上昇も1回のみ／かたやぶりには非発動）、発動メッセージ、備考（第四世代エレキブル専用特性）すべて一次情報と一致していることを確認した。

## 実装計画書
- 変更なし。仕様書と照合したが誤り・欠落はなかった。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/ability.py` の `でんきエンジン` 登録（`Event.ON_BEFORE_APPLY_MOVE` / `subject_spec="defender:self"` / `flags={"mold_breaker_ignorable"}`）と `src/jpoke/handlers/ability.py` の `でんきエンジン_absorb_electric`（共通ヘルパー `_apply_type_absorb` を使用し `stats={"spe": 1}` を指定）は、同じ発動条件を持つ `ちくでん` / `ひらいしん` と同一パターンで実装されており、仕様書と齟齬はなかった。
- `_apply_type_absorb` の条件（`ctx.move.target == "foe"` かつ `ctx.move.type == move_type` のみ発動）により「自身対象技・場対象技には非発動」を満たし、`ON_BEFORE_APPLY_MOVE` 内で無効化時に技実行ループを打ち切る仕組みにより「連続攻撃技は初回ヒットのみ無効化・すばやさ上昇も1回のみ」を満たすことをコードレベルで確認した。

## リーサル計算
- 対象外（`docs/progress/ability.md` の該当行は「リーサル実装」「リーサルテスト」ともに n/a）。

## テスト
- 追加。既存の `tests/abilities/test_ability__group.py` の `test_タイプ無効バフ系` / `test_タイプ無効バフ系_かたやぶりで無効` で基本発動とかたやぶり無効化はカバー済みだったが、`でんきエンジン` 固有の仕様観点（計画書のテスト観点2・3、および仕様書に明記された変化技・すばやさ最大時の挙動）が未カバーだったため、`tests/abilities/test_ability_ta.py` に以下4件を追加した。
  - `test_でんきエンジン_でんき変化技も無効化してすばやさが上がる`
  - `test_でんきエンジン_みがわり状態でも発動する`
  - `test_でんきエンジン_連続攻撃技を受けてもすばやさ上昇は1回のみ`（そうでん状態＋にどげりで連続攻撃技化して検証）
  - `test_でんきエンジン_すばやさ最大時は無効化のみ発動する`
- `python -m pytest tests/ -v` を実行し、全テスト成功（5070 passed, 1 skipped）を確認。ログは `C:\Users\tmtmp\Documents\pokemon\jpoke\.loop\test_logs\でんきエンジン.log` に保存。
  - 初回実行時に `test_field.py::test_オーロラベール_リフレクターとは重複しない` が急所判定の乱数起因で1件failしたが、でんきエンジンの変更とは無関係の既存flakyテストであることを再実行で確認した（再実行はpass）。最終ログは全pass時点のものを保存している。
