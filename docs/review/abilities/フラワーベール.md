# フラワーベール レビュー結果

## 仕様書
- 変更なし。一次情報（wiki）の内容と照合し、状態異常防止・ねむけ防止・能力ランク低下防止（相手由来のみ）・かたやぶり無効等の記述に誤りがないことを確認した。

## 実装計画書
- 修正した。
  - 「くさタイプの味方全員を守る」効果のうち本プロジェクトの対象範囲（シングルバトル）では自分自身のみが対象になる旨を明記。
  - ねむけ状態防止（`Event.ON_BEFORE_APPLY_VOLATILE`）のハンドラ構成が計画から欠落していたため追加。
  - 能力ランク低下防止の実装方針を「相手由来のみ防ぐ（`_block_stat_drop_by_foe` 共通ヘルパーを使用）」に修正（旧計画は自己由来のランク低下も一律に防ぐ誤った疑似コードだった）。
  - 疑似コードを実装済みコードに合わせて更新。

## 実装（handlers/ / data/）
- 修正した。
  - `src/jpoke/handlers/ability.py`
    - `フラワーベール_prevent_ailment`: 独自実装（`value=False` を返しログを出さない）を共通ヘルパー `_prevent_ailment` を使う形に変更。他の状態異常防止特性（きよめのしお等）と同様に `LogCode.AILMENT_PREVENTED` ログを記録するようにした。
    - `フラワーベール_prevent_stat_drop`: 全ての能力低下（自分自身の技/特性による自発的な低下も含む）を一律にブロックしていたバグを修正。共通ヘルパー `_block_stat_drop_by_foe`（クリアボディと同じ実装）を使い、相手由来の低下のみをブロックするように修正。これにより、リーフストームの自傷、からをやぶる、くだけるよろい、ムラっけ等の自発的なランク低下を誤って無効化しないようになった。
    - `フラワーベール_prevent_volatile`（新規追加）: くさタイプへの「ねむけ」状態付与を防ぐハンドラを追加（仕様書に明記されているが未実装だった）。きよめのしお・ふみん等と同じ `_prevent_volatile` 共通ヘルパーを使用。
  - `src/jpoke/data/ability.py`
    - `フラワーベール` の `handlers` に `Event.ON_BEFORE_APPLY_VOLATILE: h.AbilityHandler(h.フラワーベール_prevent_volatile, subject_spec="target:self")` を追加登録。

## リーサル計算
- 対象外（`docs/progress/ability.md` のリーサル実装列が `n/a`）。

## テスト
- 追加・修正した（`tests/abilities/test_ability_ha.py`）。
  - 既存の状態異常防止テストの対象ポケモンを `フシギダネ`（くさ/どく複合）から `チコリータ`（くさ単タイプ）に変更。フシギダネはどくタイプを併せ持つため、どく/もうどくの防止テストがタイプ免疫と混同され、フラワーベールの効果を正しく検証できていなかったバグを修正。
  - `test_フラワーベール_かたやぶりで状態異常を防げない` を追加（かたやぶり所持の相手からのどくどくで、もうどく状態になることを確認）。
  - `test_フラワーベール_あくびのねむけを防ぐ` を追加（新規実装した揮発性状態防止ハンドラの検証）。
  - `test_フラワーベール_相手由来の能力低下を防ぐ` を追加（クリアボディの既存テストと同じ形式）。
  - `test_フラワーベール_自己による能力低下は防げない` を追加（バグ修正の回帰防止用）。
  - `test_フラワーベール_いかくによる能力低下を防ぐ` を追加（相手特性由来のランク低下防止の実戦的な確認）。
  - `test_フラワーベール_かたやぶりで能力低下を防げない` を追加（かたやぶり所持の相手のなきごえで、こうげきランクが下がることを確認）。
  - `python -m pytest tests/ -v` で全件（5412 passed, 1 skipped）を確認。ログは `C:\Users\tmtmh\Documents\pokemon\jpoke\.loop\test_logs\フラワーベール.log` に保存。
