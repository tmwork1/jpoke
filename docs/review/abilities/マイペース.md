# マイペース レビュー結果

## 仕様書
- 変更なし（既存の `docs/spec/abilities/マイペース.md` を一次情報
  （https://wiki.pokemonwiki.com/wiki/マイペース）と再照合し、内容が完全に一致していることを確認した）。

## 実装計画書
- 新規作成（`docs/plan/abilities/マイペース.md` が存在しなかったため、既存実装・仕様書・
  `docs/plan/abilities/どんかん.md` の記述パターンをもとに新規作成した）。
  - 実装対象イベント（`ON_BEFORE_APPLY_VOLATILE` / `ON_BEFORE_MODIFY_STAT` / `ON_ABILITY_ENABLED`）と
    priority の根拠（`docs/spec/turn.md` に記載がないため同種ハンドラと同一優先度）を明記した。
  - 世代間差異・メッセージ表示の有無・バトンタッチの詳細タイミングなど、本シミュレータの対象外とする
    仕様の詳細を明記した（既存の `_prevent_volatile` / `_cure_volatile_on_enable` ヘルパーは
    せいしんりょく等の他特性と同様に常にメッセージを表示する実装になっており、これは既知の簡略化
    として許容する）。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/ability.py` の `マイペース_prevent_volatile` /
  `マイペース_block_intimidate` / `マイペース_cure_confusion_on_enable` と
  `src/jpoke/data/ability.py` の `マイペース` エントリ（`flags={"mold_breaker_ignorable"}` と
  3つのハンドラ登録）を仕様書・計画書と照合し、誤り・欠落は見つからなかった。
  - `_prevent_volatile` / `_cure_volatile_on_enable` 共通ヘルパーはどんかん・ふみん等と同一パターンで
    再利用されており、`subject_spec` もイベントが渡すコンテキストのロール（`target:self` /
    `source:self`）と一致していることを確認した。
  - バトンタッチによる揮発性状態の引き継ぎ（`SwitchManager.run_switch`）も通常の
    `VolatileManager.apply()` を経由するため、マイペースのポケモンはこんらんを引き継ごうとしても
    `ON_BEFORE_APPLY_VOLATILE` で防がれることをコードトレースで確認した。

## リーサル計算
- 対象外（`docs/progress/ability.md` のリーサル実装列が `n/a`）。

## テスト
- `tests/abilities/test_ability_ma.py` に以下のテストを追加した（既存は「いかく無効化」1件と
  `test_ability__group.py` の「かたやぶりでこんらん付与後は直後に回復する」「揮発状態耐性」
  パラメトライズのみで、実際の技経由でのこんらん無効化を確認するテストが不足していたため）:
  - `test_マイペース_あばれる終了時の自傷こんらんを防ぐ` — あばれる状態終了時の自傷こんらんも防ぐことを確認。
  - `test_マイペース_あやしいひかりでこんらんにならない` — 変化技の効果によるこんらんを防ぐことを確認。
  - `test_マイペース_いばるを受けてもランクは上昇しこんらんにならない` — いばるの能力上昇は適用されつつ
    こんらんのみ防ぐことを確認（仕様書「いばる/おだてるを受けたときは、能力が上昇した後にマイペースの
    表示が出て混乱を防ぐ」に対応する状態遷移の確認）。
- `python -m pytest tests/ -v` で5515件全て成功（1件は既存の無関係なスキップ）。ログを
  `C:\Users\tmtmh\Documents\pokemon\jpoke\.loop\test_logs\マイペース.log` に保存した。
