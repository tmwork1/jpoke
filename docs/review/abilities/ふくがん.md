# ふくがん レビュー結果

## 仕様書
- 変更なし（一次情報（ポケモンWiki）と照合し、内容は正確であることを確認した）

## 実装計画書
- 修正した内容:
  - 仕様参照パスの誤り（`spec/ability/ふくがん.md`）を実際のパス（`docs/spec/abilities/ふくがん.md`）に修正
  - 変更候補のテストファイルパスの誤り（`tests/test_ability.py`）を実際のパス（`tests/abilities/test_ability_ha.py`）に修正

## 実装（handlers/ / data/）
- 変更なし
  - `handlers/ability.py` の `ふくがん_boost_accuracy` は `ctx.move.has_flag("ohko")` で一撃必殺技を除外し、`apply_fixed_modifier(value, 5325)` で命中率を5325/4096倍にしており、仕様書と一致していることを確認した
  - `data/ability.py` の登録も `ON_MODIFY_ACCURACY` / `subject_spec="attacker:self"` で正しい

## リーサル計算
- 対象外（`docs/progress/ability.md` のリーサル実装列が n/a。命中率補正のため通常のリーサル計算の対象外）

## テスト
- 変更なし
  - `tests/abilities/test_ability_ha.py` の `test_ふくがん_命中率を1_3倍にする` で通常技（でんじほう: 50→65）と一撃必殺技（つのドリル: 30のまま）の両ケースを確認済み。実装・仕様書と整合していることを確認した
