# きけんよち レビュー結果

## 仕様書
- 変更なし（一次情報（ポケモンWiki）と照合し、共通仕様・第五世代以降の仕様の記述が一致していることを確認した）

## 実装計画書
- 変更なし（実装方針・実装対象イベント・変更対象ファイルとも実装内容と一致していることを確認した）

## 実装（handlers/ / data/）
- 変更なし
  - `きけんよち_warn_threat`（`src/jpoke/handlers/ability.py`）を仕様書と照合した。以下の点を確認し、いずれも正しく実装されていた：
    - `move.is_attack` による変化技除外
    - `move.has_flag("ohko")` による一撃必殺技の無条件検知（第五世代以降はレベル差・相性の無効化を考慮しない仕様と一致）
    - `move.type`（各技データの静的な `type` フィールド）をそのまま参照することで、ウェザーボール/だいちのはどう/めざめるダンス/テラバースト/テラクラスター/オーラぐるま/レイジングブル/ツタこんぼう/フリーズドライ/フライングプレスが仕様書どおり「場の状態・アイテム・フォルム・特性等の影響を受けない固定タイプ」として扱われることを確認（これらの技は `MoveData.type` に基底タイプが設定されており、タイプ変動効果は `ON_MODIFY_MOVE_TYPE`/`ON_CALC_DEF_TYPE_MODIFIER` 等の実戦闘時ハンドラでのみ適用されるため、きけんよちの判定では自然に無視される）
    - `Pokemon.types`（テラスタル・へんしん・特性由来タイプ・もりののろい等の `added_types` を考慮）を参照することで、自身がテラスタル中の相性判定・テラバースト/テラクラスターがステラタイプに反応しない仕様が正しく成立することを確認
    - カウンター/ミラーコート/メタルバーストは `category` が physical/special のため `is_attack` が True となり、タイプ相性判定の対象になる（第五世代以降の仕様と一致）
    - `subject_spec="source:self"` は `ON_SWITCH_IN`（`EventContext`）のロールと一致しており、他の登場時特性ハンドラとも整合している

## リーサル計算
- 対象外（`docs/progress/ability.md` の「リーサル実装」列が `n/a`。バトル結果に影響しない情報系特性のため）

## テスト
- 変更なし（`tests/abilities/test_ability_ka.py::test_きけんよち_特性が開示される` が、効果抜群の攻撃技（じしん）・一撃必殺技（つのドリル）・無効化される変化技（なきごえ）・等倍の攻撃技（たいあたり）の4パターンを網羅しており、仕様書の主要な判定条件を十分にカバーしていることを確認した）

`python -m pytest tests/ -v` 実行結果: 4761 passed, 1 skipped
