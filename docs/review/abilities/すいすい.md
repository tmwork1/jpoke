# すいすい レビュー結果

## 仕様書
- 変更なし（一次情報（docs/wiki/abilities/すいすい.html）と照合し、効果・特性の仕様・備考とも一致していることを確認した）

## 実装計画書
- 内容が実装と乖離していたため修正した:
  - 参照仕様パスの誤り（`spec/ability/すいすい.md` → `docs/spec/abilities/すいすい.md`）を修正
  - 実装方針が `battle.weather.name == "あめ"` という誤った記述になっていたため、
    実際の実装（`battle.weather_for(ctx.source).rainy`）に合わせて修正
    - `weather_for` がノーてんき/エアロック・ばんのうがさによる無効化を汎用的に処理することを明記
    - `Field.rainy` があめ・おおあめの両方をカバーすることを明記
  - 存在しない `SpeedCalculator.calc_effective_speed` のイベントチェーン云々の記述を削除
  - サーフテール・すなかきとの実装パターンの一貫性（天候直接参照 vs `weather_for` の使い分けの理由）を追記
  - テスト観点・変更候補ファイルを実際のテスト配置（`tests/abilities/test_ability__group.py` /
    `test_ability_sa.py` / `tests/items/test_item_ha.py`）に合わせて更新

## 実装（handlers/ / data/）
- ロジックは仕様書・計画書と一致しており修正なし。
  - `handlers/ability.py` の `すいすい_modify_speed` は `battle.weather_for(ctx.source).rainy` で
    判定しており、あめ・おおあめの両方、ばんのうがさ・ノーてんき/エアロックによる無効化を
    正しくカバーしている（同種特性のすなかき・サーフテールと一貫したパターン）。
  - `data/ability.py` の登録（`DomainEvent.ON_CALC_SPEED` / `subject_spec="source:self"`）も
    すなかき・サーフテールと同一パターンで問題なし。
  - docstring を「あめ中に素早さが2倍」から「あめ・おおあめ中に素早さが2倍」＋無効化条件の説明に補強した。

## リーサル計算
- 対象外（`docs/progress/ability.md` のリーサル実装列が `n/a`）

## テスト
- 既存テストは `tests/abilities/test_ability__group.py`（あめ中2倍・非対応天候で据え置き）と
  `tests/items/test_item_ha.py`（ばんのうがさ所持時に無効）でカバーされていたが、
  仕様書で明記されている「おおあめでも発動する」ケースが未検証だったため、
  `tests/abilities/test_ability_sa.py` に `test_すいすい_おおあめ中も素早さ2倍` を追加した。
