# きょううん レビュー結果

## 仕様書
- 変更なし。`docs/spec/abilities/きょううん.md` を一次情報（ポケモンWiki `docs/wiki/abilities/きょううん.html`）と
  照合し、「使う技の急所ランクが+1される」「第八世代のみ道具持ち野生ポケモンの出現率上昇（バトル対象外）」の
  記述に誤り・欠落はなかった。

## 実装計画書
- 変更なし。`docs/plan/abilities/きょううん.md` は仕様書と一致しており、`ON_CALC_CRITICAL_RANK` を
  攻撃側 (`attacker:self`) で利用する方針、テスト観点とも妥当だった。
  同イベントは `docs/spec/turn.md` に priority の記載がないが、`ひとでなし`（特性）・`するどいツメ`/
  `ピントレンズ`（アイテム）・`きゅうしょアップ`（volatile）の既存ハンドラもすべて priority 未指定の
  単純な `value + 1` 加算であり、既存パターンと整合している。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/ability.py` の `きょううん_modify_critical_rank`
  （`AttackContext` を受け取り `value + 1` を返す）、`src/jpoke/data/ability.py` の
  `ON_CALC_CRITICAL_RANK` ハンドラ登録（`subject_spec="attacker:self"`）とも仕様・計画書と一致していた。

## リーサル計算
- 対象外（`docs/progress/ability.md` の「きょううん」行はリーサル実装・リーサルテストとも `n/a`）。

## テスト
- 変更なし。`tests/abilities/test_ability_ka.py::test_きょううん_急所ランクが1上がる` と
  `tests/test_battle_option.py::test_確定のみモードで通常技はランク補正があっても急所にならない`
  で急所ランク+1の適用とランク補正経由での判定を確認済みであり、過不足なし。
- `python -m pytest tests/ -v` は 4774 passed / 1 skipped で全件通過。
  初回実行時に `tests/test_field.py::test_オーロラベール_ひかりのかべとは重複しない` が
  乱数依存（急所判定が固定されていない）で一度だけ失敗したが、きょううんの変更とは無関係の
  既存の flaky テストであり、再実行では成功した（再修正はしていない）。
