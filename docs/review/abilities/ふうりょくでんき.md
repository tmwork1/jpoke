# ふうりょくでんき レビュー結果

## 仕様書
- 変更なし（一次情報（ポケモンWiki）と照合済み。風技の一覧14種・特性の仕様・備考すべて一致していることを確認した）

## 実装計画書
- 修正した
  - `ON_DAMAGE_HIT`（priority=20）から `ON_HIT`（デフォルト優先度）へ変更し、`ctx.substitute_damage` チェックを追加する旨を反映（下記「実装」の理由と同一）
  - `ON_FIELD_ACTIVATE` ハンドラの実装詳細（`value.name` での判定、`かぜのり_boost_atk_on_tailwind_start` と同一パターンである旨）を実装に合わせて明記
  - テストケース一覧に「あやしいかぜ・ぎんいろのかぜでは発動しない」「こらえるでHP1のまま耐えたときも発動する」「みがわりに阻まれたときは発動しない」を追加

## 実装（handlers/ / data/）
- 修正した
  - **重大**: `ふうりょくでんき_on_damage` が `Event.ON_DAMAGE_HIT` に登録されていたが、`ON_DAMAGE_HIT` は `actual_damage<=0` のとき発火しない実装のため、仕様書に明記されている「HP1でこらえるを使用したときなど、風の技で受けたダメージが0だった場合は発動する」という要件を満たせなかった。どくげしょう・どくのくさり・どくのトゲ等の同種特性（`docs/spec/abilities/どくげしょう.md` 等）と同じパターンに合わせ、`Event.ON_HIT`（常に発火）へ変更し、ハンドラ内で `ctx.substitute_damage` を確認してみがわりに阻まれた場合のみ除外するよう修正した
  - `src/jpoke/data/moves/move_a.py` の「あやしいかぜ」、`src/jpoke/data/moves/move_ka.py` の「ぎんいろのかぜ」に誤って付与されていた `"wind"` フラグを削除した。`docs/spec/moves/_move_labels.md` の `wind` ラベル一覧（かぜのり・ふうりょくでんき公式wikiの風技表と一致）にこの2技は含まれておらず、`has_flag("wind")` のみを使う `ふうりょくでんき`・`かぜのり` の両特性が名前に「かぜ」を含むだけのこの2技に誤反応する不具合だった（既存テストはこの2技のフラグに依存していないことを確認済み）

## リーサル計算
- 対象外（`docs/progress/ability.md` の該当行でリーサル実装・リーサルテストは n/a。じゅうでん状態を付与するだけでダメージ計算に直接影響しないため）

## テスト
- 追加した（`tests/abilities/test_ability_ha.py`）
  - `test_ふうりょくでんき_風技を受けるとじゅうでん` に `あやしいかぜ`・`ぎんいろのかぜ`（発動しない）のケースを追加
  - `test_ふうりょくでんき_こらえるでHP1のまま耐えたときも発動する` を新規追加
  - `test_ふうりょくでんき_みがわりに阻まれたときは発動しない` を新規追加
- `python -m pytest tests/ -v` を実行し、5347 passed, 1 skipped で全件パスを確認した（結果を `.loop/test_logs/ふうりょくでんき.log` に保存）
