# せいぎのこころ レビュー結果

## 仕様書
- 変更なし（一次情報（docs/wiki/abilities/せいぎのこころ.html）と内容が一致していることを確認した）

## 実装計画書
- 変更なし（仕様書・実装内容と整合していることを確認した）

## 実装（handlers/ / data/）
- 変更なし
- `Event.ON_DAMAGE_HIT` / `subject_spec="defender:self"` で `ctx.move.type == "あく"` を判定し
  `battle.modify_stats(mon, {"atk": +1}, source=ctx.attacker)` を呼ぶ実装は仕様書・計画書通りであることを確認した
- `ON_DAMAGE_HIT` はまもる・みがわりで技が無効化された場合や変化技では発火しないため、仕様の
  「まもる/みがわりで防いだときは発動しない」「変化技では発動しない」を満たしていることを確認した

## リーサル計算
- 対象外（`docs/progress/ability.md` のリーサル実装列が `n/a`）

## テスト
- 既存テスト（あく技でA上昇/ノーマル技では上昇しない）は妥当だったため維持
- 以下のテストを追加した（`tests/abilities/test_ability_sa.py`）
  - `test_せいぎのこころ_A最大のときは発動しない`
  - `test_せいぎのこころ_まもるで防がれたときは発動しない`
  - `test_せいぎのこころ_みがわり状態では発動しない`
  - `test_せいぎのこころ_あくタイプの変化技では発動しない`
  - `test_せいぎのこころ_連続技はヒットごとに発動する`（ふくろだたきで複数回発動することを確認）
