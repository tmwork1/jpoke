# ちどりあし レビュー結果

## 仕様書
- 変更なし（一次情報（ポケモンWiki）と照合し、内容に誤り・欠落なしと確認）

## 実装計画書
- `ctx.defender.check_volatile("こんらん")` という誤ったメソッド名を `ctx.defender.has_volatile("こんらん")` に修正
  （`Pokemon` モデルに実在するメソッド名は `has_volatile` であり、`check_volatile` は存在しない）
- 疑似コード部分も同様に修正

## 実装（handlers/ / data/）
- `handlers/ability.py` の `ちどりあし_reduce_accuracy` から冗長なガード `ctx.defender is not None` を削除
  - `subject_spec="defender:self"` により `ctx.defender` の存在はイベント発火側で保証されているため、
    CLAUDE.md「状態変更ルール／Handler の約束事」の重複ガード禁止方針に反していた
  - 同パターンの `すながくれ_reduce_accuracy` / `ゆきがくれ_reduce_accuracy` / `ミラクルスキン_reduce_accuracy`
    と実装を揃えた
- `data/ability.py` の登録内容（`flags={"mold_breaker_ignorable"}`、`Event.ON_MODIFY_ACCURACY`、
  `subject_spec="defender:self"`）は仕様書・計画書と整合しており変更なし

## リーサル計算
- 対象外（`docs/progress/ability.md` の該当行はリーサル実装・リーサルテストとも `n/a`）

## テスト
- 変更なし（既存の3テストで仕様を十分カバーしていることを確認）
  - `test_ちどりあし_こんらん中命中率が半減する`
  - `test_ちどりあし_こんらんでないとき変化なし`
  - `test_ちどりあし_かたやぶりで無効`
- `python -m pytest tests/ -v` を実行し、4990 passed, 1 skipped（全テスト成功）を確認。
  ログは `.loop/test_logs/ちどりあし.log` に保存。
  ※ 実行中に `test_ごりむちゅう_特殊技には効果がない` と
  `test_むしくい_ねんちゃく持ちをひんしにさせた場合は奪える` が乱数依存のflakyで一時的に失敗したが、
  該当2テストを個別に再実行して成功することを確認済み。ちどりあしの変更とは無関係。
