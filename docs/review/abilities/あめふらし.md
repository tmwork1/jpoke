# あめふらし レビュー結果

## 仕様書
- 変更なし。一次情報（docs/wiki/abilities/あめふらし.html）と照合し、効果・天候上書きルール・第五/六世代以降の仕様・備考すべて一致していることを確認した。

## 実装計画書
- 変更なし。仕様書の内容と実装方針（`ON_SWITCH_IN` での天候発動、`WeatherManager.apply` への委譲）が一致していることを確認した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/ability.py` の `あめふらし` は `ON_SWITCH_IN` と `ON_ABILITY_ENABLED`（特性が書き換わった際の即時発動）の両方に `h.あめふらし_activate_weather` を登録済み。
- `handlers/ability.py` の `あめふらし_activate_weather` は共通関数 `_activate_weather` 経由で `WeatherManager.apply(weather="あめ", count=5)` を呼んでおり、天候の優先度比較（`WEATHER_PRIORITY`）・同一天候時の不発動・強天候（おおひでり・おおあめ・らんきりゅう）による上書き不可がすべて `WeatherManager`（`field_manager.py`）側の共通ロジックで正しく処理されていることを確認した。
- しめったいわによる継続ターン延長（5→8）は `handlers/item.py` の `しめったいわ_resolve_field_count` に委譲されており、あめふらし側での重複実装は不要であることを確認した。

## リーサル計算
- 対象外（`docs/progress/ability.md` のリーサル実装列が `n/a`）。

## テスト
- 変更なし。`tests/abilities/test_ability__group.py` に登場時発動・デフォルト継続ターン（5）・強天候による上書き不可・通常天候の上書きのテストが揃っており、実装と整合していることを確認した。
- `python -m pytest tests/ -v` で 4669 passed, 1 skipped を確認（`.loop/test_logs/あめふらし.log`）。
