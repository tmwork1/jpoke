# ターボブレイズ レビュー結果

## 仕様書
- 変更なし。既存の `docs/spec/abilities/ターボブレイズ.md` を一次情報
  （`docs/wiki/abilities/ターボブレイズ.html`）と照合し、内容が一致していることを確認した。

## 実装計画書
- 新規作成。`docs/plan/abilities/ターボブレイズ.md` が存在しなかった（`docs/progress/ability.md`
  では計画書列が `x` になっていたが実体が欠落していた）ため、仕様書をもとに新規作成した。

## 実装（handlers/ / data/）
- 修正した。`src/jpoke/data/ability.py` の `ターボブレイズ` エントリに
  `Event.ON_ABILITY_ENABLED: h.AbilityHandler(h.announce_ability_triggered, subject_spec="source:self")`
  を追加した。
  - `かたやぶり.md` 備考に「ターボブレイズ/テラボルテージはかたやぶりと全く同一の効果を持つ」と
    明記されているが、`かたやぶり` は `ON_SWITCH_IN` に加えて `ON_ABILITY_ENABLED` にも
    `announce_ability_triggered` を登録しており、かがくへんかガスの効果が切れて特性が
    再有効化されたときに発動メッセージを再度表示する。従来の `ターボブレイズ` エントリには
    この登録が抜けており、ガス解除後に再度メッセージが流れない非対称があったため追加した。
  - `handlers/ability.py` は変更なし（既存の `かたやぶり_disable_foe_ability` /
    `かたやぶり_restore_foe_ability` / `announce_ability_triggered` をそのまま再利用）。

## リーサル計算
- 対象外（`docs/progress/ability.md` のリーサル実装列が `n/a`）。

## テスト
- 追加した。`tests/abilities/test_ability_ta.py` に以下を追加（従来 `ターボブレイズ` の
  専用テストが1件も存在しなかった）:
  - `test_ターボブレイズ_場に出たときに特性開示`
  - `test_ターボブレイズ_がんじょうを無視して一撃で倒す`
  - `test_ターボブレイズ_プリズムアーマーは無効化されない`
  - `test_ターボブレイズ_かがくへんかガス解除後に特性発動ログが再度記録される`
    （上記実装修正の回帰テスト）
- `python -m pytest tests/ -v` を実行し、4978 passed, 1 skipped（既存の既知skip）で全件成功を確認した。
  ログは `C:\Users\tmtmp\Documents\pokemon\jpoke\.loop\test_logs\ターボブレイズ.log` に保存した。
