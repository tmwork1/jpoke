# しょうりのほし レビュー結果

## 仕様書
- 既存（`docs/spec/abilities/しょうりのほし.md`）。一次情報（Wiki）と照合したが、内容・倍率（4506/4096）・一撃必殺技除外・ダブル以上での味方対象などすべて一致しており変更なし。

## 実装計画書
- 既存（`docs/plan/abilities/しょうりのほし.md`）。以下を修正した。
  - 疑似コード・実装方針中の `ctx.move.has_label("ohko")` を実装と一致する `ctx.move.has_flag("ohko")` に修正
  - `変更候補` / `検証コマンド` のテストファイル参照を旧パス `tests/test_ability.py` から実際のテストファイル `tests/abilities/test_ability_sa.py` に修正
  - シングルバトル限定という対象範囲の理由（ダブル以上は味方の技も対象だが本プロジェクトはシングルのみ対象のため `subject_spec="attacker:self"` を採用）を明記

## 実装（handlers/ / data/）
- `src/jpoke/handlers/ability.py` の `しょうりのほし_modify_accuracy` と `src/jpoke/data/ability.py` の登録内容は仕様書・計画書と一致（`Event.ON_MODIFY_ACCURACY` / `subject_spec="attacker:self"` / 倍率4506 / 一撃必殺技除外）。効果は**命中率**補正であり、**威力**補正ではないことを一次情報で確認した（`docs/progress/ability.md` の従来の効果列の記述が誤りだったため修正、下記参照）。姉妹特性の `ふくがん_boost_accuracy`（倍率5325のみ異なる）と実装パターンが完全に一致していることも確認した。変更なし。

## リーサル計算
- 対象外（`docs/progress/ability.md` の「リーサル実装」列が `n/a`）。

## テスト
- `tests/abilities/test_ability_sa.py` に既存の2テスト（一撃必殺技には適用されない／命中率が1.1倍になる）があり、仕様と一致した内容で問題なし。変更なし。
- `python -m pytest tests/ -v` を実行し、しょうりのほし関連は全件PASS。無関係な既知flakyテスト（オーロラベール_リフレクターとは重複しない：急所乱数未固定、むしくい_ねんちゃく持ちをひんしにさせた場合は奪える：まひ乱数未固定）が単発失敗したが、再実行でPASSすることを確認済み（本レビューの変更に起因しない）。
