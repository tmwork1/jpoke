# ひひいろのこどう レビュー結果

## 仕様書
- 変更なし。`docs/wiki/abilities/ひひいろのこどう.html`（一次情報）から改めて変換した内容と
  既存の `docs/spec/abilities/ひひいろのこどう.md` を照合したが、内容は完全に一致していた
  （表記ゆれ「持ち物」→「アイテム」のみ、既存の意訳で問題なし）。

## 実装計画書
- 新規作成（`docs/plan/abilities/ひひいろのこどう.md`）。
  `docs/progress/ability.md` の計画書列は既に x になっていたが実体が存在しなかったため作成した。
  ひでりの計画書（共通の `_activate_weather` ヘルパー方針）を踏襲しつつ、攻撃補正側
  （`ON_CALC_ATK_MODIFIER`）の実装方針・こんらん除外・イカサマ非対象などの根拠を明記した。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/ability.py` の `ひひいろのこどう_modify_atk` を修正した。
  - 修正前は `battle.weather_for(ctx.attacker).sunny` と `ctx.move.category == "physical"` のみ
    判定しており、こんらんの自傷ダメージ（内部技 `_こんらん`、category="physical"）にも
    攻撃補正がかかってしまうバグがあった。
  - 仕様書に明記されている「こんらんのダメージに特性の効果は影響しない」を満たすため、
    既存の `ちからもち_boost_physical` と同じパターンで `ctx.move.name != "_こんらん"` の
    除外条件を追加した。
  - その他の項目（ばんのうがさ・ノーてんき/エアロック・イカサマを受ける側での非適用・
    ボディプレス・あなをほるへの適用・テラバーストの分類決定への非干渉）は、
    `battle.weather_for()` の実装や `subject_spec` によるハンドラ発火条件、
    `テラバースト_modify_move_category` が生のランク補正値を使う実装により
    既に正しく満たされていたため変更なし。

## リーサル計算
- 対象外（`docs/progress/ability.md` のリーサル実装列が n/a）。

## テスト
- 以下のテストを `tests/abilities/test_ability_ha.py` に追加した。
  - `test_ひひいろのこどう_こんらん自傷ダメージには補正なし`
  - `test_ひひいろのこどう_ボディプレスで自分の防御を1_33倍にする`
  - `test_ひひいろのこどう_あなをほるのダメージにも補正がかかる`
  - `test_ひひいろのこどう_イカサマを受けるときは補正なし`
- `python -m pytest tests/ -v` を実行し、5187 passed, 1 skipped で全テストが通ることを確認した
  （ログ: `.loop/test_logs/ひひいろのこどう.log`）。
- スキップは `tests/moves_attack/test_move_a.py::test_あなをほる_マグニチュードは地中の相手に命中し威力が2倍になる`
  の1件のみで、本タスクと無関係の既存スキップ。flakyなテストは発見されなかった
  （新規追加分・既存分ともに再実行して結果が安定していることを確認済み）。
