# ねつこうかん レビュー結果

## 仕様書
- 変更なし。一次情報（docs/wiki/abilities/ねつこうかん.html）と全項目を照合済みで誤り・欠落なし。

## 実装計画書
- 修正した:
  - 仕様参照パスの誤り（`spec/ability/ねつこうかん.md` → `docs/spec/abilities/ねつこうかん.md`）を修正。
  - 実装済みコードとの乖離（`ON_DAMAGE` と誤記していた箇所を実際の `Event.ON_DAMAGE_HIT` に修正）を解消。
  - `flags={"mold_breaker_ignorable"}` の付与理由、`ON_ABILITY_ENABLED` によるやけど回復処理、
    各イベントの `subject_spec`・priority方針など、実装済みコードの詳細を反映して全面的に更新。
  - じょうききかん・すいほうの計画書に倣い、疑似コード・検証コマンド・保留論点の節を追加。

## 実装（handlers/ / data/）
- 修正した:
  - `handlers/ability.py` の `ねつこうかん_boost_atk_on_fire` に `ctx.defender.fainted` ガードを
    追加。仕様書に明記されている「攻撃技でひんしになったときは発動しない」が実装に反映されて
    おらず、`Event.ON_DAMAGE_HIT` はKO後にも発火するため、ガードなしではひんし後もこうげきの
    ランクが変化してしまうバグだった（へんしょく・いかりのこうら等と同じ idiom で修正）。
  - `data/ability.py` の登録内容（`flags={"mold_breaker_ignorable"}` および3ハンドラ）は
    仕様書・すいほう/じょうききかんの実装パターンと整合しており変更なし。

## リーサル計算
- 対象外（`docs/progress/ability.md` のリーサル実装列が `n/a`）。

## テスト
- 追加した（`tests/abilities/test_ability_na.py`）:
  - `test_ねつこうかん_ほのお以外の技では発動しない`
  - `test_ねつこうかん_こうげきがすでに最大なら発動しない`
  - `test_ねつこうかん_まもるで防がれたときは発動しない`
  - `test_ねつこうかん_みがわり状態では発動しない`
  - `test_ねつこうかん_ひんし時は発動しない`（上記の fainted ガード追加に対する回帰テスト）
- 追加した（`tests/abilities/test_ability__group.py`）:
  - `test_状態異常無効` パラメタライズケースに `("ねつこうかん", "やけど")` を追加。
  - `test_状態異常無効_かたやぶりで無効` パラメタライズケースに
    `("ねつこうかん", "やけど", "おにび", False)` を追加。
- `python -m pytest tests/ -v` は 5097 passed, 1 skipped（skipはねつこうかんと無関係の既存項目）。
  ログは `.loop/test_logs/ねつこうかん.log` に保存。
