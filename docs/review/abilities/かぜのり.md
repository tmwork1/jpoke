# かぜのり レビュー結果

## 仕様書
- 変更なし。一次情報（docs/wiki/abilities/かぜのり.html）と照合し、対象の風の技一覧（15種）・特性の仕様（相手の場のおいかぜでは発動しない、コートチェンジでの移動では発動しない、ふきとばしを受けても発動し交代を防ぐ、かたやぶりの効果がある技には発動しない、みがわり状態でも発動する、命中判定より前に発動判定がある等）・備考（すなあらしはデータ上風の技だが発動しない）が既存の仕様書と一致していることを確認した。

## 実装計画書
- 変更なし。イベント選定（`ON_BEFORE_APPLY_MOVE` / `ON_SWITCH_IN` / `ON_FIELD_ACTIVATE`）と実装方針が仕様書と整合していることを確認した。

## 実装（handlers/ / data/）
- 変更なし。`data/ability.py` の `かぜのり` エントリは `mold_breaker_ignorable` フラグを持ち、`ON_BEFORE_APPLY_MOVE`（風技吸収）・`ON_SWITCH_IN`（おいかぜ場への登場）・`ON_FIELD_ACTIVATE`（おいかぜ発生）の3ハンドラを登録している。
- `data/moves/*.py` を確認し、仕様書に列挙された15種の風の技（エアカッター・エアロブラスト・かぜおこし・かみなりあらし・こがらしあらし・こごえるかぜ・たつまき・ねっさのあらし・ねっぷう・はなふぶき・はるのあらし・ふきとばし・ふぶき・ぼうふう・ようせいのかぜ）すべてに `flags={"wind", ...}` が付与済みであることを確認した。
- `かぜのり_absorb_wind` は `ON_BEFORE_APPLY_MOVE`（みがわりの無効化チェックである `ON_CHECK_SUBSTITUTE` より前）で発動するため、みがわり状態でも風の技を吸収してこうげきが上がる仕様と一致していることをコードのイベント順序（`enums/event.py` のコメント）から確認した。

## リーサル計算
- 対象外（`docs/progress/ability.md` のリーサル実装列が `n/a`。かぜのりはこうげきランク上昇のみで、リーサル計算（今ターンのHP/被弾結果）に直接影響しないため対象外で妥当）。

## テスト
- `tests/abilities/test_ability_ka.py` に `test_かぜのり_みがわり状態でも風の技を吸収して発動` を追加した。`ON_BEFORE_APPLY_MOVE` の発動順序がみがわりより先であることを回帰的に検証する。
- 既存テスト（おいかぜ登場時上昇・おいかぜ発生時上昇・かたやぶりで吸収無効・対象外の技は通常被弾・風の技吸収でこうげき上昇、および `tests/moves_status/test_move_ha.py` の `ふきとばし_かぜのりで無効化される`）は変更なし。
- `python -m pytest tests/ -v` で全4740件（1件skip）がパスすることを確認した。
