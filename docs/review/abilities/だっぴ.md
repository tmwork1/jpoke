# だっぴ レビュー結果

## 仕様書
- 修正した内容:
  - `docs/spec/abilities/だっぴ.md` を一次情報（`docs/wiki/abilities/だっぴ.html`）と照合。
    「効果」「特性の仕様」「備考」は一致していたが、発動率の記述が Champions 固有の値を
    明示していなかったため補強した。
    一次情報には「効果」節に一般的な発動率（1/3、第四世代のみ30%）とは別に、
    「Champions ターン終わりに30%の確率で状態異常が治る」という独立した記述がある。
    本プロジェクトの対象は Champions のみであり、SVの通常値（1/3）と混同されないよう、
    「Champions では発動率は30%となる（SVの1/3ではなく、第四世代と同じ値が採用されている）。
    本プロジェクトの実装対象はこの値。」を追記した（`きれあじ.md` 等の既存スペックにならい
    「Champions では」の形式で明記）。
  - 実装（`だっぴ_cure_ailment`）は既に0.3を使用しており、コード自体に誤りはなかった。

## 実装計画書
- 修正した内容（実質的な内容は変更なし、記述形式を更新）:
  - 既存の `docs/plan/abilities/だっぴ.md` は `ON_TURN_END_2`/`ON_TURN_END_3` という
    実装に存在しないフェーズ名を使った古い記述だったため、現行の実装
    （`Event.ON_TURN_END` priority=60、`docs/spec/turn.md` 記載）に合わせて全面的に
    書き直した。
  - `resolve_secondary_chance` を経由する理由（`EventContext` のため
    `attacker:self`/`defender:self` ロールのハンドラ〈ちからずく・てんのめぐみ・りんぷん〉は
    スキップされ、テスト用の `secondary_chance` オプションのみが効く）を明記した。
  - 実装ゴール・実装ステップ・テスト観点を最新のテンプレート形式（`ダークオーラ.md` 等）に
    合わせて整理した。

## 実装（handlers/ / data/）
- 変更なし。
  - `src/jpoke/handlers/ability.py` の `だっぴ_cure_ailment`（`mon.ailment.is_active` の
    早期リターン → `resolve_secondary_chance(ctx, 0.3)` → `ailment_manager.remove`）は
    仕様・計画と整合していることを確認した。
  - `src/jpoke/data/ability.py` の `だっぴ` エントリ（`Event.ON_TURN_END`,
    `subject_spec="source:self"`, `priority=60`）は `docs/spec/turn.md` の
    だっぴ行（priority=60、どく/もうどくダメージ90・やけどダメージ100より先）と一致することを
    確認した。
  - こんらん等の volatile は `ailment_manager` の対象外のため、実装上も自然に治療対象から
    除外されていることを確認した。

## リーサル計算
- 対象外（`docs/progress/ability.md` の だっぴ 行「リーサル実装」列が n/a）。

## テスト
- 追加した内容:
  - `tests/abilities/test_ability_ta.py` に `test_だっぴ_状態異常がなければ何も起きない` を
    追加（`うるおいボディ` 等の既存の類似特性テストにならい、状態異常なし時に
    エラーなく何も起きないことを確認するケースを補完）。
  - 既存の3テスト（各状態異常での回復・発動ターンはどくダメージを受けない・非発動時は
    状態異常が残る）は実装と整合しており変更なし。
- `python -m pytest tests/ -v` で全テスト実行、結果は
  `.loop/test_logs/だっぴ.log` に保存（4986 passed, 1 skipped）。
  ※ `tests/moves_attack/test_move_ma.py::test_むしくい_ねんちゃく持ちをひんしにさせた場合は奪える`
  が、まひによる行動不能（乱数依存、テスト側で確率固定していない）で単発flakyすることを
  確認したが、本レビューの変更（だっぴ関連ファイルのみ）とは無関係であり、再実行では成功する。
