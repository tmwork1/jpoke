# すなおこし レビュー結果

## 仕様書
- 変更なし。一次情報（docs/wiki/abilities/すなおこし.html）と照合し、効果・特性の仕様・備考・関連項目とも記載内容に相違なし。

## 実装計画書
- 変更なし。仕様書と整合しており、`ひでり`/`あめふらし` の計画書と同一パターン。

## 実装（handlers/ / data/）
- 変更なし。`data/ability.py` の `すなおこし`（`Event.ON_SWITCH_IN` / `Event.ON_ABILITY_ENABLED`、`subject_spec="source:self"`）は `ひでり`・`あめふらし` と同一パターンで登録されている。
- `handlers/ability.py` の `すなおこし_activate_weather` は共通関数 `_activate_weather(battle, ctx.source, value, weather="すなあらし", count=5)` を呼ぶのみで、`ひでり_activate_weather` / `あめふらし_activate_weather` と実装パターンが一致している。
- 天候の上書き判定（`WeatherManager.apply` の優先度比較）、さらさらいわによる継続ターン延長（`item.py` の `さらさらいわ_resolve_field_count`）は天候共通ロジックで、個別の欠落なし。

## リーサル計算
- 対象外（`docs/progress/ability.md` の「すなおこし」行はリーサル実装列 `n/a`）。

## テスト
- 変更なし。`tests/abilities/test_ability__group.py::test_天候始動特性_登場時に発動` がパラメタライズで「すなおこし」→「すなあらし」・count=5 をカバー。
- `tests/abilities/test_ability_a.py` / `test_ability_ha.py` の `test_エアロック_天候と強天候を無効化する` 等でも「すなおこし」が天候リストに含まれ間接的にカバーされている。
- `tests/abilities/test_gas.py::test_かがくへんかガス_特性無効化` で「すなおこし」がガスにより無効化されることを確認済み。
- `python -m pytest tests/ -v` で全4931件パス（1 skip はすなおこしと無関係）。ログは `.loop/test_logs/すなおこし.log` に保存。
