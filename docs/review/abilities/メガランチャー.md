# メガランチャー レビュー結果

## 仕様書
- 変更なし（一次情報 https://wiki.pokemonwiki.com/wiki/メガランチャー と全項目一致することを確認した）。

## 実装計画書
- 新規作成（`docs/plan/abilities/メガランチャー.md`）。
  - `ON_CALC_POWER_MODIFIER` で `move.has_flag("pulse")` の技に6144/4096倍を適用する方針（かたいツメ・
    きれあじ・がんじょうあご等と同一パターン）を明記した。
  - いやしのはどうの回復量分岐（3/4・五捨五超入）の実装方針を明記した。
  - だいちのはどうの威力2倍補正は `ON_CALC_POWER_MODIFIER` の基準値4096から始まる固定小数点乗算で
    常に厳密値（丸め誤差なし）になるため、他の乗算的補正（メガランチャー・フィールド一致ボーナス等）
    との適用順序に関わらず最終結果が一致することを確認し、priority制御が不要である根拠として記載した。

## 実装（handlers/ / data/）
- 変更なし。`メガランチャー_modify_power`（`handlers/ability.py`）と `いやしのはどう_heal_defender` の
  メガランチャー分岐（`handlers/move_status.py`）を仕様書・計画書と照合し、誤り・欠落がないことを確認した。
- 対象6技（あくのはどう/こんげんのはどう/だいちのはどう/はどうだん/みずのはどう/りゅうのはどう）
  すべてに `"pulse"` フラグが付与されていることをデータ側で確認した。
- だいちのはどう＋フィールド＋メガランチャーの複合補正について、ハンドラ登録順序（特性は場に出た
  時点で先に登録され、技固有のハンドラは使用時に後から登録される）を実測し、最終的な威力補正値が
  乗算順序に関わらず数学的に一致すること（だいちのはどうの×2倍が常に厳密値になるため）を確認した。
  実装の修正は不要。

## リーサル計算
- 対象外（`docs/progress/ability.md` のリーサル実装・リーサルテスト列は `n/a`）。

## テスト
- `tests/abilities/test_ability__group.py` の `test_技カテゴリによる威力補正_param` に
  `("メガランチャー", "みずのはどう", 6144)` / `("メガランチャー", "たいあたり", 4096)` を追加し、
  波動技の威力1.5倍化とそれ以外の技への非適用を検証した（既存の同種特性テストとまとめて回帰できる形に
  統一した）。
- `tests/abilities/test_ability_ma.py` に
  `test_メガランチャー_だいちのはどうとフィールドの複合補正` を新規追加し、だいちのはどう＋
  エレキフィールド＋メガランチャーの複合補正値（`power_modifier == 15974`）を検証した。
- いやしのはどうの回復量3/4テスト（`tests/moves_status/test_move_a.py::test_いやしのはどう_メガランチャーで回復量が3_4になる`）は
  既存のまま変更なし（内容を確認し問題なし）。
- `python -m pytest tests/ -v` を実行し、5603 passed, 1 skipped（既存の無関係スキップ）で全件成功を確認した。
  結果は `C:\Users\tmtmh\Documents\pokemon\jpoke\.loop\test_logs\メガランチャー.log` に保存した。
