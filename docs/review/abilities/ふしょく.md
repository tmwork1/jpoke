# ふしょく レビュー結果

## 仕様書
- 変更なし（一次情報（`docs/wiki/abilities/ふしょく.html`）と `docs/spec/abilities/ふしょく.md` の内容を照合し、効果・特性の仕様・備考のすべての記述が一致していることを確認した）。

## 実装計画書
- `docs/plan/abilities/ふしょく.md` の「未対応・要確認」「実装フロー」「テスト観点」を、実際に見つかった `どくびし` の不具合と対処内容（`allow_type_immunity_bypass` パラメータの追加）に合わせて更新した。
- 「未対応・要確認」を「未対応・要確認（レビューで解消）」に改題し、`どくびし` の `source` 参照が引き起こしていた具体的な不具合内容と修正方針を明記した。
- 「実装フロー」を実装済みの内容に更新し、`allow_type_immunity_bypass` パラメータの追加を手順に追記した。
- 「テスト観点」に、`どくびし`・`どくどくだま`・タイプ無効による追加効果不発動・相手の `どくのトゲ` に関する境界ケースを追記した。

## 実装（handlers/ / data/）
- **バグ修正**: `src/jpoke/handlers/field.py` の `どくびし_apply_poison()` が `battle.ailment_manager.apply(ctx.source, ailment, source=ctx.source)` と、繰り出したポケモン自身を `source` として渡していたため、繰り出したポケモン自身が `ふしょく` 持ちのはがね/どくタイプ（テラスタル含む）だと `AilmentManager._can_apply_by_type()` が `source.ability.name == "ふしょく"` を True と誤判定し、本来毒にならないはずのどくびしで毒状態になってしまう不具合があった（一次情報「どくびし状態の場に、はがねタイプにテラスタルしたふしょくのポケモンを繰り出してもどく状態にはならない」に反する）。
  - 実際に `コイル`（でんき/はがね、ふしょく）をどくびし2層の場に繰り出すスクリプトで再現を確認（修正前: もうどく付与、修正後: 付与されず）。
  - 対処として `src/jpoke/core/ailment_manager.py` の `AilmentManager.apply()` に `allow_type_immunity_bypass: bool = True` パラメータを追加し、`_can_apply_by_type()` に渡す `source` を `allow_type_immunity_bypass` が `False` の場合は `None` 扱いにするようにした（`ON_APPLY_AILMENT`/`ON_BEFORE_APPLY_AILMENT` に渡す `source` 自体は変更しないため、シンクロ等の `is_foe_target()` 判定には影響しない）。
  - `どくびし_apply_poison()` からの呼び出しのみ `allow_type_immunity_bypass=False` を明示的に渡すよう修正した。
  - `どくどくだま`（`handlers/item.py` の `_apply_ailment_from_item()`、`source=mon` で自分自身を渡す）は一次情報どおり「ふしょく持ちが自身のどくどくだまで、はがね/どくタイプでももうどくになる」が正しい挙動のため変更していない（`allow_type_immunity_bypass` は既定の `True` のまま）。
- その他の毒付与経路（`どくどく`・`どくづき`等の攻撃技追加効果、`なげつける`、`トーチカ`、`どくのトゲ`、`シンクロ`）は `source` の渡し方を確認し、いずれも一次情報どおりの挙動になっていることを確認した（修正不要）。

## リーサル計算
- 対象外（`docs/progress/ability.md` のリーサル実装列が `n/a`。ふしょくはダメージ量やHPに直接影響しないため対象外）。

## テスト
- `tests/test_field.py` に `test_どくびし_ふしょく持ちのはがねタイプにも効かない()` を追加し、修正したバグの再発を防止した。
- `tests/items/test_item_ta.py` に `test_どくどくだま_ふしょく持ちはどくタイプとはがねタイプにも発動する()` を追加し、`どくどくだま` 側は従来どおり `ふしょく` によるタイプ無効貫通が有効であることを固定した。
- `tests/abilities/test_ability_ha.py` に以下を追加した:
  - `test_ふしょく_どくどくではがねタイプにももうどくが入る()` — 直接API呼び出しだけでなく、実際の技（`どくどく`）経由でも `ふしょく` が機能することを確認するエンドツーエンドテスト。
  - `test_ふしょく_どく技がはがねタイプに無効化されると追加効果も発動しない()` — どく技自体がタイプ相性無効で無効化された場合、`ふしょく` 持ちでも追加効果のどく付与が発動しないことを確認。
  - `test_ふしょく_相手のどくのトゲには効果がない()` — 相手の特性 `どくのトゲ` による付与は `ふしょく` の効果対象外であることを確認。
- `python -m pytest tests/ -v` を実行し、5388 passed, 1 skipped で全件成功を確認（ログ: `.loop/test_logs/ふしょく.log`）。
