# げきりゅう レビュー結果

## 仕様書
- 変更なし（一次情報と照合し、内容・記載漏れなしを確認）

## 実装計画書
- 新規作成（`docs/plan/abilities/げきりゅう.md` が存在しなかったため、仕様書と既存実装をもとに新規作成した。
  進捗表では「計画書」列が `x` になっていたが実際にはファイルが存在しない不整合があった）

## 実装（handlers/ / data/）
- `src/jpoke/handlers/ability.py` の `しんりょくもうかげきりゅうむしのしらせ_modify_atk` から
  不要な重複ガード `ctx.move is not None` を削除した（`AttackContext.move` は必須フィールドで
  常に非 None であり、`_calc_final_attack` 内でも None チェックなしに使用されているため）。
- ロジック自体（HP×3 ≤ maxHP 判定、対応タイプ一致時に攻撃補正へ 6144/4096 倍を適用）は仕様書と一致しており、
  タイプが変わる技・そうでんによるタイプ変更にも `ctx.move.type` 経由で自然に追従することを確認した。

## リーサル計算
- 対象外（`docs/progress/ability.md` のリーサル実装列が `n/a`）

## テスト
- `tests/abilities/test_ability_ha.py` と `tests/abilities/test_ability__group.py` の両方に
  `test_ピンチ系特性_HP1_3以下で攻撃補正1_5倍`（しんりょく/もうか/げきりゅう/むしのしらせを検証）が
  完全に重複して定義されていたため、`test_ability__group.py` 側（複数の頭文字にまたがる特性をまとめる
  設計の "_group" ファイル）を正としてha.py側の重複を削除した。
- `python -m pytest tests/ -v` で全4828件パス（1件skip）を確認した。ログは
  `.loop/test_logs/げきりゅう.log` に保存。
