# カーリーヘアー レビュー結果

## 仕様書
- 変更なし。一次情報（docs/wiki/abilities/カーリーヘアー.html）と照合し、内容（直接攻撃を受けた相手のすばやさを1段階下げる、ぬめぬめと同一効果）に誤り・欠落なし。

## 実装計画書
- 変更なし。仕様書と一致しており、実装方針（`Event.ON_DAMAGE_HIT` / `subject_spec="defender:self"` / `is_contact_reaction`）に誤りなし。

## 実装（handlers/ / data/）
- `src/jpoke/handlers/ability.py` の `カーリーヘアー_lower_spd_on_contact` を修正した。
  - 特性発動ログ（`_announce_ability_triggered(battle, ctx.defender)`）の呼び出しが欠落しており、特性が発動しても特性バー表示・`ability.revealed` が立たないバグを修正（同一効果の `ぬめぬめ_lower_spd_on_contact` に合わせた）。
  - `AttackContext.attacker` は非 Optional 型のため不要だった `attacker is not None` ガードを削除し、`ぬめぬめ` 実装と同一の記述に統一した。
- `data/ability.py` の登録内容（`Event.ON_DAMAGE_HIT`, `subject_spec="defender:self"`, `mold_breaker_ignorable` フラグなし＝かたやぶりに無視されない）は仕様と一致しており変更なし。

## リーサル計算
- 対象外（`docs/progress/ability.md` のリーサル実装列が `n/a`。すばやさ変化のみでダメージ計算に影響しないため）。

## テスト
- `tests/abilities/test_ability_ka.py` の既存テストに `ability.revealed` の検証を追加した（修正した発動ログの欠落バグを検出できるように）。
  - `test_カーリーヘアー_直接攻撃で攻撃者のSが1段階下がる`: `defender.ability.revealed` が True になることを追加検証。
  - `test_カーリーヘアー_非接触技では発動しない`: `defender.ability.revealed` が False のままであることを追加検証。
- `python -m pytest tests/ -v` は 4738 passed, 1 skipped で全件成功（初回実行時に `くらいつく` 関連の3件が急所判定の乱数依存で偶発的に失敗したが、本修正とは無関係で再実行により再現せず全件成功を確認済み）。
