# ビビッドボディ レビュー結果

## 仕様書
- 変更なし。`docs/spec/abilities/ビビッドボディ.md` は一次情報（`docs/wiki/abilities/ビビッドボディ.html`）の
  「効果」「特性の仕様」「備考」節と一致しており、誤り・欠落はなかった。
  - 効果: この特性を持つポケモンが場にいる間、自分を含む味方全員は敵から優先度+1以上のわざを受け付けない。
  - 特性の仕様: じょおうのいげんと同じ効果（じょおうのいげん#特性の仕様を参照）。
  - 備考: ハギギシリの専用特性。同様の効果を持つ特性にじょおうのいげん/テイルアーマーがある。

## 実装計画書
- 変更なし。`docs/plan/abilities/ビビッドボディ.md` は仕様書と整合しており、
  じょおうのいげんと同一ハンドラ `じょおうのいげん_block_priority` を共有する方針・登録内容も実装と一致していた。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/ability.py` の `じょおうのいげん_block_priority`
  （じょおうのいげん/テイルアーマー/ビビッドボディが共有、`ctx.move.target != "foe"` で
  自己・味方対象技を除外済みのバグ修正版）は仕様（優先度+1以上の相手単体対象技のみ無効化）と一致していた。
- `src/jpoke/data/ability.py` の `ビビッドボディ` エントリは `flags={"mold_breaker_ignorable"}`、
  `Event.ON_TRY_MOVE_1` / `subject_spec="defender:self"` / `priority=40`（`docs/spec/turn.md` の
  `ON_TRY_MOVE_1` 表の「40 | じょおうのいげん: 優先度高い技失敗」と一致）で、
  `じょおうのいげん` / `テイルアーマー` のエントリと完全に一致していた。

## リーサル計算
- 対象外。`docs/progress/ability.md` の「リーサル実装」列が `n/a`
  （優先度技の無効化は技の発動可否に関わる効果でありダメージ・HP計算には影響しないため）。

## テスト
- 追加した内容（`tests/abilities/test_ability__group.py`、じょおうのいげん/テイルアーマー共通の
  パラメータ化テストに `ビビッドボディ` を追加）:
  - `test_先制技無効系_いたずらごころで優先度が上がった変化技も無効化する`
  - `test_先制技無効系_優先度ゼロの技は通る`
  - `test_先制技無効系_優先度プラスの技を無効化する`
  - `test_先制技無効系_自己対象の技は無効化されない`
  （`test_先制技無効系_かたやぶりで無効化される` は元々 `ビビッドボディ` を含んでいたため変更なし）
- `python -m pytest tests/ -v` は 5187 passed, 1 skipped（スキップは本タスクと無関係）で全テスト成功を確認した。
  ログ: `.loop/test_logs/ビビッドボディ.log`
- `tests/abilities/test_ability__group.py -k "先制技無効系"` を再実行し、15 passed で再現性も確認した。flaky なテストは見つからなかった。
