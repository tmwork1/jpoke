# かんそうはだ レビュー結果

## 仕様書
- 変更なし。`docs/spec/abilities/かんそうはだ.md` は既に存在し、一次情報（`docs/wiki/abilities/かんそうはだ.html`）の「効果」「特性の仕様」「備考」節の内容と一致していることを確認した。

## 実装計画書
- 変更なし。`docs/plan/abilities/かんそうはだ.md` は仕様書と整合しており、実装内容（`ON_BEFORE_APPLY_MOVE` でのみず技無効化+回復、`ON_CALC_DAMAGE_MODIFIER` でのほのお技威力補正、`ON_TURN_END` priority=30 での天候HP変化）と一致していることを確認した。`docs/spec/turn.md` の `Event.ON_TURN_END` 表でも priority=30 が「かんそうはだ」として明記されており整合している。

## 実装（handlers/ / data/）
- 変更なし。既存実装を確認：
  - `かんそうはだ_absorb_water`（`handlers/ability.py`）: `_apply_type_absorb` 共通ヘルパーを使用してみず技を無効化し、HPが減っていれば最大HPの1/4回復。`ちょすい`と同一ヘルパーを利用しており、対象外の技（自分自身・場対象）は無効化しない仕様と一致。
  - `かんそうはだ_modify_fire_damage`: ほのお技のダメージ倍率を5120/4096（5/4倍）に補正。
  - `かんそうはだ_change_hp_by_weather`: `battle.weather_for(mon)` を用いて天候による回復/ダメージを適用（ばんのうがさ・ノーてんき・エアロックは `weather_for` 内部の `ON_CHECK_WEATHER_IMMUNE` で自動的に無効化される）。`battle.modify_hp` 経由のためかいふくふうじによる回復ブロックも自動的に効く。
  - `data/ability.py` の `かんそうはだ` エントリは `flags={"mold_breaker_ignorable"}` を持ち、かたやぶり無効化仕様と整合。
  - `lethal_handlers` に `LethalEvent.ON_TURN_END: LethalHandler(l.かんそうはだ_weather_hp, subject="defender")` が既に登録されていることを確認した（後述）。

## リーサル計算
- 変更なし（既に実装済みであることを確認）。`docs/progress/ability.md` の当該行は「リーサル実装=-」「リーサルテスト=-」と記載されていたが、実際には `src/jpoke/handlers/lethal.py` の `かんそうはだ_weather_hp` が `あめうけざら_heal` / `アイスボディ_heal` と同じパターン（`battle.weather_for(ctx.defender)` で天候判定、`_heal` / `_damage` ヘルパーで反映）で既に実装されており、`data/ability.py` にも登録済みだった。仕様と照合し、あめ・おおあめ回復とはれ・おおひでりダメージの双方が正しく実装されていることを確認した。追加の実装は行わず、進捗表の記載を実態に合わせて修正した。

## テスト
- `tests/test_lethal.py` に `test_かんそうはだ_あめで回復` / `test_かんそうはだ_はれでダメージ` が既に存在することを確認した（リーサル計算の妥当性を確認済み）。
- `tests/abilities/test_ability_ka.py` には通常挙動のテストが存在しなかったため、以下を新規追加した：
  - `test_かんそうはだ_みず技を無効化してHPが減っていれば1_4回復`
  - `test_かんそうはだ_HP満タンならみず技無効化のみで回復しない`
  - `test_かんそうはだ_みず技はかたやぶりで無効化されず通常ダメージを受ける`
  - `test_かんそうはだ_ほのお技のダメージが5_4倍になる`
  - `test_かんそうはだ_あめ中はターン終了時に最大HPの1_8回復`
  - `test_かんそうはだ_はれ中はターン終了時に最大HPの1_8ダメージ`
  - `test_かんそうはだ_あめが止むターンでは回復しない`
  - `test_かんそうはだ_ばんのうがさ所持時は天候効果が発動しない`
- `python -m pytest tests/ -v` で全4764件（+1 skipped）がパスすることを確認した（ログ: `.loop/test_logs/かんそうはだ.log`）。
