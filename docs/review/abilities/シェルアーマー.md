# シェルアーマー レビュー結果

## 仕様書
- 変更なし。`docs/spec/abilities/シェルアーマー.md` は一次情報（wiki）の「カブトアーマーと同じ効果を持つ。カブトアーマー#特性の仕様を参照とする」という記述と一致しており、誤り・欠落は見当たらなかった。

## 実装計画書
- 新規作成。`docs/plan/abilities/シェルアーマー.md` が存在しなかったため、`docs/plan/abilities/カブトアーマー.md` を参照して新規作成した。
  実装方針はカブトアーマーの `カブトアーマー_block_crit` ハンドラをそのまま共用登録する内容とし、新規ハンドラを実装しない旨を明記した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/data/ability.py` の `シェルアーマー` エントリは `カブトアーマー` と同様に
  `flags={"mold_breaker_ignorable"}` を持ち、`Event.ON_MODIFY_CRITICAL_RATE` に `h.カブトアーマー_block_crit`
  （`subject_spec="defender:self"`）を登録しており、カブトアーマーと同一パターンで実装されていることを確認した。
  かたやぶり無効化フラグ・急所発生率を0にして `stop_event=True` を返す実装内容も仕様に合致している。

## リーサル計算
- 対象外。`docs/progress/ability.md` の「リーサル実装」列は `n/a`（急所無効化は最終ダメージのばらつきに影響するのみで、
  現行のリーサル計算モデルの対象外）のため、スキップした。

## テスト
- 追加。`tests/abilities/test_ability_sa.py` に `test_シェルアーマー_かたやぶりで無効化` と
  `test_シェルアーマー_急所に当たらない` を、`tests/abilities/test_ability_ka.py` のカブトアーマー用テストと
  同一パターンで新規追加した。
  `python -m pytest tests/ -v` で全4864件パス（1件skip、無関係）を確認した。
