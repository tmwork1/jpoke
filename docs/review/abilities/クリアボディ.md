# クリアボディ レビュー結果

## 仕様書
- 変更なし。`docs/spec/abilities/クリアボディ.md` は既に存在しており、一次情報（`docs/wiki/abilities/クリアボディ.html`）の「特性の仕様」節と照合した結果、内容は正確・網羅的だった。

## 実装計画書
- 修正した。`docs/plan/abilities/クリアボディ.md` が実装当時のまま古い形式で残っており、以下の誤りがあった：
  - 実装対象イベントが `ON_MODIFY_STAT` と記載されていたが、実際の登録は `ON_BEFORE_MODIFY_STAT`（`data/ability.py`）だった。
  - ハンドラ名が `クリアボディ_modify_stat` と記載されていたが、実際は `クリアボディ_block_stat_drop`（`_block_stat_drop_by_foe` 共有ヘルパー使用）だった。
  - テスト先が `tests/test_ability.py` と記載されていたが、実際は `tests/abilities/test_ability_ka.py`。
  - しろいけむり/メタルプロテクトとハンドラを共有している事実や、`mold_breaker_ignorable` フラグによるかたやぶり無視の仕組みが記載されていなかった。
  - 同時期にレビュー済みの `かいりきバサミ.md` / `しろいけむり.md` と同じ書式（更新日・仕様参照・実装ゴール・実装方針・変更候補・テスト観点）に揃えて全面的に書き直した。

## 実装（handlers/ / data/）
- 変更なし。`src/jpoke/handlers/ability.py` の `クリアボディ_block_stat_drop`（`_block_stat_drop_by_foe` 経由で相手由来・低下方向のみ除去）と、`src/jpoke/data/ability.py` の `クリアボディ` / `しろいけむり`（`mold_breaker_ignorable` フラグ付き）/ `メタルプロテクト`（フラグなし＝かたやぶり無効化されない）の登録内容は仕様書と一致していることを確認した。
  - `subject_spec="target:self"` は `ON_BEFORE_MODIFY_STAT` が発火する `EventContext`（`target`=ランクを変更されるポケモン）のロールと一致している。
  - `うのミサイル`（防御低下のみブロック対象）、`ルームサービス`（`source=mon` で自己低下扱いのため防げない）、`ビビリだま`（クリアボディでいかくが無効化されても発動）など周辺実装の呼び出し方も spec の記述通りだった。

## リーサル計算
- 対象外（`docs/progress/ability.md` のリーサル実装列は `n/a`）。

## テスト
- 変更なし。`tests/abilities/test_ability_ka.py` に既存の `test_クリアボディ_かたやぶりで無効` / `test_クリアボディ_能力低下を防ぐ` / `test_クリアボディ_自己低下は防げない` があり、他の技・特性・アイテムのテスト（`test_ability_yarawa.py` の わたげ、`test_item_ha.py` の ビビリだま、`test_move_ka.py` の クリアスモッグ、`test_move_a.py` の おきみやげ、`test_move_ha.py` の ひっくりかえす、`test_move_na.py` の のみこむ、`test_move_sa.py` の すてゼリフ、`test_move_ta.py` の ちからをすいとる）でも仕様に沿った相互作用が網羅的に検証されている。`python -m pytest tests/ -v` で全件（4828 passed, 1 skipped）を確認した。
