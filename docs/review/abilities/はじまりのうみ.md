# はじまりのうみ レビュー結果

## 仕様書
- 変更なし（一次情報 `docs/wiki/abilities/はじまりのうみ.html` と照合し、効果・特性の仕様・関連項目とも一致していることを確認した）

## 実装計画書
- 修正した内容:
  - 旧計画書は `ON_CHECK_MOVE` / `ON_CHECK_IMMUNE` / `ON_MODIFY_ACCURACY` などを特性側に個別実装する前提で
    書かれ、旧パス表記（`spec/ability/`, `spec/global_field/`）を使うなど実際の実装・ドキュメント構成と
    乖離していた。
  - 実装済みの設計（`_activate_weather` / `_deactivate_strong_weather` 共通ヘルパー経由での発動・解除、
    `ON_SWITCH_IN` / `ON_ABILITY_ENABLED` / `ON_SWITCH_OUT` / `ON_ABILITY_DISABLED` への接続、
    `data/field/weather.py` の `"おおあめ"` エントリへの委譲、turn.md Priority 100（`ON_SWITCH_IN`
    「はじまりのうみ（特性）」行）・`ON_TRY_MOVE_1` Priority 10（「おおあめ・おおひでりによる技失敗」行）
    の根拠）に合わせて全面的に書き直した（先行してレビュー済みの `おわりのだいち` 計画書と同構成に統一）。
  - おおあめは「あめ」の威力補正（みず1.5倍・ほのお0.5倍）のみを流用し、「あめ」のこおり無効効果
    （`あめ_prevent_freeze` 相当）は登録しない点を明記した（仕様書・`docs/spec/fields/おおあめ.md` の
    「こおり状態: こおり防止ハンドラは存在しない」と一致）。

## 実装（handlers/ / data/）
- 変更なし
  - `handlers/ability.py` の `はじまりのうみ_activate_weather` / `はじまりのうみ_deactivate_strong_weather`
    は、おわりのだいち・デルタストリームと同一パターンの共通ヘルパー（`_activate_weather` /
    `_deactivate_strong_weather`）を使用しており、仕様通り。
  - `data/ability.py` の `ON_SWITCH_IN` / `ON_ABILITY_ENABLED` / `ON_SWITCH_OUT` / `ON_ABILITY_DISABLED`
    への登録も同型特性と整合。
  - `data/field/weather.py` の `"おおあめ"` エントリ（あめ相当の威力補正の流用、`ON_TRY_MOVE_1`
    Priority 10 でのほのおタイプ技失敗）、`WEATHER_PRIORITY` による上書き制御も仕様通り。
    こおり無効ハンドラが登録されていない点も、おおあめの仕様（こおり防止効果なし）と一致している。

## リーサル計算
- 対象外（`docs/progress/ability.md` の「リーサル実装」列が `n/a`。`handlers/lethal.py` にも該当ハンドラなし）

## テスト
- 修正した内容:
  - `tests/abilities/test_ability__group.py` に、`おわりのだいち` にのみ存在していた強天候の上書き可否
    テスト（`test_おわりのだいち_らんきりゅうは上書きできない` / `test_おわりのだいち_らんきりゅう以外
    上書きする`）に対応する `はじまりのうみ` 版（`test_はじまりのうみ_らんきりゅうは上書きできない` /
    `test_はじまりのうみ_らんきりゅう以外上書きする`）を追加した。特性レベルでの上書き挙動確認が
    `おわりのだいち` にのみ存在し `はじまりのうみ` に欠けていたテストの過不足を解消した
    （天候システム自体の優先度制御は `tests/test_field.py` で `おおあめ` を対象に既にカバー済みだが、
    特性側の発動経路を通した回帰確認として追加）。
  - 登場時発動（`test_天候始動特性_登場時に発動`）・相手が同特性のとき退場しても解除されない・退場時解除
    の各テストは既存のまま仕様を満たしていることを確認した（変更なし）。
  - ほのおタイプ技失敗・あめ相当効果（威力補正）・こおり付与ありは、おおあめ天候自体のテストとして
    `tests/test_field.py`, `tests/moves_attack/`, `tests/moves_status/` 等に広くカバーされている
    （変更なし）。
  - `python -m pytest tests/ -v` で全件パス（5131 passed, 1 skipped）を確認した。再実行によるflaky
    テストは確認されなかった。
