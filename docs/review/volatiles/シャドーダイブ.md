# シャドーダイブ レビュー結果

## 仕様書

- 新規作成: `docs/spec/volatiles/シャドーダイブ.md`
- 発生条件（シャドーダイブ・ゴーストダイブの1ターン目）を記載
- 消滅条件（2ターン目完了 / 技失敗時）を記載
- 回避効果（ほとんどの技を回避・必中技も回避・特定条件で命中する場合）を記載
- まもる貫通（unprotectable ラベルによる実装）を記載
- 行動固定・交代禁止を記載
- スコープ外事項（フィールド効果回避・フリーフォール強制失敗・解除メッセージ・わたげ回避・ボール捕獲不可・おどりこ不発動）を明記

## 実装計画書

- 新規作成: `docs/plan/volatiles/シャドーダイブ.md`
- 実装済みハンドラ一覧（ON_CHECK_TRAPPED / ON_MODIFY_COMMAND_OPTIONS / ON_TRY_MOVE_1 / ON_HIT）を記載
- レビュー時の修正内容（unprotectable ラベル追加）を記載
- スコープ外事項を明記

## 実装（handlers/ / data/）

- `src/jpoke/data/move.py`: シャドーダイブの `labels` に `"unprotectable"` を追加した
  - 修正前: `labels=["contact"]`
  - 修正後: `labels=["contact", "unprotectable"]`
  - 根拠: 仕様「まもる系の状態を取り除いてから攻撃する」に対応するため
- `src/jpoke/handlers/volatile.py`: 変更なし
- `src/jpoke/data/volatile.py`: 変更なし

## テスト

- `tests/test_volatile.py` に以下のテストを追加:
  - `test_シャドーダイブ_まもる中の相手にダメージを与えられる`: まもる揮発状態の相手に対してシャドーダイブが貫通してダメージを与えることを確認
- `python scripts/sort_tests.py tests/test_volatile.py` でソート済み
- `python -m pytest tests/ -v` 全1856件パス確認
