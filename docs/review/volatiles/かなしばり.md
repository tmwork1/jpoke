# かなしばり レビュー結果

## 仕様書

- 新規作成: `docs/spec/volatiles/かなしばり.md`
  - 効果・付与条件・ターン数（第五世代以降: 4ターン固定）・消滅条件を記載
  - ターン処理の優先度（各イベントの priority）を記載
  - コマンド制限の詳細仕様（他の揮発状態との相互作用）を記載
  - Champions スコープ外事項を明記

## 実装計画書

- 新規作成: `docs/plan/volatiles/かなしばり.md`
  - 5つのハンドラ（can_apply / apply_volatile / modify_command_options / try_action / tick_volatile）を列挙
  - 各 subject_spec・priority の根拠を明記

## 実装（handlers/ / data/）

- 変更なし
- 確認済み事項:
  - `handlers/move_status.py`: `かなしばり_can_apply`（未行動・わるあがきチェック）、`かなしばり_apply_volatile_to_defender`（count=4）が正しく実装されている
  - `handlers/volatile.py`: `かなしばり_modify_command_options`（source:self）、`かなしばり_try_action`（attacker:self, priority=100）、`かなしばり_tick_volatile`（source:self, priority=110）が正しく実装されている
  - `data/move.py`: MoveHandler デフォルト subject_spec=attacker:self で正しく登録されている
  - `data/volatile.py`: 各イベントに正しい subject_spec・priority で登録されている

## テスト

- `tests/test_volatile.py` に1件追加:
  - `test_かなしばり_コマンド制限で他の技は選択可能`: 封じた技以外の技が引き続き選択可能であることを確認（既存テストが空リストで vacuously true になるのを補完）
- ソート済み (`scripts/sort_tests.py`)、全テスト通過（1847件）
