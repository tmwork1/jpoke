# いちゃもん レビュー結果

## 仕様書

- 新規作成: `docs/spec/volatiles/いちゃもん.md`
  - 効果（技を選択できなくなる）
  - 付与条件（技「いちゃもん」使用時）
  - 消滅条件（交代・メンタルハーブ、ターン経過では消えない）
  - 詳細仕様（わるあがき連続使用可、バトンタッチ引き継ぎなし）
  - Champions スコープ外（キョダイユウゲキ版・ダイマックス・アロマベール）

## 実装計画書

- 新規作成: `docs/plan/volatiles/いちゃもん.md`
  - ON_MODIFY_COMMAND_OPTIONS ハンドラ（priority=100, subject_spec="source:self"）
  - 消滅条件の処理（switch_out 時 remove_all_volatiles による自動解除）
  - テスト方針

## 実装（handlers/ / data/）

- 変更なし
- `handlers/volatile.py` の `いちゃもん_modify_command_options` は正しく実装済み
  - `.is_move_family` でコマンド判定、禁止技のみ除外、交代コマンドはそのまま通す
  - `subject_spec="source:self"` （EventContext）正しい
- `data/volatile.py` の登録も正しい（ON_MODIFY_COMMAND_OPTIONS, source:self, priority=100）

## テスト

- 追加したテスト（合計 5 件 → 旧 3 件 + 新 2 件）:
  - `test_いちゃもん_ターン経過で消えない`: ターン終了後もいちゃもん状態が持続することを確認
  - `test_いちゃもん_交代で解除される`: 交代後に状態が解除されることを確認
- `python scripts/sort_tests.py tests/test_volatile.py` で五十音順に並び替え済み
- `python scripts/generate_test_list.py` で `docs/test/test_volatile.md` を更新済み
- `python -m pytest tests/ -v` で全 1833 テストがパス
