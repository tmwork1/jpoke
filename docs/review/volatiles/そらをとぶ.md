# そらをとぶ レビュー結果

## 仕様書

- 新規作成（docs/spec/volatiles/そらをとぶ.md）
- 主な内容:
  - 発生条件: 技「そらをとぶ」「とびはねる」の1ターン目に発生
  - 消滅条件: 2ターン目攻撃完了時、または攻撃失敗時
  - 回避: ほとんどの技が命中しない。命中する技はかぜおこし・かみなり・たつまき・ぼうふう（第五世代以降）・うちおとすなど
  - 威力2倍: かぜおこし・たつまきは威力2倍（第三世代以降）
  - 行動固定・交代禁止（ゴーストタイプ除外）

## 実装計画書

- 新規作成（docs/plan/volatiles/そらをとぶ.md）
- 既存実装に対するレビューで追加した内容を計画書に記録:
  - ON_CALC_POWER_MODIFIER ハンドラ（かぜおこし・たつまきの威力2倍）
  - HIDDEN_MOVE_ALLOWED_MOVES へのぼうふう追加

## 実装（handlers/ / data/）

- handlers/volatile.py:
  - `HIDDEN_MOVE_ALLOWED_MOVES["そらをとぶ"]` に「ぼうふう」を追加（"かぜおこし", "たつまき", "かみなり", "ぼうふう" の4技に更新）
  - `そらをとぶ_boost_power` 関数を追加（かぜおこし・たつまきの威力2倍）
- data/volatile.py:
  - そらをとぶの VolatileData に `ON_CALC_POWER_MODIFIER` ハンドラ登録を追加（subject_spec="defender:self", priority=100）

## テスト

- 以下のテスト関数を追加（tests/test_volatile.py）:
  - `test_そらをとぶ_かぜおこしたつまきの威力が2倍になる`: かぜおこし・たつまきそれぞれでパラメトライズ
  - `test_そらをとぶ_ぼうふうが命中する`: ぼうふうがそらをとぶ状態に命中することを確認
- 追加後 `python scripts/sort_tests.py tests/test_volatile.py` でソート実施
- `python -m pytest tests/ -v` で全1859テストがパス
