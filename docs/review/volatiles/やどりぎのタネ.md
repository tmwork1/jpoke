# やどりぎのタネ レビュー結果

## 仕様書

**状態**: 新規作成（`docs/spec/volatiles/やどりぎのタネ.md`）

一次情報（`docs/wiki/volatiles/やどりぎのタネ_(状態変化).html`）を確認し、Champions仕様を整理して新規作成した。
主な確認事項：
- Ver.1.0.2は1/16と説明文に記載されていたがバグで実際は1/8、Ver.1.0.3で1/8に修正済み（現実装は1/8が正しい）
- くさタイプは技を無効化するが、状態変化後のタイプ変化では解除されない
- ヘドロえき持ちポケモンがやどりぎ状態の場合、回復側がダメージを受ける
- マジックガードでダメージを防ぐと回復も発生しない
- HPChangeReason="drain" を使用する（ヘドロえきのダメージ変換に必要）

## 実装計画書

**状態**: 新規作成（`docs/plan/volatiles/やどりぎのタネ.md`）

仕様書をもとに新規作成した。発見した実装不足（くさタイプ無効チェックの欠落）を記録した。

## 実装（handlers/ / data/）

**状態**: 修正あり

### 問題点

`handlers/volatile.py`の`やどりぎのタネ_drain_hp`と`data/volatile.py`の登録は正しかったが、
**くさタイプ無効チェックが未実装**だった。

- `handlers/move_status.py`に`やどりぎのタネ_can_apply`が存在しなかった
- `data/move.py`の`やどりぎのタネ`に`ON_BEFORE_APPLY_MOVE`ハンドラ登録がなかった

### 修正内容

1. `src/jpoke/handlers/move_status.py`に`やどりぎのタネ_can_apply`を追加（五十音順で`やどりぎのタネ_apply_volatile_to_defender`の直前）
   - くさタイプ判定して`False`を返して失敗させる
   - priority は docs/spec/turn.md より 130

2. `src/jpoke/data/move.py`の`やどりぎのタネ`定義に以下を追加:
   ```python
   Event.ON_BEFORE_APPLY_MOVE: h.MoveHandler(
       hs.やどりぎのタネ_can_apply,
       priority=130,
   ),
   ```

### 既存実装（変更なし）

- `やどりぎのタネ_drain_hp`（handlers/volatile.py）: 正しく実装されていた
  - `reason="drain"` を使用 → ヘドロえきが機能する
  - `r=-1/8` でダメージ → `ON_MODIFY_NON_MOVE_DAMAGE` 経由でマジックガードが機能する
  - ダメージ量（負値）を`-damage`（正値）として回復側に渡す実装が正しい
- `data/volatile.py`のやどりぎのタネ定義: subject_spec="source:self"、priority=80 は正しい

## テスト

**状態**: テスト追加

既存テスト2件は正しかった。以下3件のテストを追加した:

| テスト関数 | 確認内容 |
|-----------|---------|
| `test_やどりぎのタネ_くさタイプに失敗` | くさタイプのポケモンには技が失敗する |
| `test_やどりぎのタネ_マジックガードでダメージ無効` | マジックガード持ちはダメージなし・回復も発生しない |
| `test_やどりぎのタネ_ヘドロえきで回復がダメージに変換` | ヘドロえき持ちがやどりぎ状態のとき回復側がダメージを受ける |

`sort_tests.py`でソート後、全5件のやどりぎのタネテストが通過した（PASSED）。
