# トーチカ レビュー結果

## 仕様書

**変更あり**: `docs/spec/volatiles/トーチカ.md` を新規作成した。

- wikiの一次情報（`docs/wiki/volatiles/トーチカ.html`）をもとに作成
- タイプ: どく、優先度: +4、PP: 10、範囲: 自分
- 変化技も防ぐ（`protect_non_attack=True`、まもると同様）点を明記
- 直接攻撃者をどく状態にする仕様を記載
- ぼうごパット・ふかしのこぶし等の相互作用を記載
- priority根拠（turn.md の ON_TRY_MOVE_1 priority=100）を記載

## 実装計画書

**変更あり**: `docs/plan/volatiles/トーチカ.md` を新規作成した。

- 仕様書をもとに2つのハンドラの実装方針を記載
  1. `トーチカ_protect` (ON_TRY_MOVE_1, priority=100, defender:self)
  2. `トーチカ_remove_volatile` (ON_TURN_END, source:self)
- `protect_non_attack` はデフォルト（True）のため引数指定不要と明記
- 既に実装済みのため実装状況はすべてチェック済み

## 実装（handlers/ / data/）

**変更なし**: 実装は正しい。

### handlers/volatile.py

- `トーチカ_protect`: `_run_protect(battle, ctx, value, ailment_on_contact="どく")` を呼び出す
  - `protect_non_attack` はデフォルト（True）→ 変化技も防ぐ（wiki仕様通り）
  - `ailment_on_contact="どく"` → 直接攻撃者にどくを付与する
  - `subject_spec="defender:self"` (AttackContext) → 正しい
  - `priority=100` → turn.md の「まもる・キングシールド等による無効化」= priority 100 に一致
- `トーチカ_remove_volatile`: `remove_volatile(battle, ctx, value, volatile="トーチカ")` を呼び出す
  - `subject_spec="source:self"` (EventContext) → 正しい
  - priority 未指定（デフォルト）→ 他のまもる系と同様で正しい

### data/volatile.py

- `"トーチカ"` の登録: ON_TRY_MOVE_1 (h.トーチカ_protect, defender:self, priority=100) と ON_TURN_END (h.トーチカ_remove_volatile, source:self) が正しく登録されている
- 五十音順での配置（スレッドトラップの後、ニードルガードの前）も正しい

## テスト

**変更なし**: テストは正しく機能している。

### トーチカ固有テスト

- `test_トーチカ_接触技で毒を付与`: たいあたり（接触技）を防いだ攻撃者がどく状態になることを確認
- `test_トーチカ_非接触では毒にならない`: でんきショック（非接触技）を防いでもどく状態にならないことを確認

### まもる系共通テスト（パラメトリック）にトーチカが含まれている

- `test_まもる系_ターン終了で解除`: ターン終了でトーチカ状態が解除されることを確認
- `test_まもる系_攻撃技を無効化`: 攻撃技を無効化することを確認
- `test_まもる系_相手対象変化技への反応[トーチカ-True]`: 変化技も防ぐことを確認

### テスト実行結果

`python -m pytest tests/ -v` で全1867テストがパス（8.80s）。
