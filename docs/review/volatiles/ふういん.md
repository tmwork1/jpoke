# ふういん レビュー結果

## 仕様書

新規作成。`docs/spec/volatiles/ふういん.md` を一次情報（`docs/wiki/volatiles/ふういん_(状態変化).html`）をもとに作成した。

主な仕様:
- ふういん状態のポケモンが覚えている技と共通する技を相手が使えなくする
- 消滅条件: ふういん使用者がひんしまたは交代
- わるあがきは封じられない
- `ON_TRY_ACTION` priority=100 で処理

## 実装計画書

新規作成。`docs/plan/volatiles/ふういん.md` を仕様書をもとに作成した。

実装済み状態を反映: `ふううん_try_action` ハンドラ1件のみ。

## 実装（handlers/ / data/）

### handlers/volatile.py

`ふううん_try_action` の型アノテーションを修正:
- 修正前: `ctx: EventContext`
- 修正後: `ctx: AttackContext`（実際に `ctx.attacker`, `ctx.defender`, `ctx.move` を使用しているため）

コメントも「共有技」→「共通する技」に修正。

ロジック・subject_spec・priority はすべて正しい:
- `subject_spec="defender:self"`: ふういん状態のポケモンが defender のとき発動（attacker が技を使う時に封印照合）
- `priority=100`: turn.md の `ON_CHECK_ACTION` 優先度100 に合致

### data/volatile.py

変更なし。登録内容は正しい。

## テスト

既存テスト: 2件（共通技は使えない、非共通技は使える）

追加テスト: 1件
- `test_ふういん_交代で解除される`: ふういん状態のポケモンが交代するとふういん状態が解除され、相手は共通技を使えるようになることを確認

ソート・全テスト実行結果:
- ふういん関連テスト: 3件すべて PASSED
- 既存の失敗（マジックコート等）はふういんの変更と無関係
