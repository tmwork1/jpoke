# しおづけ レビュー結果

## 仕様書

- 新規作成: `docs/spec/volatiles/しおづけ.md`
  - Champions仕様のダメージレート（1/16 / 1/8）を明記
  - 付与条件・消滅条件・予防（りんぷん・おんみつマント）を記載
  - マジックガード特性による無効化の仕組みを説明
  - ターン処理優先度（Priority 100）を記載
  - みず・はがね両方持ちポケモン（エンペルト）の仕様を明記

## 実装計画書

- 新規作成: `docs/plan/volatiles/しおづけ.md`
  - ダメージ計算式・subject_spec・priority の根拠を明記
  - SV版（1/8・1/4）との混同に関する注意点を記載
  - テスト方針を記載

## 実装（handlers/ / data/）

- `src/jpoke/handlers/volatile.py` の `しおづけ_damage` を修正
  - 誤り: `r = -1/8`（SV レート）→ 正しい: `r = -1/16`（Champions レート）
  - みず・はがねタイプの場合は `r *= 2` で -1/8 になる（変更なし）
  - docstring に Champions 仕様の説明を追加
- `src/jpoke/data/volatile.py` は変更なし（priority=100 は正しい）

## テスト

- `tests/test_volatile.py` を修正・追加
  - `test_しおづけ` → `test_しおづけ_ターン終了時ダメージ` にリネームし docstring を充実
  - expected_frac の値を修正: ピカチュウ 8→16、みず/はがね/両方 4→8
  - 追加: `test_しおづけ_交代で解除` — 交代によるしおづけ解除を確認
  - 追加: `test_しおづけ_マジックガードでダメージ無効` — マジックガード特性によるダメージ無効を確認
  - 全 1850 テストがパス
