# あばれる レビュー結果

## 仕様書

- 新規作成 (`docs/spec/volatiles/あばれる.md`)
  - 付与条件: あばれる・げきりん・はなびらのまい・だいふんげきの命中時
  - 効果: 2〜3ターン間の強制技使用、終了後こんらん付与
  - 消滅条件（第五世代以降）: ターン経過・ねむり中断・ミス・無効化・ひんし・交代
  - こんらん付与特例: マイペース・ミストフィールド・すでにこんらん状態は付与されない（3世代以降）
  - ターン処理優先度: ON_DAMAGE_HIT priority=180、ON_TURN_END priority=150（ねむり中断）

## 実装計画書

- 新規作成 (`docs/plan/volatiles/あばれる.md`)
  - あばれる_apply（ON_HIT）: 初回命中時に揮発性状態を付与するハンドラ（未実装として記載）
  - あばれる_tick（ON_DAMAGE_HIT, priority=180）: 実装済みとして記載
  - 強制コマンド（ON_MODIFY_COMMAND_OPTIONS）: 実装済みとして記載
  - 未実装事項: ねむりによる中断、ミス・無効化による中断

## 実装（handlers/ / data/）

- 修正: `src/jpoke/handlers/move_attack.py` に `あばれる_apply` 関数を追加
  - 初回命中時（ON_HIT）にランダム 2〜3 ターンの揮発性状態を付与する
  - すでに volatile を持つ場合（2ターン目以降）はスキップする
  - 五十音順で `オーラぐるま_check_move_type` の前に配置
- 修正: `src/jpoke/data/move.py` の `あばれる` に `ON_HIT` ハンドラ登録を追加
  - `ha.あばれる_apply` を `Event.ON_HIT` に登録
- 変更なし: `src/jpoke/handlers/volatile.py` の `あばれる_tick`（正しく実装済み）
- 変更なし: `src/jpoke/data/volatile.py` の登録（`forced=True`、`ON_MODIFY_COMMAND_OPTIONS`、`ON_DAMAGE_HIT` priority=180）

## テスト

- 追加: `test_あばれる_技使用で揮発性状態付与` — 技使用（ON_HIT経由）で揮発性状態が付与されることを確認
- 追加: `test_あばれる_3ターンで解除されこんらんになる` — count=3 で3ターン後に解除されこんらん状態になることを確認
- `python scripts/sort_tests.py tests/test_volatile.py` によりテストを五十音順に並び替え済み
- `python scripts/generate_test_list.py` により `docs/test/` を更新済み
- `python -m pytest tests/ -v` で全 1829 件のテストがパスしたことを確認
