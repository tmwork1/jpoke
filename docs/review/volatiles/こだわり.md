# こだわり レビュー結果

## 仕様書

- 新規作成（`docs/spec/volatiles/こだわり.md`）
- wiki ページ（こだわりスカーフ・こだわりハチマキ・こだわりメガネ）から仕様を整理
- こだわり揮発状態の付与条件（ON_MOVE_END）・効果（コマンド制限）・消滅条件（交代）を記載
- こだわり系アイテムの固有効果（素早さ/物理/特殊×1.5）も一覧表に整理

## 実装計画書

- 新規作成（`docs/plan/volatiles/こだわり.md`）
- 揮発状態ハンドラ（volatile.py）とアイテムハンドラ（item.py）の実装方針を整理
- subject_spec 根拠（攻撃フェーズ: attacker:self、非攻撃フェーズ: source:self）を明記

## 実装（handlers/ / data/）

- 変更なし（実装は既に正しく実装済み）
- `handlers/volatile.py`: `こだわり_restrict_commands` — `restrict_commands` 共通関数を利用
- `data/volatile.py`: `ON_MODIFY_COMMAND_OPTIONS`、`subject_spec="source:self"` で正しく登録済み
- `handlers/item.py`: `こだわり_lock_move` — `ON_MOVE_END` で技使用後にこだわり付与（重複ガード付き）
- `handlers/item.py`: `こだわりスカーフ_boost_speed`、`こだわりハチマキ_boost_physical`、`こだわりメガネ_boost_special` — 各 1.5 倍補正
- `data/item.py`: 各こだわりアイテムの `ON_CALC_SPEED` / `ON_CALC_ATK_MODIFIER` / `ON_MOVE_END` 登録済み

## テスト

- 変更なし（既存テストが十分なカバレッジを持つことを確認）
- `tests/test_volatile.py`:
  - `test_こだわり`: こだわり状態で固定技のみ選択可能
  - `test_こだわり_交代で解除`: 交代でこだわり揮発状態が解除される
  - `test_こだわり_固定技がかなしばりで使えないとわるあがきになる`: かなしばりとの重複
  - `test_こだわり_固定技のPPが0だとわるあがきになる`: PP 切れ時のわるあがき
- `tests/test_item.py`:
  - `test_こだわりスカーフ_素早さ強化`: 素早さ×1.5 確認
  - `test_こだわり系_交代でロック解除` (parametrize): 3 アイテム全て
  - `test_こだわり系_技ロック` (parametrize): 技使用後のこだわり付与を確認
  - `test_こだわり系_火力補正` (parametrize): 物理/特殊の補正値確認
- 全 1848 テスト通過
