# バインド レビュー結果

## 仕様書

**ファイル**: `docs/spec/volatiles/バインド.md`

一次情報（`docs/wiki/volatiles/バインド.html`）と照合した結果、仕様書の内容は正確だった。

確認済み項目:
- ダメージ倍率: 第六世代以降（Champions含む）は最大HPの 1/8 → 正しい
- 継続ターン数: 4〜5ターン（第五世代以降）→ 正しい
- しめつけバンドによる 1/8 → 1/6 強化 → 正しい
- マジックガードによる無効化 → 正しい
- ゴーストタイプの交代可能（第六世代以降）→ 正しい
- 解除条件（ターン経過・発生源交代・こうそくスピン・みがわり）→ 正しい
- バトンタッチでは引き継がれない → 正しい

**変更なし**

## 実装計画書

**ファイル**: `docs/plan/volatiles/バインド.md`

仕様書と照合した結果、計画書の内容は正確だった。

確認済み項目:
- VolatileData の定義（ON_TURN_END / ON_CHECK_TRAPPED / ON_SWITCH_OUT）→ 正しい
- subject_spec の割り当て → 正しい
- Priority の根拠（docs/spec/turn.md の「バインドダメージ = Priority 100」）→ 正しい

**変更なし**

## 実装（handlers/ / data/）

**ファイル**:
- `src/jpoke/handlers/volatile.py`
- `src/jpoke/data/volatile.py`

照合結果:
- `バインド_damage`: tick後にhas_volatile確認、ダメージは `ON_MODIFY_BIND_DAMAGE` 経由 → 正しい
- `バインド_remove`: 発生源が交代時に相手のバインドを解除 → 正しい
- `data/volatile.py` の `"バインド"` 定義: 3つのイベントハンドラが正しく登録されている
  - `ON_TURN_END` priority=100 / subject_spec="source:self" → 正しい
  - `ON_CHECK_TRAPPED` / subject_spec="source:self" → 正しい
  - `ON_SWITCH_OUT` / subject_spec="source:foe" → 正しい
- 五十音順の並び（`のろい` の直後に `バインド`）→ 正しい

**変更なし**

## テスト

**ファイル**: `tests/test_volatile.py`

既存テスト確認:
- `test_バインド_ゴーストタイプは交代可能` ✓
- `test_バインド_ターン経過でダメージ` ✓
- `test_バインド_交代不可` ✓
- `test_バインド_発生源が交代すると解除` ✓

**追加したテスト**:
- `test_バインド_マジックガードでダメージ無効`: 仕様書に記載があったがテストが欠如していたため追加

全5件 PASSED を確認済み。
