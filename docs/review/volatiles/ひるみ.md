# ひるみ レビュー結果

## 仕様書

変更なし。`docs/spec/volatiles/ひるみ.md` は一次情報と整合しており正確。

- ON_TRY_ACTION priority=40（turn.md の ON_CHECK_ACTION 行と一致）
- ON_TURN_END での解除（priority=100）
- マイペースによる免疫・ふくつのこころによる追加効果も記載済み

## 実装計画書

変更なし。`docs/plan/volatiles/ひるみ.md` は仕様書と整合。

- ひるみ_block_action: subject_spec="attacker:self", priority=40
- ひるみ_remove_volatile: subject_spec="source:self", priority=100

## 実装（handlers/ / data/）

変更なし。仕様書・計画書と完全に一致。

- `handlers/volatile.py`: `ひるみ_block_action` は `ctx.attacker` を参照し `HandlerReturn(value=False, stop_event=True)` を返す。`AttackContext` で呼ばれるため `subject_spec="attacker:self"` と整合。
- `handlers/volatile.py`: `ひるみ_remove_volatile` は共通 `remove_volatile` を呼び出し、subject_spec="source:self" と整合。
- `data/volatile.py`: ひるみは五十音順（バインドとふういんの間）に正しく配置。priority も正確。

## テスト

テスト追加あり。

既存: `test_ひるみ_行動不能` — ひるみ状態で行動不能になることを確認（存在済み・正常）

追加: `test_ひるみ_ターン終了で解除` — ターン終了後にひるみ状態が解除されることを確認

全テスト実行結果: 1867 passed（6 failed は test_move_a.py の LogCode import 問題で既存不具合、ひるみと無関係）
