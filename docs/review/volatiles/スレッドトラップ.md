# スレッドトラップ レビュー結果

## 仕様書

- 新規作成（`docs/spec/volatiles/スレッドトラップ.md`）
  - 技基本情報（タイプ: むし、優先度: +4、PP: 10、範囲: 自分）
  - 攻撃技の無効化（変化技は防げない、`protect_non_attack=False`）
  - 素早さ（S）ランク1段階低下（直接攻撃時のみ）
  - ターン終了時に解除（count=1）
  - 連続使用による失敗率上昇
  - 関連インタラクション（ぼうごパット、ふかしのこぶし、まもる系共通）

## 実装計画書

- 新規作成（`docs/plan/volatiles/スレッドトラップ.md`）
  - ハンドラ3件の計画（_protect / _remove_volatile / 技ON_STATUS_HIT）
  - priority根拠を `docs/spec/turn.md` から明記
  - subject_spec の根拠を明記

## 実装（handlers/ / data/）

- 変更なし
  - `handlers/volatile.py`: `スレッドトラップ_protect` は `protect_non_attack=False` + `stats_change_on_contact={"S": -1}` で正しく実装済み
  - `handlers/volatile.py`: `スレッドトラップ_remove_volatile` は `remove_volatile(... volatile="スレッドトラップ")` で正しく実装済み
  - `data/volatile.py`: `ON_TRY_MOVE_1` (priority=100, subject_spec="defender:self") / `ON_TURN_END` で正しく登録済み
  - `handlers/move_status.py`: `スレッドトラップ_apply` で `apply_volatile_to_attacker` を呼び出し済み
    （2026-07-10再レビュー時点の関数名。旧名 `スレッドトラップ_apply_volatile_to_attacker` から
    キングシールド等と同じ命名規則 `<技名>_apply` に整理済み）
  - `data/move.py`: タイプ=むし、分類=変化、PP=10、優先度=+4、target=self で正しく登録済み

## テスト

- 変更なし（既存テストをソート後に全件パス確認）
  - `test_スレッドトラップ_接触技で素早さを下げる`: 接触技で S ランク -1 を確認
  - `test_スレッドトラップ_非接触では素早さが下がらない`: 非接触技で S ランク変化なしを確認
  - `test_まもる系_攻撃技を無効化` (parametrize): 攻撃技が無効化されることを確認
  - `test_まもる系_ターン終了で解除` (parametrize): ターン終了で揮発性状態が解除されることを確認
  - `test_まもる系_相手対象変化技への反応` (parametrize): `blocks_status=False` で変化技が貫通することを確認
  - 全1856件パス
