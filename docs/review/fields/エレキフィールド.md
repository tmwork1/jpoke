# エレキフィールド レビュー結果

## 仕様書

- 新規作成: `docs/spec/fields/エレキフィールド.md`
  - でんき技 1.3 倍（第八世代仕様）
  - 地面にいるポケモンへのねむり・ねむけ無効
  - 地面にいるポケモンのねむる失敗（HP 回復・状態異常解除なし）
  - 既存ねむりは回復されない
  - 浮いているポケモンは対象外

## 実装計画書

- 新規作成: `docs/plan/fields/エレキフィールド.md`
  - ハンドラ一覧（power_modifier / prevent_sleep / prevent_nemuke / tick_terrain）
  - ねむる失敗チェック追加方針（move_status.py の ねむる_check に追記）

## 実装（handlers/ / data/）

- `src/jpoke/handlers/move_status.py` の `ねむる_check` にエレキフィールド接地チェックを追加
  - 既存チェック（HP 満タン・すでにねむり）の後に、エレキフィールド下で接地している場合は MOVE_FAILED ログを出力して失敗させる
  - `ねむる_check_apply` は `ねむる_check` を呼ぶだけなので自動的に対応済み
- `src/jpoke/handlers/field.py`: 変更なし（エレキフィールド関連ハンドラは正常実装済み）
- `src/jpoke/data/field/terrain.py`: 変更なし（登録は正常）
- `python scripts/sort_handlers.py src/jpoke/handlers/move_status.py` を実行して五十音順を確認

## テスト

- `tests/test_field.py` に以下を追加:
  - `test_エレキフィールド_ねむる失敗`: 接地ポケモンのねむるが失敗し HP が回復されないことを確認
  - `test_エレキフィールド_非接地はねむる使用可能`: 浮遊ポケモンのねむるは正常動作することを確認
- `python scripts/sort_tests.py tests/test_field.py` でソート済み
- `python -m pytest tests/ -v` 全 2503 件 pass（skipped 1 件）
