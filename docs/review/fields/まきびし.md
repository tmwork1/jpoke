# まきびし レビュー結果

## 仕様書

- 新規作成（`docs/spec/fields/まきびし.md`）
- ダメージ量（1/8・1/6・1/4）、免疫条件（ひこうタイプ・ふゆう・でんじふゆう等）、発生・解除条件、優先度を記載

## 実装計画書

- 新規作成（`docs/plan/fields/まきびし.md`）
- 既存実装に合わせた内容を記録

## 実装（handlers/ / data/）

- 変更なし（実装は仕様書と一致していた）
  - `handlers/field.py`: `まきびし_damage` — is_hazard_immune / is_floating / 層数別ダメージ正しく実装済み
  - `handlers/move_status.py`: `まきびし_set_field` — 3層上限チェック（max_count=3）正しく実装済み
  - `data/field/side_field.py`: `max_count=3`、ON_SWITCH_IN ハンドラ、`subject_spec="source:self"` 正しく設定済み
  - `data/move.py`: type="じめん"、category="status"、pp=20、target="foe_side" 正しく設定済み

## テスト

- `test_まきびし_あつぞこブーツで無効` を追加（あつぞこブーツ持ちはダメージを受けない）
- `test_まきびし_マジックガードで無効` を追加（マジックガード持ちはダメージを受けない）
- `python scripts/sort_tests.py tests/test_field.py` でソート実施
- 全テスト 2537 passed, 0 failed, 1 skipped を確認
