# トリックルーム レビュー結果

## 仕様書

- 新規作成（`docs/spec/fields/トリックルーム.md`）
  - Champions 版の素早さ反転仕様（負数化による降順ソート）を明記
  - 優先度・あとだし・せんせいのツメ等の例外動作を記載
  - 再使用による即時解除条件を追加
  - ルームサービスはアイテム担当でフィールド仕様外と明記

## 実装計画書

- 新規作成（`docs/plan/fields/トリックルーム.md`）
  - `ON_CHECK_SPEED_REVERSE` と `ON_TURN_END` の2ハンドラ構成を記載
  - `トリックルーム_activate_global_field`（move_status.py）の役割も明記
  - テスト一覧に `test_トリックルーム_再使用で解除` を追加

## 実装（handlers/ / data/）

- 変更なし
  - `handlers/field.py`: `トリックルーム_reverse_speed`（`-value` を返す）は正しい
  - `handlers/field.py`: `トリックルーム_tick_global_field` は正しい
  - `data/field/global_field.py`: `DomainEvent.ON_CHECK_SPEED_REVERSE` と `Event.ON_TURN_END(priority=140)` の登録は正しい
  - `handlers/move_status.py`: `トリックルーム_activate_global_field`（発動中なら解除、未発動なら起動）は正しい
- 付帯修正: `core/move_executor.py` の `_execute_status_hit` に `self.move_success = False` を追加
  - `ON_STATUS_HIT` が False を返した場合（技失敗時）に `move_success` が True のまま残るバグを修正
  - これにより `test_フェアリーロック_発動済みは失敗する` が通るようになった（フェアリーロック側のバグ修正）

## テスト

- `test_トリックルーム_再使用で解除` を新規追加
  - トリックルーム発動中に再使用するとフィールドが解除される挙動を確認
- `python scripts/sort_tests.py tests/test_field.py` でソート実施
- `python scripts/generate_test_list.py` でテスト一覧更新
- 全 2512 件パス（スキップ 1 件）
