# あめ レビュー結果

## 仕様書

- 新規作成: `docs/spec/fields/あめ.md`
  - `docs/wiki/items/ばんのうがさ.html` の「詳細な仕様」セクションからばんのうがさ相互作用を確認
  - 効果（みず技1.5倍・ほのお技0.5倍）、ばんのうがさ防御側無効、カウントダウン priority、こおり防止なしを記載

## 実装計画書

- 新規作成: `docs/plan/fields/あめ.md`
  - `あめ_power_modifier` と `tick_weather` の実装内容、登録方法、テスト観点を記載

## 実装（handlers/ / data/）

- 変更なし
- `handlers/field.py` の `あめ_power_modifier` は仕様書と一致
  - subject_spec="attacker:self" ✓（AttackContext のため正しい）
  - `battle.weather_for(ctx.defender).name == ""` でばんのうがさ防御側チェック ✓
  - みず技: apply_fixed_modifier(value, 6144) = 1.5倍 ✓
  - ほのお技: apply_fixed_modifier(value, 2048) = 0.5倍 ✓
- `data/field.py` の `"あめ"` FieldData 登録は仕様書と一致
  - ON_CALC_POWER_MODIFIER: subject_spec="attacker:self" ✓
  - ON_TURN_END: subject_spec="source:self", priority=10 ✓（turn.md Priority 10 と一致）

## テスト

- `test_あめはれ_ばんのうがさ防御側は補正なし` を新規追加
  - あめ時みず技補正なし（ばんのうがさ防御側）✓
  - あめ時ほのお技補正なし（ばんのうがさ防御側）✓
  - はれ時ほのお技補正なし（ばんのうがさ防御側）✓
  - はれ時みず技補正なし（ばんのうがさ防御側）✓
- `python scripts/sort_tests.py tests/test_field.py` 実行（62 関数ソート済み）
- `python scripts/generate_test_list.py` 実行して `docs/tests/` を更新
- 全 1896 テスト通過確認
