# いやしのねがい レビュー結果

## 仕様書

- 新規作成: `docs/spec/fields/いやしのねがい.md`
  - 技基本情報（タイプ: エスパー、分類: 変化、PP: 10）
  - 使用者をひんしにして自陣サイドフィールドを設置する効果を記述
  - 第八世代以降の特殊ルール（HP満タン・状態異常なしなら保留）を明記
  - こんらん等の揮発性状態（volatile）は回復対象外と明記
  - 発動優先度（ON_SWITCH_IN: Priority 100）を docs/spec/turn.md に基づき記載

## 実装計画書

- 新規作成: `docs/plan/fields/いやしのねがい.md`
  - ハンドラ 2 本（いやしのねがい_faint_and_set_side_field / いやしのねがい_heal_on_switch_in）の仕様を記述
  - subject_spec・priority・ロジックを明記

## 実装（handlers/ / data/）

- 変更なし
- 実装の正当性を確認:
  - `handlers/move_status.py`: `いやしのねがい_faint_and_set_side_field` — `side.activate("いやしのねがい", 1)` → `battle.faint(mon)` の順序は正しい
  - `handlers/field.py`: `いやしのねがい_heal_on_switch_in` — HP満タン・状態異常なし両方を満たす場合のみ保留（第八世代以降仕様）、それ以外は HP 全回復・ailment 解除・フィールド解除
  - `data/field/side_field.py`: ON_SWITCH_IN ハンドラのみ（ON_TURN_END なし＝ターン経過では解除されない）、priority=100 は仕様通り
  - `data/move.py`: `Event.ON_STATUS_HIT` で登録、flags={"heal"} は正しい

## テスト

- 追加: `tests/test_field.py` に `test_いやしのねがい_HP減少ポケモンの交代で全回復しフィールドが解除される`
  - HP が減少しているが状態異常のないポケモンが交代で場に出たとき、HP が全回復してフィールドが解除されることを確認
  - `side0={"いやしのねがい": 1}` を使って直接フィールドを設置するパターン（移行技を使わない単体テスト）
- 既存テスト `tests/moves_status/test_move_a.py` に 4 件（使用者ひんし＋フィールド設置、HP全回復・状態異常解除、HP満タン・状態異常なし保留、HP満タン・状態異常あり解除）が存在し正常動作を確認
- 全テスト 2522 件パス
