# ねばねばネット レビュー結果

## 仕様書
- 新規作成（`docs/spec/fields/ねばねばネット.md`）
- 効果・免疫・特性相互作用（たんじゅん/あまのじゃく/かちき/まけんき/ミラーアーマー）・しろいハーブ連携・じゅうりょくとの相互作用・発生/解除条件を記述

## 実装計画書
- 新規作成（`docs/plan/fields/ねばねばネット.md`）
- 技データ・フィールドデータ・ハンドラ実装方針・テスト計画を記述

## 実装（handlers/ / data/）
- 変更なし
- `handlers/field.py` の `ねばねばネット_speed_drop` は仕様と一致している
  - `is_hazard_immune` でブーツ免疫を処理
  - `is_floating` で浮遊チェックを処理
  - `modify_stats(..., source=foe)` でかちき/まけんき・たんじゅん・あまのじゃく等を処理
- `data/field/side_field.py` の登録は `subject_spec="source:self"` で正しい
- `handlers/move_status.py` の `ねばねばネット_set_side_field` は `side.activate("ねばねばネット", 1)` で設置・重複失敗を処理

## テスト
- 追加: `test_ねばねばネット_あつぞこブーツで無効` — あつぞこブーツ持ちはすばやさが下がらない
- 追加: `test_ねばねばネット_あまのじゃくは1段階上がる` — あまのじゃく持ちはすばやさ+1
- 追加: `test_ねばねばネット_たんじゅんは2段階下がる` — たんじゅん持ちはすばやさ-2
- 既存: `test_ねばねばネット`・`test_ねばねばネット_浮いているポケモンには効かない` は変更なし
- 全5件パス（既存2件の失敗 `いかりのまえば` はねばねばネットと無関係）
