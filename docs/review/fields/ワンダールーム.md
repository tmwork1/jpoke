# ワンダールーム レビュー結果

## 仕様書

- 新規作成（`docs/spec/fields/ワンダールーム.md`）
- 一次情報 `docs/wiki/global_fields/ワンダールーム_(場の状態).html` をもとに作成
- 主な仕様: 5ターン間、物理技受時に特防実数値+防御ランク・特殊技受時に防御実数値+特防ランクでダメージ計算
- 再使用による即時解除
- 実装対象外の複雑な相互作用（サイコショック系、せいなるつるぎ、てんねん特性等）を明記

## 実装計画書

- 新規作成（`docs/plan/fields/ワンダールーム.md`）
- handlers/field.py の3ハンドラ（def_rank_modifier, def_modifier, tick）と move_status.py の activate/deactivate ハンドラを整理
- subject_spec は ON_CALC_DEF_RANK_MODIFIER / ON_CALC_DEF_MODIFIER が `defender:self`、ON_TURN_END が `source:self`（仕様に準拠）
- priority 140 は `docs/spec/turn.md` 準拠

## 実装（handlers/ / data/）

- 変更なし
- `handlers/field.py`: `ワンダールーム_def_modifier`・`ワンダールーム_def_rank_modifier`・`ワンダールーム_tick_global_field` は正しく実装済み
- `handlers/move_status.py`: `ワンダールーム_activate_global_field` で発動中の再使用時に即時解除されることを確認
- `data/field/global_field.py`: subject_spec が `defender:self`（攻撃コンテキスト）で正しく設定済み
- `data/move.py`: ON_STATUS_HIT で正しく登録済み

## テスト

- `test_ワンダールーム_再使用で解除` を新規追加（`tests/test_field.py`）
- 既存テスト4件（物理/特殊の実数値・ランク参照）+ カウントダウンテストは問題なし
- `python scripts/sort_tests.py tests/test_field.py` 実行済み
- `python scripts/generate_test_list.py` 実行済み
- `python -m pytest tests/ -v` で全 2517 件パス（1 skipped）
