# 設計書 - 難易度優先アプローチ版

**作成日**: 2026年2月7日  
**最終更新**: 2026年2月8日  
**対象**: Phase 1-4 実装計画（[docs/plan/00_実装計画_統合版.md](../plan/00_実装計画_統合版.md) に基づく）  
**期間**: 2026年2月～6月（5ヶ月）  
**戦略**: 難易度S→A→B→Cの順で実装（難しいものから攻略）  
**目標実装率**: 18% → **50%**

---

## 1. 現況（2026年2月8日時点）

### 1.1 実装済みシステム（✅ 動作確認済み）

| システム | 内容 | 実装ファイル | テスト | 完成度 |
|---------|------|---------|--------|--------|
| **イベント駆動** | EventManager、Handler派生クラス | `core/event.py` | ✅ | 100% |
| **ダメージ計算** | 4096基準、タイプ相性、ランク補正 | `core/damage.py` | ✅ | 100% |
| **急所判定** | ランク計算、確率判定、1.5倍ダメージ | `core/damage.py` | 5/5 Pass | 100% |
| **優先度システム** | -7～+5範囲、素早さ統合 | `core/turn_controller.py` | ✅ | 100% |
| **命中判定** | ランク補正・天候補正統合 | `handlers/move.py` | 11/11 Pass | 100% |
| **HP吸収・反動** | apply_hp_drain / apply_recoil | `handlers/move.py` | 8/8 Pass | 100% |
| **状態異常** | 6種（まひ、どく、やけど等） | `handlers/ailment.py` | ✅ | 100% |
| **揮発性状態** | 48/50種 | `handlers/volatile.py` | ✅ | 96% |
| **フィールド管理** | 天候・地形・場の状態 | `handlers/field.py` | ✅ | 100% |

**現在の実装率**: 
- 特性: 17/304 (5.6%)
- アイテム: 56/154 (36.4%)
- 技: 129/691 (18.7%)
- **全体**: 約18%

**基盤システム**: ✅ **完成**（難易度S実装の準備完了）

---

## 2. 難易度優先アプローチの設計思想

### 2.1 なぜ難しいものから実装するのか

| 理由 | 具体的メリット | Phase 4での効果 |
|------|---------------|----------------|
| **アーキテクチャの早期検証** | 複雑な機能で設計の問題を発見 | Phase 4で大規模リファクタ不要 |
| **技術的負債の回避** | 後回しにすると設計変更が困難 | 既存パターンが確立済み |
| **学習曲線の最大化** | 難しい実装から得た知見を活用 | 実装速度が10倍以上に向上 |
| **実装パターンの確立** | Phase 1-3で定型化 | コピペで大量実装可能 |

### 2.2 Phase別の技術的焦点

#### Phase 1（難易度S）: システム基盤の拡張

**焦点**: 既存システムでは実現不可能な機能の追加

| 機能 | 技術課題 | 期待成果 |
|------|---------|---------|
| **フォルム管理システム** | Pokemon.forms, change_form() | 動的ステータス変更基盤 |
| **技固定ロジック** | Pokemon.locked_move | 技選択制限フレームワーク |
| **動的能力変更** | 特性・タイプの実行時変更 | 柔軟な能力管理 |
| **グローバル制御** | かがくへんかガス等の全体影響 | 広域効果フレームワーク |

#### Phase 2（難易度A）: 複雑計算ロジック

**焦点**: 既存ダメージ計算の拡張

| 機能 | 技術課題 | 期待成果 |
|------|---------|---------|
| **体重依存ダメージ** | Pokemon.weight属性追加 | 動的威力計算パターン |
| **ターン内状態管理** | 被ダメージフラグ、行動順序記録 | 条件判定フレームワーク |
| **動的タイプ変更** | ウェザーボール等 | 実行時タイプ変更 |
| **溜め技フレームワーク** | 揮発性状態での2ターン技 | 複数ターン処理 |

#### Phase 3（難易度B）: パターン適用

**焦点**: Phase 1-2で確立したパターンの適用

#### Phase 4（難易度C）: 大量実装

**焦点**: 確立済みパターンのコピペ展開

### 2.3 リスク管理

| リスク項目 | Phase | 確率 | 影響 | 緩和策 |
|-----------|-------|------|------|--------|
| **フォルム管理の設計ミス** | 1 | 🟠 中 | 🔴 極高 | Phase 1開始前に設計レビュー必須 |
| **Phase 1が想定以上に困難** | 1 | 🟠 中 | 🔴 高 | Phase 1を2ヶ月に延長可、Phase 4削減 |
| **複雑計算の正確性** | 2 | 🟡 低 | 🟠 中 | テストケース充実、仕様書確認 |
| **リサーチ資料不足** | 全体 | 🟡 低 | 🟠 中 | Phase 1は資料ある要素から着手 |
| **モチベーション低下** | 全体 | 🟡 低 | 🟠 中 | Phase 1突破で達成感 |
| **工数見積もり誤差** | 全体 | 🟠 中 | 🟡 低 | Phase 4は調整弁として活用 |

---

## 3. 実装順序と依存関係

### 3.1 フェーズ1-3の実装順序

```
Week 1-2:【基盤構築】
├─ 命中判定システム ────────────┐
│  └─ ランク補正、天候補正      │
└─ ひるみシステム              │
   └─ ON_TRY_ACTION Priority 40 │
                                 ├─→ Week 2-3: 統合テスト
├─ HP吸収・反動ダメージ ────────┤
│  └─ ON_TURN_END / ON_HIT 拡張 │
│                                 │
Week 3-4:【特性実装 Phase 1】    │
├─ タイプ別威力強化 (23種) ────┐│
├─ 素早さ操作 (8種) ────────────┤├─→ Week 4-5: 特性統合テスト
├─ 状態異常付与 (4種) ──────────┤│
├─ 天候・フィールド (10種) ────┘│
│                                │
Week 5-6:【技・アイテム実装】    │
├─ 能力ランク変化技 (80技) ────┘
├─ 状態異常付与技 (60技)
├─ こだわり系アイテム (3種)

Optional (余力時):
Week 7-8: 特殊計算・一撃必殺・ダメージ軽減
```

### 3.2 イベント処理フロー（ターン内イベント順序）

```
ON_TURN_START (Priority 100～0)
  ↓
ON_SWITCH_IN (特性トリガー例: あめふらし)
  ↓
ON_TRY_MOVE (こだわり系アイテム判定)
  ↓ [技選択]
  ↓
ON_CALC_ACCURACY (命中率補正) ← ②命中判定ではここで補正確認
  ↓
[命中判定] → ③ひるみは「次ターンの行動キャンセル」として判定
  ↓ [不命中 or ひるみでスキップ]
  ↓
ON_CALC_POWER, ON_CALC_CRITICAL ← ①急所判定（フェーズ1-2完了）
  ↓
[ダメージ計算]
  ↓
ON_HIT (HP吸収・反動など) ← ④HP吸収・反動処理
  ↓
ON_TURN_END (ターン終了時の状態異常ダメージ等)
```

---

## 4. 詳細設計仕様

### 4.1 命中判定システム（Task 1, Week 1-2）

**実装ファイル**: `src/jpoke/core/move_executor.py`（新規メインロジック）

**命中判定の流れ**:
```
基本命中率 (0～100)
  ↓
ランク補正 [-6～+6]
  → 攻撃側 +ランク: ×(1 + rank/3)
  → 防御側 +ランク: ×(1 - rank/4)
  ↓
天候補正 [あめ, はれ, すなあらし等]
  → 例: あめ中なら かみなり/ぼうふう → 100%
  ↓
特性補正 [ロックオン, レーダー等]
  → 例: ロックオン使用後 → 次技 100%
  ↓
アイテム補正 [ワイドレンズ等]
  → ×1.1倍
  ↓
最終命中率 = max(30%, min(100%, 計算結果))
```

**実装パターン**:

```python
def calc_accuracy(
    base_accuracy: int,
    attacker_rank: int = 0,
    defender_rank: int = 0,
    weather: Optional[str] = None,
    ability_modifiers: Dict[str, float] = None,
) -> float:
    """
    Args:
        base_accuracy: 技の基本命中率 (0-100)
        attacker_rank: 攻撃側の命中率ランク調整 (-6 ~ +6)
        defender_rank: 防御側の回避率ランク調整 (-6 ~ +6)
        weather: 天候 ("rain", "sun", "hail", ...)
        ability_modifiers: 特性による補正倍率
    
    Returns:
        最終命中率 (0.0 ~ 1.0)
    """
    # ランク補正
    accuracy = base_accuracy
    accuracy *= (1 + attacker_rank / 3)  # 攻撃側
    accuracy *= (1 - defender_rank / 4)  # 防御側
    
    # 天候補正
    if weather == "rain" and move_name in ["thunder", "blizzard"]:
        accuracy = 100.0
    
    # 特性・アイテム補正
    if ability_modifiers:
        for mod in ability_modifiers.values():
            accuracy *= mod
    
    # 制約
    return max(30, min(100, accuracy)) / 100  # 0.0 ~ 1.0
```

**テスト対象**:
- 基本命中率（100%, 90%, 75%, 50%等）
- ランク補正（±6～±1）
- 天候補正（かみなり×雨 = 100%）
- 必中技（かげぬい等）
- 低命中呪文（30%技の検証）

**参照仕様**: [docs/spec/accuracy.md](../research/accuracy.md)

---

### 4.2 ひるみシステム（Task 2, Week 2）

**実装ファイル**: `src/jpoke/core/turn_controller.py`（TurnController 拡張）

**ひるみの概要**:
- 技が命中後「確率」で対象がひるみ判定
- ひるみ状態になると「次ターンの行動がキャンセル」される
- 優先度 +4 以上の技（まもる等）ではひるみしない

**実装箇所**:

```python
class TurnController:
    def execute_turn(self, actions: List[Action]) -> None:
        # 1. 行動順序決定（優先度で並び替え）
        sorted_actions = self.sort_actions(actions)
        
        for action in sorted_actions:
            pokemon = action.actor
            move = action.move
            
            # 2. ひるみ判定（前ターンで付与されていたら）
            if pokemon.volatile_status == "flinch":
                pokemon.volatile_status = None
                # 行動キャンセル → 次のアクションへ
                continue
            
            # 3. 技発動（命中判定 → ダメージ等）
            self.execute_move(pokemon, move, action.target)
            
            # 4. ひるみ確率判定（技の stun フラグで）
            if move.stun and move.hit_check(random_num):
                action.target.volatile_status = "flinch"
```

**ひるみ確率テーブル**（研究資料から）:
| 技名 | ひるみ確率 | 優先度 |
|------|----------|--------|
| いわなだれ | 30% | 0 |
| アイロンヘッド | 30% | 0 |
| ロックブラスト | 30% | 0 |
| ... (約34技) | - | - |

**特例**:
- 先制技（Priority ≥ 1）ではひるみしない
- せいしんりょく特性ではひるみしない

---

### 4.3 HP吸収・反動ダメージ（Task 3, Week 2）

**実装ファイル**: `src/jpoke/handlers/move.py`（ON_HIT イベントハンドラ）

**概要**:
- `ON_HIT` イベントで、技命中直後に HP 変化を処理
- 吸収率 (1/2, 1/4等) または反動率（1/3, 1/2等）を技定義に記載

**実装パターン**:

```python
# src/jpoke/data/move.py での技定義
moves_data = {
    "giga_drain": {
        "power": 75,
        "accuracy": 100,
        "type": "grass",
        "hp_drain": {"type": "absorb", "rate": 0.5},  # 50%吸収
    },
    "submission": {
        "power": 80,
        "accuracy": 100,
        "type": "fighting",
        "recoil": {"type": "damage", "rate": 1/4},  # 1/4反動
    },
}

# handlers/move.py でのハンドラ実装
class HPDrainHandler:
    event_name = "ON_HIT"
    priority = 20
    
    def handle(self, event_data):
        """HP吸収・反動ダメージを処理"""
        attacker = event_data["attacker"]
        damage = event_data["damage"]
        move = event_data["move"]
        
        if "hp_drain" in move.extra_effects:
            drain_effect = move.extra_effects["hp_drain"]
            if drain_effect["type"] == "absorb":
                recovery = int(damage * drain_effect["rate"])
                attacker.hp = min(attacker.hp + recovery, attacker.max_hp)
        
        if "recoil" in move.extra_effects:
            recoil_effect = move.extra_effects["recoil"]
            if recoil_effect["type"] == "damage":
                recoil_damage = max(1, int(damage * recoil_effect["rate"]))
                attacker.hp = max(0, attacker.hp - recoil_damage)
```

**対象技例**:
| 技名 | タイプ | 効果 |
|------|--------|------|
| きゅうけつ | 虫 | 50%吸収 |
| すてみタックル | ノーマル | 1/3反動 |
| つるぎのまい | ノーマル | （吸収なし） |
| ギガドレイン | 草 | 50%吸収 |

---

### 4.4 特性実装 Phase 1（Task 4, Week 3-4）

**実装ファイル**: `src/jpoke/handlers/ability.py`（新規 45 種）

**4つのグループ別実装**:

#### Group 1: タイプ別威力強化（23種, 2日）

```python
handlers_dict = {
    "adaptive_ability": {  # てきおうりょく
        "event": "ON_CALC_POWER_MODIFIER",
        "condition": lambda pokemon, move: move.type == pokemon.type1 or move.type == pokemon.type2,
        "effect": lambda power: power * 1.2,  # 1.2倍
    },
    "iron_fist": {  # てつのこぶし
        "event": "ON_CALC_POWER_MODIFIER",
        "condition": lambda pokemon, move: move.contact_type == "punch",
        "effect": lambda power: power * 1.2,
    },
    # ... (計23種)
}
```

**参照**: [docs/spec/abiity/ability_list.md](../research/abiity/ability_list.md)

#### Group 2: 素早さ操作（8種, 1.5日）

```python
handlers_dict = {
    "swift_swim": {  # すいすい
        "event": "ON_CALC_SPEED",
        "condition": lambda pokemon, weather: weather == "rain",
        "effect": lambda speed: speed * 2,  # 2倍
    },
    "sand_rush": {  # すなかき
        "event": "ON_CALC_SPEED",
        "condition": lambda pokemon, weather: weather == "sandstorm",
        "effect": lambda speed: speed * 2,
    },
    # ... (計8種)
}
```

#### Group 3: 状態異常付与（4種, 1日）

```python
handlers_dict = {
    "poison_point": {  # どくのトゲ
        "event": "ON_HIT",
        "condition": lambda battle, attacker, ...: random() < 0.3,
        "effect": lambda defender: afflict_status(defender, "poison"),
    },
    "static": {  # せいでんき
        "event": "ON_HIT",
        "condition": lambda ...: random() < 0.3,
        "effect": lambda defender: afflict_status(defender, "paralysis"),
    },
    # ... (計4種)
}
```

#### Group 4: 天候・フィールド設定（10種, 1.5日）

```python
handlers_dict = {
    "drizzle": {  # あめふらし
        "event": "ON_SWITCH_IN",
        "priority": 100,
        "effect": lambda field: set_weather(field, "rain"),
    },
    "drought": {  # ひでり
        "event": "ON_SWITCH_IN",
        "priority": 100,
        "effect": lambda field: set_weather(field, "sun"),
    },
    # ... (計10種)
}
```

---

### 4.5 技・アイテム実装（Task 5, Week 5-6）

#### Task 5.1: 能力ランク変化技（80技, 1.5日）

**既存システム活用**: `src/jpoke/handlers/move.py` の `modify_stat()` を拡張

```python
moves_data = {
    "swords_dance": {
        "power": None,
        "effect": {"type": "stat_change", "target": "self", "stat": "attack", "stage": 1},
    },
    "ice_beam": {
        "power": 90,
        "effect": {
            "type": "multi",
            "effects": [
                {"type": "stat_change", "target": "opponent", "stat": "speed", "stage": -1, "probability": 0.1},
            ]
        },
    },
    # ... (統計技 80技)
}
```

#### Task 5.2: 状態異常付与技（60技, 1日）

**状態異常ハンドラ統合**:
```python
moves_data = {
    "stun_spore": {
        "power": None,
        "accuracy": 75,
        "effect": {"type": "status_ailment", "ailment": "paralysis", "probability": 1.0},
    },
    "thunder_wave": {
        "power": None,
        "accuracy": 90,
        "effect": {"type": "status_ailment", "ailment": "paralysis", "probability": 1.0},
    },
    # ... (60技)
}
```

#### Task 5.3: こだわり系アイテム（3種, 1.5日）

**実装ファイル**: `src/jpoke/handlers/item.py`

```python
items_data = {
    # こだわりハチマキ (Choice Band)
    "choice_band": {
        "event": "ON_TRY_MOVE",
        "priority": 10,
        "effect": lambda pokemon, move: lock_move(pokemon, move) if not pokemon.locked_move else True,
    },
    # こだわりメガネ (Choice Specs)
    "choice_specs": {...},
    # こだわりスカーフ (Choice Scarf)
    "choice_scarf": {...},
}
```

**ロック機構**:
```python
class Pokemon:
    locked_move: Optional[Move] = None
    locked_turns: int = 0
    
    def try_move(self, move: Move) -> bool:
        if self.locked_move and self.locked_move != move:
            return False  # 異なる技は選択不可
        return True
```

---

## 5. イベント定義追加（フェーズ1-3で新規）

```python
# src/jpoke/utils/enums/event.py

class BattleEvent(Enum):
    # フェーズ1-2で追加済み
    ON_CALC_CRITICAL = "on_calc_critical"
    
    # フェーズ1-3で新規追加
    ON_CALC_ACCURACY = "on_calc_accuracy"       # 命中率補正
    ON_TRY_ACTION = "on_try_action"             # 行動実行前割り込み（ひるみ等）
    ON_CALC_POWER_MODIFIER = "on_calc_power"    # タイプ威力強化
    ON_CALC_SPEED = "on_calc_speed"             # 素早さ補正
    ON_HIT = "on_hit"                           # 技命中後（HP吸収等）
    ON_SWITCH_IN = "on_switch_in"               # 交代時（特性：あめふらし等）
    ON_TRY_MOVE = "on_try_move"                 # 技選択前（こだわり判定等）
```

---

## 6. テスト設計

### 6.1 テスト項目（単体テスト）

**命中判定テスト** (`tests/test_accuracy.py`):
```python
def test_accuracy_basic_100():
    """基本命中率 100%"""
    
def test_accuracy_rank_modifier():
    """ランク補正（±1～±6）"""
    
def test_accuracy_weather_correction():
    """天候補正（あめ時かみなり→100%）"""
    
def test_accuracy_must_hit_moves():
    """必中技リスト検証"""
```

**ひるみシステムテスト** (`tests/test_flinch.py`):
```python
def test_flinch_probability():
    """ひるみ確率（いわなだれ 30%等）"""
    
def test_flinch_action_cancel():
    """ひるみ時 行動キャンセル"""
    
def test_flinch_seishin_ryoku_negation():
    """せいしんりょく特性でひるみ無効"""
```

**特性テスト** (`tests/test_ability.py` 拡張):
```python
def test_ability_adaptive_power():
    """てきおうりょく: 威力1.2倍"""
    
def test_ability_swift_swim():
    """すいすい: 雨中で素早さ2倍"""
    
def test_ability_drizzle():
    """あめふらし: 交代時に雨設定"""
```

### 6.2 統合テスト

**バトルシーン統合テスト** (`tests/integration/test_battle_scenario.py`):
```python
def test_battle_scenario_rainy_weather():
    """
    雨天下での命中判定統合:
    - あめふらし特性で雨開始
    - かみなり: 基本命中 70% → 100%
    - ぼうふう: 基本命中 55% → 100%
    """
```

---

## 7. スケジュール詳細

| Week | 担当 | タスク | 産出物 | レビュア |
|------|------|--------|---------|----------|
| 1-2 | COD | 命中判定システム実装 | `move_executor.py`, テスト | TST, ARC |
| 2 | COD | ひるみ・HP吸収実装 | `turn_controller.py`, `handlers/move.py` | TST, ARC |
| 2-3 | TST | 基盤システム統合テスト | テストレポート | ARC |
| 3-4 | COD | 特性45種実装 | `handlers/ability.py` | TST |
| 4-5 | TST | 特性統合テスト | テストレポート | ARC |
| 5-6 | COD | 技・アイテム実装 | `data/move.py`, `handlers/item.py` | TST |
| 6-7 | TST | 全体統合テスト | 最終テストレポート | ARC |
| 7-8 | COD | 低優先タスク（余力） | オプション | - |

---

## 8. Architect チェックリスト

### 実装前確認

- [ ] 全8つのタスク（含む4つの低優先タスク）の技術的実現性を確認
- [ ] イベント定義（8つ）が既存システムと互換性を持つか検証
- [ ] 依存関係に循環参照がないか確認（命中→ひるみ→特性の順序）
- [ ] テスト設計が実装仕様をカバーしているか確認（命中・ひるみ・吸収・特性）
- [ ] 既存コードとの統合ポイント明確化（TurnController, DamageCalculator, EventSystem）
- [ ] リスク対応策（命中判定エッジケース、特性相互作用）が実行可能か検証
- [ ] スケジュール（8週間、27.5日工数）が現実的か判断
- [ ] Phase 1-4への低優先タスク延期ポリシーの合意

### 実装開始時サインオフ

- [ ] Coder が実装開始の準知可能か確認
- [ ] TST がテスト環境の準備完了を確認
- [ ] 仕様書と詳細設計が alignment している
- [ ] リスク・課題がプロジェクトマネージャーに報告済み

---

## 9. 参照ドキュメント

**計画書**:
- [docs/plan/00_実装計画_統合版.md](../plan/00_実装計画_統合版.md) - 実装計画（次の実装タスク一覧）
- [docs/review/20260207_実装状況レビューレポート.md](../review/20260207_実装状況レビューレポート.md) - 詳細分析

**仕様書・調査書**:
- [docs/spec/accuracy.md](../research/accuracy.md) - 命中判定仕様
- [docs/spec/abiity/ability_list.md](../research/abiity/ability_list.md) - 特性リスト
- [docs/spec/move/](../research/move/) - 技別分類
- [docs/spec/event.md](../research/event.md) - イベント処理フロー

**レガシー設計書**:
- [01_設計書_フェーズ1-2.md](01_設計書_フェーズ1-2.md) - フェーズ1-2の詳細（参考用）

---

**設計書作成**: 2026-02-07  
**Status**: Ready for Coder Implementation  
**Next Step**: Week 1 命中判定システム実装開始
