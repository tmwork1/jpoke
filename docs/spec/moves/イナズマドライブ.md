# イナズマドライブ（Electro Drift）仕様書

参照: docs/wiki/moves/イナズマドライブ.html（ポケモンWiki）

## 基本情報

* タイプ: でんき
* 分類: 特殊
* 威力: 100
* 命中: 100
* PP: 8（ミライドンの専用技。`src/jpoke/data/ps-champ-ja/moves.json` に本技自体は掲載されていないが、
  `docs/champions/changes_from_sv.md` の「全技PPを8・12・16・20の4段階に統一」ルールは
  専用技を除外していない。同じ本家PP5の技で move_list.txt に掲載がある `ソウルビート`
  （line 434、PP=8）が実際にこのルールの対象になっていることを確認できるため、
  同じ本家PP5の `イナズマドライブ` も8を採用する。なお `ソウルビート` の実装コードは
  現時点で `pp=5` のまま未修正であり、これは本技とは別の未レビュー技の既知の移行漏れである）
* 対象: 相手1体
* 優先度: 0
* 接触: あり（直接攻撃）
* フラグ: `contact`
* 専用技: ミライドンのみが習得可能

## 効果

* 効果がバツグン（タイプ相性が1倍を超える）の場合、ダメージが4/3倍になる。
  正確な補正率は5461/4096倍。

## 判定フラグ

* 急所: 通常
* 命中判定: あり（命中率100）
* 追加効果: なし
* まもる: 防がれる
* おうじゃのしるし: 第五世代以降は対象になる（本技はひるみ効果を持たないため実質無関係。
  一次情報の判定表に記載されているため参考として記載）
* マジックコート: 対象外（変化技ではないため）
* ゆびをふる: 選ばれない（一次情報の「技の仕様」に明記）

## イベント / 優先度

* 威力補正はダメージ計算内部の `Event.ON_CALC_POWER_MODIFIER` で行う。
  `docs/spec/turn.md` に本イベントは掲載されていないため、同種の効果抜群時威力ブースト技
  （`アクセルブレイク`）で採用済みの実装パターン・優先度（デフォルト）に合わせる。
* タイプ相性は `damage_calculator.calc_def_type_modifier(ctx)` の戻り値（4096が等倍）を
  参照し、4096を超える（=効果抜群）場合に `apply_fixed_modifier` で5461/4096倍を適用する。

## リーサル計算

* `docs/progress/move.md` の該当行は「リーサル実装: n/a」「リーサルテスト: n/a」であり、
  本レビューの対象範囲外（対象外）とする。

## 参照

* docs/progress/move.md（イナズマドライブ行の効果欄）
* src/jpoke/data/ps-champ-ja/moves.json（本技は非掲載。ミライドンの専用技のため）
* docs/champions/changes_from_sv.md（技のPP統一ルール）
* docs/spec/moves/アクセルブレイク.md 相当（かくとうタイプ版・同一の効果抜群時威力ブースト
  ハンドラ `効果抜群時威力ブースト` を共有。仕様書は未作成のため
  `src/jpoke/handlers/move_attack.py` の該当ハンドラのdocstringを参照）
